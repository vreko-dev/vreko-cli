#!/usr/bin/env node
import { Sentry, createSentryConfig } from './chunk-YPTTIXKC.js';
import { detectOrphans } from './chunk-F7GEJLP7.js';
import './chunk-ZIIRQODJ.js';
import { resolveWorkspaceRoot, generateClaudeIntegration, findWorkspaceRoot, createHooksCommand } from './chunk-AWWND52L.js';
import { cliState } from './chunk-GRMRYWYS.js';
export { cliState } from './chunk-GRMRYWYS.js';
import { isVrekoInitialized, getWorkspaceDir, getViolations, getProtectedFiles, appendVrekoJsonl, getCredentials, createGlobalDirectory, saveCredentials, clearCredentials, getWorkspaceVitals, saveProtectedFiles, getCurrentSession, endCurrentSession, isLoggedIn, getWorkspaceConfig, recordLearning, getLearnings, readVrekoJson, recordViolation, writeVrekoJson, getGlobalPath, getGlobalDir, saveCurrentSession, loadVrekoJsonl, getGlobalConfig, saveGlobalConfig, saveWorkspaceConfig } from './chunk-P4EZUU47.js';
import { connectToDaemon, isDaemonAvailable, withDaemon, withDaemonOptional, disconnectFromDaemon, isDaemonConnected, getDaemonClient } from './chunk-PMJIMMYS.js';
import { JsonRpcClientError, getDefaultSocketPath, createServiceClient, connectServiceClient, createSessionViaDaemon, endSessionViaDaemon, WorkspaceStatusOutput, isServiceRunning, VrekoLocalClient, readServicePid, isServiceHealthy, StartStatusOutput, getLogPath, formatDuration, formatBytes, getServiceSocketPath, getServicePidPath } from './chunk-IXUUBQB4.js';
import { createInitCommand } from './chunk-2YR4MS3U.js';
import { isInteractive, getRenderMode } from './chunk-H7773ONB.js';
import './chunk-DMXC2JTC.js';
import { isRedisAvailable, getCache, setCache } from './chunk-WD5F2AL4.js';
import { withBreaker, getCircuitBreakerState } from './chunk-GOYL3F4T.js';
import { createLogger, LogLevel, extractErrorCode, PIONEER_EVENTS, SENSITIVE_PATTERNS } from './chunk-U5TVNIXX.js';
import { generateId } from './chunk-KJWKY4L4.js';
import { normalizeMethodName, ProtocolErrorCode, LearningType } from './chunk-AHZGBIQG.js';
import { detectAIClients, getClientConfigPath, getClient, getVrekoMCPConfig, writeClientConfig, getServerKey, validateConfig, removeVrekoConfig, patchApiKeyInClientConfig, detectWorkspaceConfig, validateClientConfig, repairClientConfig, isCommandExecutable2, readClientConfig } from './chunk-MJVY2XUN.js';
import { __commonJS, __name, __require } from './chunk-EWOJGXRX.js';
import * as fs from 'fs';
import { existsSync, promises, readFileSync, mkdirSync, writeFileSync, statSync, realpathSync, readdirSync, rmSync, renameSync, unlinkSync, openSync, closeSync, watchFile, createWriteStream } from 'fs';
import * as path10 from 'path';
import path10__default, { join, relative, basename, dirname, resolve, normalize as normalize$1, isAbsolute, extname } from 'path';
import { config } from 'dotenv';
import { fileURLToPath } from 'url';
import { exec, spawn, execFile, execSync, spawnSync, execFileSync } from 'child_process';
import chalk2 from 'chalk';
import { Command, createCommand, Option } from 'commander';
import * as crypto2__default from 'crypto';
import crypto2__default__default, { createHash, randomUUID, randomBytes, createCipheriv, createDecipheriv, scryptSync, timingSafeEqual } from 'crypto';
import { createServer } from 'http';
import ora from 'ora';
import { z } from 'zod';
import * as fs3 from 'fs/promises';
import { unlink, readFile, access, mkdir, writeFile, chmod, constants, appendFile, readdir, stat, rename } from 'fs/promises';
import * as os from 'os';
import { homedir, hostname, platform, userInfo, release, arch } from 'os';
import Table from 'cli-table3';
import { execa, execaSync } from 'execa';
import 'log-update';
import * as clack3 from '@clack/prompts';
import 'boxen';
import { promisify } from 'util';
import { EventEmitter } from 'events';
import chokidar from 'chokidar';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';
import { createConnection } from 'net';
import ky, { HTTPError } from 'ky';
import QuickLRU from 'quick-lru';
import pino from 'pino';
import { nanoid } from 'nanoid';
import { ok, err } from 'neverthrow';
import pRetry, { AbortError } from 'p-retry';
import { minimatch } from 'minimatch';
import { gunzipSync, gzipSync, createGzip } from 'zlib';
import { glob } from 'glob';
import ts from 'typescript';
import { Server } from '@modelcontextprotocol/sdk/server/index.js';
import { isInitializeRequest, PingRequestSchema, ListToolsRequestSchema, CallToolRequestSchema } from '@modelcontextprotocol/sdk/types.js';
import { StreamableHTTPServerTransport } from '@modelcontextprotocol/sdk/server/streamableHttp.js';
import { WebStandardStreamableHTTPServerTransport } from '@modelcontextprotocol/sdk/server/webStandardStreamableHttp.js';
import { Listr } from 'listr2';
import Conf from 'conf';

process.env.VREKO_CLI='true';process.env.NODE_NO_WARNINGS='1';

// package.json
var require_package = __commonJS({
  "package.json"(exports, module) {
    module.exports = {
      name: "@vreko/cli",
      version: "3.1.6",
      description: "Vreko CLI \u2014 AI-aware developer intelligence for the command line",
      homepage: "https://vreko.dev",
      repository: {
        type: "git",
        url: "https://github.com/vreko-dev/vreko-cli.git"
      },
      bugs: {
        url: "https://github.com/vreko-dev/vreko-cli/issues"
      },
      license: "Apache-2.0",
      author: {
        name: "Vreko Team",
        url: "https://vreko.dev"
      },
      keywords: [
        "vreko",
        "ambient-codebase-intelligence",
        "codebase-intelligence",
        "ai-codebase-intelligence",
        "ai-coding-mistakes-prevention",
        "fragile-files",
        "cursor-companion",
        "claude-code-companion",
        "copilot-companion",
        "ai-agent-context",
        "cli",
        "snapshots",
        "intelligence"
      ],
      bin: {
        vreko: "dist/index.js",
        vr: "dist/index.js"
      },
      files: [
        "dist",
        "scripts",
        "README.md",
        "LICENSE"
      ],
      dependencies: {
        "@asteasolutions/zod-to-openapi": "catalog:",
        "@babel/parser": "catalog:",
        "@babel/traverse": "catalog:",
        "@better-auth/passkey": "catalog:",
        "@better-auth/sso": "catalog:",
        "@clack/prompts": "catalog:",
        "@electric-sql/pglite": "catalog:",
        "@hubspot/api-client": "catalog:",
        "@inkjs/ui": "catalog:",
        "@modelcontextprotocol/sdk": "catalog:",
        "@neondatabase/serverless": "catalog:",
        "@opentelemetry/exporter-metrics-otlp-http": "catalog:",
        "@opentelemetry/exporter-trace-otlp-http": "catalog:",
        "@opentelemetry/instrumentation-pino": "catalog:",
        "@opentelemetry/sdk-metrics": "catalog:",
        "@opentelemetry/sdk-trace-node": "catalog:",
        "@react-email/components": "catalog:",
        "@react-email/render": "catalog:",
        "@sentry/node": "catalog:",
        "@sindresorhus/slugify": "catalog:",
        "@typescript-eslint/parser": "catalog:",
        "@vreko/auth": "workspace:*",
        "@vreko/claims-ledger": "workspace:*",
        "@vreko/contracts": "workspace:*",
        "@vreko/intelligence": "workspace:*",
        "@vreko/local-service": "workspace:*",
        "@vreko/local-service-client": "workspace:*",
        "@vreko/mcp": "workspace:*",
        "@vreko/mcp-client": "workspace:*",
        "@vreko/mcp-config": "workspace:*",
        "@vreko/sentry-privacy": "workspace:*",
        "better-auth": "catalog:",
        boxen: "catalog:",
        chalk: "catalog:",
        chokidar: "catalog:",
        "cli-table3": "catalog:",
        commander: "catalog:",
        conf: "catalog:",
        cookie: "catalog:",
        dotenv: "catalog:",
        "drizzle-orm": "catalog:",
        "drizzle-zod": "catalog:",
        glob: "catalog:",
        minimatch: "catalog:",
        nanoid: "catalog:",
        neverthrow: "catalog:",
        "p-retry": "catalog:",
        pino: "catalog:",
        "pino-pretty": "catalog:",
        "better-sqlite3": "catalog:",
        "posthog-node": "catalog:",
        "quick-lru": "catalog:",
        typescript: "catalog:",
        eventemitter2: "catalog:",
        execa: "catalog:",
        ink: "catalog:",
        ky: "catalog:",
        listr2: "catalog:",
        "llm-guard": "^0.1.8",
        "log-update": "catalog:",
        opossum: "catalog:",
        ora: "catalog:",
        "oxc-parser": "catalog:",
        "p-queue": "catalog:",
        pg: "catalog:",
        "prom-client": "catalog:",
        react: "catalog:",
        redis: "catalog:",
        resend: "catalog:",
        stripe: "catalog:",
        "ts-pattern": "catalog:",
        zod: "catalog:"
      },
      optionalDependencies: {
        keytar: "7.9.0"
      },
      bundledDependencies: [
        "@vreko/auth",
        "@vreko/claims-ledger",
        "@vreko/contracts",
        "@vreko/intelligence",
        "@vreko/local-service",
        "@vreko/local-service-client",
        "@vreko/mcp",
        "@vreko/mcp-client",
        "@vreko/mcp-config",
        "@vreko/sentry-privacy"
      ],
      devDependencies: {
        "@types/react": "catalog:",
        "@vitest/coverage-v8": "catalog:",
        "ink-gradient": "^4.0.0",
        "ink-testing-library": "catalog:",
        "react-devtools-core": "catalog:",
        tsup: "catalog:",
        tsx: "catalog:",
        typescript: "catalog:",
        vitest: "catalog:"
      },
      scripts: {
        postinstall: "node scripts/postinstall.mjs",
        preuninstall: "node scripts/preuninstall.mjs",
        build: "tsup && tsc --build tsconfig.build.json --emitDeclarationOnly && node scripts/verify-jsx-transform.mjs",
        check: "biome check .",
        "check:banned-words": "tsx scripts/check-banned-words.ts",
        dev: "tsup --watch",
        format: "biome format --write .",
        lint: "biome lint .",
        "lint:fix": "biome lint --fix .",
        prepublishOnly: `node -e "if(!process.env.npm_execpath?.includes('pnpm')){console.error('\\x1b[31mERROR: Use pnpm publish, not npm publish (catalog: protocol requires pnpm)\\x1b[0m');process.exit(1)}" && pnpm run build && pnpm run test`,
        test: "vitest run",
        "test:coverage": "vitest run --coverage",
        "test:watch": "vitest",
        "type-check": "tsc -p tsconfig.json --noEmit",
        "publish:clean": "node scripts/publish-clean.mjs",
        "cli:install": "pnpm build && pnpm link --global && echo '\u2705 CLI installed globally. Run: vreko --help or vr --help'",
        "cli:uninstall": "pnpm unlink --global && echo '\u2705 CLI uninstalled globally'",
        "cli:reinstall": "pnpm run cli:uninstall && pnpm run cli:install",
        "mcp:configure": "node dist/index.js tools configure --yes",
        "mcp:configure:dev": "node dist/index.js tools configure --yes --dev",
        "mcp:status": "node dist/index.js tools configure --list"
      },
      publishConfig: {
        access: "public",
        registry: "https://registry.npmjs.org/"
      },
      exports: {
        ".": "./dist/index.js",
        "./services/vreko-dir": "./dist/services/vreko-dir.js"
      },
      type: "module",
      engines: {
        node: ">=20.9.0",
        npm: ">=8.0.0",
        pnpm: ">=8.0.0"
      }
    };
  }
});
process.env.VREKO_CLI = "true";
process.env.VREKO_CLI = "true";
function isRunningUnderDoppler() {
  return !!(process.env.DOPPLER_PROJECT || process.env.DOPPLER_CONFIG || process.env.DOPPLER_ENVIRONMENT || process.env.DOPPLER_TOKEN);
}
__name(isRunningUnderDoppler, "isRunningUnderDoppler");
function loadLocalEnv() {
  if (isRunningUnderDoppler()) {
    return;
  }
  const envPath = resolve(process.cwd(), ".env.local");
  if (!existsSync(envPath)) {
    return;
  }
  const result7 = config({
    path: envPath,
    quiet: true
  });
  if (result7.parsed && Object.keys(result7.parsed).length > 0) {
    Object.keys(result7.parsed).join(", ");
  }
}
__name(loadLocalEnv, "loadLocalEnv");
var CRITICAL_VARS = [
  "DATABASE_URL",
  "REDIS_URL",
  "BETTER_AUTH_SECRET"
];
function validateCriticalVars() {
  CRITICAL_VARS.filter((varName) => !process.env[varName]);
}
__name(validateCriticalVars, "validateCriticalVars");
loadLocalEnv();
validateCriticalVars();
var __defProp = Object.defineProperty;
var __name2 = /* @__PURE__ */ __name((target, value) => __defProp(target, "name", {
  value,
  configurable: true
}), "__name");
var WORKSPACE_ID_LENGTH = 12;
var WORKSPACE_ID_PATTERN = /^[a-f0-9]{12}$/;
function generateWorkspaceId(workspacePath) {
  if (!workspacePath || typeof workspacePath !== "string") {
    throw new Error(`Invalid workspace path: ${workspacePath}`);
  }
  const normalizedPath = workspacePath.replace(/\\/g, "/").toLowerCase();
  return createHash("sha256").update(normalizedPath).digest("hex").slice(0, WORKSPACE_ID_LENGTH);
}
__name(generateWorkspaceId, "generateWorkspaceId");
__name2(generateWorkspaceId, "generateWorkspaceId");
function isValidWorkspaceId(workspaceId) {
  if (typeof workspaceId !== "string") {
    return false;
  }
  return WORKSPACE_ID_PATTERN.test(workspaceId);
}
__name(isValidWorkspaceId, "isValidWorkspaceId");
__name2(isValidWorkspaceId, "isValidWorkspaceId");
function assertValidWorkspaceId(workspaceId, context) {
  if (!isValidWorkspaceId(workspaceId)) {
    const contextMsg = context ? ` in ${context}` : "";
    throw new Error(`Invalid workspace ID format${contextMsg}: expected 12 hex chars, got "${workspaceId}"`);
  }
}
__name(assertValidWorkspaceId, "assertValidWorkspaceId");
__name2(assertValidWorkspaceId, "assertValidWorkspaceId");
function generatePrefixedWorkspaceId(workspacePath) {
  const id = generateWorkspaceId(workspacePath);
  return `ws_${id}`;
}
__name(generatePrefixedWorkspaceId, "generatePrefixedWorkspaceId");
__name2(generatePrefixedWorkspaceId, "generatePrefixedWorkspaceId");
function extractWorkspaceId(prefixedId) {
  if (prefixedId.startsWith("ws_")) {
    return prefixedId.slice(3);
  }
  return prefixedId;
}
__name(extractWorkspaceId, "extractWorkspaceId");
__name2(extractWorkspaceId, "extractWorkspaceId");
(class _SafeWorkspaceId {
  static {
    __name(this, "_SafeWorkspaceId");
  }
  static {
    __name2(this, "SafeWorkspaceId");
  }
  value;
  constructor(value) {
    this.value = value;
  }
  /**
  * Create from raw string with validation
  */
  static fromString(value) {
    assertValidWorkspaceId(value, "SafeWorkspaceId.fromString");
    return new _SafeWorkspaceId(value);
  }
  /**
  * Generate from workspace path
  */
  static fromPath(workspacePath) {
    const id = generateWorkspaceId(workspacePath);
    return new _SafeWorkspaceId(id);
  }
  /**
  * Get raw string value
  */
  toString() {
    return this.value;
  }
  /**
  * Check equality with another workspace ID
  */
  equals(other) {
    return this.value === other.value;
  }
});
var SessionTokensSchema = z.object({
  accessToken: z.string(),
  refreshToken: z.string(),
  expiresAt: z.string(),
  sessionId: z.string()
});
var SessionUserInfoSchema = z.object({
  id: z.string(),
  email: z.string(),
  name: z.string().optional(),
  tier: z.enum([
    "free",
    "pro",
    "team",
    "enterprise"
  ]),
  organizationId: z.string().optional()
});
var WorkspaceSessionEntrySchema = z.object({
  workspacePath: z.string(),
  workspaceId: z.string(),
  session: SessionTokensSchema,
  user: SessionUserInfoSchema,
  authMethod: z.enum([
    "oauth",
    "device-code",
    "api-key",
    "global-fallback"
  ]),
  createdAt: z.string(),
  updatedAt: z.string(),
  lastUsedAt: z.string(),
  apiKey: z.string().optional()
});
var WorkspaceSessionRegistrySchema = z.object({
  version: z.literal(1),
  lastCleanupAt: z.string(),
  workspaces: z.record(z.string(), WorkspaceSessionEntrySchema),
  global: z.object({
    session: SessionTokensSchema,
    user: SessionUserInfoSchema,
    authMethod: z.enum([
      "oauth",
      "device-code",
      "api-key",
      "global-fallback"
    ]),
    apiKey: z.string().optional()
  }).optional()
});
var WorkspaceAuthError = class extends Error {
  static {
    __name(this, "WorkspaceAuthError");
  }
  code;
  workspaceId;
  constructor(message, code, workspaceId) {
    super(message);
    this.code = code;
    this.workspaceId = workspaceId;
    this.name = "WorkspaceAuthError";
  }
};
var WorkspaceAuthErrorCode;
(function(WorkspaceAuthErrorCode2) {
  WorkspaceAuthErrorCode2["SESSION_EXPIRED"] = "SESSION_EXPIRED";
  WorkspaceAuthErrorCode2["SESSION_REVOKED"] = "SESSION_REVOKED";
  WorkspaceAuthErrorCode2["REFRESH_FAILED"] = "REFRESH_FAILED";
  WorkspaceAuthErrorCode2["WORKSPACE_NOT_FOUND"] = "WORKSPACE_NOT_FOUND";
  WorkspaceAuthErrorCode2["INVALID_WORKSPACE"] = "INVALID_WORKSPACE";
  WorkspaceAuthErrorCode2["INVALID_CREDENTIALS"] = "INVALID_CREDENTIALS";
  WorkspaceAuthErrorCode2["NETWORK_ERROR"] = "NETWORK_ERROR";
  WorkspaceAuthErrorCode2["STORAGE_ERROR"] = "STORAGE_ERROR";
})(WorkspaceAuthErrorCode || (WorkspaceAuthErrorCode = {}));
function isSessionExpired(expiresAt, bufferSeconds = 60) {
  const expiry = new Date(expiresAt);
  const buffer = bufferSeconds * 1e3;
  return Date.now() > expiry.getTime() - buffer;
}
__name(isSessionExpired, "isSessionExpired");
function getExpiresIn(expiresAt) {
  const expiry = new Date(expiresAt);
  const remaining = expiry.getTime() - Date.now();
  return Math.max(0, Math.floor(remaining / 1e3));
}
__name(getExpiresIn, "getExpiresIn");
function validateRegistry(data) {
  const result7 = WorkspaceSessionRegistrySchema.safeParse(data);
  return result7.success ? result7.data : null;
}
__name(validateRegistry, "validateRegistry");
function createEmptyRegistry() {
  return {
    version: 1,
    lastCleanupAt: (/* @__PURE__ */ new Date()).toISOString(),
    workspaces: {}
  };
}
__name(createEmptyRegistry, "createEmptyRegistry");
function credentialsToWorkspaceSession(workspacePath, credentials, authMethod = "device-code") {
  const workspaceId = generateWorkspaceId(workspacePath);
  const now = (/* @__PURE__ */ new Date()).toISOString();
  return {
    workspacePath,
    workspaceId,
    session: {
      accessToken: credentials.accessToken,
      refreshToken: credentials.refreshToken || credentials.accessToken,
      expiresAt: credentials.expiresAt || new Date(Date.now() + 7 * 24 * 60 * 60 * 1e3).toISOString(),
      sessionId: `sess_${workspaceId}`
    },
    user: {
      id: credentials.email,
      email: credentials.email,
      tier: credentials.tier === "free" ? "free" : "pro"
    },
    authMethod,
    createdAt: now,
    updatedAt: now,
    lastUsedAt: now
  };
}
__name(credentialsToWorkspaceSession, "credentialsToWorkspaceSession");
function getGlobalDir2() {
  const home = homedir();
  if (!home || typeof home !== "string") {
    throw new Error("Unable to determine home directory. Please ensure HOME environment variable is set.");
  }
  return join(home, ".vreko");
}
__name(getGlobalDir2, "getGlobalDir");
var GLOBAL_DIR = getGlobalDir2();
var REGISTRY_FILE = join(GLOBAL_DIR, "workspaces.json");
var SERVICE_NAME = "vreko-cli-workspace";
async function readRegistry() {
  try {
    const content = await readFile(REGISTRY_FILE, "utf-8");
    const data = JSON.parse(content);
    const validated = validateRegistry(data);
    return validated || createEmptyRegistry();
  } catch {
    return createEmptyRegistry();
  }
}
__name(readRegistry, "readRegistry");
async function writeRegistry(registry) {
  await mkdir(dirname(REGISTRY_FILE), {
    recursive: true
  });
  await writeFile(REGISTRY_FILE, JSON.stringify(registry, null, 2), {
    mode: 384
  });
}
__name(writeRegistry, "writeRegistry");
async function createKeytarProvider() {
  try {
    const keytar = await import('keytar');
    return {
      name: "keytar",
      async isAvailable() {
        try {
          await keytar.getPassword("__vreko_test__", "__test__");
          return true;
        } catch {
          return false;
        }
      },
      async getPassword(service, account) {
        return keytar.getPassword(service, account);
      },
      async setPassword(service, account, password2) {
        await keytar.setPassword(service, account, password2);
      },
      async deletePassword(service, account) {
        return keytar.deletePassword(service, account);
      }
    };
  } catch {
    return null;
  }
}
__name(createKeytarProvider, "createKeytarProvider");
var ENCRYPTION_ALGORITHM = "aes-256-gcm";
var KEY_LENGTH = 32;
var IV_LENGTH = 12;
var AUTH_TAG_LENGTH = 16;
var SALT_LENGTH = 32;
function deriveMachineKey(salt) {
  const machineData = [
    hostname(),
    platform(),
    userInfo().username,
    homedir(),
    process.arch,
    process.platform
  ].join("|");
  return scryptSync(machineData, salt, KEY_LENGTH);
}
__name(deriveMachineKey, "deriveMachineKey");
function encryptData(data, salt) {
  const key = deriveMachineKey(salt);
  const iv = randomBytes(IV_LENGTH);
  const cipher = createCipheriv(ENCRYPTION_ALGORITHM, key, iv);
  const encrypted = Buffer.concat([
    cipher.update(data, "utf8"),
    cipher.final()
  ]);
  const authTag = cipher.getAuthTag();
  return Buffer.concat([
    salt,
    iv,
    authTag,
    encrypted
  ]);
}
__name(encryptData, "encryptData");
function decryptData(data) {
  const salt = data.subarray(0, SALT_LENGTH);
  const iv = data.subarray(SALT_LENGTH, SALT_LENGTH + IV_LENGTH);
  const authTag = data.subarray(SALT_LENGTH + IV_LENGTH, SALT_LENGTH + IV_LENGTH + AUTH_TAG_LENGTH);
  const encrypted = data.subarray(SALT_LENGTH + IV_LENGTH + AUTH_TAG_LENGTH);
  const key = deriveMachineKey(salt);
  const decipher = createDecipheriv(ENCRYPTION_ALGORITHM, key, iv);
  decipher.setAuthTag(authTag);
  const decrypted = Buffer.concat([
    decipher.update(encrypted),
    decipher.final()
  ]);
  return decrypted.toString("utf8");
}
__name(decryptData, "decryptData");
var ENCRYPTED_TOKENS_FILE = join(GLOBAL_DIR, "workspace-tokens.enc");
async function readEncryptedTokens() {
  try {
    const data = await readFile(ENCRYPTED_TOKENS_FILE);
    const decrypted = decryptData(data);
    return JSON.parse(decrypted);
  } catch {
    return {};
  }
}
__name(readEncryptedTokens, "readEncryptedTokens");
async function writeEncryptedTokens(tokens) {
  const salt = randomBytes(SALT_LENGTH);
  const encrypted = encryptData(JSON.stringify(tokens), salt);
  await mkdir(dirname(ENCRYPTED_TOKENS_FILE), {
    recursive: true
  });
  await writeFile(ENCRYPTED_TOKENS_FILE, encrypted, {
    mode: 384
  });
}
__name(writeEncryptedTokens, "writeEncryptedTokens");
var SecureTokenStorage = class SecureTokenStorage2 {
  static {
    __name(this, "SecureTokenStorage");
  }
  provider = null;
  initialized = false;
  async initialize() {
    if (this.initialized) {
      return;
    }
    const keytarProvider = await createKeytarProvider();
    if (keytarProvider && await keytarProvider.isAvailable()) {
      this.provider = keytarProvider;
    }
    this.initialized = true;
  }
  getAccountName(workspaceId) {
    return `tokens_${workspaceId}`;
  }
  async getTokens(workspaceId) {
    await this.initialize();
    if (this.provider) {
      const stored = await this.provider.getPassword(SERVICE_NAME, this.getAccountName(workspaceId));
      if (stored) {
        try {
          const parsed = JSON.parse(stored);
          const validated = SessionTokensSchema.safeParse(parsed);
          if (validated.success) {
            return validated.data;
          }
        } catch {
        }
      }
    }
    const tokens = await readEncryptedTokens();
    return tokens[workspaceId] || null;
  }
  async setTokens(workspaceId, tokens) {
    await this.initialize();
    if (this.provider) {
      await this.provider.setPassword(SERVICE_NAME, this.getAccountName(workspaceId), JSON.stringify(tokens));
      return;
    }
    const allTokens = await readEncryptedTokens();
    allTokens[workspaceId] = tokens;
    await writeEncryptedTokens(allTokens);
  }
  async deleteTokens(workspaceId) {
    await this.initialize();
    if (this.provider) {
      await this.provider.deletePassword(SERVICE_NAME, this.getAccountName(workspaceId));
    }
    const allTokens = await readEncryptedTokens();
    delete allTokens[workspaceId];
    await writeEncryptedTokens(allTokens);
  }
  async clearAllTokens() {
    await this.initialize();
    const registry = await readRegistry();
    const workspaceIds = Object.keys(registry.workspaces);
    if (this.provider) {
      for (const workspaceId of workspaceIds) {
        await this.provider.deletePassword(SERVICE_NAME, this.getAccountName(workspaceId));
      }
    }
    try {
      await unlink(ENCRYPTED_TOKENS_FILE);
    } catch {
    }
  }
};
var secureTokenStorage = null;
function getSecureTokenStorage() {
  if (!secureTokenStorage) {
    secureTokenStorage = new SecureTokenStorage();
  }
  return secureTokenStorage;
}
__name(getSecureTokenStorage, "getSecureTokenStorage");
async function storeWorkspaceSession(workspacePath, entry) {
  const workspaceId = generateWorkspaceId(workspacePath);
  const registry = await readRegistry();
  registry.workspaces[workspaceId] = {
    ...entry,
    workspaceId,
    workspacePath
  };
  await getSecureTokenStorage().setTokens(workspaceId, entry.session);
  await writeRegistry(registry);
}
__name(storeWorkspaceSession, "storeWorkspaceSession");
async function getWorkspaceSession(workspacePath) {
  const workspaceId = generateWorkspaceId(workspacePath);
  const registry = await readRegistry();
  const metadata = registry.workspaces[workspaceId];
  if (!metadata) {
    return null;
  }
  const tokens = await getSecureTokenStorage().getTokens(workspaceId);
  if (!tokens) {
    return null;
  }
  return {
    ...metadata,
    session: tokens
  };
}
__name(getWorkspaceSession, "getWorkspaceSession");
async function updateWorkspaceSessionTokens(workspacePath, tokens) {
  const workspaceId = generateWorkspaceId(workspacePath);
  const registry = await readRegistry();
  const metadata = registry.workspaces[workspaceId];
  if (!metadata) {
    throw new Error(`Workspace session not found: ${workspacePath}`);
  }
  await getSecureTokenStorage().setTokens(workspaceId, tokens);
  metadata.updatedAt = (/* @__PURE__ */ new Date()).toISOString();
  metadata.session = tokens;
  await writeRegistry(registry);
}
__name(updateWorkspaceSessionTokens, "updateWorkspaceSessionTokens");
async function touchWorkspaceSession(workspacePath) {
  const workspaceId = generateWorkspaceId(workspacePath);
  const registry = await readRegistry();
  const metadata = registry.workspaces[workspaceId];
  if (metadata) {
    metadata.lastUsedAt = (/* @__PURE__ */ new Date()).toISOString();
    await writeRegistry(registry);
  }
}
__name(touchWorkspaceSession, "touchWorkspaceSession");
async function deleteWorkspaceSession(workspacePath) {
  const workspaceId = generateWorkspaceId(workspacePath);
  const registry = await readRegistry();
  delete registry.workspaces[workspaceId];
  await writeRegistry(registry);
  await getSecureTokenStorage().deleteTokens(workspaceId);
}
__name(deleteWorkspaceSession, "deleteWorkspaceSession");
async function listWorkspaceSessions() {
  const registry = await readRegistry();
  return Object.values(registry.workspaces).map(({ session, ...metadata }) => ({
    ...metadata,
    expiresAt: session.expiresAt
  }));
}
__name(listWorkspaceSessions, "listWorkspaceSessions");
async function storeGlobalFallback(entry) {
  const registry = await readRegistry();
  registry.global = entry;
  await writeRegistry(registry);
}
__name(storeGlobalFallback, "storeGlobalFallback");
async function getGlobalFallback() {
  const registry = await readRegistry();
  return registry.global || null;
}
__name(getGlobalFallback, "getGlobalFallback");
async function cleanupExpiredSessions() {
  const registry = await readRegistry();
  const now = /* @__PURE__ */ new Date();
  let cleaned = 0;
  for (const [workspaceId, entry] of Object.entries(registry.workspaces)) {
    const expiresAt = new Date(entry.session.expiresAt);
    if (expiresAt < now) {
      delete registry.workspaces[workspaceId];
      await getSecureTokenStorage().deleteTokens(workspaceId);
      cleaned++;
    }
  }
  registry.lastCleanupAt = now.toISOString();
  await writeRegistry(registry);
  return cleaned;
}
__name(cleanupExpiredSessions, "cleanupExpiredSessions");

// ../../packages/auth/dist/workspace-session-manager.js
var _auth = null;
var _authPromise = null;
async function getAuth() {
  if (_auth) {
    return _auth;
  }
  if (_authPromise) {
    return _authPromise;
  }
  _authPromise = import('./auth-QDSN5C4X.js').then((m) => {
    _auth = m.auth;
    return _auth;
  });
  return _authPromise;
}
__name(getAuth, "getAuth");
var refreshFlights = /* @__PURE__ */ new Map();
var REFLIGHT_TIMEOUT_MS = 3e4;
async function withSingleFlight(key, fn) {
  const existing = refreshFlights.get(key);
  if (existing && Date.now() - existing.timestamp < REFLIGHT_TIMEOUT_MS) {
    return existing.promise;
  }
  const promise = fn().finally(() => {
    setTimeout(() => {
      refreshFlights.delete(key);
    }, 100);
  });
  refreshFlights.set(key, {
    promise,
    timestamp: Date.now()
  });
  return promise;
}
__name(withSingleFlight, "withSingleFlight");
async function refreshWithBetterAuth(refreshToken) {
  try {
    const auth = await getAuth();
    const api = auth.api;
    if (!api.refreshToken) {
      return null;
    }
    const result7 = await api.refreshToken({
      body: {
        refreshToken
      }
    });
    if (!result7?.token) {
      return null;
    }
    return {
      accessToken: result7.token,
      refreshToken: result7.refreshToken,
      expiresAt: new Date(Date.now() + result7.expiresIn * 1e3).toISOString(),
      sessionId: `sess_${Date.now()}`
    };
  } catch (_error) {
    return null;
  }
}
__name(refreshWithBetterAuth, "refreshWithBetterAuth");
var WorkspaceSessionManager = class {
  static {
    __name(this, "WorkspaceSessionManager");
  }
  options;
  constructor(options = {}) {
    this.options = {
      refreshBufferSeconds: 60,
      ...options
    };
  }
  // =========================================================================
  // CORE SESSION OPERATIONS
  // =========================================================================
  /**
   * Get valid session for workspace (auto-refresh if needed, auto-link if available)
   */
  async getSession(workspacePath) {
    if (!workspacePath || typeof workspacePath !== "string") {
      throw new WorkspaceAuthError("Invalid workspace path: path must be a non-empty string", WorkspaceAuthErrorCode.INVALID_WORKSPACE);
    }
    const entry = await getWorkspaceSession(workspacePath);
    if (entry) {
      if (this.needsRefresh(entry.session)) {
        try {
          const refreshed = await this.refreshSession(workspacePath);
          if (refreshed) {
            return refreshed;
          }
        } catch (error) {
          if (error.code === "PERMANENT_SESSION_DEAD") {
            try {
              await deleteWorkspaceSession(workspacePath);
            } catch {
            }
          } else {
            await touchWorkspaceSession(workspacePath);
            return {
              workspaceId: entry.workspaceId,
              workspacePath: entry.workspacePath,
              tokens: entry.session,
              user: entry.user,
              authMethod: entry.authMethod,
              isValid: !isSessionExpired(entry.session.expiresAt),
              expiresIn: getExpiresIn(entry.session.expiresAt),
              apiKey: entry.apiKey
            };
          }
        }
      } else {
        await touchWorkspaceSession(workspacePath);
        return {
          workspaceId: entry.workspaceId,
          workspacePath: entry.workspacePath,
          tokens: entry.session,
          user: entry.user,
          authMethod: entry.authMethod,
          isValid: true,
          expiresIn: getExpiresIn(entry.session.expiresAt),
          apiKey: entry.apiKey
        };
      }
    }
    const autoLinked = await this.autoLinkWorkspace(workspacePath);
    if (autoLinked) {
      return this.getSession(workspacePath);
    }
    return this.getGlobalFallbackSession(workspacePath);
  }
  /**
   * Set session for workspace (after login)
   */
  async setSession(workspacePath, session, user, authMethod, apiKey) {
    const now = (/* @__PURE__ */ new Date()).toISOString();
    await storeWorkspaceSession(workspacePath, {
      workspacePath,
      session,
      user,
      authMethod: authMethod === "global-fallback" ? "oauth" : authMethod,
      createdAt: now,
      updatedAt: now,
      lastUsedAt: now,
      apiKey
    });
  }
  /**
   * Persist a provisioned API key into an existing workspace session.
   * Called after autoProvisionApiKey returns a sk_live_* key so it can
   * be reused on subsequent MCP launches without re-authenticating.
   */
  async persistApiKey(workspacePath, apiKey) {
    const entry = await getWorkspaceSession(workspacePath);
    if (!entry) {
      return;
    }
    await storeWorkspaceSession(workspacePath, {
      ...entry,
      apiKey,
      updatedAt: (/* @__PURE__ */ new Date()).toISOString()
    });
  }
  /**
   * Clear session for workspace (logout)
   */
  async clearSession(workspacePath) {
    await deleteWorkspaceSession(workspacePath);
  }
  /**
   * Check if workspace has valid session
   */
  async hasSession(workspacePath) {
    const session = await getWorkspaceSession(workspacePath);
    if (!session) {
      return false;
    }
    return !isSessionExpired(session.session.expiresAt, this.options.refreshBufferSeconds);
  }
  /**
   * Refresh session explicitly
   */
  async refreshSession(workspacePath) {
    const entry = await getWorkspaceSession(workspacePath);
    if (!entry) {
      return null;
    }
    const refreshFn = this.options.refreshFn ?? refreshWithBetterAuth;
    const refreshedTokens = await withSingleFlight(`refresh:${entry.workspaceId}`, () => refreshFn(entry.session.refreshToken));
    if (!refreshedTokens) {
      this.options.onSessionExpired?.(entry.workspaceId);
      try {
        await deleteWorkspaceSession(workspacePath);
      } catch {
      }
      throw new WorkspaceAuthError("Session refresh failed  -  stale session cleared. Run 'vreko login' to re-authenticate.", WorkspaceAuthErrorCode.REFRESH_FAILED, entry.workspaceId);
    }
    await updateWorkspaceSessionTokens(workspacePath, refreshedTokens);
    const session = {
      workspaceId: entry.workspaceId,
      workspacePath,
      tokens: refreshedTokens,
      user: entry.user,
      authMethod: entry.authMethod,
      isValid: true,
      expiresIn: getExpiresIn(refreshedTokens.expiresAt)
    };
    this.options.onSessionRefreshed?.(entry.workspaceId, session);
    return session;
  }
  /**
   * List all authenticated workspaces
   */
  async listSessions() {
    const sessions = await listWorkspaceSessions();
    return sessions.map((s) => ({
      workspaceId: s.workspaceId,
      workspacePath: s.workspacePath,
      userEmail: s.user.email,
      tier: s.user.tier,
      authMethod: s.authMethod,
      expiresAt: (/* @__PURE__ */ new Date()).toISOString(),
      lastUsedAt: s.lastUsedAt
    }));
  }
  // =========================================================================
  // GLOBAL FALLBACK (BACKWARDS COMPATIBILITY)
  // =========================================================================
  /**
   * Migrate global credentials to workspace-scoped
   */
  async migrateGlobalCredentials(workspacePath, credentials) {
    try {
      const session = credentialsToWorkspaceSession(workspacePath, credentials, "global-fallback");
      await storeWorkspaceSession(workspacePath, session);
      await storeGlobalFallback({
        session: session.session,
        user: session.user,
        authMethod: "device-code"
      });
      return true;
    } catch (_error) {
      return false;
    }
  }
  /**
   * Get or create session from global fallback
   */
  async getGlobalFallbackSession(workspacePath) {
    const global = await getGlobalFallback();
    if (!global) {
      return null;
    }
    if (isSessionExpired(global.session.expiresAt)) {
      return null;
    }
    await this.migrateGlobalCredentials(workspacePath, {
      accessToken: global.session.accessToken,
      refreshToken: global.session.refreshToken,
      email: global.user.email,
      tier: global.user.tier === "free" ? "free" : "pro",
      expiresAt: global.session.expiresAt
    });
    return {
      workspaceId: `ws_${Date.now()}`,
      workspacePath,
      tokens: global.session,
      user: global.user,
      authMethod: "global-fallback",
      isValid: !isSessionExpired(global.session.expiresAt),
      expiresIn: getExpiresIn(global.session.expiresAt)
    };
  }
  // =========================================================================
  // UTILITY METHODS
  // =========================================================================
  /**
   * Check if session needs refresh
   */
  needsRefresh(session) {
    return isSessionExpired(session.expiresAt, this.options.refreshBufferSeconds);
  }
  /**
   * Cleanup expired sessions
   */
  async cleanup() {
    return cleanupExpiredSessions();
  }
  /**
   * Auto-link workspace to existing session
   *
   * Only links to global fallback credentials for security.
   * Does NOT clone sessions from other workspaces to prevent
   * unauthorized session sharing across different projects.
   *
   * @param workspacePath - New workspace to link
   * @returns true if linked successfully, false if no existing session found
   */
  async autoLinkWorkspace(workspacePath) {
    const existing = await getWorkspaceSession(workspacePath);
    if (existing) {
      return true;
    }
    const global = await getGlobalFallback();
    if (global && !isSessionExpired(global.session.expiresAt, 60)) {
      await this.migrateGlobalCredentials(workspacePath, {
        accessToken: global.session.accessToken,
        refreshToken: global.session.refreshToken,
        email: global.user.email,
        tier: global.user.tier === "free" ? "free" : "pro",
        expiresAt: global.session.expiresAt
      });
      return true;
    }
    return false;
  }
  /**
   * Get auth headers for API requests
   */
  async getAuthHeaders(workspacePath) {
    const session = await this.getSession(workspacePath);
    if (!session) {
      return {};
    }
    return {
      Authorization: `Bearer ${session.tokens.accessToken}`
    };
  }
  // =========================================================================
  // ENV VAR FALLBACK (STATIC)
  // =========================================================================
  /**
   * Get API key from environment variables
   *
   * Checks VREKO_API_KEY first, then VREKO_AUTH_TOKEN (legacy).
   * This is a fallback when no workspace session exists.
   *
   * @returns API key from env vars, or null if not set
   */
  static getEnvVarApiKey() {
    return process.env.VREKO_API_KEY ?? process.env.VREKO_AUTH_TOKEN ?? null;
  }
  /**
   * Check if using legacy VREKO_AUTH_TOKEN env var
   */
  static isUsingLegacyEnvVar() {
    return !process.env.VREKO_API_KEY && !!process.env.VREKO_AUTH_TOKEN;
  }
};
var defaultManager = null;
function getWorkspaceSessionManager(options) {
  if (!defaultManager) {
    defaultManager = new WorkspaceSessionManager(options);
  }
  return defaultManager;
}
__name(getWorkspaceSessionManager, "getWorkspaceSessionManager");
function readVrekoConfig(workspacePath) {
  const configPath = join(workspacePath, ".vreko", "config.json");
  try {
    if (!existsSync(configPath)) {
      return void 0;
    }
    return JSON.parse(readFileSync(configPath, "utf8"));
  } catch {
    return void 0;
  }
}
__name(readVrekoConfig, "readVrekoConfig");
async function scanMcpClients() {
  const result7 = detectAIClients();
  return {
    detected: result7.detected,
    needsSetup: result7.needsSetup,
    configured: result7.detected.filter((c) => c.hasVreko).length
  };
}
__name(scanMcpClients, "scanMcpClients");
async function linkMcpClient(options) {
  const workspaceValidation = resolveWorkspaceRoot(options.workspace);
  const workspaceRoot = workspaceValidation.valid ? workspaceValidation.root : process.cwd();
  const configPath = getClientConfigPath(options.client, workspaceRoot);
  if (!configPath) {
    throw new Error(`Unknown client: ${options.client}
Supported: claude, cursor, vscode, qoder, windsurf, cline, zed, continue`);
  }
  let client = getClient(options.client);
  if (!client) {
    client = {
      name: options.client,
      displayName: options.client.charAt(0).toUpperCase() + options.client.slice(1),
      configPath,
      exists: false,
      hasVreko: false,
      format: options.client
    };
  }
  let workspaceId = options.workspaceId;
  if (!workspaceId) {
    const config2 = readVrekoConfig(workspaceRoot);
    if (config2?.workspaceId) {
      workspaceId = config2.workspaceId;
    }
  }
  const useRemoteTransport = options.useSse || options.useStreamableHttp;
  let additionalEnv;
  if (!options.useDoppler && !useRemoteTransport) {
    const manager = getWorkspaceSessionManager();
    const session = await manager.getSession(workspaceRoot);
    if (session) {
      additionalEnv = {
        VREKO_SESSION_TOKEN: session.tokens.accessToken,
        VREKO_SESSION_TYPE: session.authMethod === "api-key" ? "api_key" : "bearer",
        VREKO_USER_EMAIL: session.user.email
      };
    }
  }
  const mcpConfig = getVrekoMCPConfig({
    client: options.client,
    apiKey: options.apiKey,
    workspaceId,
    workspaceRoot,
    useLocalDev: !options.useDoppler && !useRemoteTransport,
    localCliPath: options.localCliPath,
    additionalEnv,
    // Doppler options
    useDoppler: options.useDoppler,
    dopplerProject: options.dopplerProject,
    dopplerConfig: options.dopplerConfig,
    // SSE/HTTP options
    useSse: options.useSse,
    useStreamableHttp: options.useStreamableHttp
  });
  mkdirSync(dirname(client.configPath), {
    recursive: true
  });
  const result7 = writeClientConfig(client, mcpConfig);
  if (!result7.success) {
    return {
      success: false,
      clientDisplayName: client.displayName,
      serverKey: getServerKey(options.client),
      configPath: client.configPath,
      error: result7.error,
      validated: false
    };
  }
  const validated = validateConfig(client);
  return {
    success: true,
    clientDisplayName: client.displayName,
    serverKey: getServerKey(options.client),
    configPath: client.configPath,
    backup: result7.backup,
    validated
  };
}
__name(linkMcpClient, "linkMcpClient");
async function unlinkMcpClient(options) {
  const configPath = getClientConfigPath(options.client);
  if (!configPath) {
    throw new Error(`Unknown client: ${options.client}
Supported: claude, cursor, vscode, qoder, windsurf, cline, zed, continue`);
  }
  const client = getClient(options.client);
  if (!client) {
    return {
      success: true,
      clientDisplayName: options.client,
      configPath
    };
  }
  const result7 = removeVrekoConfig(client);
  return {
    success: result7.success,
    clientDisplayName: client.displayName,
    configPath: client.configPath,
    error: result7.error
  };
}
__name(unlinkMcpClient, "unlinkMcpClient");
function formatScanResult(result7) {
  const lines = [];
  lines.push(chalk2.bold("\nVreko MCP Configuration Scan"));
  lines.push(chalk2.gray("=".repeat(40)));
  if (result7.detected.length === 0) {
    lines.push(chalk2.yellow("\nNo supported AI clients detected."));
  } else {
    lines.push(chalk2.cyan(`
Detected ${result7.detected.length} client(s):`));
    for (const client of result7.detected) {
      const serverKey = getServerKey(client.format);
      const status = client.hasVreko ? chalk2.green("\u2713 Configured") : chalk2.yellow("\u2717 Not configured");
      lines.push(`  ${chalk2.bold(client.displayName)}: ${status}`);
      lines.push(chalk2.gray(`    Path: ${client.configPath}`));
      lines.push(chalk2.gray(`    Key: ${serverKey}`));
    }
  }
  if (result7.needsSetup.length > 0) {
    lines.push(chalk2.yellow(`
${result7.needsSetup.length} client(s) need Vreko setup:`));
    for (const client of result7.needsSetup) {
      lines.push(`  - ${client.displayName}`);
    }
    lines.push(chalk2.gray("\nRun: vr mcp link --client <name>"));
  }
  lines.push(chalk2.gray(`
Summary: ${result7.configured}/${result7.detected.length} clients configured`));
  return lines.join("\n");
}
__name(formatScanResult, "formatScanResult");
function formatLinkResult(result7) {
  const lines = [];
  if (result7.success) {
    lines.push(chalk2.green(`\u2713 Vreko configured for ${result7.clientDisplayName}`));
    lines.push(chalk2.gray(`  Server key: ${result7.serverKey}`));
    lines.push(chalk2.gray(`  Config: ${result7.configPath}`));
    if (result7.backup) {
      lines.push(chalk2.gray(`  Backup: ${result7.backup}`));
    }
    if (result7.validated) {
      lines.push(chalk2.green("\u2713 Configuration validated"));
    }
  } else {
    lines.push(chalk2.red(`\u2717 Failed to configure: ${result7.error}`));
  }
  return lines.join("\n");
}
__name(formatLinkResult, "formatLinkResult");
async function syncApiKeyToAllConfigs(apiKey) {
  const result7 = detectAIClients();
  const updated = [];
  for (const client of result7.detected) {
    if (!client.hasVreko) {
      continue;
    }
    const patched = patchApiKeyInClientConfig(client, apiKey);
    if (patched) {
      updated.push(client.displayName);
    }
  }
  return updated;
}
__name(syncApiKeyToAllConfigs, "syncApiKeyToAllConfigs");
function formatUnlinkResult(result7) {
  const lines = [];
  if (result7.success) {
    lines.push(chalk2.green(`\u2713 Vreko removed from ${result7.clientDisplayName}`));
    lines.push(chalk2.gray(`  Config: ${result7.configPath}`));
    if (result7.backup) {
      lines.push(chalk2.gray(`  Backup: ${result7.backup}`));
    }
  } else {
    lines.push(chalk2.red(`\u2717 Failed to remove: ${result7.error}`));
  }
  return lines.join("\n");
}
__name(formatUnlinkResult, "formatUnlinkResult");

// src/commands/auth.ts
function resolveApiUrl(raw = process.env.VREKO_API_URL) {
  const value = raw || "https://api.vreko.dev";
  let parsed;
  try {
    parsed = new URL(value);
  } catch {
    throw new Error(`Invalid VREKO_API_URL: ${value}`);
  }
  const isLoopback = parsed.hostname === "localhost" || parsed.hostname === "127.0.0.1" || parsed.hostname === "::1";
  if (!isLoopback && parsed.protocol !== "https:") {
    throw new Error(`VREKO_API_URL must use https:// for non-localhost hosts (got ${parsed.protocol}//${parsed.hostname}). Cleartext to a remote API exposes the device-code flow to interception.`);
  }
  return value;
}
__name(resolveApiUrl, "resolveApiUrl");
var API_URL = resolveApiUrl();
var CONSOLE_URL = process.env.VREKO_CONSOLE_URL || "https://console.vreko.dev";
function isApiKeyCredentials(creds) {
  return "apiKey" in creds && !("accessToken" in creds);
}
__name(isApiKeyCredentials, "isApiKeyCredentials");
function isTokenCredentials(creds) {
  return "accessToken" in creds;
}
__name(isTokenCredentials, "isTokenCredentials");
async function loadCredentials() {
  try {
    const creds = await getCredentials();
    if (creds?.accessToken) {
      return creds;
    }
  } catch (error) {
    console.error(chalk2.gray(`Secure credential read failed: ${error instanceof Error ? error.message : String(error)}`));
  }
  try {
    const { readFile: readFile10 } = await import('fs/promises');
    const { homedir: homedir15 } = await import('os');
    const { join: join28 } = await import('path');
    const credPath = join28(homedir15(), ".vreko", "credentials.json");
    const raw = await readFile10(credPath, "utf8");
    const parsed = JSON.parse(raw);
    if (typeof parsed.apiKey === "string" && parsed.apiKey.length > 0) {
      return {
        apiKey: parsed.apiKey
      };
    }
    if (typeof parsed.accessToken === "string" && parsed.accessToken.length > 0) {
      return parsed;
    }
    return null;
  } catch {
    return null;
  }
}
__name(loadCredentials, "loadCredentials");
async function validateApiKey(key) {
  try {
    const response = await fetch(`${API_URL}/api/auth/get-session`, {
      headers: {
        "x-api-key": key
      }
    });
    if (!response.ok) {
      return null;
    }
    const data = await response.json();
    return {
      email: data?.user?.email ?? "unknown"
    };
  } catch (error) {
    console.error(chalk2.gray(`API key validation failed: ${error instanceof Error ? error.message : String(error)}`));
    return null;
  }
}
__name(validateApiKey, "validateApiKey");
async function validateToken(token) {
  try {
    const response = await fetch(`${API_URL}/api/auth/get-session`, {
      headers: {
        Authorization: `Bearer ${token}`
      }
    });
    if (!response.ok) {
      return null;
    }
    const data = await response.json();
    return {
      email: data?.user?.email ?? "unknown",
      tier: data?.user?.tier
    };
  } catch (error) {
    console.error(chalk2.gray(`Token validation failed: ${error instanceof Error ? error.message : String(error)}`));
    return null;
  }
}
__name(validateToken, "validateToken");
function openBrowser(url) {
  if (!url.startsWith("https://") && !url.startsWith("http://localhost")) {
    return;
  }
  if (process.platform === "darwin") {
    execFile("open", [
      url
    ], () => {
    });
  } else if (process.platform === "win32") {
    execFile("cmd", [
      "/c",
      "start",
      "",
      url
    ], () => {
    });
  } else {
    execFile("xdg-open", [
      url
    ], () => {
    });
  }
}
__name(openBrowser, "openBrowser");
function reportStorageResult(result7) {
  if (!result7 || result7.secure) {
    const backend = result7?.backend === "keychain" ? "OS keychain" : "encrypted file";
    console.log(chalk2.gray(`  Credentials stored securely (${result7 ? backend : "secure backend"}).`));
    return;
  }
  console.log(chalk2.yellow("  \u26A0 Secure storage unavailable  -  credentials saved to a restricted-permission (0600) plaintext file."));
  if (result7.downgradeReason) {
    console.log(chalk2.dim(`    Reason: ${result7.downgradeReason}`));
  }
  console.log(chalk2.dim("    Install/enable a system keychain (keytar) for encrypted storage."));
}
__name(reportStorageResult, "reportStorageResult");
function buildLoopbackAuthUrl(consoleUrl, redirectUri, state) {
  return `${consoleUrl}/auth/cli?redirect=${encodeURIComponent(redirectUri)}&state=${state}`;
}
__name(buildLoopbackAuthUrl, "buildLoopbackAuthUrl");
function readRequestBody(req, maxBytes = 64 * 1024) {
  return new Promise((resolve11, reject) => {
    const chunks = [];
    let total = 0;
    req.on("data", (chunk) => {
      total += chunk.length;
      if (total > maxBytes) {
        reject(new Error("body too large"));
        req.destroy();
        return;
      }
      chunks.push(chunk);
    });
    req.on("end", () => resolve11(Buffer.concat(chunks).toString("utf8")));
    req.on("error", reject);
  });
}
__name(readRequestBody, "readRequestBody");
function isExpectedState(received, expected) {
  if (typeof received !== "string" || received.length !== expected.length) {
    return false;
  }
  try {
    return timingSafeEqual(Buffer.from(received, "utf8"), Buffer.from(expected, "utf8"));
  } catch {
    return false;
  }
}
__name(isExpectedState, "isExpectedState");
async function deviceCodeLogin() {
  const spinner2 = ora("Requesting device code...").start();
  let deviceCode;
  try {
    const response = await fetch(`${API_URL}/api/auth/device/code`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        client_id: "cli"
      })
    });
    if (!response.ok) {
      spinner2.fail("Failed to request device code from server.");
      const errorBody = await response.text().catch(() => "");
      console.error(chalk2.red(`Server responded with ${response.status}: ${errorBody}`));
      process.exit(1);
      return;
    }
    deviceCode = await response.json();
  } catch (error) {
    spinner2.fail("Failed to connect to Vreko server.");
    console.error(chalk2.red(`Network error: ${error instanceof Error ? error.message : String(error)}`));
    process.exit(1);
    return;
  }
  spinner2.stop();
  console.log();
  console.log(chalk2.bold("  To authenticate, visit:"));
  console.log(`  ${chalk2.cyan(deviceCode.verification_uri)}`);
  console.log();
  console.log(chalk2.bold("  Enter code: ") + chalk2.yellow.bold(deviceCode.user_code));
  console.log();
  const openUrl = deviceCode.verification_uri_complete || deviceCode.verification_uri;
  openBrowser(openUrl);
  console.log(chalk2.gray("  Browser opened automatically. If not, copy the URL above."));
  console.log();
  const pollSpinner = ora("Waiting for authorization...").start();
  let interval = (deviceCode.interval || 5) * 1e3;
  const deadline = Date.now() + deviceCode.expires_in * 1e3;
  while (Date.now() < deadline) {
    await new Promise((resolve11) => setTimeout(resolve11, interval));
    try {
      const tokenResponse = await fetch(`${API_URL}/api/auth/device/token`, {
        method: "POST",
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify({
          device_code: deviceCode.device_code,
          grant_type: "urn:ietf:params:oauth:grant-type:device_code",
          client_id: "cli"
        })
      });
      if (tokenResponse.ok) {
        const tokenData = await tokenResponse.json();
        pollSpinner.succeed("Authorized!");
        const expiresAt = new Date(Date.now() + tokenData.expires_in * 1e3).toISOString();
        const credentials = {
          accessToken: tokenData.access_token,
          refreshToken: tokenData.refresh_token,
          email: tokenData.user?.email ?? "unknown",
          tier: tokenData.tier ?? "free",
          expiresAt
        };
        await createGlobalDirectory();
        const storageResult = await saveCredentials(credentials);
        console.log(chalk2.green("\u2713"), `Authenticated as ${credentials.email}`);
        reportStorageResult(storageResult);
        try {
          await syncApiKeyToAllConfigs(tokenData.access_token);
        } catch {
          console.log(chalk2.gray("  Note: MCP config sync skipped (non-critical)."));
        }
        return;
      }
      const errorData = await tokenResponse.json();
      if (errorData.error === "authorization_pending") {
        continue;
      }
      if (errorData.error === "slow_down") {
        interval += 5e3;
        continue;
      }
      if (errorData.error === "access_denied") {
        pollSpinner.fail("Authorization denied.");
        console.error(chalk2.red("The authorization request was denied. Please try again."));
        process.exit(1);
        return;
      }
      if (errorData.error === "not_in_cohort") {
        pollSpinner.stop();
        console.error(chalk2.red("\n\u2717 Your email is not in Pioneer Batch 1."));
        console.error(chalk2.dim("  Join the waitlist: https://vreko.dev/pioneer\n"));
        process.exit(1);
        return;
      }
      if (errorData.error === "expired_token") {
        pollSpinner.fail("Device code expired.");
        console.error(chalk2.red("The device code has expired. Please run 'vreko login' again."));
        process.exit(1);
        return;
      }
      pollSpinner.fail("Authentication failed.");
      console.error(chalk2.red(`Server error: ${errorData.error_description || errorData.error}`));
      process.exit(1);
      return;
    } catch (error) {
      pollSpinner.text = "Waiting for authorization... (retrying after network error)";
      console.error(chalk2.gray(`
  Poll error: ${error instanceof Error ? error.message : String(error)}`));
    }
  }
  pollSpinner.fail("Device code expired.");
  console.error(chalk2.red("The device code has expired. Please run 'vreko login' again."));
  process.exit(1);
}
__name(deviceCodeLogin, "deviceCodeLogin");
async function browserOAuthLogin() {
  const ports = [
    51234,
    51235,
    51236
  ];
  let server = null;
  let resolvedPort = null;
  for (const port of ports) {
    try {
      server = await new Promise((resolve11, reject) => {
        const s = createServer();
        s.listen(port, "127.0.0.1", () => resolve11(s));
        s.on("error", reject);
      });
      resolvedPort = port;
      break;
    } catch {
    }
  }
  if (!server || !resolvedPort) {
    console.error(chalk2.red("Failed to start local auth server. Ports 51234-51236 are in use."));
    console.log(chalk2.gray("Try 'vreko login' (device code flow) instead."));
    process.exit(1);
    return;
  }
  const expectedState = randomBytes(32).toString("hex");
  const redirectUri = `http://localhost:${resolvedPort}/callback`;
  const authUrl = buildLoopbackAuthUrl(CONSOLE_URL, redirectUri, expectedState);
  console.log();
  console.log(chalk2.bold("  Opening browser for authentication..."));
  console.log(chalk2.gray(`  If the browser doesn't open, visit: ${authUrl}`));
  console.log();
  openBrowser(authUrl);
  const spinner2 = ora("Waiting for browser authorization...").start();
  const timeoutMs = 12e4;
  const timeout = setTimeout(() => {
    spinner2.fail("Browser authorization timed out.");
    console.error(chalk2.red("No callback received within 2 minutes. Please try again."));
    server?.close();
    process.exit(1);
  }, timeoutMs);
  return new Promise((resolve11) => {
    server?.on("request", async (req, res) => {
      const url = new URL(req.url || "/", `http://localhost:${resolvedPort}`);
      if (url.pathname !== "/callback") {
        res.writeHead(404);
        res.end("Not found");
        return;
      }
      if (req.method !== "POST") {
        res.writeHead(200, {
          "Content-Type": "text/html"
        });
        res.end("<html><body><h1>Vreko CLI login</h1><p>Waiting for the console to deliver your credentials. You can close this window once the terminal confirms.</p></body></html>");
        return;
      }
      const body = await readRequestBody(req);
      let payload;
      try {
        payload = JSON.parse(body);
      } catch {
        res.writeHead(400, {
          "Content-Type": "application/json"
        });
        res.end(JSON.stringify({
          error: "invalid_body"
        }));
        return;
      }
      const receivedState = payload.state;
      if (!receivedState || !isExpectedState(receivedState, expectedState)) {
        res.writeHead(400, {
          "Content-Type": "application/json"
        });
        res.end(JSON.stringify({
          error: "invalid_state"
        }));
        return;
      }
      const accessToken = payload.access_token;
      const refreshToken = payload.refresh_token ?? void 0;
      const email = payload.email ?? "unknown";
      const tier = payload.tier ?? "free";
      const expiresIn = Number.parseInt(String(payload.expires_in ?? "604800"), 10);
      if (!accessToken) {
        res.writeHead(400, {
          "Content-Type": "application/json"
        });
        res.end(JSON.stringify({
          error: "missing_access_token"
        }));
        return;
      }
      const credentials = {
        accessToken,
        refreshToken,
        email,
        tier,
        expiresAt: new Date(Date.now() + expiresIn * 1e3).toISOString()
      };
      await createGlobalDirectory();
      const storageResult = await saveCredentials(credentials);
      res.writeHead(200, {
        "Content-Type": "application/json"
      });
      res.end(JSON.stringify({
        ok: true
      }));
      clearTimeout(timeout);
      spinner2.succeed("Authorized!");
      console.log(chalk2.green("\u2713"), `Authenticated as ${email}`);
      reportStorageResult(storageResult);
      try {
        await syncApiKeyToAllConfigs(accessToken);
      } catch {
        console.log(chalk2.gray("  Note: MCP config sync skipped (non-critical)."));
      }
      server?.close();
      server?.unref();
      resolve11();
    });
  });
}
__name(browserOAuthLogin, "browserOAuthLogin");
function createLoginCommand() {
  return new Command("login").description("Authenticate with Vreko").option("--api-key <key>", "Authenticate with an API key (starts with sk_live_)").option("--browser", "Authenticate via browser OAuth (local callback)").action(async (options) => {
    if (options.apiKey) {
      const key = options.apiKey;
      if (!key.startsWith("sk_live_")) {
        console.error(chalk2.red("Invalid API key format. Keys start with sk_live_"));
        process.exit(1);
        return;
      }
      console.log(chalk2.gray("Validating API key..."));
      const user = await validateApiKey(key);
      if (!user) {
        console.error(chalk2.red("Invalid API key. Please check your key and try again."));
        console.log(chalk2.gray("Get your key at: https://console.vreko.dev"));
        process.exit(1);
        return;
      }
      await createGlobalDirectory();
      await saveCredentials({
        accessToken: key,
        email: user.email,
        tier: "free"
      });
      try {
        await syncApiKeyToAllConfigs(key);
      } catch {
      }
      console.log(chalk2.green("\u2713"), `Authenticated as ${user.email}`);
      console.log(chalk2.gray("API key saved to ~/.vreko/credentials.json"));
      return;
    }
    if (options.browser) {
      await browserOAuthLogin();
      return;
    }
    await deviceCodeLogin();
  });
}
__name(createLoginCommand, "createLoginCommand");
function createSetKeyCommand() {
  return new Command("set-key").description("Store your Vreko API key").argument("<key>", "Your API key (starts with sk_live_)").action(async (key) => {
    if (!key.startsWith("sk_live_")) {
      console.error(chalk2.red("Invalid API key format. Keys start with sk_live_"));
      process.exit(1);
      return;
    }
    console.log(chalk2.gray("Validating API key..."));
    const user = await validateApiKey(key);
    if (!user) {
      console.error(chalk2.red("Invalid API key. Please check your key and try again."));
      console.log(chalk2.gray("Get your key at: https://console.vreko.dev"));
      process.exit(1);
      return;
    }
    await createGlobalDirectory();
    await saveCredentials({
      accessToken: key,
      email: user.email,
      tier: "free"
    });
    try {
      await syncApiKeyToAllConfigs(key);
    } catch {
    }
    console.log(chalk2.green("\u2713"), `Authenticated as ${user.email}`);
    console.log(chalk2.gray("API key saved to ~/.vreko/credentials.json"));
  });
}
__name(createSetKeyCommand, "createSetKeyCommand");
function createLogoutCommand() {
  return new Command("logout").description("Remove stored credentials").action(async () => {
    await clearCredentials();
    console.log(chalk2.green("\u2713"), "Credentials cleared.");
  });
}
__name(createLogoutCommand, "createLogoutCommand");
function createWhoamiCommand() {
  return new Command("whoami").description("Show current authentication status").action(async () => {
    const creds = await loadCredentials();
    if (!creds) {
      console.log(chalk2.yellow("Not authenticated."));
      console.log(chalk2.gray("Run: vreko login"));
      console.log(chalk2.gray("  or: vreko login --api-key <your-api-key>"));
      return;
    }
    if (isApiKeyCredentials(creds)) {
      const preview = `${creds.apiKey.substring(0, 16)}...`;
      console.log(chalk2.green("\u2713"), `Authenticated with API key: ${chalk2.bold(preview)}`);
      console.log(chalk2.gray("  Auth method: api-key"));
      const user = await validateApiKey(creds.apiKey).catch(() => null);
      if (user) {
        console.log(chalk2.gray(`  User: ${user.email}`));
      } else {
        console.log(chalk2.yellow("  Warning: API key validation failed (offline or key revoked?)"));
      }
      return;
    }
    if (isTokenCredentials(creds)) {
      const isApiKey = creds.accessToken.startsWith("sk_live_");
      const authMethod = isApiKey ? "api-key" : "device-code";
      console.log(chalk2.green("\u2713"), `Authenticated as ${chalk2.bold(creds.email)}`);
      console.log(chalk2.gray(`  Auth method: ${authMethod}`));
      console.log(chalk2.gray(`  Tier: ${creds.tier}`));
      if (creds.expiresAt) {
        const expiresAt = new Date(creds.expiresAt);
        const now = /* @__PURE__ */ new Date();
        if (expiresAt < now) {
          console.log(chalk2.yellow(`  Token expired: ${expiresAt.toLocaleString()}`));
        } else {
          console.log(chalk2.gray(`  Expires: ${expiresAt.toLocaleString()}`));
        }
      }
      const user = await validateToken(creds.accessToken).catch(() => null);
      if (user) {
        console.log(chalk2.gray(`  Verified: ${user.email}`));
      } else {
        console.log(chalk2.yellow("  Warning: Token validation failed (offline or token expired?)"));
      }
      return;
    }
  });
}
__name(createWhoamiCommand, "createWhoamiCommand");
function createWorkspacesCommand() {
  return new Command("workspaces").description("Show authentication info").action(async () => {
    const creds = await loadCredentials();
    if (!creds) {
      console.log(chalk2.yellow("Not authenticated."));
      console.log(chalk2.gray("Run: vreko login"));
      return;
    }
    if (isApiKeyCredentials(creds)) {
      console.log(chalk2.green("\u2713"), "Authenticated (global)");
      console.log(chalk2.gray(`  Key: ${creds.apiKey.substring(0, 16)}...`));
      console.log(chalk2.gray("  Scope: all workspaces"));
    } else if (isTokenCredentials(creds)) {
      console.log(chalk2.green("\u2713"), `Authenticated as ${creds.email} (global)`);
      console.log(chalk2.gray(`  Tier: ${creds.tier}`));
      console.log(chalk2.gray("  Scope: all workspaces"));
    }
  });
}
__name(createWorkspacesCommand, "createWorkspacesCommand");
function createAnalyzeCommand() {
  return new Command("analyze").description("Run full workspace intelligence analysis via service").option("-w, --workspace <path>", "Workspace path", process.cwd()).option("--skip-baseline", "Skip baseline computation (faster but less complete)").option("--skip-learnings", "Skip learning seeding").option("--force-seed", "Force re-seed learnings even if already seeded").option("--json", "Output result as JSON").action(async (options) => {
    const workspace = options.workspace;
    if (!options.json) {
      console.log(chalk2.gray("  Credit estimate: ~20-30 credits (full_codebase_scan). Check vr credits for your balance."));
    }
    try {
      const spinner2 = ora("Connecting to service...").start();
      if (!await isDaemonAvailable()) {
        spinner2.fail("Service not available");
        process.exit(1);
      }
      spinner2.text = "Running workspace analysis...";
      const client = await connectToDaemon();
      const result7 = await client.workspace.analyze({
        workspace,
        skipBaseline: options.skipBaseline,
        skipLearnings: options.skipLearnings,
        forceSeedLearnings: options.forceSeed
      });
      spinner2.succeed(`Analysis complete in ${(result7.duration / 1e3).toFixed(1)}s`);
      if (options.json) {
        console.log(JSON.stringify(result7, null, 2));
        return;
      }
      displayAnalysisResults(result7);
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
}
__name(createAnalyzeCommand, "createAnalyzeCommand");
function displayAnalysisResults(result7) {
  const readinessColor = result7.readiness.level === "ready" ? chalk2.green : result7.readiness.level === "partial" ? chalk2.yellow : chalk2.red;
  console.log(chalk2.bold("Workspace Analysis Results"));
  console.log(chalk2.gray("\u2500".repeat(50)));
  console.log();
  console.log(`Readiness: ${readinessColor(result7.readiness.level.toUpperCase())}`);
  if (result7.profile) {
    console.log(`Framework: ${result7.profile.framework.name ?? "detected"} (${Math.round(result7.profile.framework.confidence * 100)}%)`);
  } else {
    console.log("Framework: not detected");
  }
  const factors = result7.readiness.factors;
  console.log(`Baseline:  ${factors.hasBaseline ? chalk2.green("\u2713") : chalk2.red("\u2717")}`);
  console.log(`Learnings: ${factors.hasLearnings ? chalk2.green("\u2713") : chalk2.red("\u2717")}`);
  if (result7.learningsSeeded.seeded) {
    const { count, tiers } = result7.learningsSeeded;
    console.log(`  Seeded ${count} learning(s)  -  hot: ${tiers.hot}, warm: ${tiers.warm}, cold: ${tiers.cold}`);
  } else if (result7.learningsSeeded.alreadySeeded) {
    console.log("  Learnings already seeded");
  }
  if (result7.baselineJobId) {
    console.log(`  Baseline job: ${result7.baselineJobId}`);
  }
  if (result7.readiness.recommendations.length > 0) {
    console.log("\nRecommendations:");
    for (const rec of result7.readiness.recommendations) {
      console.log(`  \u2192 ${rec}`);
    }
  }
  if (result7.profile?.gaps && result7.profile.gaps.length > 0) {
    const criticalGaps = result7.profile.gaps.filter((g) => g.severity === "critical");
    const warningGaps = result7.profile.gaps.filter((g) => g.severity === "warning");
    if (criticalGaps.length > 0) {
      console.log("\nCritical gaps:");
      for (const gap of criticalGaps) {
        console.log(`  ${chalk2.red("\u2717")} ${gap.message ?? gap.type}`);
      }
    }
    if (warningGaps.length > 0) {
      console.log("\nWarnings:");
      for (const gap of warningGaps) {
        console.log(`  ${chalk2.yellow("\u26A0")} ${gap.message ?? gap.type}`);
      }
    }
  }
  if (result7.readiness.level !== "ready") {
    console.log();
    if (!result7.readiness.factors.hasBaseline) {
      console.log("  Run `vreko baseline` to establish baseline commit history");
    }
    if (!result7.readiness.factors.hasLearnings) {
      console.log("  Run `vreko learn` to seed learnings for this workspace");
    }
  }
}
__name(displayAnalysisResults, "displayAnalysisResults");
function createClaudeSyncCommand() {
  return new Command("claude-sync").description("Generate or refresh Claude Code integration (.mcp.json, .claude/agents/)").argument("[path]", "Workspace path (default: current directory)").option("--force", "Overwrite existing files").option("--channel", "Include channel server config (research preview  -  requires --channels flag at Claude Code startup)").option("--json", "Output structured JSON result").action(async (pathArg, _options, cmd) => {
    const merged = cmd.optsWithGlobals();
    await runClaudeSync(pathArg, merged);
  });
}
__name(createClaudeSyncCommand, "createClaudeSyncCommand");
async function runClaudeSync(pathArg, options) {
  const workspacePath = resolve(pathArg ?? process.cwd());
  const jsonMode = !!options.json;
  const spinner2 = jsonMode ? null : ora("Fetching intelligence...").start();
  let baseline;
  try {
    const client = await connectToDaemon();
    const record = await client.call("baseline/get", {
      workspace: workspacePath
    });
    if (record?.fragileFiles !== void 0 || record?.coChangeClusters !== void 0) {
      baseline = {
        fragileFiles: record.fragileFiles,
        coChangeClusters: record.coChangeClusters
      };
    }
    if (spinner2) {
      spinner2.text = "Generating Claude Code integration...";
    }
  } catch {
    if (spinner2) {
      spinner2.text = "Generating Claude Code integration (service unavailable, no intelligence)...";
    }
  }
  const result7 = generateClaudeIntegration({
    workspacePath,
    overwrite: options.force ?? false,
    includeChannel: options.channel ?? false
  }, baseline);
  if (jsonMode) {
    console.log(JSON.stringify(result7));
    return;
  }
  if (result7.filesWritten.length === 0 && result7.filesSkipped.length === 0) {
    spinner2?.warn("Nothing generated");
    return;
  }
  if (result7.filesWritten.length === 0) {
    spinner2?.info("Files already exist  -  use --force to overwrite");
    for (const f of result7.filesSkipped) {
      console.log(chalk2.gray(`  ~ ${f}`));
    }
    return;
  }
  spinner2?.succeed(`Generated ${result7.filesWritten.length} file${result7.filesWritten.length !== 1 ? "s" : ""}`);
  for (const f of result7.filesWritten) {
    console.log(chalk2.gray(`  + ${f}`));
  }
  if (result7.filesSkipped.length > 0) {
    for (const f of result7.filesSkipped) {
      console.log(chalk2.gray(`  ~ ${f}`));
    }
  }
  console.log();
  if (result7.intelligenceAvailable) {
    console.log(chalk2.gray(`  Intelligence: ${result7.fragileFilesIncluded} fragile file${result7.fragileFilesIncluded !== 1 ? "s" : ""}, ${result7.coChangePatternsIncluded} co-change pattern${result7.coChangePatternsIncluded !== 1 ? "s" : ""}`));
  } else {
    console.log(chalk2.gray("  Intelligence: none yet  -  run vr analyze to build it"));
  }
  console.log(chalk2.gray("  Tip: commit .mcp.json and .claude/ to share with your team"));
}
__name(runClaudeSync, "runClaudeSync");
var FIXES = {
  "missing-gitignore": {
    description: "Add .vreko to .gitignore",
    fix: fixMissingGitignore
  },
  "no-protection": {
    description: "Auto-detect and protect critical files",
    fix: fixNoProtection
  },
  "stale-session": {
    description: "End stale session (>24h old)",
    fix: fixStaleSession
  },
  "high-violations": {
    description: "Review and learn from violation patterns",
    fix: fixHighViolations
  },
  "not-logged-in": {
    description: "Login to Vreko for full features",
    fix: fixNotLoggedIn
  }
};
function createFixCommand() {
  const fix = new Command("fix").description("Auto-fix detected issues").argument("[issue]", "Issue ID to fix (from vr status)").option("--dry-run", "Show what would be fixed without making changes").option("--all", "Fix all detected issues").option("--list", "List all available fixes").option("--json", "Output result as JSON for LLM consumption").option("-q, --quiet", "Suppress non-essential output").action(async (issue, options) => {
    const cwd = process.cwd();
    try {
      if (options.list) {
        displayAvailableFixes();
        return;
      }
      if (!await isVrekoInitialized(cwd)) {
        console.log(chalk2.yellow("Vreko not initialized in this workspace"));
        console.log(chalk2.gray("Run: vr init"));
        process.exit(1);
      }
      if (options.all) {
        await fixAll(cwd, options.dryRun);
        return;
      }
      if (!issue) {
        console.log(chalk2.yellow("No issue specified"));
        console.log();
        console.log("Usage:");
        console.log(chalk2.gray("  vr fix <issue-id>    Fix a specific issue"));
        console.log(chalk2.gray("  vr fix --all         Fix all detected issues"));
        console.log(chalk2.gray("  vr fix --list        List all available fixes"));
        console.log();
        console.log("Run", chalk2.cyan("vr status"), "to see detected issues.");
        return;
      }
      const fixer = FIXES[issue];
      if (!fixer) {
        console.log(chalk2.red(`Unknown issue: ${issue}`));
        console.log();
        displayAvailableFixes();
        return;
      }
      if (options.dryRun) {
        console.log(chalk2.cyan("Dry run:"), `Would fix "${issue}"`);
        console.log(chalk2.gray(`  ${fixer.description}`));
      }
      const result7 = await fixer.fix(cwd, options.dryRun);
      if (result7.success) {
        if (options.dryRun) {
          console.log(chalk2.cyan("\u2713"), result7.message);
        } else {
          console.log(chalk2.green("\u2713"), result7.message);
        }
      } else {
        console.log(chalk2.yellow("\u25CB"), result7.message);
      }
      if (result7.details && result7.details.length > 0) {
        for (const detail of result7.details) {
          console.log(chalk2.gray(`  ${detail}`));
        }
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  return fix;
}
__name(createFixCommand, "createFixCommand");
async function fixMissingGitignore(workspaceRoot, dryRun) {
  const gitignorePath = join(workspaceRoot, ".gitignore");
  try {
    await access(gitignorePath, constants.F_OK);
    const content = await readFile(gitignorePath, "utf-8");
    if (content.includes(".vreko")) {
      return {
        success: false,
        message: ".vreko already in .gitignore"
      };
    }
    const addition = "\n# Vreko snapshots (large binary data)\n.vreko/snapshots/\n.vreko/embeddings.db\n";
    if (dryRun) {
      return {
        success: true,
        message: "Would add .vreko entries to .gitignore",
        details: [
          ".vreko/snapshots/",
          ".vreko/embeddings.db"
        ]
      };
    }
    await appendFile(gitignorePath, addition);
    return {
      success: true,
      message: "Added .vreko entries to .gitignore",
      details: [
        ".vreko/snapshots/",
        ".vreko/embeddings.db"
      ]
    };
  } catch {
    if (dryRun) {
      return {
        success: true,
        message: "Would create .gitignore with .vreko entries"
      };
    }
    const content = "# Vreko snapshots (large binary data)\n.vreko/snapshots/\n.vreko/embeddings.db\n";
    const { writeFile: writeFile6 } = await import('fs/promises');
    await writeFile6(gitignorePath, content);
    return {
      success: true,
      message: "Created .gitignore with .vreko entries"
    };
  }
}
__name(fixMissingGitignore, "fixMissingGitignore");
async function fixNoProtection(workspaceRoot, dryRun) {
  const currentProtected = await getProtectedFiles(workspaceRoot);
  if (currentProtected.length > 0) {
    return {
      success: false,
      message: `Already have ${currentProtected.length} protected files`
    };
  }
  const vitals = await getWorkspaceVitals(workspaceRoot);
  const criticalPatterns = await detectCriticalPatterns(workspaceRoot, vitals?.framework);
  if (criticalPatterns.length === 0) {
    return {
      success: false,
      message: "No critical files detected to protect"
    };
  }
  if (dryRun) {
    return {
      success: true,
      message: `Would protect ${criticalPatterns.length} critical file patterns`,
      details: criticalPatterns.map((p) => p.pattern)
    };
  }
  await saveProtectedFiles(criticalPatterns, workspaceRoot);
  return {
    success: true,
    message: `Protected ${criticalPatterns.length} critical file patterns`,
    details: criticalPatterns.map((p) => `${p.pattern} (${p.reason})`)
  };
}
__name(fixNoProtection, "fixNoProtection");
async function fixStaleSession(workspaceRoot, dryRun) {
  const session = await getCurrentSession(workspaceRoot);
  if (!session) {
    return {
      success: false,
      message: "No active session"
    };
  }
  const sessionStart = new Date(session.startedAt);
  const hoursSinceStart = (Date.now() - sessionStart.getTime()) / (1e3 * 60 * 60);
  if (hoursSinceStart <= 24) {
    return {
      success: false,
      message: `Session is only ${Math.floor(hoursSinceStart)}h old (not stale)`
    };
  }
  if (dryRun) {
    return {
      success: true,
      message: `Would end stale session (${Math.floor(hoursSinceStart)}h old)`,
      details: [
        `ID: ${session.id.substring(0, 8)}`,
        session.task ? `Task: ${session.task}` : "(no task)",
        `Snapshots: ${session.snapshotCount}`
      ]
    };
  }
  const { appendVrekoJsonl: appendVrekoJsonl2 } = await import('./vreko-dir-ELQLOFKA.js');
  await appendVrekoJsonl2("session/history.jsonl", {
    ...session,
    endedAt: (/* @__PURE__ */ new Date()).toISOString(),
    endMessage: "Auto-ended by vr fix (stale session)"
  }, workspaceRoot);
  await endCurrentSession(workspaceRoot);
  return {
    success: true,
    message: `Ended stale session (${Math.floor(hoursSinceStart)}h old)`,
    details: [
      `ID: ${session.id.substring(0, 8)}`,
      `Snapshots: ${session.snapshotCount}`
    ]
  };
}
__name(fixStaleSession, "fixStaleSession");
async function fixHighViolations(workspaceRoot, _dryRun) {
  const violations = await getViolations(workspaceRoot);
  const oneWeekAgo = new Date(Date.now() - 7 * 24 * 60 * 60 * 1e3);
  const recentViolations = violations.filter((v) => new Date(v.date) > oneWeekAgo);
  if (recentViolations.length <= 5) {
    return {
      success: false,
      message: `Only ${recentViolations.length} violations this week (threshold: >5)`
    };
  }
  const byType = {};
  for (const v of recentViolations) {
    byType[v.type] = (byType[v.type] || 0) + 1;
  }
  const sorted = Object.entries(byType).sort((a, b) => b[1] - a[1]);
  const details = sorted.map(([type, count]) => {
    const emoji = count >= 3 ? "\u{1F534}" : count >= 2 ? "\u{1F7E1}" : "\u26AA";
    const promoted = count >= 3 ? " (pattern candidate)" : "";
    return `${emoji} ${type}: ${count} occurrences${promoted}`;
  });
  for (const line of details) {
    console.log(`  ${line}`);
  }
  return {
    success: true,
    message: "Violation patterns analyzed",
    details: [
      "Patterns with 3+ occurrences are candidates for automation.",
      "Run: vr patterns list  to see promoted patterns",
      'Run: vr learn "<trigger>" "<action>"  to record learnings'
    ]
  };
}
__name(fixHighViolations, "fixHighViolations");
async function fixNotLoggedIn(_workspaceRoot, _dryRun) {
  return {
    success: true,
    message: "Login instructions displayed",
    details: [
      "Run: vr login"
    ]
  };
}
__name(fixNotLoggedIn, "fixNotLoggedIn");
function displayAvailableFixes() {
  console.log(chalk2.cyan("Available Fixes:"));
  console.log();
  for (const [id, { description }] of Object.entries(FIXES)) {
    console.log(`  ${chalk2.bold(id)}`);
    console.log(chalk2.gray(`    ${description}`));
  }
  console.log();
  console.log(chalk2.gray("Usage: vr fix <issue-id>"));
  console.log(chalk2.gray("       vr fix --all"));
}
__name(displayAvailableFixes, "displayAvailableFixes");
async function fixAll(workspaceRoot, dryRun) {
  let fixedCount = 0;
  let skippedCount = 0;
  console.log(chalk2.cyan(dryRun ? "Dry run: Would fix all issues" : "Fixing all detected issues..."));
  for (const [id, { fix }] of Object.entries(FIXES)) {
    try {
      const result7 = await fix(workspaceRoot, dryRun);
      if (result7.success) {
        fixedCount++;
        if (dryRun) {
          console.log(chalk2.cyan("\u2713"), `${id}: ${result7.message}`);
        } else {
          console.log(chalk2.green("\u2713"), `${id}: ${result7.message}`);
        }
      } else {
        skippedCount++;
        console.log(chalk2.yellow("\u25CB"), `${id}: ${result7.message}`);
      }
    } catch (error) {
      skippedCount++;
      const message = error instanceof Error ? error.message : String(error);
      console.log(chalk2.red("\u2717"), `${id}: ${message}`);
    }
  }
  console.log();
  console.log(`Fixed: ${fixedCount}, Skipped: ${skippedCount}`);
}
__name(fixAll, "fixAll");
async function detectCriticalPatterns(workspaceRoot, framework) {
  const patterns = [];
  const now = (/* @__PURE__ */ new Date()).toISOString();
  const envPatterns = [
    ".env",
    ".env.local",
    ".env.production",
    ".env.development"
  ];
  for (const pattern of envPatterns) {
    try {
      await access(join(workspaceRoot, pattern), constants.F_OK);
      patterns.push({
        pattern,
        addedAt: now,
        reason: "Environment variables"
      });
    } catch {
    }
  }
  patterns.push({
    pattern: ".env.*",
    addedAt: now,
    reason: "Environment variables (all variants)"
  });
  const configPatterns = [
    "tsconfig.json",
    "package.json",
    "pnpm-lock.yaml",
    "yarn.lock",
    "package-lock.json"
  ];
  for (const pattern of configPatterns) {
    try {
      await access(join(workspaceRoot, pattern), constants.F_OK);
      patterns.push({
        pattern,
        addedAt: now,
        reason: "Configuration file"
      });
    } catch {
    }
  }
  patterns.push({
    pattern: "*.config.js",
    addedAt: now,
    reason: "Configuration files"
  });
  patterns.push({
    pattern: "*.config.ts",
    addedAt: now,
    reason: "Configuration files"
  });
  if (framework) {
    const frameworkPatterns = getFrameworkPatterns(framework);
    patterns.push(...frameworkPatterns.map((p) => ({
      ...p,
      addedAt: now
    })));
  }
  const criticalDirs = [
    {
      dir: "prisma",
      pattern: "prisma/**",
      reason: "Database schema"
    },
    {
      dir: "drizzle",
      pattern: "drizzle/**",
      reason: "Database schema"
    },
    {
      dir: "src/auth",
      pattern: "src/auth/**",
      reason: "Authentication"
    },
    {
      dir: "src/lib/auth",
      pattern: "src/lib/auth/**",
      reason: "Authentication"
    },
    {
      dir: "app/api/auth",
      pattern: "app/api/auth/**",
      reason: "Authentication API"
    }
  ];
  for (const { dir, pattern, reason } of criticalDirs) {
    try {
      await access(join(workspaceRoot, dir), constants.F_OK);
      patterns.push({
        pattern,
        addedAt: now,
        reason
      });
    } catch {
    }
  }
  return patterns;
}
__name(detectCriticalPatterns, "detectCriticalPatterns");
function getFrameworkPatterns(framework) {
  const patterns = [];
  switch (framework.toLowerCase()) {
    case "next.js":
    case "nextjs":
      patterns.push({
        pattern: "next.config.*",
        reason: "Next.js configuration"
      });
      patterns.push({
        pattern: "middleware.ts",
        reason: "Next.js middleware"
      });
      break;
    case "nuxt":
      patterns.push({
        pattern: "nuxt.config.*",
        reason: "Nuxt configuration"
      });
      break;
    case "sveltekit":
      patterns.push({
        pattern: "svelte.config.js",
        reason: "SvelteKit configuration"
      });
      break;
    case "astro":
      patterns.push({
        pattern: "astro.config.*",
        reason: "Astro configuration"
      });
      break;
    case "remix":
      patterns.push({
        pattern: "remix.config.js",
        reason: "Remix configuration"
      });
      break;
  }
  return patterns;
}
__name(getFrameworkPatterns, "getFrameworkPatterns");
function checkAuthToken() {
  if (process.env.VREKO_SERVICE_TOKEN) return {
    hasToken: true,
    source: "env-service"
  };
  if (process.env.VREKO_API_KEY) return {
    hasToken: true,
    source: "env-api"
  };
  try {
    const authPath = join(homedir(), ".vreko", "auth.json");
    if (existsSync(authPath)) {
      const auth = JSON.parse(readFileSync(authPath, "utf-8"));
      if (auth.token) return {
        hasToken: true,
        source: "auth-file"
      };
    }
  } catch {
  }
  return {
    hasToken: false,
    source: "none"
  };
}
__name(checkAuthToken, "checkAuthToken");
function createOnboardCommand() {
  return new Command("onboard").description("New-user onboarding wizard -- runs vr init then configures AI tools").option("--dry-run", "Show what would happen without writing any files").option("--skip-mcp", "Skip MCP client detection and link step").option("--apply-all", "Link all detected AI clients without prompting").option("-q, --quiet", "Suppress informational output").action(async (options) => {
    const dryRun = !!options.dryRun;
    const skipMcp = !!options.skipMcp;
    const applyAll = !!options.applyAll;
    const quiet = !!options.quiet;
    if (!quiet) console.log(chalk2.bold("\nVreko Onboarding\n"));
    try {
      const { createInitCommand: createInitCommand3 } = await import('./init-command-BRCA3ICG.js');
      const initCmd = createInitCommand3();
      const argv = [
        "node",
        "init"
      ];
      if (dryRun) argv.push("--dry-run");
      argv.push("--skip-mcp");
      await initCmd.parseAsync(argv, {
        from: "node"
      });
    } catch (err6) {
      console.error(chalk2.red("Init step failed:", err6 instanceof Error ? err6.message : String(err6)));
      process.exit(1);
    }
    const authStatus = checkAuthToken();
    if (!authStatus.hasToken) {
      console.warn(chalk2.yellow("\nWarning: Sync is configured but no API key was found.\n  Workspace metadata will not sync to the intelligence platform.\n  Run `vr login` or set VREKO_API_KEY.\n"));
    } else if (!quiet) {
      console.log(chalk2.green(`Auth token found (${authStatus.source})`));
    }
    if (skipMcp) {
      if (!quiet) console.log(chalk2.gray("MCP link step skipped (--skip-mcp)"));
      return;
    }
    let scan;
    try {
      scan = await scanMcpClients();
    } catch (err6) {
      console.warn(chalk2.yellow("MCP client detection failed (non-fatal):", err6 instanceof Error ? err6.message : String(err6)));
      return;
    }
    if (scan.needsSetup.length === 0) {
      if (!quiet && scan.detected.length > 0) {
        console.log(chalk2.green("All detected AI clients are already configured."));
      }
      return;
    }
    if (!quiet) {
      console.log(chalk2.bold("\nDetected AI clients needing Vreko MCP configuration:"));
      for (const client of scan.needsSetup) console.log(`  ${chalk2.cyan(client.displayName)}`);
      console.log("");
    }
    if (dryRun) {
      for (const client of scan.needsSetup) {
        console.log(chalk2.gray(`would link: ${client.displayName}`));
      }
      return;
    }
    let clientsToLink = scan.needsSetup;
    if (!applyAll && process.stdin.isTTY) {
      const { createInterface } = await import('readline');
      const rl = createInterface({
        input: process.stdin,
        output: process.stdout
      });
      const confirmed = [];
      for (const client of scan.needsSetup) {
        await new Promise((resolve11) => {
          rl.question(chalk2.cyan(`  Link Vreko MCP to ${client.displayName}? [Y/n] `), (answer) => {
            const t = answer.trim().toLowerCase();
            if (t === "" || t === "y" || t === "yes") confirmed.push(client);
            resolve11();
          });
        });
      }
      rl.close();
      clientsToLink = confirmed;
    }
    if (clientsToLink.length === 0) {
      if (!quiet) console.log(chalk2.gray("No clients selected for MCP link."));
      return;
    }
    for (const client of clientsToLink) {
      if (!quiet) process.stdout.write(`  Linking ${client.displayName}... `);
      try {
        const result7 = await linkMcpClient({
          client: client.format,
          localCliPath: process.argv[1]
        });
        if (!quiet) {
          if (result7.success) {
            console.log(chalk2.green("done"));
            console.log(formatLinkResult(result7));
          } else {
            console.log(chalk2.red(`failed: ${result7.error ?? "unknown error"}`));
          }
        }
      } catch (err6) {
        console.warn(chalk2.yellow("  Link failed for " + client.displayName + ": " + (err6 instanceof Error ? err6.message : String(err6))));
      }
    }
    if (!quiet) console.log(chalk2.bold("\nOnboarding complete."));
  });
}
__name(createOnboardCommand, "createOnboardCommand");
function createStatusCommand() {
  return new Command("status").description("Show workspace health and status").option("--json", "Output as JSON").option("--plain", "Machine-readable plain output (same as VREKO_PLAIN=1)").option("-q, --quiet", "Suppress non-essential output").action(async (options) => {
    const cwd = process.cwd();
    if (options.plain) {
      process.env.VREKO_PLAIN = "1";
    }
    if (cliState.renderMode === "json") {
      process.env.VREKO_JSON = "1";
    }
    const { isInteractive: isInteractive2 } = await import('./guards-QAFC64NO.js');
    if (isInteractive2()) {
      const { launchTui } = await import('./tui-PFFYLIOW.js');
      await launchTui({
        panel: "dashboard",
        statusFocus: true
      });
      return;
    }
    try {
      if (!await isVrekoInitialized(cwd)) {
        console.log(chalk2.yellow("\u{1F98E} Vreko not initialized in this workspace"));
        console.log(chalk2.gray("Run: vr init"));
        process.exit(1);
      }
      const status = await gatherStatus(cwd);
      const warnings = [
        ...detectPluginIncompleteWarnings(cwd),
        ...detectIngressDegradedWarnings()
      ];
      if (cliState.renderMode === "json") {
        const parsed = WorkspaceStatusOutput.parse(status);
        console.log(JSON.stringify({
          ...parsed,
          warnings
        }, null, 2));
        return;
      }
      displayStatus(status, warnings);
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(chalk2.red("Error:"), message);
      process.exit(1);
    }
  });
}
__name(createStatusCommand, "createStatusCommand");
async function gatherStatus(workspaceRoot) {
  const status = {
    initialized: true,
    loggedIn: false,
    protection: {
      count: 0,
      patterns: []
    },
    violations: {
      total: 0,
      recent: 0
    },
    snapshots: {
      count: 0,
      totalSize: "0 KB"
    },
    issues: []
  };
  status.loggedIn = await isLoggedIn();
  if (status.loggedIn) {
    const creds = await getCredentials();
    if (creds) {
      status.user = {
        email: creds.email,
        tier: creds.tier
      };
    }
  }
  let daemonClient = null;
  try {
    daemonClient = await connectToDaemon();
  } catch {
  }
  try {
    const config2 = await getWorkspaceConfig(workspaceRoot);
    if (config2) {
      status.workspace = {
        id: config2.workspaceId,
        tier: config2.tier,
        syncEnabled: config2.syncEnabled
      };
    }
  } catch {
  }
  try {
    if (daemonClient) {
      const vitals = await daemonClient.session.vitals({
        workspacePath: workspaceRoot
      });
      if (vitals) {
        status.vitals = {
          framework: vitals.framework,
          packageManager: vitals.packageManager,
          typescript: vitals.typescript?.enabled,
          typescriptStrict: vitals.typescript?.strict
        };
      }
    } else {
      const vitals = await getWorkspaceVitals(workspaceRoot);
      if (vitals) {
        status.vitals = {
          framework: vitals.framework,
          packageManager: vitals.packageManager,
          typescript: vitals.typescript?.enabled,
          typescriptStrict: vitals.typescript?.strict
        };
      }
    }
  } catch {
    const vitals = await getWorkspaceVitals(workspaceRoot);
    if (vitals) {
      status.vitals = {
        framework: vitals.framework,
        packageManager: vitals.packageManager,
        typescript: vitals.typescript?.enabled,
        typescriptStrict: vitals.typescript?.strict
      };
    }
  }
  try {
    if (daemonClient) {
      const sessionResp = await daemonClient.session.current({
        workspacePath: workspaceRoot
      });
      const sessionData = sessionResp;
      if (sessionData) {
        status.session = {
          id: sessionData.id,
          startedAt: sessionData.startedAt,
          snapshotCount: sessionData.snapshotIds?.length ?? 0
        };
      }
    } else {
      const session = await getCurrentSession(workspaceRoot);
      if (session) {
        status.session = {
          id: session.id,
          task: session.task,
          startedAt: session.startedAt,
          snapshotCount: session.snapshotCount
        };
      }
    }
  } catch {
    const session = await getCurrentSession(workspaceRoot);
    if (session) {
      status.session = {
        id: session.id,
        task: session.task,
        startedAt: session.startedAt,
        snapshotCount: session.snapshotCount
      };
    }
  }
  try {
    if (daemonClient) {
      const protectedFiles = await daemonClient.protection.listDaemon({
        workspace: workspaceRoot
      });
      status.protection = {
        count: protectedFiles?.length ?? 0,
        patterns: (protectedFiles ?? []).slice(0, 5).map((f) => f.pattern)
      };
    } else {
      const protectedFiles = await getProtectedFiles(workspaceRoot);
      status.protection = {
        count: protectedFiles.length,
        patterns: protectedFiles.slice(0, 5).map((f) => f.pattern)
      };
    }
  } catch {
    const protectedFiles = await getProtectedFiles(workspaceRoot);
    status.protection = {
      count: protectedFiles.length,
      patterns: protectedFiles.slice(0, 5).map((f) => f.pattern)
    };
  }
  try {
    if (daemonClient) {
      const violationResp = await daemonClient.violation.list({
        workspace: workspaceRoot
      });
      const violationList = violationResp?.violations ?? [];
      const oneWeekAgo = new Date(Date.now() - 7 * 24 * 60 * 60 * 1e3);
      status.violations = {
        total: violationList.length,
        recent: violationList.filter((v) => new Date(v.date) > oneWeekAgo).length
      };
    } else {
      const violations = await getViolations(workspaceRoot);
      const oneWeekAgo = new Date(Date.now() - 7 * 24 * 60 * 60 * 1e3);
      status.violations = {
        total: violations.length,
        recent: violations.filter((v) => new Date(v.date) > oneWeekAgo).length
      };
    }
  } catch {
    const violations = await getViolations(workspaceRoot);
    const oneWeekAgo = new Date(Date.now() - 7 * 24 * 60 * 60 * 1e3);
    status.violations = {
      total: violations.length,
      recent: violations.filter((v) => new Date(v.date) > oneWeekAgo).length
    };
  }
  const snapshotInfo = await getSnapshotInfo(workspaceRoot);
  status.snapshots = snapshotInfo;
  try {
    if (daemonClient && daemonClient.workspace?.status) {
      const wkStatus = await daemonClient.workspace.status({
        workspace: workspaceRoot
      });
      const profile = wkStatus?.profile;
      if (profile) {
        status.intelligence = {
          overallRisk: profile.overallRisk,
          confidence: profile.confidence,
          topDriver: profile.topDrivers?.[0]?.label || "N/A",
          snapshotFrequency: profile.recommendedConfig?.snapshotFrequency || "balanced"
        };
      }
    }
  } catch {
  }
  try {
    if (daemonClient && daemonClient.topology?.status) {
      const topStatus = await daemonClient.topology.status({
        workspace: workspaceRoot
      });
      if (topStatus?.topologyWarning) {
        status.topologyWarning = topStatus.topologyWarning;
      }
    }
  } catch {
  }
  status.issues = await detectIssues(workspaceRoot, status);
  return status;
}
__name(gatherStatus, "gatherStatus");
async function getSnapshotInfo(workspaceRoot) {
  const snapshotsDir = join(getWorkspaceDir(workspaceRoot), "snapshots");
  try {
    await access(snapshotsDir, constants.F_OK);
    const entries = await readdir(snapshotsDir, {
      withFileTypes: true
    });
    const snapDirs = entries.filter((e) => e.isDirectory());
    let totalBytes = 0;
    for (const dir of snapDirs) {
      const stats = await getDirectorySize(join(snapshotsDir, dir.name));
      totalBytes += stats;
    }
    return {
      count: snapDirs.length,
      totalSize: formatBytes2(totalBytes)
    };
  } catch {
    return {
      count: 0,
      totalSize: "0 KB"
    };
  }
}
__name(getSnapshotInfo, "getSnapshotInfo");
async function getDirectorySize(dirPath) {
  let size = 0;
  try {
    const entries = await readdir(dirPath, {
      withFileTypes: true
    });
    for (const entry of entries) {
      const fullPath = join(dirPath, entry.name);
      if (entry.isDirectory()) {
        size += await getDirectorySize(fullPath);
      } else {
        const stats = await stat(fullPath);
        size += stats.size;
      }
    }
  } catch {
  }
  return size;
}
__name(getDirectorySize, "getDirectorySize");
async function detectIssues(workspaceRoot, status) {
  const issues = [];
  if (!status.loggedIn) {
    issues.push({
      id: "not-logged-in",
      severity: "warning",
      description: "Not logged in - some features are unavailable",
      fix: "vr login"
    });
  }
  if (status.protection.count === 0) {
    issues.push({
      id: "no-protection",
      severity: "warning",
      description: "No files are protected",
      fix: "vr protect env && vr protect config"
    });
  }
  const gitignorePath = join(workspaceRoot, ".gitignore");
  try {
    const { readFile: readFile10 } = await import('fs/promises');
    const content = await readFile10(gitignorePath, "utf-8");
    if (!content.includes(".vreko")) {
      issues.push({
        id: "missing-gitignore",
        severity: "warning",
        description: ".vreko not in .gitignore",
        fix: "Add .vreko/snapshots/ to .gitignore"
      });
    }
  } catch {
  }
  if (status.session) {
    const sessionStart = new Date(status.session.startedAt);
    const hoursSinceStart = (Date.now() - sessionStart.getTime()) / (1e3 * 60 * 60);
    if (hoursSinceStart > 24) {
      issues.push({
        id: "stale-session",
        severity: "warning",
        description: `Session started ${Math.floor(hoursSinceStart)}h ago`,
        fix: "vr session end"
      });
    }
  }
  if (status.violations.recent > 5) {
    issues.push({
      id: "high-violations",
      severity: "warning",
      description: `${status.violations.recent} violations in the last week`,
      fix: "vr patterns list"
    });
  }
  return issues;
}
__name(detectIssues, "detectIssues");
var VR_INIT_WARNING = "Vreko MCP server is registered but the PostToolUse ingress hook is missing - per-edit activity will not reach the daemon. Run `vr init` to complete the install.";
function safeReadJson(path4) {
  if (!existsSync(path4)) {
    return null;
  }
  try {
    return JSON.parse(readFileSync(path4, "utf-8"));
  } catch (err6) {
    console.error(`[vr status] Failed to parse ${path4}:`, err6);
    return null;
  }
}
__name(safeReadJson, "safeReadJson");
function hasVrekoMcpServer(config2) {
  if (!config2 || typeof config2.mcpServers !== "object" || config2.mcpServers === null) {
    return false;
  }
  return Object.keys(config2.mcpServers).some((key) => key.startsWith("vreko"));
}
__name(hasVrekoMcpServer, "hasVrekoMcpServer");
function hasVrekoPostToolUseHook(settings) {
  if (!settings) {
    return false;
  }
  const postToolUse = settings.hooks?.PostToolUse;
  if (!Array.isArray(postToolUse)) {
    return false;
  }
  return postToolUse.some((entry) => (entry?.hooks ?? []).some((h) => typeof h?.command === "string" && h.command.includes("vreko-file-notify")));
}
__name(hasVrekoPostToolUseHook, "hasVrekoPostToolUseHook");
function detectPluginIncompleteWarnings(workspaceRoot) {
  const warnings = [];
  const mcpJson = safeReadJson(join(workspaceRoot, ".mcp.json"));
  const settings = safeReadJson(join(workspaceRoot, ".claude", "settings.json"));
  const mcpRegistered = hasVrekoMcpServer(mcpJson) || hasVrekoMcpServer(settings);
  const postHookPresent = hasVrekoPostToolUseHook(settings);
  if (mcpRegistered && !postHookPresent) {
    warnings.push(VR_INIT_WARNING);
  }
  return warnings;
}
__name(detectPluginIncompleteWarnings, "detectPluginIncompleteWarnings");
function ingressDegradedMarkerPath() {
  return join(homedir(), ".vreko", "daemon", "ingress-degraded");
}
__name(ingressDegradedMarkerPath, "ingressDegradedMarkerPath");
function detectIngressDegradedWarnings() {
  const markerPath = ingressDegradedMarkerPath();
  if (!existsSync(markerPath)) {
    return [];
  }
  let reason = "unknown";
  let at = "";
  try {
    const contents = readFileSync(markerPath, "utf-8").trim();
    const reasonMatch = contents.match(/reason=(\S+)/);
    if (reasonMatch) {
      reason = reasonMatch[1];
    }
    const atMatch = contents.match(/at=(\S+)/);
    if (atMatch) {
      at = atMatch[1];
    }
  } catch (err6) {
    console.error(`[vr status] Failed to read ${markerPath}:`, err6);
  }
  const when = at ? ` (last seen ${at})` : "";
  return [
    `Per-edit ingress to the daemon is degraded${when}: ${reason}. Agent edits may not be reaching Vreko. Ensure \`jq\` and \`socat\` (or \`nc\`) are installed and the daemon is running (\`vr start\`).`
  ];
}
__name(detectIngressDegradedWarnings, "detectIngressDegradedWarnings");
function displayStatus(status, warnings = []) {
  console.log(chalk2.cyan.bold("Workspace Status"));
  console.log(chalk2.gray("\u2550".repeat(40)));
  console.log();
  if (status.user) {
    console.log(chalk2.green("\u2713"), "Logged in as", chalk2.cyan(status.user.email), status.user.tier === "pro" ? chalk2.magenta("Pro \u2B50") : "");
  } else {
    console.log(chalk2.yellow("\u25CB"), "Not logged in");
  }
  if (status.workspace?.id) {
    console.log(chalk2.green("\u2713"), "Workspace:", chalk2.gray(status.workspace.id.substring(0, 8)), status.workspace.syncEnabled ? chalk2.green("(synced)") : chalk2.gray("(local)"));
  }
  console.log();
  if (status.vitals) {
    console.log(chalk2.cyan("Stack:"));
    if (status.vitals.framework) {
      console.log("  \u2022", status.vitals.framework);
    }
    if (status.vitals.packageManager) {
      console.log("  \u2022", status.vitals.packageManager);
    }
    if (status.vitals.typescript) {
      console.log("  \u2022", "TypeScript", status.vitals.typescriptStrict ? chalk2.green("(strict)") : "");
    }
    console.log();
  }
  if (status.session) {
    console.log(chalk2.cyan("Active Session:"));
    console.log("  ID:", chalk2.gray(status.session.id.substring(0, 8)));
    if (status.session.task) {
      console.log("  Task:", status.session.task);
    }
    console.log("  Snapshots:", status.session.snapshotCount);
    console.log();
  }
  if (status.intelligence) {
    console.log(chalk2.cyan("Intelligence:"));
    console.log("  Risk:", status.intelligence.overallRisk);
    console.log("  Top driver:", status.intelligence.topDriver);
    console.log();
  }
  console.log(chalk2.cyan("Stats:"));
  console.log("  Protected files:", status.protection.count);
  console.log("  Snapshots:", status.snapshots.count, chalk2.gray(`(${status.snapshots.totalSize})`));
  console.log("  Violations:", status.violations.total, chalk2.gray(`(${status.violations.recent} this week)`));
  console.log();
  if (status.issues.length > 0) {
    console.log(chalk2.yellow("Issues:"));
    for (const issue of status.issues) {
      const icon = issue.severity === "error" ? chalk2.red("\u2717") : chalk2.yellow("\u26A0");
      console.log(` ${icon}`, issue.description);
      if (issue.fix) {
        console.log(chalk2.gray(`    Fix: ${issue.fix}`));
      }
    }
  } else {
    console.log(chalk2.green("\u2713"), "No issues detected");
  }
  if (warnings.length > 0) {
    console.log(`
${chalk2.yellow("Warnings:")}`);
    for (const warning of warnings) {
      console.log(` ${chalk2.yellow("\u26A0")}`, warning);
    }
  }
}
__name(displayStatus, "displayStatus");
function formatBytes2(bytes) {
  if (bytes === 0) {
    return "0 KB";
  }
  const units = [
    "B",
    "KB",
    "MB",
    "GB"
  ];
  const exp = Math.min(Math.floor(Math.log(bytes) / Math.log(1024)), units.length - 1);
  const size = bytes / 1024 ** exp;
  return `${size.toFixed(exp > 0 ? 1 : 0)} ${units[exp]}`;
}
__name(formatBytes2, "formatBytes");

// src/utils/print.ts
function print(...args) {
  if (args.length === 0) {
    process.stdout.write("\n");
  } else {
    process.stdout.write(`${args.map(String).join(" ")}
`);
  }
}
__name(print, "print");

// src/commands/completion.ts
var __dirname$1 = fileURLToPath(new URL(".", import.meta.url));
async function loadCompletionScript(shell) {
  const resourcesDir = join(__dirname$1, "../../resources/completions");
  const filename = `snap.${shell}`;
  try {
    return await readFile(join(resourcesDir, filename), "utf-8");
  } catch {
    return null;
  }
}
__name(loadCompletionScript, "loadCompletionScript");
function createCompletionCommand() {
  return new Command("completion").description("Generate shell completion scripts").argument("<shell>", "Shell type: bash, zsh, or fish").addHelpText("after", `
Examples:
  ${chalk2.gray("# Bash (add to ~/.bashrc)")}
  ${chalk2.cyan("source <(vr completion bash)")}

  ${chalk2.gray("# Zsh (add to ~/.zshrc)")}
  ${chalk2.cyan("source <(vr completion zsh)")}

  ${chalk2.gray("# Fish")}
  ${chalk2.cyan("vr completion fish > ~/.config/fish/completions/vr.fish")}
`).action(async (shell) => {
    const shellLower = shell.toLowerCase();
    const validShells = [
      "bash",
      "zsh",
      "fish"
    ];
    if (!validShells.includes(shellLower)) {
      process.exit(1);
    }
    const script = await loadCompletionScript(shellLower);
    if (!script) {
      process.exit(1);
    }
    print(script);
  });
}
__name(createCompletionCommand, "createCompletionCommand");

// src/services/intelligence-service.ts
var DaemonIntelligenceProxy = class DaemonIntelligenceProxy2 {
  static {
    __name(this, "DaemonIntelligenceProxy");
  }
  workspaceRoot;
  constructor(workspaceRoot) {
    this.workspaceRoot = workspaceRoot;
  }
  async getContext(params) {
    const client = await connectToDaemon();
    return client.context.get({
      workspace: this.workspaceRoot,
      task: params.task ?? "",
      keywords: params.keywords,
      files: params.files
    });
  }
  async validateCode(code, filePath) {
    const client = await connectToDaemon();
    const result7 = await client.validation.comprehensive({
      workspace: this.workspaceRoot,
      filePath,
      code
    });
    return {
      passed: result7.passed,
      confidence: result7.confidence,
      totalIssues: result7.totalIssues,
      recommendation: result7.recommendation
    };
  }
  async reportViolation(params) {
    const client = await connectToDaemon();
    const result7 = await client.violation.report({
      workspace: this.workspaceRoot,
      type: params.type,
      file: params.file,
      whatHappened: params.message,
      whyItHappened: params.reason,
      prevention: params.prevention
    });
    return {
      recorded: result7.reported,
      // Use a composite ID from type and file since daemon returns file path
      violationId: `${params.type}:${params.file}`
    };
  }
  async getLearningStats() {
    const client = await connectToDaemon();
    const learnings = await client.learning.list({
      workspace: this.workspaceRoot
    });
    return {
      totalLearnings: learnings.total,
      totalViolations: 0,
      byType: {}
    };
  }
  async getStats() {
    return this.getLearningStats();
  }
  async getViolationsSummary() {
    return {
      total: 0,
      byType: {},
      byFile: {}
    };
  }
  async dispose() {
  }
};
var instances = /* @__PURE__ */ new Map();
async function getIntelligence(workspaceRoot) {
  const cwd = workspaceRoot || process.cwd();
  if (!await isVrekoInitialized(cwd)) {
    throw new Error("Vreko not initialized. Run: vr init");
  }
  if (instances.has(cwd)) {
    const cached = instances.get(cwd);
    if (cached) {
      return cached;
    }
  }
  const proxy = new DaemonIntelligenceProxy(cwd);
  instances.set(cwd, proxy);
  return proxy;
}
__name(getIntelligence, "getIntelligence");
async function getIntelligenceWithSemantic(workspaceRoot) {
  return getIntelligence(workspaceRoot);
}
__name(getIntelligenceWithSemantic, "getIntelligenceWithSemantic");

// src/commands/context.ts
function createContextCommand() {
  const context = new Command("context").description("Get relevant context before starting work").argument("[task]", "Description of what you want to implement").option("-f, --files <files...>", "Files you plan to modify").option("-k, --keywords <keywords...>", "Keywords to search for").option("--json", "Output as JSON").option("--semantic", "Use semantic search (slower, more accurate)").action(async (task, options) => {
    await handleContextCommand(task, options);
  });
  return context;
}
__name(createContextCommand, "createContextCommand");
async function handleContextCommand(task, options) {
  const cwd = process.cwd();
  try {
    const intelligence = options.semantic ? await getIntelligenceWithSemantic(cwd) : await getIntelligence(cwd);
    const contextInput = {
      task: task || "general development",
      files: options.files || [],
      keywords: options.keywords || extractKeywords(task)
    };
    const result7 = await intelligence.getContext(contextInput);
    if (options.json) {
      console.log(JSON.stringify(result7, null, 2));
      return;
    }
    displayContextResults(result7, options.semantic);
  } catch (error) {
    const message = error instanceof Error ? error.message : String(error);
    if (message.includes("not initialized")) {
      process.exit(1);
    }
    process.exit(1);
  }
}
__name(handleContextCommand, "handleContextCommand");
function displayContextResults(result7, usedSemantic) {
  const summaryContent = formatContextSummary(result7, usedSemantic);
  if (summaryContent) {
    console.log(summaryContent);
  }
  if (result7.learnings && result7.learnings.length > 0) {
    console.log("\nLearnings:");
    for (const learning of result7.learnings.slice(0, 5)) {
      let content;
      if (typeof learning === "string") {
        content = learning;
      } else {
        const l = learning;
        content = l.trigger && l.action ? `${l.trigger} \u2192 ${l.action}` : l.content ?? String(learning);
      }
      console.log(`  \u2022 ${content.slice(0, 100)}${content.length > 100 ? "..." : ""}`);
    }
    if (result7.learnings.length > 5) {
      console.log(`  \u2026 and ${result7.learnings.length - 5} more`);
    }
  }
  if (result7.patterns && result7.patterns.length > 0) {
    console.log("\nPatterns:");
    for (const pattern of result7.patterns.slice(0, 3)) {
      const text2 = typeof pattern === "string" ? pattern : pattern.pattern ?? String(pattern);
      console.log(`  \u2022 ${text2}`);
    }
  }
}
__name(displayContextResults, "displayContextResults");
function formatContextSummary(result7, usedSemantic) {
  const parts = [];
  if (result7.patterns?.length) {
    parts.push(`${chalk2.bold("Patterns:")} ${result7.patterns.length} found`);
  }
  if (result7.constraints?.length) {
    parts.push(`${chalk2.bold("Constraints:")} ${result7.constraints.length} rules`);
  }
  if (result7.learnings?.length) {
    parts.push(`${chalk2.bold("Learnings:")} ${result7.learnings.length} relevant`);
  }
  if (result7.files?.length) {
    parts.push(`${chalk2.bold("Files:")} ${result7.files.length} indexed`);
  }
  if (usedSemantic) {
    parts.push(`${chalk2.bold("Semantic:")} enabled`);
  }
  if (parts.length === 0) {
    parts.push("No specific context found for this task.");
    parts.push("Try adding --keywords to refine the search.");
  }
  return parts.join("\n");
}
__name(formatContextSummary, "formatContextSummary");
function extractKeywords(task) {
  if (!task) {
    return [];
  }
  const stopWords = /* @__PURE__ */ new Set([
    "the",
    "a",
    "an",
    "to",
    "for",
    "of",
    "in",
    "on",
    "with",
    "and",
    "or",
    "is",
    "are",
    "it",
    "this",
    "that"
  ]);
  return task.toLowerCase().split(/\s+/).filter((word) => word.length > 2 && !stopWords.has(word)).slice(0, 5);
}
__name(extractKeywords, "extractKeywords");
function createStatsCommand() {
  const stats = new Command("stats").description("Show learning statistics").option("--json", "Output as JSON").action(async (options) => {
    await handleStatsCommand(options);
  });
  return stats;
}
__name(createStatsCommand, "createStatsCommand");
async function handleStatsCommand(options) {
  const cwd = process.cwd();
  try {
    const intelligence = await getIntelligence(cwd);
    const learningStats = await intelligence.getStats();
    const violationsSummary = await intelligence.getViolationsSummary();
    if (options.json) {
      console.log(JSON.stringify({
        learningStats,
        violationsSummary
      }, null, 2));
      return;
    }
    displayStatsResults(learningStats, violationsSummary);
  } catch (error) {
    const message = error instanceof Error ? error.message : String(error);
    if (message.includes("not initialized")) {
      console.log(chalk2.yellow("\u{1F98E} Vreko not initialized"));
      console.log(chalk2.gray("Run: vr init"));
      process.exit(1);
    }
    console.error(chalk2.red("Error:"), message);
    process.exit(1);
  }
}
__name(handleStatsCommand, "handleStatsCommand");
function displayStatsResults(learningStats, violationsSummary) {
  console.log(chalk2.cyan.bold("Learning Statistics"));
  console.log();
  console.log(`  Total Learnings:  ${learningStats.totalLearnings}`);
  console.log(`  Total Violations: ${learningStats.totalViolations}`);
  console.log(`  Types:            ${Object.keys(learningStats.byType).length}`);
  console.log();
  const violationTypes = Object.entries(violationsSummary.byType);
  if (violationTypes.length > 0) {
    const table = new Table({
      head: [
        chalk2.cyan("Type"),
        chalk2.cyan("Count")
      ],
      style: {
        head: [],
        border: []
      }
    });
    for (const [type, count] of violationTypes.slice(0, 10)) {
      table.push([
        type,
        String(count)
      ]);
    }
    console.log(table.toString());
    if (violationTypes.length > 10) {
      console.log(chalk2.gray(`  ... and ${violationTypes.length - 10} more`));
    }
  } else {
    console.log(chalk2.green("\u2713"), "No violations recorded");
    console.log(chalk2.gray('  Record with: vr learn "trigger" "action"'));
  }
}
__name(displayStatsResults, "displayStatsResults");
var GitNotInstalledError = class extends Error {
  static {
    __name(this, "GitNotInstalledError");
  }
  constructor() {
    super("Git is not installed or not accessible in PATH");
    this.name = "GitNotInstalledError";
  }
};
var GitNotRepositoryError = class extends Error {
  static {
    __name(this, "GitNotRepositoryError");
  }
  constructor(path4) {
    super(`Not a git repository: ${path4}`);
    this.name = "GitNotRepositoryError";
  }
};
var GitBinaryFileError = class extends Error {
  static {
    __name(this, "GitBinaryFileError");
  }
  constructor(path4) {
    super(`Cannot read binary file: ${path4}`);
    this.name = "GitBinaryFileError";
  }
};
var GitClient = class {
  static {
    __name(this, "GitClient");
  }
  cwd;
  timeout;
  constructor(options = {}) {
    this.cwd = options.cwd || process.cwd();
    this.timeout = options.timeout || 1e4;
  }
  /**
  * Check if git is installed
  */
  async isGitInstalled() {
    try {
      await execa("git", [
        "--version"
      ], {
        timeout: 5e3
      });
      return true;
    } catch {
      return false;
    }
  }
  /**
  * Check if cwd is inside a git repository
  */
  async isGitRepository() {
    try {
      await execa("git", [
        "rev-parse",
        "--is-inside-work-tree"
      ], {
        cwd: this.cwd,
        timeout: this.timeout
      });
      return true;
    } catch {
      return false;
    }
  }
  /**
  * Get repository root path
  */
  async getRepositoryRoot() {
    const { stdout } = await execa("git", [
      "rev-parse",
      "--show-toplevel"
    ], {
      cwd: this.cwd,
      timeout: this.timeout
    });
    return stdout.trim();
  }
  /**
  * Get list of staged files with their status
  */
  async getStagedFiles() {
    try {
      const { stdout } = await execa("git", [
        "diff",
        "--cached",
        "--name-status"
      ], {
        cwd: this.cwd,
        timeout: this.timeout
      });
      if (!stdout.trim()) {
        return [];
      }
      return stdout.split(/\r?\n/).filter(Boolean).map((line) => this.parseStatusLine(line));
    } catch (error) {
      if (this.isNotRepositoryError(error)) {
        throw new GitNotRepositoryError(this.cwd);
      }
      throw error;
    }
  }
  /**
  * Get staged file content (what will be committed)
  */
  async getStagedContent(filePath) {
    try {
      const { stdout } = await execa("git", [
        "show",
        `:${filePath}`
      ], {
        cwd: this.cwd,
        timeout: this.timeout
      });
      return stdout;
    } catch (error) {
      if (this.isBinaryFileError(error)) {
        throw new GitBinaryFileError(filePath);
      }
      throw error;
    }
  }
  /**
  * Get diff of staged changes for a file
  */
  async getStagedDiff(filePath) {
    const args = [
      "diff",
      "--cached",
      "--color=always"
    ];
    if (filePath) {
      args.push("--", filePath);
    }
    const { stdout } = await execa("git", args, {
      cwd: this.cwd,
      timeout: this.timeout
    });
    return stdout;
  }
  /**
  * Get diff statistics
  */
  async getStagedStats() {
    const { stdout } = await execa("git", [
      "diff",
      "--cached",
      "--numstat"
    ], {
      cwd: this.cwd,
      timeout: this.timeout
    });
    let additions = 0;
    let deletions = 0;
    let files = 0;
    for (const line of stdout.split(/\r?\n/).filter(Boolean)) {
      const [add, del] = line.split("	");
      if (add !== "-") {
        additions += Number.parseInt(add, 10);
      }
      if (del !== "-") {
        deletions += Number.parseInt(del, 10);
      }
      files++;
    }
    return {
      additions,
      deletions,
      files
    };
  }
  /**
  * Get current branch name
  */
  async getCurrentBranch() {
    const { stdout } = await execa("git", [
      "branch",
      "--show-current"
    ], {
      cwd: this.cwd,
      timeout: this.timeout
    });
    return stdout.trim();
  }
  /**
  * Get short commit hash of HEAD
  */
  async getHeadCommit() {
    const { stdout } = await execa("git", [
      "rev-parse",
      "--short",
      "HEAD"
    ], {
      cwd: this.cwd,
      timeout: this.timeout
    });
    return stdout.trim();
  }
  // ===========================================================================
  // PRIVATE HELPERS
  // ===========================================================================
  parseStatusLine(line) {
    const [status, ...pathParts] = line.split("	");
    const path4 = pathParts[pathParts.length - 1];
    const oldPath = pathParts.length > 1 ? pathParts[0] : void 0;
    return {
      path: path4,
      status: this.parseStatus(status),
      ...oldPath && {
        oldPath
      }
    };
  }
  parseStatus(status) {
    const char = status.charAt(0);
    switch (char) {
      case "A":
        return "added";
      case "M":
        return "modified";
      case "D":
        return "deleted";
      case "R":
        return "renamed";
      case "C":
        return "copied";
      default:
        return "modified";
    }
  }
  isNotRepositoryError(error) {
    return error instanceof Error && error.message.toLowerCase().includes("not a git repository");
  }
  isBinaryFileError(error) {
    return error instanceof Error && error.message.toLowerCase().includes("binary");
  }
};
function isCodeFile(filePath) {
  const codeExtensions = [
    ".ts",
    ".tsx",
    ".js",
    ".jsx",
    ".mjs",
    ".cjs",
    ".py",
    ".java",
    ".go",
    ".rs",
    ".rb",
    ".php",
    ".c",
    ".cpp",
    ".h",
    ".hpp",
    ".cs",
    ".swift",
    ".kt",
    ".scala",
    ".clj",
    ".ex",
    ".exs"
  ];
  return codeExtensions.some((ext) => filePath.endsWith(ext));
}
__name(isCodeFile, "isCodeFile");
var ProgressTracker = class {
  static {
    __name(this, "ProgressTracker");
  }
  current = 0;
  total;
  label;
  quiet;
  showElapsed;
  startTime;
  spinner = null;
  isTTY;
  constructor(options) {
    this.total = options.total;
    this.label = options.label || "Processing";
    this.quiet = options.quiet || false;
    this.showElapsed = options.showElapsed ?? true;
    this.startTime = Date.now();
    this.isTTY = process.stdout.isTTY ?? false;
  }
  /**
  * Start the progress tracker
  */
  start() {
    if (this.quiet) {
      return;
    }
    if (this.isTTY) {
      this.spinner = ora({
        text: this.formatProgress(""),
        spinner: "dots"
      }).start();
    } else {
      console.log(this.formatProgress(""));
    }
  }
  /**
  * Update progress with current item
  */
  update(currentItem) {
    this.current++;
    if (this.quiet) {
      return;
    }
    const progressText = this.formatProgress(currentItem);
    if (this.isTTY && this.spinner) {
      this.spinner.text = progressText;
    } else {
      if (this.current % 10 === 0 || this.current === this.total) {
        console.log(progressText);
      }
    }
  }
  /**
  * Complete the progress with final message
  */
  complete(message) {
    if (this.quiet) {
      return;
    }
    if (this.isTTY && this.spinner) {
      this.spinner.succeed(message);
    } else {
      console.log(message);
    }
  }
  /**
  * Fail the progress with error message
  */
  fail(message) {
    if (this.isTTY && this.spinner) {
      this.spinner.fail(message);
    } else {
      console.error(message);
    }
  }
  /**
  * Get elapsed time string
  */
  getElapsed() {
    const elapsed = Date.now() - this.startTime;
    if (elapsed < 1e3) {
      return `${elapsed}ms`;
    }
    return `${(elapsed / 1e3).toFixed(1)}s`;
  }
  /**
  * Get current progress count
  */
  getCurrent() {
    return this.current;
  }
  /**
  * Get total count
  */
  getTotal() {
    return this.total;
  }
  formatProgress(currentItem) {
    const counter = chalk2.cyan(`[${this.current}/${this.total}]`);
    const item = currentItem ? chalk2.gray(this.truncateItem(currentItem, 40)) : "";
    const elapsed = this.showElapsed && Date.now() - this.startTime > 2e3 ? chalk2.dim(` (${this.getElapsed()})`) : "";
    return `${this.label} ${counter} ${item}${elapsed}`;
  }
  truncateItem(item, maxLength) {
    if (item.length <= maxLength) {
      return item;
    }
    return `...${item.slice(-(maxLength - 3))}`;
  }
};

// src/commands/validate.ts
function createValidateCommand() {
  const validate = new Command("validate").description("Run validation pipeline on code").argument("[file]", "File to validate").option("-a, --all", "Validate all staged files").option("-q, --quiet", "Only output if issues found").option("--json", "Output as JSON").action(async (file, options) => {
    await handleValidateCommand(file, options);
  });
  return validate;
}
__name(createValidateCommand, "createValidateCommand");
async function handleValidateCommand(file, options) {
  const cwd = process.cwd();
  try {
    const intelligence = await getIntelligence(cwd);
    const filesToValidate = await getFilesToValidate(file, options.all, cwd);
    if (filesToValidate.length === 0) {
      if (!options.quiet) {
        console.log(chalk2.yellow("No files to validate"));
        console.log(chalk2.gray("Usage: vr validate <file> or vr validate --all"));
      }
      return;
    }
    const progress = new ProgressTracker({
      total: filesToValidate.length,
      label: "Validating",
      quiet: options.quiet
    });
    progress.start();
    const results = [];
    let hasErrors = false;
    let progressCompleted = false;
    const warnings = [];
    const serviceClient = createServiceClient();
    try {
      await connectServiceClient(serviceClient);
      const learningResult = await serviceClient.learning.evaluate({
        workspace: cwd,
        commandName: "validate",
        filesOrPaths: filesToValidate,
        intent: "review",
        mode: "warn"
      });
      if (learningResult.applicable && learningResult.applicable.length > 0) {
        for (const learning of learningResult.applicable) {
          warnings.push({
            title: learning.trigger || "Warning",
            message: learning.action || "Check code for potential issues",
            severity: "warning"
          });
        }
      }
    } catch {
    } finally {
      serviceClient.close();
    }
    try {
      for (const filePath of filesToValidate) {
        progress.update(filePath);
        try {
          const content = await readFile(resolve(cwd, filePath), "utf-8");
          const pipelineResult = await intelligence.validateCode(content, filePath);
          const recommendation = pipelineResult.passed ? "auto_merge" : "full_review";
          results.push({
            file: filePath,
            passed: pipelineResult.passed,
            confidence: pipelineResult.confidence,
            issues: pipelineResult.totalIssues,
            recommendation
          });
          if (!pipelineResult.passed) {
            hasErrors = true;
          }
        } catch {
          results.push({
            file: filePath,
            passed: false,
            confidence: 0,
            issues: 1,
            recommendation: "error"
          });
          hasErrors = true;
        }
      }
      const passedCount = results.filter((r) => r.passed).length;
      if (hasErrors) {
        progress.fail(`${passedCount}/${results.length} files passed`);
      } else {
        progress.complete(`All ${results.length} files passed validation`);
      }
      progressCompleted = true;
      if (options.json) {
        console.log(JSON.stringify(results, null, 2));
      } else if (!options.quiet || hasErrors) {
        displayValidationResults(results, hasErrors, warnings);
      }
      process.exit(hasErrors ? 1 : 0);
    } finally {
      if (!progressCompleted) {
        progress.fail("Validation interrupted");
      }
    }
  } catch (error) {
    const message = error instanceof Error ? error.message : String(error);
    if (message.includes("not initialized")) {
      console.log(chalk2.yellow("\u{1F98E} Vreko not initialized"));
      console.log(chalk2.gray("Run: vr init"));
      process.exit(1);
    }
    console.error(chalk2.red("Error:"), message);
    process.exit(1);
  }
}
__name(handleValidateCommand, "handleValidateCommand");
async function getFilesToValidate(file, all, cwd) {
  if (file) {
    return [
      file
    ];
  }
  if (all) {
    try {
      const git = new GitClient({
        cwd
      });
      if (!await git.isGitInstalled()) {
        return [];
      }
      if (!await git.isGitRepository()) {
        return [];
      }
      const stagedFiles = await git.getStagedFiles();
      return stagedFiles.filter((f) => f.status !== "deleted" && isCodeFile(f.path)).map((f) => f.path);
    } catch {
      return [];
    }
  }
  return [];
}
__name(getFilesToValidate, "getFilesToValidate");
function displayValidationResults(results, hasErrors, warnings = []) {
  if (warnings.length > 0) {
    for (const warning of warnings) {
      const warningType = warning.severity === "error" ? "error" : "warning";
      const icon = warningType === "error" ? chalk2.red("\u2717") : chalk2.yellow("\u26A0");
      console.log(`${icon} ${warning.title}: ${warning.message}`);
    }
  }
  if (hasErrors) {
    const failedFiles = results.filter((r) => !r.passed);
    console.log(chalk2.red(`
\u2717 ${failedFiles.length} file(s) failed validation:`));
    for (const f of failedFiles) {
      console.log(`  ${chalk2.bold(f.file)}: ${f.issues} issue${f.issues !== 1 ? "s" : ""}`);
    }
  }
}
__name(displayValidationResults, "displayValidationResults");
function createMetricsCommand() {
  const metrics = new Command("metrics").alias("m").description("Show momentum scores for files\n\nExamples:\n  $ vr metrics              Show top 10 critical files\n  $ vr metrics src/auth.ts  Score specific file\n  $ vr metrics --all        Show all files (not only critical)\n  $ vr metrics --json       JSON output\n  $ vr metrics --top 20     Show top 20 files\n\nRelated Commands:\n  vr sync     Collect signals before scoring\n  vr refresh  Incremental score updates").argument("[path]", "Specific file to score").option("--all", "Show all files, not only critical (>0.7 score)").option("--json", "Output as JSON for programmatic use").option("--top <n>", "Top N files to show", "10").option("--verbose", "Show verbose signal breakdown").action(async (path4, options) => {
    await handleMetricsCommand(path4, options);
  });
  return metrics;
}
__name(createMetricsCommand, "createMetricsCommand");
async function handleMetricsCommand(targetPath, options) {
  const cwd = process.cwd();
  if (!await isVrekoInitialized(cwd)) {
    process.exit(1);
  }
  if (options.verbose) {
    console.log(chalk2.gray("Connecting to service..."));
  }
  await withDaemon("metrics", async (client) => {
    const status = await client.momentum.status({
      workspace: cwd
    });
    let result7;
    if (targetPath) {
      const scoreResponse = await client.momentum.score({
        workspace: cwd,
        filePath: targetPath
      });
      result7 = {
        filePath: scoreResponse.filePath,
        score: scoreResponse.score,
        factors: scoreResponse.factors,
        timestamp: scoreResponse.timestamp
      };
    }
    if (options.json) {
      console.log(JSON.stringify(result7 ?? status, null, 2));
      return;
    }
    if (result7) {
      displaySingleFileMetrics(result7);
    } else {
      displayStatus2(status);
    }
  });
}
__name(handleMetricsCommand, "handleMetricsCommand");
function displayStatus2(status) {
  console.log(chalk2.cyan.bold("Momentum Status"));
  console.log();
  console.log(`  Active: ${status.active ? chalk2.green("yes") : chalk2.yellow("no")}`);
  console.log(`  Last sync: ${status.lastSync ?? chalk2.gray("never")}`);
  console.log(`  Files tracked: ${status.fileCount}`);
  console.log(`  Average score: ${status.averageScore.toFixed(2)}`);
}
__name(displayStatus2, "displayStatus");
function displaySingleFileMetrics(result7) {
  const isCritical = result7.score > 0.7;
  console.log(`  Score: ${result7.score.toFixed(2)}${isCritical ? " (critical)" : ""}`);
}
__name(displaySingleFileMetrics, "displaySingleFileMetrics");
function createRefreshCommand() {
  const refresh = new Command("refresh").description("Incrementally update momentum signals").option("--since <commit>", "Compare against specific commit").option("--full", "Full re-fit of normalizers").option("--json", "Output as JSON").option("--verbose", "Show verbose output").action(async (options) => {
    await handleRefreshCommand(options);
  });
  return refresh;
}
__name(createRefreshCommand, "createRefreshCommand");
async function handleRefreshCommand(options) {
  const cwd = process.cwd();
  const startTime = Date.now();
  if (!await isVrekoInitialized(cwd)) {
    console.error("Vreko not initialized in this workspace. Run 'vreko init' first.");
    process.exit(1);
  }
  if (options.verbose) {
    console.log(`Refreshing workspace: ${cwd}`);
  }
  return withDaemon("refresh", async (client) => {
    const response = await client.momentum.refresh({
      workspace: cwd,
      since: options.since,
      full: options.full
    });
    const durationMs = Date.now() - startTime;
    const result7 = {
      refreshed: response.refreshed,
      since: response.since,
      full: response.full,
      timestamp: response.timestamp,
      durationMs
    };
    if (options.json) {
      console.log(JSON.stringify(result7, null, 2));
      return;
    }
    displayRefreshResults(result7);
  });
}
__name(handleRefreshCommand, "handleRefreshCommand");
function displayRefreshResults(result7) {
  const durationSec = (result7.durationMs / 1e3).toFixed(1);
  console.log(`\u2713 Refresh complete in ${durationSec}s`);
  if (result7.since) {
    console.log(`  Since: ${new Date(result7.since).toLocaleString()}`);
  }
  if (result7.full) {
    console.log("  Mode: full refresh");
  }
}
__name(displayRefreshResults, "displayRefreshResults");
function createSyncCommand() {
  const sync = new Command("sync").alias("s").description("Collect signals and fit momentum scoring normalizers\n\nExamples:\n  $ vr sync                    Full sync with all collectors\n  $ vr sync --quick            Quick sync (git + AST only)\n  $ vr sync --json             JSON output\n  $ vr sync src/auth.ts        Analyze specific file\n  $ vr sync --since HEAD~30    Last 30 days\n\nRelated Commands:\n  vr metrics    Show momentum scores after sync\n  vr refresh    Incremental updates").argument("[paths...]", "Specific files/dirs to analyze").option("--quick", "Quick mode (git + AST only, ~5x faster)").option("--json", "Output as JSON for programmatic use").option("--verbose", "Show detailed progress and signal breakdown").option("--since <ref>", "Analyze commits since ref (default: 90 days)", "HEAD~90").action(async (paths, options) => {
    await handleSyncCommand(paths, options);
  });
  return sync;
}
__name(createSyncCommand, "createSyncCommand");
async function handleSyncCommand(paths, options) {
  const cwd = process.cwd();
  const startTime = Date.now();
  if (!await isVrekoInitialized(cwd)) {
    console.error("Vreko not initialized in this workspace. Run 'vreko init' first.");
    process.exit(1);
  }
  if (options.verbose) {
    console.log(chalk2.gray("Connecting to service..."));
  }
  if (!options.json) {
    console.log(chalk2.gray("  Credit estimate: ~1 credit (memory_sync). Check vr credits for your balance."));
  }
  if (options.verbose) {
    if (paths.length > 0) {
      console.log(chalk2.gray(`Syncing paths: ${paths.join(", ")}`));
    }
  }
  await withDaemon("sync", async (client) => {
    const response = await client.momentum.sync({
      workspace: cwd,
      quick: options.quick,
      ...paths.length > 0 && {
        paths
      },
      ...options.since && {
        since: options.since
      }
    });
    const durationMs = Date.now() - startTime;
    const result7 = {
      filesAnalyzed: response.filesAnalyzed,
      quick: response.quick,
      timestamp: response.timestamp,
      durationMs
    };
    if (options.json) {
      console.log(JSON.stringify(result7, null, 2));
      return;
    }
    displaySyncResults(result7);
  });
}
__name(handleSyncCommand, "handleSyncCommand");
function displaySyncResults(result7) {
  const durationSec = (result7.durationMs / 1e3).toFixed(1);
  console.log(`\u2713 Sync complete in ${durationSec}s`);
}
__name(displaySyncResults, "displaySyncResults");
function analyzeDeduplication(_cwd, _threshold) {
  throw new Error("Learning consolidation is temporarily disabled. Phase 3B will re-enable via service IPC.");
}
__name(analyzeDeduplication, "analyzeDeduplication");
async function consolidateLearnings(_cwd, _options) {
  throw new Error("Learning consolidation is temporarily disabled. Phase 3B will re-enable via service IPC.");
}
__name(consolidateLearnings, "consolidateLearnings");
function createConsolidateCommand() {
  const consolidate = new Command("consolidate").description("Consolidate duplicate learnings (Phase 1 deduplication)").option("--threshold <number>", "Similarity threshold (0-1, enriched score)", "0.65").option("--dry-run", "Preview without writing", false).option("--max <number>", "Max learnings to process (safety limit)", "5000").option("--json", "Output machine-readable JSON", false).option("--analyze", "Only analyze duplication potential (no consolidation)", false).action(async (options) => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        console.log(chalk2.yellow("\u{1F98E} Vreko not initialized"));
        console.log(chalk2.gray("Run: vr init"));
        process.exit(1);
      }
      const threshold = Number.parseFloat(options.threshold);
      const maxLearnings = Number.parseInt(options.max, 10);
      if (options.analyze) {
        const spinner3 = ora("Analyzing deduplication potential...").start();
        const analysis = analyzeDeduplication(cwd, threshold);
        spinner3.succeed("Analysis complete");
        if (options.json) {
          console.log(JSON.stringify(analysis, null, 2));
          return;
        }
        console.log(chalk2.cyan.bold("Deduplication Analysis"));
        console.log();
        console.log(`  Total learnings: ${analysis.totalLearnings}`);
        console.log(`  Estimated duplicates: ${analysis.estimatedDuplicates}`);
        console.log(`  Estimated unique: ${analysis.estimatedUnique}`);
        console.log(`  Reduction: ${analysis.reductionPercent.toFixed(1)}%`);
        if (analysis.topClusters.length > 0) {
          console.log();
          console.log(chalk2.cyan("Top clusters:"));
          for (const cluster of analysis.topClusters) {
            console.log(`  \u2022 ${cluster.canonical} (${cluster.size} entries)`);
          }
        }
        if (analysis.estimatedDuplicates > 50) {
          console.log();
          console.log(chalk2.yellow("High duplication detected. Run without --analyze to consolidate."));
        }
        return;
      }
      const spinner2 = ora("Finding duplicate learnings...").start();
      const result7 = await consolidateLearnings(cwd, {
        threshold,
        dryRun: options.dryRun,
        maxLearnings
      });
      spinner2.succeed("Consolidation complete");
      if (options.json) {
        console.log(JSON.stringify(result7, null, 2));
        return;
      }
      console.log(chalk2.cyan.bold("Consolidation Results"));
      console.log();
      console.log(`  Original: ${result7.originalCount}`);
      console.log(`  Consolidated: ${result7.consolidatedCount}`);
      console.log(`  Duplicates found: ${result7.duplicatesFound}`);
      console.log(`  Reduction: ${result7.reductionPercent.toFixed(1)}%`);
      if (result7.topClusters.length > 0) {
        console.log();
        console.log(chalk2.cyan("Top clusters:"));
        for (const cluster of result7.topClusters) {
          console.log(`  \u2022 ${cluster.canonical} (${cluster.size} entries)`);
        }
      }
      if (options.dryRun) {
        console.log();
        console.log(chalk2.yellow("Dry run  -  no changes written. Run without --dry-run to apply."));
      } else {
        console.log();
        console.log(chalk2.green("\u2713"), `Archive saved to: ${result7.archivePath}`);
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(chalk2.red("Error:"), message);
      process.exit(1);
    }
  });
  return consolidate;
}
__name(createConsolidateCommand, "createConsolidateCommand");
function createLearnCommand() {
  const learn = new Command("learn").description("Record learnings for future reference").argument("<trigger>", "When to apply this learning (keyword or situation)").argument("<action>", "What to do when triggered").option("-t, --type <type>", "Learning type: pattern, pitfall, efficiency, discovery, workflow", "pattern").option("-s, --source <source>", "Where this learning came from", "cli").action(async (trigger, action, options) => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        console.log(chalk2.yellow("\u{1F98E} Vreko not initialized in this workspace"));
        console.log(chalk2.gray("Run: vr init"));
        process.exit(1);
      }
      const validTypes = [
        "pattern",
        "pitfall",
        "efficiency",
        "discovery",
        "workflow"
      ];
      if (!validTypes.includes(options.type)) {
        console.log(chalk2.red(`Invalid type: ${options.type}`));
        console.log(chalk2.gray(`Valid types: ${validTypes.join(", ")}`));
        process.exit(1);
      }
      console.log(chalk2.gray("  Credit estimate: ~1 credit (memory_sync). Learning stored locally first, synced on next vr sync."));
      const learning = {
        id: generateId("L"),
        type: options.type,
        trigger,
        action,
        source: options.source,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      };
      let storedViaDaemon = false;
      if (await isDaemonAvailable()) {
        try {
          const client = await connectToDaemon();
          await client.learning.add({
            workspace: cwd,
            type: learning.type,
            trigger: learning.trigger,
            action: learning.action,
            source: learning.source ?? "cli"
          });
          storedViaDaemon = true;
        } catch {
        }
      }
      if (!storedViaDaemon) {
        await recordLearning(learning, cwd);
      }
      console.log(chalk2.green("\u2713"), "Learning recorded:", chalk2.cyan(trigger), "\u2192", action);
      console.log();
      console.log(`  ${chalk2.cyan("Type:")}    ${formatType(learning.type)}`);
      console.log(`  ${chalk2.cyan("Trigger:")} ${trigger}`);
      console.log(`  ${chalk2.cyan("Action:")}  ${action}`);
      console.log();
      if (!storedViaDaemon) {
        const daemonRunning = await isDaemonAvailable();
        if (daemonRunning) {
          console.log(chalk2.yellow("   (Stored locally  -  service IPC error: storage will sync on next session)"));
        } else {
          console.log(chalk2.gray("   (Stored locally  -  start service for unified storage)"));
          console.log(chalk2.gray("   Start with: vr service start --detach"));
        }
        console.log();
      }
      console.log(chalk2.gray(`Query with: vr learn list --keyword "${trigger.split(" ")[0]}"`));
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  learn.command("list").description("List recorded learnings").option("-t, --type <type>", "Filter by type").option("-k, --keyword <keyword>", "Search by keyword in trigger").option("-n, --number <count>", "Number of learnings to show", "20").option("--json", "Output as JSON").action(async (options) => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        console.log(chalk2.yellow("\u{1F98E} Vreko not initialized"));
        console.log(chalk2.gray("Run: vr init"));
        process.exit(1);
      }
      let learnings = await getLearnings(cwd);
      if (options.type) {
        learnings = learnings.filter((l) => l.type === options.type);
      }
      if (options.keyword) {
        const keyword = options.keyword.toLowerCase();
        learnings = learnings.filter((l) => l.trigger.toLowerCase().includes(keyword) || l.action.toLowerCase().includes(keyword));
      }
      const count = Number.parseInt(options.number, 10);
      const recent = learnings.slice(-count).reverse();
      if (options.json) {
        console.log(JSON.stringify(recent, null, 2));
        return;
      }
      if (recent.length === 0) {
        console.log(chalk2.yellow("No learnings found"));
        console.log(chalk2.gray('Record with: vr learn "trigger" "action"'));
        return;
      }
      console.log(chalk2.cyan(`Learnings (${recent.length}):`));
      console.log();
      for (const learning of recent) {
        console.log(formatType(learning.type), chalk2.bold(learning.trigger));
        console.log(`  \u2192 ${learning.action}`);
        console.log(chalk2.gray(`  ${learning.createdAt} \u2022 ${learning.source}`));
        console.log();
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  return learn;
}
__name(createLearnCommand, "createLearnCommand");
function formatType(type) {
  const formats = {
    pattern: chalk2.blue("\u{1F4CB} pattern"),
    pitfall: chalk2.red("\u26A0\uFE0F  pitfall"),
    efficiency: chalk2.green("\u26A1 efficiency"),
    discovery: chalk2.yellow("\u{1F4A1} discovery"),
    workflow: chalk2.magenta("\u{1F504} workflow"),
    constraint: chalk2.cyan("\u{1F512} constraint"),
    preference: chalk2.gray("\u{1F4AC} preference")
  };
  return formats[type] || type;
}
__name(formatType, "formatType");
function createPatternsCommand() {
  const patterns = new Command("patterns").description("Manage workspace patterns");
  patterns.action(async () => {
    const { isInteractive: isInteractive2 } = await import('./guards-QAFC64NO.js');
    if (isInteractive2()) {
      const { launchTui } = await import('./tui-PFFYLIOW.js');
      await launchTui("learnings");
      return;
    }
    patterns.help();
  });
  patterns.command("list").description("List promoted patterns").option("--json", "Output as JSON").action(async (options) => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        console.log(chalk2.yellow("Vreko not initialized"));
        console.log(chalk2.gray("Run: vr init"));
        process.exit(1);
      }
      const patternsList = await readVrekoJson("patterns/workspace-patterns.json", cwd);
      if (options.json) {
        console.log(JSON.stringify(patternsList || [], null, 2));
        return;
      }
      if (!patternsList || patternsList.length === 0) {
        console.log(chalk2.yellow("No patterns recorded yet"));
        console.log(chalk2.gray("Patterns are promoted after 3 occurrences of the same violation."));
        return;
      }
      console.log(chalk2.cyan(`Active Patterns (${patternsList.length}):`));
      console.log();
      for (const pattern of patternsList) {
        console.log(chalk2.bold(pattern.type));
        console.log(`  ${pattern.description}`);
        console.log(chalk2.green(`  Prevention: ${pattern.prevention}`));
        console.log(chalk2.gray(`  ${pattern.occurrences} occurrences \u2022 Promoted: ${pattern.promotedAt}`));
        console.log();
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  patterns.command("report").description("Report a pattern violation").argument("<type>", "Violation type (e.g., 'layer-boundary-violation')").argument("<file>", "File where it occurred").argument("<message>", "What happened").option("-p, --prevention <prevention>", "How to prevent in future").action(async (type, file, message, options) => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        console.log(chalk2.yellow("Vreko not initialized"));
        console.log(chalk2.gray("Run: vr init"));
        process.exit(1);
      }
      const violations = await getViolations(cwd);
      const count = violations.filter((v) => v.type === type).length + 1;
      const violation = {
        type,
        file,
        message,
        count,
        date: (/* @__PURE__ */ new Date()).toISOString(),
        ...options.prevention && {
          prevention: options.prevention
        }
      };
      await recordViolation(violation, cwd);
      console.log(chalk2.yellow("\u26A0"), `Violation recorded: ${type}`);
      console.log(chalk2.gray(`  File: ${file}`));
      console.log(chalk2.gray(`  Message: ${message}`));
      console.log(chalk2.gray(`  Occurrences: ${count}/3 for promotion`));
      if (count >= 3) {
        await promoteToPattern(type, message, options.prevention, cwd);
        console.log();
        console.log(chalk2.green("\u{1F4C8}"), `Pattern promoted: ${type}`);
        console.log(chalk2.gray("  This pattern will now be detected automatically."));
      }
    } catch (error) {
      const message2 = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message2}`);
      process.exit(1);
    }
  });
  patterns.command("violations").description("Show recent violations").option("-n, --number <count>", "Number of violations to show", "20").option("--json", "Output as JSON").action(async (options) => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        console.log(chalk2.yellow("Vreko not initialized"));
        console.log(chalk2.gray("Run: vr init"));
        process.exit(1);
      }
      const violations = await getViolations(cwd);
      const count = Number.parseInt(options.number, 10);
      const recent = violations.slice(-count).reverse();
      if (options.json) {
        console.log(JSON.stringify(recent, null, 2));
        return;
      }
      if (recent.length === 0) {
        console.log(chalk2.green("\u2713"), "No violations recorded");
        return;
      }
      console.log(chalk2.cyan(`Recent Violations (${recent.length}):`));
      console.log();
      const grouped = /* @__PURE__ */ new Map();
      for (const v of recent) {
        const existing = grouped.get(v.type) || [];
        existing.push(v);
        grouped.set(v.type, existing);
      }
      for (const [type, vList] of grouped) {
        const count2 = vList.length;
        const promotionStatus = count2 >= 3 ? chalk2.green("(promoted)") : chalk2.gray(`(${count2}/3)`);
        console.log(`  ${type} ${promotionStatus}`);
        for (const v of vList.slice(0, 3)) {
          console.log(chalk2.gray(`  ${v.file}`));
          console.log(chalk2.gray(`    ${v.message}`));
        }
        if (vList.length > 3) {
          console.log(chalk2.gray(`  ... and ${vList.length - 3} more`));
        }
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  patterns.command("summary").description("Show violation summary and promotion status").option("--json", "Output as JSON").action(async (options) => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        process.exit(1);
      }
      const violations = await getViolations(cwd);
      const patterns2 = await readVrekoJson("patterns/workspace-patterns.json", cwd) || [];
      const typeCounts = /* @__PURE__ */ new Map();
      for (const v of violations) {
        typeCounts.set(v.type, (typeCounts.get(v.type) || 0) + 1);
      }
      const summary = {
        totalViolations: violations.length,
        promotedPatterns: patterns2.length,
        pendingPromotion: [
          ...typeCounts.entries()
        ].filter(([_, count]) => count >= 2 && count < 3).length,
        byType: Object.fromEntries(typeCounts)
      };
      if (options.json) {
        console.log(JSON.stringify(summary));
        return;
      }
      console.log(`Total violations: ${summary.totalViolations}`);
      console.log(`Promoted patterns: ${summary.promotedPatterns}`);
      console.log(`Pending promotion: ${summary.pendingPromotion}`);
      if (typeCounts.size > 0) {
        const sorted = [
          ...typeCounts.entries()
        ].sort((a, b) => b[1] - a[1]);
        for (const [type, count] of sorted) {
          const status = count >= 3 ? chalk2.green("promoted") : chalk2.gray(`${count}/3`);
          console.log(`  ${type}: ${count} ${status}`);
        }
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  patterns.command("debt").description("Scan for TODO/FIXME/XXX comments (technical debt markers)").option("-d, --dir <directory>", "Directory to scan", ".").option("--json", "Output as JSON").option("--verbose", "Show file paths for each item").action(async (options) => {
    const cwd = process.cwd();
    const targetDir = options.dir === "." ? cwd : join(cwd, options.dir);
    try {
      const debtItems = await scanTechnicalDebt(targetDir, cwd);
      if (options.json) {
        console.log(JSON.stringify(debtItems, null, 2));
        return;
      }
      if (debtItems.length === 0) {
        return;
      }
      const grouped = {
        TODO: debtItems.filter((d) => d.type === "TODO"),
        FIXME: debtItems.filter((d) => d.type === "FIXME"),
        XXX: debtItems.filter((d) => d.type === "XXX"),
        HACK: debtItems.filter((d) => d.type === "HACK")
      };
      if (grouped.FIXME.length > 0) {
        console.log(`  FIXME: ${grouped.FIXME.length}`);
      }
      if (grouped.XXX.length > 0) {
        console.log(`  XXX: ${grouped.XXX.length}`);
      }
      if (grouped.HACK.length > 0) {
        console.log(`  HACK: ${grouped.HACK.length}`);
      }
      if (grouped.TODO.length > 0) {
        console.log(`  TODO: ${grouped.TODO.length}`);
      }
      if (options.verbose) {
        for (const [type, items] of Object.entries(grouped)) {
          if (items.length === 0) {
            continue;
          }
          const color = type === "FIXME" ? chalk2.red : type === "XXX" || type === "HACK" ? chalk2.yellow : chalk2.blue;
          for (const item of items.slice(0, 10)) {
            console.log(`  ${color(item.type)} ${item.file}:${item.line}  -  ${item.message}`);
          }
          if (items.length > 10) {
            console.log(`  ... and ${items.length - 10} more`);
          }
        }
      } else {
        const highPriority = [
          ...grouped.FIXME,
          ...grouped.XXX
        ].slice(0, 5);
        if (highPriority.length > 0) {
          for (const item of highPriority) {
            const color = item.type === "FIXME" ? chalk2.red : chalk2.yellow;
            console.log(`  ${color(item.type)} ${item.file}:${item.line}  -  ${item.message}`);
          }
        }
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  return patterns;
}
__name(createPatternsCommand, "createPatternsCommand");
async function promoteToPattern(type, description, prevention, workspaceRoot) {
  const violations = await getViolations(workspaceRoot);
  const typeViolations = violations.filter((v) => v.type === type);
  const occurrences = typeViolations.length;
  const patterns = await readVrekoJson("patterns/workspace-patterns.json", workspaceRoot) || [];
  const existingIndex = patterns.findIndex((p) => p.type === type);
  const pattern = {
    type,
    description,
    prevention: prevention || typeViolations.find((v) => v.prevention)?.prevention || "Review and fix manually",
    occurrences,
    promotedAt: existingIndex >= 0 ? patterns[existingIndex].promotedAt : (/* @__PURE__ */ new Date()).toISOString(),
    lastSeenAt: (/* @__PURE__ */ new Date()).toISOString()
  };
  if (existingIndex >= 0) {
    patterns[existingIndex] = pattern;
  } else {
    patterns.push(pattern);
  }
  await writeVrekoJson("patterns/workspace-patterns.json", patterns, workspaceRoot);
}
__name(promoteToPattern, "promoteToPattern");
async function scanTechnicalDebt(_directory, _workspaceRoot) {
  return [];
}
__name(scanTechnicalDebt, "scanTechnicalDebt");
var __defProp2 = Object.defineProperty;
var __name3 = /* @__PURE__ */ __name((target, value) => __defProp2(target, "name", {
  value,
  configurable: true
}), "__name");
function composeHint(params) {
  if (params.daemonError) {
    return "Pulse unavailable (daemon error). Proceed with caution; Vreko intelligence degraded.";
  }
  if (params.noSession) {
    return "No active session. Call vreko to open one before working.";
  }
  if (params.noFilesTracked) {
    return "Session open, no files tracked yet. First save will register files with Vreko.";
  }
  const lines = [];
  lines.push(`You are in ${params.workspaceName}, session ${params.sessionId} (${params.durationMinutes}m).`);
  const fragile = params.fragileFiles;
  if (fragile.length === 1) {
    const f = fragile[0];
    const evidence = f.evidence ? `; ${f.evidence}` : "";
    lines.push(`${f.path} is flagged fragile (${f.rollbackCount} rollbacks in 90 days${evidence}).`);
  } else if (fragile.length >= 2) {
    const list = fragile.map((f) => f.path).join(", ");
    const topByRollback = [
      ...fragile
    ].sort((a, b) => b.rollbackCount - a.rollbackCount)[0];
    lines.push(`${fragile.length} files in scope are flagged fragile: ${list}. Highest risk: ${topByRollback.path} (${topByRollback.rollbackCount} rollbacks).`);
  }
  const untouched = params.coChanges.filter((c) => !c.partnerTouched);
  if (untouched.length > 0) {
    const c = untouched[0];
    lines.push(`Co-change partner ${c.partner} hasn't been touched this session; if you're changing ${c.primary}, verify ${c.partner} expectations match.`);
  }
  if (params.warnings.length > 0) {
    const severityRank = {
      critical: 3,
      warn: 2,
      info: 1
    };
    const top = [
      ...params.warnings
    ].sort((a, b) => (severityRank[b.severity] ?? 0) - (severityRank[a.severity] ?? 0))[0];
    lines.push(`Active warnings: ${params.warnings.length}. Highest severity: ${top.message}.`);
  }
  const hasFragility = fragile.length > 0;
  const hasWarnings = params.warnings.length > 0;
  if (!hasFragility && !hasWarnings) {
    lines.push("No concerning signals. Proceed.");
  } else if (hasFragility && !hasWarnings) {
    lines.push(`Recommend: before editing ${fragile[0].path}, restate change plan and call vreko_learn.`);
  } else if (!hasFragility && hasWarnings) {
    lines.push("Recommend: address warning before continuing.");
  } else {
    lines.push(`Recommend: before editing ${fragile[0].path}, restate change plan and call vreko_learn. Also address warning before continuing.`);
  }
  return lines.join("\n");
}
__name(composeHint, "composeHint");
__name3(composeHint, "composeHint");
function createPulseCommand() {
  const cmd = new Command("pulse");
  cmd.description("Get a mid-session intelligence snapshot (advisory  -  read-only)").option("--format <format>", "Output format: json or human", "human").option("--focus <file>", "Narrow fragility output to a specific file path").action(async (opts) => {
    const format = opts.format === "json" ? "json" : "human";
    const focus = opts.focus;
    await runPulse({
      format,
      focus
    });
  });
  return cmd;
}
__name(createPulseCommand, "createPulseCommand");
async function runPulse(options) {
  const workspace = process.cwd();
  const workspaceName = basename(workspace) || workspace;
  await withDaemonOptional("pulse", async (service) => {
    if (!service) {
      printDaemonUnavailable(options.format);
      return;
    }
    const [sessionResult, fragileResult, coChangesResult, warningsResult] = await Promise.allSettled([
      service.call("session/current", {
        workspacePath: workspace
      }),
      service.call("intelligence/fragile-files", {
        workspace
      }),
      service.call("intelligence/co-changes", {
        workspace
      }),
      service.call("intelligence/warnings", {
        workspace
      })
    ]);
    const rawSession = sessionResult.status === "fulfilled" ? sessionResult.value : null;
    const active = isActiveSession(rawSession);
    if (!active || rawSession === null) {
      const localSession = await getCurrentSession(workspace);
      const isLocalActive = localSession !== null && localSession.active !== false && localSession.state !== "ended";
      if (isLocalActive) {
        const durationMinutes2 = localSession.startedAt ? Math.max(0, Math.round((Date.now() - new Date(localSession.startedAt).getTime()) / 6e4)) : 0;
        const hintParams3 = {
          workspaceName,
          sessionId: localSession.id,
          durationMinutes: durationMinutes2,
          fragileFiles: [],
          coChanges: [],
          warnings: []
        };
        const hint2 = composeHint(hintParams3);
        emitOutput(options.format, {
          sessionId: localSession.id,
          llmHint: hint2,
          fragileFiles: [],
          warnings: []
        });
        return;
      }
      const hintParams2 = {
        workspaceName,
        sessionId: "unknown",
        durationMinutes: 0,
        fragileFiles: [],
        coChanges: [],
        warnings: [],
        noSession: true
      };
      const hint = composeHint(hintParams2);
      emitOutput(options.format, {
        sessionId: null,
        llmHint: hint,
        fragileFiles: [],
        warnings: []
      });
      return;
    }
    const fragileFilesRaw = fragileResult.status === "fulfilled" ? fragileResult.value?.files ?? [] : [];
    const coChangesRaw = coChangesResult.status === "fulfilled" ? coChangesResult.value?.pairs ?? [] : [];
    const warningsRaw = warningsResult.status === "fulfilled" ? warningsResult.value?.warnings ?? [] : [];
    const filteredFragile = options.focus ? fragileFilesRaw.filter((f) => {
      const target = options.focus;
      return f.path === target || f.path.endsWith(target) || target.endsWith(f.path);
    }) : fragileFilesRaw;
    const fragileForHint = filteredFragile.map((f) => ({
      path: f.path,
      fragility: typeof f.fragility === "number" && Number.isFinite(f.fragility) ? f.fragility : 0,
      rollbackCount: f.rollbackCount ?? 0,
      evidence: f.reason
    }));
    const touchedFiles = new Set(rawSession.touchedFiles ?? []);
    const coChangesForHint = coChangesRaw.filter((c) => touchedFiles.has(c.source) !== touchedFiles.has(c.target)).map((c) => ({
      primary: touchedFiles.has(c.source) ? c.source : c.target,
      partner: touchedFiles.has(c.source) ? c.target : c.source,
      partnerTouched: false
    }));
    const warningsForHint = warningsRaw.filter((w) => w.severity !== "info").map((w) => ({
      message: w.message,
      severity: w.severity
    }));
    const durationMinutes = rawSession.startedAt ? Math.max(0, Math.round((Date.now() - new Date(rawSession.startedAt).getTime()) / 6e4)) : 0;
    const noFilesTracked = (rawSession.touchedFiles?.length ?? 0) === 0;
    const hintParams = noFilesTracked ? {
      workspaceName,
      sessionId: rawSession.id ?? "unknown",
      durationMinutes,
      fragileFiles: [],
      coChanges: [],
      warnings: [],
      noFilesTracked: true
    } : {
      workspaceName,
      sessionId: rawSession.id ?? "unknown",
      durationMinutes,
      fragileFiles: fragileForHint,
      coChanges: coChangesForHint,
      warnings: warningsForHint
    };
    const llmHint = composeHint(hintParams);
    emitOutput(options.format, {
      sessionId: rawSession.id ?? null,
      llmHint,
      fragileFiles: fragileForHint,
      warnings: warningsForHint.map((w) => `[${w.severity}] ${w.message}`)
    });
  });
}
__name(runPulse, "runPulse");
function emitOutput(format, payload) {
  if (format === "json") {
    console.log(JSON.stringify(payload));
    return;
  }
  console.log(payload.llmHint);
  if (payload.fragileFiles.length > 0 || payload.warnings.length > 0) {
    console.log("");
    if (payload.fragileFiles.length > 0) {
      console.log("Fragile files in scope:");
      for (const f of payload.fragileFiles) {
        const evidence = f.evidence ? `  -  ${f.evidence}` : "";
        console.log(`  - ${f.path} (${f.rollbackCount} rollbacks)${evidence}`);
      }
    }
    if (payload.warnings.length > 0) {
      console.log("Warnings:");
      for (const w of payload.warnings) {
        console.log(`  - ${w}`);
      }
    }
  }
}
__name(emitOutput, "emitOutput");
function printDaemonUnavailable(format) {
  if (format === "json") {
    console.log("{}");
  } else {
    console.log("vreko pulse: service unavailable  -  proceeding advisory");
  }
}
__name(printDaemonUnavailable, "printDaemonUnavailable");
function isActiveSession(raw) {
  if (!raw) {
    return false;
  }
  if (raw.state === "active") {
    return true;
  }
  if (raw.active === true) {
    return true;
  }
  return false;
}
__name(isActiveSession, "isActiveSession");
function countDir(dirPath) {
  if (!existsSync(dirPath)) {
    return {
      count: 0,
      bytes: 0
    };
  }
  try {
    const entries = readdirSync(dirPath, {
      recursive: true
    });
    let bytes = 0;
    for (const entry of entries) {
      try {
        const st = statSync(join(dirPath, entry));
        if (st.isFile()) {
          bytes += st.size;
        }
      } catch {
      }
    }
    return {
      count: entries.filter((e) => !e.includes("/") && !e.includes("\\")).length,
      bytes
    };
  } catch {
    return {
      count: 0,
      bytes: 0
    };
  }
}
__name(countDir, "countDir");
function formatBytes3(bytes) {
  if (bytes < 1024) {
    return `${bytes} B`;
  }
  if (bytes < 1024 * 1024) {
    return `${(bytes / 1024).toFixed(1)} KB`;
  }
  return `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
}
__name(formatBytes3, "formatBytes");
function gatherCounts(vrekoDir2) {
  const learnings = countDir(join(vrekoDir2, "learnings"));
  const snapshots = countDir(join(vrekoDir2, "snapshots"));
  const blobs = countDir(join(vrekoDir2, "blobs"));
  const audit = countDir(join(vrekoDir2, "audit"));
  const dbFiles = [];
  for (const db of [
    "knowledge.db",
    "intelligence.db"
  ]) {
    if (existsSync(join(vrekoDir2, db))) {
      dbFiles.push(db);
    }
  }
  return {
    learnings: learnings.count,
    snapshots: snapshots.count,
    snapshotBytes: snapshots.bytes,
    blobs: blobs.count,
    blobBytes: blobs.bytes,
    auditEntries: audit.count,
    dbFiles
  };
}
__name(gatherCounts, "gatherCounts");
function printCounts(counts) {
  console.log("");
  console.log(chalk2.bold("Data to be deleted:"));
  console.log(`  Knowledge store  ${chalk2.cyan(counts.learnings)} learnings`);
  console.log(`  Snapshots        ${chalk2.cyan(counts.snapshots)} snapshots ${chalk2.dim(formatBytes3(counts.snapshotBytes))}`);
  if (counts.blobs > 0) {
    console.log(`  Blob cache       ${chalk2.cyan(counts.blobs)} files ${chalk2.dim(formatBytes3(counts.blobBytes))}`);
  }
  if (counts.auditEntries > 0) {
    console.log(`  Audit log        ${chalk2.cyan(counts.auditEntries)} entries`);
  }
  if (counts.dbFiles.length > 0) {
    console.log(`  Databases        ${chalk2.cyan(counts.dbFiles.join(", "))}`);
  }
  console.log("");
}
__name(printCounts, "printCounts");
function deleteVrekoData(vrekoDir2) {
  const dirsToDelete = [
    "learnings",
    "snapshots",
    "blobs",
    "audit"
  ];
  for (const dir of dirsToDelete) {
    const p = join(vrekoDir2, dir);
    if (existsSync(p)) {
      rmSync(p, {
        recursive: true,
        force: true
      });
    }
  }
  for (const db of [
    "knowledge.db",
    "intelligence.db"
  ]) {
    const p = join(vrekoDir2, db);
    if (existsSync(p)) {
      rmSync(p, {
        force: true
      });
    }
  }
}
__name(deleteVrekoData, "deleteVrekoData");
function createPurgeCommand() {
  return new Command("purge").description("Delete all local Vreko data from this workspace").option("--dry-run", "Show what would be deleted without deleting").option("-y, --yes", "Skip confirmation prompt").option("--json", "Output structured JSON").action(async (options) => {
    const vrekoDir2 = join(process.cwd(), ".vreko");
    if (!existsSync(vrekoDir2)) {
      if (options.json) {
        console.log(JSON.stringify({
          success: false,
          reason: "no-vreko-dir"
        }));
      } else {
        console.log("No .vreko/ directory found in the current workspace.");
      }
      return;
    }
    const counts = gatherCounts(vrekoDir2);
    const totalItems = counts.learnings + counts.snapshots + counts.blobs + counts.auditEntries + counts.dbFiles.length;
    if (options.json) {
      console.log(JSON.stringify({
        dryRun: !!options.dryRun,
        counts,
        totalItems
      }));
      if (options.dryRun) {
        return;
      }
    } else {
      printCounts(counts);
      if (options.dryRun) {
        console.log(chalk2.dim("Dry run  -  no data deleted. Run without --dry-run to delete."));
        return;
      }
      if (totalItems === 0) {
        console.log("Nothing to delete.");
        return;
      }
    }
    if (!options.yes && !cliState.yes && !options.json) {
      const confirmed = await clack3.confirm({
        message: chalk2.yellow("Permanently delete all Vreko data from this workspace? This cannot be undone."),
        initialValue: false
      });
      if (clack3.isCancel(confirmed) || !confirmed) {
        clack3.cancel("Purge cancelled. No data was deleted.");
        return;
      }
    }
    deleteVrekoData(vrekoDir2);
    if (options.json) {
      console.log(JSON.stringify({
        success: true,
        deleted: counts
      }));
    } else {
      console.log(`${chalk2.green("\u2713")} All local Vreko data deleted.`);
      console.log(chalk2.dim("  Run `vreko init` to reinitialize this workspace."));
    }
  });
}
__name(createPurgeCommand, "createPurgeCommand");

// ../../packages/contracts/dist/types/snapshot-storage.js
var __rewriteRelativeImportExtension = function(path4, preserveJsx) {
  if (/^\.\.?\//.test(path4)) {
    return path4.replace(/\.(tsx)$|((?:\.d)?)((?:\.[^./]+?)?)\.([cm]?)ts$/i, function(m, tsx, d, ext, cm) {
      return tsx ? ".js" : d && (!ext || !cm) ? m : d + ext + "." + cm.toLowerCase() + "js";
    });
  }
  return path4;
};
async function createSnapshotStorage(basePath) {
  try {
    const { StorageBrokerAdapter: StorageBrokerAdapter2 } = await import(__rewriteRelativeImportExtension(
      /* webpackIgnore: true */
      "@vreko/sdk/storage"
    ));
    const storage = new StorageBrokerAdapter2(`${basePath}/.vreko/vreko.db`);
    await storage.initialize();
    return {
      create: /* @__PURE__ */ __name(async (data) => {
        const snapshot = {
          id: `vreko-${Date.now()}-${Math.random().toString(36).slice(2, 9)}`,
          timestamp: Date.now(),
          version: "1.0",
          meta: {
            ...data,
            protected: data.protected || false,
            description: data.description || ""
          }
        };
        await storage.save(snapshot);
        return snapshot;
      }, "create"),
      retrieve: /* @__PURE__ */ __name(async (id) => {
        return await storage.get(id);
      }, "retrieve"),
      list: /* @__PURE__ */ __name(async () => {
        return await storage.list();
      }, "list"),
      restore: /* @__PURE__ */ __name(async (id, _targetPath, options) => {
        const snapshot = await storage.get(id);
        if (!snapshot) {
          return {
            success: false,
            restoredFiles: [],
            errors: [
              `Snapshot ${id} not found`
            ]
          };
        }
        if (options?.dryRun) {
          return {
            success: true,
            restoredFiles: snapshot.files || [],
            errors: []
          };
        }
        return {
          success: true,
          restoredFiles: snapshot.files || [],
          errors: []
        };
      }, "restore")
    };
  } catch (error) {
    throw new Error(`Failed to initialize snapshot storage: ${error instanceof Error ? error.message : String(error)}. Ensure @vreko/sdk is installed and storage path is writable.`);
  }
}
__name(createSnapshotStorage, "createSnapshotStorage");
async function analyzeFileRisk(filePath, content, workspaceRoot) {
  try {
    const client = await connectToDaemon();
    const result7 = await client.validation.comprehensive({
      workspace: workspaceRoot,
      filePath,
      code: content
    });
    const riskScore = result7.passed ? Math.round((1 - result7.confidence) * 4) : Math.round(4 + (1 - result7.confidence) * 6);
    const riskLevel = riskScore > 7 ? "high" : riskScore > 4 ? "medium" : "low";
    return {
      riskScore,
      riskLevel,
      signals: []
    };
  } catch {
    return analyzeFileRiskWithPatterns(filePath, content);
  }
}
__name(analyzeFileRisk, "analyzeFileRisk");
function analyzeFileRiskWithPatterns(filePath, content) {
  const factors = [];
  if (content.includes("eval(")) {
    factors.push({
      signal: "eval()",
      value: 3
    });
  }
  if (content.includes("Function(")) {
    factors.push({
      signal: "Function constructor",
      value: 2
    });
  }
  if (/process\.env\.[A-Z_]{6,}/.test(content)) {
    factors.push({
      signal: "env access",
      value: 1
    });
  }
  if (/secret|password|api[_-]?key/i.test(filePath)) {
    factors.push({
      signal: "sensitive filename",
      value: 4
    });
  }
  const riskScore = Math.min(10, factors.reduce((sum, f) => sum + f.value, 0));
  const riskLevel = riskScore > 7 ? "high" : riskScore > 4 ? "medium" : "low";
  return {
    riskScore,
    riskLevel,
    signals: factors
  };
}
__name(analyzeFileRiskWithPatterns, "analyzeFileRiskWithPatterns");
async function getAllFiles(dir, baseDir = dir) {
  const files = [];
  try {
    const entries = await readdir(dir, {
      withFileTypes: true
    });
    for (const entry of entries) {
      const fullPath = join(dir, entry.name);
      if (entry.name === "node_modules" || entry.name === ".git" || entry.name.startsWith(".")) {
        continue;
      }
      if (entry.isDirectory()) {
        files.push(...await getAllFiles(fullPath, baseDir));
      } else {
        files.push(relative(baseDir, fullPath));
      }
    }
  } catch {
  }
  return files;
}
__name(getAllFiles, "getAllFiles");
var ERROR_SUGGESTIONS = [
  // Authentication errors
  {
    pattern: /not logged in|unauthorized|401/i,
    suggestion: "You need to authenticate first",
    command: "vreko login"
  },
  {
    pattern: /token expired|session expired/i,
    suggestion: "Your session has expired. Please log in again",
    command: "vreko login"
  },
  {
    pattern: /invalid.*api.*key/i,
    suggestion: "Your API key appears to be invalid. Get a new one at console.vreko.dev/app/settings/api-keys",
    command: "vreko login --api-key <your-key>"
  },
  // Workspace errors
  {
    pattern: /not initialized|no.*\.vreko/i,
    suggestion: "This workspace hasn't been set up for Vreko yet",
    command: "vreko init"
  },
  {
    pattern: /already initialized/i,
    suggestion: "Vreko is already configured here. Use --force to reinitialize",
    command: "vreko init --force"
  },
  // File errors
  {
    pattern: /ENOENT|file not found|no such file/i,
    suggestion: "The file or directory doesn't exist. Check the path and try again"
  },
  {
    pattern: /EACCES|permission denied/i,
    suggestion: "You don't have permission to access this file. Check file permissions"
  },
  {
    pattern: /EEXIST|already exists/i,
    suggestion: "A file with that name already exists. Use --force to overwrite"
  },
  // Network errors
  {
    pattern: /ECONNREFUSED|connection refused/i,
    suggestion: "Cannot connect to the server. Check your internet connection",
    command: "vreko doctor"
  },
  {
    pattern: /ETIMEDOUT|timeout|timed out/i,
    suggestion: "The request timed out. The server may be slow or unreachable",
    command: "vreko doctor"
  },
  {
    pattern: /ENOTFOUND|DNS|network/i,
    suggestion: "Network error. Check your internet connection and try again",
    command: "vreko doctor"
  },
  // Git errors
  {
    pattern: /not a git repository/i,
    suggestion: "This directory is not a Git repository",
    command: "git init"
  },
  {
    pattern: /git.*not.*installed|git.*not found/i,
    suggestion: "Git is required but not installed. Please install Git first"
  },
  // Config errors
  {
    pattern: /invalid.*config|parse.*error.*json/i,
    suggestion: "The configuration file is malformed. Try resetting it",
    command: "vreko config path"
  },
  // MCP errors
  {
    pattern: /mcp.*not configured|no.*ai.*tools/i,
    suggestion: "No AI tools are configured for MCP integration",
    command: "vreko tools configure"
  }
];
var KNOWN_COMMANDS = [
  // Auth
  {
    name: "login",
    description: "Authenticate with Vreko"
  },
  {
    name: "logout",
    description: "Log out of Vreko"
  },
  {
    name: "whoami",
    description: "Show current user"
  },
  {
    name: "set-key",
    description: "Set API key directly"
  },
  {
    name: "workspaces",
    description: "List connected workspaces"
  },
  // Workspace management
  {
    name: "init",
    description: "Initialize workspace"
  },
  {
    name: "status",
    description: "Show workspace status"
  },
  {
    name: "analyze",
    description: "Run workspace intelligence analysis"
  },
  {
    name: "fix",
    description: "Auto-fix detected issues"
  },
  {
    name: "claude-sync",
    description: "Generate Claude Code integration files"
  },
  // Protection
  {
    name: "protect",
    description: "Protect files from changes"
  },
  {
    name: "session",
    description: "Manage work sessions"
  },
  // §15.1: snapshot has aliases ss and snap; use --list to list snapshots
  {
    name: "snapshot",
    description: "Create a code snapshot (--list to list)",
    aliases: [
      "ss",
      "snap"
    ]
  },
  // Intelligence
  {
    name: "context",
    description: "Get relevant context before starting work"
  },
  {
    name: "validate",
    description: "Validate patterns and changes"
  },
  {
    name: "stats",
    description: "Show statistics"
  },
  // Momentum scoring
  {
    name: "sync",
    description: "Collect signals for momentum scoring"
  },
  {
    name: "metrics",
    description: "Show momentum scores for files",
    aliases: [
      "m"
    ]
  },
  {
    name: "refresh",
    description: "Incremental score update"
  },
  // Learning
  {
    name: "learn",
    description: "Record a learning or pattern"
  },
  {
    name: "patterns",
    description: "Manage learned patterns"
  },
  {
    name: "consolidate",
    description: "Consolidate duplicate learnings"
  },
  // File analysis
  {
    name: "check",
    description: "Pre-commit risk check"
  },
  {
    name: "risk-analyze",
    description: "Analyze file risk signals"
  },
  {
    name: "watch",
    description: "Watch files for AI-driven changes"
  },
  // MCP / ACP integration
  {
    name: "tools",
    description: "Configure AI tools for MCP integration"
  },
  {
    name: "mcp",
    description: "MCP server management"
  },
  {
    name: "acp",
    description: "Agent Communication Protocol integration"
  },
  // Daemon / service
  {
    name: "daemon",
    description: "Manage the Vreko service"
  },
  {
    name: "service",
    description: "Manage the Vreko system service"
  },
  {
    name: "baseline",
    description: "Manage workspace baselines"
  },
  // Interactive / guided
  {
    name: "interactive",
    description: "Interactive guided workflow"
  },
  // Polish / admin
  {
    name: "config",
    description: "Manage configuration"
  },
  {
    name: "doctor",
    description: "Diagnose installation and health"
  },
  {
    name: "diagnostics",
    description: "Advanced troubleshooting tools"
  },
  {
    name: "upgrade",
    description: "Upgrade Vreko"
  },
  {
    name: "undo",
    description: "Undo recent operations"
  },
  {
    name: "alias",
    description: "Create command shortcuts"
  },
  {
    name: "completion",
    description: "Generate shell completion scripts"
  },
  {
    name: "help",
    description: "Show help"
  }
];
var INTENT_MAPPINGS = {
  // Snapshot-related intents  -  §15.2: "snap" MUST suggest "snapshot" first // tui-vocab-allowed
  snap: "snapshot",
  ss: "snapshot",
  snapshot: "snapshot",
  save: "snapshot",
  backup: "snapshot",
  store: "snapshot",
  capture: "snapshot",
  // "list" is removed; snapshots are listed via `snapshot --list`
  list: "snapshot",
  // Check-related intents (not "validate"  -  that is a real registered command)
  verify: "check",
  test: "check",
  // Auth-related intents
  auth: "login",
  signin: "login",
  signon: "login",
  // Status-related intents
  info: "status",
  show: "status"
};
function levenshteinDistance(a, b) {
  const matrix = [];
  for (let i = 0; i <= b.length; i++) {
    matrix[i] = [
      i
    ];
  }
  for (let j = 0; j <= a.length; j++) {
    matrix[0][j] = j;
  }
  for (let i = 1; i <= b.length; i++) {
    for (let j = 1; j <= a.length; j++) {
      if (b.charAt(i - 1) === a.charAt(j - 1)) {
        matrix[i][j] = matrix[i - 1][j - 1];
      } else {
        matrix[i][j] = Math.min(matrix[i - 1][j - 1] + 1, matrix[i][j - 1] + 1, matrix[i - 1][j] + 1);
      }
    }
  }
  return matrix[b.length][a.length];
}
__name(levenshteinDistance, "levenshteinDistance");
function findSimilarCommands(input, maxSuggestions = 3) {
  const inputLower = input.toLowerCase();
  const intentMatch = INTENT_MAPPINGS[inputLower];
  if (intentMatch) {
    const cmd = KNOWN_COMMANDS.find((c) => c.name === intentMatch);
    if (cmd) {
      const others = KNOWN_COMMANDS.filter((c) => c.name !== intentMatch).map((c) => ({
        name: c.name,
        distance: levenshteinDistance(inputLower, c.name)
      })).filter((s) => s.distance <= 3).sort((a, b) => a.distance - b.distance).slice(0, maxSuggestions - 1).map((s) => s.name);
      return [
        intentMatch,
        ...others
      ];
    }
  }
  for (const cmd of KNOWN_COMMANDS) {
    if (cmd.aliases?.includes(inputLower)) {
      return [
        cmd.name
      ];
    }
  }
  const suggestions = KNOWN_COMMANDS.map((cmd) => ({
    name: cmd.name,
    distance: levenshteinDistance(inputLower, cmd.name)
  })).filter((s) => s.distance <= 3).sort((a, b) => a.distance - b.distance).slice(0, maxSuggestions).map((s) => s.name);
  return suggestions;
}
__name(findSimilarCommands, "findSimilarCommands");
function displaySmartError(error) {
  const errorData = normalizeError(error);
  if (cliState.json) {
    process.stdout.write(`${JSON.stringify({
      error: errorData.message,
      code: errorData.code || null
    })}
`);
    return;
  }
  const lines = [];
  if (errorData.code) {
    lines.push(`${chalk2.red.bold(`[${errorData.code}]`)} ${chalk2.red.bold(errorData.title)}`);
  } else {
    lines.push(chalk2.red.bold(errorData.title));
  }
  lines.push("");
  lines.push(errorData.message);
  if (errorData.context && Object.keys(errorData.context).length > 0) {
    lines.push("");
    for (const [key, value] of Object.entries(errorData.context)) {
      lines.push(chalk2.gray(`${key}: ${value}`));
    }
  }
  if (errorData.suggestion) {
    lines.push("");
    lines.push(chalk2.yellow("\u{1F4A1} Suggestion:"));
    lines.push(chalk2.yellow(`   ${errorData.suggestion}`));
  }
  if (errorData.command) {
    lines.push("");
    lines.push(chalk2.cyan("\u{1F4CB} Try running:"));
    lines.push(chalk2.cyan(`   $ ${errorData.command}`));
  }
  if (errorData.docLink) {
    lines.push("");
    lines.push(chalk2.gray(`\u{1F4DA} More info: ${errorData.docLink}`));
  }
  console.error(lines.join("\n"));
}
__name(displaySmartError, "displaySmartError");
function displayUnknownCommandError(command) {
  const suggestions = findSimilarCommands(command);
  const lines = [];
  lines.push(chalk2.red.bold(`Unknown command: ${command}`));
  lines.push("");
  if (suggestions.length > 0) {
    lines.push(chalk2.yellow("Did you mean:"));
    for (const suggestion of suggestions) {
      lines.push(chalk2.cyan(`  $ vreko ${suggestion}`));
    }
  } else {
    lines.push(chalk2.gray("Run 'vreko --help' to see available commands"));
  }
  console.error(lines.join("\n"));
}
__name(displayUnknownCommandError, "displayUnknownCommandError");
function normalizeError(error) {
  if (typeof error === "object" && "code" in error && "title" in error) {
    return error;
  }
  if (typeof error === "string") {
    return {
      code: "ERR_UNKNOWN",
      title: "Error",
      message: error,
      ...findErrorSuggestion(error)
    };
  }
  const message = error.message;
  const suggestion = findErrorSuggestion(message);
  return {
    code: extractErrorCode(error) || "ERR_UNKNOWN",
    title: error.name || "Error",
    message,
    ...suggestion
  };
}
__name(normalizeError, "normalizeError");
function findErrorSuggestion(message) {
  for (const { pattern, suggestion, command } of ERROR_SUGGESTIONS) {
    const regex = typeof pattern === "string" ? new RegExp(pattern, "i") : pattern;
    if (regex.test(message)) {
      return {
        suggestion,
        command
      };
    }
  }
  return {};
}
__name(findErrorSuggestion, "findErrorSuggestion");
async function clackConfirm(message, options) {
  if (!isInteractive()) {
    return options?.defaultValue ?? false;
  }
  const result7 = await clack3.confirm({
    message,
    active: options?.active ?? "yes",
    inactive: options?.inactive ?? "no",
    initialValue: options?.defaultValue ?? false
  });
  if (clack3.isCancel(result7)) {
    clack3.cancel("Cancelled.");
    process.exit(0);
  }
  return result7;
}
__name(clackConfirm, "clackConfirm");

// src/utils/safe-ops.ts
var HISTORY_FILE = ".vreko/operation-history.json";
var MAX_HISTORY_SIZE = 50;
function getHistoryPath() {
  const homeDir = process.env.HOME || process.env.USERPROFILE || "";
  return path10.join(homeDir, HISTORY_FILE);
}
__name(getHistoryPath, "getHistoryPath");
function loadHistory() {
  try {
    const historyPath = getHistoryPath();
    if (fs.existsSync(historyPath)) {
      return JSON.parse(fs.readFileSync(historyPath, "utf-8"));
    }
  } catch {
  }
  return {
    operations: []
  };
}
__name(loadHistory, "loadHistory");
function saveHistory(history) {
  try {
    const historyPath = getHistoryPath();
    const dir = path10.dirname(historyPath);
    fs.mkdirSync(dir, {
      recursive: true
    });
    if (history.operations.length > MAX_HISTORY_SIZE) {
      history.operations = history.operations.slice(-MAX_HISTORY_SIZE);
    }
    fs.writeFileSync(historyPath, JSON.stringify(history, null, 2));
  } catch {
  }
}
__name(saveHistory, "saveHistory");
function getLastOperation() {
  const history = loadHistory();
  return history.operations.filter((op) => op.canUndo).pop();
}
__name(getLastOperation, "getLastOperation");
function getRecentOperations(count = 10) {
  const history = loadHistory();
  return history.operations.slice(-count).reverse();
}
__name(getRecentOperations, "getRecentOperations");
function removeOperation(id) {
  const history = loadHistory();
  history.operations = history.operations.filter((op) => op.id !== id);
  saveHistory(history);
}
__name(removeOperation, "removeOperation");
async function undoLastOperation() {
  const lastOp = getLastOperation();
  if (!lastOp) {
    return false;
  }
  if (!lastOp.canUndo) {
    return false;
  }
  const shouldUndo = await clackConfirm("Undo this operation?", {
    defaultValue: false
  });
  if (!shouldUndo) {
    return false;
  }
  let success = true;
  for (const change of lastOp.changes) {
    try {
      switch (change.type) {
        case "create":
          if (change.path && fs.existsSync(change.path)) {
            fs.unlinkSync(change.path);
          }
          break;
        case "delete":
          if (change.before !== void 0) {
            fs.writeFileSync(change.path, change.before);
          }
          break;
        case "update":
          if (change.before !== void 0) {
            fs.writeFileSync(change.path, change.before);
          }
          break;
      }
    } catch (_error) {
      success = false;
    }
  }
  if (success) {
    removeOperation(lastOp.id);
  }
  return success;
}
__name(undoLastOperation, "undoLastOperation");

// src/commands/check.ts
function createCheckCommand() {
  return new Command("check").description("Run invariant checks (default) or staged-file AI risk analysis (--staged)").option("--staged", "Check staged files for AI risk (pre-commit behavior)").option("--json", "Emit machine-readable JSON output (for CI/lefthook)").option("-s, --snapshot", "Create snapshot if risky changes detected").option("-q, --quiet", "Suppress output unless issues found").option("-a, --all", "Check all files, not just staged (legacy behavior); in invariant mode, also runs architecture suite").option("--override-invariant <id>", "Increment override counter for the named invariant; >= 2 demotes to advisory").action(async (options) => {
    const cwd = process.cwd();
    if (options.staged) {
      try {
        const git = new GitClient({
          cwd
        });
        if (!await git.isGitInstalled()) {
          throw new GitNotInstalledError();
        }
        if (!await git.isGitRepository()) {
          throw new GitNotRepositoryError(cwd);
        }
        let filesToCheck;
        if (options.all) {
          const allFiles = await getAllFiles(cwd);
          filesToCheck = allFiles.filter(isCodeFile);
        } else {
          const stagedFiles = await git.getStagedFiles();
          filesToCheck = stagedFiles.filter((f) => f.status !== "deleted").filter((f) => isCodeFile(f.path)).map((f) => f.path);
        }
        if (filesToCheck.length === 0) {
          if (!options.quiet) {
            console.log(chalk2.green("\u2713"), "No staged files to check");
          }
          return;
        }
        const progress = new ProgressTracker({
          total: filesToCheck.length,
          label: "Analyzing",
          quiet: options.quiet
        });
        progress.start();
        const fileResults = [];
        let hasRiskyChanges = false;
        for (const file of filesToCheck) {
          progress.update(file);
          try {
            const content = options.all ? await readFile(resolve(cwd, file), "utf-8") : await git.getStagedContent(file);
            const riskResult = await analyzeFileRisk(file, content, cwd);
            fileResults.push({
              file,
              riskScore: riskResult.riskScore,
              riskLevel: riskResult.riskLevel,
              topSignal: riskResult.signals.filter((s) => s.value > 0)[0]?.signal
            });
            if (riskResult.riskScore > 5) {
              hasRiskyChanges = true;
            }
          } catch {
          }
        }
        const highRisk = fileResults.filter((f) => f.riskScore > 7).length;
        const mediumRisk = fileResults.filter((f) => f.riskScore > 4 && f.riskScore <= 7).length;
        if (hasRiskyChanges) {
          progress.fail(`Found risks in ${highRisk + mediumRisk} files (${highRisk} high, ${mediumRisk} medium) - ${progress.getElapsed()}`);
          if (!options.quiet && fileResults.length > 0) {
            for (const result7 of fileResults.filter((f) => f.riskScore > 4)) {
              const level = result7.riskScore > 7 ? chalk2.red("HIGH") : chalk2.yellow("MED");
              console.log(`  ${level} ${result7.file} (${result7.riskScore.toFixed(1)})`);
            }
          }
          if (options.snapshot) {
            const snapshotSpinner = ora("Creating snapshot...").start();
            try {
              const storage = await createSnapshotStorage(cwd);
              const snap = await storage.create({
                description: "Pre-commit snapshot for risky AI changes",
                protected: true
              });
              snapshotSpinner.succeed(`Snapshot created: ${snap.id.substring(0, 8)}`);
              const maxRiskScore = Math.max(...fileResults.map((f) => f.riskScore));
              console.log(`  Max risk score: ${maxRiskScore.toFixed(2)}`);
            } catch (error) {
              snapshotSpinner.fail("Failed to create snapshot");
              displaySmartError(error instanceof Error ? error : String(error));
              process.exit(1);
            }
          } else if (!options.quiet) {
            console.log(chalk2.gray("  Tip: use --snapshot to auto-snapshot risky changes"));
          }
          if (!options.quiet) {
            console.log(chalk2.gray("  Run vr check --staged --snapshot to protect before committing"));
          }
          process.exit(1);
        } else {
          progress.complete(`No risky changes detected in ${filesToCheck.length} files - ${progress.getElapsed()}`);
        }
      } catch (error) {
        if (error instanceof GitNotInstalledError) {
          process.exit(1);
        }
        if (error instanceof GitNotRepositoryError) {
          process.exit(1);
        }
        if (!options.quiet) {
          displaySmartError(error instanceof Error ? error : String(error));
        }
        process.exit(1);
      }
      return;
    }
    await runInvariantCheck(cwd, options);
  });
}
__name(createCheckCommand, "createCheckCommand");
async function runInvariantCheck(cwd, options) {
  const startTime = Date.now();
  const invariantsDir = join(cwd, ".vreko", "invariants");
  if (options.overrideInvariant) {
    const id = String(options.overrideInvariant).trim();
    if (!/^[a-z0-9][a-z0-9.-]*$/i.test(id)) {
      process.stderr.write(`Invalid invariant id: ${id}
`);
      process.exit(2);
    }
    const invPath = join(cwd, ".vreko", "invariants", `${id}.json`);
    if (!existsSync(invPath)) {
      process.stderr.write(`Invariant not found: ${id}
`);
      process.exit(2);
    }
    const inv = JSON.parse(readFileSync(invPath, "utf-8"));
    inv.overrideCount = (inv.overrideCount ?? 0) + 1;
    if (inv.overrideCount >= 2 && inv.mode !== "advisory") {
      inv.mode = "advisory";
    }
    writeFileSync(invPath, `${JSON.stringify(inv, null, 2)}
`, "utf-8");
    process.stderr.write(`Override recorded: ${id} (count=${inv.overrideCount}, mode=${inv.mode})
`);
    process.exit(0);
  }
  if (!existsSync(invariantsDir)) {
    if (!options.quiet) {
      console.log("No .vreko/invariants/ directory found.");
      console.log("To initialize: create .vreko/invariants/ and add invariant JSON files.");
      console.log("See: https://docs.vreko.dev/invariants (or .vreko/invariants/ in this repo for examples)");
    }
    process.exit(0);
  }
  const invariantFiles = readdirSync(invariantsDir).filter((f) => f.endsWith(".json"));
  if (invariantFiles.length === 0) {
    if (!options.quiet) {
      console.log("No invariant files found in .vreko/invariants/");
      console.log("Add .json invariant files to get started.");
    }
    process.exit(0);
  }
  const violations = [];
  let runnerError = false;
  for (const file of invariantFiles) {
    const filePath = join(invariantsDir, file);
    const fileContent = readFileSync(filePath, "utf-8");
    const sha = createHash("sha256").update(fileContent).digest("hex").slice(0, 16);
    let invariant2;
    try {
      invariant2 = JSON.parse(fileContent);
    } catch {
      console.error(`Runner error: malformed JSON in ${file}`);
      runnerError = true;
      continue;
    }
    const invariantId = String(invariant2.invariant_id ?? file.replace(".json", ""));
    const severity = invariant2.severity ?? "error";
    const verification = invariant2.verification;
    const violated = evaluation(verification);
    if (violated) {
      violations.push({
        invariant_id: invariantId,
        file_path: file,
        sha,
        confidence: 1,
        message: String(invariant2.description ?? `Invariant ${invariantId} violated`),
        severity
      });
    }
  }
  if (options.all && !options.staged) {
    const { spawnSync: spawnSync2 } = await import('child_process');
    const archResult = spawnSync2("pnpm", [
      "vitest",
      "run",
      "tests/architecture/",
      "--reporter=json"
    ], {
      cwd,
      encoding: "utf-8",
      stdio: [
        "ignore",
        "pipe",
        "pipe"
      ],
      timeout: 12e4,
      maxBuffer: 50 * 1024 * 1024
    });
    if (archResult.signal === "SIGTERM" || archResult.error?.code === "ETIMEDOUT") {
      violations.push({
        invariant_id: "architecture-suite-timeout",
        file_path: "tests/architecture/",
        sha: "n/a",
        confidence: 1,
        message: "architecture suite timed out after 120s",
        severity: "warning"
      });
    } else {
      try {
        const json = JSON.parse(archResult.stdout || "{}");
        const testResults = json.testResults ?? [];
        for (const file of testResults) {
          for (const test of file.assertionResults ?? []) {
            if (test.status === "failed") {
              violations.push({
                invariant_id: `architecture-test:${test.fullName ?? test.title ?? "unknown"}`,
                file_path: file.name ?? "tests/architecture/",
                sha: "n/a",
                confidence: 1,
                message: (test.failureMessages ?? []).join("\n").slice(0, 500),
                severity: "error"
              });
            }
          }
        }
      } catch (err6) {
        violations.push({
          invariant_id: "architecture-suite-parse-error",
          file_path: "tests/architecture/",
          sha: "n/a",
          confidence: 1,
          message: `failed to parse vitest JSON output: ${err6.message}`,
          severity: "warning"
        });
      }
    }
  }
  const duration_ms = Date.now() - startTime;
  const output = {
    version: "1.0",
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    workspace: cwd,
    duration_ms,
    summary: {
      total: invariantFiles.length,
      passed: invariantFiles.length - violations.length,
      failed: violations.length
    },
    violations
  };
  if (options.json) {
    console.log(JSON.stringify(output, null, 2));
  } else if (!options.quiet) {
    if (violations.length === 0) {
      console.log(`\u2713 ${invariantFiles.length} invariant(s) passed (${duration_ms}ms)`);
    } else {
      console.log(`\u2717 ${violations.length} violation(s) found:`);
      for (const v of violations) {
        console.log(`  [${v.severity.toUpperCase()}] ${v.invariant_id}: ${v.message}`);
      }
      console.log(JSON.stringify(output, null, 2));
    }
  }
  if (runnerError) {
    process.exit(2);
  }
  if (violations.length > 0) {
    process.exit(1);
  }
  process.exit(0);
}
__name(runInvariantCheck, "runInvariantCheck");
function evaluation(verification) {
  if (!verification) {
    return false;
  }
  if (verification.alwaysFail === true) {
    return true;
  }
  return false;
}
__name(evaluation, "evaluation");
function createInteractiveCommand() {
  return new Command("interactive").description("Open the interactive TUI dashboard").action(async () => {
    if (cliState.renderMode === "ink") {
      const { launchTui } = await import('./tui-PFFYLIOW.js');
      await launchTui({
        initialPanel: "dashboard"
      });
    }
  });
}
__name(createInteractiveCommand, "createInteractiveCommand");
function createRiskAnalyzeCommand() {
  return new Command("risk-analyze").alias("ra").description("Analyze a single file for risk (alias: ra)").argument("<file>", "File path to analyze").option("-i, --interactive", "Interactive mode with detailed analysis").action(async (file) => {
    try {
      const fullPath = resolve(process.cwd(), file);
      const text2 = await readFile(fullPath, "utf-8");
      const spinner2 = ora("Analyzing file...").start();
      const riskData = await analyzeFileRisk(file, text2, process.cwd());
      spinner2.succeed("Analysis complete");
      if (riskData.signals && riskData.signals.length > 0) {
        console.log(chalk2.bold("\nRisk Signals:"));
        for (const signal of riskData.signals) {
          console.log(`  ${chalk2.yellow("\u2022")} ${signal}`);
        }
      }
      if (riskData.riskScore > 7) {
        console.log(chalk2.red(`
\u26A0 High risk score: ${riskData.riskScore}/10`));
      } else if (riskData.riskScore > 4) {
        console.log(chalk2.yellow(`
\u26A1 Moderate risk score: ${riskData.riskScore}/10`));
      }
    } catch (error) {
      displaySmartError(error instanceof Error ? error : String(error));
      process.exit(1);
    }
  });
}
__name(createRiskAnalyzeCommand, "createRiskAnalyzeCommand");
function createProtectCommand() {
  const protect = new Command("protect").description("Manage file protection");
  protect.command("add").description("Add a file or pattern to protection").argument("<pattern>", "File path or glob pattern to protect").option("-r, --reason <reason>", "Reason for protection").action(async (pattern, options) => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        console.log(chalk2.yellow("Vreko not initialized in this workspace"));
        console.log(chalk2.gray("Run: vr init"));
        process.exit(1);
      }
      const protectedFiles = await getProtectedFiles(cwd);
      const existing = protectedFiles.find((f) => f.pattern === pattern);
      if (existing) {
        console.log(chalk2.yellow(`Already protected: ${pattern}`));
        return;
      }
      if (!pattern.includes("*") && !pattern.includes("**")) {
        try {
          await access(join(cwd, pattern), constants.F_OK);
        } catch {
          console.log(chalk2.yellow(`Warning: File not found: ${pattern}`));
          console.log(chalk2.gray("Protection will still be added for future files matching this path."));
        }
      }
      const newProtection = {
        pattern,
        addedAt: (/* @__PURE__ */ new Date()).toISOString(),
        ...options.reason && {
          reason: options.reason
        }
      };
      protectedFiles.push(newProtection);
      await saveProtectedFiles(protectedFiles, cwd);
      console.log(chalk2.green("\u2713"), `Added protection: ${pattern}`);
      if (options.reason) {
        console.log(chalk2.gray(`  Reason: ${options.reason}`));
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  protect.command("remove").description("Remove a file or pattern from protection").argument("<pattern>", "File path or glob pattern to unprotect").action(async (pattern) => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        console.log(chalk2.yellow("Vreko not initialized in this workspace"));
        console.log(chalk2.gray("Run: vr init"));
        process.exit(1);
      }
      const protectedFiles = await getProtectedFiles(cwd);
      const index = protectedFiles.findIndex((f) => f.pattern === pattern);
      if (index === -1) {
        console.log(chalk2.yellow(`Not protected: ${pattern}`));
        return;
      }
      protectedFiles.splice(index, 1);
      await saveProtectedFiles(protectedFiles, cwd);
      console.log(chalk2.green("\u2713"), `Removed protection: ${pattern}`);
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  protect.command("list").description("List all protected files").option("--json", "Output as JSON").action(async (options) => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        console.log(chalk2.yellow("Vreko not initialized in this workspace"));
        console.log(chalk2.gray("Run: vr init"));
        process.exit(1);
      }
      const protectedFiles = await getProtectedFiles(cwd);
      if (options.json) {
        console.log(JSON.stringify(protectedFiles, null, 2));
        return;
      }
      if (protectedFiles.length === 0) {
        console.log(chalk2.yellow("No files protected"));
        console.log(chalk2.gray("Run: vr protect add <pattern>"));
        return;
      }
      console.log(chalk2.cyan("Protected files:"));
      console.log();
      for (const file of protectedFiles) {
        console.log(chalk2.green("\u2022"), file.pattern);
        if (file.reason) {
          console.log(chalk2.gray(`  Reason: ${file.reason}`));
        }
      }
      console.log();
      console.log(chalk2.gray(`Total: ${protectedFiles.length} protected`));
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  protect.command("env").description("Protect all .env files").action(async () => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        console.log(chalk2.yellow("Vreko not initialized in this workspace"));
        console.log(chalk2.gray("Run: vr init"));
        process.exit(1);
      }
      const patterns = [
        ".env",
        ".env.*",
        "*.env"
      ];
      const protectedFiles = await getProtectedFiles(cwd);
      let added = 0;
      for (const pattern of patterns) {
        if (!protectedFiles.some((f) => f.pattern === pattern)) {
          protectedFiles.push({
            pattern,
            addedAt: (/* @__PURE__ */ new Date()).toISOString(),
            reason: "Environment variables"
          });
          added++;
        }
      }
      if (added > 0) {
        await saveProtectedFiles(protectedFiles, cwd);
        console.log(chalk2.green("\u2713"), `Added ${added} environment file patterns`);
      } else {
        console.log(chalk2.yellow("Environment files already protected"));
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  protect.command("config").description("Protect common configuration files").action(async () => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        console.log(chalk2.yellow("Vreko not initialized in this workspace"));
        console.log(chalk2.gray("Run: vr init"));
        process.exit(1);
      }
      const patterns = [
        "*.config.js",
        "*.config.ts",
        "*.config.mjs",
        "tsconfig.json",
        "package.json",
        "pnpm-lock.yaml",
        "yarn.lock",
        "package-lock.json"
      ];
      const protectedFiles = await getProtectedFiles(cwd);
      let added = 0;
      for (const pattern of patterns) {
        if (!protectedFiles.some((f) => f.pattern === pattern)) {
          protectedFiles.push({
            pattern,
            addedAt: (/* @__PURE__ */ new Date()).toISOString(),
            reason: "Configuration file"
          });
          added++;
        }
      }
      if (added > 0) {
        await saveProtectedFiles(protectedFiles, cwd);
        console.log(chalk2.green("\u2713"), `Added ${added} configuration file patterns`);
      } else {
        console.log(chalk2.yellow("Configuration files already protected"));
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  return protect;
}
__name(createProtectCommand, "createProtectCommand");

// src/analytics/emit.ts
var ENDPOINT = process.env.VREKO_API_URL ?? "https://api.vreko.dev";
function emitPioneerEvent(name, properties) {
  fetch(`${ENDPOINT}/v1/analytics/events`, {
    method: "POST",
    headers: {
      "Content-Type": "application/json"
    },
    body: JSON.stringify({
      event: name,
      properties
    })
  }).catch(() => {
    if (process.env.VREKO_DEBUG === "true") {
      process.stderr.write(`[vreko:analytics] failed to emit event: ${name}
`);
    }
  });
}
__name(emitPioneerEvent, "emitPioneerEvent");

// src/commands/session.ts
function emitFirstSessionOnce() {
  try {
    const flagFile = getGlobalPath("first-session");
    if (!existsSync(flagFile)) {
      mkdirSync(getGlobalDir(), {
        recursive: true
      });
      writeFileSync(flagFile, (/* @__PURE__ */ new Date()).toISOString(), "utf8");
      emitPioneerEvent(PIONEER_EVENTS.FIRST_SESSION);
    }
  } catch {
  }
}
__name(emitFirstSessionOnce, "emitFirstSessionOnce");
function createSessionCommand() {
  const session = new Command("session").description("Manage development sessions");
  session.action(async () => {
    const { isInteractive: isInteractive2 } = await import('./guards-QAFC64NO.js');
    if (isInteractive2()) {
      const { launchTui } = await import('./tui-PFFYLIOW.js');
      await launchTui("session");
      return;
    }
    session.help();
  });
  session.command("start").description("Start a new development session").argument("[task]", "Task description").option("-f, --force", "End current session and start a new one").option("--json", "Output session info as JSON").action(async (task, options) => {
    const cwd = process.cwd();
    const jsonMode = options.json || cliState.json;
    try {
      if (!await isVrekoInitialized(cwd)) {
        if (jsonMode) {
          process.stdout.write(`${JSON.stringify({
            error: "Vreko not initialized in this workspace"
          })}
`);
          process.exit(1);
        }
        console.log(chalk2.yellow("\u{1F98E} Vreko not initialized in this workspace"));
        console.log(chalk2.gray("Run: vr init"));
        process.exit(1);
      }
      const existingSession = await withDaemonOptional("session start", async (client) => {
        if (!client) {
          return await getCurrentSession(cwd);
        }
        const response1 = await client.session.current({
          workspacePath: cwd
        });
        const s1 = response1;
        if (s1) {
          return {
            id: s1.id,
            startedAt: s1.startedAt,
            snapshotCount: s1.snapshotIds?.length ?? 0
          };
        }
        return null;
      });
      if (existingSession && !options.force) {
        if (jsonMode) {
          process.stdout.write(`${JSON.stringify({
            error: "A session is already active",
            sessionId: existingSession.id
          })}
`);
          process.exit(1);
        }
        console.log(chalk2.yellow("A session is already active:"));
        console.log(`  ID: ${chalk2.gray(existingSession.id.substring(0, 8))}`);
        if (existingSession.task) {
          console.log(`  Task: ${existingSession.task}`);
        }
        console.log(`  Started: ${formatTimeAgo(existingSession.startedAt)}`);
        console.log(`  Snapshots: ${existingSession.snapshotCount}`);
        console.log();
        console.log(chalk2.gray("Use --force to end this session and start a new one"));
        return;
      }
      if (existingSession && options.force) {
        if (!jsonMode) {
          console.log(chalk2.gray(`Ended previous session: ${existingSession.id.substring(0, 8)}`));
        }
        await archiveSession(existingSession, cwd);
        await endCurrentSession(cwd);
      }
      const newSession = {
        id: generateId("sess"),
        task,
        startedAt: (/* @__PURE__ */ new Date()).toISOString(),
        snapshotCount: 0,
        state: "active",
        active: true
      };
      try {
        const client = await connectToDaemon();
        const daemonSession = await client.session.start({
          workspacePath: cwd
        });
        newSession.id = daemonSession.id ?? newSession.id;
      } catch (err6) {
        const msg = err6 instanceof Error ? err6.message : String(err6);
        console.warn(`\u26A0 Service unavailable  -  session stored locally. Syncing resumes on next service connection. (${msg})`);
      }
      await saveCurrentSession(newSession, cwd);
      emitFirstSessionOnce();
      if (jsonMode) {
        process.stdout.write(`${JSON.stringify({
          sessionId: newSession.id,
          workspacePath: cwd,
          startedAt: newSession.startedAt
        })}
`);
        return;
      }
      console.log(chalk2.green("\u2713"), "Session started");
      console.log(`  ID: ${chalk2.gray(newSession.id.substring(0, 8))}`);
      if (task) {
        console.log(`  Task: ${task}`);
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      if (jsonMode) {
        process.stdout.write(`${JSON.stringify({
          error: message
        })}
`);
        process.exit(1);
      }
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    } finally {
      disconnectFromDaemon();
    }
  });
  session.command("status").description("Show current session status").option("--json", "Output as JSON").action(async (options) => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        console.log(chalk2.yellow("\u{1F98E} Vreko not initialized"));
        console.log(chalk2.gray("Run: vr init"));
        process.exit(1);
      }
      let currentSession = null;
      try {
        const CONNECT_TIMEOUT_MS = 3e3;
        let connectTimeoutId;
        const client = await Promise.race([
          connectToDaemon(),
          new Promise((_, reject) => {
            connectTimeoutId = setTimeout(() => reject(new Error("Service connection timed out")), CONNECT_TIMEOUT_MS);
          })
        ]).finally(() => clearTimeout(connectTimeoutId));
        const response2 = await client.session.current({
          workspacePath: cwd
        });
        const s2 = response2;
        if (s2) {
          currentSession = {
            id: s2.id,
            startedAt: s2.startedAt,
            snapshotCount: s2.snapshotIds?.length ?? 0
          };
        }
      } catch (err6) {
        const msg = err6 instanceof Error ? err6.message : String(err6);
        console.warn(`\u26A0 Service unavailable  -  showing local session state. (${msg})`);
        currentSession = await getCurrentSession(cwd);
      }
      if (!currentSession) {
        if (options.json) {
          console.log(JSON.stringify({
            active: false
          }, null, 2));
        } else {
          console.log(chalk2.yellow("No active session"));
          console.log(chalk2.gray("Run: vr session start [task]"));
        }
        return;
      }
      if (options.json) {
        console.log(JSON.stringify({
          active: true,
          ...currentSession
        }, null, 2));
        return;
      }
      console.log(chalk2.cyan("Active Session:"));
      console.log();
      console.log(`  ID:        ${chalk2.gray(currentSession.id.substring(0, 8))}`);
      if (currentSession.task) {
        console.log(`  Task:      ${currentSession.task}`);
      }
      console.log(`  Started:   ${formatTimeAgo(currentSession.startedAt)}`);
      console.log(`  Snapshots: ${currentSession.snapshotCount}`);
      console.log(`  Duration:  ${formatDuration3(currentSession.startedAt)}`);
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    } finally {
      disconnectFromDaemon();
    }
  });
  session.command("end").description("End the current session").option("-m, --message <message>", "Session end message/summary").option("-l, --learning <text>", "Add a learning (can be used multiple times)", []).option("--ceremony [level]", "Validate before close: quick, standard, comprehensive").option("--outcome <type>", "Session outcome: completed, abandoned, blocked", "completed").option("--force", "Clear stuck session unconditionally regardless of service state").option("--no-tui", "Use plain text ceremony output (for scripts/CI)").action(async (options) => {
    const cwd = process.cwd();
    if (options.force) {
      try {
        const stuck = await getCurrentSession(cwd);
        if (stuck) {
          await archiveSession(stuck, cwd, options.message);
        }
      } catch (err6) {
        const msg = err6 instanceof Error ? err6.message : String(err6);
        console.warn(`\u26A0 Could not read session before clearing  -  forcing delete anyway. (${msg})`);
      }
      try {
        await endCurrentSession(cwd);
      } catch {
      }
      console.log("Session cleared.");
      return;
    }
    try {
      if (!await isVrekoInitialized(cwd)) {
        console.log(chalk2.yellow("\u{1F98E} Vreko not initialized"));
        console.log(chalk2.gray("Run: vr init"));
        process.exit(1);
      }
      const currentSession = await getCurrentSession(cwd);
      if (!currentSession) {
        console.log(chalk2.yellow("No active session"));
        return;
      }
      const learnings = Array.isArray(options.learning) ? options.learning : options.learning ? [
        options.learning
      ] : [];
      if (learnings.length > 0 || options.ceremony) {
        try {
          const { createServiceClient: createServiceClient3, connectServiceClient: connectServiceClient2, isServiceRunning: isServiceRunning2 } = await import('./local-service-adapter-G7CJAIMC.js');
          if (isServiceRunning2()) {
            const client = createServiceClient3();
            try {
              await connectServiceClient2(client);
              if (learnings.length > 0) {
                console.log(chalk2.gray(`Capturing ${learnings.length} learning(s)...`));
              }
              if (options.ceremony) {
                try {
                  const record = await client.call("session/review", {
                    workspacePath: cwd,
                    sessionId: currentSession.id
                  });
                  const { isInteractive: isInteractive2 } = await import('./guards-QAFC64NO.js');
                  const useTui = isInteractive2() && cliState.renderMode === "ink" && !options.noTui;
                  if (useTui) {
                    const { render } = await import('ink');
                    const React = await import('react');
                    const { CeremonyView } = await import('./CeremonyView-LQS7FTMK.js');
                    const instance3 = render(React.createElement(CeremonyView, {
                      record
                    }));
                    await instance3.waitUntilExit();
                  } else {
                    const { renderCeremony } = await import('./ceremony-M7CXVBVA.js');
                    console.log(renderCeremony(record ?? null));
                  }
                } catch {
                  const { isInteractive: isInteractive2 } = await import('./guards-QAFC64NO.js');
                  const useTui = isInteractive2() && cliState.renderMode === "ink" && !options.noTui;
                  if (useTui) {
                    const { render } = await import('ink');
                    const React = await import('react');
                    const { CeremonyView } = await import('./CeremonyView-LQS7FTMK.js');
                    const instance3 = render(React.createElement(CeremonyView, {
                      record: null
                    }));
                    await instance3.waitUntilExit();
                  } else {
                    const { renderCeremony } = await import('./ceremony-M7CXVBVA.js');
                    console.log(renderCeremony(null));
                  }
                }
              }
            } finally {
              client.close();
            }
          } else if (options.ceremony) {
            const { isInteractive: isInteractive2 } = await import('./guards-QAFC64NO.js');
            const useTui = isInteractive2() && cliState.renderMode === "ink" && !options.noTui;
            if (useTui) {
              const { render } = await import('ink');
              const React = await import('react');
              const { CeremonyView } = await import('./CeremonyView-LQS7FTMK.js');
              const instance3 = render(React.createElement(CeremonyView, {
                record: null
              }));
              await instance3.waitUntilExit();
            } else {
              const { renderCeremony } = await import('./ceremony-M7CXVBVA.js');
              console.log(renderCeremony(null));
            }
          }
        } catch (err6) {
          const msg = err6 instanceof Error ? err6.message : String(err6);
          console.warn(`\u26A0 Session review unavailable (service offline)  -  rendering summary without AI insights. (${msg})`);
          if (options.ceremony) {
            const { isInteractive: isInteractive2 } = await import('./guards-QAFC64NO.js');
            const useTui = isInteractive2() && cliState.renderMode === "ink" && !options.noTui;
            if (useTui) {
              const { render } = await import('ink');
              const React = await import('react');
              const { CeremonyView } = await import('./CeremonyView-LQS7FTMK.js');
              const instance3 = render(React.createElement(CeremonyView, {
                record: null
              }));
              await instance3.waitUntilExit();
            } else {
              const { renderCeremony } = await import('./ceremony-M7CXVBVA.js');
              console.log(renderCeremony(null));
            }
          }
        }
      }
      const sessionSummary = {
        ...currentSession,
        endMessage: options.message,
        outcome: options.outcome,
        learnings: learnings.length > 0 ? learnings : void 0
      };
      await archiveSession(sessionSummary, cwd, options.message);
      await endCurrentSession(cwd);
      if (currentSession.id) {
        try {
          const { isServiceRunning: isServiceRunning2, createServiceClient: createServiceClient3, connectServiceClient: connectServiceClient2 } = await import('./local-service-adapter-G7CJAIMC.js');
          const { endSessionViaDaemon: endSessionViaDaemon2 } = await import('./local-service-adapter-G7CJAIMC.js');
          if (isServiceRunning2()) {
            const svcClient = createServiceClient3();
            try {
              await connectServiceClient2(svcClient);
              await endSessionViaDaemon2(svcClient, currentSession.id, cwd);
            } finally {
              svcClient.close();
            }
          }
        } catch (err6) {
          const msg = err6 instanceof Error ? err6.message : String(err6);
          console.warn(`\u26A0 Service notification failed  -  session closed locally. (${msg})`);
        }
      }
      const duration = formatDuration3(currentSession.startedAt);
      console.log(`\u2713 Session ended (duration: ${duration})`);
      if (learnings.length > 0) {
        for (const learning of learnings) {
          console.log(`  Learning: ${learning}`);
        }
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  session.command("history").description("Show session history").option("-n, --number <count>", "Number of sessions to show", "10").option("--json", "Output as JSON").action(async (options) => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        process.exit(1);
      }
      const { loadVrekoJsonl: loadVrekoJsonl2 } = await import('./vreko-dir-ELQLOFKA.js');
      const history = await loadVrekoJsonl2("session/history.jsonl", cwd);
      const count = Number.parseInt(options.number, 10);
      const recent = history.slice(-count).reverse();
      if (options.json) {
        console.log(JSON.stringify(recent, null, 2));
        return;
      }
      if (recent.length === 0) {
        console.log(chalk2.yellow("No session history"));
        return;
      }
      console.log(chalk2.cyan(`Session History (${recent.length}):`));
      for (const session2 of recent) {
        const duration = formatDurationFromDates(session2.startedAt, session2.endedAt);
        console.log(`  ${session2.id.substring(0, 8)} (${duration})`);
        if (session2.endMessage) {
          console.log(`    ${session2.endMessage}`);
        }
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  return session;
}
__name(createSessionCommand, "createSessionCommand");
async function archiveSession(session, workspaceRoot, endMessage) {
  const archived = {
    ...session,
    endedAt: (/* @__PURE__ */ new Date()).toISOString(),
    ...endMessage && {
      endMessage
    }
  };
  await appendVrekoJsonl("session/history.jsonl", archived, workspaceRoot);
}
__name(archiveSession, "archiveSession");
function formatTimeAgo(isoDate) {
  const date = new Date(isoDate);
  const now = /* @__PURE__ */ new Date();
  const seconds = Math.floor((now.getTime() - date.getTime()) / 1e3);
  if (seconds < 60) {
    return "just now";
  }
  if (seconds < 3600) {
    return `${Math.floor(seconds / 60)} minutes ago`;
  }
  if (seconds < 86400) {
    return `${Math.floor(seconds / 3600)} hours ago`;
  }
  return `${Math.floor(seconds / 86400)} days ago`;
}
__name(formatTimeAgo, "formatTimeAgo");
function formatDuration3(startIso) {
  const start = new Date(startIso);
  const now = /* @__PURE__ */ new Date();
  const seconds = Math.floor((now.getTime() - start.getTime()) / 1e3);
  return formatSeconds(seconds);
}
__name(formatDuration3, "formatDuration");
function formatDurationFromDates(startIso, endIso) {
  const start = new Date(startIso);
  const end = new Date(endIso);
  const seconds = Math.floor((end.getTime() - start.getTime()) / 1e3);
  return formatSeconds(seconds);
}
__name(formatDurationFromDates, "formatDurationFromDates");
function formatSeconds(seconds) {
  if (seconds < 60) {
    return `${seconds}s`;
  }
  if (seconds < 3600) {
    return `${Math.floor(seconds / 60)}m`;
  }
  const hours = Math.floor(seconds / 3600);
  const minutes = Math.floor(seconds % 3600 / 60);
  return minutes > 0 ? `${hours}h ${minutes}m` : `${hours}h`;
}
__name(formatSeconds, "formatSeconds");
var execAsync = promisify(exec);
function createSnapshotCommand() {
  const snapshot = new Command("snapshot").description("Manage workspace snapshots");
  snapshot.action(async () => {
    const { isInteractive: isInteractive2 } = await import('./guards-QAFC64NO.js');
    if (isInteractive2()) {
      const { launchTui } = await import('./tui-PFFYLIOW.js');
      await launchTui("snapshots");
      return;
    }
    snapshot.help();
  });
  snapshot.command("list").description("List available snapshots").option("-l, --limit <number>", "Maximum number of snapshots to show", "10").option("--json", "Output as JSON").action(async (options) => {
    const cwd = process.cwd();
    if (!isServiceRunning()) {
      process.exitCode = 1;
      return;
    }
    const client = createServiceClient();
    try {
      await connectServiceClient(client);
      const limit = Number.parseInt(options.limit, 10) || 10;
      const result7 = await client.call("snapshot/list", {
        workspace: cwd,
        limit
      });
      if (options.json) {
        console.log(JSON.stringify(result7));
        return;
      }
      if (result7.snapshots.length === 0) {
        console.log("No snapshots found. Vreko creates snapshots automatically as you work.");
        return;
      }
      for (const snap of result7.snapshots) {
        const age = formatTimeAgo(snap.createdAt);
        const reason = snap.reason ? chalk2.gray(`  -  ${snap.reason}`) : "";
        const fileCount = snap.fileCount ?? "?";
        console.log(`  ${chalk2.cyan(snap.id.slice(0, 8))}  ${age}  ${fileCount} file(s)${reason}`);
      }
      if (result7.total && result7.total > result7.snapshots.length) {
        console.log(chalk2.gray(`  ... and ${result7.total - result7.snapshots.length} more`));
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exitCode = 1;
    } finally {
      client.close();
    }
  });
  snapshot.command("restore").description("Restore files from a snapshot").argument("<id>", "Snapshot ID (can be partial)").option("--dry-run", "Preview changes without restoring").option("-f, --files <files...>", "Only restore specific files").action(async (id, options) => {
    const cwd = process.cwd();
    if (!isServiceRunning()) {
      process.exitCode = 1;
      return;
    }
    const client = createServiceClient();
    try {
      await connectServiceClient(client);
      const result7 = await client.call("snapshot/list", {
        workspace: cwd,
        limit: 100
      });
      const snapshot2 = result7.snapshots.find((s) => s.id.startsWith(id));
      if (!snapshot2) {
        console.error(`\u2717 Snapshot "${id}" not found`);
        process.exitCode = 1;
        return;
      }
      if (options.dryRun) {
        const preview = await client.call("snapshot/restore", {
          workspace: cwd,
          snapshotId: snapshot2.id,
          files: options.files,
          dryRun: true
        });
        console.log(`Preview restore from snapshot ${snapshot2.id.slice(0, 8)}:`);
        if (preview.changes && preview.changes.length > 0) {
          for (const change of preview.changes) {
            console.log(`  ${chalk2.cyan(change.action.padEnd(8))} ${change.file}`);
          }
        } else {
          console.log("  No changes would be made");
        }
        return;
      }
      const restoreResult = await client.call("snapshot/restore", {
        workspace: cwd,
        snapshotId: snapshot2.id,
        files: options.files,
        dryRun: false
      });
      if (restoreResult.restored) {
        console.log(`\u2713 Restored ${restoreResult.filesRestored} file(s) from snapshot ${snapshot2.id.slice(0, 8)}`);
      } else {
        console.error("\u2717 Restore failed");
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exitCode = 1;
    } finally {
      client.close();
    }
  });
  snapshot.command("diagnose").description("Run snapshot system diagnostics").option("--json", "Output as JSON").action(async (options) => {
    const cwd = process.cwd();
    const vrekoDir2 = join(cwd, ".vreko");
    const diagnostics = {
      workspace: cwd,
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      vrekoExists: false,
      permissions: {},
      diskSpace: {},
      snapshotCount: 0,
      storageSize: "0",
      serviceStatus: "unknown",
      issues: [],
      recommendations: []
    };
    try {
      diagnostics.vrekoExists = existsSync(vrekoDir2);
      if (diagnostics.vrekoExists) {
        try {
          const stats = statSync(vrekoDir2);
          diagnostics.permissions = {
            readable: !!(stats.mode & 292),
            writable: !!(stats.mode & 146),
            executable: !!(stats.mode & 73)
          };
          if (!diagnostics.permissions.writable) {
            diagnostics.issues.push("PERMISSION_DENIED: .vreko directory is not writable");
            diagnostics.recommendations.push("Run: chmod -R 755 .vreko/");
          }
        } catch (error) {
          diagnostics.issues.push(`Permission check failed: ${error}`);
        }
        try {
          const { stdout } = await execAsync(`du -sh "${vrekoDir2}"`);
          diagnostics.storageSize = stdout.split("	")[0].trim();
        } catch {
        }
        try {
          const { stdout } = await execAsync(`find "${vrekoDir2}/snapshots" -type f -name "*.json" 2>/dev/null | wc -l`);
          diagnostics.snapshotCount = Number.parseInt(stdout.trim(), 10);
        } catch {
        }
      } else {
        diagnostics.issues.push("VREKO_NOT_INITIALIZED: .vreko directory does not exist");
        diagnostics.recommendations.push("Initialize Vreko in this workspace first");
      }
      try {
        const { stdout } = await execAsync(`df -h "${cwd}"`);
        const lines = stdout.trim().split("\n");
        if (lines.length > 1) {
          const parts = lines[1].split(/\s+/);
          diagnostics.diskSpace = {
            total: parts[1],
            used: parts[2],
            available: parts[3],
            usedPercent: parts[4]
          };
          const usedPercent = Number.parseInt(parts[4].replace("%", ""), 10);
          if (usedPercent > 90) {
            diagnostics.issues.push(`STORAGE_FULL: Disk is ${usedPercent}% full`);
            diagnostics.recommendations.push("Free up disk space or prune old snapshots");
          } else if (usedPercent > 80) {
            diagnostics.issues.push(`STORAGE_WARNING: Disk is ${usedPercent}% full`);
            diagnostics.recommendations.push("Consider pruning old snapshots: snap snapshot prune");
          }
        }
      } catch {
      }
      diagnostics.serviceStatus = isServiceRunning() ? "running" : "stopped";
      if (!isServiceRunning()) {
        diagnostics.recommendations.push("Start service: snap service start --service");
      }
      if (options.json) {
        console.log(JSON.stringify(diagnostics, null, 2));
        return;
      }
      console.log(`Workspace: ${diagnostics.workspace}`);
      console.log(`Service:   ${diagnostics.serviceStatus === "running" ? chalk2.green("running") : chalk2.red("stopped")}`);
      if (diagnostics.vrekoExists) {
        console.log(`Snapshots: ${diagnostics.snapshotCount}`);
        console.log(`Storage:   ${diagnostics.storageSize}`);
      } else {
        console.log(`Vreko:  ${chalk2.yellow("not initialized")}`);
      }
      if (diagnostics.diskSpace.total) {
        console.log(`Disk:      ${diagnostics.diskSpace.used}/${diagnostics.diskSpace.total} (${diagnostics.diskSpace.usedPercent} used)`);
      }
      if (diagnostics.issues.length > 0) {
        console.log();
        console.log("Issues:");
        for (const issue of diagnostics.issues) {
          console.log(`  ${chalk2.red("\u2717")} ${issue}`);
        }
      }
      if (diagnostics.recommendations.length > 0) {
        console.log();
        console.log("Recommendations:");
        for (const rec of diagnostics.recommendations) {
          console.log(`  \u2192 ${rec}`);
        }
      } else if (diagnostics.issues.length === 0) {
        console.log();
        console.log(chalk2.green("\u2713 No issues found"));
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  return snapshot;
}
__name(createSnapshotCommand, "createSnapshotCommand");
var DEFAULT_CONFIG = {
  debounceMs: 120,
  depth: 10,
  ignored: [
    "**/{node_modules,.git,.vscode,dist,.next,.nuxt,coverage}/**",
    "**/*.log",
    "**/.*cache*/**",
    "**/.vreko/snapshots/**",
    "**/.vreko/embeddings.db"
  ]
};
var CRITICAL_FILE_PATTERNS = [
  /\.env.*$/,
  /config\.(json|yaml|yml|toml)$/,
  /secrets?\.(json|yaml|yml)$/,
  /\.pem$/,
  /\.key$/,
  /auth\.ts$/,
  /middleware\.ts$/,
  /schema\.(ts|prisma)$/
];
var RISKY_CHANGE_PATTERNS = [
  /package\.json$/,
  /package-lock\.json$/,
  /pnpm-lock\.yaml$/,
  /yarn\.lock$/,
  /tsconfig\.json$/
];
var VrekoWatcher = class extends EventEmitter {
  static {
    __name(this, "VrekoWatcher");
  }
  watcher = null;
  config;
  stats;
  signalBuffer = [];
  flushTimer = null;
  changeCountByFile = /* @__PURE__ */ new Map();
  constructor(config2) {
    super();
    this.config = {
      ...DEFAULT_CONFIG,
      ...config2
    };
    this.stats = {
      startedAt: (/* @__PURE__ */ new Date()).toISOString(),
      filesWatched: 0,
      signalsRecorded: 0,
      patternsDetected: 0,
      lastActivityAt: null
    };
  }
  /**
  * Start watching the workspace
  */
  async start() {
    if (this.watcher) {
      throw new Error("Watcher already started");
    }
    if (!await isVrekoInitialized(this.config.workspaceRoot)) {
      throw new Error("Vreko not initialized. Run: vr init");
    }
    const { workspaceRoot, debounceMs, depth, ignored } = this.config;
    this.log("Starting watcher...");
    this.watcher = chokidar.watch(workspaceRoot, {
      ignoreInitial: true,
      ignored,
      awaitWriteFinish: {
        stabilityThreshold: debounceMs,
        pollInterval: 50
      },
      ignorePermissionErrors: true,
      depth,
      persistent: true
    });
    this.watcher.on("ready", () => {
      this.stats.filesWatched = this.getWatchedCount();
      this.log(`Ready. Watching ${this.stats.filesWatched} files`);
      this.emit("ready", this.stats);
    }).on("change", (path4) => this.handleChange("change", path4)).on("add", (path4) => this.handleChange("add", path4)).on("unlink", (path4) => this.handleChange("unlink", path4)).on("error", (error) => {
      const message = error instanceof Error ? error.message : String(error);
      this.log(`Error: ${message}`, true);
      this.emit("error", error);
    });
    this.watchVrekoDir();
    this.startFlushTimer();
  }
  /**
  * Stop watching
  */
  async stop() {
    if (this.flushTimer) {
      clearInterval(this.flushTimer);
      this.flushTimer = null;
    }
    await this.flushSignals();
    if (this.watcher) {
      await this.watcher.close();
      this.watcher = null;
      this.log("Stopped");
    }
  }
  /**
  * Get current stats
  */
  getStats() {
    return {
      ...this.stats
    };
  }
  /**
  * Check if watcher is running
  */
  isRunning() {
    return this.watcher !== null;
  }
  // =============================================================================
  // PRIVATE METHODS
  // =============================================================================
  handleChange(type, path4) {
    const relativePath = relative(this.config.workspaceRoot, path4);
    this.stats.lastActivityAt = (/* @__PURE__ */ new Date()).toISOString();
    const signal = {
      type: type === "add" ? "file_add" : type === "unlink" ? "file_delete" : "file_change",
      path: relativePath,
      timestamp: this.stats.lastActivityAt
    };
    const count = (this.changeCountByFile.get(relativePath) || 0) + 1;
    this.changeCountByFile.set(relativePath, count);
    const isCritical = this.isCriticalFile(relativePath);
    const isRisky = this.isRiskyChange(relativePath);
    if (isCritical) {
      signal.metadata = {
        critical: true
      };
      this.emit("signal", {
        ...signal,
        suggestion: "Consider protecting this file"
      });
    }
    if (isRisky) {
      signal.metadata = {
        ...signal.metadata,
        risky: true
      };
    }
    this.signalBuffer.push(signal);
    this.emit(type, relativePath, {
      isCritical,
      isRisky,
      changeCount: count
    });
    this.detectPatterns(relativePath, count);
    this.log(`${type}: ${relativePath}${isCritical ? " [CRITICAL]" : ""}${isRisky ? " [RISKY]" : ""}`);
  }
  watchVrekoDir() {
    const vrekoDir2 = getWorkspaceDir(this.config.workspaceRoot);
    const violationsWatcher = chokidar.watch(`${vrekoDir2}/patterns/violations.jsonl`, {
      ignoreInitial: true,
      awaitWriteFinish: {
        stabilityThreshold: 200,
        pollInterval: 50
      }
    });
    violationsWatcher.on("change", async () => {
      await this.checkViolationPromotion();
    });
  }
  async checkViolationPromotion() {
    try {
      const violations = await getViolations(this.config.workspaceRoot);
      const typeCounts = /* @__PURE__ */ new Map();
      for (const v of violations) {
        typeCounts.set(v.type, (typeCounts.get(v.type) || 0) + 1);
      }
      for (const [type, count] of typeCounts) {
        if (count >= 3) {
          this.emit("pattern", {
            type: "PROMOTION_READY",
            violationType: type,
            count,
            message: `Violation "${type}" seen ${count}x - ready for promotion`
          });
          this.stats.patternsDetected++;
        }
      }
    } catch {
    }
  }
  detectPatterns(path4, changeCount) {
    if (changeCount >= 5) {
      this.emit("pattern", {
        type: "FREQUENTLY_CHANGED",
        path: path4,
        count: changeCount,
        message: `File "${basename(path4)}" changed ${changeCount}x - consider protection`
      });
      this.stats.patternsDetected++;
    }
    if (this.isCriticalFile(path4) && changeCount >= 2) {
      this.checkProtectionSuggestion(path4);
    }
  }
  async checkProtectionSuggestion(path4) {
    const protectedFiles = await getProtectedFiles(this.config.workspaceRoot);
    const isProtected = protectedFiles.some((p) => p.pattern === path4 || p.pattern.includes("*") && new RegExp(p.pattern.replace(/\*/g, ".*")).test(path4));
    if (!isProtected) {
      this.emit("signal", {
        type: "protection_added",
        path: path4,
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        suggestion: `Consider: vr protect add "${path4}"`
      });
    }
  }
  isCriticalFile(path4) {
    return CRITICAL_FILE_PATTERNS.some((pattern) => pattern.test(path4));
  }
  isRiskyChange(path4) {
    return RISKY_CHANGE_PATTERNS.some((pattern) => pattern.test(path4));
  }
  startFlushTimer() {
    this.flushTimer = setInterval(() => {
      this.flushSignals().catch(() => {
      });
    }, 3e4);
  }
  async flushSignals() {
    if (this.signalBuffer.length === 0) {
      return;
    }
    const signals = [
      ...this.signalBuffer
    ];
    this.signalBuffer = [];
    for (const signal of signals) {
      await appendVrekoJsonl("learnings/behavioral-signals.jsonl", signal, this.config.workspaceRoot);
      this.stats.signalsRecorded++;
    }
  }
  getWatchedCount() {
    if (!this.watcher) {
      return 0;
    }
    const watched = this.watcher.getWatched();
    return Object.values(watched).reduce((acc, files) => acc + files.length, 0);
  }
  log(_message, isError = false) {
    if (this.config.verbose) ;
  }
};
function createWatcher(config2) {
  return new VrekoWatcher(config2);
}
__name(createWatcher, "createWatcher");
async function getBehavioralSignals(workspaceRoot) {
  return loadVrekoJsonl("learnings/behavioral-signals.jsonl", workspaceRoot);
}
__name(getBehavioralSignals, "getBehavioralSignals");
async function analyzeBehavioralSignals(workspaceRoot) {
  const signals = await getBehavioralSignals(workspaceRoot);
  if (signals.length === 0) {
    return [];
  }
  const fileCounts = /* @__PURE__ */ new Map();
  for (const signal of signals) {
    if (signal.type === "file_change") {
      fileCounts.set(signal.path, (fileCounts.get(signal.path) || 0) + 1);
    }
  }
  const learnings = [];
  for (const [path4, count] of fileCounts) {
    if (count >= 10) {
      learnings.push({
        id: `behavioral_${Date.now()}_${Math.random().toString(36).substring(2, 6)}`,
        type: "pattern",
        trigger: `editing ${basename(path4)}`,
        action: `This file is frequently modified (${count}x). Consider adding protection.`,
        source: "behavioral-analysis",
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
    }
  }
  return learnings;
}
__name(analyzeBehavioralSignals, "analyzeBehavioralSignals");

// src/commands/watch.ts
function createWatchCommand() {
  const watch = new Command("watch").description("Start file watcher for behavioral learning").option("-v, --verbose", "Enable verbose logging").option("--depth <number>", "Max directory depth", "10").action(async (options) => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        console.error("Not initialized. Run `vreko init` first.");
        process.exit(1);
      }
      const watcher = createWatcher({
        workspaceRoot: cwd,
        verbose: options.verbose,
        depth: Number.parseInt(options.depth, 10)
      });
      watcher.on("ready", (_stats) => {
        console.log("Watching for changes... (Ctrl+C to stop)");
      });
      watcher.on("change", (path4, meta) => {
        const icon = meta.isCritical ? chalk2.red("\u25CF") : meta.isRisky ? chalk2.yellow("\u25CF") : chalk2.blue("\u25CF");
        console.log(`${icon} ${path4}`);
        if (meta.isCritical && meta.changeCount === 1) {
          console.log(`  ${chalk2.red("Critical change detected  -  snapshot recommended")}`);
        }
      });
      watcher.on("add", (path4) => {
        console.log(`${chalk2.green("+")} ${path4}`);
      });
      watcher.on("unlink", (path4) => {
        console.log(`${chalk2.red("-")} ${path4}`);
      });
      watcher.on("signal", (signal) => {
        if (signal.suggestion) {
          console.log(`  ${chalk2.cyan("\u2192")} ${signal.suggestion}`);
        }
      });
      watcher.on("pattern", (pattern) => {
        if (pattern.type === "PROMOTION_READY") {
          console.log(`  ${chalk2.green("\u2605")} Pattern ready: ${pattern.message}`);
        } else if (pattern.type === "FREQUENTLY_CHANGED") {
          console.log(`  ${chalk2.yellow("\u21BA")} Frequently changed: ${pattern.message}`);
        }
      });
      watcher.on("error", (error) => {
        console.error(`\u2717 Watcher error: ${error.message}`);
      });
      const shutdown = /* @__PURE__ */ __name(async () => {
        await watcher.stop();
        const stats = watcher.getStats();
        if (stats.signalsRecorded > 0) {
          console.log(`
Session summary: ${stats.signalsRecorded} signal(s) recorded`);
        }
        process.exit(0);
      }, "shutdown");
      process.on("SIGINT", shutdown);
      process.on("SIGTERM", shutdown);
      await watcher.start();
      await new Promise(() => {
      });
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Watch error: ${message}`);
      process.exit(1);
    }
  });
  watch.command("status").description("Show watcher statistics").option("--json", "Output as JSON").action(async (options) => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        process.exit(1);
      }
      const signals = await getBehavioralSignals(cwd);
      const stats = {
        totalSignals: signals.length,
        byType: {},
        mostChanged: [],
        criticalChanges: 0
      };
      const fileCounts = /* @__PURE__ */ new Map();
      for (const signal of signals) {
        stats.byType[signal.type] = (stats.byType[signal.type] || 0) + 1;
        if (signal.type === "file_change") {
          fileCounts.set(signal.path, (fileCounts.get(signal.path) || 0) + 1);
        }
        if (signal.metadata?.critical) {
          stats.criticalChanges++;
        }
      }
      stats.mostChanged = [
        ...fileCounts.entries()
      ].sort((a, b) => b[1] - a[1]).slice(0, 5).map(([path4, count]) => ({
        path: path4,
        count
      }));
      if (options.json) {
        console.log(JSON.stringify(stats, null, 2));
        return;
      }
      console.log(`Total signals: ${stats.totalSignals}`);
      console.log(`Critical:      ${stats.criticalChanges}`);
      if (Object.keys(stats.byType).length > 0) {
        console.log("By type:");
        for (const [type, count] of Object.entries(stats.byType)) {
          console.log(`  ${type.padEnd(20)} ${count}`);
        }
      }
      if (stats.mostChanged.length > 0) {
        console.log("Most changed:");
        for (const { path: path4, count } of stats.mostChanged) {
          console.log(`  ${String(count).padStart(3)}x  ${path4}`);
        }
      }
      if (stats.totalSignals === 0) {
        console.log("No behavioral signals recorded yet. Start watching with `vreko watch`.");
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  watch.command("analyze").description("Analyze behavioral signals and generate learnings").option("--json", "Output as JSON").action(async (options) => {
    const cwd = process.cwd();
    try {
      if (!await isVrekoInitialized(cwd)) {
        process.exit(1);
      }
      const learnings = await analyzeBehavioralSignals(cwd);
      if (options.json) {
        console.log(JSON.stringify(learnings, null, 2));
        return;
      }
      if (learnings.length === 0) {
        console.log("No learnings generated. More behavioral data is needed.");
        return;
      }
      console.log(`Generated ${learnings.length} learning(s):`);
      for (const learning of learnings) {
        const text2 = typeof learning === "string" ? learning : learning.content ?? String(learning);
        console.log(`  \u2022 ${text2}`);
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  return watch;
}
__name(createWatchCommand, "createWatchCommand");

// ../../packages/core/dist/errors/service-error.js
function errorMessage(error) {
  return error instanceof Error ? error.message : String(error);
}
__name(errorMessage, "errorMessage");
var __defProp3 = Object.defineProperty;
var __name4 = /* @__PURE__ */ __name((target, value) => __defProp3(target, "name", {
  value,
  configurable: true
}), "__name");
var SessionId = /* @__PURE__ */ __name4((value) => value, "SessionId");
var SnapshotId = /* @__PURE__ */ __name4((value) => value, "SnapshotId");
var AuditId = /* @__PURE__ */ __name4((value) => value, "AuditId");
var ID_PREFIX = {
  SESSION: "sess",
  AUDIT: "audit",
  CHECKPOINT: "cp"
};
function randomSuffix(length = 6) {
  const chars = "abcdefghijklmnopqrstuvwxyz0123456789";
  let result7 = "";
  const bytes = new Uint8Array(length);
  crypto.getRandomValues(bytes);
  for (let i = 0; i < length; i++) {
    result7 += chars[bytes[i] % chars.length];
  }
  return result7;
}
__name(randomSuffix, "randomSuffix");
__name4(randomSuffix, "randomSuffix");
function slugify(description, maxLength = 30) {
  return description.toLowerCase().trim().replace(/[^a-z0-9\s-]/g, "").replace(/\s+/g, "-").replace(/-+/g, "-").replace(/^-|-$/g, "").slice(0, maxLength);
}
__name(slugify, "slugify");
__name4(slugify, "slugify");
function generateId2(prefix) {
  const id = nanoid();
  return prefix ? `${prefix}-${id}` : id;
}
__name(generateId2, "generateId");
__name4(generateId2, "generateId");
function generateSessionId() {
  return SessionId(`${ID_PREFIX.SESSION}-${Date.now()}-${randomSuffix()}`);
}
__name(generateSessionId, "generateSessionId");
__name4(generateSessionId, "generateSessionId");
function generateSnapshotId(description) {
  if (description && description.length > 0) {
    const slug = slugify(description);
    if (slug.length > 0) {
      return SnapshotId(`snapshot-${slug}-${Date.now()}-${nanoid(9)}`);
    }
  }
  return SnapshotId(`snapshot-${Date.now()}-${nanoid(9)}`);
}
__name(generateSnapshotId, "generateSnapshotId");
__name4(generateSnapshotId, "generateSnapshotId");
function generateAuditId() {
  return AuditId(`${ID_PREFIX.AUDIT}-${Date.now()}-${randomSuffix()}`);
}
__name(generateAuditId, "generateAuditId");
__name4(generateAuditId, "generateAuditId");
function generateCheckpointId() {
  return `${ID_PREFIX.CHECKPOINT}-${Date.now()}-${randomSuffix()}`;
}
__name(generateCheckpointId, "generateCheckpointId");
__name4(generateCheckpointId, "generateCheckpointId");
function randomId(length = 6) {
  return randomSuffix(length);
}
__name(randomId, "randomId");
__name4(randomId, "randomId");
function parseIdTimestamp(id) {
  if (!id.match(/^(?:sess|snap|snapshot|audit|cp)-/)) {
    return null;
  }
  const match = id.match(/-(\d{13})-[a-zA-Z0-9_-]+$/);
  if (!match) {
    return null;
  }
  const timestamp = Number.parseInt(match[1], 10);
  return Number.isNaN(timestamp) ? null : timestamp;
}
__name(parseIdTimestamp, "parseIdTimestamp");
__name4(parseIdTimestamp, "parseIdTimestamp");
function parseIdPrefix(id) {
  if (id.startsWith("snapshot-")) {
    return "snap";
  }
  const match = id.match(/^(sess|snap|audit|cp)-/);
  return match ? match[1] : null;
}
__name(parseIdPrefix, "parseIdPrefix");
__name4(parseIdPrefix, "parseIdPrefix");
function isValidId(id, expectedPrefix) {
  const isSnapshotId = id.startsWith("snap-") || id.startsWith("snapshot-");
  if (expectedPrefix === "snap" || !expectedPrefix && isSnapshotId) {
    return /^(?:snap|snapshot)-(?:[a-z0-9]+-)?(\d{13})-[a-zA-Z0-9_-]{9}$/.test(id);
  }
  if (expectedPrefix) {
    const regex = new RegExp(`^${expectedPrefix}-\\d{13}-[a-z0-9]{6}$`);
    return regex.test(id);
  }
  return /^(?:sess|audit|cp)-\d{13}-[a-z0-9]{6}$/.test(id);
}
__name(isValidId, "isValidId");
__name4(isValidId, "isValidId");
function createLogger2(options) {
  const { name, level = 1, timestamps = false } = options;
  return {
    debug(messageOrObj, metaOrMsg) {
    },
    info(messageOrObj, metaOrMsg) {
    },
    warn(messageOrObj, metaOrMsg) {
    },
    error(messageOrObj, metaOrMsg) {
    }
  };
}
__name(createLogger2, "createLogger");
__name4(createLogger2, "createLogger");
function createSilentLogger() {
  return {
    debug: /* @__PURE__ */ __name4(() => {
    }, "debug"),
    info: /* @__PURE__ */ __name4(() => {
    }, "info"),
    warn: /* @__PURE__ */ __name4(() => {
    }, "warn"),
    error: /* @__PURE__ */ __name4(() => {
    }, "error")
  };
}
__name(createSilentLogger, "createSilentLogger");
__name4(createSilentLogger, "createSilentLogger");
(class _NoOpInstrumentationProvider {
  static {
    __name(this, "_NoOpInstrumentationProvider");
  }
  static {
    __name4(this, "NoOpInstrumentationProvider");
  }
  static noopSpan = {
    setAttribute: /* @__PURE__ */ __name4(() => {
    }, "setAttribute"),
    setAttributes: /* @__PURE__ */ __name4(() => {
    }, "setAttributes"),
    addEvent: /* @__PURE__ */ __name4(() => {
    }, "addEvent"),
    setStatus: /* @__PURE__ */ __name4(() => {
    }, "setStatus"),
    recordException: /* @__PURE__ */ __name4(() => {
    }, "recordException"),
    end: /* @__PURE__ */ __name4(() => {
    }, "end"),
    isRecording: /* @__PURE__ */ __name4(() => false, "isRecording")
  };
  startSpan(_name, _options) {
    return _NoOpInstrumentationProvider.noopSpan;
  }
  async withSpan(_name, fn, _options) {
    return await fn(_NoOpInstrumentationProvider.noopSpan);
  }
  injectContext(_carrier) {
  }
  extractContext(_carrier) {
    return null;
  }
  recordMetric(_name, _value, _attributes) {
  }
  recordEvent(_name, _attributes) {
  }
  async shutdown() {
  }
});
var HttpAttributes = {
  /** HTTP request method (GET, POST, etc.) */
  METHOD: "http.method",
  /** HTTP response status code */
  STATUS_CODE: "http.status_code",
  /** HTTP route pattern */
  ROUTE: "http.route",
  /** HTTP target URL path */
  TARGET: "http.target",
  /** HTTP scheme (http, https) */
  SCHEME: "http.scheme",
  /** HTTP host header */
  HOST: "http.host",
  /** User agent string */
  USER_AGENT: "http.user_agent"};
var NetworkAttributes = {
  /** Client IP address */
  PEER_IP: "net.peer.ip"};
var SemanticConventions = {
  Http: HttpAttributes,
  Network: NetworkAttributes};
var SpanStatusCode = /* @__PURE__ */ (function(SpanStatusCode2) {
  SpanStatusCode2[SpanStatusCode2["UNSET"] = 0] = "UNSET";
  SpanStatusCode2[SpanStatusCode2["OK"] = 1] = "OK";
  SpanStatusCode2[SpanStatusCode2["ERROR"] = 2] = "ERROR";
  return SpanStatusCode2;
})({});
var TRACE_PARENT_HEADER = "traceparent";
var TRACE_STATE_HEADER = "tracestate";
function extractTraceHeaders(headers) {
  const traceHeaders = {};
  if (headers instanceof Headers) {
    headers.forEach((value, key) => {
      const lowerKey = key.toLowerCase();
      if (lowerKey === TRACE_PARENT_HEADER || lowerKey === TRACE_STATE_HEADER) {
        traceHeaders[key] = value;
      }
    });
  } else {
    for (const [key, value] of Object.entries(headers)) {
      const lowerKey = key.toLowerCase();
      if (lowerKey === TRACE_PARENT_HEADER || lowerKey === TRACE_STATE_HEADER) {
        traceHeaders[key] = value;
      }
    }
  }
  return traceHeaders;
}
__name(extractTraceHeaders, "extractTraceHeaders");
__name4(extractTraceHeaders, "extractTraceHeaders");
function setHttpRequestAttributes(span, request) {
  const { method, url, target, route, host, userAgent, clientIp, scheme, forwardedProto } = request;
  span.setAttribute(SemanticConventions.Http.METHOD, method);
  if (target) {
    span.setAttribute(SemanticConventions.Http.TARGET, target);
  }
  if (route) {
    span.setAttribute(SemanticConventions.Http.ROUTE, route);
  }
  if (host) {
    span.setAttribute(SemanticConventions.Http.HOST, host);
  }
  if (userAgent) {
    span.setAttribute(SemanticConventions.Http.USER_AGENT, userAgent);
  }
  if (clientIp) {
    span.setAttribute(SemanticConventions.Network.PEER_IP, clientIp);
  }
  const detectedScheme = forwardedProto || scheme || (url.startsWith("https") ? "https" : "http");
  span.setAttribute(SemanticConventions.Http.SCHEME, detectedScheme);
}
__name(setHttpRequestAttributes, "setHttpRequestAttributes");
__name4(setHttpRequestAttributes, "setHttpRequestAttributes");
function setSpanStatusFromHttp(span, statusCode, options) {
  span.setAttribute(SemanticConventions.Http.STATUS_CODE, statusCode);
  if (statusCode >= 500) {
    span.setStatus({
      code: SpanStatusCode.ERROR,
      message: options?.errorMessage || `HTTP ${statusCode}`
    });
    if (options?.addErrorEvent) {
      span.addEvent("server_error", {
        status: statusCode,
        ...options.errorMessage && {
          message: options.errorMessage
        }
      });
    }
  } else if (statusCode >= 400) {
    span.setStatus({
      code: SpanStatusCode.OK
    });
    if (options?.addErrorEvent) {
      span.addEvent("client_error", {
        status: statusCode,
        ...options.errorMessage && {
          message: options.errorMessage
        }
      });
    }
  } else {
    span.setStatus({
      code: SpanStatusCode.OK
    });
  }
}
__name(setSpanStatusFromHttp, "setSpanStatusFromHttp");
__name4(setSpanStatusFromHttp, "setSpanStatusFromHttp");
var PurchaseTypeEnum = z.enum([
  "ONE_TIME",
  "SUBSCRIPTION",
  "addon"
]);
z.object({
  id: z.string(),
  organizationId: z.string().nullable().optional(),
  userId: z.string().nullable().optional(),
  type: PurchaseTypeEnum,
  customerId: z.string(),
  subscriptionId: z.string().nullable().optional(),
  productId: z.string(),
  status: z.string().nullable().optional(),
  createdAt: z.date(),
  updatedAt: z.date().nullable().optional()
});
function toNeverthrow(legacy) {
  if (legacy.success) {
    return ok(legacy.value);
  }
  return err(legacy.error);
}
__name(toNeverthrow, "toNeverthrow");
__name4(toNeverthrow, "toNeverthrow");
function fromNeverthrow(result7) {
  if (result7.isOk()) {
    return {
      success: true,
      value: result7.value
    };
  }
  return {
    success: false,
    error: result7.error
  };
}
__name(fromNeverthrow, "fromNeverthrow");
__name4(fromNeverthrow, "fromNeverthrow");
function legacyOk(value) {
  return {
    success: true,
    value
  };
}
__name(legacyOk, "legacyOk");
__name4(legacyOk, "legacyOk");
function legacyErr(error) {
  return {
    success: false,
    error
  };
}
__name(legacyErr, "legacyErr");
__name4(legacyErr, "legacyErr");
function legacyIsOk(result7) {
  return result7.success === true;
}
__name(legacyIsOk, "legacyIsOk");
__name4(legacyIsOk, "legacyIsOk");
function legacyIsErr(result7) {
  return result7.success === false;
}
__name(legacyIsErr, "legacyIsErr");
__name4(legacyIsErr, "legacyIsErr");
function createDefaultWorkspace(userId, repoPath) {
  return {
    workspaceId: `ws_default_${userId}`,
    workspaceName: "Personal Workspace",
    repoPath,
    userId,
    permissions: {
      read: true,
      write: true,
      manage: true,
      invite: false
    },
    resolvedFrom: "default_workspace"
  };
}
__name(createDefaultWorkspace, "createDefaultWorkspace");
__name4(createDefaultWorkspace, "createDefaultWorkspace");
function hasPermission(binding, permission) {
  return binding.permissions[permission];
}
__name(hasPermission, "hasPermission");
__name4(hasPermission, "hasPermission");
function generateCacheKey(repoPath, userId) {
  return `workspace_resolution:${userId}:${repoPath}`;
}
__name(generateCacheKey, "generateCacheKey");
__name4(generateCacheKey, "generateCacheKey");
var SessionSchemaVersion = "sb.session.v1";
var ChangeOpSchema = z.enum([
  "created",
  "modified",
  "deleted",
  "renamed"
]);
var EOLTypeSchema = z.enum([
  "lf",
  "crlf"
]);
var SessionTriggerSchema = z.enum([
  "filewatch",
  "pre-commit",
  "manual",
  "idle-finalize"
]);
var SessionChangeSchema = z.object({
  /** Relative POSIX path from workspace root */
  p: z.string(),
  /** Operation type */
  op: ChangeOpSchema,
  /** Prior relative path (for rename operations only) */
  from: z.string().optional(),
  /** SHA-256 hash before change (CAS reference) - computed on finalize */
  hOld: z.string().optional(),
  /** SHA-256 hash after change (CAS reference) - computed on finalize */
  hNew: z.string().optional(),
  /** File size before change (bytes) */
  sizeBefore: z.number().int().nonnegative().optional(),
  /** File size after change (bytes) */
  sizeAfter: z.number().int().nonnegative().optional(),
  /** Modification time before change (Unix epoch ms) */
  mtimeBefore: z.number().int().nonnegative().optional(),
  /** Modification time after change (Unix epoch ms) */
  mtimeAfter: z.number().int().nonnegative().optional(),
  /** File permissions before change (Unix mode) */
  modeBefore: z.number().int().nonnegative().optional(),
  /** File permissions after change (Unix mode) */
  modeAfter: z.number().int().nonnegative().optional(),
  /** Line ending style before change */
  eolBefore: EOLTypeSchema.optional(),
  /** Line ending style after change */
  eolAfter: EOLTypeSchema.optional()
});
z.object({
  /** Schema version for backward compatibility */
  schema: z.literal(SessionSchemaVersion),
  /** Unique session identifier (CUID) */
  sessionId: z.string(),
  /** Session start timestamp (ISO 8601) */
  startedAt: z.string().datetime(),
  /** Session end timestamp (ISO 8601) - undefined if active */
  endedAt: z.string().datetime().optional(),
  /** VS Code workspace folder URI (multi-root workspace safe) */
  workspaceUri: z.string(),
  /** Offline-generated semantic label (never transmitted) */
  name: z.string().optional(),
  /** Trigger sources for this session */
  triggers: z.array(SessionTriggerSchema),
  /** Total number of file changes in this session */
  changeCount: z.number().int().nonnegative(),
  /** Chronological list of file changes */
  filesChanged: z.array(SessionChangeSchema),
  /** Array of snapshot IDs created during this session */
  snapshots: z.array(z.string()).optional()
});
z.object({
  sessionId: z.string(),
  startedAt: z.string().datetime(),
  endedAt: z.string().datetime().optional(),
  name: z.string().optional(),
  changeCount: z.number().int().nonnegative(),
  triggers: z.array(SessionTriggerSchema)
});
z.object({
  /** Only return sessions for this workspace URI */
  workspaceUri: z.string().optional(),
  /** Only return active sessions (endedAt is null) */
  activeOnly: z.boolean().optional(),
  /** Only return finalized sessions (endedAt is not null) */
  finalizedOnly: z.boolean().optional(),
  /** Return sessions that started after this timestamp */
  after: z.date().optional(),
  /** Return sessions that started before this timestamp */
  before: z.date().optional(),
  /** Maximum number of results */
  limit: z.number().int().positive().optional(),
  /** Offset for pagination */
  offset: z.number().int().nonnegative().optional()
});
z.object({
  /** VS Code workspace folder URI */
  workspaceUri: z.string(),
  /** Initial trigger sources */
  triggers: z.array(SessionTriggerSchema).default([
    "filewatch"
  ]),
  /** Optional semantic name (generated offline) */
  name: z.string().optional()
});
z.object({
  /** VS Code workspace folder URI (multi-root safe) */
  workspaceUri: z.string(),
  /** Idle timeout in milliseconds (default: 15 minutes) */
  idleMs: z.number().int().positive().default(15 * 6e4),
  /** Batch size for flushing changes to database (default: 50) */
  flushBatchSize: z.number().int().positive().default(50),
  /** Flush interval in milliseconds (default: 5 seconds) */
  flushIntervalMs: z.number().int().positive().default(5e3),
  /** Use VS Code file system watcher (default: true) */
  useVSCodeWatcher: z.boolean().default(true),
  /** Patterns to ignore (.snapbackignore) */
  ignorePatterns: z.array(z.string()).default([
    "node_modules/**",
    ".next/**",
    "dist/**",
    "build/**",
    "coverage/**",
    ".git/**",
    "*.log",
    "*.tmp",
    "*.swp",
    ".DS_Store"
  ])
});
function encodeTriggerBitmask(triggers) {
  let mask = 0;
  for (const trigger of triggers) {
    switch (trigger) {
      case "filewatch":
        mask |= 1;
        break;
      case "pre-commit":
        mask |= 2;
        break;
      case "manual":
        mask |= 4;
        break;
      case "idle-finalize":
        mask |= 8;
        break;
    }
  }
  return mask;
}
__name(encodeTriggerBitmask, "encodeTriggerBitmask");
__name4(encodeTriggerBitmask, "encodeTriggerBitmask");
function decodeTriggerBitmask(mask) {
  const triggers = [];
  if (mask & 1) {
    triggers.push("filewatch");
  }
  if (mask & 2) {
    triggers.push("pre-commit");
  }
  if (mask & 4) {
    triggers.push("manual");
  }
  if (mask & 8) {
    triggers.push("idle-finalize");
  }
  return triggers;
}
__name(decodeTriggerBitmask, "decodeTriggerBitmask");
__name4(decodeTriggerBitmask, "decodeTriggerBitmask");
var ModificationSourceSchema = z.enum([
  "extension",
  "mcp",
  "daemon",
  "cli"
]);
var ModificationTypeSchema = z.enum([
  "create",
  "update",
  "delete"
]);
var FileModificationSchema = z.object({
  /** Absolute path to the modified file */
  path: z.string().min(1, "Path cannot be empty"),
  /** Modification timestamp (ms since epoch) */
  timestamp: z.number().positive("Timestamp must be positive"),
  /** Type of modification */
  type: ModificationTypeSchema,
  /** Lines changed (0 if unknown or delete) */
  linesChanged: z.number().int().nonnegative().default(0),
  /** Whether this change was AI-attributed (detected by AIPresenceDetector) */
  aiAttributed: z.boolean().default(false),
  /** Which AI tool made the change, if detected (e.g., 'copilot', 'cursor', 'claude') */
  aiTool: z.string().nullable().default(null),
  /** Source surface that recorded this modification */
  source: ModificationSourceSchema
});
function parseFileModification(input) {
  return FileModificationSchema.parse(input);
}
__name(parseFileModification, "parseFileModification");
__name4(parseFileModification, "parseFileModification");
function fromMCPFileChange(change) {
  return {
    path: change.file,
    timestamp: change.timestamp,
    type: change.type === "created" ? "create" : change.type === "deleted" ? "delete" : "update",
    linesChanged: change.linesChanged,
    aiAttributed: change.aiAttributed,
    aiTool: null,
    source: "mcp"
  };
}
__name(fromMCPFileChange, "fromMCPFileChange");
__name4(fromMCPFileChange, "fromMCPFileChange");
function toMCPFileChange(mod) {
  return {
    file: mod.path,
    type: mod.type === "create" ? "created" : mod.type === "delete" ? "deleted" : "modified",
    timestamp: mod.timestamp,
    aiAttributed: mod.aiAttributed,
    linesChanged: mod.linesChanged
  };
}
__name(toMCPFileChange, "toMCPFileChange");
__name4(toMCPFileChange, "toMCPFileChange");
function fromIntelligenceFileModification(mod, source = "extension") {
  return {
    path: mod.path,
    timestamp: mod.timestamp,
    type: mod.type,
    linesChanged: mod.linesChanged ?? 0,
    aiAttributed: false,
    aiTool: null,
    source
  };
}
__name(fromIntelligenceFileModification, "fromIntelligenceFileModification");
__name4(fromIntelligenceFileModification, "fromIntelligenceFileModification");
function toIntelligenceFileModification(mod) {
  return {
    path: mod.path,
    timestamp: mod.timestamp,
    type: mod.type,
    linesChanged: mod.linesChanged
  };
}
__name(toIntelligenceFileModification, "toIntelligenceFileModification");
__name4(toIntelligenceFileModification, "toIntelligenceFileModification");
function filterModificationsSince(mods, since) {
  return mods.filter((m) => m.timestamp >= since);
}
__name(filterModificationsSince, "filterModificationsSince");
__name4(filterModificationsSince, "filterModificationsSince");
function getUniqueModifiedPaths(mods) {
  return [
    ...new Set(mods.map((m) => m.path))
  ];
}
__name(getUniqueModifiedPaths, "getUniqueModifiedPaths");
__name4(getUniqueModifiedPaths, "getUniqueModifiedPaths");
function countAIAttributedModifications(mods) {
  return mods.filter((m) => m.aiAttributed).length;
}
__name(countAIAttributedModifications, "countAIAttributedModifications");
__name4(countAIAttributedModifications, "countAIAttributedModifications");
function getTotalLinesChanged(mods) {
  return mods.reduce((sum, m) => sum + m.linesChanged, 0);
}
__name(getTotalLinesChanged, "getTotalLinesChanged");
__name4(getTotalLinesChanged, "getTotalLinesChanged");
function groupByAITool(mods) {
  const groups = /* @__PURE__ */ new Map();
  for (const mod of mods) {
    const tool = mod.aiTool;
    if (!groups.has(tool)) {
      groups.set(tool, []);
    }
    groups.get(tool)?.push(mod);
  }
  return groups;
}
__name(groupByAITool, "groupByAITool");
__name4(groupByAITool, "groupByAITool");
z.object({
  /** Active extension IDs in the IDE environment */
  extensionIds: z.array(z.string()).default([]),
  /** Optional file content to analyze for AI signatures */
  content: z.string().optional(),
  /** Character velocity (chars/ms) from burst detection */
  velocity: z.number().nonnegative().optional(),
  /** Total characters changed in the operation */
  charCount: z.number().int().nonnegative().optional()
});
var AiDetectionOutputSchema = z.object({
  /** Detected AI tool name, or null if none detected */
  tool: z.string().nullable(),
  /** Confidence score (0-1) */
  confidence: z.number().min(0).max(1),
  /** Detection method that triggered */
  method: z.enum([
    "extension",
    "velocity",
    "pattern",
    "combined"
  ]).nullable(),
  /** Indicators that contributed to detection */
  indicators: z.array(z.string()).optional()
});
z.object({
  /** Code content to scan for threats */
  content: z.string()
});
var ThreatPatternSchema = z.object({
  /** Description of the threat pattern */
  description: z.string(),
  /** Severity score (0-1), where 1 is most critical */
  severity: z.number().min(0).max(1)
});
var ThreatDetectionOutputSchema = z.object({
  /** Total number of threats detected */
  threatCount: z.number().int().nonnegative(),
  /** List of detected threat patterns */
  patterns: z.array(ThreatPatternSchema),
  /** Overall severity level */
  severity: z.enum([
    "none",
    "low",
    "medium",
    "high",
    "critical"
  ]),
  /** Aggregated threat score (0-10) */
  score: z.number().min(0).max(10)
});
z.object({
  /** File path being analyzed */
  filePath: z.string(),
  /** Number of characters changed */
  charCount: z.number().int().nonnegative(),
  /** Timestamp of the change (ms since epoch) */
  timestamp: z.number().int().positive().optional()
});
var BurstDetectionOutputSchema = z.object({
  /** Whether a burst was detected */
  isBurst: z.boolean(),
  /** Character velocity (chars/ms) */
  velocity: z.number().nonnegative(),
  /** File path analyzed */
  filePath: z.string(),
  /** Total characters in the change */
  charCount: z.number().int().nonnegative(),
  /** Timestamp of detection */
  timestamp: z.number().int().positive()
});
var ComplexityFileInputSchema = z.object({
  /** File path */
  path: z.string(),
  /** File content */
  content: z.string(),
  /** Line count of the file */
  lineCount: z.number().int().nonnegative()
});
z.object({
  /** Files to analyze */
  files: z.array(ComplexityFileInputSchema)
});
var ComplexityAnalysisOutputSchema = z.object({
  /** Average complexity score across all files (0-1) */
  avgComplexity: z.number().min(0).max(1),
  /** Maximum complexity score of any single file (0-1) */
  maxComplexity: z.number().min(0).max(1),
  /** List of files with complexity > 0.7 */
  highComplexityFiles: z.array(z.string()),
  /** Number of files analyzed */
  fileCount: z.number().int().nonnegative(),
  /** Overall complexity value (same as avgComplexity) */
  value: z.number().min(0).max(1)
});
z.object({
  /** Extension IDs for AI detection */
  extensionIds: z.array(z.string()).default([]),
  /** Content to analyze (for threats, AI patterns, complexity) */
  content: z.string(),
  /** File path being analyzed */
  filePath: z.string(),
  /** Line count (for complexity calculation) */
  lineCount: z.number().int().nonnegative().optional(),
  /** Character count (for burst detection) */
  charCount: z.number().int().nonnegative().optional(),
  /** Velocity (for AI detection) */
  velocity: z.number().nonnegative().optional(),
  /** Timestamp (for burst detection) */
  timestamp: z.number().int().positive().optional()
});
z.object({
  /** Signal name */
  signal: z.enum([
    "ai",
    "threats",
    "burst",
    "complexity"
  ]),
  /** Signal-specific score/value */
  value: z.number(),
  /** Whether this signal is considered "triggered" */
  triggered: z.boolean()
});
z.object({
  /** Individual signal results */
  signals: z.object({
    ai: AiDetectionOutputSchema,
    threats: ThreatDetectionOutputSchema,
    burst: BurstDetectionOutputSchema.optional(),
    complexity: ComplexityAnalysisOutputSchema
  }),
  /** Overall risk score (0-1), weighted combination of all signals */
  overallRisk: z.number().min(0).max(1),
  /** Risk level classification */
  riskLevel: z.enum([
    "low",
    "medium",
    "high",
    "critical"
  ]),
  /** Summary of triggered signals */
  triggeredSignals: z.array(z.enum([
    "ai",
    "threats",
    "burst",
    "complexity"
  ])),
  /** Processing time in milliseconds */
  processingTimeMs: z.number().nonnegative()
});
var SignalTypeSchema = z.enum([
  "ai",
  "threats",
  "burst",
  "complexity",
  "comprehensive"
]);
z.object({
  /** Error code */
  code: z.string(),
  /** Human-readable error message */
  message: z.string(),
  /** Signal type that failed */
  signal: SignalTypeSchema.optional()
});
var SyncPayloadSchema = z.object({
  sessionId: z.string().uuid(),
  workspaceId: z.string(),
  startedAt: z.number(),
  endedAt: z.number().nullable(),
  durationMs: z.number().nullable(),
  clientType: z.string(),
  snapshotCount: z.number().int().min(0),
  learningCount: z.number().int().min(0),
  touchedFileCount: z.number().int().min(0),
  riskScores: z.array(z.number().min(0).max(1))
}).strict();
function buildSyncPayload(params) {
  const durationMs = params.endedAt ? params.endedAt - params.startedAt : null;
  return SyncPayloadSchema.parse({
    sessionId: params.sessionId,
    workspaceId: params.workspaceId,
    startedAt: params.startedAt,
    endedAt: params.endedAt,
    durationMs,
    clientType: params.clientType,
    snapshotCount: params.snapshotCount,
    learningCount: params.learningCount,
    touchedFileCount: params.touchedFileCount,
    riskScores: params.riskScores
  });
}
__name(buildSyncPayload, "buildSyncPayload");
__name4(buildSyncPayload, "buildSyncPayload");
var SeveritySchema = z.enum([
  "critical",
  "high",
  "medium",
  "low",
  "info"
]);
var RiskSeveritySchema = z.enum([
  "critical",
  "high",
  "medium",
  "low"
]);
var ValidationSeveritySchema = z.enum([
  "critical",
  "warning",
  "info"
]);
function toSeverity(validationSeverity) {
  switch (validationSeverity) {
    case "critical":
      return "critical";
    case "warning":
      return "medium";
    case "info":
      return "info";
  }
}
__name(toSeverity, "toSeverity");
__name4(toSeverity, "toSeverity");
function toValidationSeverity(severity) {
  switch (severity) {
    case "critical":
    case "high":
      return "critical";
    case "medium":
    case "low":
      return "warning";
    case "info":
      return "info";
  }
}
__name(toValidationSeverity, "toValidationSeverity");
__name4(toValidationSeverity, "toValidationSeverity");
var BaseIssueSchema = z.object({
  /** Severity level */
  severity: z.union([
    SeveritySchema,
    ValidationSeveritySchema
  ]),
  /** Issue type code (e.g., UNSAFE_EVAL, PATH_TRAVERSAL) */
  type: z.string(),
  /** Human-readable message */
  message: z.string(),
  /** Line number (1-indexed) */
  line: z.number().optional(),
  /** Suggested fix */
  fix: z.string().optional()
});
BaseIssueSchema.extend({
  severity: ValidationSeveritySchema
});
BaseIssueSchema.extend({
  /** Unique identifier for deduplication: analyzer/type/file/line */
  id: z.string(),
  /** Severity level */
  severity: SeveritySchema,
  /** File path where issue was found */
  file: z.string().optional(),
  /** Column number (1-indexed) */
  column: z.number().optional(),
  /** Code snippet showing the issue */
  snippet: z.string().optional(),
  /** Rule ID if from a lint tool */
  rule: z.string().optional()
});
z.object({
  /** Whether validation passed */
  passed: z.boolean(),
  /** Issues found */
  issues: z.array(BaseIssueSchema),
  /** Duration in milliseconds */
  duration: z.number().optional()
});
var CircuitBreakerStateEnumSchema = z.enum([
  "closed",
  "open",
  "half-open"
]);
z.object({
  /** Current state */
  state: CircuitBreakerStateEnumSchema,
  /** Failure count */
  failures: z.number(),
  /** Failure threshold */
  threshold: z.number(),
  /** Last failure timestamp */
  lastFailure: z.number().optional(),
  /** Cooldown period in ms */
  cooldownMs: z.number()
});
var ConfigFileTypeSchema = z.enum([
  "package",
  "typescript",
  "linting",
  "build",
  "environment",
  "testing",
  "framework",
  "database",
  "ci"
]);
var SupportedLanguageSchema = z.enum([
  "javascript",
  "typescript",
  "python",
  "universal"
]);
var FileBaselineSchema = z.object({
  path: z.string(),
  hash: z.string(),
  timestamp: z.number(),
  size: z.number()
});
z.object({
  path: z.string(),
  type: ConfigFileTypeSchema,
  language: SupportedLanguageSchema,
  critical: z.boolean().default(false),
  baseline: FileBaselineSchema.optional()
});
z.object({
  type: z.string(),
  path: z.string(),
  name: z.string(),
  critical: z.boolean().default(false)
});
z.object({
  content: z.any(),
  valid: z.boolean(),
  error: z.string().optional(),
  metadata: z.record(z.string(), z.any()).optional()
});
z.object({
  valid: z.boolean(),
  errors: z.array(z.string()).default([]),
  warnings: z.array(z.string()).default([])
});
z.object({
  type: z.enum([
    "added",
    "modified",
    "deleted"
  ]),
  file: z.string(),
  timestamp: z.number(),
  baseline: FileBaselineSchema.optional()
});
z.object({
  autoDetect: z.boolean().default(true),
  watchChanges: z.boolean().default(true),
  autoProtect: z.boolean().default(true),
  customPatterns: z.array(z.any()).optional()
});
z.object({
  enabled: z.boolean(),
  patterns: z.array(z.string()).optional(),
  threshold: z.number().optional(),
  includePatterns: z.array(z.string()).optional(),
  excludePatterns: z.array(z.string()).optional()
});
var LearningTypeSchema = z.enum([
  "pattern",
  "pitfall",
  "efficiency",
  "workflow",
  "discovery"
]);
var LearningIntentSchema = z.enum([
  "implement",
  "debug",
  "refactor",
  "review"
]);
z.enum([
  "add-flag",
  "set-env",
  "inject-validation",
  "warn",
  "suggest-file"
]);
var WarnSeveritySchema = z.enum([
  "info",
  "warning",
  "error"
]);
var AddFlagPayloadSchema = z.object({
  flag: z.string().min(1).describe("CLI flag to add, e.g. '--validate-expiry'"),
  value: z.union([
    z.string(),
    z.boolean()
  ]).optional().describe("Flag value if applicable"),
  reason: z.string().min(1).describe("Human-readable reason shown in verbose mode")
});
var SetEnvPayloadSchema = z.object({
  key: z.string().min(1).regex(/^[A-Z_][A-Z0-9_]*$/, "Must be valid env var name"),
  value: z.string().describe("Environment variable value"),
  reason: z.string().min(1).describe("Human-readable reason shown in verbose mode")
});
var InjectValidationPayloadSchema = z.object({
  validationType: z.string().min(1).describe("Validation type identifier, e.g. 'jwt-expiry', 'silent-catch'"),
  targetFile: z.string().optional().describe("Optional file to target for validation"),
  reason: z.string().min(1).describe("Human-readable reason shown in verbose mode")
});
var WarnPayloadSchema = z.object({
  message: z.string().min(1).describe("Warning message to display"),
  severity: WarnSeveritySchema.describe("Severity level affects display styling")
});
var SuggestFilePayloadSchema = z.object({
  filePath: z.string().min(1).describe("Suggested file path, may contain {feature} placeholder"),
  reason: z.string().min(1).describe("Human-readable reason for suggestion")
});
var LearningTriggerSchema = z.object({
  commands: z.array(z.string()).optional().describe("Command names that trigger this learning, e.g. ['auth', 'deploy']"),
  intent: z.array(LearningIntentSchema).optional().describe("Intent types that trigger this learning"),
  filePatterns: z.array(z.string()).optional().describe("Glob patterns for file matching, e.g. ['**/*auth*.ts']"),
  flags: z.array(z.string()).optional().describe("CLI flags that trigger this learning, e.g. ['--provider']"),
  description: z.string().min(1).describe("Human-readable description for fuzzy matching")
});
var LearningActionSchema = z.discriminatedUnion("type", [
  z.object({
    type: z.literal("add-flag"),
    payload: AddFlagPayloadSchema
  }),
  z.object({
    type: z.literal("set-env"),
    payload: SetEnvPayloadSchema
  }),
  z.object({
    type: z.literal("inject-validation"),
    payload: InjectValidationPayloadSchema
  }),
  z.object({
    type: z.literal("warn"),
    payload: WarnPayloadSchema
  }),
  z.object({
    type: z.literal("suggest-file"),
    payload: SuggestFilePayloadSchema
  })
]);
var LearningSchema = z.object({
  id: z.string().min(1).describe("Unique identifier for this learning"),
  type: LearningTypeSchema.describe("Categorical classification"),
  trigger: LearningTriggerSchema.describe("Conditions that activate this learning"),
  action: LearningActionSchema.describe("Concrete action to execute"),
  confidence: z.number().min(0).max(1).describe("Confidence score (0.0-1.0), static at creation"),
  created: z.number().int().positive().describe("Creation timestamp (ms since epoch)"),
  lastUsed: z.number().int().positive().optional().describe("Last usage timestamp (ms since epoch)"),
  usageCount: z.number().int().nonnegative().default(0).describe("Number of times this learning was applied"),
  tags: z.array(z.string()).default([]).describe("Searchable tags for categorization"),
  archived: z.boolean().default(false).describe("Whether this learning is archived (excluded from matching)")
});
var LearningInputSchema = LearningSchema.omit({
  id: true,
  created: true,
  lastUsed: true,
  usageCount: true,
  archived: true
}).extend({
  tags: z.array(z.string()).optional().default([]),
  archived: z.boolean().optional()
});
z.enum([
  "observe",
  "warn",
  "apply-safe",
  "apply-all",
  "off"
]);
z.object({
  workspaceId: z.string().min(1),
  startedAt: z.number().int().positive(),
  lastCommandAt: z.number().int().positive(),
  appliedLearnings: z.array(z.string()).describe("Learning IDs applied in this session")
});
z.object({
  workspaceId: z.string().min(1),
  commandName: z.string().min(1),
  args: z.record(z.unknown()).optional().describe("Parsed command arguments/flags"),
  filesOrPaths: z.array(z.string()).optional().describe("Files involved in command"),
  intent: LearningIntentSchema.optional().describe("User intent if known")
});
var SelectedLearningSchema = z.object({
  id: z.string(),
  title: z.string().describe("Human-readable title derived from trigger.description"),
  type: LearningTypeSchema,
  score: z.number().min(0).max(1),
  action: LearningActionSchema,
  tags: z.array(z.string())
});
z.object({
  selectedLearnings: z.array(SelectedLearningSchema),
  debug: z.object({
    evaluatedCount: z.number().int().nonnegative(),
    durationMs: z.number().nonnegative(),
    skippedReason: z.string().optional()
  }).optional()
});
var SENSITIVE_DATA_PATTERNS = [
  /api[_-]?key/i,
  /secret/i,
  /password/i,
  /token/i,
  /credential/i,
  /private[_-]?key/i,
  /auth[_-]?token/i,
  /bearer/i,
  /ssh[_-]?key/i,
  /aws[_-]?access/i,
  /database[_-]?url/i,
  /connection[_-]?string/i
];
function containsSensitiveData(text2) {
  return SENSITIVE_DATA_PATTERNS.some((pattern) => pattern.test(text2));
}
__name(containsSensitiveData, "containsSensitiveData");
__name4(containsSensitiveData, "containsSensitiveData");
function validateLearningForSensitiveData(learning) {
  const violations = [];
  if (containsSensitiveData(learning.trigger.description)) {
    violations.push("trigger.description");
  }
  learning.trigger.filePatterns?.forEach((pattern, i) => {
    if (containsSensitiveData(pattern)) {
      violations.push(`trigger.filePatterns[${i}]`);
    }
  });
  const payload = learning.action.payload;
  if ("reason" in payload && typeof payload.reason === "string" && containsSensitiveData(payload.reason)) {
    violations.push("action.payload.reason");
  }
  if ("message" in payload && typeof payload.message === "string" && containsSensitiveData(payload.message)) {
    violations.push("action.payload.message");
  }
  if ("filePath" in payload && typeof payload.filePath === "string" && containsSensitiveData(payload.filePath)) {
    violations.push("action.payload.filePath");
  }
  learning.tags?.forEach((tag, i) => {
    if (containsSensitiveData(tag)) {
      violations.push(`tags[${i}]`);
    }
  });
  return violations;
}
__name(validateLearningForSensitiveData, "validateLearningForSensitiveData");
__name4(validateLearningForSensitiveData, "validateLearningForSensitiveData");
function createLearning(input, idGenerator) {
  const parsed = LearningInputSchema.parse(input);
  const violations = validateLearningForSensitiveData(parsed);
  if (violations.length > 0) {
    throw new Error(`Learning contains sensitive data in: ${violations.join(", ")}`);
  }
  return {
    ...parsed,
    id: idGenerator(),
    created: Date.now(),
    usageCount: 0,
    archived: parsed.archived ?? false
  };
}
__name(createLearning, "createLearning");
__name4(createLearning, "createLearning");
function calculateRecencyDecay(lastUsed, maxAgeDays = 90) {
  if (!lastUsed) {
    return 0.5;
  }
  const ageMs = Date.now() - lastUsed;
  const ageDays = ageMs / (1e3 * 60 * 60 * 24);
  const decay = 1 - ageDays / maxAgeDays * 0.5;
  return Math.max(0.5, Math.min(1, decay));
}
__name(calculateRecencyDecay, "calculateRecencyDecay");
__name4(calculateRecencyDecay, "calculateRecencyDecay");
function calculateLearningScore(learning) {
  const recencyDecay = calculateRecencyDecay(learning.lastUsed);
  return learning.confidence * recencyDecay;
}
__name(calculateLearningScore, "calculateLearningScore");
__name4(calculateLearningScore, "calculateLearningScore");
var ProtectionLevelSchema = z.enum([
  "watch",
  "warn",
  "block"
]);
z.object({
  level: ProtectionLevelSchema,
  icon: z.string(),
  label: z.string(),
  description: z.string(),
  color: z.string(),
  themeColor: z.string().optional()
});
var ProtectedFileSchema = z.object({
  path: z.string(),
  level: ProtectionLevelSchema,
  reason: z.string().optional(),
  addedAt: z.date(),
  pattern: z.string().optional()
});
var PatternRuleSchema = z.object({
  pattern: z.string(),
  level: ProtectionLevelSchema,
  reason: z.string().optional(),
  enabled: z.boolean().default(true)
});
var ProtectionConfigSchema = z.object({
  patterns: z.array(PatternRuleSchema).default([]),
  defaultLevel: ProtectionLevelSchema.default("watch"),
  enabled: z.boolean().default(true),
  autoProtectConfigs: z.boolean().default(true)
});
z.object({
  config: ProtectionConfigSchema.optional(),
  persistRegistry: z.boolean().default(true),
  registryPath: z.string().optional()
});
z.object({
  isProtected: z.boolean(),
  level: ProtectionLevelSchema.optional(),
  reason: z.string().optional(),
  file: ProtectedFileSchema.optional()
});
var DEFAULT_CRITICAL_PATTERNS = Object.freeze([
  // Dependency locks - wrong versions break builds
  {
    pattern: "**/package-lock.json",
    level: "block",
    reason: "Lock file - wrong version breaks reproducible Node.js builds"
  },
  {
    pattern: "**/yarn.lock",
    level: "block",
    reason: "Lock file - ensures reproducible Yarn installs"
  },
  {
    pattern: "**/pnpm-lock.yaml",
    level: "block",
    reason: "Lock file - critical for pnpm monorepos"
  },
  {
    pattern: "**/poetry.lock",
    level: "block",
    reason: "Lock file - Python dependency lock"
  },
  {
    pattern: "**/Cargo.lock",
    level: "block",
    reason: "Lock file - Rust dependency lock"
  },
  {
    pattern: "**/go.sum",
    level: "block",
    reason: "Lock file - Go module checksums"
  },
  {
    pattern: "**/Gemfile.lock",
    level: "block",
    reason: "Lock file - Ruby gem dependencies"
  },
  {
    pattern: "**/composer.lock",
    level: "block",
    reason: "Lock file - PHP composer dependencies"
  },
  // Environment & Secrets - exposing causes immediate security breaches
  {
    pattern: "**/.env*",
    level: "block",
    reason: "Sensitive environment variables and secrets"
  },
  // Core configuration files - wrong changes break builds
  {
    pattern: "package.json",
    level: "warn",
    reason: "Core Node.js configuration - dependencies and scripts"
  },
  {
    pattern: "tsconfig.json",
    level: "warn",
    reason: "TypeScript compiler configuration"
  },
  // Infrastructure - controls deployment and infrastructure
  {
    pattern: "Dockerfile",
    level: "warn",
    reason: "Container image definition"
  },
  {
    pattern: "docker-compose.yml",
    level: "warn",
    reason: "Multi-container orchestration"
  },
  {
    pattern: "**/docker-compose.yaml",
    level: "warn",
    reason: "Multi-container orchestration (yaml variant)"
  },
  {
    pattern: "**/*.tf",
    level: "warn",
    reason: "Terraform infrastructure definitions"
  },
  {
    pattern: ".github/workflows/*.yml",
    level: "warn",
    reason: "GitHub Actions CI/CD workflows"
  },
  {
    pattern: ".github/workflows/*.yaml",
    level: "warn",
    reason: "GitHub Actions CI/CD workflows (yaml variant)"
  }
]);
var EXTENDED_PATTERNS = Object.freeze([
  // Documentation - passive watching
  {
    pattern: "*.md",
    level: "watch",
    reason: "Documentation files"
  },
  {
    pattern: "*.txt",
    level: "watch",
    reason: "Text files"
  },
  {
    pattern: "README*",
    level: "watch",
    reason: "README documentation"
  },
  // General configuration files
  {
    pattern: "*.json",
    level: "watch",
    reason: "JSON configuration files"
  },
  {
    pattern: ".editorconfig",
    level: "watch",
    reason: "Editor configuration"
  },
  {
    pattern: ".prettierrc*",
    level: "watch",
    reason: "Prettier formatting configuration"
  },
  {
    pattern: ".eslintrc*",
    level: "watch",
    reason: "ESLint configuration"
  },
  {
    pattern: ".babelrc",
    level: "watch",
    reason: "Babel transpiler configuration"
  },
  {
    pattern: ".gitignore",
    level: "warn",
    reason: "Git ignore rules"
  },
  // IDE and editor settings
  {
    pattern: ".vscode/settings.json",
    level: "watch",
    reason: "VS Code settings"
  },
  {
    pattern: ".idea/**",
    level: "watch",
    reason: "IDE configuration directory"
  },
  // Build tool configurations
  {
    pattern: "vite.config.*",
    level: "warn",
    reason: "Vite bundler configuration"
  },
  {
    pattern: "webpack.config.*",
    level: "warn",
    reason: "Webpack bundler configuration"
  },
  {
    pattern: "rollup.config.*",
    level: "warn",
    reason: "Rollup bundler configuration"
  },
  {
    pattern: "esbuild.config.*",
    level: "warn",
    reason: "esbuild bundler configuration"
  },
  {
    pattern: "Makefile",
    level: "watch",
    reason: "Make build configuration"
  },
  {
    pattern: "CMakeLists.txt",
    level: "watch",
    reason: "CMake build configuration"
  },
  // Language-specific package managers and configs
  {
    pattern: "requirements.txt",
    level: "watch",
    reason: "Python dependencies"
  },
  {
    pattern: "Gemfile",
    level: "warn",
    reason: "Ruby gem configuration"
  },
  {
    pattern: "composer.json",
    level: "warn",
    reason: "PHP composer configuration"
  },
  {
    pattern: "setup.py",
    level: "warn",
    reason: "Python package setup"
  },
  {
    pattern: "pyproject.toml",
    level: "warn",
    reason: "Python project configuration"
  },
  {
    pattern: "pom.xml",
    level: "warn",
    reason: "Maven Java build configuration"
  },
  {
    pattern: "build.gradle*",
    level: "warn",
    reason: "Gradle Java build configuration"
  },
  {
    pattern: "*.csproj",
    level: "warn",
    reason: ".NET C# project file"
  },
  {
    pattern: "go.mod",
    level: "warn",
    reason: "Go module definition"
  },
  {
    pattern: "Cargo.toml",
    level: "warn",
    reason: "Rust package configuration"
  },
  {
    pattern: "bunfig.toml",
    level: "warn",
    reason: "Bun runtime configuration"
  },
  {
    pattern: "*.sln",
    level: "watch",
    reason: "Visual Studio solution file"
  },
  // Kubernetes and container orchestration
  {
    pattern: "kubernetes/*.yaml",
    level: "warn",
    reason: "Kubernetes manifests"
  }
]);
Object.freeze([
  ...DEFAULT_CRITICAL_PATTERNS,
  ...EXTENDED_PATTERNS
]);
var LEGACY_TO_CANONICAL = {
  Watched: "watch",
  Warning: "warn",
  Protected: "block"
};
var CANONICAL_TO_LEGACY = {
  watch: "Watched",
  warn: "Warning",
  block: "Protected"
};
var VALID_LEVELS = [
  "watch",
  "warn",
  "block"
];
function legacyToCanonical(legacy) {
  return LEGACY_TO_CANONICAL[legacy];
}
__name(legacyToCanonical, "legacyToCanonical");
__name4(legacyToCanonical, "legacyToCanonical");
function canonicalToLegacy(canonical) {
  return CANONICAL_TO_LEGACY[canonical];
}
__name(canonicalToLegacy, "canonicalToLegacy");
__name4(canonicalToLegacy, "canonicalToLegacy");
function isProtectionLevel(value) {
  return typeof value === "string" && VALID_LEVELS.includes(value);
}
__name(isProtectionLevel, "isProtectionLevel");
__name4(isProtectionLevel, "isProtectionLevel");
var DiffChangeSchema = z.object({
  added: z.boolean().optional().default(false),
  removed: z.boolean().optional().default(false),
  value: z.string(),
  count: z.number().optional()
});
var RiskScoreSchema = z.object({
  score: z.number().min(0).max(10),
  factors: z.array(z.string()),
  severity: RiskSeveritySchema
});
z.object({
  metrics: z.record(z.string(), z.number()),
  trends: z.record(z.string(), z.array(z.number())),
  insights: z.array(z.string()),
  timestamp: z.number(),
  snapshotRecommendations: z.object({
    shouldCreateSnapshot: z.boolean(),
    reason: z.string(),
    urgency: RiskSeveritySchema,
    suggestedTiming: z.string()
  }).optional()
});
z.object({
  trigger: z.string().default("manual"),
  risk: z.number().min(0).max(10).optional(),
  content: z.string().optional(),
  files: z.array(z.string()).optional()
});
z.object({
  trigger: z.string().default("manual"),
  risk: z.number().min(0).max(10).optional(),
  content: z.string().optional()
});
z.object({
  id: z.string(),
  timestamp: z.number(),
  meta: z.object({
    trigger: z.string().optional(),
    risk: z.number().optional()
  }).optional()
});
z.object({
  changes: z.array(DiffChangeSchema)
});
z.object({
  before: z.record(z.string(), z.any()),
  after: z.record(z.string(), z.any())
});
z.object({
  message: z.string(),
  code: z.string().optional(),
  details: z.any().optional()
});
var RetrySchema = z.object({
  retries: z.number().int().min(0).default(2),
  factor: z.number().min(1).default(2),
  min: z.number().int().default(250),
  max: z.number().int().default(1500),
  jitter: z.boolean().default(true)
});
var CircuitSchema = z.object({
  enabled: z.boolean().default(true),
  errorThresholdPercentage: z.number().int().min(1).max(100).default(50),
  volumeThreshold: z.number().int().min(1).default(10),
  timeoutMs: z.number().int().default(5e3),
  resetMs: z.number().int().default(3e4),
  rollingCountMs: z.number().int().default(6e4),
  rollingCountBuckets: z.number().int().default(6)
});
z.object({
  timeoutMs: z.number().int().default(5e3),
  maxConcurrent: z.number().int().min(1).default(4),
  retry: RetrySchema,
  circuit: CircuitSchema,
  batch: z.object({
    size: z.number().int().min(1).default(5),
    maxWaitMs: z.number().int().default(150)
  })
});
z.object({
  debounceMs: z.number().int().default(120),
  awaitWriteFinish: z.object({
    stabilityThreshold: z.number().int().default(200),
    pollInterval: z.number().int().default(50)
  }),
  ignored: z.array(z.string()).default([
    "**/{node_modules,.git,.vscode,dist,.next,.nuxt,coverage}/**"
  ])
});
var SnapshotTriggerSchema = z.enum([
  "manual",
  "auto",
  "auto_save",
  "ai_detected",
  "ai-detected",
  "pre_save",
  "pre-save",
  "session_start",
  "session_end",
  "mcp_snap_start",
  "cli_protect",
  "api_request",
  "engine_internal",
  "recovery"
]);
var SnapshotOriginSchema = z.enum([
  "manual",
  "auto",
  "ai-detected",
  "recovery",
  "INTERACTIVE",
  "AUTOMATED"
]);
var SnapshotReasonCodeSchema = z.enum([
  // AI detection
  "AI_DETECTED",
  // Manual operations
  "MANUAL_CHECKPOINT",
  "MANUAL_SAVE",
  // Risk-based triggers
  "CRITICAL_FILE",
  "HIGH_RISK",
  "RISK_BURST_START",
  "RISK_LARGE_DELETE",
  "RISK_MULTI_FILE",
  // Session lifecycle
  "SESSION_START",
  "SESSION_END",
  "IDLE_FINALIZE",
  // Rollback operations
  "PRE_ROLLBACK",
  // Optimization modes
  "BURST_MODE"
]);
var CheckpointTypeSchema = z.enum([
  "PRE",
  "POST",
  "PRE_ROLLBACK"
]);
var SnapshotSchema = z.object({
  id: z.string(),
  timestamp: z.number(),
  version: z.string().optional().default("1.0"),
  meta: z.record(z.string(), z.any()).optional(),
  files: z.array(z.string()).optional(),
  fileContents: z.record(z.string(), z.string()).optional()
});
var FileStateSchema = z.object({
  path: z.string(),
  content: z.string(),
  /** SHA-256 hash of content (optional, computed for dedup) */
  hash: z.string().optional(),
  /** File size in bytes */
  size: z.number().optional(),
  /** Encrypted data (for sensitive files - VSCode format) */
  encrypted: z.object({
    /** Initialization vector */
    iv: z.string(),
    /** Authentication tag */
    tag: z.string(),
    /** Optional: algorithm used (default: aes-256-gcm) */
    algorithm: z.string().optional()
  }).optional()
});
var CompressionCodecSchema = z.enum([
  "zstd",
  "gzip",
  "none"
]);
var SnapshotFileRefV2Schema = z.object({
  /** SHA-256 hash of file content (CAS reference) */
  blobHash: z.string(),
  /** File size in bytes */
  size: z.number(),
  /** Compression codec used (optional, defaults to 'none') */
  codec: CompressionCodecSchema.optional()
});
z.object({
  id: z.string(),
  timestamp: z.number(),
  files: z.array(FileStateSchema)
});
z.enum([
  "pending",
  "complete",
  "failed",
  "deleted"
]);
SnapshotSchema.extend({
  name: z.string(),
  fileStates: z.array(FileStateSchema).optional(),
  isProtected: z.boolean(),
  icon: z.string().optional(),
  iconColor: z.string().optional()
});
z.object({
  id: z.string(),
  name: z.string(),
  timestamp: z.number(),
  fileCount: z.number(),
  origin: SnapshotOriginSchema.optional(),
  isProtected: z.boolean(),
  label: z.string().optional()
});
z.object({
  path: z.string(),
  content: z.string(),
  action: z.enum([
    "add",
    "modify",
    "delete"
  ])
});
z.object({
  /** Description/reason for the snapshot */
  description: z.string().optional(),
  /** Whether to protect from auto-deletion */
  protected: z.boolean().optional(),
  /** What triggered the snapshot */
  trigger: SnapshotTriggerSchema.optional(),
  /** Origin classification for DORA metrics */
  origin: SnapshotOriginSchema.optional(),
  /** Time since last file change in ms (DORA lead time metric) */
  timeSinceLastChangeMs: z.number().optional()
});
z.object({
  filePath: z.string().optional(),
  before: z.date().optional(),
  after: z.date().optional(),
  protected: z.boolean().optional(),
  limit: z.number().int().positive().optional(),
  offset: z.number().int().nonnegative().optional()
});
var FileDiffSchema = z.object({
  path: z.string(),
  operation: z.enum([
    "create",
    "modify",
    "delete"
  ]),
  linesAdded: z.number(),
  linesRemoved: z.number(),
  preview: z.string(),
  currentChecksum: z.string().optional(),
  snapshotChecksum: z.string().optional()
});
var DiffPreviewSchema = z.object({
  totalFiles: z.number(),
  filesCreated: z.number(),
  filesModified: z.number(),
  filesDeleted: z.number(),
  totalLinesAdded: z.number(),
  totalLinesRemoved: z.number(),
  diffs: z.array(FileDiffSchema)
});
var ConflictReportSchema = z.object({
  path: z.string(),
  reason: z.string(),
  currentChecksum: z.string(),
  snapshotChecksum: z.string()
});
z.object({
  success: z.boolean(),
  restoredFiles: z.array(z.string()),
  errors: z.array(z.string()).optional(),
  diffPreview: DiffPreviewSchema.optional(),
  conflicts: z.array(ConflictReportSchema).optional(),
  verification: z.object({
    allVerified: z.boolean(),
    results: z.array(z.object({
      path: z.string(),
      verified: z.boolean(),
      checksum: z.string(),
      expected: z.string()
    }))
  }).optional()
});
z.object({
  enableDeduplication: z.boolean().default(true),
  namingStrategy: z.enum([
    "git",
    "semantic",
    "timestamp",
    "custom"
  ]).default("semantic"),
  autoProtect: z.boolean().default(false),
  maxSnapshots: z.number().int().positive().optional()
});
z.object({
  id: z.string(),
  path: z.string(),
  hash: z.string().optional(),
  size: z.number().optional(),
  language: z.string().optional(),
  risk: RiskScoreSchema.optional(),
  lastModified: z.number().optional(),
  createdAt: z.number().optional()
});
z.object({
  // Core fields (required)
  id: z.string(),
  timestamp: z.number(),
  fileCount: z.number(),
  // Analytics fields (optional)
  totalSize: z.number().optional(),
  riskScore: RiskScoreSchema.optional(),
  tags: z.array(z.string()).optional(),
  // V2 Hierarchy fields (from VSCode ManifestV2)
  /** Sequential snapshot number (1-based, monotonic) */
  seq: z.number().int().positive().optional(),
  /** Parent snapshot seq (null for root) */
  parentSeq: z.number().int().positive().nullable().optional(),
  /** Parent snapshot ID */
  parentId: z.string().nullable().optional(),
  /** Checkpoint type */
  type: CheckpointTypeSchema.optional(),
  /** Main file that triggered this snapshot */
  anchorFile: z.string().optional(),
  // DORA Metrics fields
  /** Time since last file change in ms (for lead time metric) */
  timeSinceLastChangeMs: z.number().optional(),
  /** Compression ratio achieved (for storage efficiency) */
  compressionRatio: z.number().optional(),
  /** Storage size in bytes (after compression) */
  storageSizeBytes: z.number().optional(),
  // Origin & Classification
  /** Origin classification for analytics */
  origin: SnapshotOriginSchema.optional(),
  /** Reason codes for explainability */
  reasons: z.array(SnapshotReasonCodeSchema).optional(),
  // AI Detection
  aiDetection: z.object({
    detected: z.boolean(),
    tool: z.string().optional(),
    confidence: z.number().min(0).max(1).optional()
  }).optional(),
  // Session linkage
  /** SnapBack session ID */
  sessionId: z.string().optional(),
  /** External task ID (from LLM agent) */
  taskId: z.string().optional(),
  // UI fields
  name: z.string().optional(),
  icon: z.string().optional()
});
z.object({
  /** Schema version - always 2 for V2 */
  schemaVersion: z.literal(2),
  /** Unique ID: snap-{timestamp}-{random} */
  id: z.string(),
  /** Sequential snapshot number (1-based, monotonic) */
  seq: z.number().int().positive(),
  /** Parent snapshot seq (null for root) */
  parentSeq: z.number().int().positive().nullable(),
  /** Parent snapshot ID (null for root) */
  parentId: z.string().nullable(),
  /** Unix timestamp (ms) */
  timestamp: z.number(),
  /** Human-readable name */
  name: z.string(),
  /** Checkpoint type */
  type: CheckpointTypeSchema,
  /** The main file that triggered this snapshot */
  anchorFile: z.string(),
  /** Files in snapshot (path → ref). Empty for PRE checkpoints. */
  files: z.record(z.string(), SnapshotFileRefV2Schema),
  /** Optional metadata */
  metadata: z.object({
    /** Risk score 0-1 */
    riskScore: z.number().min(0).max(1).optional(),
    /** Origin classification */
    origin: SnapshotOriginSchema.optional(),
    /** Stable reason codes */
    reasons: z.array(SnapshotReasonCodeSchema).optional(),
    /** AI detection info */
    aiDetection: z.object({
      detected: z.boolean(),
      tool: z.string().optional(),
      confidence: z.number().optional()
    }).optional(),
    /** SnapBack session ID */
    sessionId: z.string().optional(),
    /** External task ID */
    taskId: z.string().optional(),
    /** DORA: Time since last change */
    timeSinceLastChangeMs: z.number().optional()
  }).optional()
});
z.object({
  /** Unique ID */
  id: z.string(),
  /** Unix timestamp (ms) */
  timestamp: z.number(),
  /** Human-readable name */
  name: z.string(),
  /** Trigger reason */
  trigger: z.enum([
    "auto",
    "manual",
    "ai-detected",
    "pre-save"
  ]),
  /** Main file that triggered snapshot */
  anchorFile: z.string(),
  /** Files in snapshot (path → ref) */
  files: z.record(z.string(), z.object({
    blob: z.string(),
    size: z.number()
  })),
  /** Optional metadata */
  metadata: z.object({
    riskScore: z.number().optional(),
    aiDetection: z.object({
      detected: z.boolean(),
      tool: z.string().optional(),
      confidence: z.number().optional()
    }).optional(),
    sessionId: z.string().optional(),
    taskId: z.string().optional()
  }).optional()
});
function isSnapshotManifestV2(manifest) {
  return typeof manifest === "object" && manifest !== null && "schemaVersion" in manifest && manifest.schemaVersion === 2;
}
__name(isSnapshotManifestV2, "isSnapshotManifestV2");
__name4(isSnapshotManifestV2, "isSnapshotManifestV2");
function isPostCheckpoint(manifest) {
  if (!isSnapshotManifestV2(manifest)) {
    return true;
  }
  return manifest.type === "POST";
}
__name(isPostCheckpoint, "isPostCheckpoint");
__name4(isPostCheckpoint, "isPostCheckpoint");
function isPointerCheckpoint(manifest) {
  if (!isSnapshotManifestV2(manifest)) {
    return false;
  }
  return manifest.type === "PRE" || manifest.type === "PRE_ROLLBACK";
}
__name(isPointerCheckpoint, "isPointerCheckpoint");
__name4(isPointerCheckpoint, "isPointerCheckpoint");
z.object({
  /** Total files in snapshot */
  totalFiles: z.number().int().nonnegative(),
  /** New blobs written to storage (not deduplicated) */
  newBlobsWritten: z.number().int().nonnegative(),
  /** Files that were deduplicated (already existed in CAS) */
  dedupedFiles: z.number().int().nonnegative(),
  /** Deduplication ratio (0-1) - higher means more storage saved */
  dedupRatio: z.number().min(0).max(1),
  /** Actual bytes written to storage */
  bytesWritten: z.number().int().nonnegative(),
  /** Original size of all files (before dedup) */
  originalSize: z.number().int().nonnegative(),
  /** Storage savings in bytes (originalSize - bytesWritten) */
  bytesSaved: z.number().int().nonnegative()
});
function calculateDedupRatio(newBlobs, totalFiles) {
  if (totalFiles === 0) {
    return 0;
  }
  return 1 - newBlobs / totalFiles;
}
__name(calculateDedupRatio, "calculateDedupRatio");
__name4(calculateDedupRatio, "calculateDedupRatio");
function createStorageMetrics(blobResults) {
  const totalFiles = blobResults.length;
  const newBlobsWritten = blobResults.filter((r) => r.isNew).length;
  const dedupedFiles = totalFiles - newBlobsWritten;
  const originalSize = blobResults.reduce((sum, r) => sum + r.size, 0);
  const bytesWritten = blobResults.filter((r) => r.isNew).reduce((sum, r) => sum + r.size, 0);
  const bytesSaved = originalSize - bytesWritten;
  const dedupRatio = calculateDedupRatio(newBlobsWritten, totalFiles);
  return {
    totalFiles,
    newBlobsWritten,
    dedupedFiles,
    dedupRatio,
    bytesWritten,
    originalSize,
    bytesSaved
  };
}
__name(createStorageMetrics, "createStorageMetrics");
__name4(createStorageMetrics, "createStorageMetrics");
z.object({
  workspaceId: z.string(),
  period: z.object({
    start: z.number(),
    end: z.number()
  }),
  risk: RiskScoreSchema,
  fileStats: z.object({
    total: z.number(),
    byLanguage: z.record(z.string(), z.number()),
    byRisk: z.record(z.string(), z.number())
  }),
  snapshotStats: z.object({
    total: z.number(),
    frequency: z.number(),
    averageSize: z.number().optional()
  }),
  snapshotRecommendations: z.object({
    shouldCreateSnapshot: z.boolean(),
    reason: z.string(),
    urgency: RiskSeveritySchema,
    suggestedTiming: z.string()
  }),
  trends: z.object({
    risk: z.array(z.object({
      timestamp: z.number(),
      score: z.number()
    })),
    activity: z.array(z.object({
      timestamp: z.number(),
      count: z.number()
    }))
  })
});

// ../../packages-oss/infrastructure/dist/index.js
var __defProp4 = Object.defineProperty;
var __name5 = /* @__PURE__ */ __name((target, value) => __defProp4(target, "name", {
  value,
  configurable: true
}), "__name");
var redactPaths = [
  "user.email",
  "user.password",
  "apiKey",
  "session.token",
  "req.headers.authorization",
  "auth.*.password",
  "config.*.secret",
  "env.*"
];
var pinoLogger = pino({
  level: process.env.LOG_LEVEL || "info",
  redact: {
    paths: redactPaths,
    censor: "[REDACTED]"
  }
});
var logger = {
  debug: /* @__PURE__ */ __name5((message, meta) => {
    if (meta) {
      pinoLogger.debug(meta, message);
    } else {
      pinoLogger.debug(message);
    }
  }, "debug"),
  info: /* @__PURE__ */ __name5((message, meta) => {
    if (meta) {
      pinoLogger.info(meta, message);
    } else {
      pinoLogger.info(message);
    }
  }, "info"),
  warn: /* @__PURE__ */ __name5((message, meta) => {
    if (meta) {
      pinoLogger.warn(meta, message);
    } else {
      pinoLogger.warn(message);
    }
  }, "warn"),
  error: /* @__PURE__ */ __name5((message, meta) => {
    if (meta instanceof Error) {
      pinoLogger.error(meta, message);
    } else if (meta) {
      pinoLogger.error(meta, message);
    } else {
      pinoLogger.error(message);
    }
  }, "error"),
  // Add child method for creating scoped loggers
  child: /* @__PURE__ */ __name5((bindings) => {
    const childLogger = pinoLogger.child(bindings);
    return {
      debug: /* @__PURE__ */ __name5((message, meta) => {
        if (meta) {
          childLogger.debug(meta, message);
        } else {
          childLogger.debug(message);
        }
      }, "debug"),
      info: /* @__PURE__ */ __name5((message, meta) => {
        if (meta) {
          childLogger.info(meta, message);
        } else {
          childLogger.info(message);
        }
      }, "info"),
      warn: /* @__PURE__ */ __name5((message, meta) => {
        if (meta) {
          childLogger.warn(meta, message);
        } else {
          childLogger.warn(message);
        }
      }, "warn"),
      error: /* @__PURE__ */ __name5((message, meta) => {
        if (meta instanceof Error) {
          childLogger.error(meta, message);
        } else if (meta) {
          childLogger.error(meta, message);
        } else {
          childLogger.error(message);
        }
      }, "error"),
      level: childLogger.level,
      child: /* @__PURE__ */ __name5((nestedBindings) => logger.child({
        ...bindings,
        ...nestedBindings
      }), "child")
    };
  }, "child")
};
var ERROR_BUDGET = 0.01;
var ALERT_THRESHOLD = 5e-3;
var errorMetrics = {
  totalRequests: 0,
  errorCount: 0,
  lastAlertTime: 0
};
function recordSuccess() {
  errorMetrics.totalRequests++;
}
__name(recordSuccess, "recordSuccess");
__name5(recordSuccess, "recordSuccess");
function recordError() {
  errorMetrics.totalRequests++;
  errorMetrics.errorCount++;
}
__name(recordError, "recordError");
__name5(recordError, "recordError");
function getErrorRate() {
  if (errorMetrics.totalRequests === 0) {
    return 0;
  }
  return errorMetrics.errorCount / errorMetrics.totalRequests;
}
__name(getErrorRate, "getErrorRate");
__name5(getErrorRate, "getErrorRate");
async function checkErrorBudget() {
  const errorRate = getErrorRate();
  if (errorRate > ALERT_THRESHOLD && Date.now() - errorMetrics.lastAlertTime > 6e4) {
    logger.warn("Error rate approaching budget threshold", {
      errorRate: `${(errorRate * 100).toFixed(2)}%`,
      threshold: `${(ALERT_THRESHOLD * 100).toFixed(2)}%`,
      errorCount: errorMetrics.errorCount,
      totalRequests: errorMetrics.totalRequests
    });
    errorMetrics.lastAlertTime = Date.now();
  }
  if (errorRate > ERROR_BUDGET) {
    logger.error("\u{1F6A8} Error budget exceeded!", {
      errorRate: `${(errorRate * 100).toFixed(2)}%`,
      budget: `${(ERROR_BUDGET * 100).toFixed(2)}%`,
      errorCount: errorMetrics.errorCount,
      totalRequests: errorMetrics.totalRequests,
      recommendation: "Investigate root cause immediately and consider rolling back"
    });
    await sendAlert({
      channel: "#alerts",
      message: `\u{1F6A8} Error budget exceeded! Current error rate: ${(errorRate * 100).toFixed(2)}% (Budget: ${(ERROR_BUDGET * 100).toFixed(2)}%)`
    });
  }
}
__name(checkErrorBudget, "checkErrorBudget");
__name5(checkErrorBudget, "checkErrorBudget");
async function sendAlert(alert) {
  logger.info("Alert sent", alert);
}
__name(sendAlert, "sendAlert");
__name5(sendAlert, "sendAlert");
function resetMetrics() {
  errorMetrics.totalRequests = 0;
  errorMetrics.errorCount = 0;
  errorMetrics.lastAlertTime = 0;
}
__name(resetMetrics, "resetMetrics");
__name5(resetMetrics, "resetMetrics");
function getMetrics() {
  return {
    ...errorMetrics,
    errorRate: getErrorRate()
  };
}
__name(getMetrics, "getMetrics");
__name5(getMetrics, "getMetrics");
var __defProp5 = Object.defineProperty;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __name6 = /* @__PURE__ */ __name((target, value) => __defProp5(target, "name", {
  value,
  configurable: true
}), "__name");
var __esm = /* @__PURE__ */ __name((fn, res) => /* @__PURE__ */ __name(function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
}, "__init"), "__esm");
var __export = /* @__PURE__ */ __name((target, all) => {
  for (var name in all) __defProp5(target, name, {
    get: all[name],
    enumerable: true
  });
}, "__export");
var SessionRecovery_exports = {};
__export(SessionRecovery_exports, {
  SessionRecovery: /* @__PURE__ */ __name(() => SessionRecovery, "SessionRecovery")
});
var SessionRecovery;
var init_SessionRecovery = __esm({
  "src/session/SessionRecovery.ts"() {
    SessionRecovery = class {
      static {
        __name(this, "SessionRecovery");
      }
      static {
        __name6(this, "SessionRecovery");
      }
      workspaceRoot;
      journalDir;
      constructor(workspaceRoot, journalDir = path10.join(workspaceRoot, ".sb_journal")) {
        this.workspaceRoot = workspaceRoot;
        this.journalDir = journalDir;
      }
      /**
      * Recover all pending transactions
      *
      * Call this on startup to ensure workspace consistency after crashes
      */
      async recoverAll() {
        const results = [];
        try {
          const pendingDir = path10.join(this.journalDir, "pending");
          if (!existsSync(pendingDir)) {
            return results;
          }
          const files = await fs3.readdir(pendingDir);
          for (const file of files) {
            if (!file.endsWith(".json")) {
              continue;
            }
            const filePath = path10.join(pendingDir, file);
            try {
              const content = await fs3.readFile(filePath, "utf-8");
              const journal = JSON.parse(content);
              const result7 = await this.recoverJournal(journal, filePath);
              results.push(result7);
            } catch (error) {
              results.push({
                sessionId: path10.basename(file, ".json"),
                status: "failed",
                filesRestored: 0,
                error: error instanceof Error ? error.message : String(error)
              });
            }
          }
          await this.cleanupOldJournals();
          return results;
        } catch (_error) {
          return results;
        }
      }
      /**
      * Recover a single journal entry
      */
      async recoverJournal(journal, journalPath) {
        const result7 = {
          sessionId: journal.sessionId,
          status: "cleaned",
          filesRestored: 0
        };
        try {
          const backupExists = await this.checkBackupsExist(journal.backups);
          if (!backupExists) {
            await fs3.unlink(journalPath);
            result7.status = "cleaned";
            return result7;
          }
          const restoredCount = await this.rollbackFromBackups(journal.backups);
          result7.filesRestored = restoredCount;
          result7.status = "recovered";
          await fs3.unlink(journalPath);
          this.emitRecoveryTelemetry(journal.sessionId, restoredCount);
          return result7;
        } catch (error) {
          result7.status = "failed";
          result7.error = error instanceof Error ? error.message : String(error);
          return result7;
        }
      }
      /**
      * Check if any backup files exist
      */
      async checkBackupsExist(backups) {
        for (const backup of backups) {
          if (existsSync(backup.backup)) {
            return true;
          }
        }
        return false;
      }
      /**
      * Rollback from per-file backups
      */
      async rollbackFromBackups(backups) {
        let restoredCount = 0;
        for (const { original, backup } of backups) {
          try {
            if (!existsSync(backup)) {
              continue;
            }
            await this.safeRename(backup, original);
            restoredCount++;
            if (existsSync(backup)) {
              await fs3.unlink(backup);
            }
          } catch (_error) {
          }
        }
        return restoredCount;
      }
      /**
      * Safe file rename with EXDEV fallback (cross-device support for Windows)
      */
      async safeRename(src, dst) {
        try {
          await fs3.rename(src, dst);
        } catch (error) {
          if (error.code === "EXDEV") {
            await fs3.copyFile(src, dst);
            await fs3.unlink(src);
          } else {
            throw error;
          }
        }
      }
      /**
      * Cleanup old committed journals (older than 7 days)
      */
      async cleanupOldJournals() {
        try {
          const committedDir = path10.join(this.journalDir, "committed");
          if (!existsSync(committedDir)) {
            return;
          }
          const files = await fs3.readdir(committedDir);
          const now = Date.now();
          const sevenDaysMs = 7 * 24 * 60 * 60 * 1e3;
          for (const file of files) {
            if (!file.endsWith(".json")) {
              continue;
            }
            const filePath = path10.join(committedDir, file);
            try {
              const content = await fs3.readFile(filePath, "utf-8");
              const journal = JSON.parse(content);
              if (now - journal.timestamp > sevenDaysMs) {
                await fs3.unlink(filePath);
              }
            } catch {
            }
          }
        } catch {
        }
      }
      /**
      * Emit recovery telemetry (privacy-safe: no file paths)
      */
      emitRecoveryTelemetry(_sessionId, _filesRestored) {
      }
      /**
      * Manual recovery: Get list of pending journals
      */
      async listPendingJournals() {
        const pendingDir = path10.join(this.journalDir, "pending");
        try {
          if (!existsSync(pendingDir)) {
            return [];
          }
          const files = await fs3.readdir(pendingDir);
          const journals = [];
          for (const file of files) {
            if (!file.endsWith(".json")) {
              continue;
            }
            const filePath = path10.join(pendingDir, file);
            const content = await fs3.readFile(filePath, "utf-8");
            const journal = JSON.parse(content);
            journals.push(journal);
          }
          return journals;
        } catch {
          return [];
        }
      }
      /**
      * Manual recovery: Force recover a specific journal
      */
      async recoverJournalById(sessionId) {
        const journalPath = path10.join(this.journalDir, "pending", `${sessionId}.json`);
        if (!existsSync(journalPath)) {
          return {
            sessionId,
            status: "failed",
            filesRestored: 0,
            error: "Journal not found"
          };
        }
        const content = await fs3.readFile(journalPath, "utf-8");
        const journal = JSON.parse(content);
        return this.recoverJournal(journal, journalPath);
      }
      /**
      * Manual recovery: Delete a specific journal without recovery
      */
      async deleteJournal(sessionId) {
        const pendingPath = path10.join(this.journalDir, "pending", `${sessionId}.json`);
        const committedPath = path10.join(this.journalDir, "committed", `${sessionId}.json`);
        if (existsSync(pendingPath)) {
          await fs3.unlink(pendingPath);
        }
        if (existsSync(committedPath)) {
          await fs3.unlink(committedPath);
        }
      }
      /**
      * Cleanup all orphaned backup files (.bak-* files)
      */
      async cleanupOrphanedBackups() {
        let cleanedCount = 0;
        try {
          const orphanedBackups = await this.findOrphanedBackups(this.workspaceRoot);
          for (const backupPath of orphanedBackups) {
            try {
              await fs3.unlink(backupPath);
              cleanedCount++;
            } catch {
            }
          }
          return cleanedCount;
        } catch {
          return cleanedCount;
        }
      }
      /**
      * Find orphaned backup files (*.bak-* pattern)
      */
      async findOrphanedBackups(dir) {
        const backups = [];
        try {
          const entries = await fs3.readdir(dir, {
            withFileTypes: true
          });
          for (const entry of entries) {
            const fullPath = path10.join(dir, entry.name);
            if (entry.isDirectory()) {
              if (entry.name === ".git" || entry.name === "node_modules" || entry.name === ".next" || entry.name === "dist" || entry.name === "build") {
                continue;
              }
              const subBackups = await this.findOrphanedBackups(fullPath);
              backups.push(...subBackups);
            } else if (entry.name.includes(".bak-")) {
              backups.push(fullPath);
            }
          }
        } catch {
        }
        return backups;
      }
    };
  }
});
var SessionRollback_exports = {};
__export(SessionRollback_exports, {
  SessionRollback: /* @__PURE__ */ __name(() => SessionRollback, "SessionRollback")
});
var SessionRollback;
var init_SessionRollback = __esm({
  "src/session/SessionRollback.ts"() {
    SessionRollback = class {
      static {
        __name(this, "SessionRollback");
      }
      static {
        __name6(this, "SessionRollback");
      }
      workspaceRoot;
      blobStore;
      journalDir;
      constructor(workspaceRoot, blobStore, journalDir) {
        this.workspaceRoot = workspaceRoot;
        this.blobStore = blobStore;
        this.journalDir = journalDir ?? path10.join(workspaceRoot, ".sb_journal");
      }
      /**
      * Rollback a session to its starting state
      */
      async rollback(manifest, options) {
        const startTime = performance.now();
        const result7 = {
          success: false,
          filesReverted: [],
          filesSkipped: [],
          errors: []
        };
        try {
          await fs3.mkdir(path10.join(this.journalDir, "pending"), {
            recursive: true
          });
          await fs3.mkdir(path10.join(this.journalDir, "committed"), {
            recursive: true
          });
          const journalPath = path10.join(this.journalDir, "pending", `${manifest.sessionId}.json`);
          const journal = {
            sessionId: manifest.sessionId,
            timestamp: Date.now(),
            workspaceRoot: this.workspaceRoot,
            changes: manifest.filesChanged,
            backups: [],
            status: "pending"
          };
          await fs3.writeFile(journalPath, JSON.stringify(journal, null, 2));
          result7.journalPath = journalPath;
          options?.onProgress?.(10);
          const reversedChanges = this.reverseChanges(manifest.filesChanged);
          const stagingDir = path10.join(this.workspaceRoot, `.snapback-rollback-${manifest.sessionId}`);
          await fs3.mkdir(stagingDir, {
            recursive: true
          });
          const deletedDir = path10.join(stagingDir, ".deleted");
          await fs3.mkdir(deletedDir, {
            recursive: true
          });
          const stagedFiles = /* @__PURE__ */ new Map();
          const filesToDelete = /* @__PURE__ */ new Set();
          for (let i = 0; i < reversedChanges.length; i++) {
            const change = reversedChanges[i];
            options?.onProgress?.(10 + i / reversedChanges.length * 40);
            try {
              await this.stageChange(change, stagingDir, stagedFiles, filesToDelete, result7);
            } catch (error) {
              result7.filesSkipped.push(change.p);
              result7.errors.push({
                path: change.p,
                error: error instanceof Error ? error.message : String(error)
              });
            }
          }
          options?.onProgress?.(50);
          if (!options?.skipVerification) {
            const validationErrors = await this.validateStaging(stagedFiles);
            if (validationErrors.length > 0) {
              for (const error of validationErrors) {
                result7.errors.push(error);
                result7.filesSkipped.push(error.path);
              }
              await this.cleanupStaging(stagingDir);
              journal.status = "rolled-back";
              await fs3.writeFile(journalPath, JSON.stringify(journal, null, 2));
              return result7;
            }
          }
          options?.onProgress?.(60);
          if (options?.dryRun) {
            result7.success = true;
            result7.filesReverted = Array.from(stagedFiles.keys());
            await this.cleanupStaging(stagingDir);
            await fs3.unlink(journalPath);
            return result7;
          }
          const swapResult = await this.atomicSwap(stagedFiles, filesToDelete, journal, manifest.sessionId, options);
          result7.filesReverted = swapResult.reverted;
          result7.filesSkipped.push(...swapResult.skipped);
          result7.errors.push(...swapResult.errors);
          result7.success = swapResult.success;
          options?.onProgress?.(90);
          if (swapResult.success) {
            journal.status = "committed";
            await fs3.writeFile(path10.join(this.journalDir, "committed", `${manifest.sessionId}.json`), JSON.stringify(journal, null, 2));
            await fs3.unlink(journalPath);
            for (const backup of journal.backups) {
              if (existsSync(backup.backup)) {
                await fs3.unlink(backup.backup);
              }
            }
          } else {
            journal.status = "pending";
            await fs3.writeFile(journalPath, JSON.stringify(journal, null, 2));
          }
          await this.cleanupStaging(stagingDir);
          options?.onProgress?.(100);
          const _duration = performance.now() - startTime;
          return result7;
        } catch (error) {
          result7.errors.push({
            path: "<session>",
            error: `Rollback failed: ${error instanceof Error ? error.message : String(error)}`
          });
          return result7;
        }
      }
      /**
      * Reverse changes to get inverse operations
      */
      reverseChanges(changes) {
        const reversed = [
          ...changes
        ].reverse();
        return reversed.map((change) => {
          const inversed = {
            ...change
          };
          switch (change.op) {
            case "created":
              inversed.op = "deleted";
              inversed.hOld = change.hNew;
              inversed.hNew = void 0;
              break;
            case "modified":
              inversed.hOld = change.hNew;
              inversed.hNew = change.hOld;
              inversed.sizeBefore = change.sizeAfter;
              inversed.sizeAfter = change.sizeBefore;
              inversed.mtimeBefore = change.mtimeAfter;
              inversed.mtimeAfter = change.mtimeBefore;
              inversed.modeBefore = change.modeAfter;
              inversed.modeAfter = change.modeBefore;
              inversed.eolBefore = change.eolAfter;
              inversed.eolAfter = change.eolBefore;
              break;
            case "deleted":
              inversed.op = "created";
              inversed.hNew = change.hOld;
              inversed.hOld = void 0;
              break;
            case "renamed": {
              const temp = inversed.p;
              inversed.p = change.from || change.p;
              inversed.from = temp;
              inversed.hOld = change.hNew;
              inversed.hNew = change.hOld;
              break;
            }
          }
          return inversed;
        });
      }
      /**
      * Stage a change for rollback
      */
      async stageChange(change, stagingDir, stagedFiles, filesToDelete, _result) {
        path10.join(this.workspaceRoot, change.p);
        const stagingPath = path10.join(stagingDir, change.p);
        if (change.op === "deleted") {
          filesToDelete.add(change.p);
          return;
        }
        const hash = change.hNew;
        if (!hash) {
          throw new Error(`Missing hash for ${change.op} operation on ${change.p}`);
        }
        const blobResult = await this.blobStore.get(hash);
        if (!blobResult.success) {
          throw new Error(`Failed to retrieve blob ${hash}: ${blobResult.error.message}`);
        }
        const content = blobResult.value;
        if (!content) {
          throw new Error(`Blob not found: ${hash}`);
        }
        await fs3.mkdir(path10.dirname(stagingPath), {
          recursive: true
        });
        await fs3.writeFile(stagingPath, content);
        if (change.mtimeAfter) {
          const mtimeDate = new Date(change.mtimeAfter);
          await fs3.utimes(stagingPath, mtimeDate, mtimeDate);
        }
        if (change.modeAfter && process.platform !== "win32") {
          await fs3.chmod(stagingPath, change.modeAfter);
        }
        stagedFiles.set(change.p, {
          hash,
          content
        });
      }
      /**
      * Validate staging directory (verify hashes)
      */
      async validateStaging(stagedFiles) {
        const errors = [];
        for (const [filePath, { hash, content }] of stagedFiles.entries()) {
          const computedHash = createHash("sha256").update(content).digest("hex");
          if (computedHash !== hash) {
            errors.push({
              path: filePath,
              error: `Hash mismatch: expected ${hash}, got ${computedHash}`
            });
          }
        }
        return errors;
      }
      /**
      * Perform per-file atomic swap
      */
      async atomicSwap(stagedFiles, filesToDelete, journal, sessionId, options) {
        const reverted = [];
        const skipped = [];
        const errors = [];
        try {
          let processed = 0;
          const total = stagedFiles.size + filesToDelete.size;
          for (const [relPath] of stagedFiles.entries()) {
            processed++;
            options?.onProgress?.(60 + processed / total * 30);
            const absPath = path10.join(this.workspaceRoot, relPath);
            const stagingPath = path10.join(this.workspaceRoot, `.snapback-rollback-${sessionId}`, relPath);
            const backupPath = `${absPath}.bak-${sessionId}`;
            try {
              if (existsSync(absPath)) {
                await this.safeRename(absPath, backupPath);
                journal.backups.push({
                  original: absPath,
                  backup: backupPath
                });
              }
              await this.safeRename(stagingPath, absPath);
              reverted.push(relPath);
            } catch (error) {
              skipped.push(relPath);
              errors.push({
                path: relPath,
                error: error instanceof Error ? error.message : String(error)
              });
              if (existsSync(backupPath)) {
                await this.safeRename(backupPath, absPath);
              }
            }
          }
          for (const relPath of filesToDelete) {
            processed++;
            options?.onProgress?.(60 + processed / total * 30);
            const absPath = path10.join(this.workspaceRoot, relPath);
            const backupPath = `${absPath}.bak-${sessionId}`;
            try {
              if (existsSync(absPath)) {
                await this.safeRename(absPath, backupPath);
                journal.backups.push({
                  original: absPath,
                  backup: backupPath
                });
                reverted.push(relPath);
              } else {
                skipped.push(relPath);
              }
            } catch (error) {
              skipped.push(relPath);
              errors.push({
                path: relPath,
                error: error instanceof Error ? error.message : String(error)
              });
              if (existsSync(backupPath)) {
                await this.safeRename(backupPath, absPath);
              }
            }
          }
          return {
            success: errors.length === 0,
            reverted,
            skipped,
            errors
          };
        } catch (error) {
          for (const backup of journal.backups) {
            if (existsSync(backup.backup)) {
              try {
                await this.safeRename(backup.backup, backup.original);
              } catch {
              }
            }
          }
          return {
            success: false,
            reverted,
            skipped,
            errors: [
              {
                path: "<session>",
                error: `Fatal error during swap: ${error instanceof Error ? error.message : String(error)}`
              },
              ...errors
            ]
          };
        }
      }
      /**
      * Safe file rename with EXDEV fallback (cross-device support for Windows)
      */
      async safeRename(src, dst) {
        try {
          await fs3.rename(src, dst);
        } catch (error) {
          if (error.code === "EXDEV") {
            await fs3.copyFile(src, dst);
            await fs3.unlink(src);
          } else {
            throw error;
          }
        }
      }
      /**
      * Clean up staging directory
      */
      async cleanupStaging(stagingDir) {
        try {
          if (existsSync(stagingDir)) {
            await fs3.rm(stagingDir, {
              recursive: true,
              force: true
            });
          }
        } catch {
        }
      }
      /**
      * Get pending journal entries (for recovery)
      */
      async getPendingJournals() {
        const pendingDir = path10.join(this.journalDir, "pending");
        try {
          if (!existsSync(pendingDir)) {
            return [];
          }
          const files = await fs3.readdir(pendingDir);
          const journals = [];
          for (const file of files) {
            if (!file.endsWith(".json")) {
              continue;
            }
            const filePath = path10.join(pendingDir, file);
            const content = await fs3.readFile(filePath, "utf-8");
            const journal = JSON.parse(content);
            journals.push(journal);
          }
          return journals;
        } catch {
          return [];
        }
      }
    };
  }
});
(class {
  static {
    __name(this, "FileChangeAnalyzer");
  }
  static {
    __name6(this, "FileChangeAnalyzer");
  }
  workspaceRoot;
  fileSystem;
  /**
  * Creates a new FileChangeAnalyzer
  *
  * @param workspaceRoot - Root directory of the workspace
  * @param fileSystem - File system provider for platform-specific operations
  */
  constructor(workspaceRoot, fileSystem) {
    this.workspaceRoot = workspaceRoot;
    this.fileSystem = fileSystem;
  }
  /**
  * Analyzes all files in a snapshot and compares with current state
  *
  * @param snapshotFiles - Map of relative file paths to snapshot content
  * @returns Promise that resolves to array of file changes with detailed analysis
  */
  async analyzeSnapshot(snapshotFiles) {
    const changes = [];
    for (const [relativePath, snapshotContent] of Object.entries(snapshotFiles)) {
      try {
        const absolutePath = `${this.workspaceRoot}/${relativePath}`;
        const change = await this.analyzeFile(absolutePath, snapshotContent);
        changes.push(change);
      } catch (_error) {
        changes.push({
          filePath: `${this.workspaceRoot}/${relativePath}`,
          relativePath,
          fileName: this.getFileName(relativePath),
          changeType: "unchanged",
          linesAdded: 0,
          linesDeleted: 0,
          snapshotContent,
          icon: "error",
          changeSummary: "Error analyzing changes"
        });
      }
    }
    const typePriority = {
      modified: 0,
      deleted: 1,
      added: 2,
      unchanged: 3
    };
    return changes.sort((a, b) => {
      const priorityDiff = typePriority[a.changeType] - typePriority[b.changeType];
      if (priorityDiff !== 0) {
        return priorityDiff;
      }
      return a.fileName.localeCompare(b.fileName);
    });
  }
  /**
  * Analyzes a single file's changes
  *
  * @param absoluteFilePath - Absolute file path
  * @param snapshotContent - Content from snapshot
  * @returns Promise that resolves to detailed file change information
  */
  async analyzeFile(absoluteFilePath, snapshotContent) {
    const relativePath = this.fileSystem.getRelativePath(this.workspaceRoot, absoluteFilePath);
    const fileName = this.getFileName(relativePath);
    const fileExists2 = await this.fileSystem.fileExists(absoluteFilePath);
    let currentContent;
    if (fileExists2) {
      currentContent = await this.fileSystem.readFile(absoluteFilePath);
    }
    let changeType;
    let icon;
    let changeSummary;
    let linesAdded = 0;
    let linesDeleted = 0;
    if (!fileExists2) {
      changeType = "deleted";
      icon = "diff-removed";
      const lineCount = snapshotContent.split("\n").length;
      linesDeleted = lineCount;
      changeSummary = `Deleted (${lineCount} lines)`;
    } else if (currentContent === snapshotContent) {
      changeType = "unchanged";
      icon = "circle-outline";
      changeSummary = "No changes";
    } else {
      if (!currentContent) {
        throw new Error("Unexpected: currentContent is undefined for existing file");
      }
      changeType = "modified";
      icon = "diff-modified";
      const stats = this.calculateDiffStats(snapshotContent, currentContent);
      linesAdded = stats.added;
      linesDeleted = stats.deleted;
      if (linesAdded === 0 && linesDeleted === 0) {
        changeSummary = "Modified (whitespace only)";
      } else if (linesAdded > 0 && linesDeleted > 0) {
        changeSummary = `+${linesAdded} -${linesDeleted}`;
      } else if (linesAdded > 0) {
        changeSummary = `+${linesAdded}`;
      } else {
        changeSummary = `-${linesDeleted}`;
      }
    }
    return {
      filePath: absoluteFilePath,
      relativePath,
      fileName,
      changeType,
      linesAdded,
      linesDeleted,
      snapshotContent,
      currentContent,
      icon,
      changeSummary
    };
  }
  /**
  * Calculates simple diff statistics between two file contents
  *
  * Uses line-based comparison to estimate additions and deletions.
  * This is a simplified diff algorithm suitable for UI display.
  *
  * @param oldContent - Original content (snapshot)
  * @param newContent - Current content
  * @returns Object with added and deleted line counts
  */
  calculateDiffStats(oldContent, newContent) {
    const oldLines = oldContent.split("\n");
    const newLines = newContent.split("\n");
    const oldSet = new Set(oldLines);
    const newSet = new Set(newLines);
    let added = 0;
    let deleted = 0;
    for (const line of newLines) {
      if (!oldSet.has(line)) {
        added++;
      }
    }
    for (const line of oldLines) {
      if (!newSet.has(line)) {
        deleted++;
      }
    }
    return {
      added,
      deleted
    };
  }
  /**
  * Extract file name from path
  *
  * @param filePath - File path
  * @returns File name only
  */
  getFileName(filePath) {
    const parts = filePath.split("/");
    return parts[parts.length - 1] || filePath;
  }
});
function createChangeSummary(changes) {
  const modifiedCount = changes.filter((c) => c.changeType === "modified").length;
  const addedCount = changes.filter((c) => c.changeType === "added").length;
  const deletedCount = changes.filter((c) => c.changeType === "deleted").length;
  const unchangedCount = changes.filter((c) => c.changeType === "unchanged").length;
  const parts = [];
  if (modifiedCount > 0) {
    parts.push(`${modifiedCount} modified`);
  }
  if (deletedCount > 0) {
    parts.push(`${deletedCount} deleted`);
  }
  if (addedCount > 0) {
    parts.push(`${addedCount} added`);
  }
  if (unchangedCount > 0 && parts.length === 0) {
    parts.push(`${unchangedCount} unchanged`);
  }
  return parts.join(", ");
}
__name(createChangeSummary, "createChangeSummary");
__name6(createChangeSummary, "createChangeSummary");
var DEFAULT_THRESHOLDS_FROZEN = Object.freeze({
  /**
  * Session lifecycle thresholds
  */
  session: Object.freeze({
    idleTimeout: 105e3,
    minSessionDuration: 5e3,
    maxSessionDuration: 36e5
  }),
  /**
  * Burst detection thresholds
  */
  burst: Object.freeze({
    timeWindow: 5e3,
    minCharsInserted: 100,
    maxKeystrokeInterval: 200,
    minLinesAffected: 3,
    minInsertDeleteRatio: 3
  }),
  /**
  * Experience tier thresholds
  */
  experience: Object.freeze({
    explorer: Object.freeze({
      snapshotsCreated: 5,
      sessionsRecorded: 3,
      protectedFiles: 2,
      manualRestores: 1,
      aiAssistedSessions: 0,
      daysSinceFirstUse: 7,
      commandDiversity: 0.3
    }),
    intermediate: Object.freeze({
      snapshotsCreated: 20,
      sessionsRecorded: 10,
      protectedFiles: 5,
      manualRestores: 5,
      aiAssistedSessions: 2,
      daysSinceFirstUse: 30,
      commandDiversity: 0.6
    }),
    power: Object.freeze({
      snapshotsCreated: 100,
      sessionsRecorded: 50,
      protectedFiles: 20,
      manualRestores: 20,
      aiAssistedSessions: 10,
      daysSinceFirstUse: 90,
      commandDiversity: 0.9
    })
  }),
  /**
  * Session tagging thresholds
  */
  tagging: Object.freeze({
    minBurstConfidence: 0.7,
    minLongSessionDuration: 18e5,
    maxShortSessionDuration: 3e4,
    minLargeEditLines: 1e3,
    normalization: Object.freeze({
      multiFileThreshold: 5,
      multiFileNormalization: 10,
      longSessionNormalization: 72e5,
      largeEditsNormalization: 5e3
    })
  }),
  /**
  * Risk analysis thresholds
  */
  risk: Object.freeze({
    blockingThreshold: 8,
    criticalThreshold: 7,
    highThreshold: 5,
    mediumThreshold: 3
  }),
  /**
  * Security pattern scores (0-10 scale)
  */
  securityScores: Object.freeze({
    evalUsage: 4,
    functionConstructor: 4,
    dangerousHtml: 3,
    execCommand: 5,
    sqlConcat: 6,
    hardcodedSecrets: 4,
    weakCrypto: 3
  }),
  /**
  * Detection thresholds
  */
  detection: Object.freeze({
    entropyThreshold: 2.5,
    typosquattingDistance: 3
  }),
  /**
  * Protection level thresholds
  */
  protection: Object.freeze({
    protectedCooldown: 6e5,
    otherCooldown: 3e5,
    debounceWindow: 5e3
  }),
  /**
  * Resource limits
  */
  resources: Object.freeze({
    dedupCacheSize: 500,
    snapshotMaxFiles: 1e4,
    snapshotMaxFileSize: 10 * 1024 * 1024,
    snapshotMaxTotalSize: 500 * 1024 * 1024,
    diffHaloSize: 3,
    trialSnapshotLimit: 50,
    freeMonthlyLimit: 100
  }),
  /**
  * Quality of Service thresholds
  */
  qos: Object.freeze({
    rateLimitCapacity: 100,
    rateLimitRefill: 6e4,
    eventBusTimeout: 5e3,
    eventBusMaxRetries: 3,
    errorBudgetHard: 0.01,
    errorBudgetWarn: 5e-3,
    batchMax: 10,
    batchIntervalMs: 1e3,
    retryBaseMs: 100,
    retryMaxMs: 5e3,
    maxQueueSize: 1e3,
    httpTimeout: 3e4
  }),
  /**
  * Documentation for threshold values
  */
  docs: Object.freeze({
    session: Object.freeze({
      idleTimeout: "Duration of inactivity before automatically finalizing a session",
      minSessionDuration: "Minimum duration required for a session to be considered valid",
      maxSessionDuration: "Maximum duration before automatically finalizing a long-running session"
    }),
    burst: Object.freeze({
      timeWindow: "Time window to analyze for burst patterns (rapid insertions)",
      minCharsInserted: "Minimum characters inserted within time window to qualify as burst",
      maxKeystrokeInterval: "Maximum time between consecutive keystrokes to qualify as burst",
      minLinesAffected: "Minimum number of lines changed to qualify as burst",
      minInsertDeleteRatio: "Minimum ratio of inserted to deleted characters (e.g., 3:1 means 3x more insertions)"
    }),
    experience: Object.freeze({
      description: "Thresholds for classifying users into experience tiers based on usage patterns. Users must meet ALL thresholds within a tier to qualify.",
      explorer: "New users getting started with SnapBack (low activity)",
      intermediate: "Regular users with moderate experience (consistent usage)",
      power: "Advanced users who leverage many features (high engagement)"
    }),
    tagging: Object.freeze({
      description: "Thresholds for automatically tagging sessions based on detected patterns and characteristics",
      minBurstConfidence: "Minimum confidence level (0-1) required to tag session as having burst patterns",
      minLongSessionDuration: "Minimum duration to classify and tag a session as 'long-session'",
      maxShortSessionDuration: "Maximum duration to classify and tag a session as 'short-session'",
      minLargeEditLines: "Minimum lines added to classify and tag a session as having 'large-edits'"
    }),
    risk: Object.freeze({
      description: "Risk thresholds for security analysis and blocking operations",
      blockingThreshold: "Score above which operations are blocked (0-10 scale)",
      criticalThreshold: "Threshold for critical severity classification",
      highThreshold: "Threshold for high severity classification",
      mediumThreshold: "Threshold for medium severity classification"
    }),
    detection: Object.freeze({
      entropyThreshold: "Shannon entropy threshold (bits per symbol) for detecting potential secrets and high-entropy strings",
      typosquattingDistance: "Maximum Levenshtein distance for detecting potential typosquatting in dependency names"
    }),
    protection: Object.freeze({
      protectedCooldown: "Cooldown period after saving protected files before next protection check",
      otherCooldown: "Cooldown period for warn and watch protection levels",
      debounceWindow: "Debounce window to prevent rapid-fire protection checks"
    }),
    resources: Object.freeze({
      dedupCacheSize: "Maximum number of entries in the deduplication cache (FIFO eviction)",
      snapshotMaxFiles: "Maximum number of files allowed in a single snapshot operation",
      diffHaloSize: "Number of context lines to include around changed regions in diffs"
    }),
    qos: Object.freeze({
      rateLimitCapacity: "Token bucket capacity for rate limiting API requests",
      errorBudgetHard: "Hard error rate threshold triggering alerts and degradation (1% = 0.01)",
      batchMax: "Maximum number of items to batch together before flushing",
      batchIntervalMs: "Time interval to wait before auto-flushing a batch (milliseconds)",
      retryBaseMs: "Base delay for exponential backoff retry logic (milliseconds)",
      retryMaxMs: "Maximum delay cap for exponential backoff retries (milliseconds)",
      maxQueueSize: "Maximum queue size before dropping new items to prevent memory overflow",
      httpTimeout: "HTTP request timeout for QoS API calls (milliseconds)"
    })
  })
});
var DEFAULT_THRESHOLDS = DEFAULT_THRESHOLDS_FROZEN;
var THRESHOLDS = structuredClone(DEFAULT_THRESHOLDS_FROZEN);
function createThresholds(overrides) {
  return {
    session: {
      ...THRESHOLDS.session,
      ...overrides?.session
    },
    burst: {
      ...THRESHOLDS.burst,
      ...overrides?.burst
    },
    experience: {
      explorer: {
        ...THRESHOLDS.experience.explorer,
        ...overrides?.experience?.explorer
      },
      intermediate: {
        ...THRESHOLDS.experience.intermediate,
        ...overrides?.experience?.intermediate
      },
      power: {
        ...THRESHOLDS.experience.power,
        ...overrides?.experience?.power
      }
    },
    tagging: {
      ...THRESHOLDS.tagging,
      ...overrides?.tagging,
      normalization: {
        ...THRESHOLDS.tagging.normalization,
        ...overrides?.tagging?.normalization
      }
    },
    risk: {
      ...THRESHOLDS.risk,
      ...overrides?.risk
    },
    securityScores: {
      ...THRESHOLDS.securityScores,
      ...overrides?.securityScores
    },
    detection: {
      ...THRESHOLDS.detection,
      ...overrides?.detection
    },
    protection: {
      ...THRESHOLDS.protection,
      ...overrides?.protection
    },
    resources: {
      ...THRESHOLDS.resources,
      ...overrides?.resources
    },
    qos: {
      ...THRESHOLDS.qos,
      ...overrides?.qos
    },
    docs: THRESHOLDS.docs
  };
}
__name(createThresholds, "createThresholds");
__name6(createThresholds, "createThresholds");
function updateThresholds(overrides) {
  const updated = createThresholds(overrides);
  Object.assign(THRESHOLDS, updated);
}
__name(updateThresholds, "updateThresholds");
__name6(updateThresholds, "updateThresholds");
function resetThresholds() {
  Object.assign(THRESHOLDS, DEFAULT_THRESHOLDS);
}
__name(resetThresholds, "resetThresholds");
__name6(resetThresholds, "resetThresholds");
var DEFAULT_RISK_THRESHOLDS = {
  blockingThreshold: THRESHOLDS.risk.blockingThreshold,
  criticalThreshold: THRESHOLDS.risk.criticalThreshold,
  highThreshold: THRESHOLDS.risk.highThreshold,
  mediumThreshold: THRESHOLDS.risk.mediumThreshold
};
var SECURITY_PATTERNS = [
  {
    name: "eval_usage",
    pattern: /\beval\s*\(/g,
    score: THRESHOLDS.securityScores.evalUsage,
    message: "eval() usage detected - high security risk",
    recommendation: "Avoid using eval() as it can execute arbitrary code. Use safer alternatives like JSON.parse() for data or explicit function calls."
  },
  {
    name: "function_constructor",
    pattern: /\bnew\s+Function\s*\(/g,
    score: THRESHOLDS.securityScores.functionConstructor,
    message: "Function constructor usage detected - high security risk",
    recommendation: "Avoid using Function constructor as it can execute arbitrary code. Use regular function declarations instead."
  },
  {
    name: "dangerous_html",
    pattern: /\binnerHTML\s*=/g,
    score: THRESHOLDS.securityScores.dangerousHtml,
    message: "innerHTML usage detected - XSS risk",
    recommendation: "Use textContent or safer DOM manipulation methods to prevent XSS attacks. If HTML is necessary, sanitize it first."
  },
  {
    name: "exec_command",
    pattern: /\bexec\s*\(/g,
    score: THRESHOLDS.securityScores.execCommand,
    message: "exec() usage detected - command injection risk",
    recommendation: "Avoid using exec(). If shell commands are necessary, use execFile() with hardcoded command paths and validate all inputs."
  },
  {
    name: "sql_concat",
    pattern: /SELECT\s+.+\+\s*['"]/gi,
    score: THRESHOLDS.securityScores.sqlConcat,
    message: "Potential SQL injection through string concatenation",
    recommendation: "Use parameterized queries or prepared statements to prevent SQL injection attacks."
  },
  {
    name: "hardcoded_secrets",
    pattern: /(password|secret|api[_-]?key|token)\s*=\s*['"][^'"]+['"]/gi,
    score: THRESHOLDS.securityScores.hardcodedSecrets,
    message: "Potential hardcoded secret detected",
    recommendation: "Store secrets in environment variables or secure secret management systems, never in source code."
  },
  {
    name: "weak_crypto",
    pattern: /\b(MD5|SHA1)\b/gi,
    score: THRESHOLDS.securityScores.weakCrypto,
    message: "Weak cryptographic algorithm detected",
    recommendation: "Use modern algorithms like SHA-256 or bcrypt for hashing, and AES-256 for encryption."
  }
];
(class {
  static {
    __name(this, "RiskAnalyzer");
  }
  static {
    __name6(this, "RiskAnalyzer");
  }
  thresholds;
  customPatterns = [];
  /**
  * Creates a new RiskAnalyzer
  *
  * @param thresholds - Custom threshold configuration (optional)
  */
  constructor(thresholds) {
    this.thresholds = {
      ...DEFAULT_RISK_THRESHOLDS,
      ...thresholds
    };
  }
  /**
  * Add a custom security pattern for detection
  *
  * @param pattern - Security pattern to add
  */
  addPattern(pattern) {
    this.customPatterns.push(pattern);
  }
  /**
  * Analyze content for security risks
  *
  * @param content - Code content to analyze
  * @param filePath - Path to the file (for context, optional)
  * @returns Analysis result with risk score and factors
  */
  analyze(content, _filePath) {
    const factors = [];
    const recommendations = [];
    let totalScore = 0;
    const allPatterns = [
      ...SECURITY_PATTERNS,
      ...this.customPatterns
    ];
    for (const pattern of allPatterns) {
      const matches = content.matchAll(pattern.pattern);
      for (const match of matches) {
        factors.push({
          type: pattern.name,
          message: pattern.message,
          line: this.getLineNumber(content, match.index || 0)
        });
        if (!recommendations.includes(pattern.recommendation)) {
          recommendations.push(pattern.recommendation);
        }
        totalScore += pattern.score;
      }
    }
    const cappedScore = Math.min(totalScore, 10);
    const severity = this.calculateSeverity(cappedScore);
    return {
      score: cappedScore,
      severity,
      factors,
      recommendations
    };
  }
  /**
  * Check if a risk score should block a save operation
  *
  * @param score - Risk score to check
  * @returns True if score exceeds blocking threshold
  */
  shouldBlock(score) {
    return score > this.thresholds.blockingThreshold;
  }
  /**
  * Calculate severity based on risk score
  *
  * @param score - Risk score (0-10)
  * @returns Severity classification
  */
  calculateSeverity(score) {
    if (score >= this.thresholds.criticalThreshold) {
      return "critical";
    }
    if (score >= this.thresholds.highThreshold) {
      return "high";
    }
    if (score >= this.thresholds.mediumThreshold) {
      return "medium";
    }
    return "low";
  }
  /**
  * Get line number from character index
  *
  * @param content - Full content string
  * @param index - Character index
  * @returns Line number (1-indexed)
  */
  getLineNumber(content, index) {
    const lines = content.substring(0, index).split("\n");
    return lines.length;
  }
  /**
  * Get current thresholds
  *
  * @returns Current threshold configuration
  */
  getThresholds() {
    return {
      ...this.thresholds
    };
  }
  /**
  * Update thresholds
  *
  * @param thresholds - New threshold values
  */
  setThresholds(thresholds) {
    this.thresholds = {
      ...this.thresholds,
      ...thresholds
    };
  }
});
var globalConfig = {
  env: process.env.NODE_ENV || "development",
  includeStack: true
};
var violationCounts = /* @__PURE__ */ new Map();
var consoleReporter = {
  report(violation) {
    if (violation.file) ;
    if (violation.context) ;
  }
};
function configureInvariant(config2) {
  globalConfig = {
    ...globalConfig,
    ...config2
  };
}
__name(configureInvariant, "configureInvariant");
__name6(configureInvariant, "configureInvariant");
function getInvariantConfig() {
  return {
    ...globalConfig
  };
}
__name(getInvariantConfig, "getInvariantConfig");
__name6(getInvariantConfig, "getInvariantConfig");
function resetViolationCounts() {
  violationCounts.clear();
}
__name(resetViolationCounts, "resetViolationCounts");
__name6(resetViolationCounts, "resetViolationCounts");
function getViolationCounts() {
  return new Map(violationCounts);
}
__name(getViolationCounts, "getViolationCounts");
__name6(getViolationCounts, "getViolationCounts");
function invariant(condition, invariantId, message, context) {
  if (condition) {
    return;
  }
  const count = (violationCounts.get(invariantId) || 0) + 1;
  violationCounts.set(invariantId, count);
  const violation = {
    invariantId,
    message,
    context,
    timestamp: Date.now()
  };
  if (globalConfig.includeStack) {
    const err32 = new Error();
    violation.stack = err32.stack;
    const stackLines = err32.stack?.split("\n") || [];
    const callerLine = stackLines[2];
    const match = callerLine?.match(/at\s+(?:.+?\s+)?\(?(.+?):(\d+):\d+\)?/);
    if (match) {
      violation.file = match[1];
      violation.line = Number.parseInt(match[2], 10);
    }
  }
  const reporter = globalConfig.reporter || consoleReporter;
  reporter.report(violation);
  if (globalConfig.env === "development" || globalConfig.env === "test") {
    const err32 = new Error(`Invariant violation [${invariantId}]: ${message}`);
    err32.name = "InvariantError";
    throw err32;
  }
}
__name(invariant, "invariant");
__name6(invariant, "invariant");
function softInvariant(condition, invariantId, message, context) {
  if (condition) {
    return true;
  }
  const count = (violationCounts.get(invariantId) || 0) + 1;
  violationCounts.set(invariantId, count);
  const violation = {
    invariantId,
    message: `[SOFT] ${message}`,
    context,
    timestamp: Date.now()
  };
  const reporter = globalConfig.reporter || consoleReporter;
  reporter.report(violation);
  return false;
}
__name(softInvariant, "softInvariant");
__name6(softInvariant, "softInvariant");
function typeInvariant(value, guard, invariantId, message, context) {
  invariant(guard(value), invariantId, message, {
    ...context,
    actualType: typeof value,
    actualValue: value
  });
}
__name(typeInvariant, "typeInvariant");
__name6(typeInvariant, "typeInvariant");
function createScopedInvariant(scope) {
  return /* @__PURE__ */ __name6(/* @__PURE__ */ __name(function scopedInvariant(condition, id, message, context) {
    invariant(condition, `${scope}:${id}`, message, context);
  }, "scopedInvariant"), "scopedInvariant");
}
__name(createScopedInvariant, "createScopedInvariant");
__name6(createScopedInvariant, "createScopedInvariant");
function assertDefined(value, invariantId, message) {
  invariant(value != null, invariantId, message, {
    value
  });
}
__name(assertDefined, "assertDefined");
__name6(assertDefined, "assertDefined");
function assertNonEmptyString(value, invariantId, message) {
  invariant(typeof value === "string" && value.length > 0, invariantId, message, {
    actualType: typeof value,
    length: typeof value === "string" ? value.length : "N/A"
  });
}
__name(assertNonEmptyString, "assertNonEmptyString");
__name6(assertNonEmptyString, "assertNonEmptyString");
function assertPositiveNumber(value, invariantId, message) {
  invariant(typeof value === "number" && value > 0 && !Number.isNaN(value), invariantId, message, {
    actualType: typeof value,
    value
  });
}
__name(assertPositiveNumber, "assertPositiveNumber");
__name6(assertPositiveNumber, "assertPositiveNumber");
function assertNonEmptyArray(value, invariantId, message) {
  invariant(Array.isArray(value) && value.length > 0, invariantId, message, {
    isArray: Array.isArray(value),
    length: Array.isArray(value) ? value.length : "N/A"
  });
}
__name(assertNonEmptyArray, "assertNonEmptyArray");
__name6(assertNonEmptyArray, "assertNonEmptyArray");
function assertPathWithinRoot(path11, root, invariantId) {
  const normalizedPath = path11.replace(/\\/g, "/");
  const normalizedRoot = root.replace(/\\/g, "/");
  invariant(normalizedPath.startsWith(normalizedRoot) && !normalizedPath.includes(".."), invariantId, "Path must be within allowed root directory", {
    path: path11,
    root
  });
}
__name(assertPathWithinRoot, "assertPathWithinRoot");
__name6(assertPathWithinRoot, "assertPathWithinRoot");
var SNAPBACK_LAYER_RULES = [
  {
    id: "web-no-platform",
    description: "Web layer (apps/web) cannot import @vreko/platform directly - use SDK/API instead",
    source: [
      "apps/web"
    ],
    target: [
      "@vreko/platform",
      "packages/platform"
    ],
    type: "no-dependency",
    severity: "error",
    exclude: [
      "tests/integration",
      "app/api"
    ]
  },
  {
    id: "presentation-no-infrastructure",
    description: "Presentation layer (apps/) cannot import @vreko/infrastructure directly",
    source: [
      "apps/vscode",
      "apps/web",
      "apps/cli"
    ],
    target: [
      "@vreko/infrastructure",
      "packages/infrastructure"
    ],
    type: "no-dependency",
    severity: "error"
  },
  {
    id: "core-no-presentation",
    description: "Core packages cannot import presentation layer",
    source: [
      "packages/core",
      "packages/intelligence"
    ],
    target: [
      "apps/vscode",
      "apps/web",
      "apps/cli"
    ],
    type: "no-dependency",
    severity: "error"
  },
  {
    id: "sdk-no-vscode",
    description: "SDK cannot import VS Code specific modules",
    source: [
      "packages/sdk",
      "packages-oss/sdk"
    ],
    target: [
      "vscode",
      "apps/vscode"
    ],
    type: "no-dependency",
    severity: "error"
  },
  {
    id: "contracts-standalone",
    description: "Contracts package should have no internal dependencies",
    source: [
      "packages/contracts"
    ],
    target: [
      "packages/core",
      "packages/infrastructure",
      "packages/intelligence",
      "packages/sdk",
      "apps/"
    ],
    type: "no-dependency",
    severity: "error"
  },
  {
    id: "storage-workspace-only",
    description: "Storage operations must use workspace paths, not global paths",
    source: [
      "**/storage/**",
      "**/snapshot/**"
    ],
    target: [
      "globalStorageUri",
      "globalState"
    ],
    type: "no-dependency",
    severity: "warning"
  },
  {
    id: "no-circular-core",
    description: "Core packages must be free of circular dependencies",
    source: [
      "packages/core/src"
    ],
    target: [],
    type: "cycle-free",
    severity: "error"
  },
  {
    id: "no-circular-intelligence",
    description: "Intelligence package must be free of circular dependencies",
    source: [
      "packages/intelligence/src"
    ],
    target: [],
    type: "cycle-free",
    severity: "error"
  }
];
async function runArchCheck(config2) {
  const startTime = Date.now();
  const rules = config2.overrideDefaultRules ? config2.customRules || [] : [
    ...SNAPBACK_LAYER_RULES,
    ...config2.customRules || []
  ];
  const violations = [];
  const ruleResults = [];
  for (const rule of rules) {
    const ruleViolations = await checkRule(rule, config2);
    ruleResults.push({
      ruleId: rule.id,
      passed: ruleViolations.length === 0,
      violations: ruleViolations
    });
    violations.push(...ruleViolations);
  }
  const errorCount = violations.filter((v) => v.severity === "error").length;
  return {
    passed: errorCount === 0,
    rulesChecked: rules.length,
    rulesPassed: ruleResults.filter((r) => r.passed).length,
    violations,
    durationMs: Date.now() - startTime,
    ruleResults
  };
}
__name(runArchCheck, "runArchCheck");
__name6(runArchCheck, "runArchCheck");
async function checkRule(rule, config2) {
  const violations = [];
  switch (rule.type) {
    case "no-dependency":
      violations.push(...await checkNoDependency(rule, config2));
      break;
    case "cycle-free":
      violations.push(...await checkCycleFree());
      break;
  }
  return violations;
}
__name(checkRule, "checkRule");
__name6(checkRule, "checkRule");
async function checkNoDependency(rule, config2) {
  const violations = [];
  const { join: join62, relative: relative42 } = await import('path');
  const { readdir: readdir32, readFile: readFile42 } = await import('fs/promises');
  const { existsSync: existsSync52 } = await import('fs');
  const sources = Array.isArray(rule.source) ? rule.source : [
    rule.source
  ];
  const targets = Array.isArray(rule.target) ? rule.target : [
    rule.target
  ];
  for (const source of sources) {
    const sourceDir = join62(config2.workspaceRoot, source);
    if (!existsSync52(sourceDir)) {
      continue;
    }
    const sourceFiles = [];
    const collectFiles = /* @__PURE__ */ __name6(async (dir) => {
      const entries = await readdir32(dir, {
        withFileTypes: true
      });
      for (const entry of entries) {
        const fullPath = join62(dir, entry.name);
        if (entry.isDirectory()) {
          if (![
            "node_modules",
            "dist",
            ".next",
            "__tests__",
            "__mocks__"
          ].includes(entry.name) && !config2.globalExclude?.some((ex) => fullPath.includes(ex))) {
            await collectFiles(fullPath);
          }
        } else if (/\.(ts|tsx|js|jsx)$/.test(entry.name) && !/\.(test|spec)\.(ts|tsx|js|jsx)$/.test(entry.name)) {
          sourceFiles.push(fullPath);
        }
      }
    }, "collectFiles");
    try {
      await collectFiles(sourceDir);
    } catch {
      continue;
    }
    let extractImports = null;
    try {
      const coreAnalysis = await import('@vreko/core/analysis');
      extractImports = coreAnalysis.extractImports;
    } catch {
      extractImports = null;
    }
    for (const filePath of sourceFiles) {
      if (rule.exclude?.some((ex) => filePath.includes(ex))) {
        continue;
      }
      try {
        const content = await readFile42(filePath, "utf-8");
        const imports = [];
        if (extractImports) {
          const extraction = extractImports(content, filePath);
          if (extraction.parseSuccess) {
            imports.push(...extraction.imports);
          }
        } else {
          const importRegex = /(?:import|export)\s+.*?\s+from\s+['"]([^'"]+)['"]/g;
          let match;
          while ((match = importRegex.exec(content)) !== null) {
            imports.push({
              source: match[1]
            });
          }
        }
        for (const imp of imports) {
          for (const target of targets) {
            const matchesTarget = imp.source === target || imp.source.startsWith(`${target}/`) || imp.source.includes(`/${target}`) || imp.source.includes(target.replace(/^packages\//, ""));
            if (matchesTarget) {
              violations.push({
                ruleId: rule.id,
                ruleDescription: rule.description,
                severity: rule.severity,
                sourceFile: relative42(config2.workspaceRoot, filePath),
                targetFile: target,
                importPath: imp.source,
                line: imp.line
              });
            }
          }
        }
      } catch {
      }
    }
  }
  return violations;
}
__name(checkNoDependency, "checkNoDependency");
__name6(checkNoDependency, "checkNoDependency");
async function checkCycleFree(_rule, _config) {
  return [];
}
__name(checkCycleFree, "checkCycleFree");
__name6(checkCycleFree, "checkCycleFree");
function createRule(id, description) {
  const rule = {
    id,
    description,
    severity: "error"
  };
  return {
    filesIn(pattern) {
      rule.source = pattern;
      return this;
    },
    shouldNotDependOn(pattern) {
      rule.target = pattern;
      rule.type = "no-dependency";
      return this;
    },
    shouldBeCycleFree() {
      rule.type = "cycle-free";
      rule.target = [];
      return this;
    },
    withSeverity(severity) {
      rule.severity = severity;
      return this;
    },
    excluding(patterns) {
      rule.exclude = patterns;
      return this;
    },
    build() {
      if (!rule.source) {
        throw new Error(`Rule ${id} must specify source with filesIn()`);
      }
      if (!rule.type) {
        throw new Error(`Rule ${id} must specify constraint type`);
      }
      return rule;
    }
  };
}
__name(createRule, "createRule");
__name6(createRule, "createRule");
var DeviceAuthClient = class {
  static {
    __name(this, "DeviceAuthClient");
  }
  static {
    __name6(this, "DeviceAuthClient");
  }
  http;
  config;
  state = "idle";
  abortController = null;
  currentInterval = 5e3;
  constructor(config2) {
    this.config = config2;
    this.http = config2.httpClient ?? ky.create({
      prefixUrl: config2.baseUrl,
      timeout: 3e4
    });
  }
  /**
  * Get current flow state
  */
  getState() {
    return this.state;
  }
  /**
  * Cancel the authentication flow
  */
  cancel() {
    this.abortController?.abort();
    this.state = "cancelled";
  }
  /**
  * Start device authorization flow
  *
  * @param callbacks - Event callbacks for tracking progress
  * @returns AuthResult on success
  * @throws Error on failure or cancellation
  */
  async authenticate(callbacks) {
    if (this.state === "requesting_code" || this.state === "waiting_for_approval") {
      throw new Error("Authentication already in progress");
    }
    this.abortController = new AbortController();
    this.state = "requesting_code";
    try {
      const deviceCodeResponse = await this.requestDeviceCode();
      callbacks?.onDeviceCode?.(deviceCodeResponse);
      this.currentInterval = deviceCodeResponse.interval * 1e3;
      this.state = "waiting_for_approval";
      const result7 = await this.pollForToken(deviceCodeResponse, callbacks);
      this.state = "approved";
      callbacks?.onApproved?.(result7);
      return result7;
    } catch (error) {
      const currentState = this.state;
      if (currentState !== "cancelled") {
        this.state = "error";
      }
      const err32 = error instanceof Error ? error : new Error(String(error));
      if (currentState === "cancelled") {
        callbacks?.onCancelled?.();
      } else {
        callbacks?.onError?.(err32);
      }
      throw err32;
    }
  }
  /**
  * Request device code from authorization server (RFC 8628 Section 3.1)
  */
  async requestDeviceCode() {
    const signal = this.mergeSignals();
    try {
      const response = await this.http.post("deviceAuth/requestCode", {
        json: {
          client_id: this.config.clientId,
          scope: this.config.scope
        },
        signal
      }).json();
      if (!response.device_code) {
        throw new Error("Invalid device code response: missing device_code");
      }
      return response;
    } catch (error) {
      this.handleAbortError(error);
      throw new Error(`Device code request failed: ${error instanceof Error ? error.message : String(error)}`);
    }
  }
  /**
  * Poll for token with RFC 8628 compliant error handling
  */
  async pollForToken(deviceCodeResponse, callbacks) {
    const { device_code, expires_in } = deviceCodeResponse;
    const startTime = Date.now();
    const timeoutMs = expires_in * 1e3;
    let attempt = 0;
    while (true) {
      if (Date.now() - startTime > timeoutMs) {
        throw new Error("Device code expired - authentication timeout");
      }
      if (this.abortController?.signal.aborted) {
        this.state = "cancelled";
        throw new Error("Authentication cancelled");
      }
      await this.delay(this.currentInterval);
      attempt++;
      callbacks?.onPoll?.(attempt, this.currentInterval);
      try {
        const signal = this.mergeSignals();
        const response = await this.http.post("deviceAuth/pollToken", {
          json: {
            device_code,
            grant_type: "urn:ietf:params:oauth:grant-type:device_code",
            client_id: this.config.clientId
          },
          signal
        }).json();
        if ("access_token" in response) {
          return this.mapTokenToAuthResult(response);
        }
        if ("error" in response) {
          switch (response.error) {
            case "authorization_pending":
              break;
            case "slow_down":
              this.currentInterval += 5e3;
              callbacks?.onSlowDown?.(this.currentInterval);
              break;
            case "access_denied":
              callbacks?.onError?.(new Error("Authorization denied by user"), "access_denied");
              throw new Error("Authorization denied by user");
            case "expired_token":
              callbacks?.onError?.(new Error("Device code expired"), "expired_token");
              throw new Error("Device code expired on server");
            case "invalid_request":
              callbacks?.onError?.(new Error("Invalid device code"), "invalid_request");
              throw new Error("Invalid device code format");
            default:
              throw new Error(`Unknown error: ${response.error}`);
          }
        }
      } catch (error) {
        this.handleAbortError(error);
        if (error instanceof Error) {
          if (error.message.includes("cancelled") || error.message.includes("denied") || error.message.includes("expired") || error.message.includes("Invalid")) {
            throw error;
          }
        }
      }
    }
  }
  /**
  * Map token response to AuthResult
  */
  mapTokenToAuthResult(token) {
    return {
      api_key: token.access_token,
      user_id: "user-from-token",
      tier: "free",
      access_token: token.access_token,
      refresh_token: token.refresh_token,
      expires_in: token.expires_in
    };
  }
  /**
  * Delay execution with cancellation support
  */
  delay(ms) {
    return new Promise((resolve22) => {
      const timeout = setTimeout(resolve22, ms);
      this.abortController?.signal.addEventListener("abort", () => {
        clearTimeout(timeout);
        resolve22();
      }, {
        once: true
      });
    });
  }
  /**
  * Merge configured signal with internal abort controller
  */
  mergeSignals() {
    const signals = [];
    if (this.abortController?.signal) {
      signals.push(this.abortController.signal);
    }
    if (this.config.signal) {
      signals.push(this.config.signal);
    }
    if (signals.length === 0) {
      return void 0;
    }
    if (signals.length === 1) {
      return signals[0];
    }
    const combined = new AbortController();
    for (const signal of signals) {
      if (signal.aborted) {
        combined.abort();
        break;
      }
      signal.addEventListener("abort", () => combined.abort(), {
        once: true
      });
    }
    return combined.signal;
  }
  /**
  * Handle abort errors consistently
  */
  handleAbortError(error) {
    if (error instanceof Error && error.name === "AbortError") {
      this.state = "cancelled";
      throw new Error("Authentication cancelled");
    }
  }
};
function createDeviceAuthClient(baseUrl, clientId, options) {
  return new DeviceAuthClient({
    baseUrl,
    clientId,
    ...options
  });
}
__name(createDeviceAuthClient, "createDeviceAuthClient");
__name6(createDeviceAuthClient, "createDeviceAuthClient");
var LRUCache = class {
  static {
    __name(this, "LRUCache");
  }
  static {
    __name6(this, "LRUCache");
  }
  cache;
  config;
  constructor(config2) {
    this.config = {
      maxSize: 1e3,
      ...config2
    };
    this.cache = new QuickLRU({
      maxSize: this.config.maxSize || 1e3
    });
  }
  /**
  * Get a value from the cache
  * @returns The cached value or null if not found/expired
  */
  get(key) {
    if (!this.config.enabled) {
      return null;
    }
    const item = this.cache.get(key);
    if (!item) {
      return null;
    }
    const now = Date.now();
    if (now > item.expiry) {
      this.cache.delete(key);
      return null;
    }
    return item.value;
  }
  /**
  * Set a value in the cache
  * @param key - Cache key
  * @param value - Value to cache
  * @param ttlSeconds - Time to live in seconds (default: 300)
  */
  set(key, value, ttlSeconds = 300) {
    if (!this.config.enabled) {
      return;
    }
    const expiry = Date.now() + ttlSeconds * 1e3;
    this.cache.set(key, {
      value,
      expiry
    });
  }
  /**
  * Check if a key exists in the cache and is not expired
  */
  has(key) {
    if (!this.config.enabled) {
      return false;
    }
    const item = this.cache.get(key);
    if (!item) {
      return false;
    }
    const now = Date.now();
    if (now > item.expiry) {
      this.cache.delete(key);
      return false;
    }
    return true;
  }
  /**
  * Delete a key from the cache
  */
  delete(key) {
    return this.cache.delete(key);
  }
  /**
  * Clear all items from the cache
  */
  clear() {
    this.cache.clear();
  }
  /**
  * Get the number of items in the cache
  */
  size() {
    return this.cache.size;
  }
};
var CODE_EXTENSIONS = [
  "ts",
  "js",
  "jsx",
  "tsx",
  "py",
  "java",
  "c",
  "cpp",
  "h",
  "hpp",
  "rb",
  "go",
  "php",
  "pl",
  "rs",
  "swift",
  "kt",
  "dart",
  "scala",
  "sql"
];
var CONFIG_EXTENSIONS = [
  "json",
  "env",
  "yml",
  "yaml",
  "xml",
  "html",
  "css",
  "scss",
  "less",
  "map",
  "lock",
  "conf",
  "ini",
  "bat",
  "ps1"
];
var DOCUMENT_EXTENSIONS = [
  "md",
  "txt",
  "csv",
  "log",
  "pdf",
  "docx",
  "doc",
  "xlsx",
  "xls",
  "pptx",
  "ppt"
];
var IMAGE_EXTENSIONS = [
  "svg",
  "png",
  "jpg",
  "jpeg",
  "gif",
  "bmp",
  "ico",
  "webp",
  "avif"
];
var MEDIA_EXTENSIONS = [
  "mp4",
  "mov",
  "avi",
  "mkv",
  "mp3",
  "wav",
  "ogg",
  "flac",
  "aac",
  "m4a",
  "webm"
];
var ARCHIVE_EXTENSIONS = [
  "zip",
  "tar",
  "gz",
  "tgz",
  "rar",
  "7z"
];
var DATABASE_EXTENSIONS = [
  "db",
  "db3",
  "sqlite",
  "dbf",
  "bak"
];
var BINARY_EXTENSIONS = [
  "wasm"
];
var ALL_EXTENSIONS = [
  ...CODE_EXTENSIONS,
  ...CONFIG_EXTENSIONS,
  ...DOCUMENT_EXTENSIONS,
  ...IMAGE_EXTENSIONS,
  ...MEDIA_EXTENSIONS,
  ...ARCHIVE_EXTENSIONS,
  ...DATABASE_EXTENSIONS,
  ...BINARY_EXTENSIONS
];
var FILE_EXTENSION_REGEX = new RegExp(`\\b\\w+\\.(${ALL_EXTENSIONS.join("|")})\\b`, "gi");
var PrivacySanitizer = class {
  static {
    __name(this, "PrivacySanitizer");
  }
  static {
    __name6(this, "PrivacySanitizer");
  }
  config;
  constructor(config2) {
    this.config = config2;
  }
  /**
  * Sanitize file metadata to ensure privacy
  * Removes any potentially sensitive data
  */
  sanitize(metadata) {
    const copy = structuredClone(metadata);
    if (this.config.hashFilePaths && "path" in copy) {
      const filePath = copy.path;
      const hashedPath = this.hashFilePath(filePath);
      copy.pathHash = hashedPath;
      copy.path = hashedPath;
    }
    const risk = copy.risk;
    if (risk?.factors) {
      risk.factors = risk.factors.map((factor) => ({
        ...factor,
        type: this.sanitizeString(factor.type ?? "")
      }));
    }
    return copy;
  }
  /**
  * Validate that metadata contains no sensitive data
  */
  isPrivacySafe(metadata) {
    const forbiddenProps = [
      "content",
      "sourceCode",
      "fileContent",
      "code",
      "text",
      "body",
      "fullPath"
    ];
    for (const prop of forbiddenProps) {
      if (prop in metadata) {
        return false;
      }
    }
    if ("path" in metadata && "pathHash" in metadata) {
      if (metadata.path !== metadata.pathHash) {
        return false;
      }
    } else if ("path" in metadata && !("pathHash" in metadata)) {
      return false;
    }
    for (const [_key, value] of Object.entries(metadata)) {
      if (typeof value === "object" && value !== null) {
        if (!this.isPrivacySafe(value)) {
          return false;
        }
      }
      if (typeof value === "string" && value.length > 1e3) {
        return false;
      }
    }
    return true;
  }
  /**
  * Hash file path with workspace salt
  */
  hashFilePath(filePath) {
    return crypto2__default__default.createHash("sha256").update(filePath).digest("hex");
  }
  /**
  * Sanitize string to remove specific identifiers
  */
  sanitizeString(str) {
    if (!str) {
      return "";
    }
    if (str.length > 1e4) {
      throw new Error("Input too large for sanitization");
    }
    return str.replace(/"[^"]*"/g, '"<redacted>"').replace(FILE_EXTENSION_REGEX, "<file>").replace(/\/[\w/]+/g, "<path>");
  }
  /**
  * Public method to sanitize individual risk factors
  * @param factor - Risk factor string to sanitize
  * @returns Sanitized risk factor string
  */
  sanitizeFactor(factor) {
    return this.sanitizeString(factor);
  }
};
var SDKConfigSchema = z.object({
  endpoint: z.string().url(),
  apiKey: z.string().min(1, "API key is required"),
  privacy: z.object({
    hashFilePaths: z.boolean(),
    anonymizeWorkspace: z.boolean()
  }),
  cache: z.object({
    enabled: z.boolean(),
    ttl: z.record(z.string(), z.number())
  }),
  retry: z.object({
    maxRetries: z.number().min(0),
    backoffMs: z.number().min(0)
  })
});
var WorkspaceIdSchema = z.string().min(1, "Workspace ID is required");
var FilesArraySchema = z.array(z.any()).min(1, "At least one file is required");
var LocalFallback = class LocalFallback2 {
  static {
    __name(this, "LocalFallback2");
  }
  static {
    __name6(this, "LocalFallback");
  }
  generateRecommendations() {
    return {
      shouldCreateSnapshot: false,
      reason: "Using local fallback - API unavailable",
      urgency: "low",
      suggestedTiming: "24h"
    };
  }
};
var SnapbackClient = class {
  static {
    __name(this, "SnapbackClient");
  }
  static {
    __name6(this, "SnapbackClient");
  }
  sanitizer;
  cache;
  config;
  httpClient;
  localFallback;
  constructor(config2) {
    this.config = SDKConfigSchema.parse(config2);
    this.sanitizer = new PrivacySanitizer(config2.privacy);
    this.cache = new LRUCache(config2.cache);
    this.localFallback = new LocalFallback();
    this.httpClient = ky.extend({
      prefixUrl: config2.endpoint,
      headers: {
        "X-API-Key": config2.apiKey,
        "X-SnapBack-SDK": "1.0.0"
      },
      retry: {
        limit: config2.retry.maxRetries,
        methods: [
          "get",
          "post",
          "put",
          "delete",
          "patch"
        ],
        statusCodes: [
          408,
          413,
          429,
          500,
          502,
          503,
          504
        ],
        // Add exponential backoff configuration
        backoffLimit: config2.retry.backoffMs * 10
      },
      timeout: 3e4
    });
  }
  /**
  * Get the HTTP client instance
  * @returns The ky HTTP client instance
  */
  getHttpClient() {
    return this.httpClient;
  }
  /**
  * Parse Retry-After header and return delay in milliseconds
  * @param retryAfter Retry-After header value
  * @returns Delay in milliseconds
  */
  parseRetryAfter(retryAfter) {
    if (/^\d+$/.test(retryAfter)) {
      return Number.parseInt(retryAfter, 10) * 1e3;
    }
    const date = new Date(retryAfter);
    if (!Number.isNaN(date.getTime())) {
      return Math.max(0, date.getTime() - Date.now());
    }
    return 1e3;
  }
  /**
  * Enhanced HTTP request with custom retry logic including Retry-After handling
  */
  async httpRequest(requestFn, options) {
    const retryLimit = options?.retryLimit ?? this.config.retry.maxRetries;
    return pRetry(async () => {
      try {
        return await requestFn();
      } catch (error) {
        const httpError = error;
        if (httpError.response?.status === 429) {
          const retryAfter = httpError.response.headers.get("Retry-After");
          if (retryAfter) {
            const delay = this.parseRetryAfter(retryAfter);
            await new Promise((resolve22) => setTimeout(resolve22, delay));
          }
          throw error;
        }
        if (httpError.response?.status && httpError.response.status >= 400 && httpError.response.status < 500) {
          throw new AbortError(error);
        }
        throw error;
      }
    }, {
      retries: retryLimit,
      factor: 2,
      minTimeout: this.config.retry.backoffMs,
      maxTimeout: this.config.retry.backoffMs * 2 ** retryLimit,
      randomize: true,
      onFailedAttempt: /* @__PURE__ */ __name6((error) => {
        if (options?.onRetry) {
          options.onRetry(error, error.attemptNumber);
        }
      }, "onFailedAttempt")
    });
  }
  /**
  * Send file metadata batch to API
  * Automatically sanitizes metadata to ensure privacy
  */
  async sendMetadata(workspaceId, files) {
    WorkspaceIdSchema.parse(workspaceId);
    FilesArraySchema.parse(files);
    const sanitized = files.map((f) => this.sanitizer.sanitize(f));
    for (const file of sanitized) {
      if (!this.sanitizer.isPrivacySafe(file)) {
        throw new Error("Privacy validation failed: file contains sensitive data");
      }
    }
    try {
      const response = await this.httpRequest(() => this.httpClient.post("v1/metadata/files/batch", {
        json: {
          workspaceId,
          files: sanitized
        },
        timeout: 3e4
      }).json(), {
        onRetry: /* @__PURE__ */ __name6((error, attempt) => {
          logger.warn(`Attempt ${attempt} failed. Retrying...`, {
            error: error.message
          });
        }, "onRetry")
      });
      return response;
    } catch (error) {
      logger.warn("API metadata upload failed, continuing with local operation", {
        error: error.message
      });
      return {
        accepted: 0,
        rejected: files.length
      };
    }
  }
  /**
  * Get analytics for workspace
  * Uses cache when available
  */
  async getAnalytics(workspaceId, options) {
    WorkspaceIdSchema.parse(workspaceId);
    const cacheKey = `analytics:${workspaceId}`;
    if (!options?.forceRefresh && this.cache.has(cacheKey)) {
      return this.cache.get(cacheKey);
    }
    try {
      const response = await this.httpRequest(() => this.httpClient.get(`v1/analytics/workspace/${workspaceId}`, {
        timeout: 3e4
      }).json(), {
        onRetry: /* @__PURE__ */ __name6((error, attempt) => {
          logger.warn(`Analytics API attempt ${attempt} failed. Retrying...`, {
            error: error.message
          });
        }, "onRetry")
      });
      const ttl = this.config.cache.ttl.analytics || 3600;
      this.cache.set(cacheKey, response, ttl);
      return response;
    } catch (error) {
      if (this.cache.has(cacheKey)) {
        logger.warn("API unavailable, using stale cached analytics");
        return this.cache.get(cacheKey);
      }
      throw new Error(`Failed to fetch analytics: ${error.message}`);
    }
  }
  /**
  * Get smart recommendations
  */
  async getRecommendations(workspaceId) {
    WorkspaceIdSchema.parse(workspaceId);
    try {
      const response = await this.httpRequest(() => this.httpClient.get("v1/intelligence/recommendations", {
        searchParams: {
          workspaceId
        },
        timeout: 3e4
      }).json(), {
        onRetry: /* @__PURE__ */ __name6((error, attempt) => {
          logger.warn(`Recommendations API attempt ${attempt} failed. Retrying...`, {
            error: error.message
          });
        }, "onRetry")
      });
      return response.snapshotRecommendations;
    } catch (error) {
      logger.warn("API unavailable, using local fallback for recommendations", {
        error: error.message
      });
      return this.localFallback.generateRecommendations();
    }
  }
  /**
  * Health check endpoint
  */
  async healthCheck() {
    try {
      const response = await this.httpClient.get("health").json();
      return response;
    } catch (_error) {
      return {
        status: "error",
        version: "unknown"
      };
    }
  }
};
(class {
  static {
    __name(this, "ProtectionClient");
  }
  static {
    __name6(this, "ProtectionClient");
  }
  http;
  cache;
  constructor(http, cache) {
    this.http = http;
    this.cache = cache;
  }
  async protect(path11, level, reason) {
    const response = await this.http.post("protection", {
      json: {
        path: path11,
        level,
        reason
      }
    }).json();
    this.cache.delete(`protection:${path11}`);
    this.invalidateListCache();
    return response;
  }
  async unprotect(path11) {
    await this.http.delete("protection", {
      json: {
        path: path11
      }
    });
    this.cache.delete(`protection:${path11}`);
    this.invalidateListCache();
  }
  async get(path11) {
    const cacheKey = `protection:${path11}`;
    const cached = this.cache.get(cacheKey);
    if (cached !== void 0) {
      return cached === null ? null : cached;
    }
    try {
      const response = await this.http.get("protection", {
        searchParams: {
          path: path11
        }
      }).json();
      this.cache.set(cacheKey, response);
      return response;
    } catch (error) {
      if (error.response?.status === 404) {
        this.cache.set(cacheKey, null);
        return null;
      }
      throw error;
    }
  }
  async list(filters) {
    const cacheKey = `protection:list:${JSON.stringify(filters || {})}`;
    const cached = this.cache.get(cacheKey);
    if (cached) {
      return cached;
    }
    const searchParams = {};
    if (filters?.level) {
      searchParams.level = String(filters.level);
    }
    const response = await this.http.get("protection/list", {
      searchParams: Object.keys(searchParams).length > 0 ? searchParams : void 0
    }).json();
    this.cache.set(cacheKey, response);
    return response;
  }
  async update(path11, level, reason) {
    const response = await this.http.put("protection", {
      json: {
        path: path11,
        level,
        reason
      }
    }).json();
    this.cache.delete(`protection:${path11}`);
    this.invalidateListCache();
    return response;
  }
  /**
  * Invalidate all protection list caches (including filtered lists)
  */
  invalidateListCache() {
    for (const key of this.cache.keys()) {
      if (key.startsWith("protection:list")) {
        this.cache.delete(key);
      }
    }
  }
});
(class {
  static {
    __name(this, "SnapshotClient");
  }
  static {
    __name6(this, "SnapshotClient");
  }
  http;
  cache;
  constructor(http, cache) {
    this.http = http;
    this.cache = cache;
  }
  async create(data) {
    const response = await this.http.post("snapshots", {
      json: {
        filePath: data.filePath,
        content: data.content,
        message: data.message,
        protected: data.protected
      }
    }).json();
    this.invalidateListCache();
    return response;
  }
  async list(filters) {
    const cacheKey = `snapshots:list:${JSON.stringify(filters || {})}`;
    const cached = this.cache.get(cacheKey);
    if (cached) {
      return cached;
    }
    const searchParams = {};
    if (filters) {
      Object.entries(filters).forEach(([key, value]) => {
        if (value !== void 0) {
          if (value instanceof Date) {
            searchParams[key] = value.toISOString();
          } else {
            searchParams[key] = String(value);
          }
        }
      });
    }
    const response = await this.http.get("snapshots", {
      searchParams: Object.keys(searchParams).length > 0 ? searchParams : void 0
    }).json();
    this.cache.set(cacheKey, response);
    return response;
  }
  async get(id) {
    const cacheKey = `snapshot:${id}`;
    const cached = this.cache.get(cacheKey);
    if (cached) {
      return cached;
    }
    const response = await this.http.get(`snapshots/${id}`).json();
    this.cache.set(cacheKey, response);
    return response;
  }
  async delete(id) {
    await this.http.delete(`snapshots/${id}`);
    this.cache.delete(`snapshot:${id}`);
    this.invalidateListCache();
  }
  async restore(id) {
    const response = await this.http.post(`snapshots/${id}/restore`).json();
    return response;
  }
  async update(id, data) {
    const response = await this.http.put(`snapshots/${id}`, {
      json: data
    }).json();
    this.cache.delete(`snapshot:${id}`);
    this.invalidateListCache();
    return response;
  }
  /**
  * Invalidate all snapshot list caches (including filtered lists)
  */
  invalidateListCache() {
    for (const key of this.cache.keys()) {
      if (key.startsWith("snapshots:list")) {
        this.cache.delete(key);
      }
    }
  }
});
var defaultConfig = {
  endpoint: "https://api.snapback.dev",
  apiKey: "",
  privacy: {
    hashFilePaths: true,
    anonymizeWorkspace: false
  },
  cache: {
    enabled: true,
    ttl: {
      analytics: 3600,
      recommendations: 1800,
      patterns: 7200
    }
  },
  retry: {
    maxRetries: 3,
    backoffMs: 1e3
  }
};
function createConfig(overrides = {}) {
  return {
    ...defaultConfig,
    ...overrides,
    privacy: {
      ...defaultConfig.privacy,
      ...overrides.privacy
    },
    cache: {
      ...defaultConfig.cache,
      ...overrides.cache,
      ttl: {
        ...defaultConfig.cache.ttl,
        ...overrides.cache?.ttl
      }
    },
    retry: {
      ...defaultConfig.retry,
      ...overrides.retry
    }
  };
}
__name(createConfig, "createConfig");
__name6(createConfig, "createConfig");
(class {
  static {
    __name(this, "ConfigDetector");
  }
  static {
    __name6(this, "ConfigDetector");
  }
  workspaceRoot;
  fileSystem;
  excludePatterns;
  configFiles = /* @__PURE__ */ new Map();
  changeHandlers = [];
  /**
  * Creates a new ConfigDetector
  *
  * @param workspaceRoot - Root directory to search for configuration files
  * @param fileSystem - File system provider for platform-specific operations
  * @param options - Configuration options
  */
  constructor(workspaceRoot, fileSystem, options) {
    this.workspaceRoot = workspaceRoot;
    this.fileSystem = fileSystem;
    this.excludePatterns = options?.exclude || [
      "node_modules/**",
      ".git/**",
      "dist/**",
      "build/**"
    ];
  }
  /**
  * Detect all configuration files in the workspace
  *
  * @returns Promise that resolves to array of detected configuration files
  */
  async detectConfigFiles() {
    const patterns = [
      "package.json",
      "tsconfig.json",
      ".env*",
      ".eslintrc*",
      ".prettierrc*",
      "jest.config.*",
      "vitest.config.*",
      "webpack.config.*",
      "next.config.*",
      "vite.config.*"
    ];
    const globPatterns = patterns.map((pattern) => `**/${pattern}`);
    try {
      const files = await this.fileSystem.glob(globPatterns, this.workspaceRoot, {
        ignore: this.excludePatterns
      });
      const configFiles = files.map((file) => {
        const fullPath = `${this.workspaceRoot}/${file}`;
        const type = this.determineConfigType(file);
        const name = file.split("/").pop() || file;
        return {
          type,
          path: fullPath,
          name
        };
      });
      this.configFiles.clear();
      for (const config2 of configFiles) {
        this.configFiles.set(config2.path, config2);
      }
      return configFiles;
    } catch (_error) {
      return [];
    }
  }
  /**
  * Determine configuration file type from filename
  *
  * @param fileName - The file name to analyze
  * @returns The configuration type
  */
  determineConfigType(fileName) {
    if (fileName.includes(".env")) {
      return "env";
    }
    if (fileName.includes("package.json")) {
      return "package.json";
    }
    if (fileName.includes("tsconfig")) {
      return "tsconfig";
    }
    if (fileName.includes(".eslintrc")) {
      return "eslint";
    }
    if (fileName.includes(".prettierrc")) {
      return "prettier";
    }
    if (fileName.includes("jest.config")) {
      return "jest";
    }
    if (fileName.includes("vitest.config")) {
      return "vitest";
    }
    if (fileName.includes("webpack.config")) {
      return "webpack";
    }
    if (fileName.includes("next.config")) {
      return "next";
    }
    if (fileName.includes("vite.config")) {
      return "vite";
    }
    return "unknown";
  }
  /**
  * Parse a configuration file
  *
  * @param filePath - Path to the configuration file
  * @returns Promise that resolves to parse result
  */
  async parseConfigFile(filePath) {
    try {
      const content = await this.fileSystem.readFile(filePath);
      if (filePath.endsWith(".json") || filePath.includes("package.json") || filePath.includes("tsconfig")) {
        try {
          const parsed = JSON.parse(content);
          return {
            content: parsed,
            valid: true,
            metadata: this.extractMetadata(parsed, filePath)
          };
        } catch (jsonError) {
          return {
            content: null,
            valid: false,
            error: `Invalid JSON: ${jsonError.message}`
          };
        }
      }
      return {
        content,
        valid: true
      };
    } catch (error) {
      return {
        content: null,
        valid: false,
        error: `Failed to read file: ${error.message}`
      };
    }
  }
  /**
  * Extract metadata from configuration content
  *
  * @param content - Parsed configuration content
  * @param filePath - Path to the file (used to determine type)
  * @returns Extracted metadata or undefined
  */
  extractMetadata(content, filePath) {
    if (!content || typeof content !== "object") {
      return void 0;
    }
    const metadata = {};
    if (filePath.includes("package.json")) {
      const pkg = content;
      if (pkg.dependencies) {
        metadata.dependencies = Object.keys(pkg.dependencies);
      }
      if (pkg.devDependencies) {
        metadata.devDependencies = Object.keys(pkg.devDependencies);
      }
      if (pkg.scripts) {
        metadata.scripts = Object.keys(pkg.scripts);
      }
    }
    return Object.keys(metadata).length > 0 ? metadata : void 0;
  }
  /**
  * Validate a configuration file
  *
  * @param filePath - Path to the configuration file
  * @returns Promise that resolves to validation result
  */
  async validateConfig(filePath) {
    const result7 = {
      valid: true,
      errors: [],
      warnings: []
    };
    try {
      const parseResult = await this.parseConfigFile(filePath);
      if (!parseResult.valid) {
        result7.valid = false;
        result7.errors.push(parseResult.error || "Failed to parse config file");
        return result7;
      }
      if (filePath.includes("package.json")) {
        this.validatePackageJson(parseResult.content, result7);
      } else if (filePath.includes("tsconfig")) {
        this.validateTsConfig(parseResult.content, result7);
      }
      return result7;
    } catch (error) {
      result7.valid = false;
      result7.errors.push(`Validation error: ${error.message}`);
      return result7;
    }
  }
  /**
  * Validate package.json content
  *
  * @param content - Parsed package.json content
  * @param result - Validation result to update
  */
  validatePackageJson(content, result7) {
    const pkg = content;
    if (!pkg.name) {
      result7.errors.push("Missing required field: name");
      result7.valid = false;
    }
    if (!pkg.version) {
      result7.errors.push("Missing required field: version");
      result7.valid = false;
    }
  }
  /**
  * Validate tsconfig.json content
  *
  * @param content - Parsed tsconfig.json content
  * @param result - Validation result to update
  */
  validateTsConfig(content, result7) {
    const tsconfig = content;
    if (tsconfig && typeof tsconfig === "object" && tsconfig.compilerOptions) {
      if (tsconfig.compilerOptions.target && typeof tsconfig.compilerOptions.target !== "string") {
        result7.warnings.push("compilerOptions.target should be a string");
      }
      if (tsconfig.compilerOptions.module && typeof tsconfig.compilerOptions.module !== "string") {
        result7.warnings.push("compilerOptions.module should be a string");
      }
    }
  }
  /**
  * Register a handler for configuration changes
  *
  * @param handler - Function to call when configuration changes
  */
  onConfigChange(handler2) {
    this.changeHandlers.push(handler2);
  }
});
var VALID_PROTECTION_LEVELS = [
  "Watched",
  "Warning",
  "Protected"
];
(class {
  static {
    __name(this, "SnapBackRCParser");
  }
  static {
    __name6(this, "SnapBackRCParser");
  }
  /**
  * Parse a .snapbackrc configuration string
  *
  * @param content - The configuration file content as a string
  * @param filePath - Optional file path for error context
  * @returns Parse result with validation status and errors
  */
  parse(content, filePath) {
    const errors = [];
    const detailedErrors = [];
    const warnings = [];
    let config2;
    try {
      config2 = JSON.parse(content);
    } catch (error) {
      const errorMessage2 = error instanceof Error ? error.message : String(error);
      const position = this.extractJsonErrorPosition(errorMessage2, content);
      const detailedError = {
        message: `Invalid JSON: ${errorMessage2}`,
        line: position.line,
        column: position.column,
        category: "syntax",
        suggestion: this.suggestJsonFix(errorMessage2, content, position)
      };
      const fileName = filePath ? this.extractFileName(filePath) : ".snapbackrc";
      const location = position.line ? ` at line ${position.line}, column ${position.column}` : "";
      return {
        isValid: false,
        errors: [
          `${fileName} has invalid JSON${location}. Using default policy.`
        ],
        detailedErrors: [
          detailedError
        ]
      };
    }
    const validationErrors = this.validate(config2);
    errors.push(...validationErrors);
    for (const error of validationErrors) {
      detailedErrors.push({
        message: error,
        category: "validation",
        suggestion: this.suggestValidationFix(error)
      });
    }
    return {
      isValid: errors.length === 0,
      config: errors.length === 0 ? config2 : void 0,
      errors: errors.length > 0 ? errors : void 0,
      detailedErrors: errors.length > 0 ? detailedErrors : void 0,
      warnings: warnings.length > 0 ? warnings : void 0
    };
  }
  /**
  * Extract line and column from JSON parse error message
  */
  extractJsonErrorPosition(errorMessage2, content) {
    const positionMatch = errorMessage2.match(/position\s+(\d+)/i);
    if (positionMatch) {
      const position = Number.parseInt(positionMatch[1], 10);
      return this.offsetToLineColumn(content, position);
    }
    const lineColMatch = errorMessage2.match(/line\s+(\d+)[,:]?\s*column\s+(\d+)/i);
    if (lineColMatch) {
      return {
        line: Number.parseInt(lineColMatch[1], 10),
        column: Number.parseInt(lineColMatch[2], 10)
      };
    }
    return {};
  }
  /**
  * Convert character offset to line/column
  */
  offsetToLineColumn(content, offset) {
    const lines = content.substring(0, offset).split("\n");
    const line = lines.length;
    const column = lines[lines.length - 1].length + 1;
    return {
      line,
      column
    };
  }
  /**
  * Extract filename from path
  */
  extractFileName(filePath) {
    return filePath.split(/[/\\]/).pop() || ".snapbackrc";
  }
  /**
  * Suggest a fix for JSON syntax errors
  */
  suggestJsonFix(errorMessage2, _content, _position) {
    if (errorMessage2.includes("Unexpected token")) {
      return "Check for missing commas, extra commas, or unmatched brackets";
    }
    if (errorMessage2.includes("Unexpected end of JSON")) {
      return "File appears to be truncated - check for missing closing braces";
    }
    if (errorMessage2.includes("Expected")) {
      return "Check JSON syntax - ensure all strings are quoted and values are valid";
    }
    return "Validate your JSON using a JSON linter";
  }
  /**
  * Suggest a fix for validation errors
  */
  suggestValidationFix(error) {
    if (error.includes("level must be one of")) {
      return "Use one of: Watched, Warning, Protected (case-sensitive)";
    }
    if (error.includes("pattern is required")) {
      return "Add a pattern field with a glob pattern like 'src/**/*.ts'";
    }
    if (error.includes("must be an array")) {
      return "Wrap the value in square brackets: []";
    }
    if (error.includes("must be a number")) {
      return "Ensure the value is a number without quotes";
    }
    return "Check the configuration documentation for correct format";
  }
  /**
  * Validate a configuration object
  *
  * @param config - The configuration to validate
  * @returns Array of validation error messages
  */
  validate(config2) {
    const errors = [];
    if (config2.protection !== void 0) {
      if (!Array.isArray(config2.protection)) {
        errors.push("protection must be an array");
      } else {
        for (let i = 0; i < config2.protection.length; i++) {
          const rule = config2.protection[i];
          const ruleErrors = this.validateProtectionRule(rule, i);
          errors.push(...ruleErrors);
        }
      }
    }
    if (config2.ignore !== void 0 && !Array.isArray(config2.ignore)) {
      errors.push("ignore must be an array of strings");
    }
    if (config2.settings !== void 0) {
      const settingsErrors = this.validateSettings(config2.settings);
      errors.push(...settingsErrors);
    }
    if (config2.policies !== void 0) {
      const policyErrors = this.validatePolicies(config2.policies);
      errors.push(...policyErrors);
    }
    return errors;
  }
  /**
  * Validate a single protection rule
  *
  * @param rule - The protection rule to validate
  * @param index - The index of the rule in the array
  * @returns Array of validation error messages
  */
  validateProtectionRule(rule, index) {
    const errors = [];
    const prefix = `protection[${index}]`;
    if (!rule.pattern) {
      errors.push(`${prefix}: pattern is required`);
    }
    if (!rule.level) {
      errors.push(`${prefix}: level is required`);
    } else if (!VALID_PROTECTION_LEVELS.includes(rule.level)) {
      errors.push(`${prefix}: level must be one of ${VALID_PROTECTION_LEVELS.join(", ")} (got "${rule.level}")`);
    }
    if (rule.debounce !== void 0 && (typeof rule.debounce !== "number" || rule.debounce < 0)) {
      errors.push(`${prefix}: debounce must be a non-negative number`);
    }
    if (rule.excludeFrom !== void 0 && !Array.isArray(rule.excludeFrom)) {
      errors.push(`${prefix}: excludeFrom must be an array of strings`);
    }
    if (rule.autoSnapshot !== void 0 && typeof rule.autoSnapshot !== "boolean") {
      errors.push(`${prefix}: autoSnapshot must be a boolean`);
    }
    return errors;
  }
  /**
  * Validate settings
  *
  * @param settings - The settings to validate
  * @returns Array of validation error messages
  */
  validateSettings(settings) {
    const errors = [];
    if (settings.maxSnapshots !== void 0) {
      if (typeof settings.maxSnapshots !== "number" || settings.maxSnapshots < 1) {
        errors.push("settings.maxSnapshots must be a positive number");
      }
    }
    if (settings.defaultProtectionLevel !== void 0 && !VALID_PROTECTION_LEVELS.includes(settings.defaultProtectionLevel)) {
      errors.push(`settings.defaultProtectionLevel must be one of ${VALID_PROTECTION_LEVELS.join(", ")}`);
    }
    if (settings.protectionDebounce !== void 0 && (typeof settings.protectionDebounce !== "number" || settings.protectionDebounce < 0)) {
      errors.push("settings.protectionDebounce must be a non-negative number");
    }
    if (settings.maxStorageSize !== void 0 && (typeof settings.maxStorageSize !== "number" || settings.maxStorageSize < 0)) {
      errors.push("settings.maxStorageSize must be a non-negative number");
    }
    if (settings.parallelOperations !== void 0 && (typeof settings.parallelOperations !== "number" || settings.parallelOperations < 1)) {
      errors.push("settings.parallelOperations must be a positive number");
    }
    return errors;
  }
  /**
  * Validate policies
  *
  * @param policies - The policies to validate
  * @returns Array of validation error messages
  */
  validatePolicies(policies) {
    const errors = [];
    if (policies.minimumProtectionLevel !== void 0 && !VALID_PROTECTION_LEVELS.includes(policies.minimumProtectionLevel)) {
      errors.push(`policies.minimumProtectionLevel must be one of ${VALID_PROTECTION_LEVELS.join(", ")}`);
    }
    return errors;
  }
  /**
  * Merge two configurations
  *
  * Combines two configurations with the override taking precedence.
  * Arrays are concatenated, objects are deeply merged.
  *
  * @param base - The base configuration
  * @param override - The override configuration
  * @param options - Merge options including provenance tracking
  * @returns Merged configuration
  */
  merge(base, override, options) {
    const result7 = {};
    if (base.protection || override.protection) {
      result7.protection = [];
      if (base.protection) {
        result7.protection.push(...base.protection.map((rule) => ({
          ...rule,
          _provenance: options?.baseProvenance
        })));
      }
      if (override.protection) {
        result7.protection.push(...override.protection.map((rule) => ({
          ...rule,
          _provenance: options?.overrideProvenance
        })));
      }
    }
    if (base.ignore || override.ignore) {
      const ignoreSet = /* @__PURE__ */ new Set();
      if (base.ignore) {
        for (const pattern of base.ignore) {
          ignoreSet.add(pattern);
        }
      }
      if (override.ignore) {
        for (const pattern of override.ignore) {
          ignoreSet.add(pattern);
        }
      }
      result7.ignore = Array.from(ignoreSet);
    }
    if (base.settings || override.settings) {
      result7.settings = {
        ...base.settings,
        ...override.settings
      };
    }
    if (base.policies || override.policies) {
      result7.policies = {
        ...base.policies,
        ...override.policies
      };
    }
    if (base.hooks || override.hooks) {
      result7.hooks = {
        ...base.hooks,
        ...override.hooks
      };
    }
    if (base.templates || override.templates) {
      result7.templates = [
        ...base.templates || [],
        ...override.templates || []
      ];
    }
    return result7;
  }
  /**
  * Filter configuration by pattern
  *
  * Returns a new configuration containing only protection rules
  * that match the given pattern.
  *
  * @param config - The configuration to filter
  * @param pattern - The pattern to match
  * @returns Filtered configuration
  */
  filterByPattern(config2, pattern) {
    const result7 = {
      ...config2
    };
    if (config2.protection) {
      result7.protection = config2.protection.filter((rule) => rule.pattern === pattern);
    }
    return result7;
  }
});
var AI_EXTENSION_IDS = {
  GITHUB_COPILOT: "github.copilot",
  GITHUB_COPILOT_CHAT: "github.copilot-chat",
  CLAUDE: "claude.claude",
  TABNINE: "tabnine.tabnine-vscode",
  CODEIUM: "codeium.codeium",
  AMAZON_Q: "amazonwebservices.aws-toolkit-vscode",
  CONTINUE: "continue.continue",
  BLACKBOX: "blackboxapp.blackbox",
  WINDSURF: "windsurf.windsurf"
};
var ASSISTANT_DISPLAY_NAMES = {
  GITHUB_COPILOT: "GitHub Copilot",
  GITHUB_COPILOT_CHAT: "GitHub Copilot Chat",
  CLAUDE: "Claude",
  TABNINE: "Tabnine",
  CODEIUM: "Codeium",
  AMAZON_Q: "Amazon Q",
  CONTINUE: "Continue",
  BLACKBOX: "Blackbox",
  WINDSURF: "Windsurf"
};
(class {
  static {
    __name(this, "AIPresenceDetector");
  }
  static {
    __name6(this, "AIPresenceDetector");
  }
  extensionProvider;
  /**
  * Creates a new AIPresenceDetector
  *
  * @param extensionProvider - Platform-specific extension provider
  */
  constructor(extensionProvider) {
    this.extensionProvider = extensionProvider;
  }
  /**
  * Detects the presence of AI coding assistants
  *
  * Queries the extension provider and matches against known AI assistant
  * identifiers to determine which assistants are currently installed.
  *
  * @returns Information about detected AI assistants
  */
  detectAIPresence() {
    const installedExtensionIds = this.extensionProvider.getAllExtensionIds();
    const detectedAssistants = [];
    for (const [name, id] of Object.entries(AI_EXTENSION_IDS)) {
      if (installedExtensionIds.includes(id)) {
        if (!detectedAssistants.includes(name)) {
          detectedAssistants.push(name);
        }
      }
    }
    return {
      hasAI: detectedAssistants.length > 0,
      detectedAssistants,
      assistantDetails: ASSISTANT_DISPLAY_NAMES
    };
  }
  /**
  * Checks if a specific AI assistant is installed
  *
  * @param assistantName - Name of the AI assistant to check
  * @returns True if the assistant is installed
  */
  isAIAssistantInstalled(assistantName) {
    const extensionId = AI_EXTENSION_IDS[assistantName];
    const installedExtensionIds = this.extensionProvider.getAllExtensionIds();
    return installedExtensionIds.includes(extensionId);
  }
  /**
  * Gets a list of all installed AI assistants
  *
  * @returns Array of installed AI assistant names
  */
  getInstalledAIAssistants() {
    const installedExtensionIds = this.extensionProvider.getAllExtensionIds();
    const installed = [];
    for (const [name, id] of Object.entries(AI_EXTENSION_IDS)) {
      if (installedExtensionIds.includes(id)) {
        installed.push(name);
      }
    }
    return installed;
  }
});
var DEFAULT_BURST_CONFIG = {
  /** Time window in milliseconds to consider for burst detection */
  timeWindow: THRESHOLDS.burst.timeWindow,
  /** Minimum number of characters inserted to qualify as burst */
  minCharsInserted: THRESHOLDS.burst.minCharsInserted,
  /** Maximum time between keystrokes to qualify as burst */
  maxKeystrokeInterval: THRESHOLDS.burst.maxKeystrokeInterval,
  /** Minimum number of lines affected to qualify as burst */
  minLinesAffected: THRESHOLDS.burst.minLinesAffected,
  /** Minimum ratio of inserted to deleted chars to qualify as burst */
  minInsertDeleteRatio: THRESHOLDS.burst.minInsertDeleteRatio
};
(class {
  static {
    __name(this, "BurstHeuristicsDetector");
  }
  static {
    __name6(this, "BurstHeuristicsDetector");
  }
  /** Recent text changes for analysis */
  recentChanges = [];
  /** Timestamp of last change */
  lastChangeTime = 0;
  /** Configuration for burst detection */
  config;
  /**
  * Creates a new BurstHeuristicsDetector
  *
  * @param config Optional configuration to override defaults
  */
  constructor(config2) {
    this.config = {
      timeWindow: config2?.timeWindow ?? DEFAULT_BURST_CONFIG.timeWindow,
      minCharsInserted: config2?.minCharsInserted ?? DEFAULT_BURST_CONFIG.minCharsInserted,
      maxKeystrokeInterval: config2?.maxKeystrokeInterval ?? DEFAULT_BURST_CONFIG.maxKeystrokeInterval,
      minLinesAffected: config2?.minLinesAffected ?? DEFAULT_BURST_CONFIG.minLinesAffected,
      minInsertDeleteRatio: config2?.minInsertDeleteRatio ?? DEFAULT_BURST_CONFIG.minInsertDeleteRatio
    };
  }
  /**
  * Records a text change event for burst analysis
  *
  * @param charsInserted Number of characters inserted
  * @param charsDeleted Number of characters deleted
  * @param linesAffected Number of lines affected
  */
  recordChange(charsInserted, charsDeleted, linesAffected) {
    const now = Date.now();
    const interval = this.lastChangeTime > 0 ? now - this.lastChangeTime : 0;
    const changeInfo = {
      timestamp: now,
      charsInserted,
      charsDeleted,
      linesAffected,
      interval
    };
    this.recentChanges.push(changeInfo);
    this.lastChangeTime = now;
    this.trimOldChanges();
  }
  /**
  * Analyzes recent changes to detect burst patterns
  *
  * @returns Burst detection result
  */
  analyzeBurst() {
    if (this.recentChanges.length < 2) {
      return {
        isBurst: false,
        confidence: 0
      };
    }
    const now = Date.now();
    const windowChanges = this.recentChanges.filter((change) => now - change.timestamp <= this.config.timeWindow);
    if (windowChanges.length < 2) {
      return {
        isBurst: false,
        confidence: 0
      };
    }
    const totalInserted = windowChanges.reduce((sum, change) => sum + change.charsInserted, 0);
    const totalDeleted = windowChanges.reduce((sum, change) => sum + change.charsDeleted, 0);
    const totalLines = windowChanges.reduce((sum, change) => sum + change.linesAffected, 0);
    const duration = windowChanges.length > 1 ? windowChanges[windowChanges.length - 1].timestamp - windowChanges[0].timestamp : 0;
    const meetsCharThreshold = totalInserted >= this.config.minCharsInserted;
    const meetsLineThreshold = totalLines >= this.config.minLinesAffected;
    const ratio = totalDeleted > 0 ? totalInserted / totalDeleted : totalInserted;
    const meetsRatioThreshold = ratio >= this.config.minInsertDeleteRatio;
    const intervals = windowChanges.slice(1).map((change, i) => change.timestamp - windowChanges[i].timestamp);
    const avgInterval = intervals.length > 0 ? intervals.reduce((sum, interval) => sum + interval, 0) / intervals.length : 0;
    const meetsTimingThreshold = avgInterval <= this.config.maxKeystrokeInterval;
    const isBurst = meetsCharThreshold && meetsLineThreshold && meetsRatioThreshold && meetsTimingThreshold;
    let confidence = 0;
    if (isBurst) {
      const charConfidence = Math.min(1, totalInserted / (this.config.minCharsInserted * 2));
      const lineConfidence = Math.min(1, totalLines / (this.config.minLinesAffected * 2));
      const ratioConfidence = Math.min(1, ratio / (this.config.minInsertDeleteRatio * 2));
      const timingConfidence = avgInterval > 0 ? Math.min(1, this.config.maxKeystrokeInterval / (avgInterval * 2)) : 1;
      confidence = (charConfidence + lineConfidence + ratioConfidence + timingConfidence) / 4;
    }
    return {
      isBurst,
      confidence,
      details: isBurst ? {
        totalInserted,
        totalDeleted,
        ratio,
        changeCount: windowChanges.length,
        duration
      } : void 0
    };
  }
  /**
  * Trims old changes outside the analysis time window
  */
  trimOldChanges() {
    const cutoffTime = Date.now() - this.config.timeWindow;
    this.recentChanges = this.recentChanges.filter((change) => change.timestamp >= cutoffTime);
  }
  /**
  * Clears all recorded changes
  */
  clear() {
    this.recentChanges = [];
    this.lastChangeTime = 0;
  }
});
var CursorDetector = class {
  static {
    __name(this, "CursorDetector");
  }
  static {
    __name6(this, "CursorDetector");
  }
  env;
  constructor(env) {
    this.env = env;
  }
  /**
  * Detects Cursor IDE presence via appName heuristic
  */
  detect() {
    const appName = this.env.getAppName().toLowerCase();
    if (appName.includes("cursor")) {
      return {
        hasCursor: true,
        confidence: 7
      };
    }
    return {
      hasCursor: false,
      confidence: 8.5
    };
  }
};
var NoOpLogger = class {
  static {
    __name(this, "NoOpLogger");
  }
  static {
    __name6(this, "NoOpLogger");
  }
  debug() {
  }
  info() {
  }
  error() {
  }
};
var NodeTimerService = class {
  static {
    __name(this, "NodeTimerService");
  }
  static {
    __name6(this, "NodeTimerService");
  }
  timeouts = /* @__PURE__ */ new Map();
  intervals = /* @__PURE__ */ new Map();
  nextId = 0;
  setTimeout(callback, ms) {
    const id = `timeout_${this.nextId++}`;
    const timeout = globalThis.setTimeout(callback, ms);
    this.timeouts.set(id, timeout);
    return id;
  }
  clearTimeout(id) {
    const timeout = this.timeouts.get(id);
    if (timeout) {
      globalThis.clearTimeout(timeout);
      this.timeouts.delete(id);
    }
  }
  setInterval(callback, ms) {
    const id = `interval_${this.nextId++}`;
    const interval = globalThis.setInterval(callback, ms);
    this.intervals.set(id, interval);
    return id;
  }
  clearInterval(id) {
    const interval = this.intervals.get(id);
    if (interval) {
      globalThis.clearInterval(interval);
      this.intervals.delete(id);
    }
  }
  /**
  * Dispose all active timers
  */
  dispose() {
    for (const timeout of this.timeouts.values()) {
      globalThis.clearTimeout(timeout);
    }
    for (const interval of this.intervals.values()) {
      globalThis.clearInterval(interval);
    }
    this.timeouts.clear();
    this.intervals.clear();
  }
};
var DEFAULT_EXPERIENCE_THRESHOLDS = THRESHOLDS.experience;
(class {
  static {
    __name(this, "ExperienceClassifier");
  }
  static {
    __name6(this, "ExperienceClassifier");
  }
  storage;
  logger;
  thresholds;
  /**
  * Creates a new Experience Classifier
  *
  * @param options - Configuration options
  */
  constructor(options) {
    this.storage = options.storage;
    this.logger = options.logger || new NoOpLogger();
    this.thresholds = options.thresholds || DEFAULT_EXPERIENCE_THRESHOLDS;
  }
  /**
  * Gets the current experience tier for the user
  *
  * @returns The user's experience tier
  */
  async getExperienceTier() {
    const manualTier = await this.storage.get("experienceTier");
    if (manualTier && manualTier !== "unknown") {
      return manualTier;
    }
    const metrics = await this.getExperienceMetrics();
    if (this.meetsThreshold(metrics, "power")) {
      return "power";
    }
    if (this.meetsThreshold(metrics, "intermediate")) {
      return "intermediate";
    }
    if (this.meetsThreshold(metrics, "explorer")) {
      return "explorer";
    }
    return "unknown";
  }
  /**
  * Checks if metrics meet or exceed a threshold tier
  *
  * @param metrics - Experience metrics
  * @param tier - Tier to check against
  * @returns True if metrics meet or exceed the tier
  */
  meetsThreshold(metrics, tier) {
    const threshold = this.thresholds[tier];
    return metrics.snapshotsCreated >= threshold.snapshotsCreated && metrics.sessionsRecorded >= threshold.sessionsRecorded && metrics.protectedFiles >= threshold.protectedFiles && metrics.manualRestores >= threshold.manualRestores && metrics.aiAssistedSessions >= threshold.aiAssistedSessions && metrics.daysSinceFirstUse >= threshold.daysSinceFirstUse && metrics.commandDiversity >= threshold.commandDiversity;
  }
  /**
  * Gets experience metrics for the current user
  *
  * @returns Experience metrics
  */
  async getExperienceMetrics() {
    const snapshotsCreated = await this.storage.get("snapshotsCreated", 0);
    const sessionsRecorded = await this.storage.get("sessionsRecorded", 0);
    const protectedFiles = await this.storage.get("protectedFiles", 0);
    const manualRestores = await this.storage.get("manualRestores", 0);
    const aiAssistedSessions = await this.storage.get("aiAssistedSessions", 0);
    const firstUseTimestamp = await this.storage.get("firstUseTimestamp", Date.now());
    const commandsUsed = await this.storage.get("commandsUsed", {});
    const daysSinceFirstUse = Math.floor((Date.now() - (firstUseTimestamp || Date.now())) / (1e3 * 60 * 60 * 24));
    const commandsUsedRecord = commandsUsed || {};
    const totalCommands = Object.values(commandsUsedRecord).reduce((sum, count) => sum + count, 0);
    const uniqueCommands = Object.keys(commandsUsedRecord).length;
    const commandDiversity = totalCommands > 0 ? uniqueCommands / Math.min(totalCommands, 20) : 0;
    return {
      snapshotsCreated: snapshotsCreated || 0,
      sessionsRecorded: sessionsRecorded || 0,
      protectedFiles: protectedFiles || 0,
      manualRestores: manualRestores || 0,
      aiAssistedSessions: aiAssistedSessions || 0,
      daysSinceFirstUse,
      commandDiversity
    };
  }
  /**
  * Updates experience metrics based on user activity
  *
  * @param activity - Type of activity to record
  * @param count - Number of activities to record (default: 1)
  */
  async updateExperienceMetrics(activity, count = 1) {
    const current = await this.storage.get(activity, 0);
    await this.storage.set(activity, (current || 0) + count);
    const firstUseTimestamp = await this.storage.get("firstUseTimestamp");
    if (!firstUseTimestamp) {
      await this.storage.set("firstUseTimestamp", Date.now());
    }
    this.logger.debug("Experience metrics updated", {
      activity,
      count,
      newValue: (current || 0) + count
    });
  }
  /**
  * Records command usage for diversity calculation
  *
  * @param command - Command that was used
  */
  async recordCommandUsage(command) {
    const commandsUsed = await this.storage.get("commandsUsed", {});
    const commandsUsedRecord = commandsUsed || {};
    commandsUsedRecord[command] = (commandsUsedRecord[command] || 0) + 1;
    await this.storage.set("commandsUsed", commandsUsedRecord);
    this.logger.debug("Command usage recorded", {
      command,
      count: commandsUsedRecord[command]
    });
  }
  /**
  * Sets experience tier manually (for testing)
  *
  * @param tier - Experience tier to set
  */
  async setExperienceTier(tier) {
    await this.storage.set("experienceTier", tier);
    this.logger.info("Experience tier manually set", {
      tier
    });
  }
  /**
  * Resets experience tier (for testing)
  */
  async resetExperienceTier() {
    await this.storage.set("experienceTier", void 0);
    this.logger.info("Experience tier reset");
  }
  /**
  * Gets a description of the user's experience tier
  *
  * @returns Description of the experience tier
  */
  async getExperienceTierDescription() {
    const tier = await this.getExperienceTier();
    switch (tier) {
      case "explorer":
        return "Welcome to SnapBack! You're just getting started with file protection.";
      case "intermediate":
        return "You're becoming a SnapBack pro! You're using multiple protection levels effectively.";
      case "power":
        return "You're a SnapBack expert! You're using the full power of the extension.";
      default:
        return "We're still learning about how you use SnapBack.";
    }
  }
});
var DEFAULT_CONFIG2 = {
  idleTimeout: THRESHOLDS.session.idleTimeout,
  minSessionDuration: THRESHOLDS.session.minSessionDuration,
  maxSessionDuration: THRESHOLDS.session.maxSessionDuration,
  longSessionCheckInterval: 3e5
};
(class {
  static {
    __name(this, "SessionCoordinator");
  }
  static {
    __name6(this, "SessionCoordinator");
  }
  /** Map of active session candidates by file URI */
  candidates = /* @__PURE__ */ new Map();
  /** Timeout ID for idle detection */
  idleTimeoutId = null;
  /** Interval ID for long session checking */
  longSessionIntervalId = null;
  /** Current session start time */
  sessionStart = Date.now();
  /** Storage adapter */
  storage;
  /** Timer service */
  timers;
  /** Logger */
  logger;
  /** Event emitter (optional) */
  eventEmitter;
  /** Configuration */
  config;
  /**
  * Creates a new SessionCoordinator
  *
  * @param options - Configuration options
  */
  constructor(options) {
    this.storage = options.storage;
    this.timers = options.timers || new NodeTimerService();
    this.logger = options.logger || new NoOpLogger();
    this.eventEmitter = options.eventEmitter;
    this.config = {
      ...DEFAULT_CONFIG2,
      ...options.config
    };
    this.sessionStart = Date.now();
    this.resetIdleTimer();
    this.startLongSessionMonitoring();
  }
  /**
  * Add or update a file candidate in the current session
  *
  * @param uri - URI of the file
  * @param snapshotId - ID of the snapshot for this file
  * @param stats - Optional change statistics
  */
  addCandidate(uri, snapshotId, stats) {
    const candidate = {
      uri,
      snapshotId,
      stats,
      updatedAt: Date.now()
    };
    this.candidates.set(uri, candidate);
    this.resetIdleTimer();
    this.logger.debug("Added session candidate", {
      uri,
      snapshotId
    });
  }
  /**
  * Finalize the current session with a specific reason
  *
  * @param reason - Reason for finalizing the session
  * @returns Session ID if finalized, null if skipped
  */
  async finalizeSession(reason) {
    try {
      const now = Date.now();
      const sessionDuration = now - this.sessionStart;
      if (sessionDuration < this.config.minSessionDuration || this.candidates.size === 0) {
        this.logger.debug("Skipping session finalization - session too short or no candidates", {
          duration: sessionDuration,
          candidateCount: this.candidates.size
        });
        this.resetSession();
        return null;
      }
      const sessionId = `session-${randomUUID()}`;
      const manifest = {
        id: sessionId,
        startedAt: this.sessionStart,
        endedAt: now,
        reason,
        files: Array.from(this.candidates.values()).map((candidate) => ({
          uri: candidate.uri,
          snapshotId: candidate.snapshotId,
          changeStats: candidate.stats
        })),
        tags: []
      };
      try {
        await this.storeSessionManifest(manifest);
        if (this.eventEmitter) {
          this.eventEmitter.fire(manifest);
        }
        this.logger.info("Session finalized", {
          sessionId,
          reason,
          fileCount: manifest.files.length,
          duration: sessionDuration
        });
        this.resetSession();
        return sessionId;
      } catch (error) {
        this.logger.error(`Failed to finalize session: ${error instanceof Error ? error.message : String(error)}`, error instanceof Error ? error : void 0, {
          sessionId
        });
        return null;
      }
    } catch (error) {
      this.logger.error("Unexpected error in finalizeSession", error instanceof Error ? error : void 0);
      return null;
    }
  }
  /**
  * Handle window blur event - finalize session due to window focus change
  */
  handleWindowBlur() {
    this.finalizeSession("blur");
  }
  /**
  * Handle git commit event - finalize session due to git commit
  */
  handleGitCommit() {
    this.finalizeSession("commit");
  }
  /**
  * Handle task completion event - finalize session due to task completion
  */
  handleTaskCompletion() {
    this.finalizeSession("task");
  }
  /**
  * Handle manual session finalization
  */
  handleManualFinalization() {
    this.finalizeSession("manual");
  }
  /**
  * Handle idle timeout - finalize session due to inactivity
  */
  handleIdleTimeout() {
    if (this.candidates.size > 0) {
      this.finalizeSession("idle-break");
    } else {
      this.sessionStart = Date.now();
    }
  }
  /**
  * Check for long-running sessions and finalize them if needed
  */
  checkLongSession() {
    const now = Date.now();
    const sessionDuration = now - this.sessionStart;
    if (sessionDuration > this.config.maxSessionDuration && this.candidates.size > 0) {
      this.logger.info("Finalizing long-running session", {
        duration: sessionDuration,
        maxDuration: this.config.maxSessionDuration,
        candidateCount: this.candidates.size
      });
      this.finalizeSession("max-duration");
    }
  }
  /**
  * Reset the idle timer
  */
  resetIdleTimer() {
    if (this.idleTimeoutId) {
      this.timers.clearTimeout(this.idleTimeoutId);
    }
    this.idleTimeoutId = this.timers.setTimeout(() => {
      this.handleIdleTimeout();
    }, this.config.idleTimeout);
  }
  /**
  * Start monitoring for long sessions
  */
  startLongSessionMonitoring() {
    this.longSessionIntervalId = this.timers.setInterval(() => {
      this.checkLongSession();
    }, this.config.longSessionCheckInterval);
  }
  /**
  * Reset session state for the next session
  */
  resetSession() {
    this.candidates.clear();
    this.sessionStart = Date.now();
    if (this.idleTimeoutId) {
      this.timers.clearTimeout(this.idleTimeoutId);
    }
    this.resetIdleTimer();
  }
  /**
  * Store session manifest in persistent storage
  *
  * @param manifest - Session manifest to store
  */
  async storeSessionManifest(manifest) {
    try {
      await this.storage.storeSessionManifest(manifest);
      this.logger.debug("Stored session manifest", {
        sessionId: manifest.id,
        fileCount: manifest.files.length
      });
    } catch (error) {
      this.logger.error("Failed to store session manifest", error instanceof Error ? error : new Error(String(error)), {
        sessionId: manifest.id
      });
      throw error;
    }
  }
  /**
  * Get the number of active candidates (for testing)
  */
  getCandidateCount() {
    return this.candidates.size;
  }
  /**
  * Get the session start time (for testing)
  */
  getSessionStart() {
    return this.sessionStart;
  }
  /**
  * Dispose of the session coordinator and clean up resources
  */
  dispose() {
    if (this.idleTimeoutId) {
      this.timers.clearTimeout(this.idleTimeoutId);
    }
    if (this.longSessionIntervalId) {
      this.timers.clearInterval(this.longSessionIntervalId);
    }
    if (this.eventEmitter) {
      this.eventEmitter.dispose();
    }
  }
});
(class {
  static {
    __name(this, "SessionSummaryGenerator");
  }
  static {
    __name6(this, "SessionSummaryGenerator");
  }
  snapshotProvider;
  logger;
  /**
  * Creates a new SessionSummaryGenerator
  *
  * @param options - Configuration options (optional)
  */
  constructor(options) {
    this.snapshotProvider = options?.snapshotProvider;
    this.logger = options?.logger || createSilentLogger();
  }
  /**
  * Generates a deterministic summary for a session
  *
  * Creates a human-readable summary that describes the changes in a session
  * without including any sensitive content or file paths.
  *
  * @param session - Session manifest to summarize
  * @returns Promise that resolves to a session summary
  */
  async generateSummary(session) {
    try {
      if (this.snapshotProvider) {
        return await this.generateDetailedSummary(session);
      }
      return this.generateMetadataSummary(session);
    } catch (error) {
      this.logger.error("Failed to generate session summary", error instanceof Error ? error : void 0, {
        error
      });
      return "Session summary unavailable";
    }
  }
  /**
  * Generates a detailed summary by analyzing actual file changes
  *
  * @param session - Session manifest to summarize
  * @returns Promise that resolves to a detailed session summary
  */
  async generateDetailedSummary(session) {
    try {
      const allIdentifiers = /* @__PURE__ */ new Set();
      for (const fileEntry of session.files) {
        try {
          const snapshot = await this.snapshotProvider?.get(fileEntry.snapshotId);
          if (snapshot?.fileContents) {
            for (const [filePath, content] of Object.entries(snapshot.fileContents)) {
              const identifiers = await this.extractTopIdentifiers(content, filePath);
              for (const identifier of identifiers) {
                allIdentifiers.add(identifier);
              }
            }
          }
        } catch (error) {
          this.logger.error(`Failed to process file ${fileEntry.uri} for summary`, void 0, {
            error
          });
        }
      }
      const fileCount = session.files.length;
      const duration = Math.round((session.endedAt - session.startedAt) / 1e3);
      let summary = "";
      if (fileCount === 0) {
        summary = "Empty session";
      } else if (fileCount === 1) {
        summary = `Modified 1 file over ${duration}s`;
      } else {
        summary = `Modified ${fileCount} files over ${duration}s`;
      }
      const aiTags = session.tags?.filter((tag) => tag.includes("ai") || tag.includes("copilot") || tag.includes("claude"));
      if (aiTags && aiTags.length > 0) {
        summary = `[AI] ${summary}`;
      }
      if (allIdentifiers.size > 0) {
        const topIdentifiers = Array.from(allIdentifiers).slice(0, 3);
        summary += ` - ${topIdentifiers.join(", ")}`;
      }
      return summary;
    } catch (error) {
      this.logger.error("Failed to generate detailed session summary", error instanceof Error ? error : void 0, {
        error
      });
      return this.generateMetadataSummary(session);
    }
  }
  /**
  * Generates a summary based on session metadata only
  *
  * @param session - Session manifest to summarize
  * @returns Session summary based on metadata
  */
  generateMetadataSummary(session) {
    const fileCount = session.files.length;
    const duration = Math.round((session.endedAt - session.startedAt) / 1e3);
    if (fileCount === 0) {
      return "Empty session";
    }
    const aiTags = session.tags?.filter((tag) => tag.includes("ai") || tag.includes("copilot") || tag.includes("claude"));
    const aiPrefix = aiTags && aiTags.length > 0 ? "[AI] " : "";
    if (fileCount === 1) {
      return `${aiPrefix}Modified 1 file over ${duration}s`;
    }
    return `${aiPrefix}Modified ${fileCount} files over ${duration}s`;
  }
  /**
  * Extracts top identifiers from file content for use in summaries
  *
  * Uses regex-based extraction to identify the most important identifiers
  * (functions, classes, variables) in the file content.
  *
  * @param content - File content to analyze
  * @param filePath - Path to the file (used to determine language)
  * @returns Promise that resolves to array of top identifiers
  */
  async extractTopIdentifiers(content, filePath) {
    try {
      const extension = path10.extname(filePath).toLowerCase();
      if (extension === ".ts" || extension === ".js" || extension === ".tsx" || extension === ".jsx") {
        return this.extractIdentifiersWithRegex(content);
      }
      return this.extractIdentifiersWithRegex(content);
    } catch (error) {
      this.logger.error(`Failed to extract identifiers from ${filePath}`, void 0, {
        error
      });
      return this.extractIdentifiersWithRegex(content);
    }
  }
  /**
  * Extracts identifiers using regex patterns
  *
  * @param content - File content
  * @returns Array of identifiers
  */
  extractIdentifiersWithRegex(content) {
    const patterns = [
      /function\s+([a-zA-Z_$][a-zA-Z0-9_$]*)/g,
      /const\s+([a-zA-Z_$][a-zA-Z0-9_$]*)/g,
      /let\s+([a-zA-Z_$][a-zA-Z0-9_$]*)/g,
      /var\s+([a-zA-Z_$][a-zA-Z0-9_$]*)/g,
      /class\s+([a-zA-Z_$][a-zA-Z0-9_$]*)/g,
      /interface\s+([a-zA-Z_$][a-zA-Z0-9_$]*)/g,
      /async\s+([a-zA-Z_$][a-zA-Z0-9_$]*)\s*\(/g,
      /([a-zA-Z_$][a-zA-Z0-9_$]*)\s*\([^)]*\)\s*\{/g
    ];
    const identifiers = /* @__PURE__ */ new Set();
    for (const pattern of patterns) {
      let match = pattern.exec(content);
      while (match !== null) {
        const identifier = match[1];
        if (identifier.length > 2 && !this.isCommonKeyword(identifier)) {
          identifiers.add(identifier);
        }
        match = pattern.exec(content);
      }
    }
    return Array.from(identifiers).slice(0, 5);
  }
  /**
  * Checks if an identifier is a common keyword that should be excluded
  *
  * @param identifier - Identifier to check
  * @returns True if it's a common keyword
  */
  isCommonKeyword(identifier) {
    const commonKeywords = /* @__PURE__ */ new Set([
      "if",
      "else",
      "for",
      "while",
      "do",
      "switch",
      "case",
      "break",
      "continue",
      "try",
      "catch",
      "finally",
      "throw",
      "return",
      "yield",
      "await",
      "async",
      "function",
      "const",
      "let",
      "var",
      "class",
      "interface",
      "type",
      "enum",
      "import",
      "export",
      "from",
      "as",
      "default",
      "extends",
      "implements",
      "static",
      "public",
      "private",
      "protected",
      "readonly",
      "abstract",
      "get",
      "set",
      "constructor",
      "super",
      "this",
      "new",
      "delete",
      "typeof",
      "instanceof",
      "in",
      "of",
      "void",
      "null",
      "undefined",
      "true",
      "false",
      "boolean",
      "number",
      "string",
      "object",
      "symbol",
      "bigint",
      "any",
      "unknown",
      "never",
      "void",
      "undefined",
      "null",
      "Promise",
      "Array",
      "String",
      "Number",
      "Boolean",
      "Object",
      "Function",
      "RegExp",
      "Date",
      "Error",
      "Map",
      "Set",
      "WeakMap",
      "WeakSet",
      "Proxy",
      "Reflect"
    ]);
    return commonKeywords.has(identifier);
  }
});
var DEFAULT_CONFIG22 = {
  minBurstConfidence: THRESHOLDS.tagging.minBurstConfidence,
  minLongSessionDuration: THRESHOLDS.tagging.minLongSessionDuration,
  maxShortSessionDuration: THRESHOLDS.tagging.maxShortSessionDuration,
  minLargeEditLines: THRESHOLDS.tagging.minLargeEditLines,
  normalization: {
    multiFileThreshold: THRESHOLDS.tagging.normalization.multiFileThreshold,
    multiFileNormalization: THRESHOLDS.tagging.normalization.multiFileNormalization,
    longSessionNormalization: THRESHOLDS.tagging.normalization.longSessionNormalization,
    largeEditsNormalization: THRESHOLDS.tagging.normalization.largeEditsNormalization
  }
};
(class {
  static {
    __name(this, "SessionTagger");
  }
  static {
    __name6(this, "SessionTagger");
  }
  aiPresenceDetector;
  config;
  /**
  * Creates a new SessionTagger
  *
  * @param options Optional configuration and dependencies
  */
  constructor(options) {
    this.aiPresenceDetector = options?.aiPresenceDetector;
    this.config = {
      ...DEFAULT_CONFIG22,
      ...options?.config,
      normalization: {
        ...DEFAULT_CONFIG22.normalization,
        ...options?.config?.normalization
      }
    };
  }
  /**
  * Analyzes a session and generates appropriate tags
  *
  * @param manifest Session manifest to analyze
  * @param burstResult Optional burst detection result
  * @returns Session tagging result with tags and confidence levels
  */
  tagSession(manifest, burstResult) {
    const tags = [
      ...manifest.tags || []
    ];
    const confidence = {};
    const reasons = {};
    this.addReasonTags(manifest.reason, tags, confidence, reasons);
    const multiFileThreshold = this.config.normalization?.multiFileThreshold;
    if (multiFileThreshold && manifest.files.length > multiFileThreshold) {
      tags.push("multi-file");
      confidence["multi-file"] = Math.min(1, manifest.files.length / (this.config.normalization?.multiFileNormalization || 1));
      reasons["multi-file"] = `Session involved ${manifest.files.length} files`;
    }
    const duration = manifest.endedAt - manifest.startedAt;
    if (duration > this.config.minLongSessionDuration) {
      tags.push("long-session");
      confidence["long-session"] = Math.min(1, duration / (this.config.normalization?.longSessionNormalization || 1));
      reasons["long-session"] = `Session lasted ${Math.round(duration / 6e4)} minutes`;
    } else if (duration < this.config.maxShortSessionDuration) {
      tags.push("short-session");
      confidence["short-session"] = Math.min(1, this.config.maxShortSessionDuration / duration);
      reasons["short-session"] = `Session lasted ${Math.round(duration / 1e3)} seconds`;
    }
    const totalAdded = manifest.files.reduce((sum, file) => {
      return sum + (file.changeStats?.added || 0);
    }, 0);
    if (totalAdded > this.config.minLargeEditLines) {
      tags.push("large-edits");
      confidence["large-edits"] = Math.min(1, totalAdded / (this.config.normalization?.largeEditsNormalization || 1));
      reasons["large-edits"] = `Session involved ${totalAdded} lines added`;
    }
    if (this.aiPresenceDetector) {
      const aiPresence = this.aiPresenceDetector();
      if (aiPresence.hasAI) {
        tags.push("ai-assisted");
        confidence["ai-assisted"] = 0.9;
        reasons["ai-assisted"] = `AI assistants detected: ${aiPresence.detectedAssistants.join(", ")}`;
        for (const assistant of aiPresence.detectedAssistants) {
          const assistantName = assistant.toLowerCase().replace(/_/g, "-").replace("github-", "");
          const tag = `${assistantName}-like`;
          tags.push(tag);
          confidence[tag] = 0.8;
          reasons[tag] = `Detected ${aiPresence.assistantDetails[assistant]} presence`;
        }
      }
    }
    if (burstResult?.isBurst) {
      tags.push("burst");
      confidence.burst = burstResult.confidence;
      reasons.burst = "Session contained rapid, large insertions characteristic of AI assistance";
    }
    const uniqueTags = Array.from(new Set(tags));
    return {
      tags: uniqueTags,
      confidence,
      reasons
    };
  }
  /**
  * Updates a session manifest with appropriate tags
  *
  * @param manifest Session manifest to update
  * @param burstResult Optional burst detection result
  * @returns Updated session manifest with tags
  */
  updateSessionWithTags(manifest, burstResult) {
    const taggingResult = this.tagSession(manifest, burstResult);
    return {
      ...manifest,
      tags: taggingResult.tags
    };
  }
  /**
  * Adds tags based on session finalization reason
  */
  addReasonTags(reason, tags, confidence, reasons) {
    switch (reason) {
      case "manual":
        tags.push("manual");
        confidence.manual = 1;
        reasons.manual = "Session was manually finalized";
        break;
      case "idle-break":
        tags.push("manual");
        confidence.manual = 0.8;
        reasons.manual = "Session ended due to idle timeout";
        break;
      case "blur":
        tags.push("manual");
        confidence.manual = 0.8;
        reasons.manual = "Session ended when window lost focus";
        break;
      case "task":
        tags.push("manual");
        confidence.manual = 0.9;
        reasons.manual = "Session ended due to task boundary";
        break;
      case "commit":
        tags.push("manual");
        confidence.manual = 0.95;
        reasons.manual = "Session ended with git commit";
        break;
    }
  }
});
(class {
  static {
    __name(this, "EncryptionService");
  }
  static {
    __name6(this, "EncryptionService");
  }
  PBKDF2_ITERATIONS = 1e5;
  KEY_LENGTH = 256;
  ALGORITHM = "aes-256-gcm";
  IV_LENGTH = 12;
  /**
  * Derive encryption key from user secret using PBKDF2
  */
  async deriveKey(userSecret, salt) {
    const keyMaterial = await crypto.subtle.importKey("raw", new TextEncoder().encode(userSecret), {
      name: "PBKDF2"
    }, false, [
      "deriveKey"
    ]);
    return crypto.subtle.deriveKey({
      name: "PBKDF2",
      salt,
      iterations: this.PBKDF2_ITERATIONS,
      hash: "SHA-256"
    }, keyMaterial, {
      name: this.ALGORITHM,
      length: this.KEY_LENGTH
    }, false, [
      "encrypt",
      "decrypt"
    ]);
  }
  /**
  * Generate SHA-256 checksum of data for integrity verification
  */
  async generateChecksum(data) {
    const buffer = new TextEncoder().encode(data);
    const hashBuffer = await crypto.subtle.digest("SHA-256", buffer);
    const hashArray = Array.from(new Uint8Array(hashBuffer));
    return hashArray.map((b) => b.toString(16).padStart(2, "0")).join("");
  }
  /**
  * Encrypt a snapshot with user secret
  * @param snapshot - Snapshot object to encrypt
  * @param userSecret - User's encryption secret (from auth session or user input)
  * @returns Encrypted blob ready for upload
  */
  async encryptSnapshot(snapshot, userSecret) {
    const salt = crypto.getRandomValues(new Uint8Array(16));
    const iv = crypto.getRandomValues(new Uint8Array(12));
    const key = await this.deriveKey(userSecret, salt);
    const plaintext = JSON.stringify(snapshot);
    const checksum = await this.generateChecksum(plaintext);
    const plaintextBuffer = new TextEncoder().encode(plaintext);
    const ciphertextBuffer = await crypto.subtle.encrypt({
      name: this.ALGORITHM,
      iv
    }, key, plaintextBuffer);
    return {
      version: 1,
      salt: Array.from(salt),
      iv: Array.from(iv),
      ciphertext: Array.from(new Uint8Array(ciphertextBuffer)),
      timestamp: Date.now(),
      checksum
    };
  }
  /**
  * Decrypt an encrypted blob
  * @param blob - Encrypted blob from cloud
  * @param userSecret - User's encryption secret
  * @returns Original snapshot object
  * @throws Error if decryption fails or checksum mismatch
  */
  async decryptSnapshot(blob, userSecret) {
    const salt = new Uint8Array(blob.salt);
    const iv = new Uint8Array(blob.iv);
    const ciphertext = new Uint8Array(blob.ciphertext);
    const key = await this.deriveKey(userSecret, salt);
    let plaintextBuffer;
    try {
      plaintextBuffer = await crypto.subtle.decrypt({
        name: this.ALGORITHM,
        iv
      }, key, ciphertext);
    } catch (_error) {
      throw new Error("Decryption failed. Wrong password or corrupted data.");
    }
    const plaintext = new TextDecoder().decode(plaintextBuffer);
    const checksum = await this.generateChecksum(plaintext);
    if (checksum !== blob.checksum) {
      throw new Error("Checksum mismatch. Data may be corrupted.");
    }
    return JSON.parse(plaintext);
  }
  /**
  * Verify if a user secret can decrypt a blob without full decryption
  * (Faster than full decrypt for password verification)
  */
  async verifySecret(blob, userSecret) {
    try {
      await this.decryptSnapshot(blob, userSecret);
      return true;
    } catch {
      return false;
    }
  }
  /**
  * Encrypt plaintext data using AES-256-GCM (legacy method for backward compatibility)
  *
  * @param plaintext Data to encrypt
  * @returns Encrypted data with IV and authentication tag
  */
  encrypt(plaintext) {
    const key = Buffer.from("0123456789abcdef0123456789abcdef", "hex");
    const iv = randomBytes(this.IV_LENGTH);
    const cipher = createCipheriv(this.ALGORITHM, key, iv);
    const encrypted = Buffer.concat([
      cipher.update(plaintext, "utf8"),
      cipher.final()
    ]);
    const authTag = cipher.getAuthTag();
    return {
      ciphertext: encrypted.toString("base64"),
      iv: iv.toString("base64"),
      authTag: authTag.toString("base64"),
      algorithm: this.ALGORITHM
    };
  }
  /**
  * Decrypt encrypted data using AES-256-GCM (legacy method for backward compatibility)
  *
  * @param encrypted Encrypted data with IV and auth tag
  * @returns Decrypted plaintext
  */
  decrypt(encrypted) {
    if (encrypted.algorithm !== this.ALGORITHM) {
      throw new Error(`Unsupported algorithm: ${encrypted.algorithm}`);
    }
    const key = Buffer.from("0123456789abcdef0123456789abcdef", "hex");
    const decipher = createDecipheriv(this.ALGORITHM, key, Buffer.from(encrypted.iv, "base64"));
    decipher.setAuthTag(Buffer.from(encrypted.authTag, "base64"));
    const decrypted = Buffer.concat([
      decipher.update(Buffer.from(encrypted.ciphertext, "base64")),
      decipher.final()
    ]);
    return decrypted.toString("utf8");
  }
  /**
  * Compute content hash for deduplication (post-encryption)
  *
  * @param content Original plaintext content
  * @returns SHA-256 hash for deduplication
  */
  computeContentHash(content) {
    return createHash("sha256").update(content).digest("hex");
  }
});
var SnapBackError = class extends Error {
  static {
    __name(this, "SnapBackError");
  }
  static {
    __name6(this, "SnapBackError");
  }
  code;
  context;
  cause;
  timestamp;
  constructor(message, code, context, cause) {
    super(message), this.code = code, this.context = context, this.cause = cause;
    this.name = this.constructor.name;
    this.timestamp = Date.now();
    if (Error.captureStackTrace) {
      Error.captureStackTrace(this, this.constructor);
    }
  }
  /**
  * Serialize error to JSON for logging/API responses
  */
  toJSON() {
    return {
      name: this.name,
      message: this.message,
      code: this.code,
      context: this.context,
      timestamp: this.timestamp,
      stack: this.stack,
      cause: this.cause ? {
        name: this.cause.name,
        message: this.cause.message,
        stack: this.cause.stack
      } : void 0
    };
  }
};
var SnapshotError = class extends SnapBackError {
  static {
    __name(this, "SnapshotError");
  }
  static {
    __name6(this, "SnapshotError");
  }
};
(class extends SnapshotError {
  static {
    __name(this, "SnapshotNotFoundError");
  }
  static {
    __name6(this, "SnapshotNotFoundError");
  }
  snapshotId;
  constructor(snapshotId, cause) {
    super(`Snapshot not found: ${snapshotId}`, "SNAPSHOT_NOT_FOUND", {
      snapshotId
    }, cause), this.snapshotId = snapshotId;
  }
});
(class extends SnapshotError {
  static {
    __name(this, "SnapshotCreationError");
  }
  static {
    __name6(this, "SnapshotCreationError");
  }
  constructor(message, context, cause) {
    super(message, "SNAPSHOT_CREATION_ERROR", context, cause);
  }
});
(class extends SnapshotError {
  static {
    __name(this, "SnapshotDuplicateError");
  }
  static {
    __name6(this, "SnapshotDuplicateError");
  }
  existingId;
  constructor(existingId, context) {
    super(existingId ? `Duplicate snapshot detected (existing: ${existingId})` : "Duplicate snapshot detected", "SNAPSHOT_DUPLICATE", {
      existingId,
      ...context
    }), this.existingId = existingId;
  }
});
(class extends SnapshotError {
  static {
    __name(this, "SnapshotProtectedError");
  }
  static {
    __name6(this, "SnapshotProtectedError");
  }
  snapshotId;
  constructor(snapshotId) {
    super(`Cannot modify protected snapshot: ${snapshotId}`, "SNAPSHOT_PROTECTED", {
      snapshotId
    }), this.snapshotId = snapshotId;
  }
});
(class extends SnapshotError {
  static {
    __name(this, "SnapshotRestoreError");
  }
  static {
    __name6(this, "SnapshotRestoreError");
  }
  snapshotId;
  constructor(message, snapshotId, context, cause) {
    super(message, "SNAPSHOT_RESTORE_ERROR", {
      snapshotId,
      ...context
    }, cause), this.snapshotId = snapshotId;
  }
});
(class extends SnapshotError {
  static {
    __name(this, "SnapshotVersionError");
  }
  static {
    __name6(this, "SnapshotVersionError");
  }
  version;
  supportedVersions;
  constructor(version, supportedVersions) {
    super(`Incompatible snapshot version: ${version}. Supported: ${supportedVersions.join(", ")}`, "SNAPSHOT_VERSION_INCOMPATIBLE", {
      version,
      supportedVersions
    }), this.version = version, this.supportedVersions = supportedVersions;
  }
});
(class extends SnapshotError {
  static {
    __name(this, "SnapshotVerificationError");
  }
  static {
    __name6(this, "SnapshotVerificationError");
  }
  filePath;
  expected;
  actual;
  constructor(filePath, expected, actual) {
    super(`Verification failed for ${filePath}: checksum mismatch`, "SNAPSHOT_VERIFICATION_FAILED", {
      filePath,
      expected,
      actual
    }), this.filePath = filePath, this.expected = expected, this.actual = actual;
  }
});
var StorageError = class extends SnapBackError {
  static {
    __name(this, "StorageError");
  }
  static {
    __name6(this, "StorageError");
  }
};
(class extends StorageError {
  static {
    __name(this, "StorageFullError");
  }
  static {
    __name6(this, "StorageFullError");
  }
  required;
  available;
  constructor(required, available, cause) {
    super(`Storage full: ${required}MB required, ${available}MB available`, "STORAGE_FULL", {
      required,
      available,
      retryable: false
    }, cause), this.required = required, this.available = available;
  }
});
(class extends StorageError {
  static {
    __name(this, "StorageLockError");
  }
  static {
    __name6(this, "StorageLockError");
  }
  resource;
  constructor(resource, cause) {
    super(`Resource locked: ${resource}`, "STORAGE_LOCK", {
      resource,
      retryable: true
    }, cause), this.resource = resource;
  }
});
(class extends StorageError {
  static {
    __name(this, "StorageIOError");
  }
  static {
    __name6(this, "StorageIOError");
  }
  path;
  constructor(message, path11, cause) {
    super(message, "STORAGE_IO_ERROR", {
      path: path11
    }, cause), this.path = path11;
  }
});
var ValidationError = class extends SnapBackError {
  static {
    __name(this, "ValidationError");
  }
  static {
    __name6(this, "ValidationError");
  }
  constructor(message, code = "VALIDATION_ERROR", context, cause) {
    super(message, code, context, cause);
  }
};
(class extends ValidationError {
  static {
    __name(this, "InputValidationError");
  }
  static {
    __name6(this, "InputValidationError");
  }
  field;
  value;
  constructor(message, field, value) {
    super(message, "INPUT_VALIDATION_ERROR", {
      field,
      value
    }), this.field = field, this.value = value;
  }
});
(class extends ValidationError {
  static {
    __name(this, "PathValidationError");
  }
  static {
    __name6(this, "PathValidationError");
  }
  path;
  constructor(message, path11) {
    super(message, "PATH_VALIDATION_ERROR", {
      path: path11
    }), this.path = path11;
  }
});
(class extends ValidationError {
  static {
    __name(this, "MissingContentError");
  }
  static {
    __name6(this, "MissingContentError");
  }
  filePath;
  constructor(filePath) {
    super(`Missing content for file: ${filePath}`, "MISSING_CONTENT", {
      filePath
    }), this.filePath = filePath;
  }
});
var ApiError = class extends SnapBackError {
  static {
    __name(this, "ApiError");
  }
  static {
    __name6(this, "ApiError");
  }
  status;
  constructor(message, code, status, context, cause) {
    super(message, code, {
      status,
      ...context
    }, cause), this.status = status;
  }
};
(class extends ApiError {
  static {
    __name(this, "RateLimitError");
  }
  static {
    __name6(this, "RateLimitError");
  }
  retryAfter;
  constructor(retryAfter, cause) {
    super(`Rate limit exceeded. Retry after ${retryAfter}ms`, "RATE_LIMIT_EXCEEDED", 429, {
      retryAfter,
      retryable: true
    }, cause), this.retryAfter = retryAfter;
  }
});
(class extends ApiError {
  static {
    __name(this, "AuthenticationError");
  }
  static {
    __name6(this, "AuthenticationError");
  }
  constructor(message = "Authentication required", cause) {
    super(message, "AUTHENTICATION_REQUIRED", 401, void 0, cause);
  }
});
(class extends ApiError {
  static {
    __name(this, "AuthorizationError");
  }
  static {
    __name6(this, "AuthorizationError");
  }
  constructor(message = "Access denied", cause) {
    super(message, "ACCESS_DENIED", 403, void 0, cause);
  }
});
function isSnapBackError(error) {
  return error instanceof SnapBackError;
}
__name(isSnapBackError, "isSnapBackError");
__name6(isSnapBackError, "isSnapBackError");
function isSnapshotError(error) {
  return error instanceof SnapshotError;
}
__name(isSnapshotError, "isSnapshotError");
__name6(isSnapshotError, "isSnapshotError");
function isStorageError(error) {
  return error instanceof StorageError;
}
__name(isStorageError, "isStorageError");
__name6(isStorageError, "isStorageError");
function isValidationError(error) {
  return error instanceof ValidationError;
}
__name(isValidationError, "isValidationError");
__name6(isValidationError, "isValidationError");
function isApiError(error) {
  return error instanceof ApiError;
}
__name(isApiError, "isApiError");
__name6(isApiError, "isApiError");
function isRetryableError(error) {
  if (isSnapBackError(error)) {
    return error.context?.retryable === true;
  }
  return false;
}
__name(isRetryableError, "isRetryableError");
__name6(isRetryableError, "isRetryableError");
function toError(error) {
  if (error instanceof Error) {
    return error;
  }
  if (typeof error === "string") {
    return new Error(error);
  }
  if (typeof error === "object" && error !== null) {
    if ("message" in error && typeof error.message === "string") {
      return new Error(error.message);
    }
    try {
      return new Error(JSON.stringify(error));
    } catch {
      return new Error(String(error));
    }
  }
  return new Error(String(error));
}
__name(toError, "toError");
__name6(toError, "toError");
function ensureSnapBackError(error, code = "UNKNOWN_ERROR", context) {
  if (error instanceof SnapBackError) {
    return error;
  }
  const baseError = toError(error);
  return new SnapBackError(baseError.message, code, context, baseError);
}
__name(ensureSnapBackError, "ensureSnapBackError");
__name6(ensureSnapBackError, "ensureSnapBackError");
var DEFAULT_MAX_SIZE = 10 * 1024 * 1024;
async function atomicWriteFile(path11, content, options = {}) {
  const { encoding = "utf8", maxSize = DEFAULT_MAX_SIZE, mode } = options;
  try {
    const buffer = typeof content === "string" ? Buffer.from(content, encoding) : content;
    if (buffer.length > maxSize) {
      return err(new Error(`Content size (${buffer.length} bytes) exceeds maximum allowed (${maxSize} bytes)`));
    }
    const tempSuffix = randomBytes(8).toString("hex");
    const tempPath = `${path11}.${tempSuffix}.tmp`;
    try {
      await writeFile(tempPath, buffer, {
        mode
      });
      await rename(tempPath, path11);
      return ok(void 0);
    } catch (error) {
      try {
        await unlink(tempPath);
      } catch {
      }
      throw error;
    }
  } catch (error) {
    return err(error instanceof Error ? error : new Error(String(error)));
  }
}
__name(atomicWriteFile, "atomicWriteFile");
__name6(atomicWriteFile, "atomicWriteFile");
function atomicWriteFileSync(path11, content, options = {}) {
  const { encoding = "utf8", maxSize = DEFAULT_MAX_SIZE, mode } = options;
  try {
    const contentSize = Buffer.byteLength(content, encoding);
    if (contentSize > maxSize) {
      return {
        success: false,
        error: `Content size (${contentSize} bytes) exceeds maximum allowed (${maxSize} bytes)`
      };
    }
    const tempSuffix = randomBytes(8).toString("hex");
    const tempPath = `${path11}.${tempSuffix}.tmp`;
    try {
      writeFileSync(tempPath, content, {
        encoding,
        mode
      });
      renameSync(tempPath, path11);
      return {
        success: true
      };
    } catch (error) {
      try {
        unlinkSync(tempPath);
      } catch {
      }
      throw error;
    }
  } catch (error) {
    const message = error instanceof Error ? error.message : String(error);
    return {
      success: false,
      error: `Atomic write failed: ${message}`
    };
  }
}
__name(atomicWriteFileSync, "atomicWriteFileSync");
__name6(atomicWriteFileSync, "atomicWriteFileSync");
async function analyze(client, envelope, request) {
  const idempotentEnvelope = ensureIdempotentRequestId(envelope);
  try {
    const response = await client.getHttpClient().post("v1/analyze", {
      json: {
        ...request,
        envelope: idempotentEnvelope
      },
      timeout: 3e4
    }).json();
    return response;
  } catch (error) {
    logger.error("Analysis failed", {
      error
    });
    throw new Error(`Analysis failed: ${error.message}`);
  }
}
__name(analyze, "analyze");
__name6(analyze, "analyze");
async function evaluatePolicy(client, envelope, request) {
  const idempotentEnvelope = ensureIdempotentRequestId(envelope);
  try {
    const response = await client.getHttpClient().post("v1/policy/evaluate", {
      json: {
        ...request,
        envelope: idempotentEnvelope
      },
      timeout: 3e4
    }).json();
    return response;
  } catch (error) {
    logger.error("Policy evaluation failed", {
      error
    });
    throw new Error(`Policy evaluation failed: ${error.message}`);
  }
}
__name(evaluatePolicy, "evaluatePolicy");
__name6(evaluatePolicy, "evaluatePolicy");
async function ingestTelemetry(client, envelope, data) {
  const idempotentEnvelope = ensureIdempotentRequestId(envelope);
  try {
    const response = await client.getHttpClient().post("v1/telemetry", {
      json: {
        ...data,
        envelope: idempotentEnvelope
      },
      timeout: 3e4
    }).json();
    return response;
  } catch (error) {
    logger.error("Telemetry ingestion failed", {
      error
    });
    throw new Error(`Telemetry ingestion failed: ${error.message}`);
  }
}
__name(ingestTelemetry, "ingestTelemetry");
__name6(ingestTelemetry, "ingestTelemetry");
function ensureIdempotentRequestId(envelope) {
  if (!envelope.request_id) {
    return {
      ...envelope,
      request_id: generateRequestId()
    };
  }
  return envelope;
}
__name(ensureIdempotentRequestId, "ensureIdempotentRequestId");
__name6(ensureIdempotentRequestId, "ensureIdempotentRequestId");
function generateRequestId() {
  return `${Date.now()}-${crypto2__default__default.randomUUID().substring(0, 10)}`;
}
__name(generateRequestId, "generateRequestId");
__name6(generateRequestId, "generateRequestId");
(class {
  static {
    __name(this, "PrivacyValidator");
  }
  static {
    __name6(this, "PrivacyValidator");
  }
  /**
  * Check that payload contains only metadata
  */
  isMetadataOnly(data) {
    const forbidden = [
      "content",
      "sourceCode",
      "fileContent",
      "code",
      "text",
      "body",
      "filePath",
      "fullPath",
      "absolutePath"
    ];
    const props = this.getAllProps(data);
    for (const prop of props) {
      const propName = prop.split(".").pop() || prop;
      if (forbidden.includes(propName)) {
        return false;
      }
    }
    const strings = this.getAllStrings(data);
    for (const str of strings) {
      if (str.length > 1e3) {
        return false;
      }
      if (this.looksLikeCode(str)) {
        return false;
      }
    }
    return true;
  }
  /**
  * Get all property names recursively
  */
  getAllProps(obj, prefix = "") {
    let props = [];
    for (const [key, value] of Object.entries(obj)) {
      const fullKey = prefix ? `${prefix}.${key}` : key;
      props.push(fullKey);
      if (typeof value === "object" && value !== null && !Array.isArray(value)) {
        props = props.concat(this.getAllProps(value, fullKey));
      }
    }
    return props;
  }
  /**
  * Get all string values recursively
  */
  getAllStrings(obj) {
    let strings = [];
    for (const value of Object.values(obj)) {
      if (typeof value === "string") {
        strings.push(value);
      } else if (typeof value === "object" && value !== null) {
        strings = strings.concat(this.getAllStrings(value));
      }
    }
    return strings;
  }
  /**
  * Heuristic to detect code-like strings
  */
  looksLikeCode(str) {
    const codePatterns = [
      /function\s+\w+/,
      /const\s+\w+\s*=/,
      /let\s+\w+\s*=/,
      /var\s+\w+\s*=/,
      /class\s+\w+/,
      /import\s+.*from/,
      /export\s+(default\s+)?/,
      /if\s*\(/,
      /for\s*\(/,
      /while\s*\(/
    ];
    return codePatterns.some((pattern) => pattern.test(str));
  }
});
var ProtectionManager = class {
  static {
    __name(this, "ProtectionManager");
  }
  static {
    __name6(this, "ProtectionManager");
  }
  registry = /* @__PURE__ */ new Map();
  config;
  constructor(config2) {
    this.config = config2;
  }
  protect(filePath, level, reason) {
    const protectedFile = {
      path: filePath,
      level,
      reason,
      addedAt: /* @__PURE__ */ new Date()
    };
    this.registry.set(filePath, protectedFile);
  }
  unprotect(filePath) {
    this.registry.delete(filePath);
  }
  getProtection(filePath) {
    const directProtection = this.registry.get(filePath);
    if (directProtection) {
      return directProtection;
    }
    if (!this.config.enabled) {
      return null;
    }
    for (const rule of this.config.patterns) {
      if (!rule.enabled) {
        continue;
      }
      if (minimatch(filePath, rule.pattern)) {
        return {
          path: filePath,
          level: rule.level,
          reason: rule.reason || `Matches pattern: ${rule.pattern}`,
          addedAt: /* @__PURE__ */ new Date(),
          pattern: rule.pattern
        };
      }
    }
    return null;
  }
  isProtected(filePath) {
    return this.getProtection(filePath) !== null;
  }
  getLevel(filePath) {
    const protection = this.getProtection(filePath);
    return protection ? protection.level : null;
  }
  listProtected() {
    return Array.from(this.registry.values());
  }
  updateLevel(filePath, level) {
    const existing = this.registry.get(filePath);
    if (!existing) {
      throw new Error(`File ${filePath} is not protected`);
    }
    this.registry.set(filePath, {
      ...existing,
      level
    });
  }
  getConfig() {
    return {
      ...this.config
    };
  }
  updateConfig(config2) {
    this.config = {
      ...this.config,
      ...config2
    };
  }
};
var SecurityError = class extends Error {
  static {
    __name(this, "SecurityError");
  }
  static {
    __name6(this, "SecurityError");
  }
  constructor(message) {
    super(message);
    this.name = "SecurityError";
  }
};
function validatePath(filePath) {
  if (filePath.includes("\0")) {
    throw new SecurityError("Null bytes in path not allowed");
  }
  const normalized = path10__default.normalize(filePath);
  if (path10__default.isAbsolute(normalized)) {
    throw new SecurityError("Absolute paths not allowed");
  }
  const segments = normalized.split(path10__default.sep);
  if (segments.some((seg) => seg === "..")) {
    throw new SecurityError("Path traversal not allowed");
  }
}
__name(validatePath, "validatePath");
__name6(validatePath, "validatePath");
function sanitizeForJSON(obj) {
  const str = JSON.stringify(obj);
  return JSON.parse(str);
}
__name(sanitizeForJSON, "sanitizeForJSON");
__name6(sanitizeForJSON, "sanitizeForJSON");
var SnapshotDeduplication = class {
  static {
    __name(this, "SnapshotDeduplication");
  }
  static {
    __name6(this, "SnapshotDeduplication");
  }
  cacheSize;
  hashCache;
  constructor(cacheSize = THRESHOLDS.resources.dedupCacheSize) {
    this.cacheSize = cacheSize;
    this.hashCache = new QuickLRU({
      maxSize: this.cacheSize
    });
  }
  /**
  * Hash file content using SHA-256
  */
  hashContent(content) {
    return createHash("sha256").update(content).digest("hex");
  }
  /**
  * Hash multiple files content
  */
  hashFiles(files) {
    const sorted = [
      ...files
    ].sort((a, b) => a.path.localeCompare(b.path));
    const content = sorted.map((f) => `${f.path}:${f.action}:${f.content}`).join("|");
    return this.hashContent(content);
  }
  /**
  * Check if content is already stored (deduplication)
  */
  async isDuplicate(files, storage) {
    const hash = this.hashFiles(files);
    const cachedId = this.hashCache.get(hash);
    if (cachedId) {
      return {
        isDuplicate: true,
        existingId: cachedId
      };
    }
    if (this.supportsHashLookup(storage)) {
      const existing = await this.getByContentHash(storage, hash);
      if (existing) {
        this.hashCache.set(hash, existing.id);
        return {
          isDuplicate: true,
          existingId: existing.id
        };
      }
    } else {
      const allSnapshots = await storage.list();
      for (const snapshot of allSnapshots) {
        if (snapshot.files?.length === files.length) {
          const isMatch = files.every((file) => snapshot.fileContents?.[file.path] === file.content);
          if (isMatch) {
            this.hashCache.set(hash, snapshot.id);
            return {
              isDuplicate: true,
              existingId: snapshot.id
            };
          }
        }
      }
    }
    return {
      isDuplicate: false
    };
  }
  /**
  * Check if storage adapter supports hash-based lookup
  */
  supportsHashLookup(storage) {
    return "getByContentHash" in storage;
  }
  /**
  * Get snapshot by content hash (if supported by storage)
  */
  async getByContentHash(storage, hash) {
    if (storage.getByContentHash) {
      return await storage.getByContentHash(hash);
    }
    return null;
  }
  /**
  * Record hash for future deduplication checks
  */
  recordHash(snapshotId, files) {
    const hash = this.hashFiles(files);
    this.hashCache.set(hash, snapshotId);
  }
  /**
  * Clear hash from cache for a deleted snapshot
  * This prevents the dedup cache from referencing non-existent snapshots
  */
  clearHash(snapshotId) {
    for (const [hash, cachedId] of this.hashCache.entries()) {
      if (cachedId === snapshotId) {
        this.hashCache.delete(hash);
      }
    }
  }
};
var SnapshotNaming = class {
  static {
    __name(this, "SnapshotNaming");
  }
  static {
    __name6(this, "SnapshotNaming");
  }
  /**
  * Generate snapshot name based on naming strategy
  */
  generateName(files, strategy = "semantic") {
    switch (strategy) {
      case "git":
        return this.gitStrategy(files);
      case "semantic":
        return this.semanticStrategy(files);
      case "timestamp":
        return this.timestampStrategy();
      default:
        return this.semanticStrategy(files);
    }
  }
  /**
  * Git strategy - would parse git commit message (simplified implementation)
  */
  gitStrategy(files) {
    return this.semanticStrategy(files);
  }
  /**
  * Semantic strategy - analyze file operations
  */
  semanticStrategy(files) {
    if (files.length === 1) {
      const file = files[0];
      const fileName = file.path.split("/").pop() || "file";
      return `${this.capitalize(file.action)} ${fileName}`;
    }
    const actionCounts = files.reduce((acc, f) => {
      acc[f.action] = (acc[f.action] || 0) + 1;
      return acc;
    }, {});
    const primaryAction = Object.entries(actionCounts).sort((a, b) => b[1] - a[1])[0];
    if (primaryAction) {
      return `${this.capitalize(String(primaryAction[0]))} ${files.length} files`;
    }
    return `Snapshot of ${files.length} files`;
  }
  /**
  * Timestamp strategy - use current timestamp
  */
  timestampStrategy() {
    return `Snapshot ${/* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString()}`;
  }
  /**
  * Capitalize first letter of string
  */
  capitalize(str) {
    return str.charAt(0).toUpperCase() + str.slice(1);
  }
};
var SnapshotManager = class {
  static {
    __name(this, "SnapshotManager");
  }
  static {
    __name6(this, "SnapshotManager");
  }
  storage;
  options;
  deduplication;
  naming;
  constructor(storage, options = {}) {
    this.storage = storage;
    this.options = options;
    this.deduplication = new SnapshotDeduplication();
    this.naming = new SnapshotNaming();
  }
  async create(files, options) {
    return this.createSnapshot(files, options);
  }
  /**
  * Convenience method for creating a snapshot with a single file
  * @param params - Object containing filePath and content
  * @param options - Snapshot options
  * @returns Created snapshot
  */
  async createTest(params, options) {
    const fileInput = {
      path: params.filePath,
      content: params.content,
      action: "modify"
    };
    return this.createSnapshot([
      fileInput
    ], options);
  }
  async createSnapshot(files, options) {
    await Promise.all(files.map((file) => Promise.resolve(validatePath(file.path))));
    if (this.options.enableDeduplication) {
      const { isDuplicate, existingId } = await this.deduplication.isDuplicate(files, this.storage);
      if (isDuplicate && existingId) {
        throw new Error("Duplicate snapshot detected");
      }
    }
    const name = options?.description || this.naming.generateName(files, this.options.namingStrategy);
    const snapshot = {
      id: randomUUID(),
      timestamp: Date.now(),
      version: "1.0",
      meta: {
        name,
        protected: options?.protected || this.options.autoProtect || false
      },
      files: files.map((f) => f.path),
      fileContents: files.reduce((acc, f) => {
        acc[f.path] = f.content;
        return acc;
      }, {})
    };
    const contentHash = this.options.enableDeduplication ? this.deduplication.hashFiles(files) : void 0;
    await this.storage.save(snapshot, contentHash);
    if (this.options.enableDeduplication && contentHash) {
      this.deduplication.recordHash(snapshot.id, files);
    }
    return snapshot;
  }
  async list(filters) {
    return this.storage.list(filters);
  }
  async get(id) {
    return this.storage.get(id);
  }
  async delete(id) {
    const snapshot = await this.storage.get(id);
    if (!snapshot) {
      throw new Error(`Snapshot ${id} not found`);
    }
    if (snapshot.meta?.protected) {
      throw new Error(`Cannot delete protected snapshot ${id}`);
    }
    await this.storage.delete(id);
    if (this.options.enableDeduplication) {
      this.deduplication.clearHash(id);
    }
  }
  /**
  * Restore snapshot to target directory with atomic guarantees
  * @param id Snapshot ID to restore
  * @param targetPath Target directory path (optional, for actual file system restore)
  * @param options Restore options
  * @returns Restore result with list of restored files and any errors
  */
  async restore(id, targetPath, options) {
    const snapshot = await this.storage.get(id);
    if (!snapshot) {
      throw new Error(`Snapshot ${id} not found`);
    }
    if (!targetPath) {
      return {
        success: true,
        restoredFiles: snapshot.files || [],
        errors: []
      };
    }
    if (options?.dryRun) {
      const errors = [];
      const fileContents = snapshot.fileContents || {};
      for (const filePath of snapshot.files || []) {
        if (!fileContents[filePath]) {
          errors.push(`Missing content for file: ${filePath}`);
        }
      }
      return {
        success: errors.length === 0,
        restoredFiles: snapshot.files || [],
        errors
      };
    }
    return await this.restoreAtomic(snapshot, targetPath, options);
  }
  /**
  * Atomic restore implementation with staging and rollback
  */
  async restoreAtomic(snapshot, targetPath, options) {
    const errors = [];
    const restoredFiles = [];
    const stagingDir = `${targetPath}.staging`;
    const backupDir = `${targetPath}.backup`;
    let targetBackedUp = false;
    try {
      const fileContents = snapshot.fileContents || {};
      const files = snapshot.files || [];
      const totalFiles = files.length;
      if (existsSync(stagingDir)) {
        await fs3.rm(stagingDir, {
          recursive: true,
          force: true
        });
      }
      await fs3.mkdir(stagingDir, {
        recursive: true
      });
      for (let i = 0; i < totalFiles; i++) {
        const filePath = files[i];
        const content = fileContents[filePath];
        if (!content) {
          throw new Error(`Missing content for file: ${filePath}`);
        }
        const targetFilePath = path10.join(stagingDir, filePath);
        await fs3.mkdir(path10.dirname(targetFilePath), {
          recursive: true
        });
        await fs3.writeFile(targetFilePath, content, "utf-8");
        restoredFiles.push(filePath);
        if (options?.onProgress) {
          const progress = Math.round((i + 1) / totalFiles * 90);
          options.onProgress(progress);
        }
      }
      if (existsSync(targetPath)) {
        if (existsSync(backupDir)) {
          await fs3.rm(backupDir, {
            recursive: true,
            force: true
          });
        }
        await fs3.rename(targetPath, backupDir);
        targetBackedUp = true;
      }
      if (options?.onProgress) {
        options.onProgress(95);
      }
      await fs3.rename(stagingDir, targetPath);
      if (options?.onProgress) {
        options.onProgress(100);
      }
      if (existsSync(backupDir)) {
        await fs3.rm(backupDir, {
          recursive: true,
          force: true
        });
      }
      return {
        success: true,
        restoredFiles,
        errors: []
      };
    } catch (error) {
      if (targetBackedUp && existsSync(backupDir)) {
        try {
          if (existsSync(targetPath)) {
            await fs3.rm(targetPath, {
              recursive: true,
              force: true
            });
          }
          await fs3.rename(backupDir, targetPath);
        } catch (rollbackError) {
          errors.push(`Rollback failed: ${rollbackError instanceof Error ? rollbackError.message : String(rollbackError)}`);
        }
      }
      if (existsSync(stagingDir)) {
        try {
          await fs3.rm(stagingDir, {
            recursive: true,
            force: true
          });
        } catch {
        }
      }
      const errorMessage2 = error instanceof Error ? error.message : String(error);
      errors.push(errorMessage2);
      return {
        success: false,
        restoredFiles,
        errors
      };
    }
  }
  async protect(id) {
    const snapshot = await this.storage.get(id);
    if (!snapshot) {
      throw new Error(`Snapshot ${id} not found`);
    }
    const updated = {
      ...snapshot,
      meta: {
        ...snapshot.meta,
        protected: true
      }
    };
    const contentHash = this.options.enableDeduplication && "getStoredContentHash" in this.storage ? await this.storage.getStoredContentHash(id) : void 0;
    await this.storage.save(updated, contentHash);
  }
  async unprotect(id) {
    const snapshot = await this.storage.get(id);
    if (!snapshot) {
      throw new Error(`Snapshot ${id} not found`);
    }
    const updated = {
      ...snapshot,
      meta: {
        ...snapshot.meta,
        protected: false
      }
    };
    const contentHash = this.options.enableDeduplication && "getStoredContentHash" in this.storage ? await this.storage.getStoredContentHash(id) : void 0;
    await this.storage.save(updated, contentHash);
  }
  async search(criteria) {
    if (this.supportsOptimizedSearch(this.storage)) {
      return await this.optimizedSearch(this.storage, criteria);
    }
    const all = await this.storage.list();
    return all.filter((snapshot) => {
      if (criteria.content) {
        const hasContent = Object.values(snapshot.fileContents || {}).some((content) => content != null && criteria.content && String(content).includes(criteria.content));
        if (!hasContent) {
          return false;
        }
      }
      if (criteria.message) {
        snapshot.meta?.name || "";
        if (criteria.message) {
          const name = snapshot.meta?.name || "";
          if (criteria.message && typeof name === "string" && !name.includes(criteria.message)) {
            return false;
          }
        }
      }
      return true;
    });
  }
  /**
  * Check if storage adapter supports optimized search
  */
  supportsOptimizedSearch(storage) {
    return "search" in storage && typeof storage.search === "function";
  }
  /**
  * Perform optimized search using storage adapter capabilities
  */
  async optimizedSearch(storage, criteria) {
    const all = await storage.list();
    return all.filter((snapshot) => {
      if (criteria.content) {
        const hasContent = Object.values(snapshot.fileContents || {}).some((content) => content != null && String(content).includes(criteria.content ?? ""));
        if (!hasContent) {
          return false;
        }
      }
      if (criteria.message) {
        const name = snapshot.meta?.name || "";
        if (criteria.message && typeof name === "string" && !name.includes(criteria.message)) {
          return false;
        }
      }
      return true;
    });
  }
};
var StorageError2 = class extends Error {
  static {
    __name(this, "StorageError2");
  }
  static {
    __name6(this, "StorageError");
  }
  code;
  details;
  constructor(message, code, details) {
    super(message), this.code = code, this.details = details;
    this.name = "StorageError";
  }
};
var StorageConnectionError = class extends StorageError2 {
  static {
    __name(this, "StorageConnectionError");
  }
  static {
    __name6(this, "StorageConnectionError");
  }
  constructor(message, details) {
    super(message, "STORAGE_CONNECTION_ERROR", details);
    this.name = "StorageConnectionError";
  }
};
var StorageTransactionError = class extends StorageError2 {
  static {
    __name(this, "StorageTransactionError");
  }
  static {
    __name6(this, "StorageTransactionError");
  }
  constructor(message, details) {
    super(message, "STORAGE_TRANSACTION_ERROR", details);
    this.name = "StorageTransactionError";
  }
};
var StorageFullError2 = class extends StorageError2 {
  static {
    __name(this, "StorageFullError2");
  }
  static {
    __name6(this, "StorageFullError");
  }
  constructor(message, details) {
    super(message, "STORAGE_FULL_ERROR", details);
    this.name = "StorageFullError";
  }
};
var StorageLockError2 = class extends StorageError2 {
  static {
    __name(this, "StorageLockError2");
  }
  static {
    __name6(this, "StorageLockError");
  }
  constructor(message, details) {
    super(message, "STORAGE_LOCK_ERROR", details);
    this.name = "StorageLockError";
  }
};
var CorruptedDataError = class extends StorageError2 {
  static {
    __name(this, "CorruptedDataError");
  }
  static {
    __name6(this, "CorruptedDataError");
  }
  constructor(message, details) {
    super(message, "CORRUPTED_DATA_ERROR", details);
    this.name = "CorruptedDataError";
  }
};
var DatabaseConstructor = null;
var loadError = null;
async function loadBetterSqlite3() {
  if (DatabaseConstructor) {
    return DatabaseConstructor;
  }
  if (loadError) {
    throw loadError;
  }
  try {
    const module = await import('better-sqlite3');
    DatabaseConstructor = module.default;
    return DatabaseConstructor;
  } catch (error) {
    loadError = error instanceof Error ? error : new Error(String(error));
    throw new StorageConnectionError("Failed to load better-sqlite3", {
      cause: loadError
    });
  }
}
__name(loadBetterSqlite3, "loadBetterSqlite3");
__name6(loadBetterSqlite3, "loadBetterSqlite3");
var LocalStorage = class {
  static {
    __name(this, "LocalStorage");
  }
  static {
    __name6(this, "LocalStorage");
  }
  db = null;
  dbPath;
  constructor(dbPath) {
    this.dbPath = dbPath;
  }
  async ensureInitialized() {
    if (this.db) {
      return;
    }
    const DB = await loadBetterSqlite3();
    this.db = new DB(this.dbPath);
    this.initSchema();
  }
  initSchema() {
    if (!this.db) {
      throw new StorageError2("Database not initialized", "DB_NOT_INITIALIZED");
    }
    this.db.exec(`
      CREATE TABLE IF NOT EXISTS snapshots (
        id TEXT PRIMARY KEY,
        timestamp INTEGER NOT NULL,
        name TEXT,
        protected INTEGER DEFAULT 0,
        files TEXT,
        file_contents TEXT,
        metadata TEXT,
        content_hash TEXT,
        created_at INTEGER DEFAULT (strftime('%s', 'now'))
      );

      CREATE INDEX IF NOT EXISTS idx_timestamp ON snapshots(timestamp DESC);
      CREATE INDEX IF NOT EXISTS idx_protected ON snapshots(protected);
      CREATE INDEX IF NOT EXISTS idx_created_at ON snapshots(created_at DESC);
      CREATE INDEX IF NOT EXISTS idx_name ON snapshots(name);
      CREATE INDEX IF NOT EXISTS idx_content_hash ON snapshots(content_hash);
    `);
  }
  async save(snapshot, contentHash) {
    await this.ensureInitialized();
    try {
      const sanitizedFiles = sanitizeForJSON(snapshot.files || []);
      const sanitizedFileContents = sanitizeForJSON(snapshot.fileContents || {});
      const sanitizedMeta = sanitizeForJSON(snapshot.meta || {});
      const stmt = this.db?.prepare(`
        INSERT OR REPLACE INTO snapshots (
          id, timestamp, name, protected, files, file_contents, metadata, content_hash
        ) VALUES (?, ?, ?, ?, ?, ?, ?, ?)
      `);
      if (!stmt) {
        throw new StorageError2("Failed to prepare save statement", "DB_PREPARE_ERROR");
      }
      stmt.run(snapshot.id, snapshot.timestamp, snapshot.meta?.name || null, snapshot.meta?.protected ? 1 : 0, JSON.stringify(sanitizedFiles), JSON.stringify(sanitizedFileContents), JSON.stringify(sanitizedMeta), contentHash || null);
    } catch (error) {
      const sqliteError = error;
      if (sqliteError.code === "SQLITE_BUSY") {
        throw new StorageLockError2("Database locked", {
          retryable: true,
          cause: error
        });
      }
      if (sqliteError.code === "SQLITE_FULL") {
        throw new StorageFullError2("Disk full", {
          retryable: false,
          cause: error
        });
      }
      if (sqliteError.code === "SQLITE_CANTOPEN") {
        throw new StorageConnectionError("Cannot open database", {
          cause: error
        });
      }
      throw new StorageError2("Save failed", sqliteError.code, {
        cause: error
      });
    }
  }
  async get(id) {
    await this.ensureInitialized();
    try {
      const stmt = this.db?.prepare("SELECT * FROM snapshots WHERE id = ?");
      if (!stmt) {
        throw new StorageError2("Failed to prepare get statement", "DB_PREPARE_ERROR");
      }
      const row = stmt.get(id);
      if (!row) {
        return null;
      }
      return this.deserializeSnapshot(row);
    } catch (error) {
      const sqliteError = error;
      if (sqliteError.code === "SQLITE_BUSY") {
        throw new StorageLockError2("Database locked", {
          retryable: true,
          cause: error
        });
      }
      if (sqliteError.code === "SQLITE_CANTOPEN") {
        throw new StorageConnectionError("Cannot open database", {
          cause: error
        });
      }
      throw new StorageError2("Get failed", sqliteError.code, {
        cause: error
      });
    }
  }
  async getByContentHash(hash) {
    await this.ensureInitialized();
    try {
      const stmt = this.db?.prepare("SELECT * FROM snapshots WHERE content_hash = ? LIMIT 1");
      if (!stmt) {
        throw new StorageError2("Failed to prepare getByContentHash statement", "DB_PREPARE_ERROR");
      }
      const row = stmt.get(hash);
      if (!row) {
        return null;
      }
      return this.deserializeSnapshot(row);
    } catch (error) {
      const sqliteError = error;
      if (sqliteError.code === "SQLITE_BUSY") {
        throw new StorageLockError2("Database locked", {
          retryable: true,
          cause: error
        });
      }
      if (sqliteError.code === "SQLITE_CANTOPEN") {
        throw new StorageConnectionError("Cannot open database", {
          cause: error
        });
      }
      throw new StorageError2("GetByContentHash failed", sqliteError.code, {
        cause: error
      });
    }
  }
  /**
  * Get the stored content hash for a snapshot (for preserving hash on updates)
  */
  async getStoredContentHash(id) {
    await this.ensureInitialized();
    try {
      const stmt = this.db?.prepare("SELECT content_hash FROM snapshots WHERE id = ?");
      if (!stmt) {
        throw new StorageError2("Failed to prepare getStoredContentHash statement", "DB_PREPARE_ERROR");
      }
      const row = stmt.get(id);
      return row?.content_hash ?? null;
    } catch (error) {
      const sqliteError = error;
      if (sqliteError.code === "SQLITE_BUSY") {
        throw new StorageLockError2("Database locked", {
          retryable: true,
          cause: error
        });
      }
      if (sqliteError.code === "SQLITE_CANTOPEN") {
        throw new StorageConnectionError("Cannot open database", {
          cause: error
        });
      }
      throw new StorageError2("GetStoredContentHash failed", sqliteError.code, {
        cause: error
      });
    }
  }
  async list(filters) {
    await this.ensureInitialized();
    try {
      let query = "SELECT * FROM snapshots WHERE 1=1";
      const params = [];
      if (filters?.protected !== void 0) {
        query += " AND protected = ?";
        params.push(filters.protected ? 1 : 0);
      }
      if (filters?.after) {
        query += " AND timestamp >= ?";
        params.push(filters.after.getTime());
      }
      if (filters?.before) {
        query += " AND timestamp < ?";
        params.push(filters.before.getTime());
      }
      query += " ORDER BY timestamp DESC";
      if (filters?.offset && !filters?.limit) {
        logger.warn("Using OFFSET without LIMIT can lead to inefficient queries. Consider setting a limit for better performance.");
      }
      if (filters?.limit) {
        query += " LIMIT ?";
        params.push(filters.limit);
      }
      if (filters?.offset) {
        query += " OFFSET ?";
        params.push(filters.offset);
      }
      const stmt = this.db?.prepare(query);
      if (!stmt) {
        throw new StorageError2("Failed to prepare list statement", "DB_PREPARE_ERROR");
      }
      const rows = stmt.all(...params);
      let snapshots = rows.map((row) => this.deserializeSnapshot(row));
      if (filters?.filePath) {
        snapshots = snapshots.filter((snapshot) => snapshot.files?.includes(filters.filePath ?? ""));
      }
      return snapshots;
    } catch (error) {
      const sqliteError = error;
      if (sqliteError.code === "SQLITE_BUSY") {
        throw new StorageLockError2("Database locked", {
          retryable: true,
          cause: error
        });
      }
      if (sqliteError.code === "SQLITE_CANTOPEN") {
        throw new StorageConnectionError("Cannot open database", {
          cause: error
        });
      }
      throw new StorageError2("List failed", sqliteError.code, {
        cause: error
      });
    }
  }
  async delete(id) {
    await this.ensureInitialized();
    try {
      const stmt = this.db?.prepare("DELETE FROM snapshots WHERE id = ?");
      if (!stmt) {
        throw new StorageError2("Failed to prepare delete statement", "DB_PREPARE_ERROR");
      }
      stmt.run(id);
    } catch (error) {
      const sqliteError = error;
      if (sqliteError.code === "SQLITE_BUSY") {
        throw new StorageLockError2("Database locked", {
          retryable: true,
          cause: error
        });
      }
      if (sqliteError.code === "SQLITE_CANTOPEN") {
        throw new StorageConnectionError("Cannot open database", {
          cause: error
        });
      }
      throw new StorageError2("Delete failed", sqliteError.code, {
        cause: error
      });
    }
  }
  async close() {
    if (!this.db) {
      return;
    }
    try {
      this.db.close();
      this.db = null;
    } catch (error) {
      const sqliteError = error;
      if (sqliteError.code === "SQLITE_BUSY") {
        throw new StorageLockError2("Database locked", {
          retryable: true,
          cause: error
        });
      }
      throw new StorageError2("Close failed", sqliteError.code, {
        cause: error
      });
    }
  }
  deserializeSnapshot(row) {
    const r = row;
    try {
      return {
        id: r.id,
        timestamp: r.timestamp,
        version: r.version || "1.0",
        meta: JSON.parse(r.metadata || "{}"),
        files: JSON.parse(r.files || "[]"),
        fileContents: JSON.parse(r.file_contents || "{}")
      };
    } catch (error) {
      throw new CorruptedDataError(`Failed to deserialize snapshot ${r.id}`, {
        cause: error,
        snapshotId: r.id
      });
    }
  }
};
var MemoryStorage = class {
  static {
    __name(this, "MemoryStorage");
  }
  static {
    __name6(this, "MemoryStorage");
  }
  snapshots = /* @__PURE__ */ new Map();
  async save(snapshot, _contentHash) {
    this.snapshots.set(snapshot.id, this.cloneSnapshot(snapshot));
  }
  async get(id) {
    const snapshot = this.snapshots.get(id);
    return snapshot ? this.cloneSnapshot(snapshot) : null;
  }
  async list(filters) {
    let snapshots = Array.from(this.snapshots.values());
    if (filters?.filePath) {
      snapshots = snapshots.filter((s) => s.files?.some((f) => f === filters.filePath || filters.filePath && f.includes(filters.filePath)));
    }
    if (filters?.before) {
      const beforeMs = filters.before.getTime();
      snapshots = snapshots.filter((s) => s.timestamp < beforeMs);
    }
    if (filters?.after) {
      const afterMs = filters.after.getTime();
      snapshots = snapshots.filter((s) => s.timestamp >= afterMs);
    }
    if (filters?.protected !== void 0) {
      snapshots = snapshots.filter((s) => (s.meta?.protected ?? false) === filters.protected);
    }
    snapshots.sort((a, b) => b.timestamp - a.timestamp);
    if (filters?.limit) {
      snapshots = snapshots.slice(0, filters.limit);
    }
    return snapshots.map((s) => this.cloneSnapshot(s));
  }
  async delete(id) {
    this.snapshots.delete(id);
  }
  async close() {
    this.snapshots.clear();
  }
  /**
  * Deep clone snapshot while preserving type information
  */
  cloneSnapshot(snapshot) {
    if (typeof structuredClone !== "undefined") {
      return structuredClone(snapshot);
    }
    return structuredClone(snapshot);
  }
  /**
  * Get total count of snapshots (for testing)
  */
  get size() {
    return this.snapshots.size;
  }
};
(class {
  static {
    __name(this, "Snapback");
  }
  static {
    __name6(this, "Snapback");
  }
  storage;
  snapshotManager;
  protectionManager;
  cloudClient;
  analyticsClient;
  constructor(options) {
    if (typeof options.storage === "string") {
      if (options.storage === ":memory:") {
        this.storage = new MemoryStorage();
      } else {
        this.storage = new LocalStorage(options.storage);
      }
    } else if (options.storage) {
      this.storage = options.storage;
    } else {
      this.storage = new MemoryStorage();
    }
    const protectionConfig = {
      patterns: options.protection?.patterns || [],
      defaultLevel: options.protection?.defaultLevel || "watch",
      enabled: options.protection?.enabled !== false,
      autoProtectConfigs: options.autoProtectConfigs !== false
    };
    this.protectionManager = new ProtectionManager(protectionConfig);
    this.snapshotManager = new SnapshotManager(this.storage, {
      enableDeduplication: options.enableDeduplication,
      autoProtect: options.autoProtectConfigs
    });
    if (options.cloud) {
      const sdkConfig = {
        endpoint: options.cloud.baseUrl,
        apiKey: options.cloud.apiKey,
        privacy: {
          hashFilePaths: true,
          anonymizeWorkspace: false
        },
        cache: {
          enabled: true,
          ttl: {
            analytics: 3600
          }
        },
        retry: {
          maxRetries: options.cloud.retries || 3,
          backoffMs: options.cloud.timeout || 1e3
        }
      };
      this.cloudClient = new SnapbackClient(sdkConfig);
      this.analyticsClient = new SnapbackClient(sdkConfig);
    }
  }
  /**
  * Create a snapshot of files
  * @param files - Files to snapshot
  * @param options - Snapshot options
  * @returns Created snapshot
  */
  async createSnapshot(files, options) {
    const fileInputs = files.map((file) => ({
      path: file.path,
      content: file.content,
      action: file.action || "modify"
    }));
    return this.snapshotManager.create(fileInputs, options);
  }
  /**
  * Save a single file snapshot
  * @param path - File path
  * @param content - File content
  * @param description - Optional description
  * @returns Created snapshot
  */
  async save(path11, content, description) {
    return this.snapshotManager.create([
      {
        path: path11,
        content,
        action: "modify"
      }
    ], {
      description
    });
  }
  /**
  * List snapshots with optional filters
  * @param filters - Filter options
  * @returns Array of snapshots
  */
  async listSnapshots(filters) {
    return this.snapshotManager.list(filters);
  }
  /**
  * Get a specific snapshot by ID
  * @param id - Snapshot ID
  * @returns Snapshot or null if not found
  */
  async getSnapshot(id) {
    return this.snapshotManager.get(id);
  }
  /**
  * Delete a snapshot by ID
  * @param id - Snapshot ID
  */
  async deleteSnapshot(id) {
    return this.snapshotManager.delete(id);
  }
  /**
  * Restore a snapshot
  * @param id - Snapshot ID
  * @returns Restore result
  */
  async restoreSnapshot(id) {
    return this.snapshotManager.restore(id);
  }
  /**
  * Protect a snapshot
  * @param id - Snapshot ID
  */
  async protectSnapshot(id) {
    return this.snapshotManager.protect(id);
  }
  /**
  * Unprotect a snapshot
  * @param id - Snapshot ID
  */
  async unprotectSnapshot(id) {
    return this.snapshotManager.unprotect(id);
  }
  /**
  * Protect a file path
  * @param filePath - File path to protect
  * @param level - Protection level
  * @param reason - Optional reason
  */
  protectFile(filePath, level, reason) {
    this.protectionManager.protect(filePath, level, reason);
  }
  /**
  * Check if a file is protected
  * @param filePath - File path to check
  * @returns Protection level or null if not protected
  */
  getProtectionLevel(filePath) {
    return this.protectionManager.getLevel(filePath);
  }
  /**
  * Close the storage connection
  */
  async close() {
    if ("close" in this.storage && typeof this.storage.close === "function") {
      await this.storage.close();
    }
  }
  /**
  * Get the underlying snapshot manager for advanced operations
  */
  get snapshots() {
    return this.snapshotManager;
  }
  /**
  * Get the underlying protection manager for advanced operations
  */
  get protection() {
    return this.protectionManager;
  }
  /**
  * Get the cloud client if configured
  */
  get cloud() {
    return this.cloudClient;
  }
  /**
  * Get the analytics client if configured
  */
  get analytics() {
    return this.analyticsClient;
  }
});
(class {
  static {
    __name(this, "SessionDeduplication");
  }
  static {
    __name6(this, "SessionDeduplication");
  }
  timeDeltaMs;
  minFilesForDedup;
  fingerprintCache;
  constructor(options = {}) {
    this.timeDeltaMs = options.timeDeltaMs ?? 5 * 60 * 1e3;
    this.minFilesForDedup = options.minFilesForDedup ?? 5;
    this.fingerprintCache = new QuickLRU({
      maxSize: options.cacheSize ?? 100
    });
  }
  /**
  * Compute session fingerprint from file changes
  */
  computeFingerprint(changes) {
    if (changes.length === 0) {
      return "";
    }
    const items = changes.map((change) => {
      const path11 = change.p;
      const op = change.op;
      const hNew = change.hNew || "";
      const hOld = change.hOld || "";
      return `${path11}:${op}:${hOld}:${hNew}`;
    });
    items.sort();
    const hash = crypto2__default.createHash("sha256");
    for (const item of items) {
      hash.update(item);
    }
    return hash.digest("hex");
  }
  /**
  * Check if session is a duplicate
  */
  checkDuplicate(changes, timestamp) {
    if (changes.length < this.minFilesForDedup) {
      const fingerprint2 = this.computeFingerprint(changes);
      return {
        isDuplicate: false,
        reason: "Session too small for deduplication",
        fingerprint: fingerprint2
      };
    }
    const fingerprint = this.computeFingerprint(changes);
    const existing = this.fingerprintCache.get(fingerprint);
    if (!existing) {
      return {
        isDuplicate: false,
        fingerprint
      };
    }
    const timeDelta = timestamp - existing.timestamp;
    if (timeDelta > this.timeDeltaMs) {
      return {
        isDuplicate: false,
        reason: `Time delta ${timeDelta}ms exceeds threshold ${this.timeDeltaMs}ms`,
        fingerprint
      };
    }
    return {
      isDuplicate: true,
      reason: `Duplicate of session ${existing.sessionId} (time delta: ${timeDelta}ms)`,
      existingSessionId: existing.sessionId,
      fingerprint
    };
  }
  /**
  * Register a session fingerprint
  */
  register(sessionId, changes, timestamp) {
    const fingerprint = this.computeFingerprint(changes);
    this.fingerprintCache.set(fingerprint, {
      fingerprint,
      timestamp,
      sessionId,
      changeCount: changes.length
    });
  }
  /**
  * Remove a session fingerprint
  */
  unregister(sessionId) {
    for (const [fingerprint, data] of this.fingerprintCache.entries()) {
      if (data.sessionId === sessionId) {
        this.fingerprintCache.delete(fingerprint);
      }
    }
  }
  /**
  * Clear all fingerprints
  */
  clear() {
    this.fingerprintCache.clear();
  }
  /**
  * Get cache statistics
  */
  getStats() {
    return {
      size: this.fingerprintCache.size,
      maxSize: this.fingerprintCache.maxSize
    };
  }
});
var LARGE_INSERT_THRESHOLD = {
  minChars: 100,
  minLines: 5
};
var SimpleChangeTracker = class {
  static {
    __name(this, "SimpleChangeTracker");
  }
  static {
    __name6(this, "SimpleChangeTracker");
  }
  totalChars = 0;
  totalLines = 0;
  largeInsertCount = 0;
  multiLineInsertCount = 0;
  /**
  * Records a change event and updates aggregate metrics
  */
  recordChange(event) {
    this.totalChars += event.chars;
    this.totalLines += event.lines;
    if (event.isInsert && event.isMultiLine) {
      this.multiLineInsertCount++;
    }
    if (this.isLargeInsert(event)) {
      this.largeInsertCount++;
    }
  }
  /**
  * Determines if a change qualifies as a "large insert"
  */
  isLargeInsert(event) {
    if (!event.isInsert || !event.isMultiLine) {
      return false;
    }
    return event.chars >= LARGE_INSERT_THRESHOLD.minChars || event.lines >= LARGE_INSERT_THRESHOLD.minLines;
  }
  /**
  * Returns current aggregate metrics snapshot
  */
  snapshot() {
    return {
      totalChars: this.totalChars,
      totalLines: this.totalLines,
      largeInsertCount: this.largeInsertCount,
      multiLineInsertCount: this.multiLineInsertCount
    };
  }
  /**
  * Clears all accumulated metrics
  */
  reset() {
    this.totalChars = 0;
    this.totalLines = 0;
    this.largeInsertCount = 0;
    this.multiLineInsertCount = 0;
  }
};
var THRESHOLDS2 = {
  // Light: 1 large insert with modest total chars
  light: {
    maxTotalChars: 800
  },
  // Medium: 2+ large inserts OR higher volume
  medium: {
    minLargeInserts: 2,
    minTotalChars: 400
  },
  // Heavy: 3+ large inserts AND high total volume
  heavy: {
    minLargeInserts: 3,
    minTotalChars: 2e3
  },
  // Anti-paste guard: if only 1 large insert, cap at medium regardless of size
  maxLargeInsertsForSinglePaste: 1
};
var CONFIDENCE = {
  unknown: 0,
  noneWithNoProvider: 8.5,
  noneWithProvider: 7.5,
  inferenceOnlyCap: 7.5
};
var AiSessionTracker = class {
  static {
    __name(this, "AiSessionTracker");
  }
  static {
    __name6(this, "AiSessionTracker");
  }
  detectEnv;
  changeTracker;
  isEnabled;
  sessionId = null;
  constructor(detectEnv, changeTracker, isEnabled = true) {
    this.detectEnv = detectEnv;
    this.changeTracker = changeTracker;
    this.isEnabled = isEnabled;
  }
  /**
  * Starts a new session
  */
  startSession(sessionId) {
    this.sessionId = sessionId;
  }
  /**
  * Records a change event
  */
  recordChange(event) {
    if (!this.isEnabled) {
      return;
    }
    this.changeTracker.recordChange(event);
  }
  /**
  * Finalizes the session and returns classification result
  */
  finalizeSession() {
    if (!this.isEnabled) {
      return {
        level: "unknown",
        confidence: 0,
        provider: "none",
        reasoning: "AI detection disabled",
        metrics: this.changeTracker.snapshot()
      };
    }
    if (!this.sessionId) {
      return {
        level: "unknown",
        confidence: CONFIDENCE.unknown,
        provider: "none",
        reasoning: "No session started",
        metrics: this.changeTracker.snapshot()
      };
    }
    const provider = this.detectEnv();
    const metrics = this.changeTracker.snapshot();
    const level = this.classifyLevel(metrics, provider);
    const confidence = this.calculateConfidence(level, provider, metrics);
    const reasoning = this.generateReasoning(level, provider, metrics);
    return {
      level,
      confidence,
      provider: provider.provider,
      reasoning,
      metrics
    };
  }
  /**
  * Resets session state
  */
  reset() {
    this.sessionId = null;
    this.changeTracker.reset();
  }
  classifyLevel(metrics, _provider) {
    const { largeInsertCount, totalChars } = metrics;
    if (largeInsertCount === 0) {
      return "none";
    }
    if (largeInsertCount === THRESHOLDS2.maxLargeInsertsForSinglePaste) {
      if (totalChars <= THRESHOLDS2.light.maxTotalChars) {
        return "light";
      }
      return "medium";
    }
    if (largeInsertCount >= THRESHOLDS2.heavy.minLargeInserts && totalChars >= THRESHOLDS2.heavy.minTotalChars) {
      return "heavy";
    }
    if (largeInsertCount >= THRESHOLDS2.medium.minLargeInserts || totalChars >= THRESHOLDS2.medium.minTotalChars) {
      return "medium";
    }
    return "light";
  }
  /**
  * Calculates confidence score based on signals available
  */
  calculateConfidence(level, provider, _metrics) {
    if (level === "unknown") {
      return CONFIDENCE.unknown;
    }
    if (level === "none") {
      return provider.hasAI ? CONFIDENCE.noneWithProvider : CONFIDENCE.noneWithNoProvider;
    }
    if (provider.hasAI && provider.provider !== "unknown" && provider.provider !== "none") {
      return Math.min(provider.confidence, CONFIDENCE.inferenceOnlyCap);
    }
    return CONFIDENCE.inferenceOnlyCap;
  }
  /**
  * Generates human-readable reasoning
  */
  generateReasoning(level, provider, metrics) {
    if (level === "unknown") {
      return "No session started";
    }
    const providerName = provider.provider === "cursor" ? "Cursor" : provider.provider === "claude" ? "Claude" : null;
    if (level === "none") {
      if (providerName) {
        return `${providerName} detected but no large inserts observed`;
      }
      return "No AI provider detected, no large inserts";
    }
    const parts = [];
    if (providerName) {
      parts.push(`${providerName} detected`);
    } else if (provider.provider === "unknown") {
      parts.push("No AI provider detected");
    }
    const { largeInsertCount, totalChars } = metrics;
    if (largeInsertCount === 1) {
      parts.push("single large insert");
    } else {
      parts.push(`${largeInsertCount} large inserts`);
    }
    parts.push(`(${totalChars} chars total)`);
    if (level === "heavy") {
      parts.push("- heavy AI-like usage inferred from change patterns");
    } else if (level === "medium") {
      parts.push("- multiple large inserts inferred from change patterns");
    } else {
      parts.push("- inferred from change patterns");
    }
    return parts.join(", ");
  }
};
function makeSafeSessionStartedEvent(tier) {
  return {
    changeCount: 0,
    tier
  };
}
__name(makeSafeSessionStartedEvent, "makeSafeSessionStartedEvent");
__name6(makeSafeSessionStartedEvent, "makeSafeSessionStartedEvent");
function makeSafeSessionFinalizedEvent(changeCount, durationMs, tier, consent, changes) {
  const base = {
    changeCount,
    durationMs,
    tier
  };
  if (tier === "pro" && consent && changeCount >= 3 && changes) {
    const extCounts = computeExtensionHistogram(changes);
    if (Object.keys(extCounts).length > 0) {
      base.ext_counts = extCounts;
    }
  }
  return base;
}
__name(makeSafeSessionFinalizedEvent, "makeSafeSessionFinalizedEvent");
__name6(makeSafeSessionFinalizedEvent, "makeSafeSessionFinalizedEvent");
function computeExtensionHistogram(changes) {
  const counts = {};
  for (const change of changes) {
    const ext = getFileExtension(change.p);
    if (ext) {
      counts[ext] = (counts[ext] || 0) + 1;
    }
  }
  return counts;
}
__name(computeExtensionHistogram, "computeExtensionHistogram");
__name6(computeExtensionHistogram, "computeExtensionHistogram");
function getFileExtension(path11) {
  const lastDot = path11.lastIndexOf(".");
  const lastSlash = path11.lastIndexOf("/");
  if (lastDot === -1 || lastDot < lastSlash) {
    return null;
  }
  const ext = path11.slice(lastDot);
  if (ext.includes("-") || ext.length > 8) {
    return null;
  }
  return ext.toLowerCase();
}
__name(getFileExtension, "getFileExtension");
__name6(getFileExtension, "getFileExtension");
function createAiTracker(options) {
  const cursorDetector = new CursorDetector({
    getAppName: /* @__PURE__ */ __name6(() => {
      return process.env.VSCODE_APP_NAME || process.env.APP_NAME || "unknown";
    }, "getAppName"),
    getEnvVar: /* @__PURE__ */ __name6((key) => process.env[key], "getEnvVar")
  });
  const detectEnv = /* @__PURE__ */ __name6(() => {
    const cursorResult = cursorDetector.detect();
    return {
      provider: cursorResult.hasCursor ? "cursor" : "none",
      hasAI: cursorResult.hasCursor,
      confidence: cursorResult.confidence
    };
  }, "detectEnv");
  const changeTracker = new SimpleChangeTracker();
  const tracker = new AiSessionTracker(detectEnv, changeTracker, options.aiDetectionEnabled ?? true);
  return tracker;
}
__name(createAiTracker, "createAiTracker");
__name6(createAiTracker, "createAiTracker");
(class {
  static {
    __name(this, "SessionManager");
  }
  static {
    __name6(this, "SessionManager");
  }
  options;
  activeSession = null;
  idleTimer = null;
  flushTimer = null;
  aiTracker;
  idleMs;
  flushBatchSize;
  flushIntervalMs;
  ignorePatterns;
  tier;
  consent;
  constructor(options) {
    this.options = options;
    this.idleMs = options.idleMs ?? 15 * 6e4;
    this.flushBatchSize = options.flushBatchSize ?? 50;
    this.flushIntervalMs = options.flushIntervalMs ?? 5e3;
    this.tier = options.tier ?? "free";
    this.consent = options.consent ?? false;
    this.aiTracker = createAiTracker(options);
    this.ignorePatterns = options.ignorePatterns ?? [
      "node_modules/**",
      ".next/**",
      "dist/**",
      "build/**",
      "coverage/**",
      ".git/**",
      "*.log",
      "*.tmp",
      "*.swp",
      ".DS_Store"
    ];
    this.runCrashRecovery().catch((_err) => {
    });
  }
  /**
  * Run crash recovery on startup
  * Recovers any pending rollback transactions that were interrupted
  */
  async runCrashRecovery() {
    const startTime = performance.now();
    const { SessionRecovery: SessionRecovery2 } = await Promise.resolve().then(() => (init_SessionRecovery(), SessionRecovery_exports));
    const recovery = new SessionRecovery2(this.options.workspaceRoot);
    const results = await recovery.recoverAll();
    performance.now() - startTime;
    if (results.length > 0) {
      results.filter((r) => r.status === "recovered").length;
      results.filter((r) => r.status === "failed").length;
      results.reduce((sum, r) => sum + r.filesRestored, 0);
    }
  }
  /**
  * Start a new session
  */
  async start() {
    if (this.activeSession) {
      await this.finalize();
    }
    const sessionId = nanoid();
    const startedAt = Date.now();
    this.activeSession = {
      sessionId,
      startedAt,
      triggers: /* @__PURE__ */ new Set([
        "filewatch"
      ]),
      changeBuffer: []
    };
    if (this.options.db) {
      const triggerBitmask = this.encodeTriggers(this.activeSession.triggers);
      await this.options.db.run(`INSERT INTO sessions (session_id, workspace_uri, started_at, triggers, change_count)
         VALUES (?, ?, ?, ?, 0)`, [
        sessionId,
        this.options.workspaceUri,
        startedAt,
        triggerBitmask
      ]);
    }
    this.startIdleTimer();
    this.startFlushTimer();
    this.aiTracker.startSession(sessionId);
    this.emitSessionStarted();
    return {
      sessionId
    };
  }
  /**
  * Track a file change
  */
  track(absolutePath, op, meta) {
    const startTime = performance.now();
    if (!this.activeSession) {
      return;
    }
    const relPath = this.normalizePath(absolutePath);
    if (this.shouldIgnore(relPath)) {
      return;
    }
    let fromPath;
    if (op === "renamed" && meta) {
      if (meta.fromPath) {
        fromPath = this.normalizePath(meta.fromPath);
      } else if (meta.oldUri) {
        const oldAbsPath = meta.oldUri.replace("file://", "");
        fromPath = this.normalizePath(oldAbsPath);
      }
    }
    const change = {
      p: relPath,
      op,
      from: fromPath,
      // Metadata captured immediately
      sizeAfter: meta?.size,
      mtimeAfter: meta?.mtime,
      modeAfter: meta?.mode
    };
    this.activeSession.changeBuffer.push(change);
    this.aiTracker.recordChange({
      chars: change.sizeAfter ?? 0,
      lines: 1,
      isInsert: change.op === "created",
      isMultiLine: false
    });
    this.resetIdleTimer();
    if (this.activeSession.changeBuffer.length >= this.flushBatchSize) {
      this.scheduleFlush();
    }
    performance.now() - startTime;
  }
  /**
  * Finalize the active session
  */
  async finalize() {
    const startTime = performance.now();
    if (!this.activeSession) {
      throw new Error("No active session to finalize");
    }
    const { sessionId, startedAt, triggers, changeBuffer } = this.activeSession;
    const changeCount = changeBuffer.length;
    const endedAt = Date.now();
    const durationMs = endedAt - startedAt;
    this.cancelTimers();
    triggers.add("manual");
    await this.computeDeferredHashes(changeBuffer);
    if (this.options.db && changeBuffer.length > 0) {
      const stmt = this.options.db.prepare(`INSERT INTO session_changes
         (session_id, rel_path, op, from_path, size_after, mtime_after, mode_after, h_old, h_new, eol_after)
         VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`);
      for (const change of changeBuffer) {
        stmt.run(sessionId, change.p, change.op, change.from ?? null, change.sizeAfter ?? null, change.mtimeAfter ?? null, change.modeAfter ?? null, change.hOld ?? null, change.hNew ?? null, change.eolAfter ?? null);
      }
    }
    if (this.options.db) {
      const triggerBitmask = this.encodeTriggers(triggers);
      const aiResult = this.aiTracker.finalizeSession();
      await this.options.db.run(`UPDATE sessions
         SET ended_at = ?, change_count = ?, triggers = ?,
         ai_assist_level = ?, ai_confidence_score = ?, ai_provider = ?, ai_metadata = ?
         WHERE session_id = ?`, [
        endedAt,
        changeCount,
        triggerBitmask,
        aiResult.level,
        aiResult.confidence,
        aiResult.provider,
        JSON.stringify({
          reasoning: aiResult.reasoning,
          metrics: aiResult.metrics
        }),
        sessionId
      ]);
      const name = this.generateOfflineName(changeBuffer);
      await this.options.db.run("UPDATE sessions SET name = ? WHERE session_id = ?", [
        name,
        sessionId
      ]);
    }
    this.emitSessionFinalized(changeCount, durationMs, changeBuffer);
    this.activeSession = null;
    performance.now() - startTime;
    return {
      sessionId,
      changeCount
    };
  }
  /**
  * Get current session status
  */
  current() {
    if (!this.activeSession) {
      return {
        sessionId: null,
        changeCount: 0
      };
    }
    return {
      sessionId: this.activeSession.sessionId,
      changeCount: this.activeSession.changeBuffer.length
    };
  }
  /**
  * List recent sessions
  */
  async list(limit = 20) {
    if (!this.options.db) {
      return [];
    }
    const rows = await this.options.db.all(`SELECT session_id, workspace_uri, started_at, ended_at, name, triggers, change_count
       FROM sessions
       WHERE workspace_uri = ?
       ORDER BY started_at DESC
       LIMIT ?`, [
      this.options.workspaceUri,
      limit
    ]);
    return rows.map((row) => {
      const r = row;
      return {
        sessionId: r.session_id,
        startedAt: new Date(r.started_at).toISOString(),
        endedAt: r.ended_at ? new Date(r.ended_at).toISOString() : void 0,
        name: r.name,
        changeCount: r.change_count,
        triggers: this.decodeTriggers(r.triggers)
      };
    });
  }
  /**
  * Get session manifest
  */
  async getManifest(sessionId) {
    if (!this.options.db) {
      throw new Error("Database not available");
    }
    const session = await this.options.db.get("SELECT * FROM sessions WHERE session_id = ?", [
      sessionId
    ]);
    if (!session) {
      throw new Error(`Session not found: ${sessionId}`);
    }
    const changes = await this.options.db.all("SELECT * FROM session_changes WHERE session_id = ? ORDER BY id", [
      sessionId
    ]);
    const snapshots = await this.options.db.all("SELECT snapshot_id FROM session_snapshots WHERE session_id = ?", [
      sessionId
    ]);
    const s = session;
    return {
      schema: "sb.session.v1",
      sessionId: s.session_id,
      startedAt: new Date(s.started_at).toISOString(),
      endedAt: s.ended_at ? new Date(s.ended_at).toISOString() : void 0,
      workspaceUri: s.workspace_uri,
      name: s.name,
      triggers: this.decodeTriggers(s.triggers),
      changeCount: s.change_count,
      filesChanged: changes.map((c) => this.dbRowToSessionChange(c)),
      snapshots: snapshots.map((s2) => s2.snapshot_id)
    };
  }
  /**
  * Rollback a session to its starting state
  *
  * This is a wrapper that delegates to SessionRollback for the actual rollback logic.
  * Returns a Result type for safe error handling.
  */
  async rollback(sessionId, options) {
    const manifest = await this.getManifest(sessionId);
    const { SessionRollback: SessionRollback2 } = await Promise.resolve().then(() => (init_SessionRollback(), SessionRollback_exports));
    const rollback = new SessionRollback2(this.options.workspaceRoot, this.options.blobStore);
    return rollback.rollback(manifest, options);
  }
  // =========================================================================
  // Private Helper Methods
  // =========================================================================
  /**
  * Normalize path to POSIX-style relative path
  */
  normalizePath(absolutePath) {
    const relPath = path10.relative(this.options.workspaceRoot, absolutePath);
    return relPath.split(path10.sep).join("/");
  }
  /**
  * Check if path should be ignored
  */
  shouldIgnore(relPath) {
    return this.ignorePatterns.some((pattern) => minimatch(relPath, pattern, {
      dot: true
    }));
  }
  /**
  * Compute deferred SHA-256 hashes
  */
  async computeDeferredHashes(changes) {
    for (const change of changes) {
      const absPath = path10.join(this.options.workspaceRoot, change.p);
      try {
        if (change.op !== "deleted") {
          const content = await fs3.readFile(absPath);
          const result7 = await this.options.blobStore.put(content);
          if (result7.success) {
            change.hNew = result7.value;
          }
        }
      } catch (_error) {
      }
    }
  }
  /**
  * Generate offline session name from file changes
  */
  generateOfflineName(changes) {
    if (changes.length === 0) {
      return "Empty session";
    }
    const stems = /* @__PURE__ */ new Set();
    for (const change of changes.slice(0, 10)) {
      const basename6 = path10.basename(change.p, path10.extname(change.p));
      stems.add(basename6);
    }
    const stemList = Array.from(stems).slice(0, 3);
    if (stemList.length === 0) {
      return `Updated ${changes.length} files`;
    }
    return `Updated ${stemList.join(", ")}`;
  }
  /**
  * Encode trigger set to bitmask
  */
  encodeTriggers(triggers) {
    let mask = 0;
    for (const trigger of triggers) {
      if (trigger === "filewatch") {
        mask |= 1;
      }
      if (trigger === "pre-commit") {
        mask |= 2;
      }
      if (trigger === "manual") {
        mask |= 4;
      }
      if (trigger === "idle-finalize") {
        mask |= 8;
      }
    }
    return mask;
  }
  /**
  * Decode bitmask to trigger array
  */
  decodeTriggers(mask) {
    const triggers = [];
    if (mask & 1) {
      triggers.push("filewatch");
    }
    if (mask & 2) {
      triggers.push("pre-commit");
    }
    if (mask & 4) {
      triggers.push("manual");
    }
    if (mask & 8) {
      triggers.push("idle-finalize");
    }
    return triggers;
  }
  /**
  * Convert database row to SessionChange
  */
  dbRowToSessionChange(row) {
    const r = row;
    return {
      p: r.rel_path,
      op: r.op,
      from: r.from_path ?? void 0,
      hOld: r.h_old ?? void 0,
      hNew: r.h_new ?? void 0,
      sizeBefore: r.size_before ?? void 0,
      sizeAfter: r.size_after ?? void 0,
      mtimeBefore: r.mtime_before ?? void 0,
      mtimeAfter: r.mtime_after ?? void 0,
      modeBefore: r.mode_before ?? void 0,
      modeAfter: r.mode_after ?? void 0,
      eolBefore: r.eol_before,
      eolAfter: r.eol_after
    };
  }
  /**
  * Start idle timer
  */
  startIdleTimer() {
    this.idleTimer = setTimeout(() => {
      this.autoFinalize();
    }, this.idleMs);
  }
  /**
  * Reset idle timer (activity detected)
  */
  resetIdleTimer() {
    if (this.idleTimer) {
      clearTimeout(this.idleTimer);
    }
    this.startIdleTimer();
  }
  /**
  * Start flush timer
  */
  startFlushTimer() {
    this.flushTimer = setTimeout(() => {
      this.scheduleFlush();
    }, this.flushIntervalMs);
  }
  /**
  * Schedule a flush operation
  */
  scheduleFlush() {
  }
  /**
  * Cancel all timers
  */
  cancelTimers() {
    if (this.idleTimer) {
      clearTimeout(this.idleTimer);
      this.idleTimer = null;
    }
    if (this.flushTimer) {
      clearTimeout(this.flushTimer);
      this.flushTimer = null;
    }
  }
  /**
  * Auto-finalize on idle timeout
  */
  async autoFinalize() {
    if (!this.activeSession) {
      return;
    }
    this.activeSession.triggers.add("idle-finalize");
    await this.finalize();
  }
  /**
  * Emit SESSION_STARTED analytics event
  */
  emitSessionStarted() {
    makeSafeSessionStartedEvent(this.tier);
  }
  /**
  * Emit SESSION_FINALIZED analytics event
  */
  emitSessionFinalized(changeCount, durationMs, changes) {
    makeSafeSessionFinalizedEvent(changeCount, durationMs, this.tier, this.consent, changes);
  }
});
(class {
  static {
    __name(this, "FileConflictResolver");
  }
  static {
    __name6(this, "FileConflictResolver");
  }
  logger;
  fileSearchProvider;
  workspaceRoot;
  constructor(options = {}) {
    this.logger = options.logger ?? new NoOpLogger();
    this.fileSearchProvider = options.fileSearchProvider;
    this.workspaceRoot = options.workspaceRoot;
  }
  /**
  * Attempt to restore a file, handling conflicts.
  * Uses atomic write pattern: write to temp file, then rename.
  *
  * @param targetPath - Path where the file should be restored
  * @param content - File content to write
  * @param _originalMetadata - Original file metadata (for future use)
  * @returns Conflict resolution result
  *
  * @performance < 100ms for typical files
  */
  async resolveAndWrite(targetPath, content, _originalMetadata) {
    try {
      const hasPermission2 = await this.checkPermissions(targetPath);
      if (!hasPermission2) {
        this.logger.debug("No write permission for target path", {
          targetPath
        });
        return {
          resolved: false,
          action: "skipped",
          path: targetPath,
          error: new Error(`No write permission for ${targetPath}`)
        };
      }
      const parentDir = path10.dirname(targetPath);
      await fs3.mkdir(parentDir, {
        recursive: true
      });
      const tempPath = `${targetPath}.snapback-tmp-${Date.now()}`;
      try {
        await fs3.writeFile(tempPath, content, "utf8");
        await fs3.rename(tempPath, targetPath);
      } catch (writeError) {
        try {
          await fs3.unlink(tempPath);
        } catch {
        }
        throw writeError;
      }
      this.logger.debug("File restored successfully", {
        targetPath
      });
      return {
        resolved: true,
        action: "restored",
        path: targetPath
      };
    } catch (error) {
      this.logger.error("Failed to restore file", error instanceof Error ? error : void 0, {
        targetPath
      });
      return {
        resolved: false,
        action: "skipped",
        path: targetPath,
        error: error instanceof Error ? error : new Error(String(error))
      };
    }
  }
  /**
  * Detect if a file was renamed/moved since snapshot.
  * Uses content hash matching to find moved files.
  *
  * Requires a fileSearchProvider to be configured.
  *
  * @param originalPath - Original file path from snapshot
  * @param contentHash - SHA-256 hash of the original content
  * @returns New file path if found, null otherwise
  */
  async findRenamedFile(originalPath, contentHash) {
    if (!this.fileSearchProvider || !this.workspaceRoot) {
      this.logger.debug("File search not available - no provider or workspace root configured");
      return null;
    }
    const originalFileName = path10.basename(originalPath);
    const originalExt = path10.extname(originalPath);
    try {
      const files = await this.fileSearchProvider.findFiles(this.workspaceRoot, originalExt, "**/node_modules/**", 100);
      for (const filePath of files) {
        try {
          const content = await this.fileSearchProvider.readFile(filePath);
          const hash = this.hashContent(content);
          if (hash === contentHash) {
            this.logger.debug("Found renamed file by content hash", {
              originalPath,
              newPath: filePath
            });
            return filePath;
          }
          const fileName = path10.basename(filePath);
          if (this.isSimilarFileName(originalFileName, fileName)) {
            const similarity = this.calculateSimilarity(contentHash, hash);
            if (similarity > 0.8) {
              this.logger.debug("Found renamed file by similarity", {
                originalPath,
                newPath: filePath,
                similarity
              });
              return filePath;
            }
          }
        } catch {
        }
      }
    } catch (error) {
      this.logger.debug("Error searching for renamed file", {
        originalPath,
        error: error instanceof Error ? error.message : String(error)
      });
    }
    return null;
  }
  /**
  * Verify write permissions before attempting restore.
  *
  * @param targetPath - Path to check permissions for
  * @returns True if write permission is available
  */
  async checkPermissions(targetPath) {
    try {
      try {
        await fs3.access(targetPath, fs3.constants.W_OK);
        return true;
      } catch {
        const parentDir = path10.dirname(targetPath);
        try {
          await fs3.access(parentDir, fs3.constants.W_OK);
          return true;
        } catch {
          const rootDir = this.findExistingAncestor(parentDir);
          if (rootDir) {
            await fs3.access(rootDir, fs3.constants.W_OK);
            return true;
          }
          return false;
        }
      }
    } catch {
      return false;
    }
  }
  /**
  * Hash file content using SHA-256
  *
  * @param content - Content to hash
  * @returns SHA-256 hash as hex string
  */
  hashContent(content) {
    return crypto2__default.createHash("sha256").update(content).digest("hex");
  }
  /**
  * Find the nearest existing ancestor directory.
  *
  * @param dirPath - Starting directory path
  * @returns Nearest existing ancestor path, or null
  */
  findExistingAncestor(dirPath) {
    let current = dirPath;
    const root = path10.parse(current).root;
    while (current !== root) {
      try {
        fs.accessSync(current);
        return current;
      } catch {
        current = path10.dirname(current);
      }
    }
    return root;
  }
  /**
  * Check if two filenames are similar (for rename detection).
  *
  * Uses multiple heuristics:
  * - Exact base name match
  * - One name contains the other
  * - Levenshtein distance < 30% of max length
  *
  * @param name1 - First filename
  * @param name2 - Second filename
  * @returns True if filenames are considered similar
  */
  isSimilarFileName(name1, name2) {
    const base1 = path10.basename(name1, path10.extname(name1)).toLowerCase();
    const base2 = path10.basename(name2, path10.extname(name2)).toLowerCase();
    if (base1 === base2) {
      return true;
    }
    if (base1.includes(base2) || base2.includes(base1)) {
      return true;
    }
    const distance = this.levenshteinDistance(base1, base2);
    const maxLen = Math.max(base1.length, base2.length);
    return distance / maxLen < 0.3;
  }
  /**
  * Calculate Levenshtein distance between two strings.
  *
  * @param str1 - First string
  * @param str2 - Second string
  * @returns Edit distance as integer
  */
  levenshteinDistance(str1, str2) {
    const m = str1.length;
    const n = str2.length;
    const dp = Array.from({
      length: m + 1
    }, () => Array(n + 1).fill(0));
    for (let i = 0; i <= m; i++) {
      dp[i][0] = i;
    }
    for (let j = 0; j <= n; j++) {
      dp[0][j] = j;
    }
    for (let i = 1; i <= m; i++) {
      for (let j = 1; j <= n; j++) {
        if (str1[i - 1] === str2[j - 1]) {
          dp[i][j] = dp[i - 1][j - 1];
        } else {
          dp[i][j] = 1 + Math.min(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1]);
        }
      }
    }
    return dp[m][n];
  }
  /**
  * Calculate similarity between two hashes.
  *
  * Note: SHA-256 hashes are cryptographic - different content produces
  * completely different hashes. This returns 1.0 for exact match, 0.0 otherwise.
  * For actual content similarity, compare the content directly.
  *
  * @param hash1 - First hash
  * @param hash2 - Second hash
  * @returns Similarity score (1.0 = exact match, 0.0 = different)
  */
  calculateSimilarity(hash1, hash2) {
    return hash1 === hash2 ? 1 : 0;
  }
});
function legacyOk2(value) {
  return {
    success: true,
    value
  };
}
__name(legacyOk2, "legacyOk");
__name6(legacyOk2, "legacyOk");
function legacyErr2(error) {
  return {
    success: false,
    error
  };
}
__name(legacyErr2, "legacyErr");
__name6(legacyErr2, "legacyErr");
function isLegacyOk(result7) {
  return result7.success === true;
}
__name(isLegacyOk, "isLegacyOk");
__name6(isLegacyOk, "isLegacyOk");
function isLegacyErr(result7) {
  return result7.success === false;
}
__name(isLegacyErr, "isLegacyErr");
__name6(isLegacyErr, "isLegacyErr");
async function withRetry(operation, options) {
  const { maxAttempts, baseDelayMs, maxDelayMs = 3e4, jitter = false, onRetry } = options;
  return pRetry(operation, {
    retries: maxAttempts - 1,
    minTimeout: baseDelayMs,
    maxTimeout: maxDelayMs,
    randomize: jitter,
    onFailedAttempt: onRetry ? (error) => {
      onRetry(error, error.attemptNumber);
    } : void 0
  });
}
__name(withRetry, "withRetry");
__name6(withRetry, "withRetry");
function calculateBackoff(attempt, baseMs, maxMs, jitter) {
  const exponential = baseMs * 2 ** (attempt - 1);
  const capped = Math.min(exponential, maxMs);
  if (jitter) {
    const jitterAmount = Math.random() * capped;
    return capped + jitterAmount;
  }
  return capped;
}
__name(calculateBackoff, "calculateBackoff");
__name6(calculateBackoff, "calculateBackoff");
function diagnoseSnapshotFailure(error, files, workspaceRoot) {
  const errorMessage2 = error instanceof Error ? error.message : String(error);
  if (errorMessage2.includes("ENOENT") || errorMessage2.includes("no such file") || errorMessage2.toLowerCase().includes("file not found")) {
    const missingFiles = files.filter((f) => {
      const fullPath = path10.isAbsolute(f) ? f : path10.join(workspaceRoot, f);
      return !fs.existsSync(fullPath);
    });
    return {
      type: "FILE_NOT_FOUND",
      message: "One or more files do not exist",
      cause: "The specified file paths could not be found on disk",
      suggestedFix: "Check file paths and ensure files exist",
      userAction: missingFiles.length > 0 ? `Create missing files: ${missingFiles.join(", ")}` : "Verify file paths are correct",
      canAutoFix: false,
      confidence: 0.95,
      affectedFiles: missingFiles.length > 0 ? missingFiles : void 0
    };
  }
  if (errorMessage2.includes("absolute") || errorMessage2.includes("Absolute paths not allowed")) {
    const absolutePaths = files.filter((f) => path10.isAbsolute(f));
    return {
      type: "ABSOLUTE_PATH_REJECTED",
      message: "Snapshot tool requires relative paths",
      cause: "Absolute paths were provided instead of relative paths",
      suggestedFix: "Convert absolute paths to relative paths from workspace root",
      userAction: "Automatic: Converting to relative paths",
      canAutoFix: true,
      confidence: 1,
      affectedFiles: absolutePaths
    };
  }
  if (errorMessage2.includes("EACCES") || errorMessage2.includes("permission denied")) {
    return {
      type: "PERMISSION_DENIED",
      message: "Permission denied when accessing files",
      cause: "The current user does not have read permission for one or more files",
      suggestedFix: "Check file permissions or run with appropriate privileges",
      userAction: "Run: chmod +r <file> or check file ownership",
      canAutoFix: false,
      confidence: 0.9
    };
  }
  if (errorMessage2.includes("workspace") || errorMessage2.includes("outside workspace")) {
    return {
      type: "WORKSPACE_MISMATCH",
      message: "Files are outside the workspace root",
      cause: "The workspace root does not contain all specified files",
      suggestedFix: "Ensure all files are within the workspace directory",
      userAction: "Automatic: Resolving paths from correct workspace root",
      canAutoFix: true,
      confidence: 0.85
    };
  }
  if (errorMessage2.includes("cwd") || errorMessage2.includes("working directory")) {
    return {
      type: "WORKING_DIRECTORY_MISMATCH",
      message: "Current working directory mismatch",
      cause: "The process is not running from the expected workspace root",
      suggestedFix: "Change to the correct working directory",
      userAction: "Automatic: Adjusting paths for current directory",
      canAutoFix: true,
      confidence: 0.8
    };
  }
  if (errorMessage2.includes("ENOSPC") || errorMessage2.includes("no space")) {
    return {
      type: "STORAGE_FULL",
      message: "Insufficient disk space",
      cause: "The disk does not have enough free space for the snapshot",
      suggestedFix: "Free up disk space and try again",
      userAction: "Clear temporary files or expand disk storage",
      canAutoFix: false,
      confidence: 0.95
    };
  }
  return {
    type: "UNKNOWN",
    message: "Unknown error during snapshot creation",
    cause: errorMessage2,
    suggestedFix: "Check the error message and logs for details",
    userAction: "Review error details and try again",
    canAutoFix: false,
    confidence: 0.3
  };
}
__name(diagnoseSnapshotFailure, "diagnoseSnapshotFailure");
__name6(diagnoseSnapshotFailure, "diagnoseSnapshotFailure");
async function applyAutomaticFix(diagnosis, context) {
  switch (diagnosis.type) {
    case "ABSOLUTE_PATH_REJECTED": {
      context.files = context.files.map((f) => {
        if (path10.isAbsolute(f)) {
          return path10.relative(context.workspaceRoot, f);
        }
        return f;
      });
      return true;
    }
    case "WORKSPACE_MISMATCH":
    case "WORKING_DIRECTORY_MISMATCH": {
      context.files = context.files.map((f) => {
        if (path10.isAbsolute(f)) {
          return path10.relative(context.workspaceRoot, f);
        }
        const fromCwd = path10.resolve(process.cwd(), f);
        if (fs.existsSync(fromCwd)) {
          return path10.relative(context.workspaceRoot, fromCwd);
        }
        return f;
      });
      return true;
    }
    default:
      return false;
  }
}
__name(applyAutomaticFix, "applyAutomaticFix");
__name6(applyAutomaticFix, "applyAutomaticFix");
var DEFAULT_RETRY_CONFIG = {
  maxRetries: 3,
  delayMs: 100,
  exponentialBackoff: true,
  autoFix: true,
  verbose: false
};
async function createSnapshotWithRetry(params, snapshotFn, config2 = {}) {
  const cfg = {
    ...DEFAULT_RETRY_CONFIG,
    ...config2
  };
  const { workspaceRoot = process.cwd() } = params;
  let lastError = null;
  let lastDiagnosis = null;
  const context = {
    files: [
      ...params.files
    ],
    workspaceRoot
  };
  for (let attempt = 1; attempt <= cfg.maxRetries; attempt++) {
    try {
      if (cfg.verbose && attempt > 1) {
      }
      if (attempt > 1) {
        const delay = cfg.exponentialBackoff ? calculateBackoff(attempt - 1, cfg.delayMs, 3e4, false) : cfg.delayMs;
        await new Promise((resolve22) => setTimeout(resolve22, delay));
      }
      const snapshot = await snapshotFn({
        ...params,
        files: context.files
      });
      if (cfg.verbose && attempt > 1) {
      }
      return {
        success: true,
        snapshot,
        attempt,
        totalAttempts: attempt
      };
    } catch (error) {
      lastError = error instanceof Error ? error : new Error(String(error));
      const diagnosis = diagnoseSnapshotFailure(error, context.files, context.workspaceRoot);
      lastDiagnosis = diagnosis;
      if (cfg.autoFix && diagnosis.canAutoFix && attempt < cfg.maxRetries) {
        const fixApplied = await applyAutomaticFix(diagnosis, context);
        if (fixApplied) {
          continue;
        }
      }
      if (attempt === cfg.maxRetries) {
        break;
      }
      if (cfg.verbose && !diagnosis.canAutoFix) ;
    }
  }
  return {
    success: false,
    error: lastError?.message || "Unknown error",
    suggestion: lastDiagnosis?.userAction || "Check logs for details",
    diagnostics: lastDiagnosis || void 0,
    attempt: cfg.maxRetries,
    totalAttempts: cfg.maxRetries
  };
}
__name(createSnapshotWithRetry, "createSnapshotWithRetry");
__name6(createSnapshotWithRetry, "createSnapshotWithRetry");
async function createSnapshotWithRetrySafe(params, snapshotFn, config2 = {}) {
  const result7 = await createSnapshotWithRetry(params, snapshotFn, config2);
  if (result7.success && result7.snapshot) {
    return ok(result7.snapshot);
  }
  return err(result7.diagnostics || {
    type: "UNKNOWN",
    message: result7.error || "Unknown error",
    cause: "Snapshot creation failed after all retries",
    suggestedFix: result7.suggestion || "Check logs for details",
    userAction: "Review error and try again",
    canAutoFix: false,
    confidence: 0.1
  });
}
__name(createSnapshotWithRetrySafe, "createSnapshotWithRetrySafe");
__name6(createSnapshotWithRetrySafe, "createSnapshotWithRetrySafe");
function formatDiagnosis(diagnosis) {
  const confidencePercent = Math.round(diagnosis.confidence * 100);
  const autoFixBadge = diagnosis.canAutoFix ? "\u2705 Auto-fixable" : "\u274C Manual fix required";
  return `
\u{1F50D} Snapshot Failure Diagnosis
\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501

Type: ${diagnosis.type}
Confidence: ${confidencePercent}%
${autoFixBadge}

\u{1F4CB} Issue:
  ${diagnosis.message}

\u{1F50E} Root Cause:
  ${diagnosis.cause}

\u{1F4A1} Suggested Fix:
  ${diagnosis.suggestedFix}

\u{1F464} Action Required:
  ${diagnosis.userAction}
${diagnosis.affectedFiles ? `
\u{1F4C1} Affected Files:
${diagnosis.affectedFiles.map((f) => `  - ${f}`).join("\n")}` : ""}
`.trim();
}
__name(formatDiagnosis, "formatDiagnosis");
__name6(formatDiagnosis, "formatDiagnosis");
function toError2(error) {
  if (error instanceof Error) {
    return error;
  }
  if (typeof error === "string") {
    return new Error(error);
  }
  if (typeof error === "object" && error !== null) {
    if ("message" in error && typeof error.message === "string") {
      return new Error(error.message);
    }
    try {
      return new Error(JSON.stringify(error));
    } catch (_stringifyError) {
      return new Error(String(error));
    }
  }
  return new Error(String(error));
}
__name(toError2, "toError2");
__name6(toError2, "toError");
(class {
  static {
    __name(this, "SnapshotDeletionService");
  }
  static {
    __name6(this, "SnapshotDeletionService");
  }
  snapshotManager;
  confirmationService;
  logger;
  constructor(snapshotManager, confirmationService, options = {}) {
    this.snapshotManager = snapshotManager;
    this.confirmationService = confirmationService;
    this.logger = options.logger ?? new NoOpLogger();
  }
  /**
  * Delete a single snapshot with safety checks
  *
  * @param snapshotId - ID of snapshot to delete
  * @param options - Deletion options
  * @returns Deletion result with success status and count
  * @throws Error if snapshot is protected and unprotectFirst is false
  * @throws Error if snapshot does not exist
  *
  * @performance < 50ms including confirmation dialog
  */
  async deleteSnapshot(snapshotId, options = {}) {
    const snapshot = await this.snapshotManager.get(snapshotId);
    if (!snapshot) {
      throw new Error(`Snapshot not found: ${snapshotId}`);
    }
    if (snapshot.isProtected && !options.unprotectFirst) {
      throw new Error("Cannot delete protected snapshot. Set unprotectFirst=true to override.");
    }
    if (options.unprotectFirst && snapshot.isProtected) {
      await this.snapshotManager.unprotect(snapshotId);
    }
    if (!options.skipConfirmation) {
      const confirmed = await this.confirmationService.confirm(`Delete snapshot "${snapshot.name}"?`, "This action cannot be undone.");
      if (!confirmed) {
        return {
          success: false,
          deletedCount: 0,
          error: "User cancelled deletion"
        };
      }
    }
    await this.snapshotManager.delete(snapshotId);
    return {
      success: true,
      deletedCount: 1
    };
  }
  /**
  * Delete all snapshots older than the specified timestamp
  *
  * @param timestamp - Cutoff timestamp (milliseconds since epoch)
  * @param keepProtected - If true, skip protected snapshots
  * @returns Deletion result with count of deleted snapshots
  *
  * @performance < 500ms for 100 snapshots
  */
  async deleteOlderThan(timestamp, keepProtected = true) {
    const allSnapshots = await this.snapshotManager.getAll();
    const toDelete = allSnapshots.filter((snapshot) => {
      if (snapshot.timestamp >= timestamp) {
        return false;
      }
      if (keepProtected && snapshot.isProtected) {
        return false;
      }
      return true;
    });
    let deletedCount = 0;
    for (const snapshot of toDelete) {
      try {
        if (snapshot.isProtected) {
          await this.snapshotManager.unprotect(snapshot.id);
        }
        await this.snapshotManager.delete(snapshot.id);
        deletedCount++;
      } catch (error) {
        this.logger.error(`Failed to delete snapshot ${snapshot.id}:`, toError2(error));
      }
    }
    return {
      success: true,
      deletedCount
    };
  }
  /**
  * Perform automatic cleanup based on configuration
  *
  * @param config - Auto-cleanup configuration
  * @returns Deletion result with count of deleted snapshots
  */
  async autoCleanup(config2) {
    if (!config2.enabled) {
      return {
        success: true,
        deletedCount: 0
      };
    }
    const allSnapshots = await this.snapshotManager.getAll();
    if (allSnapshots.length <= config2.minimumSnapshots) {
      return {
        success: true,
        deletedCount: 0
      };
    }
    const cutoffTime = Date.now() - config2.olderThanDays * 24 * 60 * 60 * 1e3;
    const eligibleForDeletion = allSnapshots.filter((snapshot) => {
      if (snapshot.timestamp >= cutoffTime) {
        return false;
      }
      if (config2.keepProtected && snapshot.isProtected) {
        return false;
      }
      return true;
    });
    eligibleForDeletion.sort((a, b) => a.timestamp - b.timestamp);
    const maxToDelete = allSnapshots.length - config2.minimumSnapshots;
    const toDelete = eligibleForDeletion.slice(0, Math.max(0, maxToDelete));
    let deletedCount = 0;
    for (const snapshot of toDelete) {
      try {
        if (snapshot.isProtected) {
          await this.snapshotManager.unprotect(snapshot.id);
        }
        await this.snapshotManager.delete(snapshot.id);
        deletedCount++;
      } catch (error) {
        this.logger.error(`Auto-cleanup failed for ${snapshot.id}:`, toError2(error));
      }
    }
    return {
      success: true,
      deletedCount
    };
  }
  /**
  * Check if a snapshot can be safely deleted
  *
  * @param snapshot - Snapshot to check
  * @returns True if snapshot can be deleted without unprotectFirst
  *
  * @performance < 1ms
  */
  canDelete(snapshot) {
    return !snapshot.isProtected;
  }
});
(class _SnapshotIconStrategy {
  static {
    __name(this, "_SnapshotIconStrategy");
  }
  static {
    __name6(this, "SnapshotIconStrategy");
  }
  /**
  * Icon mapping configuration with codicon names and theme colors
  */
  static ICON_MAP = {
    "file-add": {
      icon: "file-add",
      color: "charts.green"
    },
    "file-delete": {
      icon: "trash",
      color: "charts.red"
    },
    "test-changes": {
      icon: "beaker",
      color: "charts.purple"
    },
    "update-deps": {
      icon: "package",
      color: "charts.yellow"
    },
    "config-change": {
      icon: "settings-gear",
      color: "debugConsole.warningForeground"
    },
    refactor: {
      icon: "symbol-class",
      color: "charts.blue"
    },
    "fix-bug": {
      icon: "bug",
      color: "charts.red"
    },
    "docs-update": {
      icon: "book",
      color: "charts.blue"
    },
    "style-changes": {
      icon: "paintcan",
      color: "charts.pink"
    },
    "api-changes": {
      icon: "server",
      color: "charts.yellow"
    },
    database: {
      icon: "database",
      color: "charts.orange"
    },
    protected: {
      icon: "lock",
      color: "charts.red"
    },
    default: {
      icon: "file-code",
      color: "foreground"
    }
  };
  /**
  * Pre-compiled regex patterns for performance optimization
  */
  static TEST_FILE_REGEX = /\.(test|spec)\.(ts|js|tsx|jsx)$/i;
  static CONFIG_FILE_REGEX = /\.(config\.(ts|js)|eslintrc|prettierrc|env)/i;
  static STYLE_FILE_REGEX = /\.(css|scss|less|sass)$/i;
  static DOC_FILE_REGEX = /\.(md|mdx)$/i;
  static SQL_FILE_REGEX = /\.sql$/i;
  static SCHEMA_FILE_REGEX = /schema\.(sql|prisma|ts|js)/i;
  static API_FILE_REGEX = /\.api\./i;
  /**
  * Package lock file patterns
  */
  static PACKAGE_FILES = /* @__PURE__ */ new Set([
    "package.json",
    "package-lock.json",
    "yarn.lock",
    "pnpm-lock.yaml"
  ]);
  /**
  * Config file patterns
  */
  static CONFIG_FILES = /* @__PURE__ */ new Set([
    "tsconfig.json",
    ".eslintrc.json",
    ".prettierrc"
  ]);
  /**
  * Keyword sets for name-based classification
  */
  static BUG_FIX_KEYWORDS = [
    "fix",
    "bugfix"
  ];
  static REFACTOR_KEYWORDS = [
    "refactor",
    "refactored"
  ];
  static ADDITION_KEYWORDS = [
    "added",
    "created",
    "file-add"
  ];
  static DELETION_KEYWORDS = [
    "deleted",
    "removed",
    "file-delete"
  ];
  static DOC_KEYWORDS = [
    "docs",
    "documentation",
    "docs-update"
  ];
  static STYLE_KEYWORDS = [
    "style",
    "styling",
    "style-changes"
  ];
  static API_KEYWORDS = [
    "api-changes",
    "endpoint"
  ];
  static DATABASE_KEYWORDS = [
    "database",
    "db",
    "migration",
    "schema"
  ];
  static PACKAGE_KEYWORDS = [
    "update-deps",
    "dependencies"
  ];
  static CONFIG_KEYWORDS = [
    "config-change"
  ];
  /**
  * Classifies a snapshot into an appropriate icon based on its metadata.
  *
  * Detection logic priority order:
  * 1. Protected status (highest priority)
  * 2. Name keywords (bug fix, deletion, refactor, etc.)
  * 3. File extensions (test files, package files, config files, etc.)
  * 4. Fallback to default icon
  *
  * @param metadata - The snapshot metadata to classify
  * @returns IconResult containing the codicon name and theme color
  */
  classifyIcon(metadata) {
    if (metadata.isProtected) {
      return _SnapshotIconStrategy.ICON_MAP.protected;
    }
    const nameResult = this.classifyByName(metadata.name);
    if (nameResult) {
      return nameResult;
    }
    const fileResult = this.classifyByFiles(metadata.files);
    if (fileResult) {
      return fileResult;
    }
    return _SnapshotIconStrategy.ICON_MAP.default;
  }
  /**
  * Get the icon mapping for a specific category
  *
  * @param category - The category key (e.g., 'protected', 'test-changes')
  * @returns IconMapping or undefined if not found
  */
  getIconMapping(category) {
    return _SnapshotIconStrategy.ICON_MAP[category];
  }
  /**
  * Get all available icon mappings
  *
  * @returns Record of all icon mappings
  */
  getAllIconMappings() {
    return {
      ..._SnapshotIconStrategy.ICON_MAP
    };
  }
  /**
  * Classifies based on name keywords with priority ordering.
  *
  * @param name - The snapshot name
  * @returns IconResult if keyword matched, null otherwise
  */
  classifyByName(name) {
    const lowerName = name.toLowerCase();
    if (this.matchesPrefixKeyword(lowerName, _SnapshotIconStrategy.ADDITION_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP["file-add"];
    }
    if (this.matchesPrefixKeyword(lowerName, _SnapshotIconStrategy.BUG_FIX_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP["fix-bug"];
    }
    if (this.matchesPrefixKeyword(lowerName, _SnapshotIconStrategy.DELETION_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP["file-delete"];
    }
    if (this.matchesPrefixKeyword(lowerName, _SnapshotIconStrategy.REFACTOR_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP.refactor;
    }
    if (this.matchesPrefixKeyword(lowerName, _SnapshotIconStrategy.DOC_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP["docs-update"];
    }
    if (this.matchesPrefixKeyword(lowerName, _SnapshotIconStrategy.STYLE_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP["style-changes"];
    }
    if (this.matchesPrefixKeyword(lowerName, _SnapshotIconStrategy.CONFIG_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP["config-change"];
    }
    if (this.matchesKeyword(lowerName, _SnapshotIconStrategy.BUG_FIX_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP["fix-bug"];
    }
    if (this.matchesKeyword(lowerName, _SnapshotIconStrategy.DELETION_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP["file-delete"];
    }
    if (this.matchesKeyword(lowerName, _SnapshotIconStrategy.REFACTOR_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP.refactor;
    }
    if (this.matchesKeyword(lowerName, _SnapshotIconStrategy.API_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP["api-changes"];
    }
    if (this.matchesKeyword(lowerName, _SnapshotIconStrategy.DATABASE_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP.database;
    }
    if (this.matchesKeyword(lowerName, _SnapshotIconStrategy.DOC_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP["docs-update"];
    }
    if (this.matchesKeyword(lowerName, _SnapshotIconStrategy.STYLE_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP["style-changes"];
    }
    if (this.matchesKeyword(lowerName, _SnapshotIconStrategy.ADDITION_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP["file-add"];
    }
    if (this.matchesKeyword(lowerName, _SnapshotIconStrategy.PACKAGE_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP["update-deps"];
    }
    if (this.matchesKeyword(lowerName, _SnapshotIconStrategy.CONFIG_KEYWORDS)) {
      return _SnapshotIconStrategy.ICON_MAP["config-change"];
    }
    return null;
  }
  /**
  * Classifies based on file extensions and paths.
  *
  * @param files - The list of file paths
  * @returns IconResult if file pattern matched, null otherwise
  */
  classifyByFiles(files) {
    if (!files || files.length === 0) {
      return null;
    }
    if (this.containsTestFiles(files)) {
      return _SnapshotIconStrategy.ICON_MAP["test-changes"];
    }
    if (this.containsPackageFiles(files)) {
      return _SnapshotIconStrategy.ICON_MAP["update-deps"];
    }
    if (this.containsConfigFiles(files)) {
      return _SnapshotIconStrategy.ICON_MAP["config-change"];
    }
    if (this.containsDocFiles(files)) {
      return _SnapshotIconStrategy.ICON_MAP["docs-update"];
    }
    if (this.containsStyleFiles(files)) {
      return _SnapshotIconStrategy.ICON_MAP["style-changes"];
    }
    if (this.containsDatabaseFiles(files)) {
      return _SnapshotIconStrategy.ICON_MAP.database;
    }
    if (this.containsApiFiles(files)) {
      return _SnapshotIconStrategy.ICON_MAP["api-changes"];
    }
    return null;
  }
  /**
  * Checks if name matches any keyword (case-insensitive).
  */
  matchesKeyword(name, keywords) {
    return keywords.some((k) => name.includes(k));
  }
  /**
  * Checks if name starts with keyword followed by colon (conventional commit format).
  */
  matchesPrefixKeyword(name, keywords) {
    return keywords.some((k) => name.startsWith(`${k}:`));
  }
  /**
  * Checks if files contain test files.
  */
  containsTestFiles(files) {
    return files.some((file) => {
      const fileName = path10.basename(file);
      const lowerFile = file.toLowerCase();
      return _SnapshotIconStrategy.TEST_FILE_REGEX.test(fileName) || lowerFile.includes("__tests__");
    });
  }
  /**
  * Checks if files contain package lock files.
  */
  containsPackageFiles(files) {
    return files.some((file) => {
      const fileName = path10.basename(file);
      return _SnapshotIconStrategy.PACKAGE_FILES.has(fileName);
    });
  }
  /**
  * Checks if files contain configuration files.
  */
  containsConfigFiles(files) {
    return files.some((file) => {
      const fileName = path10.basename(file);
      const lowerFile = file.toLowerCase();
      return _SnapshotIconStrategy.CONFIG_FILE_REGEX.test(fileName) || _SnapshotIconStrategy.CONFIG_FILES.has(fileName) || lowerFile.includes(".env");
    });
  }
  /**
  * Checks if files contain documentation files.
  */
  containsDocFiles(files) {
    return files.some((file) => {
      const fileName = path10.basename(file);
      const lowerFile = file.toLowerCase();
      return _SnapshotIconStrategy.DOC_FILE_REGEX.test(fileName) || lowerFile.includes("/docs/") || lowerFile.startsWith("docs/");
    });
  }
  /**
  * Checks if files contain style files.
  */
  containsStyleFiles(files) {
    return files.some((file) => {
      const fileName = path10.basename(file);
      return _SnapshotIconStrategy.STYLE_FILE_REGEX.test(fileName);
    });
  }
  /**
  * Checks if files contain database files.
  */
  containsDatabaseFiles(files) {
    return files.some((file) => {
      const fileName = path10.basename(file);
      const lowerFile = file.toLowerCase();
      return _SnapshotIconStrategy.SQL_FILE_REGEX.test(fileName) || _SnapshotIconStrategy.SCHEMA_FILE_REGEX.test(fileName) || lowerFile.includes("/migrations/") || lowerFile.startsWith("migrations/") || lowerFile.includes("/schema/") || lowerFile.startsWith("schema/");
    });
  }
  /**
  * Checks if files contain API files.
  */
  containsApiFiles(files) {
    return files.some((file) => {
      const fileName = path10.basename(file);
      const lowerFile = file.toLowerCase();
      return _SnapshotIconStrategy.API_FILE_REGEX.test(fileName) || lowerFile.includes("/api/") || lowerFile.startsWith("api/");
    });
  }
});
var execAsync2 = promisify(exec);
(class {
  static {
    __name(this, "SnapshotNamingStrategy");
  }
  static {
    __name6(this, "SnapshotNamingStrategy");
  }
  workspaceRoot;
  gitTimeoutMs;
  maxNameLength;
  logger;
  constructor(workspaceRoot, options = {}) {
    this.workspaceRoot = workspaceRoot;
    this.logger = options.logger ?? new NoOpLogger();
    this.gitTimeoutMs = options.gitTimeoutMs ?? 5e3;
    this.maxNameLength = options.maxNameLength ?? 60;
  }
  /**
  * Generates a snapshot name using multi-tier fallback strategy
  *
  * @param info - Snapshot information containing file changes
  * @returns Promise resolving to a descriptive snapshot name
  */
  async generateName(info) {
    if (info.files.length === 0) {
      return "No changes";
    }
    let baseName;
    const gitName = await this.tryGitNaming(info);
    if (gitName) {
      baseName = gitName;
    } else {
      const fileOpName = this.tryFileOperationNaming(info);
      if (fileOpName) {
        baseName = fileOpName;
      } else {
        const contentName = await this.tryContentAnalysisNaming(info);
        if (contentName) {
          baseName = contentName;
        } else {
          baseName = this.fallbackNaming(info);
        }
      }
    }
    if (info.userContext) {
      const prefix = this.formatUserContext(info.userContext);
      return `${prefix}: ${baseName}`;
    }
    return baseName;
  }
  /**
  * Tier 1: Git-based naming
  * Attempts to use actual git commands to generate names.
  * Returns null if git is unavailable or no git repo exists.
  */
  async tryGitNaming(info) {
    try {
      const isGitRepo = await this.execGit([
        "rev-parse",
        "--git-dir"
      ]);
      if (!isGitRepo) {
        return null;
      }
      const gitStatus = await this.execGit([
        "status",
        "--porcelain"
      ]);
      if (!gitStatus) {
        return null;
      }
      if (info.files.length === 1) {
        const file = info.files[0];
        return this.generateSingleFileGitName(file.status, file.path);
      }
      return this.generateMultiFileGitName(info.files);
    } catch (_error) {
      return null;
    }
  }
  /**
  * Tier 2: File operation pattern detection
  * Detects test files, configs, dependencies with priority ordering
  */
  tryFileOperationNaming(info) {
    const files = info.files;
    const testFiles = files.filter((f) => this.isTestFile(f.path));
    if (testFiles.length > 0 && testFiles.length === files.length) {
      return `Updated ${testFiles.length} test${testFiles.length > 1 ? "s" : ""}`;
    }
    const dependencyFiles = files.filter((f) => this.isDependencyFile(f.path));
    if (dependencyFiles.length > 0) {
      return "Updated dependencies";
    }
    const configFiles = files.filter((f) => this.isConfigFile(f.path));
    if (configFiles.length > 0 && configFiles.length === files.length) {
      return `Modified ${configFiles.length} config${configFiles.length > 1 ? "s" : ""}`;
    }
    if (testFiles.length > 0 && testFiles.length < files.length) {
      return `Updated ${testFiles.length} test${testFiles.length > 1 ? "s" : ""}`;
    }
    return null;
  }
  /**
  * Tier 3: Content analysis
  * Detects refactoring patterns, structure changes, and import modifications
  */
  async tryContentAnalysisNaming(info) {
    try {
      const importCount = await this.countImportChanges(info.files);
      const structureCount = await this.countStructureChanges(info.files);
      if (importCount > 0 && structureCount === 0) {
        return `Updated ${importCount} import${importCount > 1 ? "s" : ""}`;
      }
      if (structureCount > 3 && info.files.length > 1) {
        const commonDir = this.findCommonDirectory(info.files);
        const moduleName = this.extractModuleName(commonDir, info.files);
        return `Refactored ${moduleName} module (${info.files.length} files)`;
      }
      if (structureCount >= 3 && info.files.length === 1) {
        const dir = path10.dirname(info.files[0].path);
        const moduleName = this.extractModuleName(dir, info.files);
        return `Refactored ${moduleName} (${structureCount} changes)`;
      }
      if (importCount > 0) {
        return `Updated ${importCount} import${importCount > 1 ? "s" : ""}`;
      }
      return null;
    } catch (_error) {
      return null;
    }
  }
  /**
  * Tier 4: Fallback naming
  * Uses git-style format for code files, line count for unknown/non-code files
  */
  fallbackNaming(info) {
    const totalLines = info.files.reduce((sum, file) => sum + file.linesAdded + file.linesDeleted, 0);
    const fileCount = info.files.length;
    const allCodeFiles = info.files.every((f) => this.isCodeFile(f.path));
    if (info.files.length === 1) {
      const file = info.files[0];
      if (!this.isCodeFile(file.path)) {
        return `Modified 1 file (${totalLines} lines)`;
      }
      return this.generateSingleFileGitName(file.status, file.path);
    }
    if (!allCodeFiles) {
      return `Modified ${fileCount} files (${totalLines} lines)`;
    }
    const hasAdditions = info.files.some((f) => f.status === "added");
    const hasModifications = info.files.some((f) => f.status === "modified");
    const hasDeletions = info.files.some((f) => f.status === "deleted");
    if (hasAdditions || hasModifications || hasDeletions) {
      return this.generateMultiFileGitName(info.files);
    }
    return `Modified ${fileCount} files (${totalLines} lines)`;
  }
  /**
  * Execute git command with error handling
  */
  async execGit(args) {
    try {
      const { stdout } = await execAsync2(`git ${args.join(" ")}`, {
        cwd: this.workspaceRoot,
        timeout: this.gitTimeoutMs
      });
      return stdout.trim();
    } catch (_error) {
      return null;
    }
  }
  /**
  * Generate single-file git-style name
  */
  generateSingleFileGitName(status, filePath) {
    const basename6 = path10.basename(filePath);
    const sanitizedName = this.sanitizeFilename(basename6);
    const truncatedName = this.truncatePath(sanitizedName, this.maxNameLength - 20);
    switch (status) {
      case "added":
        return `Added ${truncatedName}`;
      case "modified":
        return `Modified ${truncatedName}`;
      case "deleted":
        return `Deleted ${truncatedName}`;
    }
  }
  /**
  * Generate multi-file git-style name (e.g., "3A 2M 1D in src/auth")
  */
  generateMultiFileGitName(files) {
    const added = files.filter((f) => f.status === "added").length;
    const modified = files.filter((f) => f.status === "modified").length;
    const deleted = files.filter((f) => f.status === "deleted").length;
    const parts = [];
    if (added > 0) {
      parts.push(`${added}A`);
    }
    if (modified > 0) {
      parts.push(`${modified}M`);
    }
    if (deleted > 0) {
      parts.push(`${deleted}D`);
    }
    const statusSummary = parts.join(" ");
    const commonDir = this.findCommonDirectory(files);
    const dirName = commonDir ? this.getRelativeDirectory(commonDir) : "workspace";
    return `${statusSummary} in ${dirName}`;
  }
  /**
  * Find common directory path for multiple files
  */
  findCommonDirectory(files) {
    if (files.length === 0) {
      return "";
    }
    if (files.length === 1) {
      return path10.dirname(files[0].path);
    }
    const dirPaths = files.map((f) => path10.dirname(f.path));
    const segmentArrays = dirPaths.map((dir) => dir.split(path10.sep));
    const firstSegments = segmentArrays[0];
    const commonSegments = [];
    for (let i = 0; i < firstSegments.length; i++) {
      const segment = firstSegments[i];
      const allMatch = segmentArrays.every((segments) => segments[i] === segment);
      if (allMatch) {
        commonSegments.push(segment);
      } else {
        break;
      }
    }
    return commonSegments.length === 0 ? "" : commonSegments.join(path10.sep);
  }
  /**
  * Get relative directory name from absolute path
  */
  getRelativeDirectory(absolutePath) {
    let relative42 = path10.relative(this.workspaceRoot, absolutePath);
    if (relative42) {
      relative42 = relative42.split(path10.sep).join("/");
      if (relative42.startsWith("./")) {
        relative42 = relative42.substring(2);
      }
      if (relative42 && relative42 !== "." && !relative42.startsWith("..")) {
        return relative42;
      }
    }
    return ".";
  }
  /**
  * Extract meaningful module name from directory path
  */
  extractModuleName(dirPath, files) {
    if (!dirPath) {
      return "module";
    }
    const basename6 = path10.basename(dirPath);
    if (basename6.includes("tmp") || basename6.includes("test-") || basename6.startsWith(".")) {
      if (files.length > 0) {
        const firstFile = files[0].path;
        const parts = firstFile.split(path10.sep).filter((p) => p && p !== ".");
        for (let i = parts.length - 2; i >= 0; i--) {
          const part = parts[i];
          if (!part.includes("tmp") && !part.includes("test-") && !part.startsWith(".") && part.length > 2) {
            return part;
          }
        }
      }
      return "module";
    }
    return basename6;
  }
  /**
  * Detect if file is a code file (has known code extension)
  */
  isCodeFile(filePath) {
    const basename6 = path10.basename(filePath);
    const ext = path10.extname(filePath).toLowerCase();
    const knownCodeFiles = [
      "Dockerfile",
      "Makefile",
      "README.md",
      ".gitignore"
    ];
    if (knownCodeFiles.some((known) => basename6 === known || basename6.endsWith(known))) {
      return true;
    }
    if (ext && !this.isKnownCodeExtension(ext)) {
      return false;
    }
    return true;
  }
  /**
  * Check if extension is a known code file extension
  */
  isKnownCodeExtension(ext) {
    const codeExtensions = [
      ".ts",
      ".js",
      ".tsx",
      ".jsx",
      ".py",
      ".java",
      ".c",
      ".cpp",
      ".h",
      ".hpp",
      ".go",
      ".rs",
      ".rb",
      ".php",
      ".cs",
      ".swift",
      ".kt",
      ".scala",
      ".html",
      ".css",
      ".scss",
      ".sass",
      ".less",
      ".json",
      ".xml",
      ".yaml",
      ".yml",
      ".md",
      ".config"
    ];
    return codeExtensions.includes(ext);
  }
  /**
  * Detect if file is a test file
  */
  isTestFile(filePath) {
    const basename6 = path10.basename(filePath);
    const dirname52 = path10.dirname(filePath);
    if (basename6.endsWith(".test.ts") || basename6.endsWith(".test.js")) {
      return true;
    }
    if (basename6.endsWith(".spec.ts") || basename6.endsWith(".spec.js")) {
      return true;
    }
    if (dirname52.includes("__tests__")) {
      return true;
    }
    return false;
  }
  /**
  * Detect if file is package.json or dependency-related
  */
  isDependencyFile(filePath) {
    const basename6 = path10.basename(filePath);
    return basename6 === "package.json" || basename6 === "package-lock.json" || basename6 === "pnpm-lock.yaml" || basename6 === "yarn.lock";
  }
  /**
  * Detect if file is configuration
  */
  isConfigFile(filePath) {
    const basename6 = path10.basename(filePath);
    if (basename6.includes(".config.")) {
      return true;
    }
    if (basename6.includes("rc")) {
      return true;
    }
    if (basename6.startsWith(".env")) {
      return true;
    }
    const configFiles = [
      "tsconfig.json",
      "jsconfig.json"
    ];
    return configFiles.includes(basename6);
  }
  /**
  * Count import changes via regex
  */
  async countImportChanges(files) {
    let importCount = 0;
    const importRegex = /import\s+.*from|require\(/g;
    for (const file of files) {
      try {
        const content = await promises.readFile(file.path, "utf-8");
        const matches = content.match(importRegex);
        if (matches) {
          importCount += matches.length;
        }
      } catch (error) {
        this.logger.debug("Failed to read file for import analysis", {
          path: file.path,
          error: error instanceof Error ? error.message : String(error)
        });
      }
    }
    return importCount;
  }
  /**
  * Count function/class changes via regex
  */
  async countStructureChanges(files) {
    let structureCount = 0;
    const structureRegex = /function\s+\w+|class\s+\w+|const\s+\w+\s*=\s*\(/g;
    for (const file of files) {
      try {
        const content = await promises.readFile(file.path, "utf-8");
        const matches = content.match(structureRegex);
        if (matches) {
          structureCount += matches.length;
        }
      } catch (error) {
        this.logger.debug("Failed to read file for structure analysis", {
          path: file.path,
          error: error instanceof Error ? error.message : String(error)
        });
      }
    }
    return structureCount;
  }
  /**
  * Truncate long file paths for display
  */
  truncatePath(filePath, maxLength) {
    if (filePath.length <= maxLength) {
      return filePath;
    }
    const ellipsis = "...";
    return filePath.substring(0, maxLength - ellipsis.length) + ellipsis;
  }
  /**
  * Sanitize filenames with special characters
  */
  sanitizeFilename(filename) {
    return filename.replace(/[@#$]+/g, " ").replace(/\s+/g, " ").trim();
  }
  /**
  * Format user-provided context into a snapshot name prefix
  */
  formatUserContext(context) {
    const presetMap = {
      "bug-fix": "fix",
      credentials: "chore",
      refactor: "refactor",
      testing: "test"
    };
    if (presetMap[context]) {
      return presetMap[context];
    }
    const cleaned = context.toLowerCase().replace(/[^a-z0-9\s-]/g, "").replace(/\s+/g, "-").trim();
    const maxLength = 20;
    if (cleaned.length > maxLength) {
      return cleaned.substring(0, maxLength);
    }
    return cleaned || "update";
  }
});
function compress(content) {
  return gzipSync(Buffer.from(content, "utf-8"), {
    level: 9
  });
}
__name(compress, "compress");
__name6(compress, "compress");
function generateId3() {
  const timestampPart = Date.now().toString(36);
  const randomPart = Math.random().toString(36).slice(2, 7).toLowerCase();
  return `snapshot_${timestampPart}_${randomPart}`;
}
__name(generateId3, "generateId");
__name6(generateId3, "generateId");
var ConnectionPool = class ConnectionPool2 {
  static {
    __name(this, "ConnectionPool2");
  }
  static {
    __name6(this, "ConnectionPool");
  }
  dbPath;
  dbOptions;
  connections = [];
  availableConnections = [];
  maxConnections;
  constructor(dbPath, dbOptions, size = 4) {
    this.dbPath = dbPath;
    this.dbOptions = dbOptions;
    this.maxConnections = size;
  }
  // Get a connection from the pool
  async getConnection() {
    if (this.availableConnections.length > 0) {
      const connection = this.availableConnections.pop();
      if (connection) {
        return connection;
      }
    }
    if (this.connections.length < this.maxConnections) {
      const db2 = await createDatabaseInstance(this.dbPath, this.dbOptions);
      db2.pragma("journal_mode = WAL");
      this.connections.push(db2);
      return db2;
    }
    const db = this.connections.shift();
    if (db) {
      this.connections.push(db);
      return db;
    }
    return await createDatabaseInstance(this.dbPath, this.dbOptions);
  }
  // Return a connection to the pool
  releaseConnection(_db) {
  }
  // Close all connections in the pool
  close() {
    for (const db of this.connections) {
      try {
        db.close();
      } catch (_error) {
      }
    }
    this.connections = [];
    this.availableConnections = [];
  }
};
var cachedDatabaseConstructor = null;
var cachedDatabaseError = null;
var tryLoadBetterSqlite3 = /* @__PURE__ */ __name6(async () => {
  if (cachedDatabaseConstructor) {
    return cachedDatabaseConstructor;
  }
  if (cachedDatabaseError) {
    return null;
  }
  try {
    const requiredModule = await import('better-sqlite3');
    const ctor = requiredModule.default;
    try {
      const probe = new ctor(":memory:");
      probe.close();
    } catch (instantiationError) {
      cachedDatabaseError = instantiationError instanceof Error ? instantiationError : new Error(String(instantiationError));
      return null;
    }
    cachedDatabaseConstructor = ctor;
    return cachedDatabaseConstructor;
  } catch (error) {
    cachedDatabaseError = error instanceof Error ? error : new Error(typeof error === "string" ? error : "Unknown error loading better-sqlite3");
    return null;
  }
}, "tryLoadBetterSqlite3");
var isBetterSqlite3Available = /* @__PURE__ */ __name6(async () => await tryLoadBetterSqlite3() !== null, "isBetterSqlite3Available");
var getBetterSqlite3LoadError = /* @__PURE__ */ __name6(() => cachedDatabaseError, "getBetterSqlite3LoadError");
var requireDatabaseConstructor = /* @__PURE__ */ __name6(async () => {
  const betterSqlite3 = await tryLoadBetterSqlite3();
  if (betterSqlite3) {
    return betterSqlite3;
  }
  const errorMessage2 = cachedDatabaseError ? `better-sqlite3: ${cachedDatabaseError.message}` : "better-sqlite3: not installed or not compatible";
  const detailedMessage = `No SQLite implementation available. ${errorMessage2}`;
  const error = new StorageConnectionError(detailedMessage);
  error.details = {
    betterSqlite3Error: cachedDatabaseError?.message
  };
  throw error;
}, "requireDatabaseConstructor");
var createDatabaseInstance = /* @__PURE__ */ __name6(async (pathToDatabase, options) => {
  const DatabaseCtor = await requireDatabaseConstructor();
  return new DatabaseCtor(pathToDatabase, options);
}, "createDatabaseInstance");
var StorageBroker = class {
  static {
    __name(this, "StorageBroker");
  }
  static {
    __name6(this, "StorageBroker");
  }
  dbPath;
  db = null;
  readConnectionPool = null;
  initialized = false;
  operationQueue = [];
  isProcessingQueue = false;
  writerId;
  constructor(dbPath) {
    this.dbPath = dbPath;
    this.writerId = `writer_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
  }
  /**
  * Initialize the storage broker and database connection
  */
  async initialize() {
    if (this.initialized) {
      return;
    }
    try {
      this.db = await createDatabaseInstance(this.dbPath);
      if (this.db && typeof this.db.pragma === "function") {
        this.db.pragma("journal_mode = WAL");
      }
      this.readConnectionPool = new ConnectionPool(this.dbPath, void 0, 4);
      this.runMigrations();
      this.initialized = true;
    } catch (error) {
      if (error instanceof StorageConnectionError) {
        throw error;
      }
      if (error instanceof Error) {
        const message = error.message;
        let specificError = "Failed to initialize storage broker";
        if (message.includes("sqlite")) {
          specificError = `Database initialization failed: ${message}`;
        }
        throw new StorageError2(specificError, "STORAGE_BROKER_INIT_ERROR", {
          cause: error
        });
      }
      throw new StorageError2("Failed to initialize storage broker", "STORAGE_BROKER_INIT_ERROR", {
        cause: error
      });
    }
  }
  /**
  * Get the database instance
  * @returns The database instance, or null if not initialized
  * @public This is intentionally public to allow direct database access for advanced operations
  */
  getDatabase() {
    return this.db;
  }
  /**
  * Run database migrations to ensure schema is up to date
  */
  runMigrations() {
    if (!this.db) {
      throw new StorageError2("Database not initialized", "STORAGE_NOT_INITIALIZED");
    }
    this.db.exec(`
      CREATE TABLE IF NOT EXISTS snapshots (
        id TEXT PRIMARY KEY,
        name TEXT NOT NULL,
        timestamp INTEGER NOT NULL,
        parent_id TEXT,
        metadata TEXT,
        FOREIGN KEY (parent_id) REFERENCES snapshots(id)
      );

      CREATE TABLE IF NOT EXISTS file_changes (
        snapshot_id TEXT NOT NULL,
        file_path TEXT NOT NULL,
        action TEXT CHECK(action IN ('add','modify','delete')),
        diff BLOB,
        storage_type TEXT DEFAULT 'diff',
        content_size INTEGER,
        PRIMARY KEY (snapshot_id, file_path),
        FOREIGN KEY (snapshot_id) REFERENCES snapshots(id) ON DELETE CASCADE
      );

      -- Add sessions table for session-aware snapshots
      CREATE TABLE IF NOT EXISTS sessions (
        id TEXT PRIMARY KEY,
        started_at INTEGER NOT NULL,
        ended_at INTEGER NOT NULL,
        reason TEXT NOT NULL,
        summary TEXT,
        tags TEXT,
        metadata TEXT
      );

      CREATE TABLE IF NOT EXISTS session_files (
        session_id TEXT NOT NULL,
        snapshot_id TEXT NOT NULL,
        file_path TEXT NOT NULL,
        added_count INTEGER DEFAULT 0,
        deleted_count INTEGER DEFAULT 0,
        PRIMARY KEY (session_id, file_path),
        FOREIGN KEY (session_id) REFERENCES sessions(id) ON DELETE CASCADE,
        FOREIGN KEY (snapshot_id) REFERENCES snapshots(id) ON DELETE CASCADE
      );

      -- Add queue persistence table for operation queuing
      CREATE TABLE IF NOT EXISTS queued_operations (
        id TEXT PRIMARY KEY,
        operation_name TEXT NOT NULL,
        priority INTEGER NOT NULL,
        created_at INTEGER NOT NULL,
        data BLOB,
        status TEXT CHECK(status IN ('pending','processing','completed','failed')) DEFAULT 'pending'
      );

      -- Single-column indexes
      CREATE INDEX IF NOT EXISTS idx_snapshot_timestamp ON snapshots(timestamp);
      CREATE INDEX IF NOT EXISTS idx_snapshot_parent ON snapshots(parent_id);
      CREATE INDEX IF NOT EXISTS idx_file_path ON file_changes(file_path);
      CREATE INDEX IF NOT EXISTS idx_session_started_at ON sessions(started_at);
      CREATE INDEX IF NOT EXISTS idx_session_ended_at ON sessions(ended_at);
      CREATE INDEX IF NOT EXISTS idx_session_reason ON sessions(reason);
      CREATE INDEX IF NOT EXISTS idx_queued_operations_priority ON queued_operations(priority);
      CREATE INDEX IF NOT EXISTS idx_queued_operations_status ON queued_operations(status);
      CREATE INDEX IF NOT EXISTS idx_queued_operations_created_at ON queued_operations(created_at);

      -- Covering indexes for common queries
      CREATE INDEX IF NOT EXISTS idx_snapshots_list
        ON snapshots(timestamp DESC, id, name);

      CREATE INDEX IF NOT EXISTS idx_file_changes_snapshot
        ON file_changes(snapshot_id, file_path, action);

      CREATE INDEX IF NOT EXISTS idx_file_changes_file_covering
        ON file_changes(file_path, snapshot_id)
        WHERE action != 'delete';

      -- Covering indexes for session queries
      CREATE INDEX IF NOT EXISTS idx_sessions_list
        ON sessions(ended_at DESC, id, reason);

      CREATE INDEX IF NOT EXISTS idx_session_files_session
        ON session_files(session_id, file_path);

      -- Covering index for queued operations
      CREATE INDEX IF NOT EXISTS idx_queued_operations_list
        ON queued_operations(priority ASC, created_at ASC, id, status)
        WHERE status = 'pending';

      -- Add writers_lock table for single-writer discipline across processes
      CREATE TABLE IF NOT EXISTS writers_lock (
        id INTEGER PRIMARY KEY CHECK (id = 1),
        writer_id TEXT,
        acquired_at INTEGER,
        expires_at INTEGER
      );

      -- Initialize the lock row if it doesn't exist
      INSERT OR IGNORE INTO writers_lock (id, writer_id, acquired_at, expires_at)
      VALUES (1, NULL, 0, 0);
    `);
  }
  /**
  * Close the storage broker and database connection
  */
  async close() {
    try {
      await this.processQueue();
      if (this.db) {
        this.db.close();
        this.db = null;
      }
      if (this.readConnectionPool) {
        this.readConnectionPool.close();
        this.readConnectionPool = null;
      }
      this.initialized = false;
    } catch (error) {
      throw new StorageError2("Failed to close storage broker", "STORAGE_BROKER_CLOSE_ERROR", {
        cause: error
      });
    }
  }
  /**
  * Get a read connection from the pool for concurrent read operations
  */
  async getReadConnection() {
    if (!this.readConnectionPool) {
      throw new StorageError2("Read connection pool not initialized", "STORAGE_NOT_INITIALIZED");
    }
    return await this.readConnectionPool.getConnection();
  }
  /**
  * Check if better-sqlite3 is available
  */
  static async isAvailable() {
    return await isBetterSqlite3Available();
  }
  /**
  * Get the error that occurred when loading better-sqlite3, if any
  */
  static getLoadError() {
    return getBetterSqlite3LoadError();
  }
  /**
  * Attempt to acquire a distributed lock for writing
  * @param writerId Unique identifier for this writer process
  * @param timeoutMs Maximum time to wait for the lock in milliseconds
  * @returns True if lock was acquired, false otherwise
  */
  async acquireLock(writerId, timeoutMs = 5e3) {
    if (!this.db) {
      throw new StorageError2("Database not initialized", "STORAGE_NOT_INITIALIZED");
    }
    const startTime = Date.now();
    const maxWaitTime = timeoutMs;
    let waitTime = 10;
    while (Date.now() - startTime < maxWaitTime) {
      try {
        const result7 = this.db.prepare(`
					UPDATE writers_lock
					SET writer_id = ?, acquired_at = ?, expires_at = ?
					WHERE (writer_id IS NULL OR expires_at < ?) AND id = 1
				`).run(writerId, Date.now(), Date.now() + 3e4, Date.now());
        if (result7.changes === 1) {
          return true;
        }
        await new Promise((resolve22) => setTimeout(resolve22, Math.min(waitTime, maxWaitTime - (Date.now() - startTime))));
        waitTime = Math.min(waitTime * 2, 1e3);
      } catch (_error) {
        await new Promise((resolve22) => setTimeout(resolve22, Math.min(waitTime, maxWaitTime - (Date.now() - startTime))));
        waitTime = Math.min(waitTime * 2, 1e3);
      }
    }
    return false;
  }
  /**
  * Release the distributed lock
  * @param writerId Unique identifier for this writer process
  */
  async releaseLock(writerId) {
    if (!this.db) {
      throw new StorageError2("Database not initialized", "STORAGE_NOT_INITIALIZED");
    }
    try {
      this.db.prepare(`
				UPDATE writers_lock
				SET writer_id = NULL, acquired_at = 0, expires_at = 0
				WHERE writer_id = ? AND id = 1
			`).run(writerId);
    } catch (_error) {
    }
  }
  /**
  * Create a snapshot through the queued operation system to ensure single-writer discipline
  * @param name The snapshot name
  * @param files Map of file paths to content
  * @param metadata Optional metadata
  * @param parentId Optional parent snapshot ID
  * @param id Optional snapshot ID (if not provided, one will be generated)
  * @returns Snapshot information
  */
  async createSnapshot(name, files, metadata, parentId, id) {
    return this.queueOperation("createSnapshot", async () => {
      if (!this.db) {
        throw new StorageError2("Database not initialized", "STORAGE_NOT_INITIALIZED");
      }
      const snapshotId = id || generateId3();
      const timestamp = Date.now();
      const preCompressedFiles = /* @__PURE__ */ new Map();
      Array.from(files.entries()).forEach(([filePath, content]) => {
        const contentSize = Buffer.byteLength(content, "utf-8");
        const compressed = compress(content);
        preCompressedFiles.set(filePath, {
          compressed,
          storageType: "full",
          contentSize
        });
      });
      try {
        const insert = this.db.transaction(() => {
          try {
            if (this.db) {
              this.db.prepare(`
					INSERT INTO snapshots (id, name, timestamp, parent_id, metadata)
					VALUES (?, ?, ?, ?, ?)
				`).run(snapshotId, name, timestamp, parentId || null, JSON.stringify({
                fileCount: files.size,
                createdBy: "snapback",
                ...metadata
              }));
              if (this.db) {
                const stmt = this.db.prepare(`
					INSERT INTO file_changes (snapshot_id, file_path, action, diff, storage_type, content_size)
					VALUES (?, ?, ?, ?, ?, ?)
				`);
                preCompressedFiles.forEach((preCompressed, filePath) => {
                  const { compressed, storageType, contentSize } = preCompressed;
                  const action = "add";
                  stmt.run(snapshotId, filePath, action, compressed, storageType, contentSize);
                });
              }
            }
          } catch (queryError) {
            const message = queryError instanceof Error ? queryError.message : String(queryError);
            throw new StorageError2(`Failed to create snapshot: ${message}`, "STORAGE_QUERY_ERROR", {
              cause: queryError
            });
          }
        });
        insert();
        return {
          id: snapshotId,
          name,
          fileCount: files.size,
          timestamp
        };
      } catch (error) {
        if (error instanceof StorageError2 || error instanceof StorageTransactionError) {
          throw error;
        }
        const message = error instanceof Error ? error.message : String(error);
        throw new StorageError2(`Failed to create snapshot: ${message}`, "STORAGE_CREATE_SNAPSHOT_ERROR", {
          cause: error
        });
      }
    });
  }
  /**
  * Queue an operation for execution
  * @param operationName Name of the operation for logging
  * @param operation Function to execute
  * @param priority Priority of the operation (lower number = higher priority)
  * @returns Promise that resolves when the operation completes
  */
  async queueOperation(operationName, operation, priority = 0) {
    if (!this.db) {
      throw new StorageError2("Database not initialized", "STORAGE_NOT_INITIALIZED");
    }
    return new Promise((resolve22, reject) => {
      const id = generateId3();
      const timestamp = Date.now();
      const queuedOp = {
        id,
        operationName,
        priority,
        operation,
        resolve: resolve22,
        reject,
        timestamp
      };
      try {
        if (this.db) {
          const stmt = this.db.prepare(`
            INSERT INTO queued_operations (id, operation_name, priority, created_at, status)
            VALUES (?, ?, ?, ?, ?)
          `);
          stmt.run(id, operationName, priority, timestamp, "pending");
        }
        this.operationQueue.push(queuedOp);
        this.operationQueue.sort((a, b) => {
          if (a.priority !== b.priority) {
            return a.priority - b.priority;
          }
          return a.timestamp - b.timestamp;
        });
        if (!this.isProcessingQueue) {
          this.processQueue();
        }
      } catch (error) {
        reject(error);
      }
    });
  }
  /**
  * Process the operation queue with single-writer discipline
  */
  async processQueue() {
    if (this.isProcessingQueue) {
      return;
    }
    this.isProcessingQueue = true;
    try {
      if (this.operationQueue.length > 0) {
        const lockAcquired = await this.acquireLock(this.writerId, 1e4);
        if (!lockAcquired) {
          setTimeout(() => {
            this.isProcessingQueue = false;
            if (this.operationQueue.length > 0) {
              this.processQueue();
            }
          }, 100);
          return;
        }
        try {
          this.operationQueue.sort((a, b) => {
            if (a.priority !== b.priority) {
              return a.priority - b.priority;
            }
            return a.timestamp - b.timestamp;
          });
          if (this.operationQueue.length > 0) {
            const queuedOp = this.operationQueue.shift();
            if (queuedOp) {
              try {
                const result7 = await queuedOp.operation();
                queuedOp.resolve(result7);
              } catch (error) {
                queuedOp.reject(error);
              }
            }
          }
        } finally {
          await this.releaseLock(this.writerId);
        }
        await new Promise((resolve22) => setTimeout(resolve22, 1));
      }
    } catch (_error) {
    } finally {
      this.isProcessingQueue = false;
      if (this.operationQueue.length > 0) {
        setTimeout(() => {
          this.processQueue();
        }, 1);
      }
    }
  }
  /**
  * Get a snapshot by ID
  * @param id The snapshot ID
  * @returns Snapshot information or null if not found
  */
  async getSnapshot(id) {
    if (!this.db) {
      throw new StorageError2("Database not initialized", "STORAGE_NOT_INITIALIZED");
    }
    const db = await this.getReadConnection();
    try {
      const row = db.prepare(`
				SELECT id, name, timestamp, metadata
				FROM snapshots
				WHERE id = ?
			`).get(id);
      if (!row) {
        return null;
      }
      const fileRows = db.prepare(`
				SELECT file_path, diff, storage_type
				FROM file_changes
				WHERE snapshot_id = ?
			`).all(id);
      const files = /* @__PURE__ */ new Map();
      for (const fileRow of fileRows) {
        try {
          const typedFileRow = fileRow;
          const decompressed = gunzipSync(typedFileRow.diff);
          const content = decompressed.toString("utf-8");
          files.set(typedFileRow.file_path, content);
        } catch (_error) {
          const _typedFileRow = fileRow;
        }
      }
      return {
        id: row.id,
        name: row.name,
        files,
        timestamp: row.timestamp,
        metadata: row.metadata
      };
    } catch (error) {
      throw new StorageError2(`Failed to get snapshot: ${error instanceof Error ? error.message : String(error)}`, "STORAGE_GET_SNAPSHOT_ERROR", {
        cause: error
      });
    }
  }
  /**
  * List snapshots with optional filters
  * @param limit Maximum number of snapshots to return
  * @param offset Offset for pagination
  * @param sortBy Field to sort by
  * @param sortOrder Sort order (ASC or DESC)
  * @returns List of snapshots
  */
  async listSnapshots(limit = 50, offset = 0, sortBy = "timestamp", sortOrder = "DESC") {
    if (!this.db) {
      throw new StorageError2("Database not initialized", "STORAGE_NOT_INITIALIZED");
    }
    const db = await this.getReadConnection();
    let validatedSortBy = sortBy;
    let validatedSortOrder = sortOrder;
    try {
      const validSortColumns = [
        "timestamp",
        "name",
        "id"
      ];
      if (!validSortColumns.includes(validatedSortBy)) {
        validatedSortBy = "timestamp";
      }
      if (validatedSortOrder !== "ASC" && validatedSortOrder !== "DESC") {
        validatedSortOrder = "DESC";
      }
      const rows = db.prepare(`
				SELECT id, name, timestamp, metadata
				FROM snapshots
				ORDER BY ${validatedSortBy} ${validatedSortOrder}
				LIMIT ? OFFSET ?
			`).all(limit, offset);
      return rows.map((row) => ({
        id: row.id,
        name: row.name,
        timestamp: row.timestamp,
        metadata: row.metadata
      }));
    } catch (error) {
      throw new StorageError2(`Failed to list snapshots: ${error instanceof Error ? error.message : String(error)}`, "STORAGE_LIST_SNAPSHOTS_ERROR", {
        cause: error
      });
    }
  }
  /**
  * Delete a snapshot by ID
  * @param id The snapshot ID
  */
  async deleteSnapshot(id) {
    return this.queueOperation(`deleteSnapshot-${id}`, async () => {
      if (!this.db) {
        throw new StorageError2("Database not initialized", "STORAGE_NOT_INITIALIZED");
      }
      try {
        this.db.prepare(`
					DELETE FROM snapshots
					WHERE id = ?
				`).run(id);
      } catch (error) {
        throw new StorageError2(`Failed to delete snapshot: ${error instanceof Error ? error.message : String(error)}`, "STORAGE_DELETE_SNAPSHOT_ERROR", {
          cause: error
        });
      }
    });
  }
};
(class {
  static {
    __name(this, "StorageBrokerAdapter");
  }
  static {
    __name6(this, "StorageBrokerAdapter");
  }
  broker;
  constructor(dbPath) {
    this.broker = new StorageBroker(dbPath);
  }
  async initialize() {
    await this.broker.initialize();
  }
  async save(snapshot, contentHash) {
    const filesMap = /* @__PURE__ */ new Map();
    if (snapshot.fileContents) {
      for (const [filePath, content] of Object.entries(snapshot.fileContents)) {
        filesMap.set(filePath, content);
      }
    }
    const name = snapshot.meta?.name || snapshot.meta?.trigger || "snapshot";
    const metadata = {
      ...snapshot.meta,
      contentHash
    };
    await this.broker.createSnapshot(name, filesMap, metadata, void 0, snapshot.id);
  }
  async get(id) {
    try {
      const result7 = await this.broker.getSnapshot(id);
      if (!result7) {
        return null;
      }
      let metadata = {};
      try {
        metadata = JSON.parse(result7.metadata);
      } catch (_error) {
      }
      const fileContents = {};
      for (const [filePath, content] of result7.files) {
        fileContents[filePath] = content;
      }
      return {
        id: result7.id,
        timestamp: result7.timestamp,
        version: "1.0",
        meta: metadata,
        files: Array.from(result7.files.keys()),
        fileContents
      };
    } catch (_error) {
      return null;
    }
  }
  async list(filters) {
    try {
      const limit = filters?.limit || 50;
      const offset = filters?.offset || 0;
      const snapshots = await this.broker.listSnapshots(limit, offset, "timestamp", "DESC");
      const result7 = [];
      for (const snapshot of snapshots) {
        let metadata = {};
        try {
          metadata = JSON.parse(snapshot.metadata);
        } catch (_error) {
        }
        result7.push({
          id: snapshot.id,
          timestamp: snapshot.timestamp,
          version: "1.0",
          meta: metadata,
          files: [],
          fileContents: {}
        });
      }
      return result7;
    } catch (_error) {
      return [];
    }
  }
  async delete(id) {
    try {
      await this.broker.deleteSnapshot(id);
    } catch (_error) {
    }
  }
  async close() {
    try {
      await this.broker.close();
    } catch (_error) {
    }
  }
});
function sha256(input) {
  return crypto2__default__default.createHash("sha256").update(input, "utf-8").digest("hex");
}
__name(sha256, "sha256");
__name6(sha256, "sha256");
function hashContent(content) {
  return sha256(content);
}
__name(hashContent, "hashContent");
__name6(hashContent, "hashContent");
function hashFilePath(filePath) {
  return sha256(filePath);
}
__name(hashFilePath, "hashFilePath");
__name6(hashFilePath, "hashFilePath");
function hashWorkspaceId(workspaceId) {
  return sha256(workspaceId);
}
__name(hashWorkspaceId, "hashWorkspaceId");
__name6(hashWorkspaceId, "hashWorkspaceId");
function getBlobPath(hash, levels = 2) {
  const segments = [];
  for (let i = 0; i < levels; i++) {
    segments.push(hash.slice(i * 2, (i + 1) * 2));
  }
  segments.push(hash);
  return segments.join("/");
}
__name(getBlobPath, "getBlobPath");
__name6(getBlobPath, "getBlobPath");
function normalize(path11) {
  let normalized = path11.replace(/\\/g, "/");
  normalized = normalized.replace(/\/+/g, "/");
  normalized = normalized.replace(/\/+$/, "");
  return normalized === "" && path11.startsWith("/") ? "/" : normalized;
}
__name(normalize, "normalize");
__name6(normalize, "normalize");
function isWithin(childPath, parentPath) {
  const normalizedChild = normalize(childPath);
  const normalizedParent = normalize(parentPath);
  return normalizedChild.startsWith(`${normalizedParent}/`) || normalizedChild === normalizedParent;
}
__name(isWithin, "isWithin");
__name6(isWithin, "isWithin");
function getDepth(path11) {
  const normalized = normalize(path11);
  if (normalized === "/") {
    return 1;
  }
  const segments = normalized.split("/").filter((segment) => segment.length > 0);
  const isUnixPath = path11.startsWith("/");
  return isUnixPath ? segments.length + 1 : segments.length;
}
__name(getDepth, "getDepth");
__name6(getDepth, "getDepth");
function areEqual(path1, path22) {
  const norm1 = normalize(path1);
  const norm2 = normalize(path22);
  const isWindows = /^[a-zA-Z]:/.test(norm1) || /^[a-zA-Z]:/.test(norm2);
  if (isWindows) {
    return norm1.toLowerCase() === norm2.toLowerCase();
  }
  return norm1 === norm2;
}
__name(areEqual, "areEqual");
__name6(areEqual, "areEqual");
createLogger({
  name: "llm-cache",
  level: LogLevel.INFO
});
var logger3 = createLogger({
  name: "pattern-cache",
  level: LogLevel.INFO
});
var PATTERN_CACHE_TTL = 24 * 60 * 60;
var CACHE_PREFIX = "cache:pat:";
var cacheStats = {
  hits: 0,
  misses: 0,
  totalRequests: 0,
  get hitRate() {
    return this.totalRequests > 0 ? this.hits / this.totalRequests * 100 : 0;
  }
};
function generatePatternCacheKey(key) {
  const { query, intent = "", packages = [], options = {} } = key;
  const normalizedQuery = query.trim().toLowerCase();
  const sortedPackages = [
    ...packages
  ].sort();
  const keyMaterial = JSON.stringify({
    query: normalizedQuery,
    intent,
    packages: sortedPackages,
    ...options
  });
  const hash = createHash("sha256").update(keyMaterial).digest("hex");
  return `${CACHE_PREFIX}${hash}`;
}
__name(generatePatternCacheKey, "generatePatternCacheKey");
async function getCachedPatterns(key) {
  if (!isRedisAvailable()) {
    return null;
  }
  const cacheKey = generatePatternCacheKey(key);
  try {
    const cached = await getCache(cacheKey);
    if (cached) {
      cacheStats.hits++;
      cacheStats.totalRequests++;
      logger3.info("Pattern cache HIT", {
        query: key.query.substring(0, 50),
        intent: key.intent,
        cacheAge: Date.now() - cached.cachedAt,
        hitRate: cacheStats.hitRate.toFixed(1)
      });
      return cached;
    }
    cacheStats.misses++;
    cacheStats.totalRequests++;
    logger3.debug("Pattern cache MISS", {
      query: key.query.substring(0, 50),
      hitRate: cacheStats.hitRate.toFixed(1)
    });
    return null;
  } catch (error) {
    logger3.error("Pattern cache check failed", {
      error,
      cacheKey
    });
    return null;
  }
}
__name(getCachedPatterns, "getCachedPatterns");
async function cachePatternResults(key, result7) {
  if (!isRedisAvailable()) {
    return false;
  }
  const cacheKey = generatePatternCacheKey(key);
  const cachedResult = {
    ...result7,
    cachedAt: Date.now()
  };
  try {
    const success = await setCache(cacheKey, cachedResult, PATTERN_CACHE_TTL);
    if (success) {
      logger3.debug("Pattern results cached", {
        query: key.query.substring(0, 50),
        resultsCount: result7.results.length,
        ttl: PATTERN_CACHE_TTL
      });
    }
    return success;
  } catch (error) {
    logger3.error("Failed to cache pattern results", {
      error,
      cacheKey
    });
    return false;
  }
}
__name(cachePatternResults, "cachePatternResults");
async function withPatternCache(key, retrievalFn) {
  const cached = await getCachedPatterns(key);
  if (cached) {
    return cached;
  }
  const result7 = await retrievalFn();
  cachePatternResults(key, result7).catch((error) => {
    logger3.warn("Failed to cache pattern results (non-blocking)", {
      error
    });
  });
  return result7;
}
__name(withPatternCache, "withPatternCache");

// ../../packages/platform/dist/cache/session-cache.js
createLogger({
  name: "session-cache",
  level: LogLevel.INFO
});

// ../../packages/sdk/dist/index.js
var __defProp6 = Object.defineProperty;
var __name7 = /* @__PURE__ */ __name((target, value) => __defProp6(target, "name", {
  value,
  configurable: true
}), "__name");
var __require2 = /* @__PURE__ */ ((x) => typeof __require !== "undefined" ? __require : typeof Proxy !== "undefined" ? new Proxy(x, {
  get: /* @__PURE__ */ __name((a, b) => (typeof __require !== "undefined" ? __require : a)[b], "get")
}) : x)(function(x) {
  if (typeof __require !== "undefined") return __require.apply(this, arguments);
  throw Error('Dynamic require of "' + x + '" is not supported');
});
var RISK_FACTOR_DESCRIPTIONS = {
  "eval execution": "Dynamic code execution detected - eval() allows runtime code execution",
  "sql injection": "SQL injection vulnerability pattern - concatenated user input in queries",
  "command execution": "Dangerous shell command usage - potential OS command injection",
  "hardcoded secret": "Potential secret/credential found in code - API keys, tokens exposed",
  "auth bypass": "Authentication bypass pattern - insufficient access control checks",
  "path traversal": "Directory traversal vulnerability - unrestricted path access",
  "xss pattern": "Cross-site scripting vulnerability - unsanitized user input in DOM",
  deserialization: "Unsafe deserialization detected - potential object injection",
  cryptography: "Weak cryptography usage - deprecated algorithms or insufficient key length",
  "dependency change": "Dependency version change - verify no breaking changes or vulnerabilities"
};
function describeRiskFactors(factors) {
  return factors.map((factor) => RISK_FACTOR_DESCRIPTIONS[factor.toLowerCase()] || factor);
}
__name(describeRiskFactors, "describeRiskFactors");
__name7(describeRiskFactors, "describeRiskFactors");
function describeRiskFactor(factor) {
  return RISK_FACTOR_DESCRIPTIONS[factor.toLowerCase()] || factor;
}
__name(describeRiskFactor, "describeRiskFactor");
__name7(describeRiskFactor, "describeRiskFactor");
function isKnownRiskFactor(factor) {
  return factor.toLowerCase() in RISK_FACTOR_DESCRIPTIONS;
}
__name(isKnownRiskFactor, "isKnownRiskFactor");
__name7(isKnownRiskFactor, "isKnownRiskFactor");
function getStandardRiskFactors() {
  return Object.keys(RISK_FACTOR_DESCRIPTIONS);
}
__name(getStandardRiskFactors, "getStandardRiskFactors");
__name7(getStandardRiskFactors, "getStandardRiskFactors");
function createDashboardMetricsClient(orpcClient) {
  return {
    async getDashboardMetrics() {
      return orpcClient.dashboard.getMetrics();
    }
  };
}
__name(createDashboardMetricsClient, "createDashboardMetricsClient");
__name7(createDashboardMetricsClient, "createDashboardMetricsClient");
var DiffCalculator = class {
  static {
    __name(this, "DiffCalculator");
  }
  static {
    __name7(this, "DiffCalculator");
  }
  /**
  * Calculate diff between current file and snapshot content
  */
  calculateFileDiff(filePath, currentContent, snapshotContent) {
    let operation;
    if (currentContent === null && snapshotContent !== null) {
      operation = "create";
    } else if (currentContent !== null && snapshotContent === null) {
      operation = "delete";
    } else {
      operation = "modify";
    }
    const currentLines = currentContent ? currentContent.split("\n") : [];
    const snapshotLines = snapshotContent ? snapshotContent.split("\n") : [];
    const { added, removed, preview } = this.computeLineDiff(currentLines, snapshotLines);
    const currentChecksum = currentContent ? this.calculateChecksum(currentContent) : void 0;
    const snapshotChecksum = snapshotContent ? this.calculateChecksum(snapshotContent) : void 0;
    return {
      path: filePath,
      operation,
      linesAdded: added,
      linesRemoved: removed,
      preview,
      currentChecksum,
      snapshotChecksum
    };
  }
  /**
  * Compute line-by-line diff with preview
  * Uses simple LCS algorithm for readable diffs
  */
  computeLineDiff(currentLines, snapshotLines) {
    const lcs = this.longestCommonSubsequence(currentLines, snapshotLines);
    const previewLines = [];
    let added = 0;
    let removed = 0;
    let i = 0;
    let j = 0;
    let previewLineCount = 0;
    const maxPreviewLines = 20;
    for (let k = 0; k < lcs.length && previewLineCount < maxPreviewLines; k++) {
      while (i < currentLines.length && currentLines[i] !== lcs[k]) {
        previewLines.push(`- ${currentLines[i]}`);
        removed++;
        i++;
        previewLineCount++;
        if (previewLineCount >= maxPreviewLines) {
          break;
        }
      }
      while (j < snapshotLines.length && snapshotLines[j] !== lcs[k] && previewLineCount < maxPreviewLines) {
        previewLines.push(`+ ${snapshotLines[j]}`);
        added++;
        j++;
        previewLineCount++;
        if (previewLineCount >= maxPreviewLines) {
          break;
        }
      }
      if (previewLineCount < maxPreviewLines) {
        previewLines.push(`  ${lcs[k]}`);
        previewLineCount++;
      }
      i++;
      j++;
    }
    while (i < currentLines.length && previewLineCount < maxPreviewLines) {
      previewLines.push(`- ${currentLines[i]}`);
      removed++;
      i++;
      previewLineCount++;
    }
    while (j < snapshotLines.length && previewLineCount < maxPreviewLines) {
      previewLines.push(`+ ${snapshotLines[j]}`);
      added++;
      j++;
      previewLineCount++;
    }
    added += snapshotLines.length - j;
    removed += currentLines.length - i;
    if (previewLineCount >= maxPreviewLines && (i < currentLines.length || j < snapshotLines.length)) {
      previewLines.push("... (preview truncated)");
    }
    return {
      added,
      removed,
      preview: previewLines.join("\n")
    };
  }
  /**
  * Longest Common Subsequence for diff calculation
  */
  longestCommonSubsequence(arr1, arr2) {
    const m = arr1.length;
    const n = arr2.length;
    const dp = Array.from({
      length: m + 1
    }, () => Array(n + 1).fill(0));
    for (let i2 = 1; i2 <= m; i2++) {
      for (let j2 = 1; j2 <= n; j2++) {
        if (arr1[i2 - 1] === arr2[j2 - 1]) {
          dp[i2][j2] = dp[i2 - 1][j2 - 1] + 1;
        } else {
          dp[i2][j2] = Math.max(dp[i2 - 1][j2], dp[i2][j2 - 1]);
        }
      }
    }
    const lcs = [];
    let i = m;
    let j = n;
    while (i > 0 && j > 0) {
      if (arr1[i - 1] === arr2[j - 1]) {
        lcs.unshift(arr1[i - 1]);
        i--;
        j--;
      } else if (dp[i - 1][j] > dp[i][j - 1]) {
        i--;
      } else {
        j--;
      }
    }
    return lcs;
  }
  /**
  * Calculate SHA-256 checksum for content
  */
  calculateChecksum(content) {
    const crypto3 = __require2("crypto");
    return crypto3.createHash("sha256").update(content).digest("hex");
  }
  /**
  * Generate full diff preview for multiple files
  */
  generateDiffPreview(diffs) {
    let filesCreated = 0;
    let filesModified = 0;
    let filesDeleted = 0;
    let totalLinesAdded = 0;
    let totalLinesRemoved = 0;
    for (const diff of diffs) {
      switch (diff.operation) {
        case "create":
          filesCreated++;
          break;
        case "modify":
          filesModified++;
          break;
        case "delete":
          filesDeleted++;
          break;
      }
      totalLinesAdded += diff.linesAdded;
      totalLinesRemoved += diff.linesRemoved;
    }
    return {
      totalFiles: diffs.length,
      filesCreated,
      filesModified,
      filesDeleted,
      totalLinesAdded,
      totalLinesRemoved,
      diffs
    };
  }
};
function isConsumptionAllowed(result7) {
  return result7.allowed === true;
}
__name(isConsumptionAllowed, "isConsumptionAllowed");
__name7(isConsumptionAllowed, "isConsumptionAllowed");
function isConsumptionDenied(result7) {
  return result7.allowed === false;
}
__name(isConsumptionDenied, "isConsumptionDenied");
__name7(isConsumptionDenied, "isConsumptionDenied");
var TokenBucket = class {
  static {
    __name(this, "TokenBucket");
  }
  static {
    __name7(this, "TokenBucket");
  }
  state;
  constructor(config2) {
    if (config2.capacity <= 0) {
      throw new Error("Capacity must be greater than 0");
    }
    if (config2.refillRate <= 0) {
      throw new Error("Refill rate must be greater than 0");
    }
    this.state = {
      tokens: config2.capacity,
      capacity: config2.capacity,
      refillRate: config2.refillRate,
      lastRefill: Date.now()
    };
  }
  /**
  * Attempt to consume tokens from the bucket
  *
  * @param amount - Number of tokens to consume
  * @returns Result indicating if consumption was allowed
  *
  * @throws If amount is not positive
  */
  tryConsume(amount) {
    if (amount <= 0) {
      throw new Error("Token amount must be greater than 0");
    }
    this.refill();
    if (this.state.tokens >= amount) {
      this.state.tokens -= amount;
      return {
        allowed: true,
        tokensRemaining: this.state.tokens,
        resetAt: this.calculateResetTime()
      };
    }
    return {
      allowed: false,
      tokensRemaining: this.state.tokens,
      resetAt: this.calculateResetTime()
    };
  }
  /**
  * Refill tokens based on time elapsed since last refill
  */
  refill() {
    const now = Date.now();
    const timePassed = (now - this.state.lastRefill) / 1e3;
    const tokensToAdd = timePassed * this.state.refillRate;
    this.state.tokens = Math.min(this.state.capacity, this.state.tokens + tokensToAdd);
    this.state.lastRefill = now;
  }
  /**
  * Get the current state of the bucket
  */
  getState() {
    this.refill();
    return {
      ...this.state
    };
  }
  /**
  * Reset bucket to full capacity
  */
  reset() {
    this.state.tokens = this.state.capacity;
    this.state.lastRefill = Date.now();
  }
  /**
  * Get consumption information
  */
  getConsumptionInfo() {
    this.refill();
    const consumed = this.state.capacity - this.state.tokens;
    const percentageUsed = consumed / this.state.capacity * 100;
    return {
      consumed,
      remaining: this.state.tokens,
      percentageUsed
    };
  }
  /**
  * Calculate when the bucket will be full again
  */
  calculateResetTime() {
    const tokensNeeded = this.state.capacity - this.state.tokens;
    const secondsToReset = tokensNeeded / this.state.refillRate;
    return new Date(Date.now() + secondsToReset * 1e3);
  }
};
function createTokenBucket(config2) {
  return new TokenBucket(config2);
}
__name(createTokenBucket, "createTokenBucket");
__name7(createTokenBucket, "createTokenBucket");
function isRateLimitAllowed(result7) {
  return result7.allowed === true;
}
__name(isRateLimitAllowed, "isRateLimitAllowed");
__name7(isRateLimitAllowed, "isRateLimitAllowed");
function isRateLimitDenied(result7) {
  return result7.allowed === false;
}
__name(isRateLimitDenied, "isRateLimitDenied");
__name7(isRateLimitDenied, "isRateLimitDenied");
var RateLimiter = class {
  static {
    __name(this, "RateLimiter");
  }
  static {
    __name7(this, "RateLimiter");
  }
  plans;
  buckets = /* @__PURE__ */ new Map();
  constructor(plans) {
    if (!plans || Object.keys(plans).length === 0) {
      throw new Error("At least one plan must be configured");
    }
    for (const [planName, config2] of Object.entries(plans)) {
      if (config2.capacity <= 0) {
        throw new Error(`Plan "${planName}" has invalid capacity: ${config2.capacity}`);
      }
      if (config2.refillRate <= 0) {
        throw new Error(`Plan "${planName}" has invalid refill rate: ${config2.refillRate}`);
      }
    }
    this.plans = plans;
  }
  /**
  * Check rate limit for a user and plan tier
  *
  * @param userId - Unique user identifier
  * @param planName - Plan tier name (e.g., "free", "pro")
  * @returns Rate limit result (discriminated union)
  *
  * @throws If plan doesn't exist
  *
  * @example
  * ```typescript
  * const result = await limiter.checkLimit('user1', 'free');
  *
  * if (isRateLimitAllowed(result)) {
  *   // TypeScript knows result.remaining exists
  *   logger.debug(...);
  * } else {
  *   // TypeScript knows result.retryAfter exists
  *   logger.debug(...);
  * }
  * ```
  */
  async checkLimit(userId, planName) {
    const plan = this.plans[planName];
    if (!plan) {
      throw new Error(`Unknown plan: ${planName}`);
    }
    const bucketKey = `${userId}:${planName}`;
    let bucket = this.buckets.get(bucketKey);
    if (!bucket) {
      bucket = new TokenBucket(plan);
      this.buckets.set(bucketKey, bucket);
    }
    const consumeResult = bucket.tryConsume(1);
    if (isConsumptionAllowed(consumeResult)) {
      return {
        allowed: true,
        remaining: Math.floor(consumeResult.tokensRemaining),
        limit: plan.capacity,
        resetAt: consumeResult.resetAt
      };
    }
    const now = Date.now();
    const resetTime = consumeResult.resetAt.getTime();
    const retryAfter = Math.ceil((resetTime - now) / 1e3);
    return {
      allowed: false,
      remaining: Math.floor(consumeResult.tokensRemaining),
      limit: plan.capacity,
      resetAt: consumeResult.resetAt,
      retryAfter: Math.max(1, retryAfter)
    };
  }
  /**
  * Get current state of a user's bucket for a plan
  *
  * @param userId - User identifier
  * @param planName - Plan name
  * @returns Current bucket state or undefined if not created yet
  */
  getBucketState(userId, planName) {
    const bucketKey = `${userId}:${planName}`;
    const bucket = this.buckets.get(bucketKey);
    return bucket?.getState();
  }
  /**
  * Reset a user's bucket for a plan
  *
  * @param userId - User identifier
  * @param planName - Plan name
  */
  resetBucket(userId, planName) {
    const bucketKey = `${userId}:${planName}`;
    const bucket = this.buckets.get(bucketKey);
    if (bucket) {
      bucket.reset();
    }
  }
  /**
  * Clear all buckets (use cautiously)
  */
  clearAll() {
    this.buckets.clear();
  }
  /**
  * Get total number of active buckets
  */
  getActiveBucketCount() {
    return this.buckets.size;
  }
};
(class {
  static {
    __name(this, "DistributedTokenManager");
  }
  static {
    __name7(this, "DistributedTokenManager");
  }
  redis;
  plans;
  keyPrefix;
  ttlSeconds;
  fallbackToInMemory;
  fallbackLimiter = null;
  constructor(options) {
    if (!options.redisClient) {
      throw new Error("Redis client is required");
    }
    if (!options.plans || Object.keys(options.plans).length === 0) {
      throw new Error("At least one plan must be configured");
    }
    this.redis = options.redisClient;
    this.plans = options.plans;
    this.keyPrefix = options.keyPrefix || "ratelimit:";
    this.ttlSeconds = options.ttlSeconds || 3600;
    this.fallbackToInMemory = options.fallbackToInMemory !== false;
    if (this.fallbackToInMemory) {
      this.fallbackLimiter = new RateLimiter(this.plans);
    }
  }
  /**
  * Check rate limit using Redis with fallback to in-memory
  *
  * @param userId - User identifier
  * @param planName - Plan name
  * @returns Rate limit result with backend info
  */
  async checkLimit(userId, planName) {
    const plan = this.plans[planName];
    if (!plan) {
      throw new Error(`Unknown plan: ${planName}`);
    }
    try {
      return await this.checkLimitViaRedis(userId, planName, plan);
    } catch (error) {
      if (this.fallbackToInMemory) {
        return await this.checkLimitViaFallback(userId, planName);
      }
      throw error;
    }
  }
  /**
  * Check limit using Redis backend
  */
  async checkLimitViaRedis(userId, planName, plan) {
    const key = this.getRedisKey(userId, planName);
    const bucket = await this.getOrCreateBucket(key, plan);
    const consumeResult = bucket.tryConsume(1);
    try {
      await this.redis.set(key, JSON.stringify(bucket.getState()));
      await this.redis.expire(key, this.ttlSeconds);
    } catch (error) {
      if (this.fallbackToInMemory) {
        return await this.checkLimitViaFallback(userId, planName);
      }
      throw error;
    }
    if (consumeResult.allowed) {
      return {
        allowed: true,
        remaining: Math.floor(consumeResult.tokensRemaining),
        limit: plan.capacity,
        resetAt: consumeResult.resetAt,
        reason: "redis"
      };
    }
    const now = Date.now();
    const resetTime = consumeResult.resetAt.getTime();
    const retryAfter = Math.ceil((resetTime - now) / 1e3);
    return {
      allowed: false,
      remaining: Math.floor(consumeResult.tokensRemaining),
      limit: plan.capacity,
      resetAt: consumeResult.resetAt,
      reason: "redis",
      retryAfter: Math.max(1, retryAfter)
    };
  }
  /**
  * Check limit using in-memory fallback
  */
  async checkLimitViaFallback(userId, planName) {
    if (!this.fallbackLimiter) {
      throw new Error("Fallback limiter not initialized");
    }
    const result7 = await this.fallbackLimiter.checkLimit(userId, planName);
    return {
      ...result7,
      reason: "fallback"
    };
  }
  /**
  * Get or create bucket from Redis
  */
  async getOrCreateBucket(key, plan) {
    const bucketData = await this.redis.get(key);
    if (bucketData) {
      try {
        const state = JSON.parse(bucketData);
        const bucket = new TokenBucket(plan);
        Object.assign(bucket.state || {}, state);
        return bucket;
      } catch (_error) {
        return new TokenBucket(plan);
      }
    }
    return new TokenBucket(plan);
  }
  /**
  * Get Redis key for user + plan
  */
  getRedisKey(userId, planName) {
    return `${this.keyPrefix}${userId}:${planName}`;
  }
  /**
  * Cleanup expired buckets (optional maintenance operation)
  */
  async cleanup() {
  }
});

// ../../packages/core/dist/services/billing/credit-estimator.js
var CREDIT_COST_CONFIG = {
  deep_risk_sweep: {
    baseMin: 8,
    baseMax: 15,
    driver: "Graph size + tokens",
    computeEstimate: /* @__PURE__ */ __name((params) => {
      const graphNodes = params.graphNodes || 100;
      const min = Math.min(15, Math.max(8, 8 + Math.floor(graphNodes / 200)));
      const max = Math.min(18, min + 3);
      return {
        min,
        max
      };
    }, "computeEstimate")
  },
  full_codebase_scan: {
    baseMin: 20,
    baseMax: 30,
    driver: "File count + tokens",
    computeEstimate: /* @__PURE__ */ __name((params) => {
      const fileCount = params.fileCount || 100;
      const min = Math.min(30, Math.max(20, 20 + Math.floor(fileCount / 100)));
      const max = Math.min(35, min + 5);
      return {
        min,
        max
      };
    }, "computeEstimate")
  },
  memory_sync: {
    baseMin: 1,
    baseMax: 1,
    driver: "Per sync",
    computeEstimate: /* @__PURE__ */ __name(() => ({
      min: 1,
      max: 1
    }), "computeEstimate")
  },
  synthesis: {
    baseMin: 5,
    baseMax: 12,
    driver: "Token usage",
    computeEstimate: /* @__PURE__ */ __name((params) => {
      const tokens = params.tokens || 1e3;
      const min = Math.min(12, Math.max(5, 5 + Math.floor(tokens / 2e3)));
      const max = Math.min(15, min + 3);
      return {
        min,
        max
      };
    }, "computeEstimate")
  },
  layer3_personalization: {
    baseMin: 2,
    baseMax: 5,
    driver: "Bayesian update",
    computeEstimate: /* @__PURE__ */ __name(() => {
      return {
        min: 2,
        max: 5
      };
    }, "computeEstimate")
  },
  ai_refactor_plan: {
    baseMin: 15,
    baseMax: 25,
    driver: "Token usage + complexity",
    computeEstimate: /* @__PURE__ */ __name((params) => {
      const tokens = params.tokens || 1e3;
      const filesChanged = params.filesChanged || 1;
      const min = Math.min(25, Math.max(15, 15 + Math.floor(tokens / 1e3)));
      const max = Math.min(30, min + filesChanged * 2);
      return {
        min,
        max
      };
    }, "computeEstimate")
  },
  fleet_analysis: {
    baseMin: 5,
    baseMax: 12,
    driver: "Duration + complexity",
    computeEstimate: /* @__PURE__ */ __name((params) => {
      const duration = params.durationSeconds || 10;
      const min = Math.min(12, Math.max(5, 5 + Math.floor(duration / 10)));
      const max = Math.min(15, min + 3);
      return {
        min,
        max
      };
    }, "computeEstimate")
  },
  vitals_history: {
    baseMin: 2,
    baseMax: 2,
    driver: "Per query",
    computeEstimate: /* @__PURE__ */ __name(() => ({
      min: 2,
      max: 2
    }), "computeEstimate")
  }
};
function getCreditRange(jobType) {
  const config2 = CREDIT_COST_CONFIG[jobType];
  const { min, max } = config2.computeEstimate({});
  return {
    min,
    max,
    typical: Math.round((min + max) / 2)
  };
}
__name(getCreditRange, "getCreditRange");
var __defProp7 = Object.defineProperty;
var __getOwnPropNames2 = Object.getOwnPropertyNames;
var __name8 = /* @__PURE__ */ __name((target, value) => __defProp7(target, "name", {
  value,
  configurable: true
}), "__name");
var __require3 = /* @__PURE__ */ ((x) => typeof __require !== "undefined" ? __require : typeof Proxy !== "undefined" ? new Proxy(x, {
  get: /* @__PURE__ */ __name((a, b) => (typeof __require !== "undefined" ? __require : a)[b], "get")
}) : x)(function(x) {
  if (typeof __require !== "undefined") return __require.apply(this, arguments);
  throw Error('Dynamic require of "' + x + '" is not supported');
});
var __esm2 = /* @__PURE__ */ __name((fn, res) => /* @__PURE__ */ __name(function __init() {
  return fn && (res = (0, fn[__getOwnPropNames2(fn)[0]])(fn = 0)), res;
}, "__init"), "__esm");
var __export2 = /* @__PURE__ */ __name((target, all) => {
  for (var name in all) __defProp7(target, name, {
    get: all[name],
    enumerable: true
  });
}, "__export");
function getErrorMessage(code) {
  return ERROR_MESSAGES[code];
}
__name(getErrorMessage, "getErrorMessage");
function getErrorCategory(code) {
  const codeNum = Number.parseInt(code.slice(1, 4), 10);
  if (codeNum < 100) {
    return "protocol";
  }
  if (codeNum < 200) {
    return "request";
  }
  if (codeNum < 300) {
    return "file";
  }
  if (codeNum < 400) {
    return "snapshot";
  }
  if (codeNum < 500) {
    return "validation";
  }
  return "internal";
}
__name(getErrorCategory, "getErrorCategory");
function isRecoverableError(code) {
  const category = getErrorCategory(code);
  return category !== "internal";
}
__name(isRecoverableError, "isRecoverableError");
var E001_INVALID_REQUEST;
var E002_METHOD_NOT_FOUND;
var E003_NOT_INITIALIZED;
var E004_CONNECTION_LOST;
var E005_TIMEOUT;
var E006_VERSION_MISMATCH;
var E101_MISSING_PARAM;
var E102_INVALID_PARAM;
var E103_TYPE_MISMATCH;
var E104_INVALID_WORKSPACE;
var E105_INVALID_SESSION_ID;
var E106_INVALID_SNAPSHOT_ID;
var E107_INVALID_MODE;
var E201_FILE_NOT_FOUND;
var E202_FILE_ALREADY_EXISTS;
var E203_FILE_PERMISSION_DENIED;
var E204_FILE_LOCKED;
var E205_FILE_DIRTY;
var E206_WORKSPACE_NOT_INIT;
var E207_QUOTA_EXCEEDED;
var E301_SNAPSHOT_NOT_FOUND;
var E302_SNAPSHOT_CREATE_FAILED;
var E303_SNAPSHOT_RESTORE_FAILED;
var E304_SNAPSHOT_CORRUPTED;
var E305_NO_ACTIVE_SESSION;
var E306_SESSION_ALREADY_ACTIVE;
var E307_SESSION_END_FAILED;
var E308_SNAPSHOT_PROTECTED;
var E401_VALIDATION_FAILED;
var E402_TYPESCRIPT_ERRORS;
var E403_LINT_ERRORS;
var E404_TEST_FAILURES;
var E405_PATTERN_VIOLATION;
var E406_SECURITY_SCAN_FAILED;
var E407_CIRCULAR_DEPENDENCY;
var E408_BUILD_FAILED;
var E501_INTERNAL_ERROR;
var E502_STORAGE_ERROR;
var E503_SERVICE_UNAVAILABLE;
var E504_OPERATION_TIMEOUT;
var E505_CONFIG_ERROR;
var E506_DEPENDENCY_ERROR;
var E507_RATE_LIMIT_EXCEEDED;
var ERROR_MESSAGES;
var init_codes = __esm2({
  "src/errors/codes.ts"() {
    E001_INVALID_REQUEST = "E001_INVALID_REQUEST";
    E002_METHOD_NOT_FOUND = "E002_METHOD_NOT_FOUND";
    E003_NOT_INITIALIZED = "E003_NOT_INITIALIZED";
    E004_CONNECTION_LOST = "E004_CONNECTION_LOST";
    E005_TIMEOUT = "E005_TIMEOUT";
    E006_VERSION_MISMATCH = "E006_VERSION_MISMATCH";
    E101_MISSING_PARAM = "E101_MISSING_PARAM";
    E102_INVALID_PARAM = "E102_INVALID_PARAM";
    E103_TYPE_MISMATCH = "E103_TYPE_MISMATCH";
    E104_INVALID_WORKSPACE = "E104_INVALID_WORKSPACE";
    E105_INVALID_SESSION_ID = "E105_INVALID_SESSION_ID";
    E106_INVALID_SNAPSHOT_ID = "E106_INVALID_SNAPSHOT_ID";
    E107_INVALID_MODE = "E107_INVALID_MODE";
    E201_FILE_NOT_FOUND = "E201_FILE_NOT_FOUND";
    E202_FILE_ALREADY_EXISTS = "E202_FILE_ALREADY_EXISTS";
    E203_FILE_PERMISSION_DENIED = "E203_FILE_PERMISSION_DENIED";
    E204_FILE_LOCKED = "E204_FILE_LOCKED";
    E205_FILE_DIRTY = "E205_FILE_DIRTY";
    E206_WORKSPACE_NOT_INIT = "E206_WORKSPACE_NOT_INIT";
    E207_QUOTA_EXCEEDED = "E207_QUOTA_EXCEEDED";
    E301_SNAPSHOT_NOT_FOUND = "E301_SNAPSHOT_NOT_FOUND";
    E302_SNAPSHOT_CREATE_FAILED = "E302_SNAPSHOT_CREATE_FAILED";
    E303_SNAPSHOT_RESTORE_FAILED = "E303_SNAPSHOT_RESTORE_FAILED";
    E304_SNAPSHOT_CORRUPTED = "E304_SNAPSHOT_CORRUPTED";
    E305_NO_ACTIVE_SESSION = "E305_NO_ACTIVE_SESSION";
    E306_SESSION_ALREADY_ACTIVE = "E306_SESSION_ALREADY_ACTIVE";
    E307_SESSION_END_FAILED = "E307_SESSION_END_FAILED";
    E308_SNAPSHOT_PROTECTED = "E308_SNAPSHOT_PROTECTED";
    E401_VALIDATION_FAILED = "E401_VALIDATION_FAILED";
    E402_TYPESCRIPT_ERRORS = "E402_TYPESCRIPT_ERRORS";
    E403_LINT_ERRORS = "E403_LINT_ERRORS";
    E404_TEST_FAILURES = "E404_TEST_FAILURES";
    E405_PATTERN_VIOLATION = "E405_PATTERN_VIOLATION";
    E406_SECURITY_SCAN_FAILED = "E406_SECURITY_SCAN_FAILED";
    E407_CIRCULAR_DEPENDENCY = "E407_CIRCULAR_DEPENDENCY";
    E408_BUILD_FAILED = "E408_BUILD_FAILED";
    E501_INTERNAL_ERROR = "E501_INTERNAL_ERROR";
    E502_STORAGE_ERROR = "E502_STORAGE_ERROR";
    E503_SERVICE_UNAVAILABLE = "E503_SERVICE_UNAVAILABLE";
    E504_OPERATION_TIMEOUT = "E504_OPERATION_TIMEOUT";
    E505_CONFIG_ERROR = "E505_CONFIG_ERROR";
    E506_DEPENDENCY_ERROR = "E506_DEPENDENCY_ERROR";
    E507_RATE_LIMIT_EXCEEDED = "E507_RATE_LIMIT_EXCEEDED";
    ERROR_MESSAGES = {
      // Protocol errors
      [E001_INVALID_REQUEST]: "Invalid request format",
      [E002_METHOD_NOT_FOUND]: "Method not found",
      [E003_NOT_INITIALIZED]: "Service not initialized",
      [E004_CONNECTION_LOST]: "Connection to service lost",
      [E005_TIMEOUT]: "Request timeout",
      [E006_VERSION_MISMATCH]: "Protocol version mismatch",
      // Request errors
      [E101_MISSING_PARAM]: "Required parameter missing",
      [E102_INVALID_PARAM]: "Invalid parameter value",
      [E103_TYPE_MISMATCH]: "Parameter type mismatch",
      [E104_INVALID_WORKSPACE]: "Invalid workspace path",
      [E105_INVALID_SESSION_ID]: "Invalid session ID",
      [E106_INVALID_SNAPSHOT_ID]: "Invalid snapshot ID",
      [E107_INVALID_MODE]: "Invalid mode specified",
      // File errors
      [E201_FILE_NOT_FOUND]: "File not found",
      [E202_FILE_ALREADY_EXISTS]: "File already exists",
      [E203_FILE_PERMISSION_DENIED]: "Permission denied",
      [E204_FILE_LOCKED]: "File is locked",
      [E205_FILE_DIRTY]: "File has unsaved changes",
      [E206_WORKSPACE_NOT_INIT]: "Workspace not initialized",
      [E207_QUOTA_EXCEEDED]: "Quota exceeded",
      // Snapshot errors
      [E301_SNAPSHOT_NOT_FOUND]: "Snapshot not found",
      [E302_SNAPSHOT_CREATE_FAILED]: "Failed to create snapshot",
      [E303_SNAPSHOT_RESTORE_FAILED]: "Failed to restore snapshot",
      [E304_SNAPSHOT_CORRUPTED]: "Snapshot is corrupted",
      [E305_NO_ACTIVE_SESSION]: "No active session",
      [E306_SESSION_ALREADY_ACTIVE]: "Session already active",
      [E307_SESSION_END_FAILED]: "Failed to end session",
      [E308_SNAPSHOT_PROTECTED]: "Snapshot is protected",
      // Validation errors
      [E401_VALIDATION_FAILED]: "Validation failed",
      [E402_TYPESCRIPT_ERRORS]: "TypeScript errors found",
      [E403_LINT_ERRORS]: "Linter errors found",
      [E404_TEST_FAILURES]: "Test failures detected",
      [E405_PATTERN_VIOLATION]: "Pattern violation detected",
      [E406_SECURITY_SCAN_FAILED]: "Security scan failed",
      [E407_CIRCULAR_DEPENDENCY]: "Circular dependency detected",
      [E408_BUILD_FAILED]: "Build failed",
      // Internal errors
      [E501_INTERNAL_ERROR]: "Internal error",
      [E502_STORAGE_ERROR]: "Storage error",
      [E503_SERVICE_UNAVAILABLE]: "Service unavailable",
      [E504_OPERATION_TIMEOUT]: "Operation timeout",
      [E505_CONFIG_ERROR]: "Configuration error",
      [E506_DEPENDENCY_ERROR]: "Dependency error",
      [E507_RATE_LIMIT_EXCEEDED]: "Rate limit exceeded"
    };
    __name8(getErrorMessage, "getErrorMessage");
    __name8(getErrorCategory, "getErrorCategory");
    __name8(isRecoverableError, "isRecoverableError");
  }
});
function createRequestContext(tool, options) {
  return {
    requestId: randomUUID().slice(0, 8),
    tool,
    startTime: Date.now(),
    userId: options?.userId,
    tier: options?.tier,
    clientName: options?.clientName,
    clientVersion: options?.clientVersion
  };
}
__name(createRequestContext, "createRequestContext");
function log2(level, ctx, message, metadata) {
  ({
    requestId: ctx.requestId,
    tool: ctx.tool,
    duration: Date.now() - ctx.startTime
  });
}
__name(log2, "log");
function logError(ctx, code, message, details) {
  ({
    requestId: ctx.requestId,
    tool: ctx.tool,
    duration: Date.now() - ctx.startTime
  });
}
__name(logError, "logError");
function logSuccess(ctx, message, metadata) {
  log2("info", ctx, message, {
    ...metadata
  });
}
__name(logSuccess, "logSuccess");
function buildErrorResponse(code, message, details) {
  return {
    error: code,
    message,
    ...details && {
      details
    }
  };
}
__name(buildErrorResponse, "buildErrorResponse");
function formatDiagnosticError(error, context) {
  const errorMsg = error instanceof Error ? error.message : String(error);
  const errorName = error instanceof Error ? error.name : "Error";
  if (errorMsg.includes("ENOENT")) {
    const pathMatch = errorMsg.match(/['"]([^'"]+)['"]/);
    const path4 = pathMatch ? pathMatch[1] : "unknown";
    return {
      error: "E201_FILE_NOT_FOUND",
      code: "ENOENT",
      message: `File or directory not found: ${path4}`,
      diagnostics: [
        `Path: ${path4}`,
        context.config ? `Config: ${context.config}` : "",
        "Issue: Path does not exist or is inaccessible"
      ].filter(Boolean),
      fix: context.config ? "Update config file to use valid path. Check .qoder-mcp-config.json or similar." : "Verify path exists and has correct permissions."
    };
  }
  if (errorMsg.includes("ECONNREFUSED") || errorMsg.includes("Connection refused")) {
    return {
      error: "E503_SERVICE_UNAVAILABLE",
      code: "ECONNREFUSED",
      message: "Cannot connect to service",
      diagnostics: [
        context.workspace ? `Workspace: ${context.workspace}` : "",
        "Issue: Service not running or not accepting connections"
      ].filter(Boolean),
      fix: "Start the Vreko daemon: vreko daemon start"
    };
  }
  if (errorMsg.includes("timeout") || errorMsg.includes("ETIMEDOUT")) {
    return {
      error: "E504_TIMEOUT",
      code: "TIMEOUT",
      message: "Request timed out",
      diagnostics: [
        context.tool ? `Tool: ${context.tool}` : "",
        "Issue: Operation took too long to complete"
      ].filter(Boolean),
      fix: "Check network connectivity or increase timeout. Review server logs for hung processes."
    };
  }
  if (errorMsg.includes("EACCES") || errorMsg.includes("permission denied")) {
    const pathMatch = errorMsg.match(/['"]([^'"]+)['"]/);
    const path4 = pathMatch ? pathMatch[1] : "unknown";
    return {
      error: "E303_PERMISSION_DENIED",
      code: "EACCES",
      message: `Permission denied: ${path4}`,
      diagnostics: [
        `Path: ${path4}`,
        "Issue: Insufficient permissions to access file/directory"
      ].filter(Boolean),
      fix: "Check file permissions: chmod or chown the path, or run with appropriate user."
    };
  }
  return {
    error: "E500_INTERNAL_ERROR",
    code: errorName,
    message: errorMsg,
    diagnostics: [
      context.tool ? `Tool: ${context.tool}` : "",
      context.workspace ? `Workspace: ${context.workspace}` : "",
      `Error: ${errorName}`
    ].filter(Boolean)
  };
}
__name(formatDiagnosticError, "formatDiagnosticError");
var ErrorCodes;
var CommonErrors;
var init_legacy_utils = __esm2({
  "src/errors/legacy-utils.ts"() {
    ErrorCodes = {
      // Validation errors (1xx)
      MISSING_REQUIRED_PARAM: "E101_MISSING_REQUIRED_PARAM",
      INVALID_PARAM_TYPE: "E102_INVALID_PARAM_TYPE",
      INVALID_PARAM_VALUE: "E103_INVALID_PARAM_VALUE",
      VALIDATION_FAILED: "E104_VALIDATION_FAILED",
      PATH_TRAVERSAL_BLOCKED: "E105_PATH_TRAVERSAL_BLOCKED",
      // Resource errors (2xx)
      FILE_NOT_FOUND: "E201_FILE_NOT_FOUND",
      SNAPSHOT_NOT_FOUND: "E202_SNAPSHOT_NOT_FOUND",
      SESSION_NOT_FOUND: "E203_SESSION_NOT_FOUND",
      CONTEXT_NOT_INITIALIZED: "E204_CONTEXT_NOT_INITIALIZED",
      // Permission errors (3xx)
      TIER_GATE_BLOCKED: "E301_TIER_GATE_BLOCKED",
      OPERATION_NOT_ALLOWED: "E302_OPERATION_NOT_ALLOWED",
      PERMISSION_DENIED: "E303_PERMISSION_DENIED",
      // Runtime errors (4xx)
      SNAPSHOT_CREATE_FAILED: "E401_SNAPSHOT_CREATE_FAILED",
      SNAPSHOT_RESTORE_FAILED: "E402_SNAPSHOT_RESTORE_FAILED",
      STORAGE_ERROR: "E403_STORAGE_ERROR",
      CONTEXT_OPERATION_FAILED: "E404_CONTEXT_OPERATION_FAILED",
      SESSION_OPERATION_FAILED: "E405_SESSION_OPERATION_FAILED",
      // Unknown errors (5xx)
      UNKNOWN_TOOL: "E501_UNKNOWN_TOOL",
      HANDLER_ERROR: "E502_HANDLER_ERROR",
      INTERNAL_ERROR: "E503_INTERNAL_ERROR"
    };
    __name8(createRequestContext, "createRequestContext");
    __name8(log2, "log");
    __name8(logError, "logError");
    __name8(logSuccess, "logSuccess");
    __name8(buildErrorResponse, "buildErrorResponse");
    CommonErrors = {
      missingParam: /* @__PURE__ */ __name8((param) => buildErrorResponse(ErrorCodes.MISSING_REQUIRED_PARAM, `Missing required parameter: ${param}. Check the tool documentation for required parameters.`, {
        param,
        suggestion: `Add the '${param}' parameter to your request`,
        help: "Run snap_help({ topic: 'tools' }) to see parameter requirements"
      }), "missingParam"),
      invalidParam: /* @__PURE__ */ __name8((param, expected, received) => buildErrorResponse(ErrorCodes.INVALID_PARAM_VALUE, `Invalid value for ${param}: expected ${expected}, got ${typeof received}`, {
        param,
        expected,
        received,
        suggestion: `Provide a valid ${expected} value for '${param}'`
      }), "invalidParam"),
      fileNotFound: /* @__PURE__ */ __name8((filePath) => buildErrorResponse(ErrorCodes.FILE_NOT_FOUND, `File not found: ${filePath}. The file may have been moved or deleted.`, {
        filePath,
        suggestion: "Verify the file path is correct and the file exists",
        try_these: [
          "Check for typos in the file path",
          "Use relative paths from workspace root",
          "Run 'ls' or check file explorer to verify the file exists"
        ]
      }), "fileNotFound"),
      snapshotNotFound: /* @__PURE__ */ __name8((snapshotId) => buildErrorResponse(ErrorCodes.SNAPSHOT_NOT_FOUND, `Snapshot not found: ${snapshotId}. It may have been cleaned up or the ID is incorrect.`, {
        snapshotId,
        suggestion: "Use snap_fix({ action: 'list' }) to see available snapshots",
        try_these: [
          "Run 'Vreko: List Snapshots' to see available snapshots",
          "Check .vreko/snapshots/ directory for manual recovery",
          "Verify the snapshot ID is correct (first 8 characters are usually enough)"
        ]
      }), "snapshotNotFound"),
      pathTraversalBlocked: /* @__PURE__ */ __name8((path4, reason) => buildErrorResponse(ErrorCodes.PATH_TRAVERSAL_BLOCKED, reason, {
        path: path4,
        suggestion: "Use relative paths within the workspace only",
        try_these: [
          "Remove any '../' from the path",
          "Use paths relative to workspace root",
          "Check that the path doesn't escape the project directory"
        ]
      }), "pathTraversalBlocked"),
      tierGateBlocked: /* @__PURE__ */ __name8((tool, requiredTier, currentTier) => buildErrorResponse(ErrorCodes.TIER_GATE_BLOCKED, `Tool '${tool}' requires ${requiredTier} tier`, {
        tool,
        requiredTier,
        currentTier,
        upgradeUrl: "https://vreko.dev/pricing",
        suggestion: `Upgrade to ${requiredTier} tier to access this feature`,
        try_these: [
          `Visit ${"https://vreko.dev/pricing"} to upgrade`,
          "Use alternative free-tier features",
          "Contact support for trial access"
        ]
      }), "tierGateBlocked"),
      handlerError: /* @__PURE__ */ __name8((tool, message) => buildErrorResponse(ErrorCodes.HANDLER_ERROR, `${message}. The tool encountered an unexpected error.`, {
        tool,
        suggestion: "Try the operation again. If it persists, check the parameters.",
        try_these: [
          "Retry the operation",
          "Check the tool parameters are valid",
          "Run snap_help({ topic: 'tools' }) for documentation",
          "Check for workspace/environment issues"
        ]
      }), "handlerError"),
      permissionDenied: /* @__PURE__ */ __name8((path4, operation) => buildErrorResponse(ErrorCodes.PERMISSION_DENIED, "Enable full features with quick local setup", {
        path: path4,
        operation,
        action: {
          type: "cli_install",
          command: "npm install -g @vreko/cli && vreko tools configure",
          description: "One command enables full capabilities",
          time_estimate: "~30 seconds"
        },
        help_url: "https://docs.vreko.dev/cli/setup",
        suggestion: "Install the Vreko CLI to enable full features",
        try_these: [
          "Run: npm install -g @vreko/cli",
          "Then: vreko tools configure",
          "Or check file permissions if accessing local files"
        ],
        _llm: {
          suggest: "I can enable full Vreko features with a quick CLI install. Want me to help?",
          auto_offer: true
        }
      }), "permissionDenied"),
      // P2: New actionable error builders
      sessionNotFound: /* @__PURE__ */ __name8((sessionId) => buildErrorResponse(ErrorCodes.SESSION_NOT_FOUND, `Session not found: ${sessionId}. It may have expired or been completed.`, {
        sessionId,
        suggestion: "Start a new task with vreko({ mode: 'start', task: '...' })",
        try_these: [
          "Start a new task session",
          "Check if the task was already completed",
          "Sessions expire after inactivity - start fresh"
        ]
      }), "sessionNotFound"),
      storageError: /* @__PURE__ */ __name8((operation, reason) => buildErrorResponse(ErrorCodes.STORAGE_ERROR, `Storage ${operation} failed: ${reason}`, {
        operation,
        reason,
        suggestion: "Check disk space and permissions in .vreko/ directory",
        try_these: [
          "Check available disk space",
          "Verify .vreko/ directory is writable",
          "Run: chmod -R 755 .vreko/",
          "Clear old snapshots if disk is full"
        ]
      }), "storageError")
    };
    __name8(formatDiagnosticError, "formatDiagnosticError");
  }
});
function ok4(value) {
  return {
    ok: true,
    value
  };
}
__name(ok4, "ok");
function err5(error) {
  return {
    ok: false,
    error
  };
}
__name(err5, "err");
function createMcpError(code, message, details) {
  return {
    code,
    message,
    details,
    retryable: isRetryable({
      code
    }),
    timestamp: /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString()
  };
}
__name(createMcpError, "createMcpError");
function createMcpErrorFromCause(code, message, cause, details) {
  return {
    code,
    message: `${message}: ${cause.message}`,
    details: {
      ...details,
      originalError: cause.name,
      stack: cause.stack?.split("\n").slice(0, 3).join("\n")
    },
    cause,
    retryable: isRetryable({
      code
    }),
    timestamp: /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString()
  };
}
__name(createMcpErrorFromCause, "createMcpErrorFromCause");
function isMcpError(value) {
  return value !== null && typeof value === "object" && "code" in value && "message" in value && typeof value.code === "string" && typeof value.message === "string";
}
__name(isMcpError, "isMcpError");
function isErrorCode(code) {
  return /^E\d{3}_/.test(code);
}
__name(isErrorCode, "isErrorCode");
function isRetryable(error) {
  if (!isErrorCode(error.code)) {
    return false;
  }
  return isRecoverableError(error.code);
}
__name(isRetryable, "isRetryable");
function formatForLog(error) {
  const parts = [
    `[${error.code}] ${error.message}`
  ];
  if (error.details) {
    parts.push(`details=${JSON.stringify(error.details)}`);
  }
  return parts.join(" | ");
}
__name(formatForLog, "formatForLog");
function formatForUser(error) {
  const category = isErrorCode(error.code) ? getErrorCategory(error.code) : "internal";
  const fallback = isErrorCode(error.code) ? getErrorMessage(error.code) : "An error occurred";
  return `${error.message || fallback} (${category}: ${error.code})`;
}
__name(formatForUser, "formatForUser");
function formatMcpError(error) {
  return {
    error: error.code,
    code: error.code,
    message: error.message
  };
}
__name(formatMcpError, "formatMcpError");
var McpErrorCode;
var McpErrors;
var init_result = __esm2({
  "src/errors/result.ts"() {
    init_codes();
    McpErrorCode = {
      // Protocol errors
      INVALID_REQUEST: "E001_INVALID_REQUEST",
      METHOD_NOT_FOUND: "E002_METHOD_NOT_FOUND",
      NOT_INITIALIZED: "E003_NOT_INITIALIZED",
      CONNECTION_LOST: "E004_CONNECTION_LOST",
      TIMEOUT: "E005_TIMEOUT",
      VERSION_MISMATCH: "E006_VERSION_MISMATCH",
      // Request errors
      MISSING_PARAM: "E101_MISSING_PARAM",
      INVALID_PARAM: "E102_INVALID_PARAM",
      TYPE_MISMATCH: "E103_TYPE_MISMATCH",
      INVALID_WORKSPACE: "E104_INVALID_WORKSPACE",
      INVALID_SESSION_ID: "E105_INVALID_SESSION_ID",
      INVALID_SNAPSHOT_ID: "E106_INVALID_SNAPSHOT_ID",
      INVALID_MODE: "E107_INVALID_MODE",
      // File errors
      FILE_NOT_FOUND: "E201_FILE_NOT_FOUND",
      FILE_ALREADY_EXISTS: "E202_FILE_ALREADY_EXISTS",
      FILE_PERMISSION_DENIED: "E203_FILE_PERMISSION_DENIED",
      FILE_LOCKED: "E204_FILE_LOCKED",
      FILE_DIRTY: "E205_FILE_DIRTY",
      WORKSPACE_NOT_INIT: "E206_WORKSPACE_NOT_INIT",
      QUOTA_EXCEEDED: "E207_QUOTA_EXCEEDED",
      // Snapshot errors
      SNAPSHOT_NOT_FOUND: "E301_SNAPSHOT_NOT_FOUND",
      SNAPSHOT_CREATE_FAILED: "E302_SNAPSHOT_CREATE_FAILED",
      SNAPSHOT_RESTORE_FAILED: "E303_SNAPSHOT_RESTORE_FAILED",
      SNAPSHOT_CORRUPTED: "E304_SNAPSHOT_CORRUPTED",
      NO_ACTIVE_SESSION: "E305_NO_ACTIVE_SESSION",
      SESSION_ALREADY_ACTIVE: "E306_SESSION_ALREADY_ACTIVE",
      SESSION_END_FAILED: "E307_SESSION_END_FAILED",
      SNAPSHOT_PROTECTED: "E308_SNAPSHOT_PROTECTED",
      // Validation errors
      VALIDATION_FAILED: "E401_VALIDATION_FAILED",
      TYPESCRIPT_ERRORS: "E402_TYPESCRIPT_ERRORS",
      LINT_ERRORS: "E403_LINT_ERRORS",
      TEST_FAILURES: "E404_TEST_FAILURES",
      PATTERN_VIOLATION: "E405_PATTERN_VIOLATION",
      SECURITY_SCAN_FAILED: "E406_SECURITY_SCAN_FAILED",
      CIRCULAR_DEPENDENCY: "E407_CIRCULAR_DEPENDENCY",
      BUILD_FAILED: "E408_BUILD_FAILED",
      // Internal errors
      INTERNAL_ERROR: "E501_INTERNAL_ERROR",
      STORAGE_ERROR: "E502_STORAGE_ERROR",
      SERVICE_UNAVAILABLE: "E503_SERVICE_UNAVAILABLE",
      OPERATION_TIMEOUT: "E504_OPERATION_TIMEOUT",
      CONFIG_ERROR: "E505_CONFIG_ERROR",
      DEPENDENCY_ERROR: "E506_DEPENDENCY_ERROR",
      RATE_LIMIT_EXCEEDED: "E507_RATE_LIMIT_EXCEEDED",
      // Daemon-specific errors
      DAEMON_UNAVAILABLE: "E601_DAEMON_UNAVAILABLE",
      DAEMON_REQUEST_FAILED: "E602_DAEMON_REQUEST_FAILED"
    };
    __name8(ok4, "ok");
    __name8(err5, "err");
    __name8(createMcpError, "createMcpError");
    __name8(createMcpErrorFromCause, "createMcpErrorFromCause");
    __name8(isMcpError, "isMcpError");
    __name8(isErrorCode, "isErrorCode");
    __name8(isRetryable, "isRetryable");
    __name8(formatForLog, "formatForLog");
    __name8(formatForUser, "formatForUser");
    __name8(formatMcpError, "formatMcpError");
    McpErrors = {
      invalidParam: /* @__PURE__ */ __name8((param, reason) => createMcpError(McpErrorCode.INVALID_PARAM, `Invalid parameter: ${param}${reason ? `  -  ${reason}` : ""}`), "invalidParam"),
      missingParam: /* @__PURE__ */ __name8((param) => createMcpError(McpErrorCode.MISSING_PARAM, `Required parameter missing: ${param}`), "missingParam"),
      fileNotFound: /* @__PURE__ */ __name8((path4) => createMcpError(McpErrorCode.FILE_NOT_FOUND, `File not found: ${path4}`, {
        path: path4
      }), "fileNotFound"),
      snapshotNotFound: /* @__PURE__ */ __name8((id) => createMcpError(McpErrorCode.SNAPSHOT_NOT_FOUND, `Snapshot not found: ${id}`, {
        snapshotId: id
      }), "snapshotNotFound"),
      serviceUnavailable: /* @__PURE__ */ __name8((service) => createMcpError(McpErrorCode.SERVICE_UNAVAILABLE, `Service unavailable: ${service}`, {
        service
      }), "serviceUnavailable"),
      internal: /* @__PURE__ */ __name8((message, details) => createMcpError(McpErrorCode.INTERNAL_ERROR, message, details), "internal"),
      daemonUnavailable: /* @__PURE__ */ __name8((workspace) => createMcpError(McpErrorCode.DAEMON_UNAVAILABLE, "Local service is not available", {
        workspace
      }), "daemonUnavailable"),
      daemonRequestFailed: /* @__PURE__ */ __name8((message, details) => createMcpError(McpErrorCode.DAEMON_REQUEST_FAILED, message, details), "daemonRequestFailed")
    };
  }
});
var errors_exports = {};
__export2(errors_exports, {
  CommonErrors: /* @__PURE__ */ __name(() => CommonErrors, "CommonErrors"),
  E001_INVALID_REQUEST: /* @__PURE__ */ __name(() => E001_INVALID_REQUEST, "E001_INVALID_REQUEST"),
  E002_METHOD_NOT_FOUND: /* @__PURE__ */ __name(() => E002_METHOD_NOT_FOUND, "E002_METHOD_NOT_FOUND"),
  E003_NOT_INITIALIZED: /* @__PURE__ */ __name(() => E003_NOT_INITIALIZED, "E003_NOT_INITIALIZED"),
  E004_CONNECTION_LOST: /* @__PURE__ */ __name(() => E004_CONNECTION_LOST, "E004_CONNECTION_LOST"),
  E005_TIMEOUT: /* @__PURE__ */ __name(() => E005_TIMEOUT, "E005_TIMEOUT"),
  E006_VERSION_MISMATCH: /* @__PURE__ */ __name(() => E006_VERSION_MISMATCH, "E006_VERSION_MISMATCH"),
  E101_MISSING_PARAM: /* @__PURE__ */ __name(() => E101_MISSING_PARAM, "E101_MISSING_PARAM"),
  E102_INVALID_PARAM: /* @__PURE__ */ __name(() => E102_INVALID_PARAM, "E102_INVALID_PARAM"),
  E103_TYPE_MISMATCH: /* @__PURE__ */ __name(() => E103_TYPE_MISMATCH, "E103_TYPE_MISMATCH"),
  E104_INVALID_WORKSPACE: /* @__PURE__ */ __name(() => E104_INVALID_WORKSPACE, "E104_INVALID_WORKSPACE"),
  E105_INVALID_SESSION_ID: /* @__PURE__ */ __name(() => E105_INVALID_SESSION_ID, "E105_INVALID_SESSION_ID"),
  E106_INVALID_SNAPSHOT_ID: /* @__PURE__ */ __name(() => E106_INVALID_SNAPSHOT_ID, "E106_INVALID_SNAPSHOT_ID"),
  E107_INVALID_MODE: /* @__PURE__ */ __name(() => E107_INVALID_MODE, "E107_INVALID_MODE"),
  E201_FILE_NOT_FOUND: /* @__PURE__ */ __name(() => E201_FILE_NOT_FOUND, "E201_FILE_NOT_FOUND"),
  E202_FILE_ALREADY_EXISTS: /* @__PURE__ */ __name(() => E202_FILE_ALREADY_EXISTS, "E202_FILE_ALREADY_EXISTS"),
  E203_FILE_PERMISSION_DENIED: /* @__PURE__ */ __name(() => E203_FILE_PERMISSION_DENIED, "E203_FILE_PERMISSION_DENIED"),
  E204_FILE_LOCKED: /* @__PURE__ */ __name(() => E204_FILE_LOCKED, "E204_FILE_LOCKED"),
  E205_FILE_DIRTY: /* @__PURE__ */ __name(() => E205_FILE_DIRTY, "E205_FILE_DIRTY"),
  E206_WORKSPACE_NOT_INIT: /* @__PURE__ */ __name(() => E206_WORKSPACE_NOT_INIT, "E206_WORKSPACE_NOT_INIT"),
  E207_QUOTA_EXCEEDED: /* @__PURE__ */ __name(() => E207_QUOTA_EXCEEDED, "E207_QUOTA_EXCEEDED"),
  E301_SNAPSHOT_NOT_FOUND: /* @__PURE__ */ __name(() => E301_SNAPSHOT_NOT_FOUND, "E301_SNAPSHOT_NOT_FOUND"),
  E302_SNAPSHOT_CREATE_FAILED: /* @__PURE__ */ __name(() => E302_SNAPSHOT_CREATE_FAILED, "E302_SNAPSHOT_CREATE_FAILED"),
  E303_SNAPSHOT_RESTORE_FAILED: /* @__PURE__ */ __name(() => E303_SNAPSHOT_RESTORE_FAILED, "E303_SNAPSHOT_RESTORE_FAILED"),
  E304_SNAPSHOT_CORRUPTED: /* @__PURE__ */ __name(() => E304_SNAPSHOT_CORRUPTED, "E304_SNAPSHOT_CORRUPTED"),
  E305_NO_ACTIVE_SESSION: /* @__PURE__ */ __name(() => E305_NO_ACTIVE_SESSION, "E305_NO_ACTIVE_SESSION"),
  E306_SESSION_ALREADY_ACTIVE: /* @__PURE__ */ __name(() => E306_SESSION_ALREADY_ACTIVE, "E306_SESSION_ALREADY_ACTIVE"),
  E307_SESSION_END_FAILED: /* @__PURE__ */ __name(() => E307_SESSION_END_FAILED, "E307_SESSION_END_FAILED"),
  E308_SNAPSHOT_PROTECTED: /* @__PURE__ */ __name(() => E308_SNAPSHOT_PROTECTED, "E308_SNAPSHOT_PROTECTED"),
  E401_VALIDATION_FAILED: /* @__PURE__ */ __name(() => E401_VALIDATION_FAILED, "E401_VALIDATION_FAILED"),
  E402_TYPESCRIPT_ERRORS: /* @__PURE__ */ __name(() => E402_TYPESCRIPT_ERRORS, "E402_TYPESCRIPT_ERRORS"),
  E403_LINT_ERRORS: /* @__PURE__ */ __name(() => E403_LINT_ERRORS, "E403_LINT_ERRORS"),
  E404_TEST_FAILURES: /* @__PURE__ */ __name(() => E404_TEST_FAILURES, "E404_TEST_FAILURES"),
  E405_PATTERN_VIOLATION: /* @__PURE__ */ __name(() => E405_PATTERN_VIOLATION, "E405_PATTERN_VIOLATION"),
  E406_SECURITY_SCAN_FAILED: /* @__PURE__ */ __name(() => E406_SECURITY_SCAN_FAILED, "E406_SECURITY_SCAN_FAILED"),
  E407_CIRCULAR_DEPENDENCY: /* @__PURE__ */ __name(() => E407_CIRCULAR_DEPENDENCY, "E407_CIRCULAR_DEPENDENCY"),
  E408_BUILD_FAILED: /* @__PURE__ */ __name(() => E408_BUILD_FAILED, "E408_BUILD_FAILED"),
  E501_INTERNAL_ERROR: /* @__PURE__ */ __name(() => E501_INTERNAL_ERROR, "E501_INTERNAL_ERROR"),
  E502_STORAGE_ERROR: /* @__PURE__ */ __name(() => E502_STORAGE_ERROR, "E502_STORAGE_ERROR"),
  E503_SERVICE_UNAVAILABLE: /* @__PURE__ */ __name(() => E503_SERVICE_UNAVAILABLE, "E503_SERVICE_UNAVAILABLE"),
  E504_OPERATION_TIMEOUT: /* @__PURE__ */ __name(() => E504_OPERATION_TIMEOUT, "E504_OPERATION_TIMEOUT"),
  E505_CONFIG_ERROR: /* @__PURE__ */ __name(() => E505_CONFIG_ERROR, "E505_CONFIG_ERROR"),
  E506_DEPENDENCY_ERROR: /* @__PURE__ */ __name(() => E506_DEPENDENCY_ERROR, "E506_DEPENDENCY_ERROR"),
  E507_RATE_LIMIT_EXCEEDED: /* @__PURE__ */ __name(() => E507_RATE_LIMIT_EXCEEDED, "E507_RATE_LIMIT_EXCEEDED"),
  ERROR_MESSAGES: /* @__PURE__ */ __name(() => ERROR_MESSAGES, "ERROR_MESSAGES"),
  ErrorCodes: /* @__PURE__ */ __name(() => ErrorCodes, "ErrorCodes"),
  McpErrorCode: /* @__PURE__ */ __name(() => McpErrorCode, "McpErrorCode"),
  McpErrors: /* @__PURE__ */ __name(() => McpErrors, "McpErrors"),
  buildErrorResponse: /* @__PURE__ */ __name(() => buildErrorResponse, "buildErrorResponse"),
  createMcpError: /* @__PURE__ */ __name(() => createMcpError, "createMcpError"),
  createMcpErrorFromCause: /* @__PURE__ */ __name(() => createMcpErrorFromCause, "createMcpErrorFromCause"),
  createRequestContext: /* @__PURE__ */ __name(() => createRequestContext, "createRequestContext"),
  err: /* @__PURE__ */ __name(() => err5, "err"),
  formatDiagnosticError: /* @__PURE__ */ __name(() => formatDiagnosticError, "formatDiagnosticError"),
  formatForLog: /* @__PURE__ */ __name(() => formatForLog, "formatForLog"),
  formatForUser: /* @__PURE__ */ __name(() => formatForUser, "formatForUser"),
  formatMcpError: /* @__PURE__ */ __name(() => formatMcpError, "formatMcpError"),
  getErrorCategory: /* @__PURE__ */ __name(() => getErrorCategory, "getErrorCategory"),
  getErrorMessage: /* @__PURE__ */ __name(() => getErrorMessage, "getErrorMessage"),
  isErrorCode: /* @__PURE__ */ __name(() => isErrorCode, "isErrorCode"),
  isMcpError: /* @__PURE__ */ __name(() => isMcpError, "isMcpError"),
  isRecoverableError: /* @__PURE__ */ __name(() => isRecoverableError, "isRecoverableError"),
  isRetryable: /* @__PURE__ */ __name(() => isRetryable, "isRetryable"),
  log: /* @__PURE__ */ __name(() => log2, "log"),
  logError: /* @__PURE__ */ __name(() => logError, "logError"),
  logSuccess: /* @__PURE__ */ __name(() => logSuccess, "logSuccess"),
  ok: /* @__PURE__ */ __name(() => ok4, "ok")
});
var init_errors = __esm2({
  "src/errors/index.ts"() {
    init_codes();
    init_legacy_utils();
    init_result();
  }
});
function resolveToGitRoot(startPath) {
  let currentPath = resolve(startPath);
  const maxIterations = 50;
  let iterations = 0;
  while (iterations < maxIterations) {
    iterations++;
    if (existsSync(resolve(currentPath, ".git"))) {
      return currentPath;
    }
    const parent = resolve(currentPath, "..");
    if (parent === currentPath) {
      break;
    }
    currentPath = parent;
  }
  return null;
}
__name(resolveToGitRoot, "resolveToGitRoot");
var init_workspace = __esm2({
  "src/middleware/workspace.ts"() {
    __name8(resolveToGitRoot, "resolveToGitRoot");
  }
});
function getPooledClient(workspaceRoot) {
  return clients.get(workspaceRoot);
}
__name(getPooledClient, "getPooledClient");
function setPooledClient(workspaceRoot, client) {
  clients.set(workspaceRoot, client);
}
__name(setPooledClient, "setPooledClient");
function removePooledClient(workspaceRoot) {
  const client = clients.get(workspaceRoot);
  if (client) {
    clients.delete(workspaceRoot);
    clearPoolConnectionStatus(workspaceRoot);
    pendingConnections.delete(workspaceRoot);
    try {
      client.close();
    } catch {
    }
  }
}
__name(removePooledClient, "removePooledClient");
function getAnyConnectedClient() {
  for (const c of clients.values()) {
    if (c.isConnected()) {
      return c;
    }
  }
  return void 0;
}
__name(getAnyConnectedClient, "getAnyConnectedClient");
function getAllConnectedClients() {
  const result7 = [];
  for (const client of clients.values()) {
    if (client.isConnected()) {
      result7.push(client);
    }
  }
  return result7;
}
__name(getAllConnectedClients, "getAllConnectedClients");
function clearPool() {
  const clientsToClose = Array.from(clients.values());
  clients.clear();
  connectionStatus.clear();
  pendingConnections.clear();
  for (const client of clientsToClose) {
    try {
      client.close();
    } catch {
    }
  }
}
__name(clearPool, "clearPool");
function getPoolConnectionStatus(workspaceRoot) {
  return connectionStatus.get(workspaceRoot);
}
__name(getPoolConnectionStatus, "getPoolConnectionStatus");
function setPoolConnectionStatus(workspaceRoot, status) {
  connectionStatus.set(workspaceRoot, status);
}
__name(setPoolConnectionStatus, "setPoolConnectionStatus");
function clearPoolConnectionStatus(workspaceRoot) {
  connectionStatus.delete(workspaceRoot);
}
__name(clearPoolConnectionStatus, "clearPoolConnectionStatus");
function clearAllPoolConnectionStatuses() {
  connectionStatus.clear();
}
__name(clearAllPoolConnectionStatuses, "clearAllPoolConnectionStatuses");
async function acquireClient(workspaceRoot) {
  const pending = pendingConnections.get(workspaceRoot);
  if (pending) {
    return pending;
  }
  const existing = clients.get(workspaceRoot);
  if (existing?.isConnected()) {
    return existing;
  }
  const connectionPromise = (async () => {
    let client = clients.get(workspaceRoot);
    try {
      if (!client) {
        client = new VrekoLocalClient({
          timeout: 6e4,
          autoReconnect: true,
          maxReconnectAttempts: 5,
          reconnectDelay: 1e3,
          maxReconnectDelay: 3e4
        });
        clients.set(workspaceRoot, client);
      }
      if (!client.isConnected()) {
        await client.connect();
        await client.initialize({
          protocolVersion: "1.0.0",
          clientInfo: {
            name: "vreko-mcp",
            version: "1.0.0"
          },
          capabilities: {
            notifications: true
          }
        });
      }
      return client;
    } catch (error) {
      clients.delete(workspaceRoot);
      throw error;
    }
  })();
  pendingConnections.set(workspaceRoot, connectionPromise);
  try {
    return await connectionPromise;
  } finally {
    pendingConnections.delete(workspaceRoot);
  }
}
__name(acquireClient, "acquireClient");
var clients;
var pendingConnections;
var connectionStatus;
var init_client_pool = __esm2({
  "src/daemon/client-pool.ts"() {
    clients = /* @__PURE__ */ new Map();
    pendingConnections = /* @__PURE__ */ new Map();
    connectionStatus = /* @__PURE__ */ new Map();
    __name8(getPooledClient, "getPooledClient");
    __name8(setPooledClient, "setPooledClient");
    __name8(removePooledClient, "removePooledClient");
    __name8(getAnyConnectedClient, "getAnyConnectedClient");
    __name8(getAllConnectedClients, "getAllConnectedClients");
    __name8(clearPool, "clearPool");
    __name8(getPoolConnectionStatus, "getPoolConnectionStatus");
    __name8(setPoolConnectionStatus, "setPoolConnectionStatus");
    __name8(clearPoolConnectionStatus, "clearPoolConnectionStatus");
    __name8(clearAllPoolConnectionStatuses, "clearAllPoolConnectionStatuses");
    __name8(acquireClient, "acquireClient");
  }
});
function getCircuitBreaker(workspaceRoot) {
  let cb = circuitBreakers.get(workspaceRoot);
  if (!cb) {
    cb = {
      state: "closed",
      failures: 0,
      threshold: CIRCUIT_BREAKER_CONFIG.failureThreshold,
      lastFailure: 0,
      cooldownMs: CIRCUIT_BREAKER_CONFIG.resetTimeout,
      nextRetryTime: 0
    };
    circuitBreakers.set(workspaceRoot, cb);
  }
  return cb;
}
__name(getCircuitBreaker, "getCircuitBreaker");
function recordSuccess2(workspaceRoot) {
  const cb = getCircuitBreaker(workspaceRoot);
  cb.state = "closed";
  cb.failures = 0;
}
__name(recordSuccess2, "recordSuccess");
function recordFailure(workspaceRoot) {
  const cb = getCircuitBreaker(workspaceRoot);
  cb.failures++;
  cb.lastFailure = Date.now();
  if (cb.failures >= CIRCUIT_BREAKER_CONFIG.failureThreshold) {
    cb.state = "open";
    cb.nextRetryTime = Date.now() + CIRCUIT_BREAKER_CONFIG.resetTimeout;
  }
}
__name(recordFailure, "recordFailure");
function shouldAllowRequest(workspaceRoot) {
  const cb = getCircuitBreaker(workspaceRoot);
  if (cb.state === "closed") {
    return {
      allowed: true
    };
  }
  if (cb.state === "open") {
    if (Date.now() >= cb.nextRetryTime) {
      cb.state = "half-open";
      return {
        allowed: true
      };
    }
    const retryIn = Math.ceil((cb.nextRetryTime - Date.now()) / 1e3);
    return {
      allowed: false,
      reason: `Circuit open. Retry in ${retryIn}s`
    };
  }
  return {
    allowed: true
  };
}
__name(shouldAllowRequest, "shouldAllowRequest");
function resetCircuitBreaker(workspaceRoot) {
  circuitBreakers.delete(workspaceRoot);
}
__name(resetCircuitBreaker, "resetCircuitBreaker");
async function getClient2(workspaceRoot) {
  const circuitCheck = shouldAllowRequest(workspaceRoot);
  if (!circuitCheck.allowed) {
    return null;
  }
  try {
    const client = await acquireClient(workspaceRoot);
    if (!client.isConnected()) {
      recordFailure(workspaceRoot);
      return null;
    }
    return client;
  } catch (_error) {
    recordFailure(workspaceRoot);
    return null;
  }
}
__name(getClient2, "getClient");
async function protectedCall(workspaceRoot, _operation, call, timeoutMs = 1e4) {
  const circuitCheck = shouldAllowRequest(workspaceRoot);
  if (!circuitCheck.allowed) {
    return {
      success: false,
      error: circuitCheck.reason ?? "Circuit breaker rejected request"
    };
  }
  try {
    const timeoutPromise = new Promise((_, reject) => {
      setTimeout(() => reject(new Error(`Timeout after ${timeoutMs}ms`)), timeoutMs);
    });
    const result7 = await Promise.race([
      call(),
      timeoutPromise
    ]);
    recordSuccess2(workspaceRoot);
    return {
      success: true,
      result: result7
    };
  } catch (error) {
    const errorMsg = error instanceof Error ? error.message : String(error);
    recordFailure(workspaceRoot);
    return {
      success: false,
      error: errorMsg
    };
  }
}
__name(protectedCall, "protectedCall");
async function getVitalsViaDaemon(workspaceRoot) {
  const client = await getClient2(workspaceRoot);
  if (!client) {
    const cb = getCircuitBreaker(workspaceRoot);
    return {
      success: false,
      error: cb.state === "open" ? `Daemon unavailable (circuit open, retry in ${Math.ceil((cb.nextRetryTime - Date.now()) / 1e3)}s)` : "Daemon unavailable"
    };
  }
  const result7 = await protectedCall(workspaceRoot, "intelligence/getVitals", () => client.call("intelligence/getVitals", {
    workspace: workspaceRoot
  }));
  if (!result7.success) {
    return {
      success: false,
      error: result7.error
    };
  }
  return {
    success: true,
    vitals: result7.result
  };
}
__name(getVitalsViaDaemon, "getVitalsViaDaemon");
async function getContextViaDaemon(workspaceRoot, options) {
  const client = await getClient2(workspaceRoot);
  if (!client) {
    const cb = getCircuitBreaker(workspaceRoot);
    return {
      success: false,
      error: cb.state === "open" ? `Daemon unavailable (circuit open, retry in ${Math.ceil((cb.nextRetryTime - Date.now()) / 1e3)}s)` : "Daemon unavailable"
    };
  }
  const result7 = await protectedCall(workspaceRoot, "context/get", () => client.call("context/get", {
    workspace: workspaceRoot,
    task: options?.task,
    keywords: options?.keywords ?? []
  }));
  if (!result7.success) {
    return {
      success: false,
      error: result7.error
    };
  }
  return {
    success: true,
    patterns: result7.result.patterns,
    constraints: result7.result.constraints,
    learnings: result7.result.learnings,
    context: result7.result
  };
}
__name(getContextViaDaemon, "getContextViaDaemon");
async function generateEmbeddingViaDaemon(text2, preferredWorkspace) {
  const errors = [];
  if (preferredWorkspace) {
    const circuitCheck = shouldAllowRequest(preferredWorkspace);
    if (circuitCheck.allowed) {
      const preferred = getPooledClient(preferredWorkspace);
      if (preferred?.isConnected()) {
        try {
          const result7 = await preferred.call("intelligence/generateEmbedding", {
            text: text2
          });
          recordSuccess2(preferredWorkspace);
          return {
            success: true,
            embedding: result7.embedding
          };
        } catch (error) {
          recordFailure(preferredWorkspace);
          errors.push(`${preferredWorkspace}: ${error instanceof Error ? error.message : String(error)}`);
        }
      }
    } else {
      errors.push(`${preferredWorkspace}: ${circuitCheck.reason}`);
    }
  }
  for (const client of getAllConnectedClients()) {
    try {
      const result7 = await client.call("intelligence/generateEmbedding", {
        text: text2
      });
      return {
        success: true,
        embedding: result7.embedding
      };
    } catch (error) {
      errors.push(error instanceof Error ? error.message : String(error));
    }
  }
  return {
    success: false,
    error: errors.length > 0 ? `All clients failed: ${errors.join("; ")}` : "No daemon connection available for embedding generation"
  };
}
__name(generateEmbeddingViaDaemon, "generateEmbeddingViaDaemon");
async function searchKnowledgeViaDaemon(workspaceRoot, query, limit) {
  const client = await getClient2(workspaceRoot);
  if (!client) {
    const cb = getCircuitBreaker(workspaceRoot);
    return {
      success: false,
      error: cb.state === "open" ? `Daemon unavailable (circuit open, retry in ${Math.ceil((cb.nextRetryTime - Date.now()) / 1e3)}s)` : "Daemon unavailable"
    };
  }
  const result7 = await protectedCall(workspaceRoot, "knowledge/search", () => client.call("knowledge/search", {
    workspace: workspaceRoot,
    query,
    limit: limit ?? 10
  }));
  if (!result7.success) {
    return {
      success: false,
      error: result7.error
    };
  }
  return {
    success: true,
    chunks: result7.result.chunks
  };
}
__name(searchKnowledgeViaDaemon, "searchKnowledgeViaDaemon");
async function hybridRetrieveViaDaemon(workspaceRoot, query, options) {
  const client = await getClient2(workspaceRoot);
  if (!client) {
    const cb = getCircuitBreaker(workspaceRoot);
    return {
      success: false,
      error: cb.state === "open" ? `Daemon unavailable (circuit open, retry in ${Math.ceil((cb.nextRetryTime - Date.now()) / 1e3)}s)` : "Daemon unavailable"
    };
  }
  const result7 = await protectedCall(workspaceRoot, "intelligence/hybridRetrieve", () => client.call("intelligence/hybridRetrieve", {
    workspace: workspaceRoot,
    query,
    intent: options?.intent,
    keywords: options?.keywords ?? [],
    limit: options?.limit ?? 10,
    semanticWeight: options?.semanticWeight,
    keywordWeight: options?.keywordWeight
  }));
  if (!result7.success) {
    return {
      success: false,
      error: result7.error
    };
  }
  return {
    success: true,
    results: result7.result.results,
    metrics: result7.result.metrics
  };
}
__name(hybridRetrieveViaDaemon, "hybridRetrieveViaDaemon");
async function getIntelligenceViaDaemon(workspaceRoot) {
  const client = await getClient2(workspaceRoot);
  if (!client) {
    const cb = getCircuitBreaker(workspaceRoot);
    return {
      success: false,
      error: cb.state === "open" ? `Daemon unavailable (circuit open, retry in ${Math.ceil((cb.nextRetryTime - Date.now()) / 1e3)}s)` : "Daemon unavailable"
    };
  }
  return {
    success: true,
    instance: {
      getContext: /* @__PURE__ */ __name8((options) => getContextViaDaemon(workspaceRoot, options), "getContext"),
      getVitals: /* @__PURE__ */ __name8(() => getVitalsViaDaemon(workspaceRoot), "getVitals"),
      initialize: /* @__PURE__ */ __name8(async () => {
      }, "initialize")
    }
  };
}
__name(getIntelligenceViaDaemon, "getIntelligenceViaDaemon");
async function queryPatternsViaDaemon(workspaceRoot, query) {
  const client = await getClient2(workspaceRoot);
  if (!client) {
    const cb = getCircuitBreaker(workspaceRoot);
    return {
      success: false,
      error: cb.state === "open" ? `Daemon unavailable (circuit open, retry in ${Math.ceil((cb.nextRetryTime - Date.now()) / 1e3)}s)` : "Daemon unavailable"
    };
  }
  const result7 = await protectedCall(workspaceRoot, "intelligence/queryPatterns", () => client.call("intelligence/queryPatterns", {
    workspace: workspaceRoot,
    ...query
  }));
  if (!result7.success) {
    return {
      success: false,
      error: result7.error
    };
  }
  return {
    success: true,
    results: result7.result.results,
    violationStats: result7.result.violationStats
  };
}
__name(queryPatternsViaDaemon, "queryPatternsViaDaemon");
async function indexPatternsViaDaemon(workspaceRoot, factors) {
  const client = await getClient2(workspaceRoot);
  if (!client) {
    const cb = getCircuitBreaker(workspaceRoot);
    return {
      success: false,
      error: cb.state === "open" ? `Daemon unavailable (circuit open, retry in ${Math.ceil((cb.nextRetryTime - Date.now()) / 1e3)}s)` : "Daemon unavailable"
    };
  }
  const result7 = await protectedCall(workspaceRoot, "intelligence/indexPatterns", () => client.call("intelligence/indexPatterns", {
    workspace: workspaceRoot,
    factors
  }));
  if (!result7.success) {
    return {
      success: false,
      error: result7.error
    };
  }
  return {
    success: true
  };
}
__name(indexPatternsViaDaemon, "indexPatternsViaDaemon");
async function recommendPatternsViaDaemon(workspaceRoot, context) {
  const client = await getClient2(workspaceRoot);
  if (!client) {
    const cb = getCircuitBreaker(workspaceRoot);
    return {
      success: false,
      error: cb.state === "open" ? `Daemon unavailable (circuit open, retry in ${Math.ceil((cb.nextRetryTime - Date.now()) / 1e3)}s)` : "Daemon unavailable"
    };
  }
  const result7 = await protectedCall(workspaceRoot, "intelligence/recommendPatterns", () => client.call("intelligence/recommendPatterns", {
    workspace: workspaceRoot,
    ...context
  }));
  if (!result7.success) {
    return {
      success: false,
      error: result7.error
    };
  }
  return {
    success: true,
    recommendations: result7.result.recommendations
  };
}
__name(recommendPatternsViaDaemon, "recommendPatternsViaDaemon");
async function assessRiskViaDaemon(workspaceRoot, filePath, content, previousContent, context) {
  const client = await getClient2(workspaceRoot);
  if (!client) {
    const cb = getCircuitBreaker(workspaceRoot);
    return {
      success: false,
      error: cb.state === "open" ? `Daemon unavailable (circuit open, retry in ${Math.ceil((cb.nextRetryTime - Date.now()) / 1e3)}s)` : "Daemon unavailable"
    };
  }
  const result7 = await protectedCall(workspaceRoot, "risk/assess", () => client.call("risk/assess", {
    workspace: workspaceRoot,
    filePath,
    content,
    previousContent,
    context
  }));
  if (!result7.success) {
    return {
      success: false,
      error: result7.error
    };
  }
  return {
    success: true,
    assessment: result7.result
  };
}
__name(assessRiskViaDaemon, "assessRiskViaDaemon");
async function quickAssessRiskViaDaemon(workspaceRoot, riskFactors) {
  const client = await getClient2(workspaceRoot);
  if (!client) {
    const cb = getCircuitBreaker(workspaceRoot);
    return {
      success: false,
      error: cb.state === "open" ? `Daemon unavailable (circuit open, retry in ${Math.ceil((cb.nextRetryTime - Date.now()) / 1e3)}s)` : "Daemon unavailable"
    };
  }
  const result7 = await protectedCall(workspaceRoot, "risk/quick-check", () => client.call("risk/quick-check", {
    workspace: workspaceRoot,
    riskFactors
  }));
  if (!result7.success) {
    return {
      success: false,
      error: result7.error
    };
  }
  return {
    success: true,
    assessment: result7.result
  };
}
__name(quickAssessRiskViaDaemon, "quickAssessRiskViaDaemon");
function formatRiskAssessmentViaDaemon(assessment) {
  const lines = [];
  lines.push(`Risk Assessment: ${assessment.score}/100 (${assessment.level})`);
  lines.push(`Recommendation: ${assessment.recommendation}`);
  lines.push(`Confidence: ${Math.round(assessment.confidence * 100)}%`);
  if (assessment.factors.length > 0) {
    lines.push("");
    lines.push("Risk Factors:");
    for (const factor of assessment.factors) {
      const sign = factor.score >= 0 ? "+" : "";
      lines.push(`  \u2022 [${factor.source}] ${factor.name}: ${factor.description} (${sign}${factor.score})`);
      if (factor.suggestion) {
        lines.push(`    Suggestion: ${factor.suggestion}`);
      }
    }
  }
  if (assessment.explanation) {
    lines.push("");
    lines.push(`Explanation: ${assessment.explanation}`);
  }
  return lines.join("\n");
}
__name(formatRiskAssessmentViaDaemon, "formatRiskAssessmentViaDaemon");
async function suggestRollbackViaDaemon(workspaceRoot, filePath, symptom) {
  const client = await getClient2(workspaceRoot);
  if (!client) {
    const cb = getCircuitBreaker(workspaceRoot);
    return {
      success: false,
      error: cb.state === "open" ? `Daemon unavailable (circuit open, retry in ${Math.ceil((cb.nextRetryTime - Date.now()) / 1e3)}s)` : "Daemon unavailable"
    };
  }
  const result7 = await protectedCall(workspaceRoot, "intelligence/suggestRollback", () => client.call("intelligence/suggestRollback", {
    workspace: workspaceRoot,
    filePath,
    symptom
  }));
  if (!result7.success) {
    return {
      success: false,
      error: result7.error
    };
  }
  const suggestions = result7.result.suggestions.map((s) => ({
    ...s,
    createdAt: new Date(s.createdAt)
  }));
  return {
    success: true,
    suggestions
  };
}
__name(suggestRollbackViaDaemon, "suggestRollbackViaDaemon");
var circuitBreakers;
var CIRCUIT_BREAKER_CONFIG;
var init_intelligence_adapter = __esm2({
  "src/daemon/intelligence-adapter.ts"() {
    init_client_pool();
    circuitBreakers = /* @__PURE__ */ new Map();
    CIRCUIT_BREAKER_CONFIG = {
      failureThreshold: 5,
      resetTimeout: 3e4,
      halfOpenMaxAttempts: 2
    };
    __name8(getCircuitBreaker, "getCircuitBreaker");
    __name8(recordSuccess2, "recordSuccess");
    __name8(recordFailure, "recordFailure");
    __name8(shouldAllowRequest, "shouldAllowRequest");
    __name8(resetCircuitBreaker, "resetCircuitBreaker");
    __name8(getClient2, "getClient");
    __name8(protectedCall, "protectedCall");
    __name8(getVitalsViaDaemon, "getVitalsViaDaemon");
    __name8(getContextViaDaemon, "getContextViaDaemon");
    __name8(generateEmbeddingViaDaemon, "generateEmbeddingViaDaemon");
    __name8(searchKnowledgeViaDaemon, "searchKnowledgeViaDaemon");
    __name8(hybridRetrieveViaDaemon, "hybridRetrieveViaDaemon");
    __name8(getIntelligenceViaDaemon, "getIntelligenceViaDaemon");
    __name8(queryPatternsViaDaemon, "queryPatternsViaDaemon");
    __name8(indexPatternsViaDaemon, "indexPatternsViaDaemon");
    __name8(recommendPatternsViaDaemon, "recommendPatternsViaDaemon");
    __name8(assessRiskViaDaemon, "assessRiskViaDaemon");
    __name8(quickAssessRiskViaDaemon, "quickAssessRiskViaDaemon");
    __name8(formatRiskAssessmentViaDaemon, "formatRiskAssessmentViaDaemon");
    __name8(suggestRollbackViaDaemon, "suggestRollbackViaDaemon");
  }
});
var local_service_adapter_exports = {};
__export2(local_service_adapter_exports, {
  addLearningViaDaemon: /* @__PURE__ */ __name(() => addLearningViaDaemon, "addLearningViaDaemon"),
  beginTaskViaDaemon: /* @__PURE__ */ __name(() => beginTaskViaService, "beginTaskViaDaemon"),
  beginTaskViaService: /* @__PURE__ */ __name(() => beginTaskViaService, "beginTaskViaService"),
  consolidateLearningsViaDaemon: /* @__PURE__ */ __name(() => consolidateLearningsViaDaemon, "consolidateLearningsViaDaemon"),
  createDaemonClient: /* @__PURE__ */ __name(() => createServiceClient2, "createDaemonClient"),
  createServiceClient: /* @__PURE__ */ __name(() => createServiceClient2, "createServiceClient"),
  createSnapshotViaDaemon: /* @__PURE__ */ __name(() => createSnapshotViaDaemon, "createSnapshotViaDaemon"),
  disposeAllDaemonConnections: /* @__PURE__ */ __name(() => disposeAllServiceConnections, "disposeAllDaemonConnections"),
  disposeAllServiceConnections: /* @__PURE__ */ __name(() => disposeAllServiceConnections, "disposeAllServiceConnections"),
  disposeDaemonConnection: /* @__PURE__ */ __name(() => disposeServiceConnection, "disposeDaemonConnection"),
  disposeServiceConnection: /* @__PURE__ */ __name(() => disposeServiceConnection, "disposeServiceConnection"),
  endTaskViaDaemon: /* @__PURE__ */ __name(() => endTaskViaService, "endTaskViaDaemon"),
  endTaskViaService: /* @__PURE__ */ __name(() => endTaskViaService, "endTaskViaService"),
  ensureEmbeddingsReadyViaDaemon: /* @__PURE__ */ __name(() => ensureEmbeddingsReadyViaDaemon, "ensureEmbeddingsReadyViaDaemon"),
  executeLearningGCViaDaemon: /* @__PURE__ */ __name(() => executeLearningGCViaService, "executeLearningGCViaDaemon"),
  executeLearningGCViaService: /* @__PURE__ */ __name(() => executeLearningGCViaService, "executeLearningGCViaService"),
  findSimilarPatternsViaDaemon: /* @__PURE__ */ __name(() => findSimilarPatternsViaDaemon, "findSimilarPatternsViaDaemon"),
  generateEmbeddingViaDaemon: /* @__PURE__ */ __name(() => generateEmbeddingViaDaemon, "generateEmbeddingViaDaemon"),
  getIntegrationContextViaDaemon: /* @__PURE__ */ __name(() => getIntegrationContextViaDaemon, "getIntegrationContextViaDaemon"),
  getSessionChangesViaDaemon: /* @__PURE__ */ __name(() => getSessionChangesViaService, "getSessionChangesViaDaemon"),
  getSessionChangesViaService: /* @__PURE__ */ __name(() => getSessionChangesViaService, "getSessionChangesViaService"),
  getSessionStatusViaDaemon: /* @__PURE__ */ __name(() => getSessionStatusViaService, "getSessionStatusViaDaemon"),
  getSessionStatusViaService: /* @__PURE__ */ __name(() => getSessionStatusViaService, "getSessionStatusViaService"),
  getVitalsViaDaemon: /* @__PURE__ */ __name(() => getVitalsViaDaemon, "getVitalsViaDaemon"),
  ingestKnowledgeViaDaemon: /* @__PURE__ */ __name(() => ingestKnowledgeViaDaemon, "ingestKnowledgeViaDaemon"),
  isDaemonAvailable: /* @__PURE__ */ __name(() => isServiceAvailable, "isDaemonAvailable"),
  isServiceAvailable: /* @__PURE__ */ __name(() => isServiceAvailable, "isServiceAvailable"),
  listSnapshotsViaDaemon: /* @__PURE__ */ __name(() => listSnapshotsViaDaemon, "listSnapshotsViaDaemon"),
  normalizeMethod: /* @__PURE__ */ __name(() => normalizeMethod, "normalizeMethod"),
  notifySnapshotCreatedResult: /* @__PURE__ */ __name(() => notifySnapshotCreatedResult, "notifySnapshotCreatedResult"),
  notifySnapshotCreatedViaDaemon: /* @__PURE__ */ __name(() => notifySnapshotCreatedViaService, "notifySnapshotCreatedViaDaemon"),
  notifySnapshotCreatedViaService: /* @__PURE__ */ __name(() => notifySnapshotCreatedViaService, "notifySnapshotCreatedViaService"),
  observePatternViaDaemon: /* @__PURE__ */ __name(() => observePatternViaDaemon, "observePatternViaDaemon"),
  quickCheckViaDaemon: /* @__PURE__ */ __name(() => quickCheckViaService, "quickCheckViaDaemon"),
  quickCheckViaService: /* @__PURE__ */ __name(() => quickCheckViaService, "quickCheckViaService"),
  recordFileModificationViaDaemon: /* @__PURE__ */ __name(() => recordFileModificationViaService, "recordFileModificationViaDaemon"),
  recordFileModificationViaService: /* @__PURE__ */ __name(() => recordFileModificationViaService, "recordFileModificationViaService"),
  restoreSnapshotViaDaemon: /* @__PURE__ */ __name(() => restoreSnapshotViaDaemon, "restoreSnapshotViaDaemon"),
  searchKnowledgeViaDaemon: /* @__PURE__ */ __name(() => searchKnowledgeViaDaemon, "searchKnowledgeViaDaemon"),
  searchLearningsViaDaemon: /* @__PURE__ */ __name(() => searchLearningsViaDaemon, "searchLearningsViaDaemon"),
  seedLearningsViaDaemon: /* @__PURE__ */ __name(() => seedLearningsViaDaemon, "seedLearningsViaDaemon"),
  shouldRethrowDaemonRequestError: /* @__PURE__ */ __name(() => shouldRethrowDaemonRequestError, "shouldRethrowDaemonRequestError"),
  trackAppliedViaDaemon: /* @__PURE__ */ __name(() => trackAppliedViaDaemon, "trackAppliedViaDaemon")
});
function isReadyMarkerValid() {
  try {
    if (!existsSync(READY_MARKER_PATH)) {
      return false;
    }
    const content = readFileSync(READY_MARKER_PATH, "utf8");
    const marker = JSON.parse(content);
    if (marker.ready !== true) {
      return false;
    }
    const readyAt = new Date(marker.readyAt).getTime();
    if (Date.now() - readyAt > 5 * 60 * 1e3) {
      if (!marker.pid) {
        return false;
      }
      try {
        process.kill(marker.pid, 0);
        return true;
      } catch {
        return false;
      }
    }
    return true;
  } catch {
    return false;
  }
}
__name(isReadyMarkerValid, "isReadyMarkerValid");
async function waitForReadyMarker(timeoutMs = 3e4) {
  const startTime = Date.now();
  let delay = 100;
  while (Date.now() - startTime < timeoutMs) {
    if (isReadyMarkerValid()) {
      return true;
    }
    await new Promise((r) => setTimeout(r, delay));
    delay = Math.min(delay * 2, 2e3);
  }
  return false;
}
__name(waitForReadyMarker, "waitForReadyMarker");
function findDaemonBin() {
  if (process.env.VREKO_DAEMON_BIN) {
    const explicit = process.env.VREKO_DAEMON_BIN;
    if (!existsSync(explicit)) {
      throw new Error(`VREKO_DAEMON_BIN points to missing file: ${explicit}`);
    }
    return explicit;
  }
  const cliBin = process.argv[1];
  if (!cliBin) {
    throw new Error("process.argv[1] not set  -  cannot locate daemon binary. Set VREKO_DAEMON_BIN env var.");
  }
  const workspaceRoot = resolve(dirname(cliBin), "../../..");
  const daemonBin = join(workspaceRoot, "apps", "local-service", "dist", "main.js");
  if (!existsSync(daemonBin)) {
    throw new Error(`Daemon binary not found at: ${daemonBin}. Set VREKO_DAEMON_BIN env var.`);
  }
  return daemonBin;
}
__name(findDaemonBin, "findDaemonBin");
async function autoStartDaemon() {
  if (autoStartInProgress) {
    return autoStartInProgress;
  }
  autoStartInProgress = (async () => {
    try {
      if (existsSync(SERVICE_SOCKET_PATH)) {
        try {
          await new Promise((resolve72, reject) => {
            const sock = createConnection(SERVICE_SOCKET_PATH, () => {
              sock.end();
              resolve72();
            });
            sock.on("error", reject);
            setTimeout(() => {
              sock.destroy();
              reject(new Error("connection timeout"));
            }, 500);
          });
          return;
        } catch {
          try {
            const { unlinkSync: unlinkSync42 } = await import('fs');
            unlinkSync42(SERVICE_SOCKET_PATH);
          } catch {
          }
        }
      }
      const daemonBin = findDaemonBin();
      let useDoppler = false;
      try {
        execSync("which doppler", {
          stdio: "ignore"
        });
        useDoppler = true;
      } catch {
      }
      const logDir = join(homedir(), ".vreko", "daemon");
      const logPath = join(logDir, "daemon.log");
      let logFd;
      try {
        mkdirSync(logDir, {
          recursive: true
        });
        logFd = openSync(logPath, "a");
      } catch {
        logFd = 1;
      }
      const spawnArgs = useDoppler ? [
        "run",
        "--",
        process.execPath,
        daemonBin,
        "--idle-timeout",
        "60"
      ] : [
        daemonBin,
        "--idle-timeout",
        "60"
      ];
      const spawnCmd = useDoppler ? "doppler" : process.execPath;
      const child = spawn(spawnCmd, spawnArgs, {
        detached: true,
        stdio: [
          "ignore",
          logFd,
          logFd
        ],
        env: process.env
      });
      child.unref();
      if (logFd !== 1) {
        closeSync(logFd);
      }
      child.on("error", (spawnErr) => {
        process.stderr.write(`[vreko] daemon spawn error: ${spawnErr.message}
`);
      });
      const deadline = Date.now() + 1e4;
      let lastError = null;
      while (Date.now() < deadline) {
        if (existsSync(SERVICE_SOCKET_PATH)) {
          try {
            await new Promise((resolve72, reject) => {
              const sock = createConnection(SERVICE_SOCKET_PATH, () => {
                sock.end();
                resolve72();
              });
              sock.on("error", reject);
              setTimeout(() => {
                sock.destroy();
                reject(new Error("connection timeout"));
              }, 500);
            });
            await new Promise((r) => setTimeout(r, 300));
            clearAllPoolConnectionStatuses();
            return;
          } catch (e) {
            lastError = e instanceof Error ? e : new Error(String(e));
          }
        }
        await new Promise((r) => setTimeout(r, 200));
      }
      throw new Error(`Daemon did not become ready within 10 seconds: ${lastError?.message ?? "socket not created"}`);
    } finally {
      autoStartInProgress = null;
    }
  })();
  return autoStartInProgress;
}
__name(autoStartDaemon, "autoStartDaemon");
function normalizeMethod(method) {
  const mapped = normalizeMethodName(method);
  if (mapped === method && method.includes(".")) {
    return method.replace(".", "/");
  }
  return mapped;
}
__name(normalizeMethod, "normalizeMethod");
async function getClient22(workspaceRoot) {
  const status = getPoolConnectionStatus(workspaceRoot);
  if (status) {
    const timeSinceLastAttempt = Date.now() - status.lastAttempt;
    if (status.consecutiveFailures >= MAX_CONSECUTIVE_FAILURES && timeSinceLastAttempt < MIN_RETRY_INTERVAL_MS) {
      return null;
    }
  }
  try {
    const client = await acquireClient(workspaceRoot);
    setPoolConnectionStatus(workspaceRoot, {
      connected: true,
      lastAttempt: Date.now(),
      consecutiveFailures: 0
    });
    resetCircuitBreaker(workspaceRoot);
    return client;
  } catch (connectErr) {
    const isSocketMissingOrDead = connectErr instanceof Error && (connectErr.message.includes("ENOENT") || connectErr.message.includes("no such file") || connectErr.message.includes("ECONNREFUSED") || connectErr.message.includes("Connection timeout") || connectErr.message.includes("connection timeout") || connectErr.message.includes("Not connected") || connectErr.message.includes("Connection closed") || connectErr.message.includes("connection closed"));
    if (isSocketMissingOrDead) {
      try {
        if (existsSync(SERVICE_SOCKET_PATH)) {
          try {
            unlinkSync(SERVICE_SOCKET_PATH);
          } catch {
          }
        }
        await autoStartDaemon();
        const ready = await waitForReadyMarker(3e4);
        if (!ready) {
          throw new Error("Daemon failed to initialize within 30 seconds");
        }
        removePooledClient(workspaceRoot);
        const retryClient = new VrekoLocalClient({
          timeout: 6e4,
          autoReconnect: true,
          maxReconnectAttempts: 5,
          reconnectDelay: 1e3,
          maxReconnectDelay: 3e4
        });
        await retryClient.connect();
        await retryClient.initialize({
          protocolVersion: "1.0.0",
          clientInfo: {
            name: "vreko-mcp",
            version: "1.0.0"
          },
          capabilities: {
            notifications: true
          }
        });
        setPooledClient(workspaceRoot, retryClient);
        setPoolConnectionStatus(workspaceRoot, {
          connected: true,
          lastAttempt: Date.now(),
          consecutiveFailures: 0
        });
        resetCircuitBreaker(workspaceRoot);
        return retryClient;
      } catch (_autoStartErr) {
      }
    }
    const prevStatus = getPoolConnectionStatus(workspaceRoot);
    const failures = (prevStatus?.consecutiveFailures ?? 0) + 1;
    setPoolConnectionStatus(workspaceRoot, {
      connected: false,
      lastAttempt: Date.now(),
      consecutiveFailures: failures
    });
    return null;
  }
}
__name(getClient22, "getClient2");
function isServiceAvailable(workspaceRoot) {
  return getPooledClient(workspaceRoot)?.isConnected() ?? false;
}
__name(isServiceAvailable, "isServiceAvailable");
async function disposeServiceConnection(workspaceRoot) {
  removePooledClient(workspaceRoot);
  clearPoolConnectionStatus(workspaceRoot);
}
__name(disposeServiceConnection, "disposeServiceConnection");
async function disposeAllServiceConnections() {
  clearPool();
}
__name(disposeAllServiceConnections, "disposeAllServiceConnections");
async function createSnapshotViaDaemon(workspaceRoot, files, options) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return {
      success: false,
      error: "Daemon unavailable"
    };
  }
  try {
    const result7 = await client.call("snapshot/create", {
      workspace: workspaceRoot,
      files,
      reason: options?.reason,
      trigger: options?.trigger ?? "auto"
    });
    return {
      success: true,
      snapshotId: result7.snapshotId,
      fileCount: result7.fileCount,
      totalSize: result7.totalSize,
      createdAt: result7.createdAt
    };
  } catch (error) {
    return {
      success: false,
      error: errorMessage(error)
    };
  }
}
__name(createSnapshotViaDaemon, "createSnapshotViaDaemon");
async function listSnapshotsViaDaemon(workspaceRoot, limit) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return [];
  }
  try {
    const result7 = await client.call("snapshot/list", {
      workspace: workspaceRoot,
      limit: limit ?? 20
    });
    return result7.snapshots;
  } catch {
    return [];
  }
}
__name(listSnapshotsViaDaemon, "listSnapshotsViaDaemon");
async function restoreSnapshotViaDaemon(workspaceRoot, snapshotId, options) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return {
      success: false,
      errors: [
        "Daemon unavailable"
      ]
    };
  }
  try {
    const result7 = await client.call("snapshot/restore", {
      workspace: workspaceRoot,
      snapshotId,
      files: options?.files,
      dryRun: options?.dryRun
    });
    return {
      success: result7.restored,
      restoredFiles: result7.changes.map((c) => c.file),
      errors: result7.errors
    };
  } catch (error) {
    return {
      success: false,
      errors: [
        error instanceof Error ? error.message : String(error)
      ]
    };
  }
}
__name(restoreSnapshotViaDaemon, "restoreSnapshotViaDaemon");
async function addLearningViaDaemon(workspaceRoot, learning) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return {
      success: false,
      error: "Daemon unavailable"
    };
  }
  try {
    const result7 = await client.call("learning/add", {
      workspace: workspaceRoot,
      ...learning
    });
    return {
      success: true,
      id: result7.id
    };
  } catch (error) {
    return {
      success: false,
      error: errorMessage(error)
    };
  }
}
__name(addLearningViaDaemon, "addLearningViaDaemon");
async function searchLearningsViaDaemon(workspaceRoot, keywords, limit) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return {
      success: false,
      error: "Daemon unavailable"
    };
  }
  try {
    const result7 = await client.call("learning/search", {
      workspace: workspaceRoot,
      keywords,
      limit: limit ?? 10
    });
    return {
      success: true,
      learnings: result7.learnings,
      total: result7.total
    };
  } catch (error) {
    return {
      success: false,
      error: errorMessage(error)
    };
  }
}
__name(searchLearningsViaDaemon, "searchLearningsViaDaemon");
async function trackAppliedViaDaemon(workspaceRoot, learningId, signalStrength) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return {
      success: false,
      error: "Daemon unavailable"
    };
  }
  try {
    const result7 = await client.call("learning/track-applied", {
      workspace: workspaceRoot,
      learningId,
      signalStrength: signalStrength ?? "medium"
    });
    return result7;
  } catch (error) {
    return {
      success: false,
      error: errorMessage(error)
    };
  }
}
__name(trackAppliedViaDaemon, "trackAppliedViaDaemon");
async function observePatternViaDaemon(workspaceRoot, signature, patternType, sessionId, confidence) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return {
      success: false,
      error: "Daemon unavailable"
    };
  }
  try {
    const result7 = await client.call("learning/observe-pattern", {
      workspace: workspaceRoot,
      signature,
      patternType,
      sessionId,
      confidence: confidence ?? 0.3
    });
    return {
      success: true,
      ...result7
    };
  } catch (error) {
    return {
      success: false,
      error: errorMessage(error)
    };
  }
}
__name(observePatternViaDaemon, "observePatternViaDaemon");
async function ingestKnowledgeViaDaemon(workspaceRoot, content) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return {
      success: false,
      error: "Daemon unavailable"
    };
  }
  try {
    const result7 = await client.call("knowledge/ingest", {
      workspace: workspaceRoot,
      ...content
    });
    return {
      success: true,
      id: result7.id
    };
  } catch (error) {
    return {
      success: false,
      error: errorMessage(error)
    };
  }
}
__name(ingestKnowledgeViaDaemon, "ingestKnowledgeViaDaemon");
async function ensureEmbeddingsReadyViaDaemon() {
  const client = getAnyConnectedClient() ?? null;
  if (!client) {
    return {
      success: false,
      error: "Daemon unavailable"
    };
  }
  try {
    const result7 = await client.call("intelligence/ensureEmbeddingsReady", {});
    return {
      success: true,
      ready: result7.ready
    };
  } catch (error) {
    return {
      success: false,
      error: errorMessage(error)
    };
  }
}
__name(ensureEmbeddingsReadyViaDaemon, "ensureEmbeddingsReadyViaDaemon");
async function getIntegrationContextViaDaemon(workspaceRoot, options) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return {
      success: false,
      error: "Daemon unavailable"
    };
  }
  try {
    const result7 = await client.call("integration/getContext", {
      workspace: workspaceRoot,
      files: options?.files,
      intent: options?.intent,
      skipCache: options?.skipCache
    });
    return {
      success: true,
      context: result7
    };
  } catch (error) {
    return {
      success: false,
      error: errorMessage(error)
    };
  }
}
__name(getIntegrationContextViaDaemon, "getIntegrationContextViaDaemon");
async function beginTaskViaService(workspaceRoot, task, files, keywords) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return null;
  }
  try {
    return await client.call("session/start", {
      workspacePath: workspaceRoot,
      task,
      files: files || [],
      keywords: keywords || []
    });
  } catch {
    return null;
  }
}
__name(beginTaskViaService, "beginTaskViaService");
async function getSessionChangesViaService(workspaceRoot, options) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return null;
  }
  try {
    return await client.call("session/changes", {
      workspacePath: workspaceRoot,
      ...options
    });
  } catch {
    return null;
  }
}
__name(getSessionChangesViaService, "getSessionChangesViaService");
async function endTaskViaService(workspaceRoot, outcome) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return null;
  }
  try {
    return await client.call("session/end-daemon", {
      workspacePath: workspaceRoot,
      outcome: outcome || "completed"
    });
  } catch {
    return null;
  }
}
__name(endTaskViaService, "endTaskViaService");
function isStaleSocketError(error) {
  if (!(error instanceof Error)) {
    return false;
  }
  const msg = error.message;
  return msg.includes("ECONNRESET") || msg.includes("ECONNREFUSED") || msg.includes("EPIPE") || msg.includes("ENOTCONN") || msg.includes("Connection closed") || msg.includes("connection closed") || msg.includes("Not connected") || msg.includes("Broken pipe") || msg.includes("broken pipe");
}
__name(isStaleSocketError, "isStaleSocketError");
function shouldRethrowDaemonRequestError(error) {
  return error instanceof JsonRpcClientError && error.code === ProtocolErrorCode.InvalidParams;
}
__name(shouldRethrowDaemonRequestError, "shouldRethrowDaemonRequestError");
function createServiceClient2(workspaceRoot) {
  return {
    isAvailable: /* @__PURE__ */ __name8(() => isServiceAvailable(workspaceRoot), "isAvailable"),
    // SESSION-04: retry wrapper  -  detects stale-socket errors and forces a fresh connection
    request: /* @__PURE__ */ __name8(async (method, params) => {
      for (let attempt = 0; attempt <= RECONNECT_DELAYS.length; attempt++) {
        const client = await getClient22(workspaceRoot);
        if (!client) {
          return null;
        }
        try {
          return await client.call(normalizeMethod(method), params);
        } catch (error) {
          if (attempt < RECONNECT_DELAYS.length && isStaleSocketError(error)) {
            removePooledClient(workspaceRoot);
            await new Promise((r) => setTimeout(r, RECONNECT_DELAYS[attempt]));
            continue;
          }
          if (shouldRethrowDaemonRequestError(error)) {
            throw error;
          }
          return null;
        }
      }
      return null;
    }, "request"),
    notifySnapshotCreated: /* @__PURE__ */ __name8(async (snapshotId, filePath) => {
      return notifySnapshotCreatedViaService(workspaceRoot, snapshotId, filePath);
    }, "notifySnapshotCreated")
  };
}
__name(createServiceClient2, "createServiceClient");
async function getSessionStatusViaService(workspaceRoot) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return null;
  }
  try {
    return await client.call("session/status", {
      workspacePath: workspaceRoot
    });
  } catch {
    return null;
  }
}
__name(getSessionStatusViaService, "getSessionStatusViaService");
async function quickCheckViaService(workspaceRoot, options) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return null;
  }
  try {
    return await client.call("validate/quick", {
      workspace: workspaceRoot,
      ...options
    });
  } catch {
    return null;
  }
}
__name(quickCheckViaService, "quickCheckViaService");
async function recordFileModificationViaService(workspaceRoot, filePath, linesChanged, aiAttributed) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return false;
  }
  const resolvedWorkspace = resolveToGitRoot(workspaceRoot) ?? workspaceRoot;
  try {
    await client.mcp.fileModified({
      workspace: resolvedWorkspace,
      path: filePath,
      linesChanged,
      aiAttributed,
      aiTool: "claude"
    });
    return true;
  } catch {
    return false;
  }
}
__name(recordFileModificationViaService, "recordFileModificationViaService");
async function notifySnapshotCreatedViaService(workspaceRoot, snapshotId, filePath, trigger) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return false;
  }
  try {
    await client.mcp.snapshotCreated({
      workspace: workspaceRoot,
      id: snapshotId,
      filePath: filePath || "unknown",
      trigger: trigger || "ai-detection",
      source: "mcp"
    });
    return true;
  } catch (error) {
    error instanceof Error ? createMcpErrorFromCause(McpErrorCode.DAEMON_REQUEST_FAILED, "Failed to notify service of snapshot creation", error, {
      snapshotId,
      filePath
    }) : createMcpError(McpErrorCode.DAEMON_REQUEST_FAILED, `Failed to notify service: ${String(error)}`, {
      snapshotId,
      filePath
    });
    return false;
  }
}
__name(notifySnapshotCreatedViaService, "notifySnapshotCreatedViaService");
async function notifySnapshotCreatedResult(workspaceRoot, snapshotId, filePath, trigger) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return err5(createMcpError(McpErrorCode.DAEMON_UNAVAILABLE, "Local service is not available", {
      workspaceRoot,
      snapshotId
    }));
  }
  try {
    await client.mcp.snapshotCreated({
      workspace: workspaceRoot,
      id: snapshotId,
      filePath: filePath || "unknown",
      trigger: trigger || "ai-detection",
      source: "mcp"
    });
    return ok4(void 0);
  } catch (error) {
    const mcpError = error instanceof Error ? createMcpErrorFromCause(McpErrorCode.DAEMON_REQUEST_FAILED, "Failed to notify service of snapshot creation", error, {
      snapshotId,
      filePath
    }) : createMcpError(McpErrorCode.DAEMON_REQUEST_FAILED, `Failed to notify service: ${String(error)}`, {
      snapshotId,
      filePath
    });
    return err5(mcpError);
  }
}
__name(notifySnapshotCreatedResult, "notifySnapshotCreatedResult");
async function executeLearningGCViaService(workspaceRoot, operation, dryRun) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return null;
  }
  try {
    return await client.call("learning/gc", {
      workspace: workspaceRoot,
      operation: operation || "archive",
      dryRun: dryRun ?? true
    });
  } catch {
    return null;
  }
}
__name(executeLearningGCViaService, "executeLearningGCViaService");
async function consolidateLearningsViaDaemon(workspaceRoot, options) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return {
      success: false,
      error: "Daemon unavailable"
    };
  }
  try {
    const result7 = await client.call("learning/consolidate", {
      workspace: workspaceRoot,
      threshold: options?.threshold ?? 0.65,
      dryRun: options?.dryRun ?? false,
      maxLearnings: options?.maxLearnings ?? 5e3
    });
    return {
      success: true,
      result: result7
    };
  } catch (error) {
    return {
      success: false,
      error: errorMessage(error)
    };
  }
}
__name(consolidateLearningsViaDaemon, "consolidateLearningsViaDaemon");
async function seedLearningsViaDaemon(workspaceRoot, learnings) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return {
      success: false,
      error: "Daemon unavailable"
    };
  }
  try {
    const normalizedLearnings = learnings.map((l) => ({
      ...l,
      trigger: Array.isArray(l.trigger) ? l.trigger.join(", ") : l.trigger
    }));
    const result7 = await client.call("learning/seed", {
      workspace: workspaceRoot,
      learnings: normalizedLearnings
    });
    return {
      success: true,
      seeded: result7.seeded
    };
  } catch (error) {
    return {
      success: false,
      error: errorMessage(error)
    };
  }
}
__name(seedLearningsViaDaemon, "seedLearningsViaDaemon");
async function findSimilarPatternsViaDaemon(workspaceRoot, targetFiles, limit = 5) {
  const client = await getClient22(workspaceRoot);
  if (!client) {
    return [];
  }
  try {
    const result7 = await client.call("intelligence/findSimilarPatterns", {
      workspace: workspaceRoot,
      targetFiles,
      limit
    });
    return result7.patterns || [];
  } catch {
    return [];
  }
}
__name(findSimilarPatternsViaDaemon, "findSimilarPatternsViaDaemon");
var MIN_RETRY_INTERVAL_MS;
var MAX_CONSECUTIVE_FAILURES;
var SERVICE_SOCKET_PATH;
var autoStartInProgress;
var READY_MARKER_PATH;
var RECONNECT_DELAYS;
var init_local_service_adapter = __esm2({
  "src/daemon/local-service-adapter.ts"() {
    init_errors();
    init_workspace();
    init_client_pool();
    init_intelligence_adapter();
    init_intelligence_adapter();
    MIN_RETRY_INTERVAL_MS = 2e3;
    MAX_CONSECUTIVE_FAILURES = 10;
    SERVICE_SOCKET_PATH = process.env.VREKO_DAEMON_SOCKET ?? getDefaultSocketPath();
    autoStartInProgress = null;
    READY_MARKER_PATH = join(homedir(), ".vreko", ".ready");
    __name8(isReadyMarkerValid, "isReadyMarkerValid");
    __name8(waitForReadyMarker, "waitForReadyMarker");
    __name8(findDaemonBin, "findDaemonBin");
    __name8(autoStartDaemon, "autoStartDaemon");
    __name8(normalizeMethod, "normalizeMethod");
    __name8(getClient22, "getClient");
    __name8(isServiceAvailable, "isServiceAvailable");
    __name8(disposeServiceConnection, "disposeServiceConnection");
    __name8(disposeAllServiceConnections, "disposeAllServiceConnections");
    __name8(createSnapshotViaDaemon, "createSnapshotViaDaemon");
    __name8(listSnapshotsViaDaemon, "listSnapshotsViaDaemon");
    __name8(restoreSnapshotViaDaemon, "restoreSnapshotViaDaemon");
    __name8(addLearningViaDaemon, "addLearningViaDaemon");
    __name8(searchLearningsViaDaemon, "searchLearningsViaDaemon");
    __name8(trackAppliedViaDaemon, "trackAppliedViaDaemon");
    __name8(observePatternViaDaemon, "observePatternViaDaemon");
    __name8(ingestKnowledgeViaDaemon, "ingestKnowledgeViaDaemon");
    __name8(ensureEmbeddingsReadyViaDaemon, "ensureEmbeddingsReadyViaDaemon");
    __name8(getIntegrationContextViaDaemon, "getIntegrationContextViaDaemon");
    __name8(beginTaskViaService, "beginTaskViaService");
    __name8(getSessionChangesViaService, "getSessionChangesViaService");
    __name8(endTaskViaService, "endTaskViaService");
    RECONNECT_DELAYS = [
      100,
      500,
      2e3
    ];
    __name8(isStaleSocketError, "isStaleSocketError");
    __name8(shouldRethrowDaemonRequestError, "shouldRethrowDaemonRequestError");
    __name8(createServiceClient2, "createServiceClient");
    __name8(getSessionStatusViaService, "getSessionStatusViaService");
    __name8(quickCheckViaService, "quickCheckViaService");
    __name8(recordFileModificationViaService, "recordFileModificationViaService");
    __name8(notifySnapshotCreatedViaService, "notifySnapshotCreatedViaService");
    __name8(notifySnapshotCreatedResult, "notifySnapshotCreatedResult");
    __name8(executeLearningGCViaService, "executeLearningGCViaService");
    __name8(consolidateLearningsViaDaemon, "consolidateLearningsViaDaemon");
    __name8(seedLearningsViaDaemon, "seedLearningsViaDaemon");
    __name8(findSimilarPatternsViaDaemon, "findSimilarPatternsViaDaemon");
  }
});
async function getSessionStateAsync(workspaceRoot) {
  const result7 = await getSessionStatusViaService(workspaceRoot);
  if (!result7) {
    return null;
  }
  return daemonStateToMCP(result7);
}
__name(getSessionStateAsync, "getSessionStateAsync");
async function getCurrentTaskAsync(workspaceRoot) {
  const result7 = await getSessionStatusViaService(workspaceRoot);
  if (!result7 || !result7.active) {
    return null;
  }
  return {
    id: result7.taskId ?? "",
    description: result7.task ?? "",
    startedAt: result7.startedAt ? new Date(result7.startedAt).getTime() : Date.now(),
    plannedFiles: [],
    keywords: []
  };
}
__name(getCurrentTaskAsync, "getCurrentTaskAsync");
async function startTaskAsync(workspaceRoot, task) {
  const { beginTaskViaService: beginTaskViaService2 } = await Promise.resolve().then(() => (init_local_service_adapter(), local_service_adapter_exports));
  const result7 = await beginTaskViaService2(workspaceRoot, task.description, task.plannedFiles, task.keywords);
  if (!result7) {
    return null;
  }
  return {
    id: result7.taskId,
    description: task.description,
    startedAt: Date.now(),
    plannedFiles: task.plannedFiles,
    keywords: task.keywords
  };
}
__name(startTaskAsync, "startTaskAsync");
async function endTaskAsync(workspaceRoot, outcome) {
  const result7 = await endTaskViaService(workspaceRoot, outcome);
  if (!result7) {
    return null;
  }
  return {
    id: "",
    description: "",
    startedAt: Date.now(),
    plannedFiles: [],
    keywords: []
  };
}
__name(endTaskAsync, "endTaskAsync");
async function recordFileChangeAsync(workspaceRoot, change) {
  await recordFileModificationViaService(workspaceRoot, change.file, change.linesChanged, change.aiAttributed);
}
__name(recordFileChangeAsync, "recordFileChangeAsync");
async function recordValidationEventAsync(workspaceRoot, _event) {
  await quickCheckViaService(workspaceRoot);
}
__name(recordValidationEventAsync, "recordValidationEventAsync");
async function trackActualToolCallAsync(_workspaceRoot, _tool, _mode) {
}
__name(trackActualToolCallAsync, "trackActualToolCallAsync");
async function pushObservationAsync(workspaceRoot, observation) {
  await addLearningViaDaemon(workspaceRoot, {
    type: observation.type,
    trigger: observation.message,
    action: "",
    source: "mcp-observation"
  });
}
__name(pushObservationAsync, "pushObservationAsync");
async function getSessionChangesAsync(workspaceRoot) {
  const result7 = await getSessionChangesViaService(workspaceRoot);
  if (!result7) {
    return [];
  }
  return result7.files.map((f) => ({
    file: f.path,
    type: f.status,
    timestamp: Date.now(),
    aiAttributed: f.aiAttributed ?? true,
    linesChanged: f.linesChanged
  }));
}
__name(getSessionChangesAsync, "getSessionChangesAsync");
async function computeFollowThroughRateAsync(workspaceRoot) {
  const state = await getSessionStateAsync(workspaceRoot);
  if (!state || !state.nextActionLog) {
    return {
      rate: 0,
      suggested: 0,
      followed: 0,
      details: [],
      altEvents: 0,
      altSuccess: 0,
      altRate: 0,
      combinedRate: 0
    };
  }
  const suggested = state.nextActionLog.suggested || [];
  const actualCalls = state.nextActionLog.actualCalls || [];
  const details = suggested.map((s) => ({
    tool: s.tool,
    suggested: true,
    called: actualCalls.some((a) => a.tool === s.tool && (s.mode ? a.mode === s.mode : true))
  }));
  const followed = details.filter((d) => d.called).length;
  const rate = suggested.length > 0 ? followed / suggested.length : 0;
  return {
    rate,
    suggested: suggested.length,
    followed,
    details,
    altEvents: 0,
    altSuccess: 0,
    altRate: 0,
    combinedRate: rate
  };
}
__name(computeFollowThroughRateAsync, "computeFollowThroughRateAsync");
async function recordToolCallEventAsync(workspaceRoot, event) {
  await addLearningViaDaemon(workspaceRoot, {
    type: "pattern",
    trigger: `tool_call:${event.tool}`,
    action: event.success ? "success" : `error:${event.errorCode}`,
    source: "mcp-tool-call"
  });
}
__name(recordToolCallEventAsync, "recordToolCallEventAsync");
async function drainPendingObservationsAsync(workspaceRoot) {
  const state = await getSessionStateAsync(workspaceRoot);
  if (!state) {
    return [];
  }
  const observations = [
    ...state.pendingObservations
  ];
  return observations;
}
__name(drainPendingObservationsAsync, "drainPendingObservationsAsync");
async function getBaselineFileSetAsync(workspaceRoot) {
  const state = await getSessionStateAsync(workspaceRoot);
  if (!state?.currentTask?.gitBaseline) {
    return /* @__PURE__ */ new Set();
  }
  return new Set(state.currentTask.gitBaseline.map((b) => b.file));
}
__name(getBaselineFileSetAsync, "getBaselineFileSetAsync");
function daemonStateToMCP(daemonResult) {
  if (!daemonResult || typeof daemonResult !== "object") {
    return null;
  }
  const d = daemonResult;
  return {
    currentTask: d.currentTask ? daemonSessionToCurrentTask(d.currentTask) : null,
    changesSinceTaskStart: daemonChangesToFileChanges(d.touchedFiles),
    pendingObservations: [],
    surfacedLearnings: [],
    riskAreasTouched: [],
    validatedFiles: [],
    stats: {
      tasksCompleted: 0,
      snapshotsCreated: d.snapshotIds?.length ?? 0,
      restoresPerformed: 0,
      learningsCaptured: d.learningIds?.length ?? 0,
      riskAnalysesPerformed: 0
    },
    pendingSuggestedLearnings: [],
    toolCallLog: [],
    validationEvents: [],
    workflowStage: daemonStatusToWorkflowStage(d.state)
  };
}
__name(daemonStateToMCP, "daemonStateToMCP");
function daemonSessionToCurrentTask(daemonSession) {
  if (!daemonSession || typeof daemonSession !== "object") {
    return null;
  }
  const s = daemonSession;
  return {
    id: s.id,
    description: s.metadata?.task ?? "",
    startedAt: typeof s.startedAt === "string" ? new Date(s.startedAt).getTime() : Date.now(),
    plannedFiles: s.metadata?.files ?? [],
    keywords: s.metadata?.keywords ?? []
  };
}
__name(daemonSessionToCurrentTask, "daemonSessionToCurrentTask");
function daemonChangesToFileChanges(daemonChanges) {
  if (!Array.isArray(daemonChanges)) {
    return [];
  }
  return daemonChanges.map((f, _idx) => ({
    file: f.path ?? f.file ?? "",
    type: f.status ?? "modified",
    timestamp: Date.now(),
    aiAttributed: f.aiAttributed ?? true,
    linesChanged: f.linesChanged ?? 0
  }));
}
__name(daemonChangesToFileChanges, "daemonChangesToFileChanges");
function daemonStatusToWorkflowStage(state) {
  switch (state) {
    case "active":
      return "developing";
    case "ended":
      return "idle";
    default:
      return "idle";
  }
}
__name(daemonStatusToWorkflowStage, "daemonStatusToWorkflowStage");
var init_session_adapter = __esm2({
  "src/adapters/session-adapter.ts"() {
    init_local_service_adapter();
    __name8(getSessionStateAsync, "getSessionStateAsync");
    __name8(getCurrentTaskAsync, "getCurrentTaskAsync");
    __name8(startTaskAsync, "startTaskAsync");
    __name8(endTaskAsync, "endTaskAsync");
    __name8(recordFileChangeAsync, "recordFileChangeAsync");
    __name8(recordValidationEventAsync, "recordValidationEventAsync");
    __name8(trackActualToolCallAsync, "trackActualToolCallAsync");
    __name8(pushObservationAsync, "pushObservationAsync");
    __name8(getSessionChangesAsync, "getSessionChangesAsync");
    __name8(computeFollowThroughRateAsync, "computeFollowThroughRateAsync");
    __name8(recordToolCallEventAsync, "recordToolCallEventAsync");
    __name8(drainPendingObservationsAsync, "drainPendingObservationsAsync");
    __name8(getBaselineFileSetAsync, "getBaselineFileSetAsync");
    __name8(daemonStateToMCP, "daemonStateToMCP");
    __name8(daemonSessionToCurrentTask, "daemonSessionToCurrentTask");
    __name8(daemonChangesToFileChanges, "daemonChangesToFileChanges");
    __name8(daemonStatusToWorkflowStage, "daemonStatusToWorkflowStage");
  }
});
function formatWireLabeled(type, fields, options) {
  const parts = [
    WIRE_PREFIX,
    type
  ];
  const entries = Object.entries(fields).filter(([, v]) => v !== void 0);
  if (options?.compact) {
    parts.push(...entries.map(([, v]) => String(v)));
  } else {
    parts.push(...entries.map(([k, v]) => `${k}:${v}`));
  }
  return parts.join("|");
}
__name(formatWireLabeled, "formatWireLabeled");
function formatWire(type, ...fields) {
  return [
    WIRE_PREFIX,
    type,
    ...fields.map(String)
  ].join("|");
}
__name(formatWire, "formatWire");
function formatMessage(message) {
  if (message.startsWith(BRAND_PREFIX) || message.startsWith(WIRE_PREFIX)) {
    return message;
  }
  return `${BRAND_PREFIX} ${message}`;
}
__name(formatMessage, "formatMessage");
function compress2(s, maxLength) {
  const clean = s.replace(/\s+/g, "\u2192");
  return clean.length > maxLength ? `${clean.slice(0, maxLength - 1)}\u2026` : clean;
}
__name(compress2, "compress");
function getRelativeTime(timestamp) {
  const diff = Date.now() - timestamp;
  const minutes = Math.floor(diff / 6e4);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  if (days > 0) {
    return `${days}d`;
  }
  if (hours > 0) {
    return `${hours}h`;
  }
  if (minutes > 0) {
    return `${minutes}m`;
  }
  return "just now";
}
__name(getRelativeTime, "getRelativeTime");
function getRelativeTimeHuman(timestamp) {
  const diff = Date.now() - timestamp;
  const minutes = Math.floor(diff / 6e4);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  if (days > 0) {
    return `${days} day${days !== 1 ? "s" : ""} ago`;
  }
  if (hours > 0) {
    return `${hours} hour${hours !== 1 ? "s" : ""} ago`;
  }
  if (minutes > 0) {
    return `${minutes} minute${minutes !== 1 ? "s" : ""} ago`;
  }
  return "just now";
}
__name(getRelativeTimeHuman, "getRelativeTimeHuman");
function buildStartResponse(data, options) {
  const learningsStr = data.learnings?.slice(0, 3).map((l) => compress2(l, 40)).join(",") || "";
  const hotspotsStr = data.hotspots?.slice(0, 2).join(",") || "";
  const wire = formatWireLabeled("S", {
    id: data.taskId,
    vreko: data.snapshotId || "none",
    risk: data.risk,
    prot: data.protection,
    dirty: data.dirty,
    status: data.snapshotStatus,
    ...learningsStr && {
      learn: learningsStr
    },
    ...hotspotsStr && {
      hotspots: hotspotsStr
    }
  }, options);
  const message = data.snapshotStatus === "created" ? messages.snapshot.created(data.taskDescription) : data.snapshotStatus === "reused" ? messages.snapshot.reused() : void 0;
  return {
    wire,
    message
  };
}
__name(buildStartResponse, "buildStartResponse");
function buildCheckResponse(data, options) {
  const tsStatus = data.checked?.typescript ? "\u2713" : "\u2717";
  const lintStatus = data.checked?.lint ? "\u2713" : "\u2717";
  const testsStatus = data.checked?.tests === true ? "\u2713" : data.checked?.tests === "skipped" ? "\u23ED\uFE0F" : "\u2717";
  const issuesStr = data.issues?.slice(0, 3).map((i) => compress2(i, 40)).join(",") || "";
  const wire = formatWireLabeled(data.mode, {
    status: data.status === "OK" ? "\u2713" : "\u2717",
    err: `${data.errors}E`,
    warn: `${data.warnings}W`,
    ts: tsStatus,
    lint: lintStatus,
    tests: testsStatus,
    ...issuesStr && {
      issues: issuesStr
    }
  }, options);
  const message = data.status === "OK" ? messages.validation.passed() : messages.validation.issues(data.errors, data.warnings);
  return {
    wire,
    message
  };
}
__name(buildCheckResponse, "buildCheckResponse");
function buildRestoreResponse(data) {
  const timeAgo = getRelativeTimeHuman(data.snapshotTime);
  if (data.isDry) {
    const wire2 = formatWire("R", "DRY", `${(data.files || []).length}f`, ...(data.files || []).slice(0, 5));
    return {
      wire: wire2,
      message: messages.restore.preview((data.files || []).length)
    };
  }
  const wire = formatWire("R", "OK", `${(data.files || []).length}f`, ...(data.files || []).slice(0, 5));
  const message = (data.files || []).length === 1 ? messages.restore.single((data.files || [])[0], timeAgo) : messages.restore.multiple((data.files || []).length, timeAgo);
  return {
    wire,
    message
  };
}
__name(buildRestoreResponse, "buildRestoreResponse");
function buildListResponse(snapshots) {
  if (snapshots.length === 0) {
    return {
      wire: formatWire("R", "0", "No snapshots available"),
      message: messages.restore.notFound()
    };
  }
  const parts = snapshots.slice(0, 5).map((vreko) => {
    const age = getRelativeTime(vreko.createdAt);
    return `${vreko.id.slice(0, 8)}:${age}:${vreko.fileCount}f`;
  });
  return {
    wire: formatWire("R", String(snapshots.length), ...parts)
  };
}
__name(buildListResponse, "buildListResponse");
function buildEndResponse(data, options) {
  const learningsStr = data.learnings?.slice(0, 2).map((l) => compress2(l, 30)).join(",") || "";
  const wire = formatWireLabeled("E", {
    status: data.success ? "OK" : "FAIL",
    learn: `${data.learningsCount}L`,
    files: `${data.filesModified}F`,
    lines: `+${data.linesAdded}-${data.linesRemoved}`,
    ...learningsStr && {
      summary: learningsStr
    }
  }, options);
  const wireWithSurvey = data.survey ? `${wire}|${JSON.stringify(data.survey)}` : wire;
  const statsParts = [];
  if (data.tokensSaved && data.tokensSaved > 500) {
    statsParts.push(`\u{1F4B0} ~${data.tokensSaved.toLocaleString()} tokens saved (${data.savingsPercent}%)`);
  }
  if (data.mistakesPrevented && data.mistakesPrevented > 0) {
    statsParts.push(`\u{1F6E1}\uFE0F ${data.mistakesPrevented} ${data.mistakesPrevented === 1 ? "issue" : "issues"} prevented`);
  }
  if (data.learningsCount > 0) {
    statsParts.push(`\u{1F4DA} ${data.learningsCount} captured`);
  }
  const message = statsParts.length > 0 ? statsParts.join(" | ") : void 0;
  return {
    wire: wireWithSurvey,
    message
  };
}
__name(buildEndResponse, "buildEndResponse");
function buildLearnResponse(data, options) {
  const fullType = LEARNING_TYPE_MAP[data.type] || data.type;
  const wire = formatWireLabeled("L", {
    status: "OK",
    id: data.id,
    type: fullType
  }, options);
  return {
    wire,
    message: messages.learning.captured(fullType)
  };
}
__name(buildLearnResponse, "buildLearnResponse");
function buildViolationResponse(data, options) {
  const wire = formatWireLabeled("V", {
    status: "OK",
    type: data.type,
    count: data.count,
    promote: data.shouldPromote ? "PROMOTED" : "NO",
    auto: data.shouldAutomate ? "YES" : "NO"
  }, options);
  const message = data.shouldAutomate ? messages.violation.automate(data.type) : data.shouldPromote ? messages.violation.promoted(data.type) : messages.violation.recorded(data.type, data.count);
  return {
    wire,
    message
  };
}
__name(buildViolationResponse, "buildViolationResponse");
function buildErrorResponse2(reason, code, options) {
  const wire = formatWireLabeled("!", {
    code: code || "ERR",
    reason: compress2(reason, 60)
  }, options);
  return {
    wire,
    message: messages.error.generic(reason)
  };
}
__name(buildErrorResponse2, "buildErrorResponse2");
function capitalize(s) {
  return s.charAt(0).toUpperCase() + s.slice(1);
}
__name(capitalize, "capitalize");
function formatToolResult(response, humanize = false) {
  if (humanize && response.message) {
    return `${response.wire}
${response.message}`;
  }
  return response.wire;
}
__name(formatToolResult, "formatToolResult");
var BRAND_PREFIX;
var WIRE_PREFIX;
var INTERNAL_SEPARATOR;
var messages;
var LEARNING_TYPE_MAP;
var init_branding = __esm2({
  "src/branding/index.ts"() {
    init_errors();
    BRAND_PREFIX = "\u{1F98E} Vreko:";
    WIRE_PREFIX = "\u{1F98E}";
    INTERNAL_SEPARATOR = "\n---\n";
    messages = {
      // -------------------------------------------------------------------------
      // Snapshot/Checkpoint Operations
      // -------------------------------------------------------------------------
      snapshot: {
        /** Default checkpoint created message */
        created: /* @__PURE__ */ __name8((context) => context ? `${BRAND_PREFIX} Got it. Checkpoint created before ${context}.` : `${BRAND_PREFIX} Checkpoint created.`, "created"),
        /** Checkpoint with file count */
        createdWithStats: /* @__PURE__ */ __name8((files, lines) => `${BRAND_PREFIX} Created checkpoint. Captured ${files} file${files !== 1 ? "s" : ""}, ${lines.toLocaleString()} lines.`, "createdWithStats"),
        /** Proactive checkpoint before risky operation */
        proactive: /* @__PURE__ */ __name8((reason) => `${BRAND_PREFIX} Creating a checkpoint first - ${reason}.`, "proactive"),
        /** Checkpoint reused */
        reused: /* @__PURE__ */ __name8(() => `${BRAND_PREFIX} Recent checkpoint available.`, "reused"),
        /** Checkpoint skipped */
        skipped: /* @__PURE__ */ __name8(() => `${BRAND_PREFIX} Skipped checkpoint - already protected.`, "skipped")
      },
      // -------------------------------------------------------------------------
      // Restore Operations
      // -------------------------------------------------------------------------
      restore: {
        /** Single file restored */
        single: /* @__PURE__ */ __name8((filename, timeAgo) => `${BRAND_PREFIX} Restored \`${filename}\` to ${timeAgo}.`, "single"),
        /** Multiple files restored */
        multiple: /* @__PURE__ */ __name8((count, timeAgo) => `${BRAND_PREFIX} Restored ${count} files to ${timeAgo}.`, "multiple"),
        /** Restore with reassurance about saved changes */
        withReassurance: /* @__PURE__ */ __name8((filename, snapshotId) => `${BRAND_PREFIX} Restored \`${filename}\`. Your recent changes are saved in snapshot #${snapshotId} if you need them back.`, "withReassurance"),
        /** Preview mode */
        preview: /* @__PURE__ */ __name8((count) => `${BRAND_PREFIX} Would restore ${count} file${count !== 1 ? "s" : ""}.`, "preview"),
        /** Can't restore - unsaved changes */
        cantRestoreUnsaved: /* @__PURE__ */ __name8((filename) => `${BRAND_PREFIX} Can't restore \`${filename}\` - you have unsaved changes. Save or discard first?`, "cantRestoreUnsaved"),
        /** Snapshot not found */
        notFound: /* @__PURE__ */ __name8(() => `${BRAND_PREFIX} No checkpoint found for that file. Want to create one now?`, "notFound")
      },
      // -------------------------------------------------------------------------
      // Protection Status/Warnings
      // -------------------------------------------------------------------------
      protection: {
        /** Gentle nudge for unprotected changes */
        nudge: /* @__PURE__ */ __name8((lines) => `${BRAND_PREFIX} You've got ${lines.toLocaleString()}+ lines of unprotected changes. Want a checkpoint?`, "nudge"),
        /** Warning before risky operation */
        beforeRisky: /* @__PURE__ */ __name8((filename) => `${BRAND_PREFIX} No recent checkpoint for \`${filename}\`. Should I create one before we continue?`, "beforeRisky"),
        /** All clear status */
        allClear: /* @__PURE__ */ __name8((minutesAgo) => `${BRAND_PREFIX} You're covered. Last checkpoint was ${minutesAgo} minute${minutesAgo !== 1 ? "s" : ""} ago.`, "allClear"),
        /** Files protected count */
        protected: /* @__PURE__ */ __name8((count) => `${BRAND_PREFIX} ${count} file${count !== 1 ? "s" : ""} protected.`, "protected")
      },
      // -------------------------------------------------------------------------
      // Session Operations
      // -------------------------------------------------------------------------
      session: {
        /** AI session started */
        started: /* @__PURE__ */ __name8(() => `${BRAND_PREFIX} AI session detected. Auto-protecting changes.`, "started"),
        /** Session summary on completion */
        complete: /* @__PURE__ */ __name8((checkpoints, files) => `${BRAND_PREFIX} Session complete. ${checkpoints} checkpoint${checkpoints !== 1 ? "s" : ""}, ${files} file${files !== 1 ? "s" : ""} protected.`, "complete"),
        /** Task started */
        taskStarted: /* @__PURE__ */ __name8((taskDesc) => taskDesc ? `${BRAND_PREFIX} Got it. Starting: ${taskDesc}` : `${BRAND_PREFIX} Task started.`, "taskStarted")
      },
      // -------------------------------------------------------------------------
      // Validation/Check Operations
      // -------------------------------------------------------------------------
      validation: {
        /** All checks passed */
        passed: /* @__PURE__ */ __name8(() => `${BRAND_PREFIX} All clear. Code looks good.`, "passed"),
        /** Issues found */
        issues: /* @__PURE__ */ __name8((errors, warnings) => `${BRAND_PREFIX} Found ${errors} error${errors !== 1 ? "s" : ""}, ${warnings} warning${warnings !== 1 ? "s" : ""}.`, "issues"),
        /** Quick validation complete */
        quickComplete: /* @__PURE__ */ __name8(() => `${BRAND_PREFIX} Quick check done.`, "quickComplete")
      },
      // -------------------------------------------------------------------------
      // Learning Operations
      // -------------------------------------------------------------------------
      learning: {
        /** Learning captured */
        captured: /* @__PURE__ */ __name8((type) => `${BRAND_PREFIX} Got it. ${capitalize(type)} captured for next time.`, "captured"),
        /** Learning applied */
        applied: /* @__PURE__ */ __name8((count) => `${BRAND_PREFIX} Applied ${count} past learning${count !== 1 ? "s" : ""} to this task.`, "applied")
      },
      // -------------------------------------------------------------------------
      // Violation Operations
      // -------------------------------------------------------------------------
      violation: {
        /** Violation recorded */
        recorded: /* @__PURE__ */ __name8((type, count) => `${BRAND_PREFIX} Noted: ${type} (occurrence #${count}).`, "recorded"),
        /** Violation promoted to pattern */
        promoted: /* @__PURE__ */ __name8((type) => `${BRAND_PREFIX} Heads up: ${type} is now a tracked pattern - seen 3+ times.`, "promoted"),
        /** Violation marked for automation */
        automate: /* @__PURE__ */ __name8((type) => `${BRAND_PREFIX} ${type} marked for automation - seen 5+ times.`, "automate")
      },
      // -------------------------------------------------------------------------
      // Errors & Edge Cases
      // -------------------------------------------------------------------------
      error: {
        /** Generic error */
        generic: /* @__PURE__ */ __name8((reason) => `${BRAND_PREFIX} Couldn't complete - ${reason}.`, "generic"),
        /** Storage issue */
        storage: /* @__PURE__ */ __name8(() => `${BRAND_PREFIX} Heads up - running low on snapshot storage. Older checkpoints will auto-clean.`, "storage"),
        /** Not found */
        notFound: /* @__PURE__ */ __name8((what) => `${BRAND_PREFIX} ${what} not found.`, "notFound")
      },
      // -------------------------------------------------------------------------
      // Token Savings (optional, only when meaningful)
      // -------------------------------------------------------------------------
      efficiency: {
        /** Token savings report */
        tokensSaved: /* @__PURE__ */ __name8((tokens) => `${BRAND_PREFIX} Saved you ~${tokens.toLocaleString()} tokens vs. re-prompting.`, "tokensSaved"),
        /** Session savings summary */
        sessionSavings: /* @__PURE__ */ __name8((restores, dollarsSaved) => `${BRAND_PREFIX} This session: ${restores} restore${restores !== 1 ? "s" : ""} \u2192 ~$${dollarsSaved} in tokens saved.`, "sessionSavings")
      }
    };
    __name8(formatWireLabeled, "formatWireLabeled");
    __name8(formatWire, "formatWire");
    __name8(formatMessage, "formatMessage");
    __name8(compress2, "compress");
    __name8(getRelativeTime, "getRelativeTime");
    __name8(getRelativeTimeHuman, "getRelativeTimeHuman");
    __name8(buildStartResponse, "buildStartResponse");
    __name8(buildCheckResponse, "buildCheckResponse");
    __name8(buildRestoreResponse, "buildRestoreResponse");
    __name8(buildListResponse, "buildListResponse");
    __name8(buildEndResponse, "buildEndResponse");
    LEARNING_TYPE_MAP = {
      pat: "pattern",
      pattern: "pattern",
      pit: "pitfall",
      pitfall: "pitfall",
      eff: "efficiency",
      efficiency: "efficiency",
      disc: "discovery",
      discovery: "discovery",
      wf: "workflow",
      workflow: "workflow",
      con: "constraint",
      constraint: "constraint",
      prf: "preference",
      preference: "preference"
    };
    __name8(buildLearnResponse, "buildLearnResponse");
    __name8(buildViolationResponse, "buildViolationResponse");
    __name8(buildErrorResponse2, "buildErrorResponse");
    __name8(capitalize, "capitalize");
    __name8(formatToolResult, "formatToolResult");
  }
});
function getResponseConfig(tier) {
  return TIER_LIMITS[tier] || TIER_LIMITS.local;
}
__name(getResponseConfig, "getResponseConfig");
function compressResponse(data, config2) {
  const full = JSON.stringify(data);
  if (full.length <= config2.maxBytes) {
    return data;
  }
  const compressed = JSON.parse(full);
  const arrayFields = [
    "learnings",
    "relevantLearnings",
    "violations",
    "recentViolations",
    "snapshots",
    "files"
  ];
  const maxArrayItems = config2.tier === "pro" ? 10 : config2.tier === "local" ? 5 : 3;
  for (const field of arrayFields) {
    if (Array.isArray(compressed[field]) && compressed[field].length > maxArrayItems) {
      const arr = compressed[field];
      const truncated = arr.slice(0, maxArrayItems);
      compressed[field] = truncated;
      compressed[`${field}Truncated`] = true;
      compressed[`${field}Total`] = arr.length;
    }
  }
  if (JSON.stringify(compressed).length <= config2.maxBytes) {
    return compressed;
  }
  const verboseFields = [
    "formatted",
    "fullDiagnosis",
    "stackTrace",
    "rawOutput",
    "fullPath",
    "absolutePath"
  ];
  for (const field of verboseFields) {
    if (field in compressed) {
      delete compressed[field];
    }
  }
  function removeVerboseNested(obj) {
    for (const key of Object.keys(obj)) {
      if (verboseFields.includes(key)) {
        delete obj[key];
      } else if (typeof obj[key] === "object" && obj[key] !== null && !Array.isArray(obj[key])) {
        removeVerboseNested(obj[key]);
      }
    }
  }
  __name(removeVerboseNested, "removeVerboseNested");
  __name8(removeVerboseNested, "removeVerboseNested");
  removeVerboseNested(compressed);
  if (JSON.stringify(compressed).length <= config2.maxBytes) {
    return compressed;
  }
  function truncateStrings(obj, maxLen) {
    for (const key of Object.keys(obj)) {
      const val = obj[key];
      if (typeof val === "string" && val.length > maxLen) {
        obj[key] = `${val.slice(0, maxLen)}...`;
      } else if (typeof val === "object" && val !== null && !Array.isArray(val)) {
        truncateStrings(val, maxLen);
      }
    }
  }
  __name(truncateStrings, "truncateStrings");
  __name8(truncateStrings, "truncateStrings");
  const maxStringLen = config2.tier === "pro" ? 500 : config2.tier === "local" ? 200 : 100;
  truncateStrings(compressed, maxStringLen);
  compressed._compressed = true;
  compressed._originalSize = full.length;
  compressed._tier = config2.tier;
  return compressed;
}
__name(compressResponse, "compressResponse");
function buildSmartActions(baseActions, context) {
  const actions = [];
  for (const base of baseActions) {
    const action = {
      tool: base.tool,
      priority: base.priority || 2,
      confidence: 0.7,
      reason: base.reason
    };
    switch (base.tool) {
      case "snapshot_create":
        action.confidence = context.hasUnsavedChanges ? 0.95 : 0.5;
        action.condition = "Before making risky changes";
        action.skipIf = [
          "Files unchanged since last snapshot"
        ];
        if ((context.lastSnapshotMinutes ?? 0) < 5) {
          action.confidence = 0.2;
          action.skipIf?.push("Recent snapshot exists");
        }
        break;
      case "check_patterns":
        action.confidence = context.hasCodeChanges ? 0.95 : 0.6;
        action.condition = "Before committing code";
        action.skipIf = [
          "Already validated this session"
        ];
        break;
      case "get_context":
        action.confidence = context.isNewTask ? 0.95 : 0.4;
        action.condition = "When starting a new task";
        action.skipIf = [
          "Context already loaded for this task"
        ];
        break;
      case "get_learnings":
        action.confidence = (context.violationCount ?? 0) > 2 ? 0.85 : 0.5;
        action.condition = "When encountering repeated issues";
        break;
      case "report_violation":
        action.confidence = 0.8;
        action.condition = "After catching a mistake";
        break;
    }
    actions.push(action);
  }
  return actions.sort((a, b) => {
    if (a.priority !== b.priority) {
      return a.priority - b.priority;
    }
    return b.confidence - a.confidence;
  });
}
__name(buildSmartActions, "buildSmartActions");
function isLearningStalel(learning, maxAgeDays = 90) {
  if (learning.deprecated) {
    return true;
  }
  if (learning.validUntil && new Date(learning.validUntil) < /* @__PURE__ */ new Date()) {
    return true;
  }
  if (learning.timestamp) {
    const age = Date.now() - new Date(learning.timestamp).getTime();
    const ageDays = age / (1e3 * 60 * 60 * 24);
    if (ageDays > maxAgeDays) {
      return true;
    }
  }
  return false;
}
__name(isLearningStalel, "isLearningStalel");
function filterStaleLearnings(learnings, maxAgeDays = 90) {
  const valid = [];
  const stale = [];
  for (const learning of learnings) {
    if (isLearningStalel(learning, maxAgeDays)) {
      stale.push(learning);
    } else {
      valid.push(learning);
    }
  }
  return {
    valid,
    stale
  };
}
__name(filterStaleLearnings, "filterStaleLearnings");
function formatBytes4(bytes) {
  if (bytes === 0) {
    return "0 B";
  }
  const units = [
    "B",
    "KB",
    "MB",
    "GB"
  ];
  const i = Math.floor(Math.log(bytes) / Math.log(1024));
  const value = bytes / 1024 ** i;
  return `${value.toFixed(1)} ${units[i]}`;
}
__name(formatBytes4, "formatBytes");
function getArchitectureVersion(workspaceRoot) {
  const ctxPath = `${workspaceRoot}/.vreko/ctx/context.json`;
  try {
    if (existsSync(ctxPath)) {
      const ctx = JSON.parse(readFileSync(ctxPath, "utf8"));
      return ctx.archVersion || ctx.updated_at || null;
    }
  } catch {
  }
  return null;
}
__name(getArchitectureVersion, "getArchitectureVersion");
function cleanupStaleLearnings(learningsPath, opts) {
  if (!existsSync(learningsPath)) {
    return {
      found: 0,
      stale: 0,
      deleted: 0
    };
  }
  try {
    const content = readFileSync(learningsPath, "utf8");
    const lines = content.split("\n").filter(Boolean);
    const learnings = lines.map((line) => {
      try {
        return JSON.parse(line);
      } catch {
        return null;
      }
    }).filter(Boolean);
    const { valid, stale } = filterStaleLearnings(learnings, opts.maxAgeDays || 90);
    let archStale = [];
    if (opts.archVersion) {
      archStale = valid.filter((l) => l.archVersion && l.archVersion !== opts.archVersion);
    }
    const allStale = [
      ...stale,
      ...archStale
    ];
    const remaining = valid.filter((l) => !archStale.includes(l));
    if (!opts.dryRun && allStale.length > 0) {
      const newContent = `${remaining.map((l) => JSON.stringify(l)).join("\n")}
`;
      writeFileSync(learningsPath, newContent);
    }
    return {
      found: learnings.length,
      stale: allStale.length,
      deleted: opts.dryRun ? 0 : allStale.length
    };
  } catch {
    return {
      found: 0,
      stale: 0,
      deleted: 0
    };
  }
}
__name(cleanupStaleLearnings, "cleanupStaleLearnings");
function cleanupArchivedSessions(sessionArchiveDir, opts) {
  if (!existsSync(sessionArchiveDir)) {
    return {
      found: 0,
      stale: 0,
      deleted: 0
    };
  }
  try {
    const files = readdirSync(sessionArchiveDir).filter((f) => f.endsWith(".json"));
    const cutoff = Date.now() - (opts.maxAgeDays || 30) * 24 * 60 * 60 * 1e3;
    const stale = [];
    for (const file of files) {
      const filePath = `${sessionArchiveDir}/${file}`;
      try {
        const session = JSON.parse(readFileSync(filePath, "utf8"));
        const endedAt = new Date(session.endedAt || session.startedAt).getTime();
        if (endedAt < cutoff) {
          stale.push(filePath);
        }
      } catch {
      }
    }
    if (!opts.dryRun) {
      for (const filePath of stale) {
        try {
          unlinkSync(filePath);
        } catch {
        }
      }
    }
    return {
      found: files.length,
      stale: stale.length,
      deleted: opts.dryRun ? 0 : stale.length
    };
  } catch {
    return {
      found: 0,
      stale: 0,
      deleted: 0
    };
  }
}
__name(cleanupArchivedSessions, "cleanupArchivedSessions");
var TIER_LIMITS;
var init_response_utils = __esm2({
  "src/facades/response-utils.ts"() {
    TIER_LIMITS = {
      pro: {
        tier: "pro",
        maxBytes: 50 * 1024
      },
      local: {
        tier: "local",
        maxBytes: 15 * 1024
      },
      free: {
        tier: "free",
        maxBytes: 8 * 1024
      }
    };
    __name8(getResponseConfig, "getResponseConfig");
    __name8(compressResponse, "compressResponse");
    __name8(buildSmartActions, "buildSmartActions");
    __name8(isLearningStalel, "isLearningStalel");
    __name8(filterStaleLearnings, "filterStaleLearnings");
    __name8(formatBytes4, "formatBytes");
    __name8(getArchitectureVersion, "getArchitectureVersion");
    __name8(cleanupStaleLearnings, "cleanupStaleLearnings");
    __name8(cleanupArchivedSessions, "cleanupArchivedSessions");
  }
});
function result(text2, isError = false) {
  return {
    content: [
      {
        type: "text",
        text: text2
      }
    ],
    isError
  };
}
__name(result, "result");
function jsonResult(data, options) {
  let finalData = data;
  if (options?.coaching) {
    const hint = generateCoachingHint(options.coaching);
    if (hint) {
      finalData = {
        ...finalData,
        _hint: hint
      };
    }
  }
  if (options?.compress !== false && options?.context) {
    const config2 = getResponseConfig(options.context.tier || "local");
    finalData = compressResponse(finalData, config2);
  }
  return result(JSON.stringify(finalData, null, 2));
}
__name(jsonResult, "jsonResult");
function errorJsonResult(data) {
  return result(JSON.stringify(data, null, 2), true);
}
__name(errorJsonResult, "errorJsonResult");
function listSnapshotsReadonly(workspaceRoot) {
  const snapshotDir = join(workspaceRoot, ".vreko", "snapshots");
  if (!existsSync(snapshotDir)) {
    return [];
  }
  try {
    const files = __require3("fs").readdirSync(snapshotDir).filter((f) => f.endsWith(".json"));
    return files.map((file) => {
      try {
        const content = readFileSync(join(snapshotDir, file), "utf8");
        const parsed = JSON.parse(content);
        return {
          id: parsed.id,
          createdAt: parsed.createdAt,
          files: parsed.files || []
        };
      } catch {
        return null;
      }
    }).filter((m) => m !== null).sort((a, b) => b.createdAt - a.createdAt);
  } catch {
    return [];
  }
}
__name(listSnapshotsReadonly, "listSnapshotsReadonly");
function isLocalWorkspace(workspacePath) {
  if (workspacePath.startsWith("/") || workspacePath.startsWith("~") || /^[A-Za-z]:/.test(workspacePath)) {
    return true;
  }
  return false;
}
__name(isLocalWorkspace, "isLocalWorkspace");
function generateCoachingHint(coaching) {
  if (coaching.lastAction) {
    const hints = {
      snapshot_created: "Snapshot created successfully. Continue with your task.",
      snapshot_skipped: "No changes detected. Previous snapshot still valid.",
      snapshot_restored: "Files restored. Review changes before proceeding.",
      validation_passed: "All checks passed. Ready to commit.",
      validation_failed: "Issues found. Review and fix before committing."
    };
    return hints[coaching.lastAction] || null;
  }
  return null;
}
__name(generateCoachingHint, "generateCoachingHint");
var init_utils = __esm2({
  "src/facades/handlers/utils.ts"() {
    init_response_utils();
    __name8(result, "result");
    __name8(jsonResult, "jsonResult");
    __name8(errorJsonResult, "errorJsonResult");
    __name8(listSnapshotsReadonly, "listSnapshotsReadonly");
    __name8(isLocalWorkspace, "isLocalWorkspace");
    __name8(generateCoachingHint, "generateCoachingHint");
  }
});
var handleAnalyze;
var init_analyze_handler = __esm2({
  "src/facades/handlers/analyze-handler.ts"() {
    init_errors();
    init_utils();
    handleAnalyze = /* @__PURE__ */ __name8(async (args, _context) => {
      const { type } = args;
      if (type === "risk") {
        const { changes, filePath } = args;
        const riskFactors = [];
        let severity = "low";
        if (changes && Array.isArray(changes)) {
          const hasRemovals = changes.some((c) => c.removed);
          const hasAdditions = changes.some((c) => c.added);
          if (hasRemovals && hasAdditions) {
            riskFactors.push("Mixed additions and removals");
            severity = "medium";
          }
          if (changes.length > 50) {
            riskFactors.push("Large change set (50+ lines)");
            severity = "high";
          }
        }
        if (filePath) {
          if (filePath.includes("auth") || filePath.includes("security")) {
            riskFactors.push("Security-sensitive file");
            severity = "high";
          }
          if (filePath.includes("config") || filePath.includes(".env")) {
            riskFactors.push("Configuration file");
            severity = "medium";
          }
        }
        return jsonResult({
          type: "risk_assessment",
          severity,
          riskFactors,
          recommendation: severity === "high" ? "Create snapshot before proceeding" : "Proceed with caution",
          next_actions: [
            severity === "high" ? {
              tool: "snapshot_create",
              priority: 1,
              reason: "High risk detected"
            } : null,
            {
              tool: "validate",
              priority: 2,
              reason: "Validate changes before commit"
            }
          ].filter(Boolean)
        });
      }
      if (type === "package") {
        const { packageName, targetVersion } = args;
        if (!packageName) {
          return errorJsonResult(CommonErrors.missingParam("packageName"));
        }
        return jsonResult({
          type: "package_validation",
          packageName,
          targetVersion: targetVersion || "latest",
          recommendation: "proceed",
          warnings: [],
          message: "Package validation - check npm registry for peer dependencies",
          next_actions: [
            {
              tool: "snapshot_create",
              priority: 1,
              reason: "Before package changes"
            }
          ]
        });
      }
      return errorJsonResult(CommonErrors.missingParam("type (risk | package)"));
    }, "handleAnalyze");
  }
});
var handleCleanup;
var init_cleanup_handler = __esm2({
  "src/facades/handlers/cleanup-handler.ts"() {
    init_local_service_adapter();
    init_response_utils();
    init_utils();
    handleCleanup = /* @__PURE__ */ __name8(async (args, context) => {
      const { target = "all", dryRun = true, maxAge, keepCount } = args;
      const learningsPath = join(context.workspaceRoot, ".vreko", "learnings", "learnings.jsonl");
      const sessionArchiveDir = join(context.workspaceRoot, ".vreko", "session", "archive");
      const results = {
        snapshots: {
          found: 0,
          stale: 0,
          deleted: 0
        },
        learnings: {
          found: 0,
          stale: 0,
          deleted: 0
        },
        sessions: {
          found: 0,
          stale: 0,
          deleted: 0
        },
        blobs: {
          found: 0,
          stale: 0,
          deleted: 0,
          orphaned: 0
        },
        totalBytesReclaimed: 0
      };
      if (target === "snapshots" || target === "all") {
        try {
          const allSnapshots = await listSnapshotsViaDaemon(context.workspaceRoot);
          const maxAgeDays = maxAge || 30;
          const keepCountN = keepCount || 10;
          const maxAgeMs = maxAgeDays * 24 * 60 * 60 * 1e3;
          const cutoffTime = Date.now() - maxAgeMs;
          const sorted = [
            ...allSnapshots
          ].sort((a, b) => new Date(b.createdAt).getTime() - new Date(a.createdAt).getTime());
          const stale = sorted.slice(keepCountN).filter((s) => new Date(s.createdAt).getTime() < cutoffTime);
          let deletedCount = 0;
          if (!dryRun) {
            const daemon = createServiceClient2(context.workspaceRoot);
            for (const vreko of stale) {
              try {
                await daemon.request("snapshot/delete", {
                  workspace: context.workspaceRoot,
                  snapshotId: vreko.id
                });
                deletedCount++;
              } catch {
              }
            }
          }
          results.snapshots = {
            found: allSnapshots.length,
            stale: stale.length,
            deleted: deletedCount
          };
        } catch (_err) {
        }
      }
      if (target === "learnings" || target === "all") {
        const archVersion = getArchitectureVersion(context.workspaceRoot);
        results.learnings = cleanupStaleLearnings(learningsPath, {
          maxAgeDays: maxAge || 90,
          archVersion,
          dryRun
        });
      }
      if (target === "sessions" || target === "all") {
        results.sessions = cleanupArchivedSessions(sessionArchiveDir, {
          maxAgeDays: maxAge || 30,
          dryRun
        });
      }
      if (target === "blobs" || target === "all") {
        results.blobs = {
          found: 0,
          stale: 0,
          deleted: 0,
          orphaned: 0,
          bytesReclaimed: 0
        };
      }
      const totalStale = results.snapshots.stale + results.learnings.stale + results.sessions.stale + results.blobs.orphaned;
      const totalDeleted = results.snapshots.deleted + results.learnings.deleted + results.sessions.deleted + results.blobs.deleted;
      return jsonResult({
        status: "success",
        dryRun,
        target,
        results,
        summary: {
          totalStale,
          totalDeleted,
          bytesReclaimed: results.totalBytesReclaimed,
          bytesReclaimedFormatted: formatBytes4(results.totalBytesReclaimed)
        },
        message: dryRun ? `Would remove ${totalStale} stale item(s) and reclaim ${formatBytes4(results.totalBytesReclaimed)}` : `Removed ${totalDeleted} item(s) and reclaimed ${formatBytes4(results.totalBytesReclaimed)}`,
        _hint: dryRun ? "This was a dry run. Call with dryRun: false to execute cleanup." : "Cleanup complete. Run prepare_workspace to verify workspace health.",
        next_actions: dryRun ? totalStale > 0 ? [
          {
            tool: "cleanup",
            priority: 1,
            reason: "Execute cleanup",
            args: {
              target,
              dryRun: false
            }
          }
        ] : [] : [
          {
            tool: "prepare_workspace",
            priority: 2,
            reason: "Verify workspace health"
          }
        ]
      }, {
        context
      });
    }, "handleCleanup");
  }
});
function validateFilePath(filePath, workspaceRoot) {
  if (!filePath || filePath.trim() === "") {
    return {
      valid: false,
      error: "File path cannot be empty"
    };
  }
  if (filePath.includes("\0")) {
    return {
      valid: false,
      error: "Invalid characters in file path"
    };
  }
  const normalizedPath = normalize$1(filePath);
  if (normalizedPath.includes("..")) {
    return {
      valid: false,
      error: "Path traversal not allowed"
    };
  }
  const absolutePath = isAbsolute(normalizedPath) ? normalizedPath : resolve(workspaceRoot, normalizedPath);
  const relativePath = relative(workspaceRoot, absolutePath);
  if (relativePath.startsWith("..") || isAbsolute(relativePath)) {
    return {
      valid: false,
      error: "Path must be within workspace"
    };
  }
  return {
    valid: true,
    sanitizedPath: absolutePath
  };
}
__name(validateFilePath, "validateFilePath");
function validateFilePaths(filePaths, workspaceRoot) {
  const sanitizedPaths = [];
  for (const filePath of filePaths) {
    const result7 = validateFilePath(filePath, workspaceRoot);
    if (!result7.valid) {
      return {
        valid: false,
        error: result7.error,
        invalidPath: filePath
      };
    }
    sanitizedPaths.push(result7.sanitizedPath);
  }
  return {
    valid: true,
    sanitizedPaths
  };
}
__name(validateFilePaths, "validateFilePaths");
function atomicWriteFileSync2(filePath, content, options = {}) {
  const { encoding = "utf8", maxSize = MAX_CONTEXT_FILE_SIZE } = options;
  return atomicWriteFileSync(filePath, content, {
    encoding,
    maxSize
  });
}
__name(atomicWriteFileSync2, "atomicWriteFileSync");
function validateInput(schema, input) {
  const result7 = schema.safeParse(input);
  if (result7.success) {
    return {
      valid: true,
      data: result7.data
    };
  }
  const errorMessages = (result7.error.issues || []).map((issue) => `${issue.path.join(".")}: ${issue.message}`).join("; ");
  return {
    valid: false,
    error: errorMessages,
    issues: result7.error.issues
  };
}
__name(validateInput, "validateInput");
function getToolSchema(toolName) {
  return TOOL_SCHEMAS[toolName];
}
__name(getToolSchema, "getToolSchema");
var MAX_CONTEXT_FILE_SIZE;
var snapViolationSchema;
var checkSchema;
var snapFixSchema;
var snapHelpSchema;
var pulseSchema;
var adviseSchema;
var TOOL_SCHEMAS;
var init_validation = __esm2({
  "src/validation.ts"() {
    __name8(validateFilePath, "validateFilePath");
    __name8(validateFilePaths, "validateFilePaths");
    MAX_CONTEXT_FILE_SIZE = 10 * 1024 * 1024;
    __name8(atomicWriteFileSync2, "atomicWriteFileSync");
    z.object({
      // Mode parameters - at least one required
      mode: z.enum([
        "start",
        "check",
        "context"
      ]).optional().describe("Mode (full-word, recommended): start=begin task, check=validate code, context=get status"),
      m: z.enum([
        "s",
        "c",
        "x"
      ]).optional().describe("Legacy mode: s=start, c=check, x=context (use 'mode' instead)"),
      // Task parameters
      task: z.string().optional().describe("Task description (mode: start)"),
      t: z.string().optional().describe("Legacy: use 'task' instead"),
      // File parameters
      files: z.array(z.string()).optional().describe("Files to work on (mode: start, check)"),
      f: z.array(z.string()).optional().describe("Legacy: use 'files' instead"),
      // Keyword parameters
      keywords: z.array(z.string()).optional().describe("Keywords for learning retrieval (mode: start, context)"),
      k: z.array(z.string()).optional().describe("Legacy: use 'keywords' instead"),
      // Intent parameters
      intent: z.enum([
        "implement",
        "debug",
        "refactor",
        "review",
        "explore"
      ]).optional().describe("Task intent for context loading optimization"),
      i: z.enum([
        "implement",
        "debug",
        "refactor",
        "review",
        "explore"
      ]).optional().describe("Legacy: use 'intent' instead"),
      // Other parameters
      thorough: z.boolean().optional().describe("Enable thorough 7-layer validation (mode: check)"),
      compact: z.boolean().optional().describe("Use compact positional wire format instead of labeled (default: false)"),
      goal: z.object({
        metric: z.enum([
          "bundle",
          "performance",
          "coverage"
        ]).describe("ONLY: 'bundle'|'performance'|'coverage'. NOTE: 'momentum' is NOT valid here."),
        target: z.number().describe("Target value (e.g., 100 for KB, 200 for ms, 80 for %)"),
        unit: z.string().describe("Unit: 'KB' for bundle, 'ms' for performance, '%' for coverage")
      }).optional().describe("Optional task goal. Only for tracking bundle size, performance, or coverage. Example: { metric: 'coverage', target: 80, unit: '%' }")
    }).refine((data) => data.mode !== void 0 || data.m !== void 0, {
      message: "Either 'mode' (start|check|context) or 'm' (s|c|x) must be provided",
      path: [
        "mode"
      ]
    });
    z.object({
      ok: z.union([
        z.literal(0),
        z.literal(1)
      ]).optional().describe("Success: 1=ok, 0=failed"),
      // Learnings parameters
      learnings: z.array(z.string()).optional().describe("Quick learnings as strings"),
      l: z.array(z.string()).optional().describe("Legacy: use 'learnings' instead"),
      // Other parameters
      notes: z.string().optional().describe("Completion notes"),
      /** Free-text outcome: what was accomplished, what remains, decisions made. ok:0|1 is the primary signal. */
      outcome: z.string().min(1).max(2e3).optional().describe("Free-text task outcome (what was accomplished, what remains, decisions made)"),
      // Efficiency tracking (agent-reported metrics)
      efficiency: z.object({
        saved: z.string().optional().describe("Tokens saved (e.g., '~12K')"),
        prevented: z.string().optional().describe("Mistakes prevented"),
        helped: z.string().optional().describe("What context helped most")
      }).optional().describe("Agent-reported efficiency metrics"),
      // Internal survey (LLM self-assessment)
      survey: z.object({
        patterns_used: z.number().optional().describe("Patterns applied from context"),
        pitfalls_avoided: z.number().optional().describe("Pitfalls avoided due to warnings"),
        helpfulness: z.number().min(1).max(5).optional().describe("Helpfulness rating 1-5"),
        unhelpful_count: z.number().optional().describe("Count of unhelpful suggestions")
      }).optional().describe("LLM self-assessment (internal)"),
      compact: z.boolean().optional().describe("Use compact wire format")
    });
    z.object({
      // Full-word parameters (recommended)
      trigger: z.string().optional().describe("What situation triggers this learning"),
      action: z.string().optional().describe("What to do when triggered"),
      source: z.string().optional().describe("Where this learning originated (optional)"),
      // Legacy parameters
      t: z.string().optional().describe("Legacy: use 'trigger' instead"),
      a: z.string().optional().describe("Legacy: use 'action' instead"),
      s: z.string().optional().describe("Legacy: use 'source' instead"),
      // Type (supports both full-word and abbreviated)
      type: z.union([
        LearningType,
        z.enum([
          "pat",
          "pit",
          "eff",
          "disc",
          "wf",
          "con",
          "prf"
        ])
      ]).optional().describe("Learning type (default: pattern)")
    }).refine((data) => (data.trigger !== void 0 || data.t !== void 0) && (data.action !== void 0 || data.a !== void 0), {
      message: "Either 'trigger' or 't' AND 'action' or 'a' must be provided",
      path: [
        "trigger"
      ]
    });
    snapViolationSchema = z.object({
      type: z.string().min(1).describe("Violation type (e.g., 'silent_catch', 'missing_auth_check')"),
      file: z.string().min(1).describe("File where violation occurred"),
      // Full-word parameters (recommended)
      description: z.string().optional().describe("What went wrong - description of the violation"),
      reason: z.string().optional().describe("Why it happened - root cause"),
      prevention: z.string().optional().describe("How to prevent this in future"),
      // Legacy parameters
      what: z.string().optional().describe("Legacy: use 'description' instead"),
      why: z.string().optional().describe("Legacy: use 'reason' instead"),
      prevent: z.string().optional().describe("Legacy: use 'prevention' instead")
    }).refine((data) => (data.description !== void 0 || data.what !== void 0) && (data.prevention !== void 0 || data.prevent !== void 0), {
      message: "Either 'description' or 'what' AND 'prevention' or 'prevent' must be provided",
      path: [
        "description"
      ]
    });
    checkSchema = z.object({
      // Mode parameters
      mode: z.enum([
        "quick",
        "full",
        "patterns",
        "build",
        "impact",
        "circular",
        "docs",
        "learnings",
        "architecture",
        "trace"
      ]).optional().describe("Mode (full-word, recommended): quick=fast validation, full=comprehensive, patterns=rule check, trace=dependency analysis"),
      m: z.enum([
        "q",
        "f",
        "p",
        "b",
        "i",
        "c",
        "d",
        "l",
        "a",
        "t"
      ]).optional().describe("Legacy mode: q=quick, f=full, p=patterns, b=build, i=impact, c=circular, d=docs, l=learnings, a=architecture, t=trace"),
      // File parameters
      files: z.union([
        z.string(),
        z.array(z.string())
      ]).optional().describe("File(s) to check"),
      f: z.union([
        z.string(),
        z.array(z.string())
      ]).optional().describe("Legacy: use 'files' instead"),
      code: z.string().optional().describe("Code to validate (for patterns/validate mode)"),
      tests: z.boolean().optional().describe("Run tests (quick mode only)"),
      compact: z.boolean().optional().describe("Use compact wire format")
    });
    snapFixSchema = z.object({
      action: z.enum([
        "list",
        "restore",
        "diff"
      ]).optional().describe("Operation: list (default), restore, or diff"),
      id: z.string().optional().describe("Snapshot ID (for restore/diff)"),
      // Diff parameters
      diffWith: z.string().optional().describe("Second snapshot ID (for diff operation)"),
      diff: z.string().optional().describe("Legacy: use 'diffWith' instead"),
      // Dry run parameters
      dryRun: z.boolean().optional().describe("Preview restore without applying changes"),
      dry: z.boolean().optional().describe("Legacy: use 'dryRun' instead"),
      // Other parameters
      files: z.array(z.string()).optional().describe("Specific files to restore (optional filter)"),
      compact: z.boolean().optional().describe("Use compact wire format")
    });
    snapHelpSchema = z.object({
      topic: z.enum([
        "tools",
        "status",
        "wire",
        "modes",
        "thresholds",
        "all"
      ]).optional().describe("Help topic: tools, status, wire, modes, thresholds, or all (default)"),
      q: z.enum([
        "tools",
        "status",
        "wire"
      ]).optional().describe("Legacy: use 'topic' instead")
    });
    __name8(validateInput, "validateInput");
    pulseSchema = z.object({
      record_change: z.string().optional().describe("File path to record as changed")
    }).passthrough();
    adviseSchema = z.object({
      files: z.array(z.string()).optional().describe("Files to analyze for recommendations")
    }).passthrough();
    TOOL_SCHEMAS = {
      // V2 tools removed - they handle validation internally:
      // - vreko (uses validateSnapStartParams)
      // - vreko_learn (uses SnapLearnParamsSchema)
      // - vreko_end (uses SnapEndParamsSchema)
      // - vreko_pulse (uses SnapPulseParamsSchema)
      // Consolidated tools (non-V2)
      vreko_fix: snapFixSchema,
      vreko_help: snapHelpSchema,
      vreko_violation: snapViolationSchema,
      // Backward-compat aliases (deprecated names)
      snap_fix: snapFixSchema,
      snap_help: snapHelpSchema,
      snap_violation: snapViolationSchema,
      check: checkSchema,
      pulse: pulseSchema,
      advise: adviseSchema
    };
    __name8(getToolSchema, "getToolSchema");
  }
});
var handleContext;
var init_context_handler = __esm2({
  "src/facades/handlers/context-handler.ts"() {
    init_errors();
    init_validation();
    init_utils();
    handleContext = /* @__PURE__ */ __name8(async (args, context) => {
      const { op, domain, name, value } = args;
      if (!op) {
        return errorJsonResult(CommonErrors.missingParam("op"));
      }
      const ctxDir = join(context.workspaceRoot, ".vreko", "ctx");
      const ctxPath = join(ctxDir, "context.json");
      const ctxFilePath = join(context.workspaceRoot, ".ctx");
      switch (op) {
        case "status": {
          const exists = existsSync(ctxPath);
          const ctxFileExists = existsSync(ctxFilePath);
          let contextData = null;
          if (exists) {
            try {
              contextData = JSON.parse(readFileSync(ctxPath, "utf8"));
            } catch {
            }
          }
          return jsonResult({
            op: "status",
            initialized: exists,
            path: ctxPath,
            ctxFileExists,
            projectPhase: contextData?.project_phase || "unknown",
            priority: contextData?.priority || "unknown",
            message: exists ? "Context initialized" : "Context not initialized - run op: init"
          });
        }
        case "init": {
          if (!existsSync(ctxDir)) {
            await import('fs').then((fs22) => fs22.mkdirSync(ctxDir, {
              recursive: true
            }));
          }
          const defaultContext = {
            project_phase: "development",
            priority: "stability",
            constraints: {
              extension: {
                bundle: {
                  max: 150,
                  unit: "KB",
                  description: "Max extension bundle size"
                }
              },
              web: {
                fcp: {
                  max: 1500,
                  unit: "ms",
                  description: "First Contentful Paint"
                },
                lcp: {
                  max: 2500,
                  unit: "ms",
                  description: "Largest Contentful Paint"
                }
              }
            },
            blockers: [],
            lastScanned: /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString(),
            staleAfterDays: 7,
            created_at: /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString(),
            updated_at: /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString()
          };
          const writeResult = atomicWriteFileSync2(ctxPath, JSON.stringify(defaultContext, null, 2));
          if (!writeResult.success) {
            return jsonResult({
              op: "init",
              status: "error",
              error: "E404_CONTEXT_OPERATION_FAILED",
              message: writeResult.error
            });
          }
          return jsonResult({
            op: "init",
            status: "success",
            path: ctxPath,
            message: "Context initialized with default configuration",
            next_actions: [
              {
                tool: "context",
                priority: 1,
                reason: "Check status",
                args: {
                  op: "status"
                }
              },
              {
                tool: "context",
                priority: 2,
                reason: "Build .ctx file",
                args: {
                  op: "build"
                }
              }
            ]
          });
        }
        case "build": {
          if (!existsSync(ctxPath)) {
            return jsonResult({
              op: "build",
              status: "error",
              error: "E204_CONTEXT_NOT_INITIALIZED",
              message: "Context not initialized - run op: init first"
            });
          }
          try {
            const contextData = JSON.parse(readFileSync(ctxPath, "utf8"));
            const ctxContent = JSON.stringify(contextData);
            const writeResult = atomicWriteFileSync2(ctxFilePath, ctxContent);
            if (!writeResult.success) {
              return jsonResult({
                op: "build",
                status: "error",
                error: "E404_CONTEXT_OPERATION_FAILED",
                message: writeResult.error
              });
            }
            return jsonResult({
              op: "build",
              status: "success",
              path: ctxFilePath,
              size: ctxContent.length,
              message: ".ctx file built successfully"
            });
          } catch (error) {
            const msg = error instanceof Error ? error.message : String(error);
            return jsonResult({
              op: "build",
              status: "error",
              error: "E404_CONTEXT_OPERATION_FAILED",
              message: `Failed to build .ctx: ${msg}`
            });
          }
        }
        case "validate": {
          if (!existsSync(ctxPath)) {
            return jsonResult({
              op: "validate",
              valid: false,
              reason: "Context not initialized"
            });
          }
          const ctxFileExists = existsSync(ctxFilePath);
          if (!ctxFileExists) {
            return jsonResult({
              op: "validate",
              valid: false,
              reason: ".ctx file missing - run op: build",
              next_actions: [
                {
                  tool: "context",
                  priority: 1,
                  reason: "Build .ctx",
                  args: {
                    op: "build"
                  }
                }
              ]
            });
          }
          const contextJson = readFileSync(ctxPath, "utf8");
          const ctxFile = readFileSync(ctxFilePath, "utf8");
          const isSync = JSON.stringify(JSON.parse(contextJson)) === ctxFile;
          return jsonResult({
            op: "validate",
            valid: isSync,
            reason: isSync ? "Context is in sync" : "Context is stale - run op: build",
            next_actions: isSync ? [] : [
              {
                tool: "context",
                priority: 1,
                reason: "Rebuild",
                args: {
                  op: "build"
                }
              }
            ]
          });
        }
        case "constraint": {
          if (!domain || !name) {
            return errorJsonResult(CommonErrors.missingParam("domain, name"));
          }
          if (!existsSync(ctxPath)) {
            return jsonResult({
              op: "constraint",
              status: "error",
              error: "E204_CONTEXT_NOT_INITIALIZED",
              message: "Context not initialized"
            });
          }
          try {
            const contextData = JSON.parse(readFileSync(ctxPath, "utf8"));
            const constraint = contextData?.constraints?.[domain]?.[name];
            if (!constraint) {
              return jsonResult({
                op: "constraint",
                domain,
                name,
                found: false,
                message: `Constraint ${domain}.${name} not found`
              });
            }
            return jsonResult({
              op: "constraint",
              domain,
              name,
              found: true,
              threshold: constraint.max,
              unit: constraint.unit,
              description: constraint.description
            });
          } catch (error) {
            const msg = error instanceof Error ? error.message : String(error);
            return jsonResult({
              op: "constraint",
              status: "error",
              error: "E404_CONTEXT_OPERATION_FAILED",
              message: `Failed to read constraint: ${msg}`
            });
          }
        }
        case "check": {
          if (!domain || !name || value === void 0) {
            return errorJsonResult(CommonErrors.missingParam("domain, name, value"));
          }
          if (!existsSync(ctxPath)) {
            return jsonResult({
              op: "check",
              status: "error",
              error: "E204_CONTEXT_NOT_INITIALIZED",
              message: "Context not initialized"
            });
          }
          try {
            const contextData = JSON.parse(readFileSync(ctxPath, "utf8"));
            const constraint = contextData?.constraints?.[domain]?.[name];
            if (!constraint) {
              return jsonResult({
                op: "check",
                domain,
                name,
                value,
                passes: true,
                message: `No constraint defined for ${domain}.${name}`
              });
            }
            const passes = value <= constraint.max;
            return jsonResult({
              op: "check",
              domain,
              name,
              value,
              threshold: constraint.max,
              unit: constraint.unit,
              passes,
              message: passes ? `Value ${value}${constraint.unit} is within constraint (max: ${constraint.max}${constraint.unit})` : `Value ${value}${constraint.unit} exceeds constraint (max: ${constraint.max}${constraint.unit})`
            });
          } catch (error) {
            const msg = error instanceof Error ? error.message : String(error);
            return jsonResult({
              op: "check",
              status: "error",
              error: "E404_CONTEXT_OPERATION_FAILED",
              message: `Failed to check constraint: ${msg}`
            });
          }
        }
        case "blockers": {
          if (!existsSync(ctxPath)) {
            return jsonResult({
              op: "blockers",
              blockers: [],
              message: "Context not initialized"
            });
          }
          try {
            const contextData = JSON.parse(readFileSync(ctxPath, "utf8"));
            const blockers = contextData?.blockers || [];
            const lastScanned = contextData?.lastScanned;
            const staleAfterDays = contextData?.staleAfterDays || 7;
            let isStale = false;
            let daysSinceScanned = null;
            let stalenessWarning = null;
            if (lastScanned) {
              const scanTime = new Date(lastScanned).getTime();
              daysSinceScanned = Math.floor((Date.now() - scanTime) / (1e3 * 60 * 60 * 24));
              isStale = daysSinceScanned > staleAfterDays;
              if (isStale) {
                stalenessWarning = `Data is ${daysSinceScanned} days stale - consider refreshing`;
              }
            } else {
              stalenessWarning = "No scan timestamp found - data may be stale";
            }
            const result7 = {
              op: "blockers",
              blockers,
              count: blockers.length,
              message: blockers.length > 0 ? `${blockers.length} blocker(s) found` : "No blockers",
              lastScanned,
              daysSinceScanned,
              isStale
            };
            if (stalenessWarning) {
              result7.warning = stalenessWarning;
              result7.next_actions = [
                {
                  tool: "context",
                  priority: 1,
                  reason: "Refresh stale data",
                  args: {
                    op: "scan"
                  }
                }
              ];
            }
            return jsonResult(result7);
          } catch (error) {
            const msg = error instanceof Error ? error.message : String(error);
            return jsonResult({
              op: "blockers",
              status: "error",
              error: "E404_CONTEXT_OPERATION_FAILED",
              message: `Failed to read blockers: ${msg}`
            });
          }
        }
        case "reset": {
          if (!existsSync(ctxPath)) {
            return jsonResult({
              op: "reset",
              status: "success",
              message: "No context to reset"
            });
          }
          try {
            await import('fs').then((fs22) => fs22.unlinkSync(ctxPath));
            if (existsSync(ctxFilePath)) {
              await import('fs').then((fs22) => fs22.unlinkSync(ctxFilePath));
            }
            return jsonResult({
              op: "reset",
              status: "success",
              message: "Context reset successfully - run op: init to recreate",
              next_actions: [
                {
                  tool: "context",
                  priority: 1,
                  reason: "Recreate context",
                  args: {
                    op: "init"
                  }
                }
              ]
            });
          } catch (error) {
            const msg = error instanceof Error ? error.message : String(error);
            return jsonResult({
              op: "reset",
              status: "error",
              error: "E404_CONTEXT_OPERATION_FAILED",
              message: `Failed to reset context: ${msg}`
            });
          }
        }
        case "scan": {
          if (!existsSync(ctxPath)) {
            return jsonResult({
              op: "scan",
              status: "error",
              error: "E204_CONTEXT_NOT_INITIALIZED",
              message: "Context not initialized - run op: init first",
              next_actions: [
                {
                  tool: "context",
                  priority: 1,
                  reason: "Initialize context",
                  args: {
                    op: "init"
                  }
                }
              ]
            });
          }
          try {
            const contextData = JSON.parse(readFileSync(ctxPath, "utf8"));
            const newBlockers = [];
            try {
              const { execSync: execSync6 } = await import('child_process');
              const tscOutput = execSync6("npx tsc --noEmit --pretty false 2>&1 || true", {
                cwd: context.workspaceRoot,
                encoding: "utf8",
                timeout: 3e4
              });
              const errorMatches = tscOutput.match(/error TS\d+:/g);
              const tsErrorCount = errorMatches ? errorMatches.length : 0;
              if (tsErrorCount > 0) {
                newBlockers.push({
                  key: "_ts",
                  label: "typescript-errors",
                  current: tsErrorCount,
                  target: 0
                });
              }
            } catch (_tsError) {
            }
            const extensionDistPath = join(context.workspaceRoot, "apps", "vscode", "dist");
            if (existsSync(extensionDistPath)) {
              try {
                const { readdirSync: readdirSync32, statSync: statSync62 } = await import('fs');
                let totalSize = 0;
                const calculateDirSize = /* @__PURE__ */ __name8((dir) => {
                  const files = readdirSync32(dir);
                  for (const file of files) {
                    const filePath = join(dir, file);
                    const stats = statSync62(filePath);
                    if (stats.isDirectory()) {
                      calculateDirSize(filePath);
                    } else {
                      totalSize += stats.size;
                    }
                  }
                }, "calculateDirSize");
                calculateDirSize(extensionDistPath);
                const sizeInMB = (totalSize / (1024 * 1024)).toFixed(2);
                const maxSizeMB = contextData.constraints?.extension?.bundle?.max || 150 / 1024;
                if (Number.parseFloat(sizeInMB) > maxSizeMB) {
                  newBlockers.push({
                    key: "_eb",
                    label: "bundle-size",
                    current: `${sizeInMB}MB`,
                    target: `${maxSizeMB}MB`
                  });
                }
              } catch (_bundleError) {
              }
            }
            contextData.blockers = newBlockers;
            contextData.lastScanned = /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString();
            contextData.updated_at = /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString();
            const writeResult = atomicWriteFileSync2(ctxPath, JSON.stringify(contextData, null, 2));
            if (!writeResult.success) {
              return jsonResult({
                op: "scan",
                status: "error",
                error: "E404_CONTEXT_OPERATION_FAILED",
                message: writeResult.error
              });
            }
            return jsonResult({
              op: "scan",
              status: "success",
              scannedAt: contextData.lastScanned,
              blockers: newBlockers,
              blockersFound: newBlockers.length,
              message: newBlockers.length > 0 ? `Found ${newBlockers.length} blocker(s) - ${newBlockers.map((b) => b.label).join(", ")}` : "No blockers detected",
              next_actions: newBlockers.length > 0 ? [
                {
                  tool: "context",
                  priority: 2,
                  reason: "Review blockers",
                  args: {
                    op: "blockers"
                  }
                }
              ] : []
            });
          } catch (error) {
            const msg = error instanceof Error ? error.message : String(error);
            return jsonResult({
              op: "scan",
              status: "error",
              error: "E404_CONTEXT_OPERATION_FAILED",
              message: `Scan failed: ${msg}`
            });
          }
        }
        default:
          return jsonResult({
            op,
            status: "error",
            error: "E103_INVALID_PARAM_VALUE",
            message: `Unknown context operation: ${op}`,
            validOps: [
              "init",
              "build",
              "validate",
              "status",
              "constraint",
              "check",
              "blockers",
              "reset",
              "scan"
            ]
          });
      }
    }, "handleContext");
  }
});
function getIndexedIds(workspaceRoot) {
  let ids = indexedIds.get(workspaceRoot);
  if (!ids) {
    ids = /* @__PURE__ */ new Set();
    indexedIds.set(workspaceRoot, ids);
  }
  return ids;
}
__name(getIndexedIds, "getIndexedIds");
async function ensureEmbeddingsReady(workspaceRoot) {
  if (embeddingsReady) {
    return true;
  }
  if (!workspaceRoot) {
    return false;
  }
  try {
    const result7 = await ensureEmbeddingsReadyViaDaemon();
    if (result7.success && result7.ready) {
      embeddingsReady = true;
      return true;
    }
    return false;
  } catch (_error) {
    return false;
  }
}
__name(ensureEmbeddingsReady, "ensureEmbeddingsReady");
function isEmbeddingsAvailable() {
  return embeddingsReady;
}
__name(isEmbeddingsAvailable, "isEmbeddingsAvailable");
async function indexLearning(workspaceRoot, learning, generateEmbedding = true) {
  try {
    const indexed = getIndexedIds(workspaceRoot);
    if (indexed.has(learning.id)) {
      return {
        success: true
      };
    }
    const triggerText = Array.isArray(learning.trigger) ? learning.trigger.join(", ") : learning.trigger;
    const result7 = await ingestKnowledgeViaDaemon(workspaceRoot, {
      text: `${triggerText}
${learning.action}`,
      source: learning.id,
      metadata: {
        type: learning.type,
        source: learning.source,
        domain: learning.domain,
        priority: learning.priority,
        timestamp: learning.timestamp,
        tier: learning.tier
      }
    });
    if (result7.success) {
      indexed.add(learning.id);
      if (generateEmbedding && result7.id) {
        try {
          const textForEmbedding = `${triggerText} ${learning.action}`;
          const embeddingResult = await generateEmbeddingViaDaemon(textForEmbedding);
          if (!embeddingResult.success) {
          }
        } catch (_error) {
        }
      }
      return {
        success: true
      };
    }
    return {
      success: false,
      error: result7.error
    };
  } catch (error) {
    return {
      success: false,
      error: errorMessage(error)
    };
  }
}
__name(indexLearning, "indexLearning");
async function indexAllLearnings(workspaceRoot, options = {}) {
  const result7 = {
    success: true,
    indexed: 0,
    skipped: 0,
    errors: [],
    embeddingsGenerated: 0
  };
  const learningsDir = join(workspaceRoot, ".vreko", "learnings");
  if (!existsSync(learningsDir)) {
    return result7;
  }
  const defaultFiles = [
    "hot.jsonl",
    "learnings.jsonl",
    "domain-vscode.jsonl",
    "domain-web.jsonl",
    "domain-mcp-cli.jsonl",
    "domain-testing.jsonl",
    "anti-patterns.jsonl",
    "architecture-patterns.jsonl",
    "user-learnings.jsonl"
  ];
  const filesToIndex = options.files || defaultFiles;
  const generateEmbeddings = !options.skipEmbeddings && await ensureEmbeddingsReady();
  if (options.force) {
    indexedIds.delete(workspaceRoot);
  }
  for (const filename of filesToIndex) {
    const filePath = join(learningsDir, filename);
    if (!existsSync(filePath)) {
      continue;
    }
    try {
      const content = readFileSync(filePath, "utf8");
      const lines = content.split("\n").filter((line) => line.trim());
      for (const line of lines) {
        try {
          const learning = JSON.parse(line);
          if (!learning.id) {
            learning.id = `seed_${filename.replace(".jsonl", "")}_${result7.indexed}`;
          }
          const indexResult = await indexLearning(workspaceRoot, learning, generateEmbeddings);
          if (indexResult.success) {
            result7.indexed++;
            if (generateEmbeddings) {
              result7.embeddingsGenerated++;
            }
          } else if (indexResult.error) {
            result7.errors.push(`${learning.id}: ${indexResult.error}`);
          }
        } catch (parseError) {
          result7.errors.push(`Parse error in ${filename}: ${parseError}`);
        }
      }
    } catch (readError) {
      result7.errors.push(`Read error for ${filename}: ${readError}`);
    }
  }
  result7.success = result7.errors.length === 0;
  return result7;
}
__name(indexAllLearnings, "indexAllLearnings");
function invalidateCache(workspaceRoot) {
  indexedIds.delete(workspaceRoot);
}
__name(invalidateCache, "invalidateCache");
var indexedIds;
var embeddingsReady;
var init_knowledge_ingestion_service = __esm2({
  "src/services/knowledge-ingestion-service.ts"() {
    init_local_service_adapter();
    indexedIds = /* @__PURE__ */ new Map();
    embeddingsReady = false;
    __name8(getIndexedIds, "getIndexedIds");
    __name8(ensureEmbeddingsReady, "ensureEmbeddingsReady");
    __name8(isEmbeddingsAvailable, "isEmbeddingsAvailable");
    __name8(indexLearning, "indexLearning");
    __name8(indexAllLearnings, "indexAllLearnings");
    __name8(invalidateCache, "invalidateCache");
  }
});
function detectDomainFilesFromKeywords(keywords) {
  const detected = /* @__PURE__ */ new Set();
  for (const keyword of keywords) {
    const lowerKeyword = keyword.toLowerCase();
    if (KEYWORD_DOMAIN_MAP[lowerKeyword]) {
      detected.add(KEYWORD_DOMAIN_MAP[lowerKeyword]);
      continue;
    }
    for (const [key, domain] of Object.entries(KEYWORD_DOMAIN_MAP)) {
      if (lowerKeyword.includes(key) || key.includes(lowerKeyword)) {
        detected.add(domain);
      }
    }
  }
  return [
    ...detected
  ];
}
__name(detectDomainFilesFromKeywords, "detectDomainFilesFromKeywords");
function detectDomainFilesFromPaths(filePaths) {
  const detected = /* @__PURE__ */ new Set();
  for (const filePath of filePaths) {
    for (const [pattern, domain] of Object.entries(FILE_PATH_DOMAIN_MAP)) {
      if (filePath.includes(pattern)) {
        detected.add(domain);
      }
    }
  }
  return [
    ...detected
  ];
}
__name(detectDomainFilesFromPaths, "detectDomainFilesFromPaths");
async function emitInvariantIfQualified(input) {
  const { patternId, workspaceRoot, confirmingInstances, counterExamples, description, verificationCommand } = input;
  if (!(confirmingInstances >= 3)) {
    return false;
  }
  if (counterExamples !== 0) {
    return false;
  }
  if (!/^[a-z0-9][a-z0-9.-]*$/i.test(patternId)) {
    return false;
  }
  const cmd = verificationCommand ?? `pnpm vitest run tests/architecture/invariants.test.ts --grep "${patternId}"`;
  if (!VERIFICATION_COMMAND_ALLOWLIST.test(cmd)) {
    process.stderr.write(`[invariant-emission] WARN: rejected invalid verification.command for ${patternId}
`);
    return false;
  }
  const { join: pathJoin } = await import('path');
  const dir = pathJoin(workspaceRoot, ".vreko", "invariants");
  const target = pathJoin(dir, `${patternId}.json`);
  if (existsSync(target)) {
    return false;
  }
  if (!existsSync(dir)) {
    mkdirSync(dir, {
      recursive: true
    });
  }
  const invariant2 = {
    invariant_id: patternId,
    version: "1.0",
    description: description ?? `Auto-promoted learning: ${patternId}`,
    category: "learned-invariant",
    severity: "error",
    phase: "Phase 10 (INVAR-04 auto-promotion)",
    mode: "enforced",
    confirmingInstances,
    counterExamples,
    overrideCount: 0,
    verification: {
      method: "command",
      command: cmd,
      threshold: 0
    },
    rationale: description ?? "Auto-promoted from observed pattern",
    _note: "Mode changes require code review  -  auto-set to advisory by check.ts on overrideCount >= 2 only"
  };
  writeFileSync(target, `${JSON.stringify(invariant2, null, 2)}
`, "utf-8");
  return true;
}
__name(emitInvariantIfQualified, "emitInvariantIfQualified");
function loadJsonlSafe(filepath) {
  if (!existsSync(filepath)) {
    return [];
  }
  try {
    const content = readFileSync(filepath, "utf8");
    const lines = content.split("\n").filter(Boolean);
    const items = [];
    for (const line of lines) {
      try {
        items.push(JSON.parse(line));
      } catch (_error) {
        if (process.env.DEBUG) {
        }
      }
    }
    return items;
  } catch (_error) {
    if (process.env.DEBUG) ;
    return [];
  }
}
__name(loadJsonlSafe, "loadJsonlSafe");
function scoreLearning(learning, keywords) {
  if (keywords.length === 0) {
    return 0;
  }
  const triggerText = Array.isArray(learning.trigger) ? learning.trigger.join(" ") : learning.trigger || "";
  const searchText = `${triggerText} ${learning.action || ""} ${learning.context || ""}`.toLowerCase();
  let matches = 0;
  for (const keyword of keywords) {
    if (searchText.includes(keyword.toLowerCase())) {
      matches++;
    }
  }
  return matches / keywords.length;
}
__name(scoreLearning, "scoreLearning");
function getPriorityBoost(learning) {
  if (learning.priority === "critical") {
    return CRITICAL_PRIORITY_BOOST;
  }
  if (learning.priority === "high") {
    return HIGH_PRIORITY_BOOST;
  }
  return 0;
}
__name(getPriorityBoost, "getPriorityBoost");
function createTieredLearningService(workspaceRoot, workspaceId) {
  return new TieredLearningService(workspaceRoot, workspaceId);
}
__name(createTieredLearningService, "createTieredLearningService");
var INTENT_LEARNING_FILES;
var KEYWORD_DOMAIN_MAP;
var FILE_PATH_DOMAIN_MAP;
var DEFAULT_DOMAIN_FILES;
var HOT_TIER_FILE;
var COLD_TIER_FILE;
var DEFAULT_MAX_LEARNINGS;
var MIN_RELEVANCE_SCORE;
var MIN_HOT_RELEVANCE_SCORE;
var USAGE_STATS_FILE;
var HOT_TIER_PROMOTION_THRESHOLD;
var PATTERN_OBSERVATION_THRESHOLD;
var VERIFICATION_COMMAND_ALLOWLIST;
var HOT_TIER_BOOST;
var CRITICAL_PRIORITY_BOOST;
var HIGH_PRIORITY_BOOST;
var TieredLearningService;
var init_tiered_learning_service = __esm2({
  "src/services/tiered-learning-service.ts"() {
    init_local_service_adapter();
    INTENT_LEARNING_FILES = {
      implement: [
        "architecture-patterns.jsonl",
        "architecture-context.jsonl",
        "domain-intelligence.jsonl"
      ],
      debug: [
        "anti-patterns.jsonl",
        "domain-testing.jsonl",
        "architecture-patterns.jsonl"
      ],
      refactor: [
        "workflow-patterns.jsonl",
        "architecture-context.jsonl",
        "anti-patterns.jsonl"
      ],
      review: [
        "anti-patterns.jsonl",
        "domain-testing.jsonl",
        "architecture-patterns.jsonl"
      ],
      explore: [
        "architecture-context.jsonl",
        "workflow-patterns.jsonl"
      ]
    };
    KEYWORD_DOMAIN_MAP = {
      // VSCode Extension domain
      vscode: "domain-vscode.jsonl",
      extension: "domain-vscode.jsonl",
      activation: "domain-vscode.jsonl",
      webview: "domain-vscode.jsonl",
      "vs code": "domain-vscode.jsonl",
      // Web/Next.js domain
      nextjs: "domain-web.jsonl",
      "next.js": "domain-web.jsonl",
      next: "domain-web.jsonl",
      react: "domain-web.jsonl",
      client: "domain-web.jsonl",
      turbopack: "domain-web.jsonl",
      "use client": "domain-web.jsonl",
      biome: "domain-web.jsonl",
      // API/Backend domain
      api: "domain-api.jsonl",
      procedure: "domain-api.jsonl",
      orpc: "domain-api.jsonl",
      service: "domain-api.jsonl",
      backend: "domain-api.jsonl",
      drizzle: "domain-api.jsonl",
      // MCP/CLI domain
      mcp: "domain-mcp-cli.jsonl",
      cli: "domain-mcp-cli.jsonl",
      commander: "domain-mcp-cli.jsonl",
      "model context protocol": "domain-mcp-cli.jsonl",
      stdio: "domain-mcp-cli.jsonl",
      // Testing domain
      vitest: "domain-testing.jsonl",
      test: "domain-testing.jsonl",
      testing: "domain-testing.jsonl",
      coverage: "domain-testing.jsonl",
      spec: "domain-testing.jsonl",
      mock: "domain-testing.jsonl",
      // Intelligence domain
      validation: "domain-intelligence.jsonl",
      learning: "domain-intelligence.jsonl",
      vitals: "domain-intelligence.jsonl",
      intelligence: "domain-intelligence.jsonl",
      advisory: "domain-intelligence.jsonl"
    };
    FILE_PATH_DOMAIN_MAP = {
      "apps/vscode": "domain-vscode.jsonl",
      "apps/web": "domain-web.jsonl",
      "apps/api": "domain-api.jsonl",
      "packages/mcp": "domain-mcp-cli.jsonl",
      "packages/cli": "domain-mcp-cli.jsonl",
      "packages/intelligence": "domain-intelligence.jsonl",
      ".test.": "domain-testing.jsonl",
      ".spec.": "domain-testing.jsonl",
      __tests__: "domain-testing.jsonl"
    };
    __name8(detectDomainFilesFromKeywords, "detectDomainFilesFromKeywords");
    __name8(detectDomainFilesFromPaths, "detectDomainFilesFromPaths");
    DEFAULT_DOMAIN_FILES = [
      "architecture-patterns.jsonl"
    ];
    HOT_TIER_FILE = "hot.jsonl";
    COLD_TIER_FILE = "learnings.jsonl";
    DEFAULT_MAX_LEARNINGS = 3;
    MIN_RELEVANCE_SCORE = 0.3;
    MIN_HOT_RELEVANCE_SCORE = 0.1;
    USAGE_STATS_FILE = "usage-stats.json";
    HOT_TIER_PROMOTION_THRESHOLD = {
      /** Minimum access count to be considered for promotion */
      minAccessCount: 3,
      /** Minimum applied count (higher weight) */
      minAppliedCount: 1,
      /** Maximum hot tier size */
      maxHotTierSize: 20,
      /** Recency weight - more recent = higher score */
      recencyDays: 14
    };
    PATTERN_OBSERVATION_THRESHOLD = {
      /** Minimum observations before promotion */
      minObservations: 3,
      /** Maximum days between observations to count as related */
      observationWindowDays: 30,
      /** Minimum confidence for auto-promotion */
      minConfidence: 0.7
    };
    VERIFICATION_COMMAND_ALLOWLIST = /^pnpm\s+vitest\s+run\s+tests\/architecture\/[A-Za-z0-9._\-/]+\.test\.ts(\s+--grep\s+"[^"]+")?$/;
    __name8(emitInvariantIfQualified, "emitInvariantIfQualified");
    HOT_TIER_BOOST = 100;
    CRITICAL_PRIORITY_BOOST = 50;
    HIGH_PRIORITY_BOOST = 25;
    __name8(loadJsonlSafe, "loadJsonlSafe");
    __name8(scoreLearning, "scoreLearning");
    __name8(getPriorityBoost, "getPriorityBoost");
    TieredLearningService = class {
      static {
        __name(this, "TieredLearningService");
      }
      static {
        __name8(this, "TieredLearningService");
      }
      learningsDir;
      workspaceId;
      workspaceRoot;
      constructor(workspaceRoot, workspaceId) {
        this.learningsDir = join(workspaceRoot, ".vreko", "learnings");
        this.workspaceId = workspaceId;
        this.workspaceRoot = workspaceRoot;
      }
      /**
      * Check if a learning is visible to the current workspace.
      *
      * - Learnings without a workspaceId field are "global" (visible to all workspaces)
      * - Learnings with a workspaceId are only visible to that specific workspace
      * - If no workspaceId is set on this service, all learnings are visible (backward compat)
      */
      isVisibleToWorkspace(learning) {
        const learningWsId = learning.workspaceId;
        if (!learningWsId) {
          return true;
        }
        if (!this.workspaceId) {
          return true;
        }
        return learningWsId === this.workspaceId;
      }
      /**
      * Load learnings from tiered storage based on intent
      *
      * @param options - Loading options (intent, keywords, maxLearnings)
      * @returns Scored and ranked learnings
      */
      async loadTieredLearnings(options) {
        const { intent, keywords, filePaths = [], maxLearnings = DEFAULT_MAX_LEARNINGS } = options;
        const loadedIds = /* @__PURE__ */ new Set();
        const allLearnings = [];
        const hotLearnings = this.loadHotTier();
        for (const learning of hotLearnings) {
          if (!this.isVisibleToWorkspace(learning)) {
            continue;
          }
          const id = learning.id || this.generateId(learning);
          if (!loadedIds.has(id)) {
            loadedIds.add(id);
            allLearnings.push({
              ...learning,
              score: scoreLearning(learning, keywords) + HOT_TIER_BOOST + getPriorityBoost(learning),
              loadedFrom: "hot"
            });
          }
        }
        const intentFiles = INTENT_LEARNING_FILES[intent] || DEFAULT_DOMAIN_FILES;
        const keywordDomainFiles = detectDomainFilesFromKeywords(keywords);
        const pathDomainFiles = detectDomainFilesFromPaths(filePaths);
        const allWarmFiles = [
          .../* @__PURE__ */ new Set([
            ...intentFiles,
            ...keywordDomainFiles,
            ...pathDomainFiles
          ])
        ];
        for (const filename of allWarmFiles) {
          const filepath = join(this.learningsDir, filename);
          const domainLearnings = loadJsonlSafe(filepath);
          const domain = filename.replace(/^domain-/, "").replace(/\.jsonl$/, "");
          for (const learning of domainLearnings) {
            if (!this.isVisibleToWorkspace(learning)) {
              continue;
            }
            const id = learning.id || this.generateId(learning);
            if (!loadedIds.has(id)) {
              loadedIds.add(id);
              allLearnings.push({
                ...learning,
                score: scoreLearning(learning, keywords) + getPriorityBoost(learning),
                loadedFrom: "warm",
                tier: "warm",
                domain
              });
            }
          }
        }
        return allLearnings.filter((l) => {
          const threshold = l.loadedFrom === "hot" ? MIN_HOT_RELEVANCE_SCORE : MIN_RELEVANCE_SCORE;
          const rawScore = l.score - (l.loadedFrom === "hot" ? HOT_TIER_BOOST : 0) - getPriorityBoost(l);
          return rawScore >= threshold;
        }).sort((a, b) => b.score - a.score).slice(0, maxLearnings);
      }
      /**
      * Load hot tier learnings (always loaded)
      */
      loadHotTier() {
        const hotPath = join(this.learningsDir, HOT_TIER_FILE);
        const learnings = loadJsonlSafe(hotPath);
        return learnings.map((l) => ({
          ...l,
          tier: "hot"
        }));
      }
      /**
      * Query cold tier for specific keywords (on-demand only)
      *
      * This method is NOT called by loadTieredLearnings.
      * Use it explicitly when searching archived learnings.
      */
      async queryColdTier(keywords, maxResults = 10) {
        const coldPath = join(this.learningsDir, COLD_TIER_FILE);
        const learnings = loadJsonlSafe(coldPath);
        const scored = learnings.filter((l) => this.isVisibleToWorkspace(l)).filter((l) => l.tier === "cold" || !l.tier).map((l) => ({
          ...l,
          score: scoreLearning(l, keywords),
          loadedFrom: "cold",
          tier: "cold"
        }));
        return scored.filter((l) => l.score > 0).sort((a, b) => b.score - a.score).slice(0, maxResults);
      }
      /**
      * Generate a deterministic ID for learnings without one
      */
      generateId(learning) {
        const trigger = Array.isArray(learning.trigger) ? learning.trigger.join("-") : learning.trigger || "";
        return `${learning.type}-${trigger.slice(0, 20)}`.replace(/\s+/g, "-").toLowerCase();
      }
      // =========================================================================
      // Usage Tracking Methods
      // =========================================================================
      /**
      * Load usage stats from disk
      */
      loadUsageStats() {
        const statsPath = join(this.learningsDir, USAGE_STATS_FILE);
        if (!existsSync(statsPath)) {
          return {};
        }
        try {
          return JSON.parse(readFileSync(statsPath, "utf8"));
        } catch (_error) {
          if (process.env.DEBUG) ;
          return {};
        }
      }
      /**
      * Save usage stats to disk
      * @deprecated Direct file write. Will be migrated to daemon IPC in future version.
      * @internal
      */
      saveUsageStats(stats) {
        const statsPath = join(this.learningsDir, USAGE_STATS_FILE);
        try {
          mkdirSync(this.learningsDir, {
            recursive: true
          });
          writeFileSync(statsPath, JSON.stringify(stats, null, 2));
        } catch (_error) {
          if (process.env.DEBUG) ;
        }
      }
      /**
      * Track that a learning was applied (marked as useful by user/agent).
      * Routes to daemon IPC for centralized state management.
      */
      trackApplied(learningId) {
        if (this.workspaceId) {
          trackAppliedViaDaemon(this.workspaceId, learningId, "medium").catch(() => {
          });
        }
        const stats = this.loadUsageStats();
        const now = /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString();
        if (!stats[learningId]) {
          stats[learningId] = {
            accessCount: 1,
            appliedCount: 0,
            lastAccessed: now
          };
        }
        stats[learningId].appliedCount++;
        stats[learningId].lastAccessed = now;
        this.saveUsageStats(stats);
      }
      /**
      * Calculate promotion score for a learning
      * Higher score = more likely to be promoted to hot tier
      */
      calculatePromotionScore(learning, stats) {
        const id = learning.id || this.generateId(learning);
        const usageData = stats[id];
        if (!usageData) {
          return 0;
        }
        let score = usageData.accessCount + usageData.appliedCount * 3;
        const lastAccessed = new Date(usageData.lastAccessed);
        const daysSinceAccess = (Date.now() - lastAccessed.getTime()) / (1e3 * 60 * 60 * 24);
        if (daysSinceAccess < HOT_TIER_PROMOTION_THRESHOLD.recencyDays) {
          score *= 1 + (1 - daysSinceAccess / HOT_TIER_PROMOTION_THRESHOLD.recencyDays);
        }
        if (learning.priority === "critical") {
          score *= 2;
        } else if (learning.priority === "high") {
          score *= 1.5;
        }
        return score;
      }
      /**
      * Regenerate hot.jsonl based on usage patterns
      *
      * Algorithm:
      * 1. Load all learnings from cold + warm tiers
      * 2. Score each by usage patterns (access, applied, recency)
      * 3. Preserve existing hot tier entries with priority: critical
      * 4. Fill remaining slots with highest-scoring learnings
      * 5. Write new hot.jsonl
      *
      * @returns Statistics about the regeneration
      */
      async regenerateHotTier() {
        const stats = this.loadUsageStats();
        const currentHot = this.loadHotTier();
        const criticalHot = currentHot.filter((l) => l.priority === "critical");
        const coldPath = join(this.learningsDir, COLD_TIER_FILE);
        const allLearnings = loadJsonlSafe(coldPath);
        const warmFiles = Object.values(INTENT_LEARNING_FILES).flat();
        const seenIds = /* @__PURE__ */ new Set();
        const warmLearnings = [];
        for (const filename of [
          ...new Set(warmFiles)
        ]) {
          const filepath = join(this.learningsDir, filename);
          const learnings = loadJsonlSafe(filepath);
          for (const l of learnings) {
            const id = l.id || this.generateId(l);
            if (!seenIds.has(id)) {
              seenIds.add(id);
              warmLearnings.push(l);
            }
          }
        }
        const allCandidates = [
          ...allLearnings,
          ...warmLearnings
        ];
        const candidateMap = /* @__PURE__ */ new Map();
        for (const l of allCandidates) {
          const id = l.id || this.generateId(l);
          if (!candidateMap.has(id)) {
            candidateMap.set(id, l);
          }
        }
        const scored = Array.from(candidateMap.entries()).map(([id, learning]) => ({
          id,
          learning,
          score: this.calculatePromotionScore(learning, stats)
        })).filter((item) => {
          const usageData = stats[item.id];
          if (!usageData) {
            return false;
          }
          return usageData.accessCount >= HOT_TIER_PROMOTION_THRESHOLD.minAccessCount || usageData.appliedCount >= HOT_TIER_PROMOTION_THRESHOLD.minAppliedCount;
        }).sort((a, b) => b.score - a.score);
        const newHot = [];
        const criticalIds = new Set(criticalHot.map((l) => l.id || this.generateId(l)));
        for (const l of criticalHot) {
          newHot.push({
            ...l,
            tier: "hot"
          });
        }
        HOT_TIER_PROMOTION_THRESHOLD.maxHotTierSize - newHot.length;
        let promoted = 0;
        for (const item of scored) {
          if (newHot.length >= HOT_TIER_PROMOTION_THRESHOLD.maxHotTierSize) {
            break;
          }
          if (criticalIds.has(item.id)) {
            continue;
          }
          newHot.push({
            ...item.learning,
            tier: "hot",
            promotedAt: /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString()
          });
          promoted++;
        }
        const newHotIds = new Set(newHot.map((l) => l.id || this.generateId(l)));
        const demoted = currentHot.filter((l) => {
          const id = l.id || this.generateId(l);
          return !newHotIds.has(id);
        }).length;
        return {
          preserved: criticalHot.length,
          promoted,
          demoted,
          totalHot: newHot.length
        };
      }
      /**
      * Get usage statistics for analysis
      */
      getUsageStats() {
        return this.loadUsageStats();
      }
      // =========================================================================
      // Intelligence Layer v0.3.0: Pattern Observation Methods
      // =========================================================================
      /** Pattern observations file */
      get patternObservationsPath() {
        return join(this.learningsDir, "pattern-observations.json");
      }
      /**
      * Load pattern observations from disk
      */
      loadPatternObservations() {
        if (!existsSync(this.patternObservationsPath)) {
          return /* @__PURE__ */ new Map();
        }
        try {
          const content = readFileSync(this.patternObservationsPath, "utf8");
          const records = JSON.parse(content);
          return new Map(records.map((r) => [
            r.patternId,
            r
          ]));
        } catch {
          return /* @__PURE__ */ new Map();
        }
      }
      /**
      * Save pattern observations to disk
      * @deprecated Direct file write. Will be migrated to daemon IPC in future version.
      * @internal
      */
      savePatternObservations(observations) {
        try {
          mkdirSync(this.learningsDir, {
            recursive: true
          });
          const records = Array.from(observations.values());
          writeFileSync(this.patternObservationsPath, JSON.stringify(records, null, 2));
        } catch (_error) {
          if (process.env.DEBUG) ;
        }
      }
      /**
      * Generate a pattern ID from a signature
      */
      generatePatternId(signature) {
        let hash = 0;
        for (let i = 0; i < signature.length; i++) {
          const char = signature.charCodeAt(i);
          hash = (hash << 5) - hash + char;
          hash |= 0;
        }
        return `pat_${Math.abs(hash).toString(36)}`;
      }
      /**
      * Observe a potential pattern.
      *
      * Call this when a pattern is detected during a session.
      * After 3 observations, the pattern is automatically promoted to a learning.
      *
      * Routes to daemon IPC for centralized tracking.
      * @param signature - Normalized pattern signature (e.g., "rapid-write:file.ts")
      * @param patternType - Type of pattern
      * @param sessionId - Current session ID
      * @param confidence - Initial confidence (0-1)
      * @returns Whether the pattern was promoted to a learning
      */
      async observePattern(signature, patternType, sessionId, confidence = 0.5) {
        const observations = this.loadPatternObservations();
        if (this.workspaceId) {
          observePatternViaDaemon(this.workspaceId, signature, patternType, sessionId, confidence).catch(() => {
          });
        }
        const patternId = this.generatePatternId(signature);
        const now = Date.now();
        let observation = observations.get(patternId);
        if (!observation) {
          observation = {
            patternId,
            signature,
            observationCount: 0,
            observedAt: [],
            sessionIds: [],
            workspaceIds: [],
            lastObservedAt: now,
            confidence: 0,
            patternType,
            promoted: false
          };
          observations.set(patternId, observation);
        }
        if (observation.promoted) {
          return {
            promoted: true,
            observationCount: observation.observationCount,
            patternId
          };
        }
        if (!observation.sessionIds.includes(sessionId)) {
          observation.observationCount++;
          observation.observedAt.push(now);
          observation.sessionIds.push(sessionId);
          if (this.workspaceId && !observation.workspaceIds.includes(this.workspaceId)) {
            observation.workspaceIds.push(this.workspaceId);
          }
          observation.confidence = Math.min(1, observation.confidence + confidence * 0.3);
        }
        observation.lastObservedAt = now;
        const shouldPromote = observation.observationCount >= PATTERN_OBSERVATION_THRESHOLD.minObservations && observation.confidence >= PATTERN_OBSERVATION_THRESHOLD.minConfidence && !observation.promoted;
        if (shouldPromote) {
          observation.promoted = true;
          observation.promotedAt = now;
          this.savePatternObservations(observations);
          const learningId = await this.createLearningFromPattern(observation);
          observation.learningId = learningId;
          this.savePatternObservations(observations);
          const counterExamples = observation.counterExampleCount ?? 0;
          if (observation.observationCount >= PATTERN_OBSERVATION_THRESHOLD.minObservations && counterExamples === 0) {
            await emitInvariantIfQualified({
              patternId: observation.patternId,
              // Issue: LIN-INVAR-04  -  inject workspaceRoot via constructor instead of process.cwd() fallback
              workspaceRoot: this.workspaceRoot ?? process.cwd(),
              confirmingInstances: observation.observationCount,
              counterExamples,
              description: observation.signature
            });
          }
          return {
            promoted: true,
            observationCount: observation.observationCount,
            patternId
          };
        }
        this.savePatternObservations(observations);
        return {
          promoted: false,
          observationCount: observation.observationCount,
          patternId
        };
      }
      /**
      * Create a learning from a promoted pattern observation.
      */
      async createLearningFromPattern(observation) {
        const learning = {
          id: `learn_${observation.patternId}`,
          type: observation.patternType,
          trigger: observation.signature,
          action: `Pattern observed ${observation.observationCount} times across ${observation.sessionIds.length} sessions`,
          solution: `Apply pattern from ${observation.observationCount} observations`,
          priority: observation.confidence >= 0.9 ? "high" : "medium",
          confidence: observation.confidence
        };
        return learning.id;
      }
      /**
      * Get all pattern observations.
      */
      getPatternObservations() {
        const observations = this.loadPatternObservations();
        return Array.from(observations.values());
      }
      /**
      * Get patterns ready for promotion (observed 2+ times, not yet promoted).
      */
      getPatternsPendingPromotion() {
        const observations = this.loadPatternObservations();
        return Array.from(observations.values()).filter((o) => !o.promoted && o.observationCount >= PATTERN_OBSERVATION_THRESHOLD.minObservations - 1);
      }
      /**
      * Get promoted patterns.
      */
      getPromotedPatterns() {
        const observations = this.loadPatternObservations();
        return Array.from(observations.values()).filter((o) => o.promoted);
      }
    };
    __name8(createTieredLearningService, "createTieredLearningService");
  }
});
function classifyQueryLocal(query) {
  query.length;
  const hasSpecialChars = /[<>{}[\]()]/.test(query);
  const hasMultipleTerms = query.split(/\s+/).length > 3;
  if (hasSpecialChars) {
    return {
      type: "factual",
      complexity: "high",
      confidence: 0.8,
      weights: {
        semantic: 0.3,
        keyword: 0.7
      },
      reason: "Special characters detected, likely factual lookup"
    };
  }
  if (hasMultipleTerms) {
    return {
      type: "conceptual",
      complexity: "medium",
      confidence: 0.7,
      weights: {
        semantic: 0.7,
        keyword: 0.3
      },
      reason: "Multiple terms, likely conceptual query"
    };
  }
  return {
    type: "exploratory",
    complexity: "low",
    confidence: 0.6,
    weights: {
      semantic: 0.5,
      keyword: 0.5
    },
    reason: "Simple query, using balanced weights"
  };
}
__name(classifyQueryLocal, "classifyQueryLocal");
function getHybridRetriever(workspaceRoot) {
  const cached = retrievers.get(workspaceRoot);
  if (cached && Date.now() - cached.createdAt < CACHE_TTL_MS) {
    return createRetrieverProxy(workspaceRoot);
  }
  retrievers.set(workspaceRoot, {
    workspaceRoot,
    createdAt: Date.now(),
    indexed: indexedWorkspaces.has(workspaceRoot)
  });
  return createRetrieverProxy(workspaceRoot);
}
__name(getHybridRetriever, "getHybridRetriever");
function createRetrieverProxy(workspaceRoot) {
  return {
    retrieve: /* @__PURE__ */ __name8(async (query, options) => {
      const result7 = await hybridRetrieveViaDaemon(workspaceRoot, query, options);
      if (!result7.success || !result7.results) {
        return [];
      }
      return result7.results.map((r) => ({
        id: r.id,
        text: r.text,
        source: r.source,
        score: r.score,
        chunk: {
          id: r.id,
          chunk_text: r.text,
          source_id: r.source,
          metadata: r.metadata
        },
        confidence: r.score,
        sources: [
          r.source
        ],
        metadata: r.metadata
      }));
    }, "retrieve"),
    retrieveAdaptive: /* @__PURE__ */ __name8(async (query, options) => {
      const classification = classifyQueryLocal(query);
      const result7 = await hybridRetrieveViaDaemon(workspaceRoot, query, {
        ...options,
        semanticWeight: classification.weights.semantic,
        keywordWeight: classification.weights.keyword
      });
      const results = [];
      if (result7.success && result7.results) {
        for (const r of result7.results) {
          results.push({
            id: r.id,
            text: r.text,
            source: r.source,
            score: r.score,
            chunk: {
              id: r.id,
              chunk_text: r.text,
              source_id: r.source,
              metadata: r.metadata
            },
            confidence: r.score,
            sources: [
              r.source
            ],
            metadata: r.metadata
          });
        }
      }
      return {
        results,
        classification,
        stats: result7.metrics || {}
      };
    }, "retrieveAdaptive"),
    close: /* @__PURE__ */ __name8(() => {
    }, "close")
  };
}
__name(createRetrieverProxy, "createRetrieverProxy");
async function retrieveAdaptive(workspaceRoot, query, options) {
  return withPatternCache({
    query,
    intent: options?.intent,
    packages: options?.packages
  }, async () => {
    const startTime = performance.now();
    let indexingPerformed = false;
    if (!indexedWorkspaces.has(workspaceRoot)) {
      try {
        const indexResult = await indexAllLearnings(workspaceRoot, {
          skipEmbeddings: !isEmbeddingsAvailable()
        });
        indexedWorkspaces.add(workspaceRoot);
        indexingPerformed = indexResult.indexed > 0;
        if (indexingPerformed) {
        }
      } catch (_error) {
      }
    }
    const classification = classifyQuery(query);
    let fallbackUsed;
    const embeddingsAvailable = isEmbeddingsAvailable();
    if (!embeddingsAvailable) {
      fallbackUsed = "keyword-only";
      return keywordOnlySearch(workspaceRoot, query, classification, startTime, {
        fallbackUsed,
        indexingPerformed
      });
    }
    if (classification.confidence < MIN_CLASSIFICATION_CONFIDENCE) {
      fallbackUsed = "balanced-weights";
      classification.weights = {
        semantic: 0.5,
        keyword: 0.5
      };
    }
    try {
      const retriever = getHybridRetriever(workspaceRoot);
      const result7 = await retriever.retrieveAdaptive(query);
      let boostedResults = result7.results;
      let intentBoostedCount = 0;
      const intent = options?.intent;
      if (intent) {
        const { results: boosted, boostedCount } = applyIntentBoost(result7.results, intent);
        boostedResults = boosted;
        intentBoostedCount = boostedCount;
      }
      return {
        results: boostedResults,
        classification: fallbackUsed === "balanced-weights" ? {
          ...result7.classification,
          weights: {
            semantic: 0.5,
            keyword: 0.5
          }
        } : result7.classification,
        stats: {
          semanticCandidates: result7.stats.semanticCandidates || 0,
          keywordCandidates: result7.stats.keywordCandidates || 0,
          fusedCount: boostedResults.length,
          latencyMs: performance.now() - startTime,
          fallbackUsed,
          indexingPerformed,
          intentUsed: intent,
          intentBoostedCount
        }
      };
    } catch (_error) {
      return keywordOnlySearch(workspaceRoot, query, classification, startTime, {
        fallbackUsed: "keyword-only",
        indexingPerformed,
        intent: options?.intent
      });
    }
  });
}
__name(retrieveAdaptive, "retrieveAdaptive");
async function keywordOnlySearch(workspaceRoot, query, classification, startTime, metadata) {
  const daemonResult = await searchKnowledgeViaDaemon(workspaceRoot, query, 10);
  const elapsed = performance.now() - startTime;
  let results = [];
  if (daemonResult.success && daemonResult.chunks) {
    results = daemonResult.chunks.map((chunk, index) => ({
      id: chunk.id,
      text: chunk.chunk_text,
      source: chunk.source_id,
      chunk: {
        id: chunk.id,
        chunk_text: chunk.chunk_text,
        source_id: chunk.source_id,
        metadata: chunk.metadata
      },
      score: 1 / (60 + index + 1),
      confidence: 0.5 + 0.3 * (1 - index / (daemonResult.chunks?.length || 1)),
      sources: [
        chunk.source_id
      ],
      metadata: chunk.metadata
    }));
  }
  let intentBoostedCount = 0;
  if (metadata.intent) {
    const { results: boosted, boostedCount } = applyIntentBoost(results, metadata.intent);
    results = boosted;
    intentBoostedCount = boostedCount;
  }
  return {
    results,
    classification: {
      ...classification,
      weights: {
        semantic: 0,
        keyword: 1
      },
      reason: "Fallback to keyword-only via daemon"
    },
    stats: {
      semanticCandidates: 0,
      keywordCandidates: results.length,
      fusedCount: results.length,
      latencyMs: elapsed,
      fallbackUsed: metadata.fallbackUsed,
      indexingPerformed: metadata.indexingPerformed,
      intentUsed: metadata.intent,
      intentBoostedCount
    }
  };
}
__name(keywordOnlySearch, "keywordOnlySearch");
function classifyQueryType(query) {
  return classifyQuery(query);
}
__name(classifyQueryType, "classifyQueryType");
async function initializeHybridRetrieval(workspaceRoot) {
  const fastModeEnv = process.env.VREKO_MCP_FAST_MODE;
  const fastMode = fastModeEnv === "true";
  if (fastMode) {
    return {
      embeddingsReady: false,
      indexingResult: {
        indexed: 0,
        skipped: 0,
        success: true,
        errors: [],
        embeddingsGenerated: 0
      }
    };
  }
  const embeddingsReady2 = await ensureEmbeddingsReady();
  const indexingResult = await indexAllLearnings(workspaceRoot, {
    skipEmbeddings: !embeddingsReady2
  });
  indexedWorkspaces.add(workspaceRoot);
  getHybridRetriever(workspaceRoot);
  return {
    embeddingsReady: embeddingsReady2,
    indexingResult
  };
}
__name(initializeHybridRetrieval, "initializeHybridRetrieval");
function invalidateRetrieverCache(workspaceRoot) {
  const cached = retrievers.get(workspaceRoot);
  if (cached) {
    retrievers.delete(workspaceRoot);
  }
  indexedWorkspaces.delete(workspaceRoot);
}
__name(invalidateRetrieverCache, "invalidateRetrieverCache");
function applyIntentBoost(results, intent) {
  const domainKeywords = INTENT_DOMAIN_KEYWORDS[intent] || [];
  const intentFiles = INTENT_LEARNING_FILES[intent] || [];
  let boostedCount = 0;
  const boosted = results.map((result7) => {
    const chunkText = result7.chunk.chunk_text?.toLowerCase() || "";
    const sourceId = result7.chunk.source_id?.toLowerCase() || "";
    const metadata = result7.chunk.metadata;
    const domain = metadata?.domain?.toLowerCase() || "";
    let shouldBoost = false;
    const keywordMatches = domainKeywords.filter((kw) => chunkText.includes(kw)).length;
    if (keywordMatches >= 2) {
      shouldBoost = true;
    }
    if (intentFiles.some((f) => sourceId.includes(f.replace(".jsonl", "")))) {
      shouldBoost = true;
    }
    if (domain && INTENT_DOMAIN_KEYWORDS[intent]?.some((kw) => domain.includes(kw))) {
      shouldBoost = true;
    }
    if (shouldBoost) {
      boostedCount++;
      return {
        ...result7,
        score: result7.score * (1 + INTENT_BOOST_FACTOR),
        confidence: Math.min(result7.confidence * (1 + INTENT_BOOST_FACTOR), 1),
        sources: {
          ...result7.sources,
          intentBoost: {
            intent,
            factor: INTENT_BOOST_FACTOR
          }
        }
      };
    }
    return result7;
  });
  boosted.sort((a, b) => b.score - a.score);
  return {
    results: boosted,
    boostedCount
  };
}
__name(applyIntentBoost, "applyIntentBoost");
var classifyQuery;
var MIN_CLASSIFICATION_CONFIDENCE;
var CACHE_TTL_MS;
var INTENT_BOOST_FACTOR;
var INTENT_DOMAIN_KEYWORDS;
var retrievers;
var indexedWorkspaces;
var init_hybrid_retrieval_service = __esm2({
  "src/services/hybrid-retrieval-service.ts"() {
    init_intelligence_adapter();
    init_knowledge_ingestion_service();
    init_tiered_learning_service();
    __name8(classifyQueryLocal, "classifyQueryLocal");
    classifyQuery = classifyQueryLocal;
    MIN_CLASSIFICATION_CONFIDENCE = 0.5;
    CACHE_TTL_MS = 5 * 60 * 1e3;
    INTENT_BOOST_FACTOR = 0.2;
    INTENT_DOMAIN_KEYWORDS = {
      implement: [
        "architecture",
        "pattern",
        "structure",
        "design",
        "implementation",
        "feature"
      ],
      debug: [
        "error",
        "bug",
        "fix",
        "issue",
        "problem",
        "anti-pattern",
        "pitfall",
        "test"
      ],
      refactor: [
        "workflow",
        "refactor",
        "clean",
        "extract",
        "move",
        "rename",
        "consolidate"
      ],
      review: [
        "review",
        "check",
        "audit",
        "verify",
        "pattern",
        "test"
      ],
      explore: [
        "context",
        "workflow",
        "understand",
        "how",
        "where",
        "what"
      ]
    };
    retrievers = /* @__PURE__ */ new Map();
    indexedWorkspaces = /* @__PURE__ */ new Set();
    __name8(getHybridRetriever, "getHybridRetriever");
    __name8(createRetrieverProxy, "createRetrieverProxy");
    __name8(retrieveAdaptive, "retrieveAdaptive");
    __name8(keywordOnlySearch, "keywordOnlySearch");
    __name8(classifyQueryType, "classifyQueryType");
    __name8(initializeHybridRetrieval, "initializeHybridRetrieval");
    __name8(invalidateRetrieverCache, "invalidateRetrieverCache");
    __name8(applyIntentBoost, "applyIntentBoost");
  }
});
function validateSize(learning) {
  const errors = [];
  const warnings = [];
  const trigger = Array.isArray(learning.trigger) ? learning.trigger.join(", ") : learning.trigger || "";
  const action = learning.action || "";
  const solution = learning.solution || "";
  const totalSize = trigger.length + action.length + solution.length;
  if (totalSize > MAX_LEARNING_SIZE) {
    errors.push(`Combined learning size exceeds maximum (${MAX_LEARNING_SIZE} chars): ${totalSize} chars`);
  }
  if (trigger.length > MAX_TRIGGER_LENGTH) {
    errors.push(`Trigger exceeds maximum length (${MAX_TRIGGER_LENGTH} chars): ${trigger.length} chars`);
  }
  if (action.length > MAX_ACTION_LENGTH) {
    errors.push(`Action exceeds maximum length (${MAX_ACTION_LENGTH} chars): ${action.length} chars`);
  }
  if (solution.length > MAX_SOLUTION_LENGTH) {
    errors.push(`Solution exceeds maximum length (${MAX_SOLUTION_LENGTH} chars): ${solution.length} chars`);
  }
  if (totalSize > MAX_LEARNING_SIZE * 0.8 && totalSize <= MAX_LEARNING_SIZE) {
    warnings.push(`Learning size approaching limit: ${totalSize}/${MAX_LEARNING_SIZE} chars`);
  }
  return {
    isValid: errors.length === 0,
    errors,
    warnings
  };
}
__name(validateSize, "validateSize");
function validateContent(learning) {
  const errors = [];
  const warnings = [];
  const trigger = Array.isArray(learning.trigger) ? learning.trigger.join(" ") : learning.trigger || "";
  const action = learning.action || "";
  const solution = learning.solution || "";
  const context = learning.context || "";
  const combinedContent = `${trigger} ${action} ${solution} ${context}`;
  for (const pattern of BLOCKED_PATTERNS) {
    if (pattern.test(combinedContent)) {
      errors.push(`Blocked pattern detected: ${pattern.source}`);
    }
  }
  if (combinedContent.includes("\\u") || combinedContent.includes("\\x")) {
    warnings.push("Contains escaped unicode/hex characters - verify legitimacy");
  }
  if (combinedContent.match(/\s{10,}/)) {
    warnings.push("Contains excessive whitespace - possible obfuscation");
  }
  return {
    isValid: errors.length === 0,
    errors,
    warnings
  };
}
__name(validateContent, "validateContent");
function validateProvenance(source) {
  const errors = [];
  const warnings = [];
  if (!VALID_SOURCES.includes(source)) {
    errors.push(`Invalid source: ${source}. Must be one of: ${VALID_SOURCES.join(", ")}`);
  }
  return {
    isValid: errors.length === 0,
    errors,
    warnings
  };
}
__name(validateProvenance, "validateProvenance");
function normalizeSource(source) {
  if (source === "mcp" || source === "snap_learn" || source === "cli") {
    return "user_explicit";
  }
  if (source === "violation" || source === "auto") {
    return "auto_inferred";
  }
  if (source.startsWith("ADR-") || source === "import" || source === "external") {
    return "imported";
  }
  return "user_explicit";
}
__name(normalizeSource, "normalizeSource");
function calculateExpirationDate(createdAt = /* @__PURE__ */ new Date()) {
  const expiresAt = new Date(createdAt);
  expiresAt.setDate(expiresAt.getDate() + DEFAULT_EXPIRATION_DAYS);
  return expiresAt;
}
__name(calculateExpirationDate, "calculateExpirationDate");
function validateAndEnhanceLearning(learning) {
  const allErrors = [];
  const allWarnings = [];
  const sizeResult = validateSize(learning);
  allErrors.push(...sizeResult.errors);
  allWarnings.push(...sizeResult.warnings);
  const contentResult = validateContent(learning);
  allErrors.push(...contentResult.errors);
  allWarnings.push(...contentResult.warnings);
  const source = learning.source || "user_explicit";
  const provenanceResult = validateProvenance(source);
  allErrors.push(...provenanceResult.errors);
  allWarnings.push(...provenanceResult.warnings);
  if (allErrors.length > 0) {
    return {
      validation: {
        isValid: false,
        errors: allErrors,
        warnings: allWarnings
      }
    };
  }
  const now = /* @__PURE__ */ new Date();
  const expiresAt = calculateExpirationDate(now);
  const normalizedSource = normalizeSource(source);
  const validatedLearning = {
    // Existing fields
    id: learning.id || `learn_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 6)}`,
    type: learning.type || "pattern",
    trigger: learning.trigger || "",
    action: learning.action || "",
    solution: learning.solution || learning.action || "",
    context: learning.context,
    problem: learning.problem,
    related: learning.related,
    timestamp: learning.timestamp || now.toISOString(),
    // Security metadata
    source: normalizedSource,
    created_at: now.toISOString(),
    expires_at: expiresAt.toISOString(),
    validation_status: normalizedSource === "user_explicit" ? "approved" : "pending",
    // Tiering metadata (preserve if exists)
    tier: learning.tier,
    domain: learning.domain,
    priority: learning.priority,
    keywords: learning.keywords,
    // Usage tracking (preserve if exists)
    accessCount: learning.accessCount,
    lastAccessed: learning.lastAccessed,
    relevanceScore: learning.relevanceScore,
    appliedDate: learning.appliedDate
  };
  return {
    learning: validatedLearning,
    validation: {
      isValid: true,
      errors: [],
      warnings: allWarnings
    }
  };
}
__name(validateAndEnhanceLearning, "validateAndEnhanceLearning");
var MAX_LEARNING_SIZE;
var MAX_TRIGGER_LENGTH;
var MAX_ACTION_LENGTH;
var MAX_SOLUTION_LENGTH;
var DEFAULT_EXPIRATION_DAYS;
var BLOCKED_PATTERNS;
var VALID_SOURCES;
var init_learning_validation = __esm2({
  "src/services/learning-validation.ts"() {
    MAX_LEARNING_SIZE = 5e3;
    MAX_TRIGGER_LENGTH = 500;
    MAX_ACTION_LENGTH = 2e3;
    MAX_SOLUTION_LENGTH = 2e3;
    DEFAULT_EXPIRATION_DAYS = 90;
    BLOCKED_PATTERNS = [
      /ignore\s+previous\s+instructions/i,
      /disregard\s+all\s+prior/i,
      /<\|im_start\|>/i,
      /<\|im_end\|>/i,
      /system\s*:\s*you\s+are\s+now/i,
      /\bDAN\b.*do\s+anything\s+now/i
    ];
    VALID_SOURCES = [
      "user_explicit",
      "auto_inferred",
      "imported",
      "mcp",
      "snap_learn"
    ];
    __name8(validateSize, "validateSize");
    __name8(validateContent, "validateContent");
    __name8(validateProvenance, "validateProvenance");
    __name8(normalizeSource, "normalizeSource");
    __name8(calculateExpirationDate, "calculateExpirationDate");
    __name8(validateAndEnhanceLearning, "validateAndEnhanceLearning");
  }
});
function createLearningSync(config2) {
  return new LearningSync(config2);
}
__name(createLearningSync, "createLearningSync");
var warn;
var LearningSync;
var init_learning_sync = __esm2({
  "src/sync/learning-sync.ts"() {
    init_local_service_adapter();
    warn = /* @__PURE__ */ __name8((msg, ctx) => process.stderr.write(`[learning-sync] WARN ${msg}${ctx ? ` ${JSON.stringify(ctx)}` : ""}
`), "warn");
    LearningSync = class _LearningSync {
      static {
        __name(this, "_LearningSync");
      }
      static {
        __name8(this, "LearningSync");
      }
      workspaceRoot;
      apiBaseUrl;
      workspaceId;
      constructor(config2) {
        this.workspaceRoot = config2.workspaceRoot;
        this.apiBaseUrl = config2.apiBaseUrl || "https://api.vreko.dev";
        this.workspaceId = config2.workspaceId;
      }
      /**
      * Queue a learning for cloud sync.
      * Delegates to daemon IPC (learning/queue) instead of writing to state.bin.
      */
      async queueLearning(learning) {
        const patternKey = _LearningSync.generatePatternKey(learning);
        try {
          const daemonClient = createServiceClient2(this.workspaceRoot);
          await daemonClient.request("learning/queue", {
            workspace: this.workspaceRoot,
            patternKey,
            patternType: learning.type,
            confidence: _LearningSync.calculateConfidence(learning.type)
          });
        } catch (error) {
          warn("learning/queue IPC failed  -  learning dropped this cycle", {
            error: error instanceof Error ? error.message : String(error),
            patternKey
          });
        }
      }
      /**
      * Sync pending learnings to cloud API.
      * Returns count of synced/failed items.
      */
      async syncPending(options = {}) {
        const maxBatchSize = options.maxBatchSize ?? 100;
        const result7 = {
          synced: 0,
          failed: 0,
          errors: []
        };
        let pending;
        let daemonClient;
        try {
          daemonClient = createServiceClient2(this.workspaceRoot);
          const response = await daemonClient.request("learning/queue-sync", {
            workspace: this.workspaceRoot,
            maxBatchSize
          });
          pending = response?.items ?? [];
        } catch {
          return {
            synced: 0,
            failed: 0,
            errors: [
              "daemon unavailable"
            ]
          };
        }
        if (pending.length === 0) {
          return result7;
        }
        const syncedIds = [];
        const failedIds = [];
        for (const item of pending) {
          try {
            await this.syncItem(item);
            syncedIds.push(item.id);
            result7.synced++;
          } catch (error) {
            failedIds.push(item.id);
            result7.failed++;
            result7.errors.push(error instanceof Error ? error.message : String(error));
          }
        }
        try {
          await daemonClient.request("learning/queue-sync-complete", {
            workspace: this.workspaceRoot,
            syncedIds,
            failedIds
          });
        } catch (error) {
          warn("learning/queue-sync-complete IPC failed  -  sync state may be inconsistent", {
            error: error instanceof Error ? error.message : String(error),
            syncedCount: syncedIds.length,
            failedCount: failedIds.length
          });
        }
        return result7;
      }
      /**
      * Fetch cross-workspace recommendations from API.
      * Returns empty array on error (graceful degradation).
      */
      async fetchRecommendations() {
        try {
          const url = `${this.apiBaseUrl}/v1/mcp/recommendations`;
          const response = await fetch(url, {
            method: "POST",
            headers: {
              "Content-Type": "application/json"
            },
            body: JSON.stringify({
              workspaceId: this.workspaceId
            })
          });
          if (!response.ok) {
            return [];
          }
          const data = await response.json();
          return data.recommendations || [];
        } catch {
          return [];
        }
      }
      // ===========================================================================
      // Static Helpers
      // ===========================================================================
      /**
      * Generate pattern key from learning (trigger→action format).
      * This is what gets stored in the aggregated learnings table.
      */
      static generatePatternKey(learning) {
        return `${learning.trigger}\u2192${learning.action}`;
      }
      /**
      * Calculate initial confidence based on learning type.
      * Pitfalls get higher confidence as they represent mistakes to avoid.
      */
      static calculateConfidence(type) {
        const confidenceMap = {
          pattern: 0.6,
          pitfall: 0.7,
          efficiency: 0.5,
          discovery: 0.4,
          workflow: 0.5
        };
        return confidenceMap[type] ?? 0.5;
      }
      // ===========================================================================
      // Private Helpers
      // ===========================================================================
      /**
      * Sync a single item to the API
      */
      async syncItem(item) {
        const url = `${this.apiBaseUrl}/v1/mcp/learning/record`;
        const response = await fetch(url, {
          method: "POST",
          headers: {
            "Content-Type": "application/json"
          },
          body: JSON.stringify({
            workspaceId: this.workspaceId,
            patternKey: item.patternKey,
            patternType: item.patternType,
            confidence: item.confidence
          })
        });
        if (!response.ok) {
          throw new Error(`API error: ${response.status} ${response.statusText}`);
        }
      }
    };
    __name8(createLearningSync, "createLearningSync");
  }
});
function calculatePackageScore(learningPackages, queryPackages) {
  if (!learningPackages || learningPackages.length === 0 || queryPackages.length === 0) {
    return 0;
  }
  let matches = 0;
  for (const queryPkg of queryPackages) {
    const queryLower = queryPkg.toLowerCase();
    for (const learningPkg of learningPackages) {
      const learningLower = learningPkg.toLowerCase();
      if (learningLower === queryLower || learningLower.includes(queryLower) || queryLower.includes(learningLower)) {
        matches++;
        break;
      }
    }
  }
  const matchRatio = matches / queryPackages.length;
  return Math.round(matchRatio * 100);
}
__name(calculatePackageScore, "calculatePackageScore");
var handleLearn;
var handleGetLearnings;
var init_learning_handlers = __esm2({
  "src/facades/handlers/learning-handlers.ts"() {
    init_local_service_adapter();
    init_errors();
    init_hybrid_retrieval_service();
    init_knowledge_ingestion_service();
    init_learning_validation();
    init_learning_sync();
    init_utils();
    __name8(calculatePackageScore, "calculatePackageScore");
    handleLearn = /* @__PURE__ */ __name8(async (args, context) => {
      const { type, trigger, action, source } = args;
      if (!type || !trigger || !action) {
        return errorJsonResult(CommonErrors.missingParam("type, trigger, action"));
      }
      const validationResult = validateAndEnhanceLearning({
        type,
        trigger,
        action,
        source: source || "user_explicit"
      });
      if (!validationResult.validation.isValid || !("learning" in validationResult)) {
        return errorJsonResult({
          status: "error",
          message: "Learning validation failed",
          errors: validationResult.validation.errors,
          warnings: validationResult.validation.warnings
        });
      }
      const learning = validationResult.learning;
      const { id } = learning;
      join(context.workspaceRoot, ".vreko", "learnings", "learnings.jsonl");
      const isRemoteMode = !isLocalWorkspace(context.workspaceRoot);
      if (isRemoteMode) {
        return jsonResult({
          id,
          status: "success",
          learning,
          message: "Learning captured (client-side storage required)",
          remoteMode: true,
          instruction: "Store this learning locally in .vreko/learnings/learnings.jsonl",
          warnings: validationResult.validation.warnings
        });
      }
      try {
        if (context.workspaceId) {
          assertValidWorkspaceId(context.workspaceId, "handlers.tagLearning");
        }
        const learningEntry = context.workspaceId ? {
          ...learning,
          workspaceId: context.workspaceId
        } : learning;
        const daemonResult = await addLearningViaDaemon(context.workspaceRoot, {
          type: learningEntry.type,
          trigger: Array.isArray(learningEntry.trigger) ? learningEntry.trigger.join(", ") : learningEntry.trigger,
          action: learningEntry.action,
          source: learningEntry.source
        });
        if (!daemonResult.success) {
        }
        indexLearning(context.workspaceRoot, learning).catch((_indexError) => {
        });
        invalidateRetrieverCache(context.workspaceRoot);
        invalidateCache(context.workspaceRoot);
        if (context.workspaceId) {
          const syncService = createLearningSync({
            workspaceRoot: context.workspaceRoot,
            workspaceId: context.workspaceId
          });
          syncService.queueLearning(learning).catch((_syncError) => {
          });
        }
        return jsonResult({
          id,
          status: "success",
          learning,
          message: "Learning recorded",
          storagePath: ".vreko/learnings/learnings.jsonl",
          syncQueued: !!context.workspaceId,
          warnings: validationResult.validation.warnings,
          security: {
            provenance: learning.source,
            expiresAt: learning.expires_at,
            validationStatus: learning.validation_status
          }
        });
      } catch (error) {
        const message = error instanceof Error ? error.message : String(error);
        if (message.includes("EACCES") || message.includes("EPERM") || message.includes("permission")) {
          return jsonResult({
            id,
            status: "success",
            learning,
            message: "Learning captured (permission denied, client-side storage required)",
            remoteMode: true,
            instruction: "Store this learning locally in .vreko/learnings/learnings.jsonl",
            warning: "Server cannot write to workspace directory"
          });
        }
        return result(`Failed to record learning: ${message}`, true);
      }
    }, "handleLearn");
    handleGetLearnings = /* @__PURE__ */ __name8(async (args, context) => {
      const { keywords, limit = 10, intent = "implement", includeCold = true, packages, packageMode = "boost" } = args;
      if (!keywords || keywords.length === 0) {
        return errorJsonResult(CommonErrors.missingParam("keywords"));
      }
      try {
        const { searchLearningsViaDaemon: searchLearningsViaDaemon2 } = await Promise.resolve().then(() => (init_local_service_adapter(), local_service_adapter_exports));
        const allLearnings = [];
        if (includeCold) {
          const coldResult = await searchLearningsViaDaemon2(context.workspaceRoot, keywords, limit);
          if (coldResult.success && coldResult.learnings) {
            const seen = new Set(allLearnings.map((l) => `${l.type}:${l.trigger}`));
            for (const l of coldResult.learnings) {
              if (!seen.has(`${l.type}:${l.trigger}`)) {
                allLearnings.push(l);
              }
            }
          }
        }
        const learnings = allLearnings.map((l) => ({
          id: `${l.type}-${l.trigger.slice(0, 20)}`,
          type: l.type,
          trigger: Array.isArray(l.trigger) ? l.trigger.join(", ") : l.trigger,
          action: l.action,
          source: void 0,
          timestamp: void 0,
          loadedFrom: "daemon",
          relevanceScore: l.relevanceScore
        }));
        const shouldScorePackages = packages && packages.length > 0;
        const effectiveMode = packageMode === "filter" ? "filter" : "boost";
        let scoredLearnings = learnings.map((l) => {
          const learningWithPkgs = l;
          const score = shouldScorePackages ? calculatePackageScore(learningWithPkgs.packages, packages) : void 0;
          return {
            ...l,
            packages: learningWithPkgs.packages,
            packageScore: score
          };
        });
        if (shouldScorePackages && effectiveMode === "filter") {
          scoredLearnings = scoredLearnings.filter((l) => (l.packageScore ?? 0) > 0);
        }
        if (shouldScorePackages) {
          scoredLearnings.sort((a, b) => (b.packageScore ?? 0) - (a.packageScore ?? 0));
        }
        const limited = scoredLearnings.slice(0, Math.min(limit, 50));
        return jsonResult({
          status: "success",
          count: limited.length,
          total: allLearnings.length,
          keywords,
          intent,
          learnings: limited.map((l) => ({
            id: l.id,
            type: l.type,
            trigger: l.trigger,
            action: l.action,
            source: l.source,
            timestamp: l.timestamp,
            loadedFrom: l.loadedFrom,
            packageScore: l.packageScore
          })),
          tierInfo: {
            hotAndWarm: allLearnings.length,
            cold: 0
          },
          next_actions: limited.length > 0 ? [
            {
              tool: "learn",
              priority: 2,
              reason: "Record new learnings"
            }
          ] : []
        });
      } catch (error) {
        const message = error instanceof Error ? error.message : String(error);
        return result(`Failed to get learnings: ${message}`, true);
      }
    }, "handleGetLearnings");
  }
});
function parseImportPath(path4) {
  if (path4.startsWith("@")) {
    const parts2 = path4.split("/");
    if (parts2.length >= 2) {
      return {
        packageName: `${parts2[0]}/${parts2[1]}`,
        subpath: parts2.slice(2).join("/")
      };
    }
    return {
      packageName: path4,
      subpath: ""
    };
  }
  const parts = path4.split("/");
  return {
    packageName: parts[0],
    subpath: parts.slice(1).join("/")
  };
}
__name(parseImportPath, "parseImportPath");
function findPackageDir(workspaceRoot, packageName) {
  const nodeModulesPath = join(workspaceRoot, "node_modules", ...packageName.split("/"));
  if (existsSync(nodeModulesPath)) {
    return nodeModulesPath;
  }
  const localPackagesPath = join(workspaceRoot, "packages");
  if (existsSync(localPackagesPath)) {
    const localName = packageName.startsWith("@") ? packageName.split("/")[1] : packageName;
    const localPath = join(localPackagesPath, localName);
    if (existsSync(localPath)) {
      return localPath;
    }
  }
  return null;
}
__name(findPackageDir, "findPackageDir");
async function extractExportsFromFile(packageDir, exportTarget, _workspaceRoot) {
  const exports = [];
  let targetPath = join(packageDir, exportTarget);
  const extensions = [
    "",
    ".ts",
    ".tsx",
    ".js",
    ".jsx",
    ".mjs"
  ];
  let resolvedPath = null;
  for (const ext of extensions) {
    const tryPath = targetPath + ext;
    if (existsSync(tryPath)) {
      resolvedPath = tryPath;
      break;
    }
    const indexPath = join(targetPath, `index${ext}`);
    if (existsSync(indexPath)) {
      resolvedPath = indexPath;
      break;
    }
  }
  if (!resolvedPath) {
    const srcTarget = exportTarget.replace("./dist/", "./src/").replace(".js", ".ts");
    targetPath = join(packageDir, srcTarget);
    for (const ext of extensions) {
      const tryPath = targetPath + ext;
      if (existsSync(tryPath)) {
        resolvedPath = tryPath;
        break;
      }
      const indexPath = join(targetPath, `index${ext}`);
      if (existsSync(indexPath)) {
        resolvedPath = indexPath;
        break;
      }
    }
  }
  if (!resolvedPath) {
    return exports;
  }
  throw new Error("AST export lookup not yet implemented  -  see Deferred Backlog");
}
__name(extractExportsFromFile, "extractExportsFromFile");
function getExportDescription(exportPath) {
  const pathDescriptions = {
    ".": "Main entry point",
    "./analysis": "Code analysis utilities",
    "./signals": "Signal/event system",
    "./types": "TypeScript type definitions",
    "./utils": "Utility functions",
    "./core": "Core functionality"
  };
  return pathDescriptions[exportPath] || "";
}
__name(getExportDescription, "getExportDescription");
var handleLookupExports;
var init_lookup_exports_handler = __esm2({
  "src/facades/handlers/lookup-exports-handler.ts"() {
    init_utils();
    __name8(parseImportPath, "parseImportPath");
    __name8(findPackageDir, "findPackageDir");
    __name8(extractExportsFromFile, "extractExportsFromFile");
    __name8(getExportDescription, "getExportDescription");
    handleLookupExports = /* @__PURE__ */ __name8(async (args, context) => {
      const input = args;
      const query = input.query;
      if (!query) {
        return result(JSON.stringify({
          error: "E102_MISSING_PARAM",
          message: "Required parameter 'query' is missing"
        }), true);
      }
      const workspaceRoot = context.workspaceRoot;
      const matches = [];
      try {
        const { packageName } = parseImportPath(query);
        const packageDir = findPackageDir(workspaceRoot, packageName);
        if (!packageDir) {
          return jsonResult({
            status: "success",
            query,
            matches: [],
            message: `Package not found: ${packageName}`
          });
        }
        const packageJsonPath = join(packageDir, "package.json");
        if (!existsSync(packageJsonPath)) {
          return jsonResult({
            status: "success",
            query,
            matches: [],
            message: `No package.json found in ${packageDir}`
          });
        }
        const packageJson = JSON.parse(readFileSync(packageJsonPath, "utf8"));
        const exportsMap = packageJson.exports ?? {
          ".": packageJson.main || "./index.js"
        };
        const normalizedExports = typeof exportsMap === "string" ? {
          ".": exportsMap
        } : exportsMap && typeof exportsMap === "object" ? exportsMap : {
          ".": packageJson.main || "./index.js"
        };
        for (const [exportPath, exportTarget] of Object.entries(normalizedExports)) {
          if (typeof exportTarget !== "string") {
            continue;
          }
          const fullPath = exportPath === "." ? packageName : `${packageName}${exportPath.slice(1)}`;
          if (fullPath.startsWith(query) || query === packageName) {
            const exports = await extractExportsFromFile(packageDir, exportTarget, workspaceRoot);
            matches.push({
              path: fullPath,
              exports,
              hasTypes: exports.some((e) => e.startsWith("type ")),
              description: getExportDescription(exportPath)
            });
          }
        }
        matches.sort((a, b) => {
          if (a.path === query) {
            return -1;
          }
          if (b.path === query) {
            return 1;
          }
          return a.path.localeCompare(b.path);
        });
        return jsonResult({
          status: "success",
          query,
          packageName,
          packageDir,
          matches,
          message: matches.length > 0 ? `Found ${matches.length} export path(s) matching "${query}"` : `No exports found matching "${query}"`
        });
      } catch (error) {
        const message = error instanceof Error ? error.message : String(error);
        return jsonResult({
          status: "success",
          query,
          matches: [],
          message: `Error looking up exports: ${message}`
        });
      }
    }, "handleLookupExports");
  }
});
var TOOL_HIERARCHY;
var WORKFLOWS;
var handleMeta;
var init_meta_handlers = __esm2({
  "src/facades/handlers/meta-handlers.ts"() {
    init_utils();
    TOOL_HIERARCHY = {
      entry_points: [
        {
          name: "begin_task",
          status: "implemented",
          description: "Start any task with context and optional snapshot"
        },
        {
          name: "get_context",
          status: "implemented",
          description: "Get workspace context and learnings for a task"
        },
        {
          name: "prepare_workspace",
          status: "implemented",
          description: "Pre-flight workspace check before risky ops"
        }
      ],
      protection: [
        {
          name: "snapshot_create",
          status: "implemented",
          description: "Create file snapshot"
        },
        {
          name: "snapshot_list",
          status: "implemented",
          description: "List available snapshots"
        },
        {
          name: "snapshot_restore",
          status: "implemented",
          description: "Restore from snapshot"
        },
        {
          name: "suggest_snapshot",
          status: "implemented",
          description: "Get proactive snapshot recommendations"
        },
        {
          name: "compare_snapshots",
          status: "implemented",
          description: "Compare snapshots or snapshot to current"
        },
        {
          name: "acknowledge_risk",
          status: "implemented",
          description: "Acknowledge risk state before proceeding"
        }
      ],
      validation: [
        {
          name: "check_patterns",
          status: "implemented",
          description: "Validate code against 7-layer pipeline"
        },
        {
          name: "validate",
          status: "implemented",
          description: "Quick or comprehensive code validation"
        },
        {
          name: "quick_check",
          status: "implemented",
          description: "Fast parallel TypeScript, tests, lint check"
        }
      ],
      knowledge: [
        {
          name: "learn",
          status: "implemented",
          description: "Record learnings from sessions"
        },
        {
          name: "get_learnings",
          status: "implemented",
          description: "Query past learnings with package awareness"
        },
        {
          name: "report_violation",
          status: "implemented",
          description: "Report violations with auto-promotion"
        }
      ],
      discovery: [
        {
          name: "lookup_exports",
          status: "implemented",
          description: "Find valid import paths for packages"
        },
        {
          name: "analyze",
          status: "implemented",
          description: "Risk and package analysis"
        }
      ],
      lifecycle: [
        {
          name: "complete_task",
          status: "implemented",
          description: "Finish task with summary and learnings"
        },
        {
          name: "review_work",
          status: "implemented",
          description: "Pre-commit review of changes"
        },
        {
          name: "what_changed",
          status: "implemented",
          description: "See changes since task start"
        }
      ],
      session: [
        {
          name: "session",
          status: "implemented",
          description: "Session lifecycle management"
        },
        {
          name: "context",
          status: "implemented",
          description: "Context management (init, build, validate)"
        }
      ],
      maintenance: [
        {
          name: "cleanup",
          status: "implemented",
          description: "Reclaim space by removing stale data"
        },
        {
          name: "meta",
          status: "implemented",
          description: "Tool metadata and hierarchy"
        }
      ]
    };
    WORKFLOWS = [
      {
        name: "start_development",
        description: "Begin a development task safely",
        steps: [
          "begin_task",
          "implement changes",
          "quick_check",
          "check_patterns",
          "complete_task"
        ]
      },
      {
        name: "recovery",
        description: "Recover from broken changes",
        steps: [
          "snapshot_list",
          "compare_snapshots",
          "snapshot_restore"
        ]
      },
      {
        name: "learning_loop",
        description: "Capture and retrieve knowledge",
        steps: [
          "get_learnings",
          "work",
          "learn",
          "report_violation (if needed)"
        ]
      }
    ];
    handleMeta = /* @__PURE__ */ __name8(async (_args, _context) => {
      const tools = [];
      for (const category of Object.values(TOOL_HIERARCHY)) {
        for (const tool of category) {
          if (!tools.some((t) => t.name === tool.name)) {
            tools.push(tool);
          }
        }
      }
      return jsonResult({
        version: "0.1.0",
        status: "operational",
        // Flat list for backward compatibility
        tools,
        // New hierarchical organization
        hierarchy: TOOL_HIERARCHY,
        // Workflow recommendations
        workflows: WORKFLOWS,
        // Summary statistics
        summary: {
          totalTools: tools.length,
          categories: Object.keys(TOOL_HIERARCHY).length,
          implementedCount: tools.filter((t) => t.status === "implemented").length
        }
      });
    }, "handleMeta");
  }
});
async function searchFileContext(options) {
  const { retrieval, workspaceId, filePath, taskDescription, limit = 5, threshold = 0.7 } = options;
  if (!retrieval) {
    return {
      learnings: [],
      source: "disabled"
    };
  }
  try {
    if (!retrieval.isReady()) {
      const preloadPromise = retrieval.preload();
      const timeoutPromise = new Promise((_, reject) => setTimeout(() => reject(new Error("Preload timeout")), 3e3));
      await Promise.race([
        preloadPromise,
        timeoutPromise
      ]);
    }
  } catch (error) {
    return {
      learnings: [],
      source: "fallback",
      error: error instanceof Error ? error.message : "Embedding pipeline not ready"
    };
  }
  try {
    const learnings = await retrieval.searchByFile({
      filePath,
      taskDescription,
      workspaceId,
      limit,
      threshold
    });
    return {
      learnings,
      source: "vector"
    };
  } catch (error) {
    return {
      learnings: [],
      source: "fallback",
      error: error instanceof Error ? error.message : "Vector search failed"
    };
  }
}
__name(searchFileContext, "searchFileContext");
async function searchSessionContext(options) {
  const { retrieval, workspaceId, summary, changedFiles, limit = 10 } = options;
  if (!retrieval) {
    return {
      learnings: [],
      source: "disabled"
    };
  }
  try {
    if (!retrieval.isReady()) {
      const preloadPromise = retrieval.preload();
      const timeoutPromise = new Promise((_, reject) => setTimeout(() => reject(new Error("Preload timeout")), 3e3));
      await Promise.race([
        preloadPromise,
        timeoutPromise
      ]);
    }
  } catch (error) {
    return {
      learnings: [],
      source: "fallback",
      error: error instanceof Error ? error.message : "Embedding pipeline not ready"
    };
  }
  try {
    const learnings = await retrieval.searchBySession({
      summary,
      changedFiles,
      workspaceId,
      limit
    });
    return {
      learnings,
      source: "vector"
    };
  } catch (error) {
    return {
      learnings: [],
      source: "fallback",
      error: error instanceof Error ? error.message : "Vector search failed"
    };
  }
}
__name(searchSessionContext, "searchSessionContext");
function formatVectorLearnings(results) {
  return results.map((result7) => ({
    id: result7.id,
    content: result7.content,
    score: result7.score,
    type: result7.metadata?.type || "discovery",
    source: "vector",
    metadata: result7.metadata
  }));
}
__name(formatVectorLearnings, "formatVectorLearnings");
var init_vector_context = __esm2({
  "src/context/vector-context.ts"() {
    __name8(searchFileContext, "searchFileContext");
    __name8(searchSessionContext, "searchSessionContext");
    __name8(formatVectorLearnings, "formatVectorLearnings");
  }
});
var init_context = __esm2({
  "src/context/index.ts"() {
    init_vector_context();
  }
});
function getDependencyGraphService(workspaceRoot) {
  if (!services.has(workspaceRoot)) {
    services.set(workspaceRoot, new DependencyGraphService(workspaceRoot));
  }
  return services.get(workspaceRoot);
}
__name(getDependencyGraphService, "getDependencyGraphService");
var DependencyGraphService;
var services;
var init_dependency_graph_service = __esm2({
  "src/services/dependency-graph-service.ts"() {
    init_local_service_adapter();
    DependencyGraphService = class {
      static {
        __name(this, "DependencyGraphService");
      }
      static {
        __name8(this, "DependencyGraphService");
      }
      workspaceRoot;
      cacheDir;
      graph = null;
      constructor(workspaceRoot) {
        this.workspaceRoot = workspaceRoot;
        this.cacheDir = join(workspaceRoot, ".vreko", "analysis");
      }
      /**
      * Get dependency context for planned files
      * Main entry point for begin_task integration
      */
      async getContextForFiles(files) {
        const graph = await this.getGraph();
        const planned = {};
        const suggestions = /* @__PURE__ */ new Set();
        for (const file of files) {
          const relPath = this.toRelative(file);
          const node = graph.nodes[relPath];
          if (node) {
            planned[file] = {
              imports: node.imports || [],
              importedBy: (node.importedBy || []).map((f) => ({
                file: f,
                line: 0
              })),
              depth: this.calculateDepth(relPath, graph),
              isOrphan: (node.importedBy || []).length === 0 && !this.isEntryPoint(relPath)
            };
            for (const f of (node.importedBy || []).slice(0, 3)) {
              suggestions.add(f);
            }
            for (const f of (node.imports || []).slice(0, 3)) {
              suggestions.add(f);
            }
          }
        }
        const relPlanned = files.map((f) => this.toRelative(f));
        const filteredSuggestions = [
          ...suggestions
        ].filter((s) => !relPlanned.includes(s)).slice(0, 5);
        return {
          planned,
          circular: (graph.circular || []).filter((cycle) => files.some((f) => cycle.includes(this.toRelative(f)))).map((cycle) => ({
            cycle,
            severity: "warning"
          })),
          suggestions: filteredSuggestions
        };
      }
      /**
      * Get files affected by changes to a file.
      * Queries daemon topology service via IPC first; falls back to local graph.
      */
      async getAffectedBy(file) {
        const daemonResult = await this.queryDaemonBlastRadius(file);
        if (daemonResult) {
          return daemonResult.direct;
        }
        const graph = await this.getGraph();
        const relPath = this.toRelative(file);
        const node = graph.nodes[relPath];
        return node?.importedBy || [];
      }
      /**
      * Query daemon's topology/blast-radius RPC for a file.
      * Returns null when daemon is unavailable (graceful degradation).
      */
      async queryDaemonBlastRadius(file) {
        const daemon = createServiceClient2(this.workspaceRoot);
        if (!daemon.isAvailable()) {
          return null;
        }
        return daemon.request("topology/blast-radius", {
          filePath: file
        });
      }
      /**
      * Get files that a file depends on
      */
      async getDependencies(file) {
        const graph = await this.getGraph();
        const relPath = this.toRelative(file);
        const node = graph.nodes[relPath];
        return node?.imports || [];
      }
      /**
      * Get all circular dependencies
      */
      async getCircularDependencies() {
        const graph = await this.getGraph();
        return graph.circular;
      }
      /**
      * Force refresh the graph cache
      */
      async refresh() {
        this.graph = null;
        await this.getGraph();
      }
      /**
      * Check if cache is valid
      */
      async isCacheValid() {
        const cachePath = join(this.cacheDir, "dependency-graph.json");
        if (!existsSync(cachePath)) {
          return false;
        }
        try {
          const cached = JSON.parse(readFileSync(cachePath, "utf8"));
          const currentKey = await this.computeCacheKey();
          return cached.cacheKey === currentKey;
        } catch {
          return false;
        }
      }
      // =========================================================================
      // Private Methods
      // =========================================================================
      /**
      * Get or build the dependency graph
      */
      async getGraph() {
        if (this.graph) {
          return this.graph;
        }
        const cachePath = join(this.cacheDir, "dependency-graph.json");
        const cacheKey = await this.computeCacheKey();
        if (existsSync(cachePath)) {
          try {
            const cached = JSON.parse(readFileSync(cachePath, "utf8"));
            if (cached.cacheKey === cacheKey) {
              this.graph = cached;
              return cached;
            }
          } catch {
          }
        }
        const graph = await this.buildGraph();
        graph.cacheKey = cacheKey;
        graph.generatedAt = Date.now();
        this.ensureCacheDir();
        writeFileSync(cachePath, JSON.stringify(graph, null, 2));
        this.graph = graph;
        return graph;
      }
      /**
      * Build dependency graph.
      *
      * Previously used madge, which was removed in phase 260423-79c during the
      * madge → dependency-cruiser migration. Dependency-graph computation now
      * lives in the daemon topology scanner (apps/local-service/src/services/
      * topology/) which exposes graphs via the topology API; callers should
      * prefer that route.
      *
      * This method is preserved as a no-op stub so the surrounding
      * `DependencyGraphService` public surface (and its adapter in
      * `services/adapters/dependency-graph-service-adapter.ts`) keeps its
      * contract. Returns an empty graph, which matches the previous behavior
      * when the madge dynamic import failed (the old implementation swallowed
      * all errors and returned the same empty graph shape).
      */
      async buildGraph() {
        return {
          nodes: {},
          circular: [],
          cacheKey: "",
          generatedAt: 0
        };
      }
      /**
      * Compute cache key based on source file mtimes
      */
      async computeCacheKey() {
        try {
          const glob2 = await import('glob');
          const files = await glob2.glob([
            "**/*.{ts,tsx,js,jsx}"
          ], {
            cwd: this.workspaceRoot,
            ignore: [
              "node_modules/**",
              "dist/**",
              ".next/**",
              "coverage/**"
            ]
          });
          const hash = createHash("sha256");
          for (const file of files.sort()) {
            try {
              const stat2 = statSync(join(this.workspaceRoot, file));
              hash.update(`${file}:${stat2.mtimeMs}`);
            } catch {
            }
          }
          return hash.digest("hex").substring(0, 16);
        } catch {
          return Date.now().toString(36);
        }
      }
      /**
      * Calculate max depth in import tree
      */
      calculateDepth(file, graph, visited = /* @__PURE__ */ new Set()) {
        if (visited.has(file)) {
          return 0;
        }
        visited.add(file);
        const node = graph.nodes[file];
        if (!node || (node.imports || []).length === 0) {
          return 0;
        }
        const depths = (node.imports || []).map((i) => this.calculateDepth(i, graph, new Set(visited)));
        return 1 + Math.max(0, ...depths);
      }
      /**
      * Check if file is an entry point (not expected to have importers)
      */
      isEntryPoint(file) {
        return file.includes("index.") || file.includes("main.") || file.includes("entry.") || file.includes("server.") || file.includes("app.") || file.endsWith("/page.tsx") || file.endsWith("/layout.tsx") || file.endsWith("/route.ts");
      }
      /**
      * Convert absolute path to relative
      */
      toRelative(file) {
        if (file.startsWith(this.workspaceRoot)) {
          return relative(this.workspaceRoot, file);
        }
        return file;
      }
      /**
      * Ensure cache directory exists
      */
      ensureCacheDir() {
        if (!existsSync(this.cacheDir)) {
          mkdirSync(this.cacheDir, {
            recursive: true
          });
        }
      }
    };
    services = /* @__PURE__ */ new Map();
    __name8(getDependencyGraphService, "getDependencyGraphService");
  }
});
function formatAge(ms) {
  const seconds = Math.floor(ms / 1e3);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  if (days > 0) {
    return `${days}d`;
  }
  if (hours > 0) {
    return `${hours}h`;
  }
  if (minutes > 0) {
    return `${minutes}m`;
  }
  return `${seconds}s`;
}
__name(formatAge, "formatAge");
function errorKey(error) {
  return `${error.file}:${error.line}:${error.message}`;
}
__name(errorKey, "errorKey");
function ensureDirSync(dir) {
  if (!existsSync(dir)) {
    mkdirSync(dir, {
      recursive: true
    });
  }
}
__name(ensureDirSync, "ensureDirSync");
function createErrorCacheService(workspaceRoot) {
  return new ErrorCacheService(workspaceRoot);
}
__name(createErrorCacheService, "createErrorCacheService");
var MAX_AGE_MS;
var MAX_ENTRIES_PER_SOURCE;
var SOURCE_TYPES;
var ErrorCacheService;
var init_error_cache_service = __esm2({
  "src/services/error-cache-service.ts"() {
    MAX_AGE_MS = 7 * 24 * 60 * 60 * 1e3;
    MAX_ENTRIES_PER_SOURCE = 100;
    SOURCE_TYPES = [
      "typescript",
      "test",
      "lint",
      "runtime"
    ];
    __name8(formatAge, "formatAge");
    __name8(errorKey, "errorKey");
    __name8(ensureDirSync, "ensureDirSync");
    ErrorCacheService = class {
      static {
        __name(this, "ErrorCacheService");
      }
      static {
        __name8(this, "ErrorCacheService");
      }
      cacheDir;
      constructor(workspaceRoot) {
        this.cacheDir = join(workspaceRoot, ".vreko", "errors");
      }
      /**
      * Cache errors after validation operations
      *
      * Called by quick_check after TypeScript/test/lint validation.
      * Errors are grouped by source and appended to JSONL files.
      */
      cacheErrors(errors) {
        if (errors.length === 0) {
          return;
        }
        ensureDirSync(this.cacheDir);
        const grouped = /* @__PURE__ */ new Map();
        for (const error of errors) {
          const source = error.source || "general";
          if (!grouped.has(source)) {
            grouped.set(source, []);
          }
          grouped.get(source)?.push(error);
        }
        const timestamp = Date.now();
        for (const [source, sourceErrors] of grouped) {
          const filePath = join(this.cacheDir, `${source}.jsonl`);
          const newLines = `${sourceErrors.map((e) => JSON.stringify({
            ...e,
            timestamp
          })).join("\n")}
`;
          const existing = existsSync(filePath) ? readFileSync(filePath, "utf8") : "";
          writeFileSync(filePath, existing + newLines, "utf8");
        }
      }
      /**
      * Get cached errors for specific files
      *
      * Called by begin_task to surface known issues for planned files.
      * Returns errors within retention window, deduplicated.
      */
      getErrorsForFiles(files) {
        const errors = [];
        const now = Date.now();
        const seen = /* @__PURE__ */ new Set();
        for (const source of SOURCE_TYPES) {
          const filePath = join(this.cacheDir, `${source}.jsonl`);
          if (!existsSync(filePath)) {
            continue;
          }
          try {
            const content = readFileSync(filePath, "utf8");
            const lines = content.split("\n").filter(Boolean);
            for (const line of lines) {
              try {
                const error = JSON.parse(line);
                if (now - error.timestamp > MAX_AGE_MS) {
                  continue;
                }
                const matchesFile = files.some((f) => error.file.includes(f) || f.includes(error.file) || error.file === f);
                if (!matchesFile) {
                  continue;
                }
                const key = errorKey(error);
                if (seen.has(key)) {
                  continue;
                }
                seen.add(key);
                errors.push({
                  ...error,
                  age: formatAge(now - error.timestamp)
                });
              } catch {
              }
            }
          } catch {
          }
        }
        return errors;
      }
      /**
      * Remove stale entries from cache
      *
      * Called periodically or on session end.
      * Removes entries older than 7 days and limits to 100 per source.
      */
      prune() {
        let removed = 0;
        const now = Date.now();
        for (const source of SOURCE_TYPES) {
          const filePath = join(this.cacheDir, `${source}.jsonl`);
          if (!existsSync(filePath)) {
            continue;
          }
          try {
            const content = readFileSync(filePath, "utf8");
            const lines = content.split("\n").filter(Boolean);
            const entries = [];
            for (const line of lines) {
              try {
                const error = JSON.parse(line);
                if (now - error.timestamp <= MAX_AGE_MS) {
                  entries.push(error);
                } else {
                  removed++;
                }
              } catch {
                removed++;
              }
            }
            entries.sort((a, b) => b.timestamp - a.timestamp);
            const kept = entries.slice(0, MAX_ENTRIES_PER_SOURCE);
            removed += entries.length - kept.length;
            if (kept.length > 0) {
              writeFileSync(filePath, `${kept.map((e) => JSON.stringify(e)).join("\n")}
`, "utf8");
            } else {
              writeFileSync(filePath, "", "utf8");
            }
          } catch {
          }
        }
        return {
          removed
        };
      }
      /**
      * Clear all cached errors for a specific file
      *
      * Called when a file is successfully validated with no errors.
      */
      clearForFile(file) {
        for (const source of SOURCE_TYPES) {
          const filePath = join(this.cacheDir, `${source}.jsonl`);
          if (!existsSync(filePath)) {
            continue;
          }
          try {
            const content = readFileSync(filePath, "utf8");
            const lines = content.split("\n").filter(Boolean);
            const kept = [];
            for (const line of lines) {
              try {
                const error = JSON.parse(line);
                if (error.file !== file && !error.file.includes(file) && !file.includes(error.file)) {
                  kept.push(error);
                }
              } catch {
              }
            }
            writeFileSync(filePath, kept.map((e) => JSON.stringify(e)).join("\n") + (kept.length > 0 ? "\n" : ""), "utf8");
          } catch {
          }
        }
      }
    };
    __name8(createErrorCacheService, "createErrorCacheService");
  }
});
function createGitContextService(workspaceRoot) {
  return new GitContextService(workspaceRoot);
}
__name(createGitContextService, "createGitContextService");
var MAX_RECENT_COMMITS;
var GIT_TIMEOUT_MS;
var GitContextService;
var init_git_context_service = __esm2({
  "src/services/git-context-service.ts"() {
    MAX_RECENT_COMMITS = 5;
    GIT_TIMEOUT_MS = 5e3;
    GitContextService = class {
      static {
        __name(this, "GitContextService");
      }
      static {
        __name8(this, "GitContextService");
      }
      workspaceRoot;
      constructor(workspaceRoot) {
        this.workspaceRoot = workspaceRoot;
      }
      /**
      * Get complete git context for planned files
      *
      * Called by begin_task to surface git change context.
      */
      async getContext(plannedFiles) {
        try {
          const branch = this.getBranchInfo();
          const uncommittedChanges = this.getUncommittedChanges();
          const stagedChanges = this.getStagedChanges();
          const recentCommits = this.getRecentCommits(plannedFiles);
          const fileHistory = this.getFileHistory(plannedFiles, uncommittedChanges);
          return {
            branch,
            uncommittedChanges,
            stagedChanges,
            recentCommits,
            fileHistory
          };
        } catch {
          return this.emptyContext();
        }
      }
      /**
      * Check if workspace is a git repository
      */
      async isGitRepo() {
        try {
          this.git("rev-parse --git-dir");
          return true;
        } catch {
          return false;
        }
      }
      // ===========================================================================
      // Private Methods
      // ===========================================================================
      /**
      * Get branch information
      */
      getBranchInfo() {
        try {
          const current = this.git("rev-parse --abbrev-ref HEAD").trim();
          const upstream = this.gitSafe("rev-parse --abbrev-ref @{u}")?.trim();
          let ahead = 0;
          let behind = 0;
          if (upstream) {
            try {
              const counts = this.git(`rev-list --left-right --count ${current}...${upstream}`);
              const parts = counts.trim().split(/\s+/);
              if (parts.length >= 2) {
                ahead = Number.parseInt(parts[0], 10) || 0;
                behind = Number.parseInt(parts[1], 10) || 0;
              }
            } catch {
            }
          }
          return {
            current,
            upstream,
            ahead,
            behind
          };
        } catch {
          return {
            current: "",
            ahead: 0,
            behind: 0
          };
        }
      }
      /**
      * Get uncommitted changes in working tree
      */
      getUncommittedChanges() {
        try {
          const status = this.git("status --porcelain");
          const changes = [];
          for (const line of status.split("\n")) {
            if (!line.trim()) {
              continue;
            }
            const statusCode = line.substring(0, 2);
            const file = line.substring(3).trim();
            let status2;
            if (statusCode.includes("?")) {
              status2 = "?";
            } else if (statusCode.includes("A")) {
              status2 = "A";
            } else if (statusCode.includes("D")) {
              status2 = "D";
            } else if (statusCode.includes("R")) {
              status2 = "R";
            } else if (statusCode.includes("C")) {
              status2 = "C";
            } else if (statusCode.includes("U")) {
              status2 = "U";
            } else {
              status2 = "M";
            }
            changes.push({
              file,
              status: status2
            });
          }
          return changes;
        } catch {
          return [];
        }
      }
      /**
      * Get staged changes (ready to commit)
      */
      getStagedChanges() {
        try {
          const status = this.git("diff --cached --name-status");
          const changes = [];
          for (const line of status.split("\n")) {
            if (!line.trim()) {
              continue;
            }
            const parts = line.split("	");
            if (parts.length < 2) {
              continue;
            }
            const statusCode = parts[0].charAt(0);
            const file = parts[1].trim();
            if ([
              "A",
              "M",
              "D",
              "R"
            ].includes(statusCode)) {
              changes.push({
                file,
                status: statusCode
              });
            }
          }
          return changes;
        } catch {
          return [];
        }
      }
      /**
      * Get recent commits
      */
      getRecentCommits(plannedFiles) {
        try {
          const format = '"%H|%s|%an|%ar|%ct"';
          const log22 = this.git(`log -10 --pretty=format:${format} --name-only`);
          const commits = [];
          const entries = log22.split("\n\n").filter(Boolean);
          for (const entry of entries) {
            const lines = entry.split("\n");
            if (lines.length === 0) {
              continue;
            }
            const infoLine = lines[0].replace(/^"|"$/g, "");
            const parts = infoLine.split("|");
            if (parts.length < 5) {
              continue;
            }
            const [fullHash, message, author, date] = parts;
            const files = lines.slice(1).filter(Boolean);
            const affectsPlannedFiles = plannedFiles.length > 0 && files.some((f) => plannedFiles.some((pf) => f.includes(pf) || pf.includes(f)));
            commits.push({
              hash: fullHash.substring(0, 7),
              message,
              author,
              date,
              filesChanged: files.length,
              affectsPlannedFiles
            });
            if (commits.length >= MAX_RECENT_COMMITS) {
              break;
            }
          }
          return commits;
        } catch {
          return [];
        }
      }
      /**
      * Get file history for planned files
      */
      getFileHistory(files, uncommittedChanges) {
        const history = {};
        for (const file of files) {
          try {
            const lastCommit = this.gitSafe(`log -1 --pretty=format:"%H" -- "${file}"`);
            const lastModified = this.gitSafe(`log -1 --pretty=format:"%ar" -- "${file}"`);
            const isModifiedByUser = uncommittedChanges.some((c) => c.file === file || c.file.includes(file) || file.includes(c.file));
            history[file] = {
              lastModified: lastModified?.trim().replace(/^"|"$/g, "") || "unknown",
              lastCommit: lastCommit?.trim().replace(/^"|"$/g, "").substring(0, 7) || "none",
              modifiedByUser: isModifiedByUser
            };
          } catch {
            history[file] = {
              lastModified: "unknown",
              lastCommit: "none",
              modifiedByUser: false
            };
          }
        }
        return history;
      }
      /**
      * Execute git command and return output (P0 security: uses execa)
      */
      git(cmd) {
        const args = cmd.split(" ");
        const { stdout } = execaSync("git", args, {
          cwd: this.workspaceRoot,
          encoding: "utf8",
          timeout: GIT_TIMEOUT_MS,
          shell: false,
          stdio: [
            "pipe",
            "pipe",
            "pipe"
          ]
        });
        return stdout;
      }
      /**
      * Execute git command, returning null on failure
      */
      gitSafe(cmd) {
        try {
          return this.git(cmd);
        } catch {
          return null;
        }
      }
      /**
      * Return empty context structure
      */
      emptyContext() {
        return {
          branch: {
            current: "",
            ahead: 0,
            behind: 0
          },
          uncommittedChanges: [],
          stagedChanges: [],
          recentCommits: [],
          fileHistory: {}
        };
      }
    };
    __name8(createGitContextService, "createGitContextService");
  }
});
function getTestCoverageService(workspaceRoot) {
  if (!services2.has(workspaceRoot)) {
    services2.set(workspaceRoot, new TestCoverageService(workspaceRoot));
  }
  return services2.get(workspaceRoot);
}
__name(getTestCoverageService, "getTestCoverageService");
var TestCoverageService;
var services2;
var init_test_coverage_service = __esm2({
  "src/services/test-coverage-service.ts"() {
    TestCoverageService = class {
      static {
        __name(this, "TestCoverageService");
      }
      static {
        __name8(this, "TestCoverageService");
      }
      workspaceRoot;
      cacheDir;
      constructor(workspaceRoot) {
        this.workspaceRoot = workspaceRoot;
        this.cacheDir = join(workspaceRoot, ".vreko", "coverage");
      }
      /**
      * Get test coverage context for planned files
      * Main entry point for begin_task integration
      */
      getContextForFiles(files) {
        const coverageData = this.loadCoverageData();
        const testMap = this.buildTestMap();
        const filesContext = {};
        let filesWithTests = 0;
        let totalCoverage = 0;
        let filesWithCoverage = 0;
        for (const file of files) {
          const relPath = this.toRelative(file);
          const testFiles = testMap[relPath] || this.inferTestFiles(file);
          const coverage = coverageData[relPath];
          const hasTests = testFiles.length > 0;
          if (hasTests) {
            filesWithTests++;
          }
          filesContext[file] = {
            hasTests,
            testFiles,
            coverage: coverage ? {
              lines: coverage.lines.pct,
              functions: coverage.functions.pct,
              branches: coverage.branches.pct
            } : void 0
          };
          if (coverage) {
            totalCoverage += coverage.lines.pct;
            filesWithCoverage++;
          }
        }
        return {
          files: filesContext,
          summary: {
            totalFiles: files.length,
            filesWithTests,
            averageCoverage: filesWithCoverage > 0 ? totalCoverage / filesWithCoverage : 0
          }
        };
      }
      /**
      * Get files with low or no test coverage
      * Useful for review and refactor intents
      */
      getLowCoverageFiles(threshold = 50) {
        const coverageData = this.loadCoverageData();
        const lowCoverage = [];
        for (const [file, coverage] of Object.entries(coverageData)) {
          if (file === "total") {
            continue;
          }
          if (coverage.lines.pct < threshold) {
            lowCoverage.push(file);
          }
        }
        return lowCoverage.sort((a, b) => {
          const aCov = coverageData[a]?.lines.pct ?? 0;
          const bCov = coverageData[b]?.lines.pct ?? 0;
          return aCov - bCov;
        });
      }
      /**
      * Get overall project coverage summary
      */
      getProjectCoverage() {
        const coverageData = this.loadCoverageData();
        if (coverageData.total) {
          return {
            lines: coverageData.total.lines.pct,
            functions: coverageData.total.functions.pct,
            branches: coverageData.total.branches.pct
          };
        }
        return null;
      }
      /**
      * Check if a file has tests
      */
      hasTests(file) {
        const testMap = this.buildTestMap();
        const relPath = this.toRelative(file);
        return testMap[relPath]?.length > 0 || this.inferTestFiles(file).length > 0;
      }
      /**
      * Get test files for a source file
      */
      getTestFiles(file) {
        const testMap = this.buildTestMap();
        const relPath = this.toRelative(file);
        return testMap[relPath] || this.inferTestFiles(file);
      }
      /**
      * Called after test runs to update cache
      */
      updateFromTestRun(testResults) {
        this.ensureCacheDir();
        const cachePath = join(this.cacheDir, "last-run.json");
        const data = {
          timestamp: Date.now(),
          results: testResults
        };
        writeFileSync(cachePath, JSON.stringify(data, null, 2));
      }
      /**
      * Get last test run info
      */
      getLastTestRun() {
        const cachePath = join(this.cacheDir, "last-run.json");
        if (!existsSync(cachePath)) {
          return null;
        }
        try {
          return JSON.parse(readFileSync(cachePath, "utf8"));
        } catch {
          return null;
        }
      }
      /**
      * Build or update test→source mapping from vitest config
      * Called periodically or after test file changes
      */
      updateTestMap(mappings) {
        this.ensureCacheDir();
        const cachePath = join(this.cacheDir, "test-map.json");
        writeFileSync(cachePath, JSON.stringify(mappings, null, 2));
      }
      // =========================================================================
      // Private Methods
      // =========================================================================
      /**
      * Load coverage data from vitest output
      */
      loadCoverageData() {
        const locations = [
          join(this.workspaceRoot, "coverage", "coverage-summary.json"),
          join(this.workspaceRoot, ".vitest", "coverage", "coverage-summary.json"),
          join(this.workspaceRoot, ".nyc_output", "coverage-summary.json")
        ];
        for (const loc of locations) {
          if (existsSync(loc)) {
            try {
              return JSON.parse(readFileSync(loc, "utf8"));
            } catch {
            }
          }
        }
        return {
          total: {
            lines: {
              pct: 0
            },
            functions: {
              pct: 0
            },
            branches: {
              pct: 0
            }
          }
        };
      }
      /**
      * Build test map from cache or config
      */
      buildTestMap() {
        const cachePath = join(this.cacheDir, "test-map.json");
        if (existsSync(cachePath)) {
          try {
            return JSON.parse(readFileSync(cachePath, "utf8"));
          } catch {
          }
        }
        return {};
      }
      /**
      * Infer test files from naming conventions
      * Used when no explicit test map exists
      */
      inferTestFiles(file) {
        const dir = dirname(file);
        const ext = file.endsWith(".tsx") ? ".tsx" : ".ts";
        const base = basename(file, ext);
        if (base.endsWith(".test") || base.endsWith(".spec")) {
          return [];
        }
        const candidates = [
          // Same directory conventions
          join(dir, `${base}.test${ext}`),
          join(dir, `${base}.spec${ext}`),
          // __tests__ directory
          join(dir, "__tests__", `${base}.test${ext}`),
          join(dir, "__tests__", `${base}.spec${ext}`),
          // test directory parallel to src
          `${dir.replace("/src/", "/test/")}/${base}.test${ext}`,
          `${dir.replace("/src/", "/tests/")}/${base}.test${ext}`,
          // apps/package test directories
          `${dir.replace(/\/src\//, "/__tests__/")}/${base}.test${ext}`
        ];
        return candidates.filter((c) => existsSync(c));
      }
      /**
      * Convert absolute path to relative
      */
      toRelative(file) {
        if (file.startsWith(this.workspaceRoot)) {
          return relative(this.workspaceRoot, file);
        }
        return file;
      }
      /**
      * Ensure cache directory exists
      */
      ensureCacheDir() {
        if (!existsSync(this.cacheDir)) {
          mkdirSync(this.cacheDir, {
            recursive: true
          });
        }
      }
    };
    services2 = /* @__PURE__ */ new Map();
    __name8(getTestCoverageService, "getTestCoverageService");
  }
});
function generateTaskId() {
  const timestamp = Date.now().toString(36);
  const random = Math.random().toString(36).substring(2, 8);
  return `task_${timestamp}_${random}`;
}
__name(generateTaskId, "generateTaskId");
function extractKeywords2(taskDescription) {
  const stopWords = /* @__PURE__ */ new Set([
    "a",
    "an",
    "the",
    "to",
    "for",
    "of",
    "in",
    "on",
    "at",
    "and",
    "or",
    "but",
    "is",
    "are",
    "was",
    "were",
    "be",
    "been",
    "being",
    "have",
    "has",
    "had",
    "do",
    "does",
    "did",
    "will",
    "would",
    "could",
    "should",
    "may",
    "might",
    "i",
    "we",
    "you",
    "they",
    "it",
    "this",
    "that",
    "with",
    "from",
    "by",
    "add",
    "update",
    "fix",
    "implement",
    "create",
    "make",
    "change",
    "modify"
  ]);
  const words = taskDescription.toLowerCase().replace(/[^\w\s-]/g, " ").split(/\s+/).filter((word) => word.length > 2 && !stopWords.has(word));
  return [
    ...new Set(words)
  ].slice(0, 5);
}
__name(extractKeywords2, "extractKeywords");
function formatDuration4(ms) {
  const minutes = Math.floor(ms / 6e4);
  const hours = Math.floor(minutes / 60);
  if (hours > 0) {
    const remainingMinutes = minutes % 60;
    return `${hours}h ${remainingMinutes}m`;
  }
  if (minutes > 0) {
    return `${minutes} minute${minutes !== 1 ? "s" : ""}`;
  }
  return "less than a minute";
}
__name(formatDuration4, "formatDuration");
var init_utils2 = __esm2({
  "src/session/utils.ts"() {
    __name8(generateTaskId, "generateTaskId");
    __name8(extractKeywords2, "extractKeywords");
    __name8(formatDuration4, "formatDuration");
  }
});
function getIntelligence2(workspaceRoot) {
  return {
    getContext: /* @__PURE__ */ __name8((options) => getContextViaDaemon(workspaceRoot, options), "getContext"),
    getVitals: /* @__PURE__ */ __name8(() => getVitalsViaDaemon(workspaceRoot), "getVitals"),
    initialize: /* @__PURE__ */ __name8(async () => {
    }, "initialize"),
    startSession: /* @__PURE__ */ __name8(async () => {
    }, "startSession"),
    reportViolation: /* @__PURE__ */ __name8(async (violation) => {
      const key = `${workspaceRoot}::${violation.type}::${violation.file}`;
      const prev = violationCounts2.get(key) ?? 0;
      const count = prev + 1;
      violationCounts2.set(key, count);
      try {
        const { addLearningViaDaemon: addLearningViaDaemon2 } = await Promise.resolve().then(() => (init_local_service_adapter(), local_service_adapter_exports));
        await addLearningViaDaemon2(workspaceRoot, {
          type: "violation",
          trigger: violation.type,
          action: violation.message,
          source: violation.file
        });
      } catch {
      }
      return {
        id: `violation-${violation.type}-${violation.file}-${count}`,
        count,
        shouldPromote: count >= 3,
        shouldAutomate: count >= 5
      };
    }, "reportViolation"),
    checkPatterns: /* @__PURE__ */ __name8(async (_files, _options) => {
      const startTime = Date.now();
      const issues = [];
      return {
        overall: {
          score: 100,
          status: "healthy",
          passed: true,
          confidence: 0.9,
          totalIssues: 0
        },
        recommendation: "No issues found",
        layers: [
          {
            name: "patterns",
            layer: "patterns",
            score: 100,
            issues,
            passed: true,
            duration: Date.now() - startTime
          }
        ],
        focusPoints: [],
        issues,
        duration: Date.now() - startTime
      };
    }, "checkPatterns"),
    reviewWork: /* @__PURE__ */ __name8(async (options) => {
      const validation = await (await getIntelligence2(options.workspaceRoot)).checkPatterns([], {
        workspaceRoot: options.workspaceRoot
      });
      const readyToCommit = validation.overall.passed;
      return {
        issues: validation.issues,
        suggestions: [],
        suggestedLearnings: [],
        readyToCommit,
        suggestedCommitMessage: readyToCommit ? "Ready to commit" : "Fix issues before committing",
        validation
      };
    }, "reviewWork"),
    getFileModifications: /* @__PURE__ */ __name8(async (files, _workspaceRoot) => {
      return files.map((f) => ({
        file: f,
        modifications: []
      }));
    }, "getFileModifications"),
    getVitalsSnapshot: /* @__PURE__ */ __name8(async (_workspaceRoot) => {
      const result7 = await getVitalsViaDaemon(workspaceRoot);
      if (result7.success && result7.vitals) {
        return {
          pressure: result7.vitals.pressure,
          snapshotCount: result7.vitals.snapshotCount,
          pulse: result7.vitals.pressure,
          trajectory: result7.vitals.agentGuidance ? "stable" : "unknown"
        };
      }
      return {
        pressure: 0,
        snapshotCount: 0,
        pulse: 0,
        trajectory: "unknown"
      };
    }, "getVitalsSnapshot")
  };
}
__name(getIntelligence2, "getIntelligence");
async function initializeIntelligence(workspaceRoot) {
  const result7 = await getIntelligenceViaDaemon(workspaceRoot);
  if (!result7.success) ;
}
__name(initializeIntelligence, "initializeIntelligence");
async function disposeIntelligence(workspaceRoot) {
  const { disposeServiceConnection: disposeServiceConnection2 } = await Promise.resolve().then(() => (init_local_service_adapter(), local_service_adapter_exports));
  await disposeServiceConnection2(workspaceRoot);
}
__name(disposeIntelligence, "disposeIntelligence");
async function disposeAllIntelligence() {
  const { disposeAllServiceConnections: disposeAllServiceConnections2 } = await Promise.resolve().then(() => (init_local_service_adapter(), local_service_adapter_exports));
  await disposeAllServiceConnections2();
}
__name(disposeAllIntelligence, "disposeAllIntelligence");
function getActiveInstanceCount() {
  return 0;
}
__name(getActiveInstanceCount, "getActiveInstanceCount");
function getFacades() {
  return {
    validation: {
      checkPatterns: /* @__PURE__ */ __name8(async (_root, files) => {
        const intel = getIntelligence2(_root);
        return intel.checkPatterns(files, {
          workspaceRoot: _root
        });
      }, "checkPatterns"),
      reviewWork: /* @__PURE__ */ __name8(async (options) => {
        const intel = getIntelligence2(options.workspaceRoot);
        return intel.reviewWork(options);
      }, "reviewWork")
    },
    learning: {
      getLearnings: /* @__PURE__ */ __name8(async () => [], "getLearnings")
    },
    storage: {
      getSnapshots: /* @__PURE__ */ __name8(async () => [], "getSnapshots")
    }
  };
}
__name(getFacades, "getFacades");
var violationCounts2;
var init_intelligence = __esm2({
  "src/facades/intelligence.ts"() {
    init_intelligence_adapter();
    violationCounts2 = /* @__PURE__ */ new Map();
    __name8(getIntelligence2, "getIntelligence");
    __name8(initializeIntelligence, "initializeIntelligence");
    __name8(disposeIntelligence, "disposeIntelligence");
    __name8(disposeAllIntelligence, "disposeAllIntelligence");
    __name8(getActiveInstanceCount, "getActiveInstanceCount");
    __name8(getFacades, "getFacades");
  }
});
function checkContextStaleness(workspaceRoot) {
  const ctxPath = join(workspaceRoot, ".vreko", "ctx", "context.json");
  if (!existsSync(ctxPath)) {
    return {
      exists: false,
      isStale: false,
      daysSinceScanned: 0,
      threshold: DEFAULT_STALE_AFTER_DAYS
    };
  }
  try {
    const content = JSON.parse(readFileSync(ctxPath, "utf8"));
    const lastScanned = content.lastScanned;
    const staleAfterDays = content.staleAfterDays || DEFAULT_STALE_AFTER_DAYS;
    if (!lastScanned) {
      return {
        exists: true,
        isStale: false,
        daysSinceScanned: 0,
        threshold: staleAfterDays
      };
    }
    const scanTime = new Date(lastScanned).getTime();
    const daysSinceScanned = Math.floor((Date.now() - scanTime) / (1e3 * 60 * 60 * 24));
    const isStale = daysSinceScanned > staleAfterDays;
    return {
      exists: true,
      isStale,
      daysSinceScanned,
      threshold: staleAfterDays,
      lastScanned
    };
  } catch {
    return {
      exists: false,
      isStale: false,
      daysSinceScanned: 0,
      threshold: DEFAULT_STALE_AFTER_DAYS
    };
  }
}
__name(checkContextStaleness, "checkContextStaleness");
function rebuildContext(workspaceRoot) {
  const ctxPath = join(workspaceRoot, ".vreko", "ctx", "context.json");
  try {
    if (!existsSync(ctxPath)) {
      return {
        success: false,
        error: "Context file does not exist"
      };
    }
    const content = JSON.parse(readFileSync(ctxPath, "utf8"));
    content.lastScanned = /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString();
    writeFileSync(ctxPath, JSON.stringify(content, null, 2));
    return {
      success: true
    };
  } catch (error) {
    return {
      success: false,
      error: error instanceof Error ? error.message : String(error)
    };
  }
}
__name(rebuildContext, "rebuildContext");
function handleStaleContext(workspaceRoot) {
  const warnings = [];
  const staleness = checkContextStaleness(workspaceRoot);
  if (!staleness.exists || !staleness.isStale) {
    return warnings;
  }
  const rebuild = rebuildContext(workspaceRoot);
  if (rebuild.success) {
    warnings.push({
      type: "context_rebuilt",
      message: `Context was stale (${staleness.daysSinceScanned} days since last scan, threshold: ${staleness.threshold} days). Auto-rebuilt.`,
      action: "auto_rebuilt",
      daysSinceScanned: staleness.daysSinceScanned,
      threshold: staleness.threshold
    });
  } else {
    warnings.push({
      type: "context_stale",
      message: `Context is stale (${staleness.daysSinceScanned} days since last scan, threshold: ${staleness.threshold} days). Auto-rebuild failed: ${rebuild.error}`,
      action: "manual_scan_recommended",
      daysSinceScanned: staleness.daysSinceScanned,
      threshold: staleness.threshold
    });
  }
  return warnings;
}
__name(handleStaleContext, "handleStaleContext");
function getConstraints(workspaceRoot) {
  const ctxPath = join(workspaceRoot, ".vreko", "ctx", "context.json");
  if (!existsSync(ctxPath)) {
    return [];
  }
  try {
    const content = JSON.parse(readFileSync(ctxPath, "utf8"));
    const constraints = [];
    if (content.constraints) {
      for (const [domain, domainConstraints] of Object.entries(content.constraints)) {
        for (const [name, constraint] of Object.entries(domainConstraints)) {
          const value = constraint.max || constraint.value;
          if (value !== void 0) {
            constraints.push({
              domain,
              name,
              value,
              description: constraint.description || ""
            });
          }
        }
      }
    }
    return constraints;
  } catch {
    return [];
  }
}
__name(getConstraints, "getConstraints");
var DEFAULT_STALE_AFTER_DAYS;
var init_context_manager = __esm2({
  "src/facades/begin-task/context-manager.ts"() {
    DEFAULT_STALE_AFTER_DAYS = 7;
    __name8(checkContextStaleness, "checkContextStaleness");
    __name8(rebuildContext, "rebuildContext");
    __name8(handleStaleContext, "handleStaleContext");
    __name8(getConstraints, "getConstraints");
  }
});
function getPriorityIndicator(learning) {
  if (learning.priority === "critical") {
    return "\u{1F6A8}";
  }
  if (learning.priority === "high" || learning.type === "pitfall") {
    return "\u26A0\uFE0F";
  }
  if (learning.type === "pattern") {
    return "\u{1F3AF}";
  }
  if (learning.type === "efficiency" || learning.type === "workflow") {
    return "\u{1F4A1}";
  }
  return "\u{1F4DA}";
}
__name(getPriorityIndicator, "getPriorityIndicator");
function formatCompactResult(output, gitContext) {
  const constraintMap = {};
  for (const c of (output.constraints || []).slice(0, 4)) {
    constraintMap[c.name] = String(c.value) + (c.name.includes("time") ? "ms" : c.name.includes("bundle") ? "MB" : "");
  }
  const learningStrings = (output.learnings || []).filter((l) => l != null && typeof l === "object" && "action" in l && typeof l.action === "string").slice(0, 2).map((l) => {
    const icon = getPriorityIndicator(l);
    return `${icon} ${l.action.substring(0, 100)}`;
  }).filter((s) => s.length > 0);
  const warnings = [];
  if (output.contextWarnings?.length) {
    warnings.push(output.contextWarnings[0].message);
  }
  if (output.proactive_guidance?.suggestions?.length) {
    const topSuggestion = output.proactive_guidance.suggestions[0];
    if (topSuggestion.priority <= 2) {
      warnings.push(topSuggestion.text);
    }
  }
  if (output.staticAnalysis?.skippedTests?.length) {
    warnings.push(`${output.staticAnalysis.skippedTests.length} skipped test(s)`);
  }
  let snapshotStatus;
  if (output.snapshot.created) {
    snapshotStatus = "created";
  } else if (output.snapshot.id) {
    snapshotStatus = "reused";
  } else {
    snapshotStatus = "skipped";
  }
  return {
    taskId: output.taskId,
    risk: output.riskAssessment.overallRisk,
    protection: 100,
    dirtyFiles: gitContext?.uncommittedChanges?.length ?? 0,
    constraints: constraintMap,
    learnings: learningStrings,
    warnings: warnings.slice(0, 2),
    snapshot: snapshotStatus
  };
}
__name(formatCompactResult, "formatCompactResult");
function formatCompactText(compact) {
  const lines = [];
  const snapshotIcon = compact.snapshot === "created" ? "\u{1F4F8}" : compact.snapshot === "reused" ? "\u267B\uFE0F" : "\u23ED\uFE0F";
  lines.push(`\u2713 ${compact.taskId} | risk:${compact.risk} | protection:${compact.protection}% | dirty:${compact.dirtyFiles} | ${snapshotIcon} ${compact.snapshot}`);
  if (Object.keys(compact.constraints).length > 0) {
    const constraintStr = Object.entries(compact.constraints).map(([k, v]) => `${k}<${v}`).join(", ");
    lines.push(`constraints: ${constraintStr}`);
  }
  if (compact.learnings.length > 0) {
    lines.push(`learnings: ${compact.learnings.join(" | ")}`);
  }
  if (compact.warnings.length > 0) {
    lines.push(`\u26A0\uFE0F ${compact.warnings.join(" | ")}`);
  }
  return lines.join("\n");
}
__name(formatCompactText, "formatCompactText");
var init_output_formatters = __esm2({
  "src/facades/begin-task/output-formatters.ts"() {
    __name8(getPriorityIndicator, "getPriorityIndicator");
    __name8(formatCompactResult, "formatCompactResult");
    __name8(formatCompactText, "formatCompactText");
  }
});
function assessRisk(files, _workspaceRoot) {
  const riskAreas = [];
  const recommendations = [];
  for (const file of files) {
    const lowerPath = file.toLowerCase();
    if (lowerPath.includes("auth") || lowerPath.includes("login")) {
      if (!riskAreas.includes("auth")) {
        riskAreas.push("auth");
      }
    }
    if (lowerPath.includes("payment") || lowerPath.includes("stripe")) {
      if (!riskAreas.includes("payment")) {
        riskAreas.push("payment");
      }
    }
    if (lowerPath.includes("database") || lowerPath.includes("migration")) {
      if (!riskAreas.includes("database")) {
        riskAreas.push("database");
      }
    }
    if (lowerPath.includes("config") || lowerPath.includes(".env")) {
      if (!riskAreas.includes("config")) {
        riskAreas.push("config");
      }
    }
    if (lowerPath.includes("api") || lowerPath.includes("route")) {
      if (!riskAreas.includes("api")) {
        riskAreas.push("api");
      }
    }
    if (lowerPath.includes("security") || lowerPath.includes("crypto")) {
      if (!riskAreas.includes("security")) {
        riskAreas.push("security");
      }
    }
  }
  const criticalAreas = [
    "auth",
    "payment",
    "security"
  ];
  const hasCritical = riskAreas.some((a) => criticalAreas.includes(a));
  const overallRisk = hasCritical ? "high" : riskAreas.length >= 2 ? "medium" : riskAreas.length > 0 ? "medium" : "low";
  if (riskAreas.includes("auth")) {
    recommendations.push("Test with both valid and invalid credentials");
  }
  if (riskAreas.includes("payment")) {
    recommendations.push("Use test mode/sandbox for payment testing");
  }
  if (riskAreas.includes("database")) {
    recommendations.push("Verify migrations can be rolled back");
  }
  if (riskAreas.includes("config")) {
    recommendations.push("Ensure secrets are not committed");
  }
  if (files.length >= 5) {
    recommendations.push("Consider breaking into smaller focused changes");
  }
  return {
    overallRisk,
    riskAreas,
    recommendations
  };
}
__name(assessRisk, "assessRisk");
function shouldAutoSnapshot(risk, files) {
  if (risk.overallRisk === "high") {
    return true;
  }
  const criticalAreas = [
    "auth",
    "payment",
    "database",
    "security"
  ];
  if (risk.riskAreas.some((a) => criticalAreas.includes(a))) {
    return true;
  }
  if (files && files.length >= 3) {
    return true;
  }
  if (risk.overallRisk === "medium" && files?.some((f) => f.includes("config") || f.endsWith(".env"))) {
    return true;
  }
  return false;
}
__name(shouldAutoSnapshot, "shouldAutoSnapshot");
function generateNextActions(risk, _learnings, staticAnalysis) {
  const actions = [];
  if (staticAnalysis?.skippedTests && staticAnalysis.skippedTests.length > 0) {
    actions.push({
      tool: "enable_skipped_tests",
      priority: 1,
      reason: `${staticAnalysis.skippedTests.length} skipped test(s) may be ready to enable`
    });
  }
  actions.push({
    tool: "quick_check",
    priority: 2,
    reason: "Validate changes after implementation"
  });
  if (risk.overallRisk === "high") {
    actions.push({
      tool: "what_changed",
      priority: 3,
      reason: "Track changes in high-risk area"
    });
  }
  actions.push({
    tool: "review_work",
    priority: 4,
    reason: "Review before committing"
  });
  return actions;
}
__name(generateNextActions, "generateNextActions");
var init_risk_assessment = __esm2({
  "src/facades/begin-task/risk-assessment.ts"() {
    __name8(assessRisk, "assessRisk");
    __name8(shouldAutoSnapshot, "shouldAutoSnapshot");
    __name8(generateNextActions, "generateNextActions");
  }
});
async function runStaticAnalysis(files, workspaceRoot) {
  const startTime = Date.now();
  const result7 = {
    skippedTests: [],
    success: true,
    duration: 0,
    errors: []
  };
  const testFiles = files.filter((f) => f.includes(".test.") || f.includes(".spec.") || f.includes("__tests__"));
  if (testFiles.length === 0) {
    result7.duration = Date.now() - startTime;
    return result7;
  }
  try {
    const fileMap = /* @__PURE__ */ new Map();
    for (const filePath of testFiles) {
      const fullPath = filePath.startsWith("/") ? filePath : join(workspaceRoot, filePath);
      if (existsSync(fullPath)) {
        try {
          const content = readFileSync(fullPath, "utf8");
          fileMap.set(filePath, content);
        } catch {
        }
      }
    }
    if (fileMap.size === 0) {
      result7.duration = Date.now() - startTime;
      return result7;
    }
    const { analyzeSkippedTests } = await import('./analysis-ABEO6RTN.js');
    const analysisResults = analyzeSkippedTests(fileMap);
    for (const analysisResult of analysisResults) {
      if (!analysisResult.parsed && analysisResult.error) {
        result7.errors.push(`Parse error in ${analysisResult.file}: ${analysisResult.error}`);
      }
      for (const skipped of analysisResult.skipped) {
        result7.skippedTests.push({
          file: skipped.file,
          type: skipped.type,
          name: skipped.name,
          line: skipped.line
        });
      }
    }
  } catch (error) {
    result7.errors.push(`Static analysis unavailable: ${error instanceof Error ? error.message : String(error)}`);
  }
  result7.duration = Date.now() - startTime;
  result7.success = result7.errors.length === 0;
  return result7;
}
__name(runStaticAnalysis, "runStaticAnalysis");
async function generateProactiveGuidance(files, _workspaceRoot) {
  const guidance = {
    summary: files.length > 0 ? `Analyzing ${files.length} file${files.length > 1 ? "s" : ""}` : "No files targeted",
    suggestions: []
  };
  return guidance;
}
__name(generateProactiveGuidance, "generateProactiveGuidance");
var init_static_analysis = __esm2({
  "src/facades/begin-task/static-analysis.ts"() {
    __name8(runStaticAnalysis, "runStaticAnalysis");
    __name8(generateProactiveGuidance, "generateProactiveGuidance");
  }
});
function inferIntent(task) {
  const lower = task.toLowerCase();
  if (lower.includes("fix") || lower.includes("bug") || lower.includes("error") || lower.includes("debug") || lower.includes("broken") || lower.includes("failing")) {
    return "debug";
  }
  if (lower.includes("refactor") || lower.includes("clean") || lower.includes("rename") || lower.includes("move") || lower.includes("extract") || lower.includes("consolidate")) {
    return "refactor";
  }
  if (lower.includes("review") || lower.includes("check") || lower.includes("audit") || lower.includes("verify") || lower.includes("validate")) {
    return "review";
  }
  if (lower.includes("explore") || lower.includes("understand") || lower.includes("how does") || lower.includes("what is") || lower.includes("where is") || lower.includes("find")) {
    return "explore";
  }
  return "implement";
}
__name(inferIntent, "inferIntent");
function getIntentContextHints(intent) {
  return INTENT_CONTEXT_CONFIG[intent];
}
__name(getIntentContextHints, "getIntentContextHints");
var INTENT_CONTEXT_CONFIG;
var init_types = __esm2({
  "src/facades/begin-task/types.ts"() {
    INTENT_CONTEXT_CONFIG = {
      implement: {
        prioritize: [
          "contracts",
          "patterns",
          "tests"
        ],
        include: [
          "architecture",
          "constraints"
        ],
        exclude: [
          "violations"
        ]
      },
      debug: {
        prioritize: [
          "violations",
          "failures",
          "history"
        ],
        include: [
          "tests",
          "patterns"
        ],
        exclude: []
      },
      refactor: {
        prioritize: [
          "canonical",
          "patterns",
          "duplicates"
        ],
        include: [
          "architecture",
          "migrations"
        ],
        exclude: []
      },
      review: {
        prioritize: [
          "checklist",
          "risks",
          "coverage"
        ],
        include: [
          "constraints",
          "violations"
        ],
        exclude: []
      },
      explore: {
        prioritize: [
          "architecture",
          "genealogy",
          "epochs"
        ],
        include: [
          "history",
          "decisions"
        ],
        exclude: [
          "violations"
        ]
      }
    };
    __name8(inferIntent, "inferIntent");
    __name8(getIntentContextHints, "getIntentContextHints");
  }
});
function result2(text2, isError = false) {
  return {
    content: [
      {
        type: "text",
        text: text2
      }
    ],
    isError
  };
}
__name(result2, "result2");
var handleBeginTask;
var init_begin_task = __esm2({
  "src/facades/begin-task/index.ts"() {
    init_session_adapter();
    init_context();
    init_local_service_adapter();
    init_dependency_graph_service();
    init_error_cache_service();
    init_git_context_service();
    init_hybrid_retrieval_service();
    init_test_coverage_service();
    init_utils2();
    init_learning_sync();
    init_intelligence();
    init_context_manager();
    init_output_formatters();
    init_risk_assessment();
    init_static_analysis();
    init_types();
    __name8(result2, "result");
    handleBeginTask = /* @__PURE__ */ __name8(async (args, context) => {
      const input = args;
      const task = input.task;
      const files = input.files;
      const providedKeywords = input.keywords;
      const skipSnapshot = input.skipSnapshot;
      const compact = input.compact !== false;
      const intent = input.intent ?? inferIntent(task || "");
      const intentHints = getIntentContextHints(intent);
      if (!task) {
        return result2(JSON.stringify({
          error: "E102_MISSING_PARAM",
          message: "Required parameter 'task' is missing"
        }), true);
      }
      const workspaceRoot = context.workspaceRoot;
      const contextWarnings = handleStaleContext(workspaceRoot);
      const keywords = providedKeywords ?? extractKeywords2(task);
      const daemonResult = await beginTaskViaService(workspaceRoot, task, files, keywords);
      if (daemonResult) {
        const observations2 = (await drainPendingObservationsAsync(workspaceRoot)).map((o) => ({
          type: o.type,
          message: o.message
        }));
        let lastKnownErrors2;
        if (files && files.length > 0) {
          try {
            const errorCacheService = createErrorCacheService(workspaceRoot);
            const cachedErrors = errorCacheService.getErrorsForFiles(files);
            if (cachedErrors.length > 0) {
              lastKnownErrors2 = {
                typescript: cachedErrors.filter((e) => e.source === "typescript").map((e) => ({
                  ...e,
                  age: e.age
                })),
                tests: cachedErrors.filter((e) => e.source === "test").map((e) => ({
                  ...e,
                  age: e.age
                })),
                lintErrors: cachedErrors.filter((e) => e.source === "lint").map((e) => ({
                  ...e,
                  age: e.age
                }))
              };
            }
          } catch {
          }
        }
        let gitContext2;
        try {
          const gitContextService = createGitContextService(workspaceRoot);
          gitContext2 = await gitContextService.getContext(files || []);
          if (!gitContext2.branch.current && gitContext2.uncommittedChanges.length === 0 && gitContext2.recentCommits.length === 0) {
            gitContext2 = void 0;
          }
        } catch {
        }
        const staticAnalysis2 = files?.length ? await runStaticAnalysis(files, workspaceRoot) : void 0;
        let dependencyContext2;
        if (files?.length) {
          try {
            const depGraphService = getDependencyGraphService(workspaceRoot);
            dependencyContext2 = await depGraphService.getContextForFiles(files);
            if (Object.keys(dependencyContext2.planned).length === 0 && dependencyContext2.circular.length === 0 && dependencyContext2.suggestions.length === 0) {
              dependencyContext2 = void 0;
            }
          } catch {
          }
        }
        let testCoverage2;
        if (files?.length) {
          try {
            const coverageService = getTestCoverageService(workspaceRoot);
            testCoverage2 = coverageService.getContextForFiles(files);
            if (testCoverage2.summary.filesWithTests === 0 && testCoverage2.summary.averageCoverage === 0) {
              testCoverage2 = void 0;
            }
          } catch {
          }
        }
        const proactive_guidance2 = await generateProactiveGuidance(files || []);
        const output2 = {
          taskId: daemonResult.taskId,
          intent,
          intentHints,
          snapshot: daemonResult.snapshot,
          patterns: daemonResult.patterns || [],
          constraints: daemonResult.constraints || [],
          learnings: daemonResult.learnings || [],
          observations: observations2,
          riskAssessment: daemonResult.riskAssessment,
          nextActions: daemonResult.nextActions || [],
          staticAnalysis: staticAnalysis2,
          lastKnownErrors: lastKnownErrors2,
          gitContext: gitContext2,
          dependencyContext: dependencyContext2,
          testCoverage: testCoverage2,
          proactive_guidance: proactive_guidance2,
          contextWarnings: contextWarnings.length > 0 ? contextWarnings : void 0
        };
        if (compact) {
          return result2(formatCompactText(formatCompactResult(output2, gitContext2)));
        }
        const learningsCount = daemonResult.learnings?.length || 0;
        const daemonSnapshotCreated = daemonResult.snapshot?.created ?? (daemonResult.snapshotIds?.length ?? 0) > 0;
        const daemonSnapshotId = daemonResult.snapshot?.id ?? daemonResult.snapshotIds?.[0];
        const hint2 = daemonSnapshotCreated ? `Safety snapshot created via daemon: ${daemonSnapshotId}` : learningsCount > 0 ? `${learningsCount} relevant learning(s) found` : "Ready to start coding! (daemon-coordinated)";
        return result2(JSON.stringify({
          ...output2,
          message: `Task started: ${task}`,
          _hint: hint2,
          _source: "daemon"
        }, null, 2));
      }
      const riskAssessment = files ? assessRisk(files) : {
        overallRisk: "low",
        riskAreas: [],
        recommendations: []
      };
      let snapshot = {
        created: false
      };
      if (!skipSnapshot && shouldAutoSnapshot(riskAssessment, files) && files?.length) {
        const snapshotResult = await createSnapshotViaDaemon(workspaceRoot, files, {
          reason: `Pre-task: ${task}`,
          trigger: "ai-detection"
        });
        if (snapshotResult.success && snapshotResult.snapshotId) {
          snapshot = {
            created: true,
            id: snapshotResult.snapshotId,
            reason: `Auto-created: ${riskAssessment.overallRisk} risk task`
          };
          const state2 = await getSessionStateAsync(workspaceRoot);
          if (state2) {
            state2.stats.snapshotsCreated++;
          }
        } else if (snapshotResult.reused && snapshotResult.reusedSnapshotId) {
          snapshot = {
            created: false,
            id: snapshotResult.reusedSnapshotId,
            reason: snapshotResult.error
          };
        } else {
          snapshot = {
            created: false,
            reason: `Snapshot creation failed: ${snapshotResult.error}`
          };
        }
      }
      const learnings = [];
      try {
        const searchQuery = [
          task,
          ...keywords
        ].join(" ");
        const _classification = classifyQueryType(searchQuery);
        const hybridResult = await retrieveAdaptive(workspaceRoot, searchQuery);
        if ((hybridResult.results || []).length > 0) {
          const existingTriggers = new Set(learnings.map((l) => l.trigger));
          for (const result7 of (hybridResult.results || []).slice(0, 5)) {
            const chunkText = result7.text || result7.chunk?.chunk_text || "";
            if (!existingTriggers.has(chunkText)) {
              const sourceType = result7.source || result7.chunk?.source_type || "discovery";
              const learningType = sourceType === "pattern" ? "pattern" : sourceType === "violation" ? "pitfall" : sourceType === "adr" ? "architecture" : "discovery";
              learnings.push({
                type: learningType,
                trigger: chunkText,
                action: result7.text || chunkText,
                source: "hybrid-retrieval",
                relevanceScore: result7.score || result7.confidence || 0
              });
              existingTriggers.add(chunkText);
            }
          }
        }
      } catch {
      }
      const vectorRetrieval = context.services?.vectorRetrieval;
      if (vectorRetrieval && files && files.length > 0) {
        try {
          const vectorResult = await searchFileContext({
            retrieval: vectorRetrieval,
            workspaceId: context.workspaceId || workspaceRoot,
            filePath: files[0],
            taskDescription: task,
            limit: 5,
            threshold: 0.65
          });
          if (vectorResult.learnings.length > 0 && vectorResult.source === "vector") {
            const existingTriggers = new Set(learnings.map((l) => l.trigger));
            const vectorLearnings = formatVectorLearnings(vectorResult.learnings);
            for (const vl of vectorLearnings) {
              if (!existingTriggers.has(vl.content)) {
                learnings.push({
                  type: vl.type,
                  trigger: vl.content,
                  action: vl.content,
                  source: "vector",
                  relevanceScore: vl.score
                });
                existingTriggers.add(vl.content);
              }
            }
          }
        } catch {
        }
      }
      if (context.workspaceId) {
        try {
          const syncService = createLearningSync({
            workspaceRoot,
            workspaceId: context.workspaceId
          });
          const recommendations = await syncService.fetchRecommendations();
          if (recommendations.length > 0) {
            const existingTriggers = new Set(learnings.map((l) => l.trigger));
            for (const rec of recommendations.slice(0, 3)) {
              if (!existingTriggers.has(rec.title)) {
                const validTypes = [
                  ...LearningType.options
                ];
                const learningType = validTypes.includes(rec.type) ? rec.type : "pattern";
                learnings.push({
                  type: learningType,
                  trigger: rec.title,
                  action: rec.description,
                  source: "cross-workspace",
                  relevanceScore: rec.confidence
                });
                existingTriggers.add(rec.title);
              }
            }
            syncService.syncPending().catch(() => {
            });
          }
        } catch {
        }
      }
      let patterns = [];
      try {
        const intel = getIntelligence2(workspaceRoot);
        const contextResult = await intel.getContext({
          task,
          keywords
        });
        if (contextResult.patterns) {
          const patternLines = contextResult.patterns.split("\n").filter(Boolean);
          patterns = patternLines.slice(0, 5).map((line) => ({
            name: line.substring(0, 50),
            description: line
          }));
        }
      } catch {
      }
      const constraints = getConstraints(workspaceRoot);
      const staticAnalysis = files?.length ? await runStaticAnalysis(files, workspaceRoot) : void 0;
      let lastKnownErrors;
      if (files?.length) {
        try {
          const errorCacheService = createErrorCacheService(workspaceRoot);
          const cachedErrors = errorCacheService.getErrorsForFiles(files);
          if (cachedErrors.length > 0) {
            lastKnownErrors = {
              typescript: cachedErrors.filter((e) => e.source === "typescript").map((e) => ({
                ...e,
                age: e.age
              })),
              tests: cachedErrors.filter((e) => e.source === "test").map((e) => ({
                ...e,
                age: e.age
              })),
              lintErrors: cachedErrors.filter((e) => e.source === "lint").map((e) => ({
                ...e,
                age: e.age
              }))
            };
          }
        } catch {
        }
      }
      let gitContext;
      try {
        const gitContextService = createGitContextService(workspaceRoot);
        gitContext = await gitContextService.getContext(files || []);
        if (!gitContext.branch.current && gitContext.uncommittedChanges.length === 0 && gitContext.recentCommits.length === 0) {
          gitContext = void 0;
        }
      } catch {
      }
      let dependencyContext;
      if (files?.length) {
        try {
          const depGraphService = getDependencyGraphService(workspaceRoot);
          dependencyContext = await depGraphService.getContextForFiles(files);
          if (Object.keys(dependencyContext.planned).length === 0 && dependencyContext.circular.length === 0 && dependencyContext.suggestions.length === 0) {
            dependencyContext = void 0;
          }
        } catch {
        }
      }
      let testCoverage;
      if (files?.length) {
        try {
          const coverageService = getTestCoverageService(workspaceRoot);
          testCoverage = coverageService.getContextForFiles(files);
          if (testCoverage.summary.filesWithTests === 0 && testCoverage.summary.averageCoverage === 0) {
            testCoverage = void 0;
          }
        } catch {
        }
      }
      const observations = (await drainPendingObservationsAsync(workspaceRoot)).map((o) => ({
        type: o.type,
        message: o.message
      }));
      const currentTask = await startTaskAsync(workspaceRoot, {
        description: task,
        plannedFiles: files || [],
        snapshotId: snapshot.id,
        keywords
      });
      const taskId = currentTask?.id ?? `local_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 6)}`;
      const state = await getSessionStateAsync(workspaceRoot);
      if (state) {
        state.riskAreasTouched = riskAssessment.riskAreas;
      }
      try {
        const intel = getIntelligence2(workspaceRoot);
        await intel.startSession();
      } catch {
      }
      const nextActions = generateNextActions(riskAssessment, learnings, staticAnalysis);
      const proactive_guidance = await generateProactiveGuidance(files || []);
      const output = {
        taskId,
        intent,
        intentHints,
        snapshot,
        patterns,
        constraints,
        learnings,
        observations,
        riskAssessment,
        nextActions,
        staticAnalysis,
        lastKnownErrors,
        gitContext,
        dependencyContext,
        testCoverage,
        proactive_guidance,
        contextWarnings: contextWarnings.length > 0 ? contextWarnings : void 0
      };
      if (compact) {
        return result2(formatCompactText(formatCompactResult(output, gitContext)));
      }
      const skippedCount = staticAnalysis?.skippedTests?.length ?? 0;
      const knownErrorCount = (lastKnownErrors?.typescript?.length ?? 0) + (lastKnownErrors?.tests?.length ?? 0) + (lastKnownErrors?.lintErrors?.length ?? 0);
      const uncommittedCount = gitContext?.uncommittedChanges?.length ?? 0;
      let hint;
      if (contextWarnings.length > 0) {
        const rebuiltWarning = contextWarnings.find((w) => w.type === "context_rebuilt");
        hint = rebuiltWarning ? `\u{1F504} Context was stale and auto-rebuilt (${rebuiltWarning.daysSinceScanned} days old)` : `\u26A0\uFE0F Context is stale - consider running 'context op=scan'`;
      } else if (knownErrorCount > 0) {
        hint = `\u26A0\uFE0F ${knownErrorCount} known error(s) in planned files - check lastKnownErrors`;
      } else if (skippedCount > 0) {
        hint = `\u26A0\uFE0F ${skippedCount} skipped test(s) found - may need enabling`;
      } else if (uncommittedCount > 0) {
        hint = `\u{1F4DD} ${uncommittedCount} uncommitted change(s) in working tree`;
      } else if (snapshot.created) {
        hint = `Safety snapshot created: ${snapshot.id}`;
      } else if (learnings.length > 0) {
        hint = `${learnings.length} relevant learning(s) found`;
      } else {
        hint = "Ready to start coding!";
      }
      return result2(JSON.stringify({
        ...output,
        message: `Task started: ${task}`,
        _hint: hint
      }, null, 2));
    }, "handleBeginTask");
  }
});
var init_begin_task2 = __esm2({
  "src/facades/begin-task.ts"() {
    init_begin_task();
  }
});
function getSessionFileTracker(workspaceRoot, daemonClient) {
  if (!trackers.has(workspaceRoot)) {
    trackers.set(workspaceRoot, new SessionFileTracker(workspaceRoot, daemonClient));
  } else if (daemonClient) {
    trackers.get(workspaceRoot)?.setDaemonClient(daemonClient);
  }
  return trackers.get(workspaceRoot);
}
__name(getSessionFileTracker, "getSessionFileTracker");
var STATE_FILE;
var SessionFileTracker;
var trackers;
var init_session_file_tracker = __esm2({
  "src/services/session-file-tracker.ts"() {
    STATE_FILE = ".vreko/mcp/session-files.json";
    SessionFileTracker = class {
      static {
        __name(this, "SessionFileTracker");
      }
      static {
        __name8(this, "SessionFileTracker");
      }
      workspaceRoot;
      state;
      daemonClient = null;
      constructor(workspaceRoot, daemonClient) {
        this.workspaceRoot = workspaceRoot;
        this.daemonClient = daemonClient ?? null;
        this.state = this.loadState();
      }
      /**
      * Set the daemon IPC client (for wiring after construction)
      */
      setDaemonClient(client) {
        this.daemonClient = client;
      }
      /**
      * Start tracking for a new task
      */
      startTask(taskId, plannedFiles = []) {
        this.state = {
          taskId,
          taskStartedAt: Date.now(),
          files: /* @__PURE__ */ new Map()
        };
        for (const file of plannedFiles) {
          this.trackFile(file, "planned");
        }
        this.saveState();
      }
      /**
      * End tracking for current task
      */
      endTask() {
        const files = this.getTrackedFiles();
        this.state = {
          taskId: null,
          taskStartedAt: null,
          files: /* @__PURE__ */ new Map()
        };
        this.saveState();
        return files;
      }
      /**
      * Track a file
      */
      trackFile(filePath, source, linesChanged) {
        if (!this.state.taskId) {
          return;
        }
        const normalizedPath = filePath.startsWith(this.workspaceRoot) ? relative(this.workspaceRoot, filePath) : filePath;
        const now = Date.now();
        const existing = this.state.files.get(normalizedPath);
        if (existing) {
          existing.lastTouched = now;
          if (linesChanged !== void 0) {
            existing.linesChanged = (existing.linesChanged || 0) + linesChanged;
          }
          const sourcePriority = {
            edited: 4,
            validated: 3,
            mentioned: 2,
            planned: 1
          };
          if (sourcePriority[source] > sourcePriority[existing.source]) {
            existing.source = source;
          }
        } else {
          const fullPath = join(this.workspaceRoot, normalizedPath);
          let size;
          try {
            if (existsSync(fullPath)) {
              size = statSync(fullPath).size;
            }
          } catch {
          }
          this.state.files.set(normalizedPath, {
            path: normalizedPath,
            source,
            firstTracked: now,
            lastTouched: now,
            linesChanged,
            size
          });
        }
        this.saveState();
        this.pushFileChangeToDaemon(normalizedPath, source);
      }
      /**
      * Track multiple files at once
      */
      trackFiles(files, source) {
        for (const file of files) {
          this.trackFile(file, source);
        }
      }
      /**
      * Get all tracked files for current task
      */
      getTrackedFiles() {
        return Array.from(this.state.files.values());
      }
      /**
      * Get files changed since task start (for what_changed)
      */
      getChangedFiles() {
        const files = this.getTrackedFiles();
        return files.map((f) => ({
          file: f.path,
          type: this.determineChangeType(f),
          linesChanged: f.linesChanged || 0,
          aiAttributed: f.source === "edited" || f.source === "validated",
          timestamp: f.lastTouched
        }));
      }
      /**
      * Determine change type based on file state
      */
      determineChangeType(file) {
        const fullPath = join(this.workspaceRoot, file.path);
        if (!existsSync(fullPath)) {
          return "deleted";
        }
        return "modified";
      }
      /**
      * Check if currently tracking a task
      */
      hasActiveTask() {
        return this.state.taskId !== null;
      }
      /**
      * Get current task ID
      */
      getCurrentTaskId() {
        return this.state.taskId;
      }
      /**
      * Get task duration in ms
      */
      getTaskDuration() {
        if (!this.state.taskStartedAt) {
          return 0;
        }
        return Date.now() - this.state.taskStartedAt;
      }
      /**
      * Get stats for the current session
      */
      getStats() {
        const files = this.getTrackedFiles();
        return {
          filesTracked: files.length,
          totalLinesChanged: files.reduce((sum, f) => sum + (f.linesChanged || 0), 0),
          durationMs: this.getTaskDuration()
        };
      }
      // =========================================================================
      // Daemon IPC Integration (Phase 8-A)
      // =========================================================================
      /**
      * Push file change event to daemon via IPC
      */
      async pushFileChangeToDaemon(filePath, source) {
        if (!this.daemonClient) {
          return;
        }
        const event = {
          workspace: this.workspaceRoot,
          filePath,
          eventType: this.determineChangeType(this.state.files.get(filePath) ?? {
            path: filePath,
            source,
            firstTracked: Date.now(),
            lastTouched: Date.now()
          }),
          source,
          timestamp: Date.now()
        };
        try {
          await this.daemonClient.call("watch/file-changed", {
            workspace: event.workspace,
            filePath: event.filePath,
            eventType: event.eventType,
            source: event.source,
            timestamp: event.timestamp
          });
        } catch (_error) {
        }
      }
      // =========================================================================
      // Persistence
      // =========================================================================
      getStatePath() {
        return join(this.workspaceRoot, STATE_FILE);
      }
      loadState() {
        const statePath = this.getStatePath();
        try {
          if (existsSync(statePath)) {
            const data = JSON.parse(readFileSync(statePath, "utf8"));
            return {
              taskId: data.taskId || null,
              taskStartedAt: data.taskStartedAt || null,
              files: new Map(Object.entries(data.files || {}))
            };
          }
        } catch {
        }
        return {
          taskId: null,
          taskStartedAt: null,
          files: /* @__PURE__ */ new Map()
        };
      }
      saveState() {
        const statePath = this.getStatePath();
        try {
          const dir = dirname(statePath);
          if (!existsSync(dir)) {
            mkdirSync(dir, {
              recursive: true
            });
          }
          const data = {
            taskId: this.state.taskId,
            taskStartedAt: this.state.taskStartedAt,
            files: Object.fromEntries(this.state.files)
          };
          writeFileSync(statePath, JSON.stringify(data, null, 2));
        } catch {
        }
      }
    };
    trackers = /* @__PURE__ */ new Map();
    __name8(getSessionFileTracker, "getSessionFileTracker");
  }
});
function createSnapshotService(workspaceRoot) {
  if (!serviceCache.has(workspaceRoot)) {
    serviceCache.set(workspaceRoot, new SnapshotService(workspaceRoot));
  }
  const service = serviceCache.get(workspaceRoot);
  if (!service) {
    throw new Error(`Failed to create snapshot service for ${workspaceRoot}`);
  }
  return service;
}
__name(createSnapshotService, "createSnapshotService");
var SnapshotService;
var serviceCache;
var init_snapshot_service = __esm2({
  "src/services/snapshot-service.ts"() {
    init_local_service_adapter();
    init_validation();
    SnapshotService = class {
      static {
        __name(this, "SnapshotService");
      }
      static {
        __name8(this, "SnapshotService");
      }
      workspaceRoot;
      constructor(workspaceRoot) {
        this.workspaceRoot = workspaceRoot;
      }
      // =============================================================================
      // ISnapshotService Interface Implementation
      // =============================================================================
      /**
      * Create a new snapshot (ISnapshotService interface method)
      */
      async create(input) {
        try {
          const options = {
            description: input.description || input.taskDescription || "",
            trigger: input.trigger || "manual",
            skipDedup: input.skipDedup,
            dedupWindow: input.dedupWindow
          };
          const filesToSnapshot = input.files || [];
          if (filesToSnapshot.length === 0) {
            return {
              ok: false,
              error: "No files provided for snapshot",
              code: "SNAPSHOT_CREATE_FAILED"
            };
          }
          const result7 = await this.createFromFiles(filesToSnapshot, options);
          if (!result7.success || !result7.snapshot) {
            return {
              ok: false,
              error: result7.error || "Failed to create snapshot",
              code: "SNAPSHOT_CREATE_FAILED"
            };
          }
          const snapshot = {
            id: result7.snapshot.id,
            taskId: input.taskId,
            createdAt: new Date(result7.snapshot.createdAt),
            status: "pending",
            fileCount: result7.snapshot.fileCount,
            totalSize: result7.snapshot.totalSize
          };
          return {
            ok: true,
            data: snapshot
          };
        } catch (error) {
          return {
            ok: false,
            error: errorMessage(error),
            code: "SNAPSHOT_CREATE_ERROR"
          };
        }
      }
      /**
      * Get snapshot state (ISnapshotService interface method)
      */
      async getState(input) {
        try {
          const daemon = createServiceClient2(this.workspaceRoot);
          const result7 = await daemon.request("snapshot/get", {
            workspace: this.workspaceRoot,
            snapshotId: input.snapshotId
          });
          const vreko = result7?.snapshot;
          if (!vreko) {
            return {
              ok: false,
              error: `Snapshot not found: ${input.snapshotId}`,
              code: "SNAPSHOT_NOT_FOUND"
            };
          }
          const files = vreko.files.map((f) => ({
            path: f.path,
            hash: f.hash ?? "",
            size: f.size ?? 0
          }));
          const snapshotState = {
            snapshot: {
              id: vreko.snapshotId,
              taskId: input.snapshotId,
              createdAt: new Date(vreko.createdAt),
              status: "completed",
              fileCount: vreko.files.length,
              totalSize: vreko.totalSize ?? 0
            },
            files
          };
          return {
            ok: true,
            data: snapshotState
          };
        } catch (error) {
          return {
            ok: false,
            error: errorMessage(error),
            code: "SNAPSHOT_GET_ERROR"
          };
        }
      }
      /**
      * Finalize a snapshot (ISnapshotService interface method)
      */
      async finalize(input) {
        try {
          const daemon = createServiceClient2(this.workspaceRoot);
          const result7 = await daemon.request("snapshot/get", {
            workspace: this.workspaceRoot,
            snapshotId: input.snapshotId
          });
          if (!result7?.snapshot) {
            return {
              ok: false,
              error: `Snapshot not found: ${input.snapshotId}`,
              code: "SNAPSHOT_NOT_FOUND"
            };
          }
          return {
            ok: true,
            data: void 0
          };
        } catch (error) {
          return {
            ok: false,
            error: errorMessage(error),
            code: "SNAPSHOT_FINALIZE_ERROR"
          };
        }
      }
      // =============================================================================
      // Existing Methods (Backwards Compatibility)
      // =============================================================================
      /**
      * Create a snapshot from file paths with optional deduplication
      *
      * @example
      * ```typescript
      * const service = new SnapshotService(workspaceRoot);
      * const result = await service.createFromFiles(
      *   ["src/index.ts", "src/utils.ts"],
      *   { description: "Pre-refactor", trigger: "ai-detection" }
      * );
      * if (result.success) {
      *   logger.debug(...);
      * }
      * ```
      */
      async createFromFiles(files, options) {
        if (!files || files.length === 0) {
          return {
            success: false,
            error: "No files provided"
          };
        }
        const pathValidation = validateFilePaths(files, this.workspaceRoot);
        if (!pathValidation.valid) {
          return {
            success: false,
            error: `Invalid path: ${pathValidation.invalidPath} - ${pathValidation.error}`
          };
        }
        try {
          const daemonResult = await createSnapshotViaDaemon(this.workspaceRoot, files, {
            reason: options.description,
            trigger: options.trigger
          });
          if (!daemonResult.success || !daemonResult.snapshotId) {
            return {
              success: false,
              error: daemonResult.error || "Daemon failed to create snapshot"
            };
          }
          void notifySnapshotCreatedViaService(this.workspaceRoot, daemonResult.snapshotId, files[0], options.trigger);
          return {
            success: true,
            snapshot: {
              id: daemonResult.snapshotId,
              fileCount: daemonResult.fileCount ?? files.length,
              totalSize: daemonResult.totalSize ?? 0,
              createdAt: daemonResult.createdAt ? new Date(daemonResult.createdAt).getTime() : Date.now()
            }
          };
        } catch (error) {
          return {
            success: false,
            error: errorMessage(error)
          };
        }
      }
      /**
      * List recent snapshots
      * @deprecated Synchronous engine access removed. Returns empty array.
      * Use `listSnapshotsViaDaemon(workspaceRoot)` from local-service-adapter directly.
      */
      listSnapshots(_limit = 20) {
        return [];
      }
      /**
      * Get a specific snapshot
      * @deprecated Synchronous engine access removed. Returns null.
      * Use `getDaemonClient(workspaceRoot).request('snapshot/get', ...)` directly.
      */
      getSnapshot(_id) {
        return null;
      }
      /**
      * Restore files from a snapshot via daemon IPC
      *
      * @param snapshotId - Snapshot ID to restore from
      * @param options - Restore options
      * @returns Restored file paths or preview info
      */
      async restore(snapshotId, options) {
        try {
          const result7 = await restoreSnapshotViaDaemon(this.workspaceRoot, snapshotId, {
            files: options?.files,
            dryRun: options?.preview ?? false
          });
          if (!result7.success) {
            return {
              success: false,
              error: result7.errors?.[0] ?? `Failed to restore snapshot: ${snapshotId}`
            };
          }
          if (options?.preview) {
            return {
              success: true,
              preview: true,
              files: (result7.restoredFiles ?? []).map((path4) => ({
                path: path4,
                size: 0
              }))
            };
          }
          return {
            success: true,
            restoredFiles: result7.restoredFiles ?? [],
            errors: result7.errors
          };
        } catch (error) {
          return {
            success: false,
            error: errorMessage(error)
          };
        }
      }
      /**
      * Get diff between snapshot and current file state
      * @deprecated Direct engine storage access removed. Use daemon request 'snapshot/get'
      * with file content to perform diffs, or use the compare_snapshots MCP tool.
      */
      async diff(_snapshotId, _options) {
        return {
          success: false,
          error: "Diff via local engine storage is no longer supported. Use the compare_snapshots tool or daemon request 'snapshot/get' for file content."
        };
      }
    };
    serviceCache = /* @__PURE__ */ new Map();
    __name8(createSnapshotService, "createSnapshotService");
  }
});
function inferBehaviorType(how) {
  const lowered = how.toLowerCase();
  if (lowered.includes("fix") && lowered.includes("skip")) {
    return "fixed_vs_skipped";
  }
  if (lowered.includes("test")) {
    return "tested_more";
  }
  if (lowered.includes("doc") || lowered.includes("comment")) {
    return "documented_more";
  }
  if (lowered.includes("safe") || lowered.includes("careful")) {
    return "safer_approach";
  }
  if (lowered.includes("thorough") || lowered.includes("harder")) {
    return "worked_harder";
  }
  return "other";
}
__name(inferBehaviorType, "inferBehaviorType");
function mapEffortDelta(change) {
  switch (change) {
    case "more":
      return "somewhat_more";
    case "less":
      return "less";
    default:
      return "same";
  }
}
__name(mapEffortDelta, "mapEffortDelta");
function inferFeature(trigger) {
  if (!trigger) {
    return "general_awareness";
  }
  const lowered = trigger.toLowerCase();
  if (lowered.includes("begin_task")) {
    return "begin_task_context";
  }
  if (lowered.includes("learn")) {
    return "learn_system";
  }
  if (lowered.includes("checkpoint") || lowered.includes("snapshot")) {
    return "checkpoint_visibility";
  }
  if (lowered.includes("review")) {
    return "review_work";
  }
  if (lowered.includes("complete")) {
    return "complete_task_reflection";
  }
  return "general_awareness";
}
__name(inferFeature, "inferFeature");
function buildAccountabilityEffect(reflection, taskId, durationMs, filesChanged, linesChanged, snapshotsCreated, tier, outcome) {
  const validValues = [
    "significantly",
    "somewhat",
    "not_really",
    "blocked"
  ];
  const perceived_help = validValues.includes(reflection.perceived_help) ? reflection.perceived_help : "somewhat";
  const result7 = {
    session_id: taskId,
    session_duration_ms: durationMs,
    perceived_help,
    actual_changes: {
      files_modified: filesChanged,
      lines_added: Math.max(0, Math.floor(linesChanged * 0.7)),
      lines_removed: Math.max(0, Math.floor(linesChanged * 0.3)),
      snapshots_used: snapshotsCreated
    },
    prevented_issues: {
      // These would ideally come from session tracking
      // For now, we provide reasonable defaults based on session activity
      rollbacks_avoided: snapshotsCreated > 0 ? 1 : 0,
      pattern_violations_caught: 0,
      skipped_tests_flagged: 0
    },
    tier
  };
  if (reflection.accountability !== void 0) {
    if (reflection.accountability.behaved_differently) {
      result7.accountability_behavior = {
        would_have_behaved_differently: true,
        behavior_change_type: reflection.accountability.how ? inferBehaviorType(reflection.accountability.how) : "other",
        what_triggered_accountability: reflection.accountability.triggered_by,
        effort_delta: mapEffortDelta(reflection.accountability.effort_change),
        vreko_feature_responsible: inferFeature(reflection.accountability.triggered_by),
        outcome_improved: outcome === "completed"
      };
    } else {
      result7.accountability_behavior = {
        would_have_behaved_differently: false
      };
    }
  }
  return result7;
}
__name(buildAccountabilityEffect, "buildAccountabilityEffect");
function getIntentHint(intent, outcome) {
  if (!intent) {
    return void 0;
  }
  return INTENT_COMPLETION_HINTS[intent]?.[outcome];
}
__name(getIntentHint, "getIntentHint");
function result3(text2, isError = false) {
  return {
    content: [
      {
        type: "text",
        text: text2
      }
    ],
    isError
  };
}
__name(result3, "result3");
var INTENT_COMPLETION_HINTS;
var handleCompleteTask;
var init_complete_task = __esm2({
  "src/facades/complete-task.ts"() {
    init_session_adapter();
    init_context();
    init_local_service_adapter();
    init_session_file_tracker();
    init_snapshot_service();
    init_utils2();
    __name8(inferBehaviorType, "inferBehaviorType");
    __name8(mapEffortDelta, "mapEffortDelta");
    __name8(inferFeature, "inferFeature");
    __name8(buildAccountabilityEffect, "buildAccountabilityEffect");
    INTENT_COMPLETION_HINTS = {
      implement: {
        completed: "Feature implemented. Consider running tests to verify.",
        abandoned: "Implementation paused. Snapshot preserved for resumption.",
        blocked: "Blocked on implementation. Check learnings for similar issues."
      },
      debug: {
        completed: "Bug fixed! Consider recording the fix as a learning.",
        abandoned: "Debug session paused. Current state preserved.",
        blocked: "Still debugging. Try vreko for similar issues."
      },
      refactor: {
        completed: "Refactoring complete. Run tests to ensure no regressions.",
        abandoned: "Refactor paused. Partial changes preserved in snapshot.",
        blocked: "Blocked on refactor. Consider smaller incremental changes."
      },
      review: {
        completed: "Review complete. Consider recording any patterns discovered.",
        abandoned: "Review paused. Notes preserved.",
        blocked: "Review blocked. May need additional context or access."
      },
      explore: {
        completed: "Exploration complete. Consider recording discoveries as learnings.",
        abandoned: "Exploration paused. Findings preserved.",
        blocked: "Exploration blocked. Try a different approach."
      }
    };
    __name8(getIntentHint, "getIntentHint");
    __name8(result3, "result");
    handleCompleteTask = /* @__PURE__ */ __name8(async (args, context) => {
      const { outcome = "completed", createSnapshot = outcome === "completed", acceptLearnings = [], customLearning, notes, reflection } = args;
      const workspaceRoot = context.workspaceRoot;
      const state = await getSessionStateAsync(workspaceRoot);
      const task = await getCurrentTaskAsync(workspaceRoot);
      if (!task) {
        return result3(JSON.stringify({
          error: "E301_NO_ACTIVE_TASK",
          message: "No active task to complete. Use vreko to start one.",
          _hint: "Start a new task with vreko before trying to complete it."
        }), true);
      }
      if (!state) {
        const fallbackChanges = await getSessionChangesAsync(workspaceRoot);
        if (fallbackChanges.length > 0) {
          await Promise.allSettled(fallbackChanges.map((change) => recordFileChangeAsync(workspaceRoot, change)));
        }
        const daemonResult2 = await endTaskViaService(workspaceRoot, outcome);
        if (daemonResult2) {
          return result3(JSON.stringify({
            success: true,
            taskId: task.id,
            outcome,
            summary: daemonResult2.summary,
            warnings: []
          }));
        }
        return result3(JSON.stringify({
          error: "E302_NO_SESSION_STATE",
          message: "No session state available. Task may have already been completed."
        }), true);
      }
      const sessionChanges = state.changesSinceTaskStart ?? [];
      if (sessionChanges.length > 0) {
        await Promise.allSettled(sessionChanges.map((change) => recordFileChangeAsync(workspaceRoot, change)));
      }
      const daemonResult = await endTaskViaService(workspaceRoot, outcome);
      if (daemonResult) {
        const warnings = [];
        const modifiedFiles = (state.changesSinceTaskStart || []).map((c) => c.file);
        const validatedFiles = state.validatedFiles || [];
        const unvalidatedFiles = modifiedFiles.filter((f) => !validatedFiles.includes(f));
        if (unvalidatedFiles.length > 0) {
          warnings.push(`${unvalidatedFiles.length} file(s) modified but not validated. Consider running check_patterns before committing.`);
        }
        let learningsCaptured2 = 0;
        const failedLearnings = [];
        const pendingLearnings2 = state.pendingSuggestedLearnings || [];
        for (const idx of acceptLearnings) {
          if (idx >= 0 && idx < pendingLearnings2.length) {
            const learning = pendingLearnings2[idx];
            const result7 = await addLearningViaDaemon(workspaceRoot, {
              type: learning.type || "pattern",
              trigger: learning.trigger,
              action: learning.action,
              source: learning.source || "complete-task"
            });
            if (result7.success) {
              learningsCaptured2++;
            } else {
              failedLearnings.push(`Learning ${idx}: ${result7.error}`);
            }
          }
        }
        if (customLearning) {
          const result7 = await addLearningViaDaemon(workspaceRoot, {
            type: customLearning.type || "pattern",
            trigger: customLearning.trigger,
            action: customLearning.action,
            source: customLearning.source || "complete-task"
          });
          if (result7.success) {
            learningsCaptured2++;
          } else {
            failedLearnings.push(`Custom learning: ${result7.error}`);
          }
        }
        if (failedLearnings.length > 0) {
          warnings.push(`Failed to save ${failedLearnings.length} learning(s): ${failedLearnings.join("; ")}`);
        }
        state.stats.learningsCaptured += learningsCaptured2;
        await endTaskAsync(workspaceRoot, outcome);
        const nextActions2 = [];
        if (outcome === "completed") {
          nextActions2.push({
            tool: "vreko",
            priority: 2,
            reason: "Start your next task"
          });
        } else if (outcome === "blocked") {
          nextActions2.push({
            tool: "vreko",
            priority: 1,
            reason: "Check if similar issues were solved before"
          });
        }
        if (learningsCaptured2 === 0 && daemonResult.summary.filesModified > 3) {
          nextActions2.push({
            tool: "snap_learn",
            priority: 3,
            reason: "Consider capturing a pattern from this work"
          });
        }
        let accountability_effect2;
        if (reflection?.perceived_help) {
          accountability_effect2 = buildAccountabilityEffect(reflection, task.id, daemonResult.summary.duration, daemonResult.summary.filesModified, daemonResult.summary.linesChanged, daemonResult.snapshot.created ? 1 : 0, context.tier || "free", outcome);
        }
        const output2 = {
          taskId: task.id,
          taskDescription: task.description,
          outcome,
          duration: formatDuration4(daemonResult.summary.duration),
          summary: {
            filesChanged: daemonResult.summary.filesModified,
            linesChanged: daemonResult.summary.linesChanged,
            snapshotsCreated: daemonResult.snapshot.created ? 1 : 0,
            riskAreasTouched: state.riskAreasTouched
          },
          learningsCaptured: learningsCaptured2 + daemonResult.learningsAccepted,
          finalSnapshot: daemonResult.snapshot.created ? {
            id: "daemon-snapshot",
            fileCount: daemonResult.summary.filesModified
          } : void 0,
          sessionStats: {
            tasksCompleted: state.stats.tasksCompleted,
            totalSnapshots: state.stats.snapshotsCreated,
            totalLearnings: state.stats.learningsCaptured
          },
          nextActions: nextActions2,
          accountability_effect: accountability_effect2,
          _warnings: warnings.length > 0 ? warnings : void 0
        };
        const vectorRetrieval2 = context.services?.vectorRetrieval;
        if (vectorRetrieval2 && outcome === "completed") {
          try {
            const modifiedFilePaths2 = (state.changesSinceTaskStart || []).map((c) => c.file);
            const vectorResult = await searchSessionContext({
              retrieval: vectorRetrieval2,
              workspaceId: context.workspaceId || workspaceRoot,
              summary: task.description,
              changedFiles: modifiedFilePaths2,
              limit: 5
            });
            if (vectorResult.learnings.length > 0 && vectorResult.source === "vector") {
              output2.relatedLearnings = formatVectorLearnings(vectorResult.learnings);
            }
          } catch {
          }
        }
        const emoji2 = outcome === "completed" ? "\u2705" : outcome === "blocked" ? "\u{1F6A7}" : "\u23F9\uFE0F";
        const intentHint2 = getIntentHint(task.intent, outcome);
        const baseHint2 = outcome === "completed" ? `Task completed in ${formatDuration4(daemonResult.summary.duration)}! ${daemonResult.summary.filesModified} file(s), ${daemonResult.summary.linesChanged} line(s) changed.` : outcome === "blocked" ? "Task blocked. Check learnings for similar issues." : "Task abandoned.";
        const hint2 = intentHint2 ? `${baseHint2} ${intentHint2}` : baseHint2;
        return result3(JSON.stringify({
          ...output2,
          message: `${emoji2} ${hint2}`,
          _hint: notes ? `Notes: ${notes}` : hint2,
          _intentHint: intentHint2,
          _source: "daemon"
        }, null, 2));
      }
      const fallbackWarnings = [];
      const durationMs = Date.now() - task.startedAt;
      const duration = formatDuration4(durationMs);
      let filesChanged = (state.changesSinceTaskStart || []).length;
      let linesChanged = (state.changesSinceTaskStart || []).reduce((sum, c) => sum + c.linesChanged, 0);
      if (filesChanged === 0) {
        try {
          const tracker = getSessionFileTracker(workspaceRoot);
          const tracked = tracker.getTrackedFiles();
          if (tracked.length > 0) {
            filesChanged = tracked.length;
            linesChanged = tracked.reduce((sum, f) => sum + (f.linesChanged || 0), 0);
          }
        } catch {
        }
      }
      if (filesChanged === 0) {
        try {
          const stdout = execSync("git diff --name-only HEAD", {
            cwd: workspaceRoot,
            timeout: 5e3,
            encoding: "utf-8"
          });
          const gitFiles = stdout.trim().split("\n").filter(Boolean);
          filesChanged = gitFiles.length;
        } catch {
        }
      }
      const modifiedFilePaths = (state.changesSinceTaskStart || []).map((c) => c.file);
      const validatedFilePaths = state.validatedFiles || [];
      const unvalidatedFilePaths = modifiedFilePaths.filter((f) => !validatedFilePaths.includes(f));
      if (unvalidatedFilePaths.length > 0) {
        fallbackWarnings.push(`${unvalidatedFilePaths.length} file(s) modified but not validated. Consider running check_patterns before committing.`);
      }
      let finalSnapshot;
      if (createSnapshot && filesChanged > 0) {
        const filePaths = (state.changesSinceTaskStart || []).map((c) => c.file);
        const snapshotService = createSnapshotService(workspaceRoot);
        const snapshotResult = await snapshotService.createFromFiles(filePaths, {
          description: `Task complete: ${task.description}`,
          trigger: "ai-detection",
          skipDedup: true
        });
        if (snapshotResult.success && snapshotResult.snapshot) {
          finalSnapshot = {
            id: snapshotResult.snapshot.id,
            fileCount: snapshotResult.snapshot.fileCount
          };
          state.stats.snapshotsCreated++;
        }
      }
      let learningsCaptured = 0;
      const failedLearningWrites = [];
      const pendingLearnings = state.pendingSuggestedLearnings || [];
      for (const idx of acceptLearnings) {
        if (idx >= 0 && idx < pendingLearnings.length) {
          const learning = pendingLearnings[idx];
          const writeResult = await addLearningViaDaemon(workspaceRoot, {
            type: learning.type || "pattern",
            trigger: learning.trigger,
            action: learning.action,
            source: learning.source || "complete-task"
          });
          if (writeResult.success) {
            learningsCaptured++;
          } else {
            failedLearningWrites.push(`Learning ${idx}: ${writeResult.error}`);
          }
        }
      }
      if (customLearning) {
        const writeResult = await addLearningViaDaemon(workspaceRoot, {
          type: customLearning.type || "pattern",
          trigger: customLearning.trigger,
          action: customLearning.action,
          source: customLearning.source || "complete-task"
        });
        if (writeResult.success) {
          learningsCaptured++;
        } else {
          failedLearningWrites.push(`Custom learning: ${writeResult.error}`);
        }
      }
      if (failedLearningWrites.length > 0) {
        fallbackWarnings.push(`Failed to save ${failedLearningWrites.length} learning(s): ${failedLearningWrites.join("; ")}`);
      }
      state.stats.learningsCaptured += learningsCaptured;
      await endTaskAsync(workspaceRoot, outcome);
      const nextActions = [];
      if (outcome === "completed") {
        nextActions.push({
          tool: "vreko",
          priority: 2,
          reason: "Start your next task"
        });
      } else if (outcome === "blocked") {
        nextActions.push({
          tool: "vreko",
          priority: 1,
          reason: "Check if similar issues were solved before"
        });
      }
      if (learningsCaptured === 0 && filesChanged > 3) {
        nextActions.push({
          tool: "snap_learn",
          priority: 3,
          reason: "Consider capturing a pattern from this work"
        });
      }
      let accountability_effect;
      if (reflection?.perceived_help) {
        accountability_effect = buildAccountabilityEffect(reflection, task.id, durationMs, filesChanged, linesChanged, finalSnapshot ? 1 : 0, context.tier || "free", outcome);
      }
      const output = {
        taskId: task.id,
        taskDescription: task.description,
        outcome,
        duration,
        summary: {
          filesChanged,
          linesChanged,
          snapshotsCreated: finalSnapshot ? 1 : 0,
          riskAreasTouched: state.riskAreasTouched
        },
        learningsCaptured,
        finalSnapshot,
        sessionStats: {
          tasksCompleted: state.stats.tasksCompleted,
          totalSnapshots: state.stats.snapshotsCreated,
          totalLearnings: state.stats.learningsCaptured
        },
        nextActions,
        accountability_effect,
        _warnings: fallbackWarnings.length > 0 ? fallbackWarnings : void 0
      };
      const vectorRetrieval = context.services?.vectorRetrieval;
      if (vectorRetrieval && outcome === "completed") {
        try {
          const modifiedFilePaths2 = (state.changesSinceTaskStart || []).map((c) => c.file);
          const vectorResult = await searchSessionContext({
            retrieval: vectorRetrieval,
            workspaceId: context.workspaceId || workspaceRoot,
            summary: task.description,
            changedFiles: modifiedFilePaths2,
            limit: 5
          });
          if (vectorResult.learnings.length > 0 && vectorResult.source === "vector") {
            output.relatedLearnings = formatVectorLearnings(vectorResult.learnings);
          }
        } catch {
        }
      }
      const emoji = outcome === "completed" ? "\u2705" : outcome === "blocked" ? "\u{1F6A7}" : "\u23F9\uFE0F";
      const intentHint = getIntentHint(task.intent, outcome);
      const baseHint = outcome === "completed" ? `Task completed in ${duration}! ${filesChanged} file(s), ${linesChanged} line(s) changed.` : outcome === "blocked" ? "Task blocked. Check learnings for similar issues." : "Task abandoned.";
      const hint = intentHint ? `${baseHint} ${intentHint}` : baseHint;
      return result3(JSON.stringify({
        ...output,
        message: `${emoji} ${hint}`,
        _hint: notes ? `Notes: ${notes}` : hint,
        _intentHint: intentHint
      }, null, 2));
    }, "handleCompleteTask");
  }
});
function getObservationEmoji(type) {
  switch (type) {
    case "risk":
      return "\u26A0\uFE0F";
    case "warning":
      return "\u{1F6A8}";
    case "pattern":
      return "\u{1F4DA}";
    case "suggestion":
      return "\u{1F4A1}";
    case "progress":
      return "\u2705";
    default:
      return "\u2139\uFE0F";
  }
}
__name(getObservationEmoji, "getObservationEmoji");
function generateRecommendations(state) {
  const recommendations = [];
  if (!state.currentTask) {
    recommendations.push("Call `begin_task` before making any code changes");
    return recommendations;
  }
  if (state.riskAreasTouched.length > 0) {
    recommendations.push(`You're modifying ${state.riskAreasTouched.join(", ")} - a snapshot protects these changes`);
  }
  if (state.pendingObservations.length > 3) {
    recommendations.push(`${state.pendingObservations.length} observations pending - review with next tool call`);
  }
  if (state.currentTask) {
    const durationMs = Date.now() - state.currentTask.startedAt;
    const minutes = durationMs / 6e4;
    if (minutes > 30) {
      recommendations.push("Task running 30+ minutes - consider using `review_work` to check progress");
    }
  }
  if ((state.changesSinceTaskStart || []).length > 5) {
    recommendations.push(`${(state.changesSinceTaskStart || []).length} files changed - use \`what_changed\` to review scope`);
  }
  const aiChanges = (state.changesSinceTaskStart || []).filter((c) => c.aiAttributed).length;
  if (aiChanges > 3) {
    recommendations.push(`${aiChanges} AI-attributed changes - review carefully before committing`);
  }
  if (state.stats.restoresPerformed > 0) {
    recommendations.push(`You've used restore ${state.stats.restoresPerformed} time(s) - snapshots are helping!`);
  }
  return recommendations;
}
__name(generateRecommendations, "generateRecommendations");
function generateQuickReference() {
  return `
| Situation | Tool to Call |
|-----------|-------------|
| Starting any task | \`begin_task\` |
| "Did I break anything?" | \`quick_check\` |
| "What have I changed?" | \`what_changed\` |
| Ready to commit | \`review_work\` |
| Task complete | \`complete_task\` |
| Need to undo | \`snapshot_restore\` |
`.trim();
}
__name(generateQuickReference, "generateQuickReference");
function generateProtocolText(state) {
  const lines = [
    "## Vreko Pairing Protocol",
    ""
  ];
  if (state.currentTask) {
    const duration = formatDuration4(Date.now() - state.currentTask.startedAt);
    lines.push(`**Active Task:** ${state.currentTask.description}`);
    lines.push(`**Duration:** ${duration}`);
    lines.push(`**Files Changed:** ${state.changesSinceTaskStart.length}`);
    if (state.currentTask.snapshotId) {
      lines.push(`**Snapshot:** ${state.currentTask.snapshotId} (available for rollback)`);
    }
    lines.push("");
  } else {
    lines.push("**No active task.** Call `begin_task` before making changes.");
    lines.push("");
  }
  if ((state.pendingObservations || []).length > 0) {
    lines.push("**Recent Observations:**");
    for (const obs of (state.pendingObservations || []).slice(-3)) {
      const emoji = getObservationEmoji(obs.type);
      lines.push(`- ${emoji} ${obs.message}`);
    }
    lines.push("");
  }
  if ((state.riskAreasTouched || []).length > 0) {
    lines.push(`**Risk Areas in Scope:** ${(state.riskAreasTouched || []).join(", ")}`);
    lines.push("");
  }
  lines.push("### Tool Usage Pattern");
  lines.push("");
  lines.push("**START of any coding task:**");
  lines.push("```");
  lines.push('Call: begin_task({ task: "description", files: ["planned", "files"] })');
  lines.push("```");
  lines.push("");
  lines.push("**DURING development:**");
  lines.push("- After changes: `quick_check({})` - fast validation");
  lines.push("- Review progress: `what_changed({})` - see all changes");
  lines.push("");
  lines.push("**BEFORE committing:**");
  lines.push("```");
  lines.push('Call: review_work({ intent: "what you accomplished" })');
  lines.push("```");
  lines.push("");
  lines.push("**END of task:**");
  lines.push("```");
  lines.push('Call: complete_task({ outcome: "completed", acceptLearnings: [0, 1] })');
  lines.push("```");
  lines.push("");
  lines.push("### Quick Reference");
  lines.push("");
  lines.push(generateQuickReference());
  return lines.join("\n");
}
__name(generateProtocolText, "generateProtocolText");
async function generatePairingProtocol(workspaceRoot) {
  const state = await getSessionStateAsync(workspaceRoot);
  const currentTask = await getCurrentTaskAsync(workspaceRoot);
  if (!state) {
    return {
      version: "1.0",
      currentTask: null,
      recentObservations: [],
      recommendations: [],
      riskAreas: [],
      sessionStats: {
        tasksCompleted: 0,
        snapshotsCreated: 0,
        restoresPerformed: 0,
        learningsCaptured: 0
      },
      quickReference: "",
      protocolText: ""
    };
  }
  let taskContext = null;
  if (currentTask) {
    taskContext = {
      id: currentTask.id,
      description: currentTask.description,
      duration: formatDuration4(Date.now() - currentTask.startedAt),
      filesChanged: state.changesSinceTaskStart.length,
      snapshotId: currentTask.snapshotId
    };
  }
  const recentObservations = (state.pendingObservations || []).slice(-5).map((obs) => ({
    type: obs.type,
    message: obs.message,
    emoji: getObservationEmoji(obs.type)
  }));
  const recommendations = generateRecommendations(state);
  return {
    version: "2.0",
    currentTask: taskContext,
    recentObservations,
    riskAreas: [
      ...state.riskAreasTouched
    ],
    sessionStats: {
      ...state.stats
    },
    recommendations,
    quickReference: generateQuickReference(),
    protocolText: generateProtocolText(state)
  };
}
__name(generatePairingProtocol, "generatePairingProtocol");
async function getContextSummary(workspaceRoot) {
  const state = await getSessionStateAsync(workspaceRoot);
  if (!state) {
    return {
      hasActiveTask: false,
      filesChanged: 0,
      riskAreas: [],
      pendingObservations: 0
    };
  }
  return {
    hasActiveTask: state.currentTask !== null,
    taskId: state.currentTask?.id,
    filesChanged: state.changesSinceTaskStart.length,
    riskAreas: [
      ...state.riskAreasTouched
    ],
    pendingObservations: state.pendingObservations.length
  };
}
__name(getContextSummary, "getContextSummary");
var init_pairing_protocol = __esm2({
  "src/facades/pairing-protocol.ts"() {
    init_session_adapter();
    init_utils2();
    __name8(getObservationEmoji, "getObservationEmoji");
    __name8(generateRecommendations, "generateRecommendations");
    __name8(generateQuickReference, "generateQuickReference");
    __name8(generateProtocolText, "generateProtocolText");
    __name8(generatePairingProtocol, "generatePairingProtocol");
    __name8(getContextSummary, "getContextSummary");
  }
});
function getErrorPatternRegistry() {
  if (!instance) {
    instance = new ErrorPatternRegistry();
  }
  return instance;
}
__name(getErrorPatternRegistry, "getErrorPatternRegistry");
var BUILT_IN_PATTERNS;
var ErrorPatternRegistry;
var instance;
var init_error_pattern_registry = __esm2({
  "src/services/error-pattern-registry.ts"() {
    BUILT_IN_PATTERNS = [
      // TypeScript errors
      {
        id: "ts-property-not-exist",
        pattern: /error TS2339: Property '([^']+)' does not exist on type '([^']+)'/,
        category: "typescript",
        description: "Property access on incompatible type",
        solutions: [
          "Add the property to the type definition",
          "Use type assertion if property exists at runtime",
          "Check for typos in property name"
        ],
        autoFixable: false,
        confidence: 0.9
      },
      {
        id: "ts-type-not-assignable",
        pattern: /error TS2322: Type '([^']+)' is not assignable to type '([^']+)'/,
        category: "typescript",
        description: "Type mismatch in assignment",
        solutions: [
          "Fix the value to match expected type",
          "Update the type definition to accept the value",
          "Use type assertion if intentional"
        ],
        autoFixable: false,
        confidence: 0.9
      },
      {
        id: "ts-argument-not-assignable",
        pattern: /error TS2345: Argument of type '([^']+)' is not assignable to parameter of type '([^']+)'/,
        category: "typescript",
        description: "Function argument type mismatch",
        solutions: [
          "Fix the argument value to match expected type",
          "Update function signature to accept the type",
          "Use type assertion if intentional"
        ],
        autoFixable: false,
        confidence: 0.9
      },
      {
        id: "ts-cannot-find-name",
        pattern: /error TS2304: Cannot find name '([^']+)'/,
        category: "typescript",
        description: "Reference to undefined identifier",
        solutions: [
          "Import the missing symbol",
          "Check for typos in the name",
          "Declare the variable/type"
        ],
        autoFixable: false,
        confidence: 0.85
      },
      {
        id: "ts-module-not-found",
        pattern: /error TS2307: Cannot find module '([^']+)'/,
        category: "typescript",
        description: "Module resolution failure",
        solutions: [
          "Install the package: npm install <package>",
          "Check package.json for the dependency",
          "Verify tsconfig paths configuration"
        ],
        autoFixable: true,
        autoFix: {
          action: "install_package"
        },
        confidence: 0.9
      },
      // Filesystem errors
      {
        id: "fs-enoent",
        pattern: /ENOENT: no such file or directory(?:, (?:open|stat|read) '([^']+)')?/,
        category: "filesystem",
        description: "File or directory not found",
        solutions: [
          "Create the missing file/directory",
          "Check the path for typos",
          "Normalize path separators for cross-platform"
        ],
        autoFixable: true,
        autoFix: {
          action: "create_file"
        },
        confidence: 0.95
      },
      {
        id: "fs-eacces",
        pattern: /EACCES: permission denied(?:, (?:open|write|read) '([^']+)')?/,
        category: "filesystem",
        description: "Insufficient permissions",
        solutions: [
          "Check file permissions (chmod)",
          "Run with appropriate permissions",
          "Verify the path is correct"
        ],
        autoFixable: false,
        confidence: 0.9
      },
      {
        id: "fs-enospc",
        pattern: /ENOSPC: no space left on device/,
        category: "filesystem",
        description: "Disk space exhausted",
        solutions: [
          "Free up disk space",
          "Clean node_modules and reinstall",
          "Clear temporary files"
        ],
        autoFixable: false,
        confidence: 0.95
      },
      // Module resolution
      {
        id: "module-not-found",
        pattern: /Cannot find module '([^']+)'/,
        category: "module",
        description: "Module resolution failure",
        solutions: [
          "Install the package: npm install <package>",
          "Check package.json dependencies",
          "Verify the import path is correct"
        ],
        autoFixable: true,
        autoFix: {
          action: "install_package"
        },
        confidence: 0.85
      },
      {
        id: "module-export-not-found",
        pattern: /Module '"([^"]+)"' has no exported member '([^']+)'/,
        category: "module",
        description: "Named export not found in module",
        solutions: [
          "Check the correct export name",
          "Use default import instead",
          "Update the package version"
        ],
        autoFixable: false,
        confidence: 0.85
      },
      // Build errors
      {
        id: "build-syntax-error",
        pattern: /SyntaxError: (?:Unexpected token|Missing semicolon)/,
        category: "build",
        description: "JavaScript/TypeScript syntax error",
        solutions: [
          "Check for missing brackets or semicolons",
          "Verify ESM vs CommonJS syntax",
          "Run linter for syntax hints"
        ],
        autoFixable: false,
        confidence: 0.8
      },
      // Test errors
      {
        id: "test-assertion-failed",
        pattern: /AssertionError: expected .+ to (?:equal|be|have)/,
        category: "test",
        description: "Test assertion failure",
        solutions: [
          "Check the expected vs actual values",
          "Update the test or fix the implementation",
          "Verify test data is correct"
        ],
        autoFixable: false,
        confidence: 0.8
      },
      {
        id: "test-timeout",
        pattern: /Timeout of \d+ms exceeded/,
        category: "test",
        description: "Test exceeded timeout",
        solutions: [
          "Increase test timeout",
          "Check for async operations not awaited",
          "Verify mock responses are resolving"
        ],
        autoFixable: false,
        confidence: 0.85
      },
      // Lint errors
      {
        id: "lint-unused-var",
        pattern: /'([^']+)' is (?:defined but never used|assigned .+ but never used)/,
        category: "lint",
        description: "Unused variable/import",
        solutions: [
          "Remove the unused variable/import",
          "Prefix with underscore if intentional: _var",
          "Use the variable in code"
        ],
        autoFixable: true,
        autoFix: {
          action: "run_command",
          params: {
            command: "npx biome check --apply"
          }
        },
        confidence: 0.9
      }
    ];
    ErrorPatternRegistry = class {
      static {
        __name(this, "ErrorPatternRegistry");
      }
      static {
        __name8(this, "ErrorPatternRegistry");
      }
      patterns = [];
      constructor() {
        this.patterns.push(...BUILT_IN_PATTERNS);
      }
      /**
      * Register a custom pattern
      */
      register(pattern) {
        this.patterns.push(pattern);
      }
      /**
      * Match an error message against all patterns
      * Returns the best match (highest confidence)
      */
      match(errorMessage7) {
        const matches = [];
        for (const pattern of this.patterns) {
          const regex = typeof pattern.pattern === "string" ? new RegExp(pattern.pattern) : pattern.pattern;
          const match = errorMessage7.match(regex);
          if (match) {
            const extracted = {};
            if (match.groups) {
              Object.assign(extracted, match.groups);
            } else {
              match.slice(1).forEach((val, idx) => {
                if (val) {
                  extracted[`$${idx + 1}`] = val;
                }
              });
            }
            matches.push({
              pattern,
              match,
              extracted
            });
          }
        }
        if (matches.length === 0) {
          return null;
        }
        return matches.reduce((best, current) => current.pattern.confidence > best.pattern.confidence ? current : best);
      }
      /**
      * Match multiple error messages
      * Returns unique patterns with occurrence counts
      */
      matchMultiple(errors) {
        const matches = /* @__PURE__ */ new Map();
        for (const error of errors) {
          const result7 = this.match(error);
          if (result7) {
            const existing = matches.get(result7.pattern.id);
            if (existing) {
              existing.count++;
            } else {
              matches.set(result7.pattern.id, {
                ...result7,
                count: 1
              });
            }
          }
        }
        return Array.from(matches.values());
      }
      /**
      * Get patterns by category
      */
      getByCategory(category) {
        return this.patterns.filter((p) => p.category === category);
      }
      /**
      * Get all auto-fixable patterns
      */
      getAutoFixable() {
        return this.patterns.filter((p) => p.autoFixable);
      }
      /**
      * Format match result as hint text
      */
      formatHint(result7) {
        const lines = [];
        lines.push(`[${result7.pattern.category.toUpperCase()}] ${result7.pattern.description}`);
        if (result7.pattern.solutions.length > 0) {
          lines.push("Solutions:");
          for (const solution of result7.pattern.solutions.slice(0, 3)) {
            lines.push(`  - ${solution}`);
          }
        }
        if (result7.pattern.autoFixable && result7.pattern.autoFix) {
          lines.push(`Auto-fix: ${result7.pattern.autoFix.action}`);
        }
        return lines.join("\n");
      }
    };
    instance = null;
    __name8(getErrorPatternRegistry, "getErrorPatternRegistry");
  }
});
async function runCommand(cmd, args, cwd, timeoutMs = 3e4) {
  const start = Date.now();
  return new Promise((resolve72) => {
    let stdout = "";
    let stderr = "";
    let killed = false;
    const proc = spawn(cmd, args, {
      cwd,
      shell: false,
      stdio: [
        "pipe",
        "pipe",
        "pipe"
      ]
    });
    const timeout = setTimeout(() => {
      killed = true;
      proc.kill("SIGTERM");
    }, timeoutMs);
    proc.stdout?.on("data", (data) => {
      stdout += data.toString();
    });
    proc.stderr?.on("data", (data) => {
      stderr += data.toString();
    });
    proc.on("close", (code) => {
      clearTimeout(timeout);
      resolve72({
        exitCode: killed ? -1 : code ?? 0,
        stdout,
        stderr,
        duration: Date.now() - start
      });
    });
    proc.on("error", (err22) => {
      clearTimeout(timeout);
      resolve72({
        exitCode: -1,
        stdout,
        stderr: err22.message,
        duration: Date.now() - start
      });
    });
  });
}
__name(runCommand, "runCommand");
async function checkTypeScript(files, workspaceRoot) {
  const start = Date.now();
  try {
    const result7 = await runCommand("npx", [
      "tsc",
      "--noEmit",
      "--pretty",
      "false"
    ], workspaceRoot, 15e3);
    const errors = [];
    const warnings = [];
    const lines = (result7.stdout + result7.stderr).split("\n");
    for (const line of lines) {
      if (line.includes("error TS")) {
        const isRelevant = files.some((f) => line.includes(basename(f)) || line.includes(relative(workspaceRoot, f)));
        if (isRelevant || files.length === 0) {
          errors.push(line.trim());
        }
      } else if (line.includes("warning")) {
        warnings.push(line.trim());
      }
    }
    return {
      passed: result7.exitCode === 0 || errors.length === 0,
      duration: Date.now() - start,
      errors: errors.slice(0, 10),
      warnings: warnings.slice(0, 5)
    };
  } catch {
    return {
      passed: true,
      duration: Date.now() - start,
      warnings: [
        "TypeScript check skipped: tsc not available"
      ]
    };
  }
}
__name(checkTypeScript, "checkTypeScript");
async function checkTests(files, workspaceRoot, runTests) {
  const start = Date.now();
  const testFiles = [];
  for (const file of files) {
    const dir = dirname(file);
    const base = basename(file, ".ts").replace(".tsx", "");
    const patterns = [
      join(dir, `${base}.test.ts`),
      join(dir, `${base}.test.tsx`),
      join(dir, `${base}.spec.ts`),
      join(dir, `${base}.spec.tsx`),
      join(dir, "__tests__", `${base}.test.ts`),
      join(dir, "__tests__", `${base}.test.tsx`)
    ];
    for (const pattern of patterns) {
      const fullPath = join(workspaceRoot, pattern);
      if (existsSync(fullPath)) {
        testFiles.push(pattern);
      }
    }
  }
  if (testFiles.length === 0) {
    const result7 = {
      passed: true,
      duration: Date.now() - start,
      discovered: 0,
      warnings: [
        "No related test files found"
      ]
    };
    return result7;
  }
  if (!runTests) {
    const result7 = {
      passed: true,
      duration: Date.now() - start,
      discovered: testFiles.length,
      warnings: [
        `${testFiles.length} test file(s) discovered but not run. Use runTests: true to execute.`
      ]
    };
    return result7;
  }
  try {
    const result7 = await runCommand("npx", [
      "vitest",
      "run",
      ...testFiles,
      "--reporter=json"
    ], workspaceRoot, 6e4);
    const errors = [];
    let passed = 0;
    let failed = 0;
    try {
      const jsonMatch = result7.stdout.match(/\{[\s\S]*"testResults"[\s\S]*\}/);
      if (jsonMatch) {
        const testResult2 = JSON.parse(jsonMatch[0]);
        passed = testResult2.numPassedTests || 0;
        failed = testResult2.numFailedTests || 0;
        if (testResult2.testResults) {
          for (const tr of testResult2.testResults) {
            if (tr.status === "failed" && tr.message) {
              errors.push(tr.message.slice(0, 200));
            }
          }
        }
      }
    } catch {
      const passMatch = result7.stdout.match(/(\d+) passed/);
      const failMatch = result7.stdout.match(/(\d+) failed/);
      if (passMatch) {
        passed = Number.parseInt(passMatch[1], 10);
      }
      if (failMatch) {
        failed = Number.parseInt(failMatch[1], 10);
        errors.push(`${failed} test(s) failed`);
      }
    }
    const testResult = {
      passed: result7.exitCode === 0,
      duration: Date.now() - start,
      discovered: testFiles.length,
      run: testFiles.length,
      passedCount: passed,
      failedCount: failed,
      errors: errors.slice(0, 5)
    };
    return testResult;
  } catch {
    const result7 = {
      passed: true,
      duration: Date.now() - start,
      discovered: testFiles.length,
      warnings: [
        "Test execution skipped: vitest not available"
      ]
    };
    return result7;
  }
}
__name(checkTests, "checkTests");
async function checkLint(files, workspaceRoot) {
  const start = Date.now();
  try {
    const fileArgs = files.map((f) => relative(workspaceRoot, join(workspaceRoot, f)));
    const result7 = await runCommand("npx", [
      "biome",
      "check",
      "--reporter=json",
      ...fileArgs
    ], workspaceRoot, 15e3);
    const errors = [];
    const warnings = [];
    try {
      const diagnostics = JSON.parse(result7.stdout);
      if (Array.isArray(diagnostics)) {
        for (const diag of diagnostics) {
          if (diag.severity === "error") {
            errors.push(`${diag.path}: ${diag.message}`);
          } else if (diag.severity === "warning") {
            warnings.push(`${diag.path}: ${diag.message}`);
          }
        }
      }
    } catch {
      if (result7.exitCode !== 0) {
        const lines = result7.stderr.split("\n").filter((l) => l.trim());
        errors.push(...lines.slice(0, 5));
      }
    }
    return {
      passed: result7.exitCode === 0 || errors.length === 0,
      duration: Date.now() - start,
      errors: errors.slice(0, 10),
      warnings: warnings.slice(0, 5)
    };
  } catch {
    return {
      passed: true,
      duration: Date.now() - start,
      warnings: [
        "Lint check skipped: biome not available"
      ]
    };
  }
}
__name(checkLint, "checkLint");
function result4(text2, isError = false) {
  return {
    content: [
      {
        type: "text",
        text: text2
      }
    ],
    isError
  };
}
__name(result4, "result4");
var handleQuickCheck;
var init_quick_check = __esm2({
  "src/facades/quick-check.ts"() {
    init_session_adapter();
    init_error_cache_service();
    init_error_pattern_registry();
    __name8(runCommand, "runCommand");
    __name8(checkTypeScript, "checkTypeScript");
    __name8(checkTests, "checkTests");
    __name8(checkLint, "checkLint");
    __name8(result4, "result");
    handleQuickCheck = /* @__PURE__ */ __name8(async (args, context) => {
      const { file, files: providedFiles, skipTypeScript = false, skipTests = false, skipLint = false, runTests = false } = args;
      const workspaceRoot = context.workspaceRoot;
      let files = [];
      if (providedFiles && providedFiles.length > 0) {
        files = providedFiles;
      } else if (file) {
        files = [
          file
        ];
      } else {
        const task = await getCurrentTaskAsync(workspaceRoot);
        if (task?.plannedFiles?.length) {
          files = task.plannedFiles;
        }
      }
      if (files.length === 0) {
        return result4(JSON.stringify({
          error: "E102_MISSING_PARAM",
          message: "No files specified. Provide 'file', 'files', or start a task with begin_task."
        }), true);
      }
      const nodeModules = join(workspaceRoot, "node_modules");
      const tsConfig = join(workspaceRoot, "tsconfig.json");
      if (!existsSync(nodeModules)) {
        return result4(JSON.stringify({
          overall: "fail",
          typescript: {
            passed: false,
            duration: 0,
            errors: [
              "Dependencies missing. Run `pnpm install`."
            ]
          },
          tests: {
            passed: false,
            duration: 0,
            discovered: 0,
            errors: [
              "Dependencies missing"
            ]
          },
          lint: {
            passed: false,
            duration: 0,
            errors: [
              "Dependencies missing"
            ]
          },
          summary: "Dependencies missing. Run `pnpm install` before validation.",
          nextActions: [
            {
              tool: "quick_check",
              priority: 1,
              reason: "Re-run after installing dependencies"
            }
          ]
        }), true);
      }
      if (!existsSync(tsConfig)) {
        return result4(JSON.stringify({
          overall: "fail",
          typescript: {
            passed: false,
            duration: 0,
            errors: [
              "TypeScript config missing. Create tsconfig.json."
            ]
          },
          tests: {
            passed: true,
            duration: 0,
            discovered: 0,
            warnings: [
              "TypeScript config missing"
            ]
          },
          lint: {
            passed: true,
            duration: 0,
            warnings: [
              "TypeScript config missing"
            ]
          },
          summary: "TypeScript config missing. Create tsconfig.json before validation.",
          nextActions: [
            {
              tool: "quick_check",
              priority: 1,
              reason: "Re-run after creating tsconfig.json"
            }
          ]
        }), true);
      }
      const checkResults = await Promise.allSettled([
        skipTypeScript ? Promise.resolve({
          passed: true,
          duration: 0,
          warnings: [
            "Skipped"
          ]
        }) : checkTypeScript(files, workspaceRoot),
        skipTests ? Promise.resolve({
          passed: true,
          duration: 0,
          discovered: 0,
          warnings: [
            "Skipped"
          ]
        }) : checkTests(files, workspaceRoot, runTests),
        skipLint ? Promise.resolve({
          passed: true,
          duration: 0,
          warnings: [
            "Skipped"
          ]
        }) : checkLint(files, workspaceRoot)
      ]);
      const typescript = checkResults[0].status === "fulfilled" ? checkResults[0].value : {
        passed: false,
        duration: 0,
        errors: [
          "TypeScript check failed to execute"
        ]
      };
      const tests = checkResults[1].status === "fulfilled" ? checkResults[1].value : {
        passed: false,
        duration: 0,
        discovered: 0,
        errors: [
          "Test discovery failed to execute"
        ]
      };
      const lint = checkResults[2].status === "fulfilled" ? checkResults[2].value : {
        passed: false,
        duration: 0,
        errors: [
          "Lint check failed to execute"
        ]
      };
      const allPassed = typescript.passed && tests.passed && lint.passed;
      const hasWarnings = (typescript.warnings?.length ?? 0) > 0 || (tests.warnings?.length ?? 0) > 0 || (lint.warnings?.length ?? 0) > 0;
      const overall = allPassed ? hasWarnings ? "warn" : "pass" : "fail";
      const parts = [];
      if (!typescript.passed) {
        parts.push(`TypeScript: ${typescript.errors?.length || 1} error(s)`);
      }
      if (!tests.passed) {
        parts.push(`Tests: ${tests.failedCount || 1} failed`);
      }
      if (!lint.passed) {
        parts.push(`Lint: ${lint.errors?.length || 1} error(s)`);
      }
      const summary = allPassed ? `All checks passed in ${typescript.duration + tests.duration + lint.duration}ms` : `Issues found: ${parts.join(", ")}`;
      if (!allPassed) {
        pushObservationAsync(workspaceRoot, {
          type: "warning",
          message: summary
        });
      }
      try {
        const errorCacheService = createErrorCacheService(workspaceRoot);
        const errorsToCache = [];
        const now = Date.now();
        if (typescript.errors && typescript.errors.length > 0) {
          for (const errorMsg of typescript.errors) {
            const match = errorMsg.match(/^(.+?)\((\d+),(\d+)\):\s*error\s+(TS\d+):\s*(.+)$/);
            if (match) {
              errorsToCache.push({
                file: match[1],
                line: Number.parseInt(match[2], 10),
                column: Number.parseInt(match[3], 10),
                code: match[4],
                message: match[5],
                severity: "error",
                timestamp: now,
                source: "typescript"
              });
            } else {
              errorsToCache.push({
                file: files[0] || "unknown",
                line: 0,
                message: errorMsg,
                severity: "error",
                timestamp: now,
                source: "typescript"
              });
            }
          }
        }
        if (lint.errors && lint.errors.length > 0) {
          for (const errorMsg of lint.errors) {
            const match = errorMsg.match(/^(.+?):\s*(.+)$/);
            if (match) {
              errorsToCache.push({
                file: match[1],
                line: 0,
                message: match[2],
                severity: "error",
                timestamp: now,
                source: "lint"
              });
            }
          }
        }
        const testResult = tests;
        if (testResult.errors && testResult.errors.length > 0) {
          for (const errorMsg of testResult.errors) {
            errorsToCache.push({
              file: files[0] || "unknown",
              line: 0,
              message: errorMsg,
              severity: "error",
              timestamp: now,
              source: "test"
            });
          }
        }
        if (errorsToCache.length > 0) {
          errorCacheService.cacheErrors(errorsToCache);
        }
        if (allPassed) {
          for (const file2 of files) {
            errorCacheService.clearForFile(file2);
          }
        }
      } catch {
      }
      const solutions = [];
      if (!allPassed) {
        const registry = getErrorPatternRegistry();
        const allErrors = [
          ...typescript.errors || [],
          ...lint.errors || [],
          ...tests.errors || []
        ];
        const matches = registry.matchMultiple(allErrors);
        for (const match of matches) {
          solutions.push({
            category: match.pattern.category,
            description: match.pattern.description,
            solutions: match.pattern.solutions,
            autoFixable: match.pattern.autoFixable,
            count: match.count
          });
        }
      }
      const nextActions = [];
      if (!typescript.passed) {
        nextActions.push({
          tool: "validate",
          priority: 1,
          reason: "Fix TypeScript errors before proceeding"
        });
      }
      if (!tests.passed) {
        nextActions.push({
          tool: "quick_check",
          priority: 2,
          reason: "Re-run tests after fixing failures"
        });
      }
      if (allPassed) {
        nextActions.push({
          tool: "review_work",
          priority: 3,
          reason: "All checks passed - ready for review"
        });
      }
      const output = {
        overall,
        typescript,
        tests,
        lint,
        summary,
        solutions: solutions.length > 0 ? solutions : void 0,
        nextActions
      };
      return result4(JSON.stringify({
        ...output,
        _hint: allPassed ? "All checks passed! Consider using review_work before committing." : "Issues found. Fix them and run quick_check again."
      }, null, 2));
    }, "handleQuickCheck");
  }
});
function getGitDiffStats(workspaceRoot, baselineFiles) {
  const files = [];
  let linesAdded = 0;
  let linesRemoved = 0;
  try {
    const numstat = execSync("git diff --numstat HEAD", {
      cwd: workspaceRoot,
      encoding: "utf8",
      timeout: 5e3
    });
    for (const line of numstat.split("\n")) {
      if (!line.trim()) {
        continue;
      }
      const [added, removed, file] = line.split("	");
      if (file) {
        if (baselineFiles?.has(file)) {
          continue;
        }
        const a = added === "-" ? 0 : Number.parseInt(added, 10);
        const r = removed === "-" ? 0 : Number.parseInt(removed, 10);
        linesAdded += a;
        linesRemoved += r;
        files.push({
          file,
          status: "M",
          linesChanged: a + r
        });
      }
    }
    const status = execSync("git status --porcelain", {
      cwd: workspaceRoot,
      encoding: "utf8",
      timeout: 5e3
    });
    for (const line of status.split("\n")) {
      if (!line.trim()) {
        continue;
      }
      const statusCode = line.substring(0, 2);
      const file = line.substring(3).trim();
      if (baselineFiles?.has(file)) {
        continue;
      }
      const existing = files.find((f) => f.file === file);
      if (existing) {
        if (statusCode.includes("A") || statusCode === "??") {
          existing.status = "A";
        } else if (statusCode.includes("D")) {
          existing.status = "D";
        }
      } else if (statusCode === "??" || statusCode.includes("A")) {
        files.push({
          file,
          status: "A",
          linesChanged: 0
        });
      }
    }
  } catch {
  }
  return {
    files,
    linesAdded,
    linesRemoved
  };
}
__name(getGitDiffStats, "getGitDiffStats");
function result5(text2, isError = false) {
  return {
    content: [
      {
        type: "text",
        text: text2
      }
    ],
    isError
  };
}
__name(result5, "result5");
var handleReviewWork;
var init_review_work = __esm2({
  "src/facades/review-work.ts"() {
    init_session_adapter();
    init_utils2();
    init_intelligence();
    __name8(getGitDiffStats, "getGitDiffStats");
    __name8(result5, "result");
    handleReviewWork = /* @__PURE__ */ __name8(async (args, context) => {
      const { files: filterFiles } = args;
      const workspaceRoot = context.workspaceRoot;
      const state = await getSessionStateAsync(workspaceRoot);
      const task = await getCurrentTaskAsync(workspaceRoot);
      const baselineFiles = await getBaselineFileSetAsync(workspaceRoot);
      const { files, linesAdded, linesRemoved } = getGitDiffStats(workspaceRoot, baselineFiles);
      const filteredFileNames = filterFiles && filterFiles.length > 0 ? files.filter((f) => filterFiles.some((filter) => f.file.includes(filter) || filter.includes(f.file))).map((f) => f.file) : files.map((f) => f.file);
      const reviewResult = await getFacades().validation.reviewWork({
        workspaceRoot
      });
      const duration = task ? formatDuration4(Date.now() - task.startedAt) : "No active task";
      if (state && reviewResult.suggestedLearnings && reviewResult.suggestedLearnings.length > 0) {
        state.pendingSuggestedLearnings = reviewResult.suggestedLearnings;
      }
      const nextActions = [];
      if (reviewResult.validation && !reviewResult.validation.overall.passed) {
        nextActions.push({
          tool: "validate",
          priority: 1,
          reason: "Fix validation issues before committing"
        });
      }
      if (reviewResult.readyToCommit) {
        nextActions.push({
          tool: "complete_task",
          priority: 1,
          reason: "Finalize task and optionally capture learnings"
        });
      }
      if (reviewResult.suggestedLearnings && reviewResult.suggestedLearnings.length > 0) {
        nextActions.push({
          tool: "learn",
          priority: 3,
          reason: `${reviewResult.suggestedLearnings.length} learning(s) suggested from this work`
        });
      }
      await pushObservationAsync(workspaceRoot, {
        type: reviewResult.readyToCommit ? "progress" : "warning",
        message: reviewResult.readyToCommit ? `Review complete: ${files.length} file(s) ready to commit` : `Review found ${reviewResult.validation?.issues?.length || 0} issue(s) to address`
      });
      const output = {
        taskId: task?.id ?? null,
        taskDescription: task?.description ?? null,
        duration,
        readyToCommit: reviewResult.readyToCommit,
        validation: {
          passed: reviewResult.validation?.overall?.passed ?? true,
          issues: (reviewResult.validation?.issues || []).map((i) => ({
            layer: i.rule || "unknown",
            severity: "warning",
            message: i.message,
            file: i.file,
            line: i.line
          })),
          layerSummary: (reviewResult.validation?.layers || []).map((l) => ({
            layer: l.name || l.layer || "unknown",
            passed: l.passed,
            issueCount: (l.issues || []).length
          }))
        },
        changes: {
          fileCount: filteredFileNames.length,
          linesAdded,
          linesRemoved,
          files: files.filter((f) => filteredFileNames.includes(f.file)).slice(0, 20)
        },
        suggestedCommitMessage: reviewResult.suggestedCommitMessage,
        suggestedLearnings: (reviewResult.suggestedLearnings || []).map((l) => ({
          type: l.type === "pattern" || l.type === "pitfall" || l.type === "efficiency" ? l.type : "pattern",
          trigger: l.trigger,
          action: l.action
        })),
        nextActions
      };
      return result5(JSON.stringify({
        ...output,
        _hint: reviewResult.readyToCommit ? `Ready to commit! Suggested: ${reviewResult.suggestedCommitMessage}` : `${reviewResult.validation?.issues?.length || 0} issue(s) found. Fix them before committing.`
      }, null, 2));
    }, "handleReviewWork");
  }
});
function getFileDiff(file, workspaceRoot) {
  return new Promise((resolve72) => {
    try {
      const result7 = execSync(`git diff --no-color -- "${file}"`, {
        cwd: workspaceRoot,
        encoding: "utf8",
        maxBuffer: 1024 * 1024,
        timeout: 5e3
      });
      resolve72(result7.trim() || null);
    } catch {
      resolve72(null);
    }
  });
}
__name(getFileDiff, "getFileDiff");
function getGitChanges(workspaceRoot) {
  return new Promise((resolve72) => {
    const changes = /* @__PURE__ */ new Map();
    try {
      const result7 = execSync("git status --porcelain", {
        cwd: workspaceRoot,
        encoding: "utf8",
        timeout: 5e3
      });
      for (const line of result7.split("\n")) {
        if (!line.trim()) {
          continue;
        }
        const status = line.substring(0, 2);
        const file = line.substring(3).trim();
        if (status.includes("A") || status === "??") {
          changes.set(file, "A");
        } else if (status.includes("D")) {
          changes.set(file, "D");
        } else if (status.includes("M") || status.includes("U")) {
          changes.set(file, "M");
        }
      }
    } catch {
    }
    resolve72(changes);
  });
}
__name(getGitChanges, "getGitChanges");
function countLinesChanged(diff) {
  let count = 0;
  for (const line of diff.split("\n")) {
    if (line.startsWith("+") && !line.startsWith("+++")) {
      count++;
    } else if (line.startsWith("-") && !line.startsWith("---")) {
      count++;
    }
  }
  return count;
}
__name(countLinesChanged, "countLinesChanged");
function assessChangeRisk(changes) {
  const factors = [];
  const criticalPatterns = [
    "auth",
    "payment",
    "security",
    "config",
    "migration",
    "env"
  ];
  for (const change of changes) {
    const lowerFile = change.file.toLowerCase();
    for (const pattern of criticalPatterns) {
      if (lowerFile.includes(pattern)) {
        factors.push(`Critical file modified: ${change.file}`);
        break;
      }
    }
  }
  const aiFiles = changes.filter((c) => c.aiAttributed).length;
  const aiRatio = changes.length > 0 ? aiFiles / changes.length : 0;
  if (aiRatio > 0.7 && changes.length >= 3) {
    factors.push(`High AI-attribution ratio: ${Math.round(aiRatio * 100)}% of changes`);
  }
  const totalLines = changes.reduce((sum, c) => sum + c.linesChanged, 0);
  if (totalLines > 500) {
    factors.push(`Large change set: ${totalLines} lines`);
  }
  let level = "low";
  if (factors.length >= 3) {
    level = "high";
  } else if (factors.length >= 1) {
    level = "medium";
  }
  return {
    level,
    factors
  };
}
__name(assessChangeRisk, "assessChangeRisk");
function result6(text2, isError = false) {
  return {
    content: [
      {
        type: "text",
        text: text2
      }
    ],
    isError
  };
}
__name(result6, "result6");
var handleWhatChanged;
var init_what_changed = __esm2({
  "src/facades/what-changed.ts"() {
    init_session_adapter();
    init_local_service_adapter();
    init_session_file_tracker();
    init_utils2();
    init_intelligence();
    __name8(getFileDiff, "getFileDiff");
    __name8(getGitChanges, "getGitChanges");
    __name8(countLinesChanged, "countLinesChanged");
    __name8(assessChangeRisk, "assessChangeRisk");
    __name8(result6, "result");
    handleWhatChanged = /* @__PURE__ */ __name8(async (args, context) => {
      const { includeDiff = false, filterFiles, includeAIAttribution = true } = args;
      const workspaceRoot = context.workspaceRoot;
      const task = await getCurrentTaskAsync(workspaceRoot);
      if (!task) {
        return result6(JSON.stringify({
          taskId: null,
          taskDescription: null,
          duration: "No active task",
          changes: [],
          stats: {
            totalFiles: 0,
            totalLines: 0,
            aiAttributedFiles: 0,
            aiAttributedLines: 0
          },
          riskAssessment: {
            level: "low",
            factors: []
          },
          nextActions: [
            {
              tool: "begin_task",
              priority: 1,
              reason: "Start a task to begin tracking changes"
            }
          ],
          _hint: "No active task. Use begin_task to start tracking changes."
        }, null, 2));
      }
      const daemonChanges = await getSessionChangesViaService(workspaceRoot, {
        includeDiff,
        filterFiles,
        includeAIAttribution
      });
      if (daemonChanges) {
        const duration2 = formatDuration4(Date.now() - task.startedAt);
        const changes2 = (daemonChanges.files || []).map((f) => ({
          file: f.path,
          type: f.status,
          linesChanged: f.linesChanged,
          aiAttributed: f.aiAttributed ?? false,
          timestamp: Date.now()
        }));
        let diffs2;
        if (includeDiff) {
          diffs2 = [];
          for (const change of changes2.slice(0, 10)) {
            if (change.type !== "deleted") {
              const diff = await getFileDiff(change.file, workspaceRoot);
              if (diff) {
                diffs2.push({
                  file: change.file,
                  diff: diff.length > 2e3 ? `${diff.slice(0, 2e3)}
...(truncated)` : diff
                });
              }
            }
          }
        }
        const riskAssessment2 = assessChangeRisk(changes2);
        const nextActions2 = [];
        if (daemonChanges.totalLinesChanged > 0) {
          nextActions2.push({
            tool: "quick_check",
            priority: 1,
            reason: "Validate changes before proceeding"
          });
        }
        if (riskAssessment2.level === "high") {
          nextActions2.push({
            tool: "snapshot_create",
            priority: 1,
            reason: "Create safety snapshot for high-risk changes"
          });
        }
        if (changes2.length > 0) {
          nextActions2.push({
            tool: "review_work",
            priority: 2,
            reason: "Review changes before committing"
          });
        }
        const output2 = {
          taskId: task.id,
          taskDescription: task.description,
          duration: duration2,
          changes: includeAIAttribution ? changes2 : changes2.map((c) => ({
            ...c,
            aiAttributed: false
          })),
          stats: {
            totalFiles: (daemonChanges.files || []).length,
            totalLines: daemonChanges.totalLinesChanged,
            aiAttributedFiles: changes2.filter((c) => c.aiAttributed).length,
            aiAttributedLines: changes2.filter((c) => c.aiAttributed).reduce((sum, c) => sum + c.linesChanged, 0)
          },
          diffs: diffs2,
          riskAssessment: riskAssessment2,
          nextActions: nextActions2
        };
        return result6(JSON.stringify({
          ...output2,
          _hint: daemonChanges.files.length === 0 ? "No changes detected yet. Start making modifications!" : riskAssessment2.level === "high" ? "High-risk changes detected. Consider creating a snapshot." : `${daemonChanges.files.length} file(s) changed, ${daemonChanges.totalLinesChanged} line(s) modified`,
          _source: "daemon"
        }, null, 2));
      }
      const baselineFiles = await getBaselineFileSetAsync(workspaceRoot);
      const intel = getIntelligence2(workspaceRoot);
      const intelligenceChanges = await intel.getFileModifications([
        task.id
      ], workspaceRoot);
      const gitChanges = await getGitChanges(workspaceRoot);
      const sessionTracker = getSessionFileTracker(workspaceRoot);
      const sessionChanges = sessionTracker.getChangedFiles();
      const changeMap = /* @__PURE__ */ new Map();
      for (const change of sessionChanges) {
        const relativePath = change.file.startsWith(workspaceRoot) ? relative(workspaceRoot, change.file) : change.file;
        changeMap.set(relativePath, {
          file: relativePath,
          type: change.type,
          linesChanged: change.linesChanged,
          aiAttributed: change.aiAttributed,
          timestamp: change.timestamp
        });
      }
      for (const change of intelligenceChanges) {
        const changeData = change.modifications;
        if (changeData?.path) {
          const relativePath = changeData.path.startsWith(workspaceRoot) ? relative(workspaceRoot, changeData.path) : changeData.path;
          changeMap.set(relativePath, {
            file: relativePath,
            type: changeData.type === "create" ? "created" : changeData.type === "delete" ? "deleted" : "modified",
            linesChanged: changeData.linesChanged || 0,
            aiAttributed: changeData.aiAttributed ?? false,
            timestamp: changeData.timestamp || 0
          });
        }
      }
      for (const [file, status] of gitChanges) {
        if (baselineFiles.has(file) && !changeMap.has(file)) {
          continue;
        }
        if (!changeMap.has(file)) {
          const type = status === "A" ? "created" : status === "D" ? "deleted" : "modified";
          let linesChanged = 0;
          if (type !== "deleted") {
            const diff = await getFileDiff(file, workspaceRoot);
            if (diff) {
              linesChanged = countLinesChanged(diff);
            }
          }
          changeMap.set(file, {
            file,
            type,
            linesChanged,
            aiAttributed: false,
            timestamp: Date.now()
          });
        }
      }
      let changes = Array.from(changeMap.values());
      if (filterFiles && filterFiles.length > 0) {
        changes = changes.filter((c) => filterFiles.some((f) => c.file.includes(f) || f.includes(c.file)));
      }
      changes.sort((a, b) => b.timestamp - a.timestamp);
      const totalFiles = changes.length;
      const totalLines = changes.reduce((sum, c) => sum + c.linesChanged, 0);
      const aiAttributedFiles = changes.filter((c) => c.aiAttributed).length;
      const aiAttributedLines = changes.filter((c) => c.aiAttributed).reduce((sum, c) => sum + c.linesChanged, 0);
      let diffs;
      if (includeDiff) {
        diffs = [];
        for (const change of changes.slice(0, 10)) {
          if (change.type !== "deleted") {
            const diff = await getFileDiff(change.file, workspaceRoot);
            if (diff) {
              diffs.push({
                file: change.file,
                diff: diff.length > 2e3 ? `${diff.slice(0, 2e3)}
...(truncated)` : diff
              });
            }
          }
        }
      }
      const riskAssessment = assessChangeRisk(changes);
      const nextActions = [];
      if (totalLines > 0) {
        nextActions.push({
          tool: "quick_check",
          priority: 1,
          reason: "Validate changes before proceeding"
        });
      }
      if (riskAssessment.level === "high") {
        nextActions.push({
          tool: "snapshot_create",
          priority: 1,
          reason: "Create safety snapshot for high-risk changes"
        });
      }
      if (changes.length > 0) {
        nextActions.push({
          tool: "review_work",
          priority: 2,
          reason: "Review changes before committing"
        });
      }
      const duration = task ? formatDuration4(Date.now() - task.startedAt) : "No active task";
      const output = {
        taskId: task?.id ?? null,
        taskDescription: task?.description ?? null,
        duration,
        changes: includeAIAttribution ? changes : changes.map((c) => ({
          ...c,
          aiAttributed: false
        })),
        stats: {
          totalFiles,
          totalLines,
          aiAttributedFiles: includeAIAttribution ? aiAttributedFiles : 0,
          aiAttributedLines: includeAIAttribution ? aiAttributedLines : 0
        },
        diffs,
        riskAssessment,
        nextActions
      };
      return result6(JSON.stringify({
        ...output,
        _hint: totalFiles === 0 ? "No changes detected yet. Start making modifications!" : riskAssessment.level === "high" ? "High-risk changes detected. Consider creating a snapshot." : `${totalFiles} file(s) changed, ${totalLines} line(s) modified`
      }, null, 2));
    }, "handleWhatChanged");
  }
});
var handleAcknowledgeRisk;
var handleReportViolation;
var init_risk_handlers = __esm2({
  "src/facades/handlers/risk-handlers.ts"() {
    init_errors();
    init_learning_sync();
    init_intelligence();
    init_utils();
    handleAcknowledgeRisk = /* @__PURE__ */ __name8(async (args, context) => {
      const { files, reason } = args;
      if (!files || !reason) {
        return errorJsonResult(CommonErrors.missingParam("files, reason"));
      }
      const acknowledgment = {
        acknowledgedAt: /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString(),
        tier: context.tier
      };
      return jsonResult({
        status: "acknowledged",
        acknowledged: true,
        files,
        reason,
        timestamp: acknowledgment.acknowledgedAt,
        message: "Risk acknowledged - proceed with changes",
        next_actions: [
          {
            tool: "snapshot_create",
            priority: 1,
            reason: "Create safety snapshot",
            args: {
              files,
              reason: `Pre-risk: ${reason}`
            }
          }
        ]
      });
    }, "handleAcknowledgeRisk");
    handleReportViolation = /* @__PURE__ */ __name8(async (args, context) => {
      const { type, file, whatHappened, whyItHappened, prevention } = args;
      if (!type || !file || !whatHappened || !whyItHappened || !prevention) {
        return errorJsonResult(CommonErrors.missingParam("type, file, whatHappened, whyItHappened, prevention"));
      }
      try {
        const intel = getIntelligence2(context.workspaceRoot);
        const status = await intel.reportViolation({
          type,
          file,
          message: whatHappened,
          reason: whyItHappened,
          prevention
        });
        if (status.shouldPromote && context.workspaceId) {
          const syncService = createLearningSync({
            workspaceRoot: context.workspaceRoot,
            workspaceId: context.workspaceId
          });
          const violationLearning = {
            id: status.id,
            type: "pitfall",
            trigger: `${type} in ${file}`,
            action: `${prevention} (Cause: ${whyItHappened})`,
            source: "violation",
            timestamp: /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString()
          };
          syncService.queueLearning(violationLearning).catch((_syncError) => {
          });
        }
        return jsonResult({
          recorded: true,
          violationId: status.id,
          type,
          file,
          occurrences: status.count,
          promoted: status.shouldPromote,
          promotedTo: status.shouldPromote ? "pattern" : null,
          automation: status.shouldAutomate ? "pending" : null,
          syncQueued: status.shouldPromote && !!context.workspaceId,
          message: status.shouldPromote ? `Violation promoted to pattern after ${status.count} occurrences` : `Violation recorded (${status.count}/3 for promotion)`,
          next_actions: status.shouldPromote ? [
            {
              tool: "get_context",
              priority: 2,
              reason: "New pattern available in context"
            }
          ] : []
        });
      } catch (error) {
        const message = error instanceof Error ? error.message : String(error);
        return result(`Failed to report violation: ${message}`, true);
      }
    }, "handleReportViolation");
  }
});
var handleSession;
var init_session_handler = __esm2({
  "src/facades/handlers/session-handler.ts"() {
    init_errors();
    init_validation();
    init_utils();
    handleSession = /* @__PURE__ */ __name8(async (args, context) => {
      const { op, taskDescription, files, acceptLearnings } = args;
      if (!op) {
        return errorJsonResult(CommonErrors.missingParam("op"));
      }
      const sessionDir = join(context.workspaceRoot, ".vreko", "session");
      const sessionPath = join(sessionDir, "current.json");
      const learningsPath = join(context.workspaceRoot, ".vreko", "learnings", "learnings.jsonl");
      switch (op) {
        case "start": {
          if (!existsSync(sessionDir)) {
            await import('fs').then((fs22) => fs22.mkdirSync(sessionDir, {
              recursive: true
            }));
          }
          if (existsSync(sessionPath)) {
            try {
              const existingSession = JSON.parse(readFileSync(sessionPath, "utf8"));
              return jsonResult({
                op: "start",
                status: "already_active",
                session: existingSession,
                message: "Session already active - end current session first",
                next_actions: [
                  {
                    tool: "session",
                    priority: 1,
                    reason: "End current session",
                    args: {
                      op: "end"
                    }
                  }
                ]
              });
            } catch {
            }
          }
          const sessionId = `sess_${Date.now()}_${Math.random().toString(36).substring(2, 8)}`;
          const session = {
            id: sessionId,
            startedAt: /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString(),
            taskDescription: taskDescription || "MCP session",
            plannedFiles: files || [],
            snapshotCount: 0,
            modifiedFiles: [],
            tier: context.tier
          };
          const writeResult = atomicWriteFileSync2(sessionPath, JSON.stringify(session, null, 2));
          if (!writeResult.success) {
            return jsonResult({
              op: "start",
              status: "error",
              error: "E405_SESSION_OPERATION_FAILED",
              message: writeResult.error
            });
          }
          const recommendations = [];
          if (files && files.length > 0) {
            const hasAuthFiles = files.some((f) => f.includes("auth") || f.includes("security"));
            const hasConfigFiles = files.some((f) => f.includes("config") || f.includes(".env"));
            if (hasAuthFiles) {
              recommendations.push("Create snapshot before modifying auth-related files");
            }
            if (hasConfigFiles) {
              recommendations.push("Verify config changes with prepare_workspace after edits");
            }
            if (files.length > 5) {
              recommendations.push("Consider breaking this into smaller focused sessions");
            }
          }
          return jsonResult({
            op: "start",
            status: "success",
            session,
            recommendations,
            message: "Session started",
            next_actions: [
              {
                tool: "prepare_workspace",
                priority: 1,
                reason: "Check workspace state before starting"
              }
            ]
          });
        }
        case "stats": {
          if (existsSync(sessionPath)) {
            try {
              const session = JSON.parse(readFileSync(sessionPath, "utf8"));
              const startTime = new Date(session.startedAt).getTime();
              const duration = Math.floor((Date.now() - startTime) / 6e4);
              return jsonResult({
                op: "stats",
                session: {
                  ...session,
                  durationMinutes: duration
                },
                message: "Session active"
              });
            } catch {
            }
          }
          return jsonResult({
            op: "stats",
            session: null,
            message: "No active session",
            hint: "Use op: start to begin a session"
          });
        }
        case "recommendations": {
          if (!existsSync(sessionPath)) {
            return jsonResult({
              op: "recommendations",
              recommendations: [
                "Start a session to get personalized recommendations"
              ],
              message: "No active session"
            });
          }
          try {
            const session = JSON.parse(readFileSync(sessionPath, "utf8"));
            const recommendations = [];
            const startTime = new Date(session.startedAt).getTime();
            const duration = (Date.now() - startTime) / 6e4;
            if (duration > 60) {
              recommendations.push("Session running for over an hour - consider taking a break");
            }
            if (session.snapshotCount === 0 && duration > 15) {
              recommendations.push("No snapshots created yet - consider creating a checkpoint");
            }
            if (existsSync(learningsPath)) {
              try {
                const learnings = readFileSync(learningsPath, "utf8").split("\n").filter(Boolean).map((line) => JSON.parse(line));
                const recentLearnings = learnings.slice(-3);
                for (const learning of recentLearnings) {
                  recommendations.push(`Remember: ${learning.action}`);
                }
              } catch {
              }
            }
            return jsonResult({
              op: "recommendations",
              sessionId: session.id,
              recommendations: recommendations.length > 0 ? recommendations : [
                "All clear! Keep coding."
              ],
              message: `${recommendations.length} recommendation(s)`
            });
          } catch (error) {
            const msg = error instanceof Error ? error.message : String(error);
            return jsonResult({
              op: "recommendations",
              status: "error",
              error: "E405_SESSION_OPERATION_FAILED",
              message: `Failed to get recommendations: ${msg}`
            });
          }
        }
        case "end": {
          if (!existsSync(sessionPath)) {
            return jsonResult({
              op: "end",
              status: "no_session",
              message: "No active session to end"
            });
          }
          try {
            const session = JSON.parse(readFileSync(sessionPath, "utf8"));
            const startTime = new Date(session.startedAt).getTime();
            const duration = Math.floor((Date.now() - startTime) / 6e4);
            const archiveDir = join(sessionDir, "archive");
            if (!existsSync(archiveDir)) {
              await import('fs').then((fs22) => fs22.mkdirSync(archiveDir, {
                recursive: true
              }));
            }
            const archivedSession = {
              ...session,
              endedAt: /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString(),
              durationMinutes: duration
            };
            const archivePath = join(archiveDir, `${session.id}.json`);
            const archiveWriteResult = atomicWriteFileSync2(archivePath, JSON.stringify(archivedSession, null, 2));
            if (!archiveWriteResult.success) {
              return jsonResult({
                op: "end",
                status: "error",
                error: "E405_SESSION_OPERATION_FAILED",
                message: archiveWriteResult.error
              });
            }
            await import('fs').then((fs22) => fs22.unlinkSync(sessionPath));
            const suggestedLearnings = [
              {
                index: 0,
                suggestion: "What patterns did you discover?"
              },
              {
                index: 1,
                suggestion: "Any pitfalls to avoid next time?"
              }
            ];
            return jsonResult({
              op: "end",
              status: "success",
              summary: {
                sessionId: session.id,
                durationMinutes: duration,
                snapshotCount: session.snapshotCount,
                modifiedFiles: session.modifiedFiles?.length || 0
              },
              archivePath,
              suggestedLearnings: acceptLearnings ? [] : suggestedLearnings,
              message: `Session ended after ${duration} minutes`,
              next_actions: [
                {
                  tool: "learn",
                  priority: 1,
                  reason: "Record learnings from this session"
                }
              ]
            });
          } catch (error) {
            const msg = error instanceof Error ? error.message : String(error);
            return jsonResult({
              op: "end",
              status: "error",
              error: "E405_SESSION_OPERATION_FAILED",
              message: `Failed to end session: ${msg}`
            });
          }
        }
        default:
          return jsonResult({
            op,
            status: "error",
            error: "E103_INVALID_PARAM_VALUE",
            message: `Unknown session operation: ${op}`,
            validOps: [
              "start",
              "stats",
              "recommendations",
              "end"
            ]
          });
      }
    }, "handleSession");
  }
});
var handleSnapshotCreate;
var handleSnapshotList;
var handleSnapshotRestore;
var handleSuggestSnapshot;
var handleCompareSnapshots;
var init_snapshot_handlers = __esm2({
  "src/facades/handlers/snapshot-handlers.ts"() {
    init_local_service_adapter();
    init_errors();
    init_validation();
    init_response_utils();
    init_utils();
    handleSnapshotCreate = /* @__PURE__ */ __name8(async (args, context) => {
      const { files, reason, trigger } = args;
      if (!files || files.length === 0) {
        return errorJsonResult(CommonErrors.missingParam("files"));
      }
      const pathValidation = validateFilePaths(files, context.workspaceRoot);
      if (!pathValidation.valid) {
        return errorJsonResult(CommonErrors.pathTraversalBlocked(pathValidation.invalidPath, pathValidation.error));
      }
      if (context.storageMode === "readonly" || context.storageMode === "remote") {
        return errorJsonResult({
          status: "error",
          error: "E503_SNAPSHOT_SETUP_NEEDED",
          message: "Enable snapshots with quick local setup",
          action: {
            type: "cli_install",
            command: "npm install -g @vreko/cli && vreko tools configure",
            description: "One command enables full snapshot capabilities",
            time_estimate: "~30 seconds"
          },
          help_url: "https://docs.vreko.dev/cli/setup",
          _llm: {
            suggest: "I can enable snapshots for you with a quick CLI install. Want me to help?",
            auto_offer: true
          }
        });
      }
      await listSnapshotsViaDaemon(context.workspaceRoot, 20);
      try {
        const daemonResult = await createSnapshotViaDaemon(context.workspaceRoot, files, {
          reason,
          trigger: trigger || "manual"
        });
        if (!daemonResult.success || !daemonResult.snapshotId) {
          return errorJsonResult({
            status: "error",
            error: "E106_SNAPSHOT_CREATION_FAILED",
            message: daemonResult.error || "Daemon failed to create snapshot",
            next_actions: []
          });
        }
        return jsonResult({
          status: "success",
          snapshotId: daemonResult.snapshotId,
          fileCount: daemonResult.fileCount ?? files.length,
          totalSize: daemonResult.totalSize ?? 0,
          createdAt: daemonResult.createdAt ?? /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString(),
          message: `Snapshot created with ${daemonResult.fileCount ?? files.length} file(s)`,
          attempts: 1,
          autoFixed: false,
          next_actions: buildSmartActions([
            {
              tool: "snapshot_list",
              reason: "Verify snapshot",
              priority: 2
            }
          ], {
            hasUnsavedChanges: false,
            lastSnapshotMinutes: 0
          })
        }, {
          context,
          coaching: {
            lastAction: "snapshot_create",
            taskPhase: "working"
          }
        });
      } catch (error) {
        const lastError = error instanceof Error ? error : new Error(String(error));
        const diagnosis = diagnoseSnapshotFailure(lastError, files, context.workspaceRoot);
        const formattedDiagnosis = formatDiagnosis(diagnosis);
        return errorJsonResult({
          status: "error",
          error: "E106_SNAPSHOT_CREATION_FAILED",
          message: lastError.message,
          diagnosis: {
            type: diagnosis.type,
            message: diagnosis.message,
            cause: diagnosis.cause,
            suggestedFix: diagnosis.suggestedFix,
            userAction: diagnosis.userAction,
            confidence: diagnosis.confidence,
            affectedFiles: diagnosis.affectedFiles
          },
          formatted: formattedDiagnosis,
          next_actions: diagnosis.canAutoFix ? [
            {
              tool: "snapshot_create",
              priority: 1,
              reason: "Retry with manual fix"
            }
          ] : []
        });
      }
    }, "handleSnapshotCreate");
    handleSnapshotList = /* @__PURE__ */ __name8(async (args, context) => {
      const { limit = 20, since } = args;
      let snapshots = await listSnapshotsViaDaemon(context.workspaceRoot, limit);
      if (since) {
        const sinceTime = new Date(since).getTime();
        snapshots = snapshots.filter((s) => new Date(s.createdAt).getTime() >= sinceTime);
      }
      snapshots = snapshots.slice(0, limit);
      return jsonResult({
        status: "success",
        count: snapshots.length,
        snapshots: snapshots.map((s) => ({
          id: s.id,
          createdAt: new Date(s.createdAt).toISOString(),
          fileCount: s.fileCount,
          totalSize: 0,
          description: s.reason,
          trigger: void 0,
          files: []
        })),
        next_actions: snapshots.length > 0 ? [
          {
            tool: "snapshot_restore",
            priority: 2,
            reason: "Restore if needed",
            args: {
              snapshotId: snapshots[0].id
            }
          }
        ] : [
          {
            tool: "snapshot_create",
            priority: 1,
            reason: "No snapshots yet"
          }
        ]
      });
    }, "handleSnapshotList");
    handleSnapshotRestore = /* @__PURE__ */ __name8(async (args, context) => {
      const { snapshotId, files: filterFiles, dryRun } = args;
      if (!snapshotId) {
        return errorJsonResult(CommonErrors.missingParam("snapshotId"));
      }
      if (filterFiles && filterFiles.length > 0) {
        const pathValidation = validateFilePaths(filterFiles, context.workspaceRoot);
        if (!pathValidation.valid) {
          return errorJsonResult(CommonErrors.pathTraversalBlocked(pathValidation.invalidPath, pathValidation.error));
        }
      }
      const daemon = createServiceClient2(context.workspaceRoot);
      try {
        const snapInfo = await daemon.request("snapshot/get", {
          workspace: context.workspaceRoot,
          snapshotId
        });
        if (!snapInfo?.snapshot) {
          return errorJsonResult(CommonErrors.snapshotNotFound(snapshotId));
        }
        const allSnapshotFiles = snapInfo.snapshot.files.map((f) => f.path);
        const filesToRestorePaths = filterFiles ? allSnapshotFiles.filter((p) => filterFiles.includes(p)) : allSnapshotFiles;
        if (dryRun) {
          return jsonResult({
            status: "dry_run",
            snapshotId,
            wouldRestore: filesToRestorePaths.map((p) => ({
              path: p,
              size: 0
            })),
            message: `Would restore ${filesToRestorePaths.length} file(s)`
          });
        }
        const existingFilePaths = filesToRestorePaths.filter((p) => existsSync(join(context.workspaceRoot, p)));
        let backupSnapshotId = null;
        if (existingFilePaths.length > 0) {
          const backup = await createSnapshotViaDaemon(context.workspaceRoot, existingFilePaths, {
            reason: `Auto-backup before restore from ${snapshotId}`,
            trigger: "auto"
          });
          if (backup.success && backup.snapshotId) {
            backupSnapshotId = backup.snapshotId;
          }
        }
        const restoreResult = await restoreSnapshotViaDaemon(context.workspaceRoot, snapshotId, {
          files: filterFiles,
          dryRun: false
        });
        if (!restoreResult.success) {
          return result(`Snapshot restore failed: ${restoreResult.errors?.[0] ?? "unknown error"}`, true);
        }
        return jsonResult({
          status: "success",
          snapshotId,
          backupSnapshotId,
          restoredFiles: restoreResult.restoredFiles ?? [],
          message: `Restored ${(restoreResult.restoredFiles ?? []).length} file(s) from snapshot${backupSnapshotId ? ` (backup: ${backupSnapshotId})` : ""}`,
          next_actions: [
            {
              tool: "prepare_workspace",
              priority: 1,
              reason: "Verify workspace state"
            }
          ]
        });
      } catch (error) {
        const message = error instanceof Error ? error.message : String(error);
        return result(`Snapshot restore failed: ${message}`, true);
      }
    }, "handleSnapshotRestore");
    handleSuggestSnapshot = /* @__PURE__ */ __name8(async (args, context) => {
      const { files } = args;
      try {
        const vitalsResult = await getVitalsViaDaemon(context.workspaceRoot);
        const vitals = vitalsResult.success && vitalsResult.vitals ? vitalsResult.vitals : {
          pressure: 0,
          snapshotCount: 0
        };
        const snapshots = await listSnapshotsViaDaemon(context.workspaceRoot, 5);
        const pressure = vitals.pressure ?? 0;
        const snapshotCount = vitals.snapshotCount ?? snapshots.length;
        let recommendation = "continue";
        let reason = "Workspace is stable";
        let urgency = "low";
        if (pressure >= 60) {
          recommendation = "now";
          reason = "High pressure detected - immediate snapshot recommended";
          urgency = "high";
        } else if (pressure >= 40) {
          recommendation = "soon";
          reason = "Moderate pressure - consider creating a snapshot";
          urgency = "medium";
        } else if (snapshotCount === 0) {
          recommendation = "soon";
          reason = "No snapshots in this session - consider creating one";
          urgency = "medium";
        }
        let criticalFilesTouched = 0;
        if (files && files.length > 0) {
          const sensitiveTouched = files.filter((f) => SENSITIVE_PATTERNS.some((p) => p.test(f)));
          criticalFilesTouched = sensitiveTouched.length;
          if (criticalFilesTouched > 0 && recommendation === "continue") {
            recommendation = "soon";
            reason = "Sensitive files modified - consider creating a snapshot";
            urgency = "medium";
          }
        }
        const nextActions = recommendation === "now" ? [
          {
            tool: "snapshot_create",
            priority: 1,
            reason,
            args: {
              files
            }
          }
        ] : recommendation === "soon" ? [
          {
            tool: "snapshot_create",
            priority: 2,
            reason,
            args: {
              files
            }
          }
        ] : [];
        return jsonResult({
          status: "success",
          urgency,
          recommendation,
          reason,
          metrics: {
            pressureRate: pressure / 100,
            aiActivityBurst: false,
            criticalFilesTouched,
            trajectoryConfidence: 0.7
          },
          vitals: {
            pressure,
            snapshotCount,
            trajectory: pressure >= 60 ? "critical" : pressure >= 40 ? "degrading" : "stable"
          },
          message: recommendation === "now" ? `\u26A0\uFE0F Snapshot recommended NOW: ${reason}` : recommendation === "soon" ? `\u{1F554} Snapshot recommended soon: ${reason}` : `\u2705 Continue working: ${reason}`,
          next_actions: nextActions
        });
      } catch (error) {
        const message = error instanceof Error ? error.message : String(error);
        return result(`Snapshot suggestion failed: ${message}`, true);
      }
    }, "handleSuggestSnapshot");
    handleCompareSnapshots = /* @__PURE__ */ __name8(async (args, context) => {
      const { snapshotId, targetId, files: filterFiles } = args;
      if (!snapshotId) {
        return errorJsonResult(CommonErrors.missingParam("snapshotId"));
      }
      try {
        const daemon = createServiceClient2(context.workspaceRoot);
        const diffCalculator = new DiffCalculator();
        const sourceData = await daemon.request("snapshot/get", {
          workspace: context.workspaceRoot,
          snapshotId
        });
        if (!sourceData?.snapshot) {
          return errorJsonResult(CommonErrors.snapshotNotFound(snapshotId));
        }
        const sourceFileMap = new Map(sourceData.snapshot.files.map((f) => [
          f.path,
          f.content ?? null
        ]));
        let targetFileMap;
        let targetLabel;
        if (targetId) {
          const targetData = await daemon.request("snapshot/get", {
            workspace: context.workspaceRoot,
            snapshotId: targetId
          });
          if (!targetData?.snapshot) {
            return errorJsonResult(CommonErrors.snapshotNotFound(targetId));
          }
          targetFileMap = new Map(targetData.snapshot.files.map((f) => [
            f.path,
            f.content ?? null
          ]));
          targetLabel = `snapshot ${targetId}`;
        } else {
          targetFileMap = /* @__PURE__ */ new Map();
          for (const [path4] of sourceFileMap) {
            const fullPath = join(context.workspaceRoot, path4);
            if (existsSync(fullPath)) {
              targetFileMap.set(path4, readFileSync(fullPath, "utf8"));
            }
          }
          targetLabel = "current filesystem";
        }
        const allPaths = /* @__PURE__ */ new Set([
          ...sourceFileMap.keys(),
          ...targetFileMap.keys()
        ]);
        let pathsToCompare = Array.from(allPaths);
        if (filterFiles && filterFiles.length > 0) {
          pathsToCompare = pathsToCompare.filter((p) => filterFiles.includes(p));
        }
        const diffs = pathsToCompare.map((path4) => {
          const sourceContent = sourceFileMap.get(path4) ?? null;
          const targetContent = targetFileMap.get(path4) ?? null;
          return diffCalculator.calculateFileDiff(path4, sourceContent, targetContent);
        });
        const preview = diffCalculator.generateDiffPreview(diffs);
        return jsonResult({
          status: "success",
          sourceSnapshot: snapshotId,
          target: targetId ?? "current",
          stats: {
            totalFiles: preview.totalFiles,
            filesCreated: preview.filesCreated,
            filesModified: preview.filesModified,
            filesDeleted: preview.filesDeleted,
            linesAdded: preview.totalLinesAdded,
            linesRemoved: preview.totalLinesRemoved
          },
          files: diffs.map((d) => ({
            path: d.path,
            operation: d.operation,
            linesAdded: d.linesAdded,
            linesRemoved: d.linesRemoved,
            preview: d.preview
          })),
          message: `Compared snapshot ${snapshotId} to ${targetLabel}: ${preview.filesModified} modified, +${preview.totalLinesAdded}/-${preview.totalLinesRemoved} lines`,
          next_actions: preview.filesModified > 0 ? [
            {
              tool: "snapshot_restore",
              priority: 2,
              reason: "Restore if changes unwanted",
              args: {
                snapshotId
              }
            }
          ] : []
        });
      } catch (error) {
        const message = error instanceof Error ? error.message : String(error);
        return result(`Snapshot comparison failed: ${message}`, true);
      }
    }, "handleCompareSnapshots");
  }
});
var handleValidate;
var handleCheckPatterns;
var init_validation_handlers = __esm2({
  "src/facades/handlers/validation-handlers.ts"() {
    init_errors();
    init_intelligence();
    init_utils();
    handleValidate = /* @__PURE__ */ __name8(async (args, context) => {
      const { mode = "quick", code, filePath } = args;
      if (!code || !filePath) {
        return errorJsonResult(CommonErrors.missingParam("code, filePath"));
      }
      if (mode === "comprehensive") {
        try {
          const intel = getIntelligence2(context.workspaceRoot);
          const validation2 = await intel.checkPatterns([
            filePath
          ], {
            workspaceRoot: context.workspaceRoot
          });
          return jsonResult({
            status: "success",
            mode: "comprehensive",
            filePath,
            passed: validation2.overall.passed,
            confidence: validation2.overall.confidence,
            totalIssues: validation2.overall.totalIssues,
            recommendation: validation2.recommendation,
            layers: (validation2.layers || []).map((l) => {
              const issues = l.issues || [];
              return {
                name: l.layer || l.name,
                passed: l.passed,
                issueCount: issues.length,
                issues: issues.map((i) => ({
                  severity: i.severity,
                  type: i.type,
                  message: i.message,
                  line: i.line,
                  fix: i.fix
                })),
                duration: l.duration
              };
            }),
            focusPoints: validation2.focusPoints,
            message: validation2.overall.passed ? "All validation layers passed" : `Found ${validation2.overall.totalIssues} issue(s) across ${(validation2.layers || []).filter((l) => !l.passed).length} layer(s)`,
            next_actions: validation2.overall.passed ? [] : [
              {
                tool: "validate",
                priority: 1,
                reason: "Re-validate after fixes",
                args: {
                  mode: "comprehensive"
                }
              }
            ]
          });
        } catch (error) {
          const message = error instanceof Error ? error.message : String(error);
          return result(`Comprehensive validation failed: ${message}`, true);
        }
      }
      const validation = await getFacades().validation.checkPatterns(context.workspaceRoot, [
        filePath
      ]);
      const violations = validation.issues.map((issue) => ({
        rule: `${issue.rule || "unknown"}-check`,
        severity: issue.rule?.includes("error") ? "critical" : "warning",
        line: issue.line,
        message: issue.message
      }));
      const criticalCount = violations.filter((v) => v.severity === "critical").length;
      const warningCount = violations.filter((v) => v.severity === "warning").length;
      const confidence = criticalCount > 0 ? 30 : warningCount > 2 ? 60 : violations.length === 0 ? 100 : Math.max(70, 100 - warningCount * 10);
      const recommendation = confidence >= 95 ? "auto_merge" : confidence >= 70 ? "quick_review" : "full_review";
      return jsonResult({
        status: "success",
        mode: "quick",
        filePath,
        passed: criticalCount === 0,
        confidence,
        totalIssues: violations.length,
        violations,
        recommendation,
        message: violations.length === 0 ? "No violations found" : `${violations.length} violation(s) found (${criticalCount} critical, ${warningCount} warning)`,
        next_actions: violations.length > 0 ? [
          {
            tool: "validate",
            priority: 1,
            reason: "Fix violations and re-validate"
          }
        ] : []
      });
    }, "handleValidate");
    handleCheckPatterns = /* @__PURE__ */ __name8(async (args, context) => {
      const { code, filePath } = args;
      if (!code || !filePath) {
        return errorJsonResult(CommonErrors.missingParam("code, filePath"));
      }
      try {
        const intel = getIntelligence2(context.workspaceRoot);
        const validation = await intel.checkPatterns([
          filePath
        ], {
          workspaceRoot: context.workspaceRoot
        });
        return jsonResult({
          passed: validation.overall.passed,
          confidence: validation.overall.confidence,
          totalIssues: validation.overall.totalIssues,
          recommendation: validation.recommendation,
          layers: (validation.layers || []).map((l) => {
            const issues = l.issues || [];
            return {
              name: l.layer || l.name,
              passed: l.passed,
              issueCount: issues.length,
              issues: issues.map((i) => ({
                severity: i.severity,
                type: i.type,
                message: i.message,
                line: i.line,
                fix: i.fix
              })),
              duration: l.duration
            };
          }),
          focusPoints: validation.focusPoints,
          next_actions: validation.overall.passed ? [] : [
            {
              tool: "check_patterns",
              priority: 1,
              reason: "Re-validate after fixes"
            }
          ]
        });
      } catch (error) {
        const message = error instanceof Error ? error.message : String(error);
        return result(`Failed to check patterns: ${message}`, true);
      }
    }, "handleCheckPatterns");
  }
});
var handlePrepareWorkspace;
var init_workspace_handler = __esm2({
  "src/facades/handlers/workspace-handler.ts"() {
    init_local_service_adapter();
    init_utils();
    handlePrepareWorkspace = /* @__PURE__ */ __name8(async (_args, context) => {
      const snapshots = await listSnapshotsViaDaemon(context.workspaceRoot, 20);
      const vitalsResult = await getVitalsViaDaemon(context.workspaceRoot);
      const vitals = vitalsResult.success && vitalsResult.vitals ? vitalsResult.vitals : {
        pressure: 0,
        snapshotCount: 0
      };
      const lastSnapshot = snapshots[0];
      const timeSinceLastSnapshot = lastSnapshot ? Math.floor((Date.now() - new Date(lastSnapshot.createdAt).getTime()) / 6e4) : null;
      const pressure = vitals.pressure ?? 0;
      const protectionScore = Math.max(0, 100 - pressure);
      const shouldSnapshot = pressure >= 50 || snapshots.length === 0;
      const reason = pressure >= 50 ? "High pressure detected" : snapshots.length === 0 ? "No snapshots in this session" : "Workspace is healthy";
      const urgency = pressure >= 70 ? "high" : pressure >= 50 ? "medium" : "low";
      return jsonResult({
        vitals: {
          pressure,
          snapshotCount: vitals.snapshotCount ?? snapshots.length,
          trajectory: pressure >= 70 ? "critical" : pressure >= 50 ? "degrading" : "stable"
        },
        protectionScore,
        lastSnapshot: lastSnapshot ? {
          id: lastSnapshot.id,
          createdAt: new Date(lastSnapshot.createdAt).toISOString(),
          fileCount: lastSnapshot.fileCount,
          minutesAgo: timeSinceLastSnapshot
        } : null,
        totalSnapshots: snapshots.length,
        recommendation: {
          should: shouldSnapshot,
          reason,
          urgency
        },
        safeOperations: [],
        blockedOperations: [],
        suggestion: shouldSnapshot ? reason : "Continue working",
        next_actions: shouldSnapshot ? [
          {
            tool: "snapshot_create",
            priority: 1,
            reason
          }
        ] : []
      });
    }, "handlePrepareWorkspace");
  }
});
function getHandler(toolName) {
  return facadeHandlers[toolName];
}
__name(getHandler, "getHandler");
function hasHandler(toolName) {
  return toolName in facadeHandlers;
}
__name(hasHandler, "hasHandler");
function getRegisteredTools() {
  return Object.keys(facadeHandlers);
}
__name(getRegisteredTools, "getRegisteredTools");
var handleGetPairingProtocol;
var handleGetContext;
var facadeHandlers;
var init_registry = __esm2({
  "src/facades/handlers/registry.ts"() {
    init_begin_task2();
    init_complete_task();
    init_pairing_protocol();
    init_quick_check();
    init_review_work();
    init_what_changed();
    init_analyze_handler();
    init_cleanup_handler();
    init_context_handler();
    init_learning_handlers();
    init_lookup_exports_handler();
    init_meta_handlers();
    init_risk_handlers();
    init_session_handler();
    init_snapshot_handlers();
    init_validation_handlers();
    init_workspace_handler();
    handleGetPairingProtocol = /* @__PURE__ */ __name8(async (_args, context) => {
      const protocol = generatePairingProtocol(context.workspaceRoot);
      return {
        content: [
          {
            type: "text",
            text: JSON.stringify(protocol, null, 2)
          }
        ],
        isError: false
      };
    }, "handleGetPairingProtocol");
    handleGetContext = /* @__PURE__ */ __name8(async (_args, context) => {
      const summary = await getContextSummary(context.workspaceRoot);
      return {
        content: [
          {
            type: "text",
            text: JSON.stringify(summary, null, 2)
          }
        ],
        isError: false
      };
    }, "handleGetContext");
    facadeHandlers = {
      // Core handlers
      analyze: handleAnalyze,
      prepare_workspace: handlePrepareWorkspace,
      // Snapshot handlers
      snapshot_create: handleSnapshotCreate,
      snapshot_list: handleSnapshotList,
      snapshot_restore: handleSnapshotRestore,
      suggest_snapshot: handleSuggestSnapshot,
      compare_snapshots: handleCompareSnapshots,
      // Validation handlers
      validate: handleValidate,
      check_patterns: handleCheckPatterns,
      // Context and session handlers
      context: handleContext,
      session: handleSession,
      get_context: handleGetContext,
      // Learning handlers
      learn: handleLearn,
      get_learnings: handleGetLearnings,
      // Risk handlers
      acknowledge_risk: handleAcknowledgeRisk,
      report_violation: handleReportViolation,
      // Meta handlers
      meta: handleMeta,
      cleanup: handleCleanup,
      // Pair programmer composite tools
      begin_task: handleBeginTask,
      quick_check: handleQuickCheck,
      what_changed: handleWhatChanged,
      review_work: handleReviewWork,
      complete_task: handleCompleteTask,
      get_pairing_protocol: handleGetPairingProtocol,
      // Discovery handlers
      lookup_exports: handleLookupExports
    };
    __name8(getHandler, "getHandler");
    __name8(hasHandler, "hasHandler");
    __name8(getRegisteredTools, "getRegisteredTools");
  }
});
var handlers_exports = {};
__export2(handlers_exports, {
  TOOL_HIERARCHY: /* @__PURE__ */ __name(() => TOOL_HIERARCHY, "TOOL_HIERARCHY"),
  WORKFLOWS: /* @__PURE__ */ __name(() => WORKFLOWS, "WORKFLOWS"),
  errorJsonResult: /* @__PURE__ */ __name(() => errorJsonResult, "errorJsonResult"),
  facadeHandlers: /* @__PURE__ */ __name(() => facadeHandlers, "facadeHandlers"),
  getHandler: /* @__PURE__ */ __name(() => getHandler, "getHandler"),
  getRegisteredTools: /* @__PURE__ */ __name(() => getRegisteredTools, "getRegisteredTools"),
  handleAcknowledgeRisk: /* @__PURE__ */ __name(() => handleAcknowledgeRisk, "handleAcknowledgeRisk"),
  handleAnalyze: /* @__PURE__ */ __name(() => handleAnalyze, "handleAnalyze"),
  handleCheckPatterns: /* @__PURE__ */ __name(() => handleCheckPatterns, "handleCheckPatterns"),
  handleCleanup: /* @__PURE__ */ __name(() => handleCleanup, "handleCleanup"),
  handleCompareSnapshots: /* @__PURE__ */ __name(() => handleCompareSnapshots, "handleCompareSnapshots"),
  handleContext: /* @__PURE__ */ __name(() => handleContext, "handleContext"),
  handleGetContext: /* @__PURE__ */ __name(() => handleGetContext, "handleGetContext"),
  handleGetLearnings: /* @__PURE__ */ __name(() => handleGetLearnings, "handleGetLearnings"),
  handleLearn: /* @__PURE__ */ __name(() => handleLearn, "handleLearn"),
  handleLookupExports: /* @__PURE__ */ __name(() => handleLookupExports, "handleLookupExports"),
  handleMeta: /* @__PURE__ */ __name(() => handleMeta, "handleMeta"),
  handlePrepareWorkspace: /* @__PURE__ */ __name(() => handlePrepareWorkspace, "handlePrepareWorkspace"),
  handleReportViolation: /* @__PURE__ */ __name(() => handleReportViolation, "handleReportViolation"),
  handleSession: /* @__PURE__ */ __name(() => handleSession, "handleSession"),
  handleSnapshotCreate: /* @__PURE__ */ __name(() => handleSnapshotCreate, "handleSnapshotCreate"),
  handleSnapshotList: /* @__PURE__ */ __name(() => handleSnapshotList, "handleSnapshotList"),
  handleSnapshotRestore: /* @__PURE__ */ __name(() => handleSnapshotRestore, "handleSnapshotRestore"),
  handleSuggestSnapshot: /* @__PURE__ */ __name(() => handleSuggestSnapshot, "handleSuggestSnapshot"),
  handleValidate: /* @__PURE__ */ __name(() => handleValidate, "handleValidate"),
  hasHandler: /* @__PURE__ */ __name(() => hasHandler, "hasHandler"),
  isLocalWorkspace: /* @__PURE__ */ __name(() => isLocalWorkspace, "isLocalWorkspace"),
  jsonResult: /* @__PURE__ */ __name(() => jsonResult, "jsonResult"),
  listSnapshotsReadonly: /* @__PURE__ */ __name(() => listSnapshotsReadonly, "listSnapshotsReadonly"),
  result: /* @__PURE__ */ __name(() => result, "result")
});
var init_handlers = __esm2({
  "src/facades/handlers/index.ts"() {
    init_analyze_handler();
    init_cleanup_handler();
    init_context_handler();
    init_learning_handlers();
    init_lookup_exports_handler();
    init_meta_handlers();
    init_registry();
    init_risk_handlers();
    init_session_handler();
    init_snapshot_handlers();
    init_utils();
    init_validation_handlers();
    init_workspace_handler();
  }
});
async function validateWorkspaceHealth(workspaceRoot) {
  const checks = [];
  const autoFixable = [];
  const requiresUserAction = [];
  const workspaceExists = existsSync(workspaceRoot);
  checks.push({
    name: "workspace_exists",
    passed: workspaceExists,
    message: workspaceExists ? void 0 : `Workspace does not exist: ${workspaceRoot}`,
    severity: "error"
  });
  if (!workspaceExists) {
    return {
      healthy: false,
      checks,
      autoFixable,
      requiresUserAction: [
        {
          name: "workspace_exists",
          passed: false,
          message: `Workspace does not exist: ${workspaceRoot}`,
          severity: "error"
        }
      ],
      workspaceRoot,
      workspaceId: ""
    };
  }
  const vrekoDir2 = join(workspaceRoot, ".vreko");
  const vrekoExists = existsSync(vrekoDir2);
  checks.push({
    name: "vreko_dir",
    passed: vrekoExists,
    message: vrekoExists ? void 0 : ".vreko directory missing",
    severity: "warning"
  });
  if (!vrekoExists) {
    autoFixable.push({
      name: "vreko_dir",
      passed: false,
      message: ".vreko directory missing - will auto-create",
      severity: "warning"
    });
  }
  const requiredDirs = [
    "learnings",
    "snapshots",
    "audit"
  ];
  for (const dir of requiredDirs) {
    const dirPath = join(vrekoDir2, dir);
    const dirExists = existsSync(dirPath);
    checks.push({
      name: `subdir_${dir}`,
      passed: dirExists,
      message: dirExists ? void 0 : `${dir}/ subdirectory missing`,
      severity: "info"
    });
    if (!dirExists && vrekoExists) {
      autoFixable.push({
        name: `subdir_${dir}`,
        passed: false,
        message: `${dir}/ subdirectory will be auto-created`,
        severity: "info"
      });
    }
  }
  const workspaceId = generateWorkspaceId(workspaceRoot);
  const workspaceIdValid = workspaceId.startsWith("ws_") && workspaceId.length >= 32;
  checks.push({
    name: "workspace_id",
    passed: workspaceIdValid,
    message: workspaceIdValid ? void 0 : `Invalid workspace ID format: ${workspaceId}`,
    severity: "error"
  });
  if (!workspaceIdValid) {
    requiresUserAction.push({
      name: "workspace_id",
      passed: false,
      message: `Invalid workspace ID format: ${workspaceId}`,
      severity: "error"
    });
  }
  const hasErrors = checks.some((c) => !c.passed && c.severity === "error");
  const hasWarnings = checks.some((c) => !c.passed && c.severity === "warning");
  return {
    healthy: !hasErrors && !hasWarnings,
    checks,
    autoFixable,
    requiresUserAction,
    workspaceRoot,
    workspaceId
  };
}
__name(validateWorkspaceHealth, "validateWorkspaceHealth");
async function repairWorkspaceIssues(workspaceRoot, issues) {
  let fixed = 0;
  const remaining = [];
  for (const issue of issues) {
    try {
      switch (issue.name) {
        case "vreko_dir": {
          const vrekoDir2 = join(workspaceRoot, ".vreko");
          mkdirSync(vrekoDir2, {
            recursive: true
          });
          fixed++;
          break;
        }
        case "subdir_learnings":
        case "subdir_snapshots":
        case "subdir_audit": {
          const dirName = issue.name.replace("subdir_", "");
          const dirPath = join(workspaceRoot, ".vreko", dirName);
          mkdirSync(dirPath, {
            recursive: true
          });
          fixed++;
          break;
        }
        default:
          remaining.push(issue);
      }
    } catch (error) {
      remaining.push({
        ...issue,
        message: `Failed to repair: ${error instanceof Error ? error.message : String(error)}`
      });
    }
  }
  return {
    success: remaining.length === 0,
    fixed,
    remaining
  };
}
__name(repairWorkspaceIssues, "repairWorkspaceIssues");
async function checkWorkspaceHealth(workspaceRoot) {
  return validateWorkspaceHealth(workspaceRoot);
}
__name(checkWorkspaceHealth, "checkWorkspaceHealth");
var init_integration = __esm2({
  "src/health/integration.ts"() {
    __name8(validateWorkspaceHealth, "validateWorkspaceHealth");
    __name8(repairWorkspaceIssues, "repairWorkspaceIssues");
    __name8(checkWorkspaceHealth, "checkWorkspaceHealth");
  }
});
async function handleWorkspaceHealth(args, context) {
  try {
    const health = await checkWorkspaceHealth(context.workspaceRoot);
    let checks = health.checks;
    if (args.check && args.check.length > 0) {
      checks = checks.filter((c) => args.check?.includes(c.name));
    }
    const failedChecks = checks.filter((c) => !c.passed);
    const output = {
      healthy: health.healthy,
      workspaceRoot: health.workspaceRoot,
      workspaceId: health.workspaceId,
      issues: failedChecks.map((c) => ({
        name: c.name,
        message: c.message || "Check failed",
        severity: c.severity
      })),
      canAutoFix: health.autoFixable.length > 0,
      autoFixableCount: health.autoFixable.length,
      totalChecks: checks.length,
      passedChecks: checks.filter((c) => c.passed).length
    };
    return {
      content: [
        {
          type: "text",
          text: JSON.stringify(output, null, 2)
        }
      ],
      isError: false
    };
  } catch (error) {
    const message = error instanceof Error ? error.message : String(error);
    return {
      content: [
        {
          type: "text",
          text: JSON.stringify({
            error: "HEALTH_CHECK_FAILED",
            message: `Failed to check workspace health: ${message}`
          }, null, 2)
        }
      ],
      isError: true
    };
  }
}
__name(handleWorkspaceHealth, "handleWorkspaceHealth");
var init_tool = __esm2({
  "src/health/tool.ts"() {
    init_integration();
    __name8(handleWorkspaceHealth, "handleWorkspaceHealth");
  }
});
function containsInjectionPattern(input) {
  for (const pattern of INJECTION_PATTERNS) {
    if (pattern.test(input)) {
      return {
        detected: true,
        pattern: pattern.source
      };
    }
  }
  return {
    detected: false
  };
}
__name(containsInjectionPattern, "containsInjectionPattern");
function validateStructure(input) {
  const repeatedMatch = input.match(/(.)\1{10,}/);
  if (repeatedMatch) {
    return {
      valid: false,
      reason: `Excessive repeated character: "${repeatedMatch[1]}"`
    };
  }
  let depth = 0;
  let maxDepth = 0;
  for (const char of input) {
    if ("([{<".includes(char)) {
      depth++;
      maxDepth = Math.max(maxDepth, depth);
    } else if (")]}>".includes(char)) {
      depth--;
    }
    if (depth < 0) {
      return {
        valid: false,
        reason: "Unbalanced brackets"
      };
    }
  }
  if (depth !== 0) {
    return {
      valid: false,
      reason: "Unbalanced brackets"
    };
  }
  if (maxDepth > STRUCTURAL_CHECKS.maxNestingDepth) {
    return {
      valid: false,
      reason: `Nesting depth ${maxDepth} exceeds max ${STRUCTURAL_CHECKS.maxNestingDepth}`
    };
  }
  const uniqueChars = new Set(input).size;
  const ratio = uniqueChars / Math.max(input.length, 1);
  if (input.length > 50 && ratio < STRUCTURAL_CHECKS.minUniqueCharRatio) {
    return {
      valid: false,
      reason: "Suspiciously low character diversity"
    };
  }
  return {
    valid: true
  };
}
__name(validateStructure, "validateStructure");
function normalizeContent(input) {
  return input.replace(/[\u200B-\u200D\u2060\uFEFF]/g, "").replace(/[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]/g, "").replace(/[ \t]+/g, " ").replace(/\n{3,}/g, "\n\n").trim();
}
__name(normalizeContent, "normalizeContent");
function validateSnapLearnParams(params) {
  const result7 = SnapLearnParamsSchema.safeParse(params);
  if (result7.success) {
    return {
      success: true,
      data: result7.data
    };
  }
  return {
    success: false,
    errors: result7.error.errors.map((e) => `${e.path.join(".")}: ${e.message}`)
  };
}
__name(validateSnapLearnParams, "validateSnapLearnParams");
function validateSnapViolationParams(params) {
  const result7 = SnapViolationParamsSchema.safeParse(params);
  if (result7.success) {
    return {
      success: true,
      data: result7.data
    };
  }
  return {
    success: false,
    errors: result7.error.errors.map((e) => `${e.path.join(".")}: ${e.message}`)
  };
}
__name(validateSnapViolationParams, "validateSnapViolationParams");
function validateSnapStartParams(params) {
  const result7 = SnapStartParamsSchema.safeParse(params);
  if (result7.success) {
    return {
      success: true,
      data: result7.data
    };
  }
  return {
    success: false,
    errors: result7.error.errors.map((e) => `${e.path.join(".")}: ${e.message}`)
  };
}
__name(validateSnapStartParams, "validateSnapStartParams");
function validateCheckParams(params) {
  const result7 = CheckParamsSchema.safeParse(params);
  if (result7.success) {
    return {
      success: true,
      data: result7.data
    };
  }
  return {
    success: false,
    errors: result7.error.errors.map((e) => `${e.path.join(".")}: ${e.message}`)
  };
}
__name(validateCheckParams, "validateCheckParams");
function validateGuideParams(params) {
  const result7 = GuideParamsSchema.safeParse(params);
  if (result7.success) {
    return {
      success: true,
      data: result7.data
    };
  }
  return {
    success: false,
    errors: result7.error.errors.map((e) => `${e.path.join(".")}: ${e.message}`)
  };
}
__name(validateGuideParams, "validateGuideParams");
function validateSafeToWriteParams(params) {
  const result7 = SafeToWriteParamsSchema.safeParse(params);
  if (result7.success) {
    return {
      success: true,
      data: result7.data
    };
  }
  return {
    success: false,
    errors: result7.error.errors.map((e) => `${e.path.join(".")}: ${e.message}`)
  };
}
__name(validateSafeToWriteParams, "validateSafeToWriteParams");
function validateSnapEndParams(params) {
  const result7 = SnapEndParamsSchema.safeParse(params);
  if (result7.success) {
    return {
      success: true,
      data: result7.data
    };
  }
  return {
    success: false,
    errors: result7.error.errors.map((e) => `${e.path.join(".")}: ${e.message}`)
  };
}
__name(validateSnapEndParams, "validateSnapEndParams");
function validateValidateChangeParams(params) {
  const result7 = ValidateChangeParamsSchema.safeParse(params);
  if (result7.success) {
    return {
      success: true,
      data: result7.data
    };
  }
  return {
    success: false,
    errors: result7.error.errors.map((e) => `${e.path.join(".")}: ${e.message}`)
  };
}
__name(validateValidateChangeParams, "validateValidateChangeParams");
function validateGetRiskScoreParams(params) {
  const result7 = GetRiskScoreParamsSchema.safeParse(params);
  if (result7.success) {
    return {
      success: true,
      data: result7.data
    };
  }
  return {
    success: false,
    errors: result7.error.errors.map((e) => `${e.path.join(".")}: ${e.message}`)
  };
}
__name(validateGetRiskScoreParams, "validateGetRiskScoreParams");
function validateGetContextParams(params) {
  const result7 = GetContextParamsSchema.safeParse(params);
  if (result7.success) {
    return {
      success: true,
      data: result7.data
    };
  }
  return {
    success: false,
    errors: result7.error.errors.map((e) => `${e.path.join(".")}: ${e.message}`)
  };
}
__name(validateGetContextParams, "validateGetContextParams");
function validateSuggestRollbackParams(params) {
  const result7 = SuggestRollbackParamsSchema.safeParse(params);
  if (result7.success) {
    return {
      success: true,
      data: result7.data
    };
  }
  return {
    success: false,
    errors: result7.error.errors.map((e) => `${e.path.join(".")}: ${e.message}`)
  };
}
__name(validateSuggestRollbackParams, "validateSuggestRollbackParams");
function validateQueryPatternsParams(params) {
  const result7 = QueryPatternsParamsSchema.safeParse(params);
  if (result7.success) {
    return {
      success: true,
      data: result7.data
    };
  }
  return {
    success: false,
    errors: result7.error.errors.map((e) => `${e.path.join(".")}: ${e.message}`)
  };
}
__name(validateQueryPatternsParams, "validateQueryPatternsParams");
var MAX_LENGTHS;
var INJECTION_PATTERNS;
var STRUCTURAL_CHECKS;
var sanitizedString;
var LearningTypeSchema2;
var SourceSchema;
var SnapLearnParamsSchema;
var SnapViolationParamsSchema;
var SnapStartParamsSchema;
var FilePathSchema;
var CheckModeSchema;
var CheckParamsSchema;
var GuideIntentSchema;
var GuideParamsSchema;
var SafeToWriteToolSchema;
var SafeToWriteParamsSchema;
var SnapEndOutcomeSchema;
var SnapEndParamsSchema;
var LanguageSchema;
var ValidateChangeParamsSchema;
var GetRiskScoreParamsSchema;
var GetContextParamsSchema;
var SuggestRollbackParamsSchema;
var QueryPatternsParamsSchema;
var init_input_sanitization = __esm2({
  "src/security/input-sanitization.ts"() {
    MAX_LENGTHS = {
      trigger: 500,
      action: 2e3,
      task: 500,
      description: 2e3,
      reason: 1e3,
      prevention: 1e3,
      source: 100,
      type: 50,
      file: 500,
      keyword: 100
    };
    INJECTION_PATTERNS = [
      // System/role injection attempts
      /\b(system|assistant|user|human|ai)\s*:/i,
      /\[\s*(system|inst|instructions?)\s*\]/i,
      /<\s*(system|inst|instructions?)\s*>/i,
      // XML/tag injection (common in prompt injection)
      // Match tags with content/attributes: <tag>content</tag>
      // Exclude TypeScript generics: Promise<void>, Array<T>
      /<(?!(?:void|string|number|boolean|any|unknown|never|object|null|undefined|T|K|V)\s*>)[a-z_-]+(?:\s[^>]*)?>[^<]*<\//i,
      // Event handler injection (XSS via attributes)
      /\bon(?:error|load|click|mouse|key|focus|blur|change|submit|reset|select|input)\s*=/i,
      // Instruction override attempts
      /\b(ignore|disregard|forget)\s+(previous|above|all|prior)\s+(instructions?|context|rules?)/i,
      /\bnew\s+(instructions?|rules?|context)\s*:/i,
      /\boverride\s*:/i,
      // Role hijacking
      /\b(you\s+are|act\s+as|pretend\s+to\s+be|roleplay\s+as)\b/i,
      /\b(from\s+now\s+on|starting\s+now)\b.*\b(you|your)\b/i,
      // Code execution attempts
      /\$\{.*\}/s,
      /\{\{.*\}\}/s,
      /<%.*%>/s,
      /`[^`]*`/s,
      // Control character injection
      /[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]/,
      // Zero-width character obfuscation
      /[\u200B-\u200D\u2060\uFEFF]/,
      // Unicode homoglyph detection (Cyrillic lookalikes for common injection words)
      /[\u0400-\u04FF]/,
      // Multi-line delimiter injection
      /^---+\s*$/m,
      /^===+\s*$/m,
      /^###\s*(system|context|instructions?)/im
    ];
    STRUCTURAL_CHECKS = {
      /** Max consecutive repeated characters (catches spam/padding) */
      maxRepeatedChars: 10,
      /** Max nesting depth for brackets/parens */
      maxNestingDepth: 5,
      /** Minimum unique character ratio (catches encoding tricks) */
      minUniqueCharRatio: 0.1
    };
    __name8(containsInjectionPattern, "containsInjectionPattern");
    __name8(validateStructure, "validateStructure");
    __name8(normalizeContent, "normalizeContent");
    sanitizedString = /* @__PURE__ */ __name8((maxLength, fieldName) => z.string().max(maxLength, {
      message: `${fieldName} exceeds max length of ${maxLength}`
    }).transform(normalizeContent).refine((val) => {
      const injection = containsInjectionPattern(val);
      return !injection.detected;
    }, {
      message: `${fieldName} contains potentially malicious content`
    }).refine((val) => {
      const structure = validateStructure(val);
      return structure.valid;
    }, {
      message: `${fieldName} has invalid structure`
    }), "sanitizedString");
    LearningTypeSchema2 = z.enum([
      "pattern",
      "pitfall",
      "efficiency",
      "discovery",
      "workflow",
      "constraint",
      "preference",
      "pat",
      "pit",
      "eff",
      "disc",
      "wf",
      "con",
      "prf"
    ]);
    SourceSchema = z.enum([
      "snap_learn",
      "snap_violation",
      "manual",
      "survey",
      "curated",
      "violation"
    ]).default("manual");
    SnapLearnParamsSchema = z.object({
      // Full names
      trigger: sanitizedString(MAX_LENGTHS.trigger, "trigger").optional(),
      action: sanitizedString(MAX_LENGTHS.action, "action").optional(),
      type: LearningTypeSchema2.optional(),
      source: SourceSchema.optional(),
      // Short aliases
      t: sanitizedString(MAX_LENGTHS.trigger, "trigger").optional(),
      a: sanitizedString(MAX_LENGTHS.action, "action").optional(),
      s: SourceSchema.optional()
    });
    SnapViolationParamsSchema = z.object({
      // Full names
      type: sanitizedString(MAX_LENGTHS.type, "type").optional(),
      file: sanitizedString(MAX_LENGTHS.file, "file").optional(),
      description: sanitizedString(MAX_LENGTHS.description, "description").optional(),
      reason: sanitizedString(MAX_LENGTHS.reason, "reason").optional(),
      prevention: sanitizedString(MAX_LENGTHS.prevention, "prevention").optional(),
      // Short aliases
      what: sanitizedString(MAX_LENGTHS.description, "what").optional(),
      why: sanitizedString(MAX_LENGTHS.reason, "why").optional(),
      prevent: sanitizedString(MAX_LENGTHS.prevention, "prevent").optional()
    });
    SnapStartParamsSchema = z.object({
      // Full names
      mode: z.enum([
        "start",
        "check",
        "context",
        "s",
        "c",
        "x"
      ]).optional(),
      task: sanitizedString(MAX_LENGTHS.task, "task").optional(),
      files: z.array(sanitizedString(MAX_LENGTHS.file, "file")).max(100).optional(),
      keywords: z.array(sanitizedString(MAX_LENGTHS.keyword, "keyword")).max(50).optional(),
      intent: z.enum([
        "implement",
        "debug",
        "refactor",
        "review",
        "explore"
      ]).optional(),
      // V2 only: no shorthand aliases accepted (removed m, t, f per spec 1.1)
      // k and i retained for keyword/intent shorthands during transition
      k: z.array(sanitizedString(MAX_LENGTHS.keyword, "keyword")).max(50).optional(),
      i: z.enum([
        "implement",
        "debug",
        "refactor",
        "review",
        "explore"
      ]).optional()
    });
    FilePathSchema = z.string().max(MAX_LENGTHS.file).refine((path4) => {
      if (path4.startsWith("/") || /^[A-Za-z]:/.test(path4)) {
        return false;
      }
      if (path4.includes("..")) {
        return false;
      }
      if (path4.includes("\0")) {
        return false;
      }
      return true;
    }, {
      message: "Invalid file path"
    });
    __name8(validateSnapLearnParams, "validateSnapLearnParams");
    __name8(validateSnapViolationParams, "validateSnapViolationParams");
    __name8(validateSnapStartParams, "validateSnapStartParams");
    CheckModeSchema = z.enum([
      "q",
      "f",
      "p",
      "b",
      "i",
      "c",
      "d",
      "quick",
      "full",
      "pattern",
      "build",
      "impact",
      "circular",
      "doc"
    ]);
    CheckParamsSchema = z.object({
      // Mode with strict whitelist (no arbitrary strings)
      mode: CheckModeSchema.optional(),
      m: CheckModeSchema.optional(),
      // Files array with path validation
      files: z.array(FilePathSchema).max(50).optional(),
      f: z.array(FilePathSchema).max(50).optional(),
      // Diff content - limited length, structural validation
      diff: sanitizedString(MAX_LENGTHS.description * 5, "diff").optional(),
      // Code content - larger limit but still bounded
      code: sanitizedString(MAX_LENGTHS.description * 10, "code").optional()
    });
    GuideIntentSchema = z.enum([
      "implement",
      "debug",
      "refactor",
      "review",
      "explore",
      "test",
      "document",
      "optimize",
      "migrate",
      "secure"
    ]);
    GuideParamsSchema = z.object({
      // Intent with strict whitelist
      intent: GuideIntentSchema.optional(),
      i: GuideIntentSchema.optional(),
      // Task description - sanitized for injection
      task: sanitizedString(MAX_LENGTHS.task, "task").optional(),
      t: sanitizedString(MAX_LENGTHS.task, "task").optional(),
      // Files array with path validation
      files: z.array(FilePathSchema).max(100).optional(),
      f: z.array(FilePathSchema).max(100).optional(),
      // Keywords - limited count and length per keyword
      keywords: z.array(sanitizedString(MAX_LENGTHS.keyword, "keyword")).max(50).optional(),
      k: z.array(sanitizedString(MAX_LENGTHS.keyword, "keyword")).max(50).optional()
    });
    SafeToWriteToolSchema = z.enum([
      "Write",
      "Edit",
      "MultiEdit",
      "NotebookEdit",
      "write",
      "edit",
      "multiedit",
      "notebookedit"
    ]);
    SafeToWriteParamsSchema = z.object({
      // File path with traversal protection
      filePath: FilePathSchema,
      file: FilePathSchema.optional(),
      f: FilePathSchema.optional(),
      // Proposed diff - larger limit for actual content changes
      proposedDiff: sanitizedString(MAX_LENGTHS.description * 10, "proposedDiff").optional(),
      diff: sanitizedString(MAX_LENGTHS.description * 10, "diff").optional(),
      // Tool whitelist - prevents arbitrary tool injection
      tool: SafeToWriteToolSchema.optional(),
      // Intent - reuse guide intent schema
      intent: GuideIntentSchema.optional()
    });
    SnapEndOutcomeSchema = sanitizedString(2e3, "outcome");
    SnapEndParamsSchema = z.object({
      // Success flag - boolean only
      ok: z.union([
        z.literal(0),
        z.literal(1),
        z.boolean()
      ]).optional(),
      // Outcome with strict whitelist
      outcome: SnapEndOutcomeSchema.optional(),
      // Learnings array - each item sanitized
      learnings: z.array(sanitizedString(MAX_LENGTHS.action, "learning")).max(20).optional(),
      l: z.array(sanitizedString(MAX_LENGTHS.action, "learning")).max(20).optional(),
      // Summary - sanitized
      summary: sanitizedString(MAX_LENGTHS.description, "summary").optional()
    });
    z.object({
      // Query string - sanitized for injection
      query: sanitizedString(MAX_LENGTHS.task, "query").optional(),
      q: sanitizedString(MAX_LENGTHS.task, "query").optional(),
      // File path with validation
      filePath: FilePathSchema.optional(),
      file: FilePathSchema.optional(),
      f: FilePathSchema.optional(),
      // Content for analysis - larger limit
      content: sanitizedString(MAX_LENGTHS.description * 5, "content").optional(),
      // Scope whitelist
      scope: z.enum([
        "file",
        "module",
        "project",
        "workspace"
      ]).optional()
    });
    z.object({
      // Pattern type whitelist
      type: z.enum([
        "co-change",
        "fragile",
        "hotspot",
        "coupling",
        "cochange",
        "hotspots",
        "dependencies"
      ]).optional(),
      // Files to analyze
      files: z.array(FilePathSchema).max(100).optional(),
      // Threshold values - bounded numbers
      threshold: z.number().min(0).max(1).optional(),
      limit: z.number().min(1).max(1e3).optional()
    });
    __name8(validateCheckParams, "validateCheckParams");
    __name8(validateGuideParams, "validateGuideParams");
    __name8(validateSafeToWriteParams, "validateSafeToWriteParams");
    __name8(validateSnapEndParams, "validateSnapEndParams");
    LanguageSchema = z.enum([
      "typescript",
      "javascript",
      "python",
      "go",
      "rust",
      "java",
      "ruby",
      "php",
      "csharp",
      "cpp",
      "c",
      "swift",
      "kotlin",
      "unknown"
    ]);
    ValidateChangeParamsSchema = z.object({
      filePath: FilePathSchema,
      content: sanitizedString(MAX_LENGTHS.description * 50, "content"),
      previousContent: sanitizedString(MAX_LENGTHS.description * 50, "previousContent").optional(),
      language: LanguageSchema.optional()
    });
    GetRiskScoreParamsSchema = z.object({
      filePath: FilePathSchema,
      content: sanitizedString(MAX_LENGTHS.description * 50, "content").optional()
    });
    GetContextParamsSchema = z.object({
      filePath: FilePathSchema,
      sources: z.array(z.enum([
        "github",
        "context7",
        "sentry"
      ])).max(3).optional()
    });
    SuggestRollbackParamsSchema = z.object({
      filePath: FilePathSchema,
      symptom: sanitizedString(MAX_LENGTHS.description, "symptom").optional()
    });
    QueryPatternsParamsSchema = z.object({
      query: sanitizedString(MAX_LENGTHS.task, "query"),
      filePath: FilePathSchema.optional(),
      timeRange: z.enum([
        "7d",
        "30d",
        "90d"
      ]).optional(),
      limit: z.number().min(1).max(100).optional()
    });
    __name8(validateValidateChangeParams, "validateValidateChangeParams");
    __name8(validateGetRiskScoreParams, "validateGetRiskScoreParams");
    __name8(validateGetContextParams, "validateGetContextParams");
    __name8(validateSuggestRollbackParams, "validateSuggestRollbackParams");
    __name8(validateQueryPatternsParams, "validateQueryPatternsParams");
  }
});
function getDaemonClient2(workspaceRoot) {
  return createServiceClient2(workspaceRoot);
}
__name(getDaemonClient2, "getDaemonClient");
var init_daemon = __esm2({
  "src/tools/v2/utils/daemon.ts"() {
    init_local_service_adapter();
    __name8(getDaemonClient2, "getDaemonClient");
  }
});
function buildSessionCurrentParams(workspace) {
  return {
    workspacePath: workspace
  };
}
__name(buildSessionCurrentParams, "buildSessionCurrentParams");
function buildSessionStartParams(workspace, input) {
  return {
    workspacePath: workspace,
    ...input.task ? {
      task: input.task
    } : {},
    ...input.files ? {
      files: input.files
    } : {},
    ...input.keywords ? {
      keywords: input.keywords
    } : {}
  };
}
__name(buildSessionStartParams, "buildSessionStartParams");
function buildSessionReviewParams(workspace, input) {
  return {
    workspacePath: workspace,
    ...input.sessionId ? {
      sessionId: input.sessionId
    } : {},
    ...input.files ? {
      files: input.files
    } : {}
  };
}
__name(buildSessionReviewParams, "buildSessionReviewParams");
function buildLearningSearchParams(workspace, query, limit) {
  return {
    workspace,
    keywords: tokenizeSearchQuery(query),
    ...limit !== void 0 ? {
      limit
    } : {}
  };
}
__name(buildLearningSearchParams, "buildLearningSearchParams");
function tokenizeSearchQuery(query) {
  return query.toLowerCase().replace(/[^a-z0-9\s]/g, " ").split(/\s+/).map((part) => part.trim()).filter((part, index, all) => part.length > 1 && all.indexOf(part) === index).slice(0, 10);
}
__name(tokenizeSearchQuery, "tokenizeSearchQuery");
function mapCoChangePairs(response) {
  if (!response?.items) return [];
  return response.items.map((item) => ({
    source: item.fileA,
    target: item.fileB,
    confidence: item.frequency
  }));
}
__name(mapCoChangePairs, "mapCoChangePairs");
var init_daemon_contracts = __esm2({
  "src/tools/v2/utils/daemon-contracts.ts"() {
    __name8(buildSessionCurrentParams, "buildSessionCurrentParams");
    __name8(buildSessionStartParams, "buildSessionStartParams");
    __name8(buildSessionReviewParams, "buildSessionReviewParams");
    __name8(buildLearningSearchParams, "buildLearningSearchParams");
    __name8(tokenizeSearchQuery, "tokenizeSearchQuery");
    __name8(mapCoChangePairs, "mapCoChangePairs");
  }
});
function classifyDaemonError(error) {
  if (error === null || error === void 0) {
    return "null_response";
  }
  if (error instanceof Error) {
    const message = error.message.toLowerCase();
    const code = error.code;
    if (code === -32602) {
      return "schema_error";
    }
    if (message.includes("enoent") || message.includes("no such file")) {
      return "not_running";
    }
    if (message.includes("econnrefused") || message.includes("connection refused")) {
      return "connection_refused";
    }
    if (message.includes("timeout") || message.includes("etimedout") || message.includes("timed out")) {
      return "timeout";
    }
    if (message.includes("invalid") || message.includes("parse") || message.includes("json")) {
      return "protocol_error";
    }
    if (message.includes("socket") || message.includes("daemon") || message.includes("unavailable")) {
      return "not_running";
    }
  }
  return "unknown";
}
__name(classifyDaemonError, "classifyDaemonError");
function isDaemonConnectionError(error) {
  const category = classifyDaemonError(error);
  return category === "not_running" || category === "connection_refused";
}
__name(isDaemonConnectionError, "isDaemonConnectionError");
function isDaemonNullResponse(result7) {
  return result7 === null || result7 === void 0;
}
__name(isDaemonNullResponse, "isDaemonNullResponse");
function formatDegradedModeResponse(mainContent, options) {
  const sections = [];
  sections.push(mainContent);
  sections.push("\n---");
  sections.push("\u26A0\uFE0F **DEGRADED MODE**: Running with limited functionality.");
  if (options.featuresAvailable.length > 0) {
    sections.push("\n**Working:**");
    for (const feature of options.featuresAvailable) {
      sections.push(`  \u2713 ${feature}`);
    }
  }
  if (options.featuresUnavailable.length > 0) {
    sections.push("\n**Unavailable:**");
    for (const feature of options.featuresUnavailable) {
      sections.push(`  \u2717 ${feature}`);
    }
  }
  const fix = getFixSuggestion(options.reason);
  sections.push(`
**To restore full functionality:** ${fix}`);
  return {
    content: [
      {
        type: "text",
        text: sections.join("\n")
      }
    ],
    isError: false
  };
}
__name(formatDegradedModeResponse, "formatDegradedModeResponse");
function getFixSuggestion(category) {
  switch (category) {
    case "not_running":
      return "Run `vreko daemon start` in your terminal.";
    case "connection_refused":
      return "The daemon may be starting up. Wait a moment and try again, or run `vreko daemon restart`.";
    case "timeout":
      return "The daemon is slow to respond. Try `vreko daemon restart` if this persists.";
    case "null_response":
      return "The daemon encountered an internal error. Try `vreko daemon restart`.";
    case "protocol_error":
      return "Daemon returned an unexpected response. Run `vreko daemon restart` to restore functionality.";
    case "schema_error":
      return "Parameter mismatch between CLI and daemon. Run `vreko daemon restart`. If it persists, update with `npm update -g @vreko/cli`.";
    default:
      return "Run `vreko daemon restart` to restore functionality.";
  }
}
__name(getFixSuggestion, "getFixSuggestion");
function formatDaemonError(category) {
  const cat = category ?? "not_running";
  const fix = getFixSuggestion(cat);
  const messages2 = {
    not_running: "Vreko daemon is not running. Your files are NOT protected until the daemon is running.",
    connection_refused: "Vreko daemon is starting up or busy. Your files may not be fully protected.",
    timeout: "Vreko daemon is not responding. Your files may not be fully protected during this time.",
    null_response: "Vreko daemon encountered an internal error. Some features may be unavailable.",
    protocol_error: "Vreko daemon returned an unexpected response. A restart usually resolves this.",
    schema_error: "Vreko daemon schema mismatch: the CLI and daemon disagree on method parameters. Restart to resync.",
    unknown: "Vreko daemon connection failed. Your files are NOT protected until this is resolved."
  };
  return {
    content: [
      {
        type: "text",
        text: `${messages2[cat]}

**Fix:** ${fix}`
      }
    ],
    isError: true
  };
}
__name(formatDaemonError, "formatDaemonError");
function formatUnexpectedError(context, error) {
  const category = classifyDaemonError(error);
  const fix = getFixSuggestion(category);
  return {
    content: [
      {
        type: "text",
        text: `Vreko encountered an unexpected error during ${context}.

**Fix:** ${fix}`
      }
    ],
    isError: true
  };
}
__name(formatUnexpectedError, "formatUnexpectedError");
async function checkDaemonHealth(daemon, workspace) {
  if (!daemon.isAvailable()) {
    return {
      healthy: false,
      degraded: true,
      reason: "not_running",
      message: "Daemon is not running",
      suggestions: [
        "Run `vreko daemon start` to enable file protection",
        "All tools will work in degraded mode without the daemon"
      ]
    };
  }
  try {
    const result7 = await daemon.request("health/ping", {
      workspace
    });
    if (result7 === null) {
      return {
        healthy: false,
        degraded: true,
        reason: "null_response",
        message: "Daemon returned no response",
        suggestions: [
          "The daemon may be initializing",
          "Wait a moment and retry, or run `vreko daemon restart`"
        ]
      };
    }
    resetCircuitBreaker(workspace);
    return {
      healthy: true,
      degraded: false,
      reason: "connected",
      message: "Daemon is healthy",
      suggestions: []
    };
  } catch (error) {
    const category = classifyDaemonError(error);
    const reasonMap = {
      not_running: "not_running",
      connection_refused: "connection_failed",
      timeout: "timeout",
      null_response: "null_response",
      protocol_error: "connection_failed",
      schema_error: "connection_failed",
      unknown: "connection_failed"
    };
    return {
      healthy: false,
      degraded: true,
      reason: reasonMap[category],
      message: `Daemon health check failed: ${category}`,
      suggestions: [
        getFixSuggestion(category)
      ]
    };
  }
}
__name(checkDaemonHealth, "checkDaemonHealth");
var init_errors2 = __esm2({
  "src/tools/v2/utils/errors.ts"() {
    init_intelligence_adapter();
    __name8(classifyDaemonError, "classifyDaemonError");
    __name8(isDaemonConnectionError, "isDaemonConnectionError");
    __name8(isDaemonNullResponse, "isDaemonNullResponse");
    __name8(formatDegradedModeResponse, "formatDegradedModeResponse");
    __name8(getFixSuggestion, "getFixSuggestion");
    __name8(formatDaemonError, "formatDaemonError");
    __name8(formatUnexpectedError, "formatUnexpectedError");
    __name8(checkDaemonHealth, "checkDaemonHealth");
  }
});
function isActiveSession2(session) {
  if (!session) {
    return false;
  }
  return session.active === true || session.state === "active" || session.state === "started";
}
__name(isActiveSession2, "isActiveSession");
var init_session = __esm2({
  "src/tools/v2/utils/session.ts"() {
    __name8(isActiveSession2, "isActiveSession");
  }
});
function normalizeWorkspacePath(p) {
  return resolve(p).replace(/\/+$/, "");
}
__name(normalizeWorkspacePath, "normalizeWorkspacePath");
function resolveWorkspace(context) {
  const workspaceRoot = context.workspaceRoot;
  if (!workspaceRoot) {
    throw new Error("Workspace root not available in tool context");
  }
  return normalizeWorkspacePath(workspaceRoot);
}
__name(resolveWorkspace, "resolveWorkspace");
var init_workspace2 = __esm2({
  "src/tools/v2/utils/workspace.ts"() {
    __name8(normalizeWorkspacePath, "normalizeWorkspacePath");
    __name8(resolveWorkspace, "resolveWorkspace");
  }
});
var init_utils3 = __esm2({
  "src/tools/v2/utils/index.ts"() {
    init_daemon();
    init_daemon_contracts();
    init_errors2();
    init_session();
    init_workspace2();
  }
});
function getPendingPath(workspacePath) {
  return join(workspacePath, ".vreko", "learnings", "pending.jsonl");
}
__name(getPendingPath, "getPendingPath");
function getArchivePath(workspacePath) {
  return join(workspacePath, ".vreko", "learnings", "pending.archive.jsonl");
}
__name(getArchivePath, "getArchivePath");
function getDeadLetterPath(workspacePath) {
  return join(workspacePath, ".vreko", "learnings", "dead-letter.jsonl");
}
__name(getDeadLetterPath, "getDeadLetterPath");
function appendToPendingQueue(workspacePath, entry) {
  const queuePath = getPendingPath(workspacePath);
  const archivePath = getArchivePath(workspacePath);
  mkdirSync(dirname(queuePath), {
    recursive: true
  });
  if (existsSync(queuePath)) {
    const stat2 = statSync(queuePath);
    const capHit = stat2.size >= MAX_BYTES || readFileSync(queuePath, "utf8").split("\n").filter((l) => l.trim().length > 0).length >= MAX_ENTRIES;
    if (capHit) {
      if (existsSync(archivePath)) {
        writeFileSync(archivePath, readFileSync(queuePath, "utf8"), {
          flag: "a",
          encoding: "utf8"
        });
        rmSync(queuePath);
      } else {
        renameSync(queuePath, archivePath);
      }
    }
  }
  const line = `${JSON.stringify(entry)}
`;
  writeFileSync(queuePath, line, {
    flag: "a",
    encoding: "utf8"
  });
}
__name(appendToPendingQueue, "appendToPendingQueue");
async function drainPendingQueue(workspacePath, sendToIPC) {
  const filePaths = [
    getPendingPath(workspacePath),
    getArchivePath(workspacePath)
  ];
  for (const filePath of filePaths) {
    if (!existsSync(filePath)) {
      continue;
    }
    const raw = readFileSync(filePath, "utf8");
    const lines = raw.split("\n").filter((l) => l.trim().length > 0);
    if (lines.length === 0) {
      continue;
    }
    for (const line of lines) {
      try {
        const entry = JSON.parse(line);
        await sendToIPC(entry);
      } catch (err22) {
        try {
          let parsedEntry;
          try {
            parsedEntry = JSON.parse(line);
          } catch {
            parsedEntry = {
              type: "unknown",
              trigger: line,
              action: "",
              queuedAt: /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const deadEntry = {
            entry: parsedEntry,
            rejectedAt: /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString(),
            errorCode: err22 instanceof Error && "code" in err22 ? err22.code : void 0,
            errorMessage: err22 instanceof Error ? err22.message : String(err22)
          };
          const dlPath = getDeadLetterPath(workspacePath);
          mkdirSync(dirname(dlPath), {
            recursive: true
          });
          writeFileSync(dlPath, `${JSON.stringify(deadEntry)}
`, {
            flag: "a",
            encoding: "utf8"
          });
        } catch {
        }
      }
    }
    writeFileSync(filePath, "", "utf8");
  }
}
__name(drainPendingQueue, "drainPendingQueue");
var MAX_BYTES;
var MAX_ENTRIES;
var init_pending_queue = __esm2({
  "src/tools/v2/utils/pending-queue.ts"() {
    MAX_BYTES = 10 * 1024 * 1024;
    MAX_ENTRIES = 1e4;
    __name8(getPendingPath, "getPendingPath");
    __name8(getArchivePath, "getArchivePath");
    __name8(getDeadLetterPath, "getDeadLetterPath");
    __name8(appendToPendingQueue, "appendToPendingQueue");
    __name8(drainPendingQueue, "drainPendingQueue");
  }
});
function logNonFatalError(context) {
  try {
    const timestamp = /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString();
    const errorMessage7 = context.error instanceof Error ? context.error.message : String(context.error);
    const errorStack = context.error instanceof Error ? context.error.stack : void 0;
    const logEntry = {
      timestamp,
      level: "warn",
      tool: context.tool,
      operation: context.operation,
      workspace: context.workspace,
      error: errorMessage7,
      stack: errorStack,
      metadata: context.metadata
    };
    console.error(`[Vreko V2 Tool] ${JSON.stringify(logEntry)}`);
  } catch {
  }
}
__name(logNonFatalError, "logNonFatalError");
var init_telemetry = __esm2({
  "src/tools/v2/utils/telemetry.ts"() {
    __name8(logNonFatalError, "logNonFatalError");
  }
});
function getTopBlindSpots(tool, limit = 2) {
  const spots = TOOL_BLIND_SPOTS[tool] || [];
  return spots.sort((a, b) => b.relevanceScore - a.relevanceScore).slice(0, limit);
}
__name(getTopBlindSpots, "getTopBlindSpots");
function formatBlindSpotsForBriefing(tool, confidence) {
  if (confidence < 0.7) {
    return null;
  }
  const spots = getTopBlindSpots(tool, 2);
  if (spots.length === 0) {
    return null;
  }
  let output = `
## ${tool} Structural Blind Spots

`;
  output += `Vreko detected you're using ${tool} (${(confidence * 100).toFixed(0)}% confidence).
`;
  output += "Here are areas where Vreko provides complementary intelligence:\n\n";
  for (const spot of spots) {
    output += `**${spot.title}**
`;
    output += `- ${spot.description}
`;
    output += `- Vreko mitigation: ${spot.vrekoMitigation}

`;
  }
  return output;
}
__name(formatBlindSpotsForBriefing, "formatBlindSpotsForBriefing");
var TOOL_BLIND_SPOTS;
var init_tool_blind_spots = __esm2({
  "src/tools/v2/utils/tool-blind-spots.ts"() {
    TOOL_BLIND_SPOTS = {
      cursor: [
        {
          id: "cursor-cross-file-coherence",
          title: "Cross-file coherence gaps",
          description: "Cursor's apply model excels at single-file edits but can miss ripple effects across package boundaries",
          vrekoMitigation: "Vreko's co-change graph detects files that change together 85%+ of the time, surfacing hidden dependencies",
          relevanceScore: 0.9
        },
        {
          id: "cursor-revert-amnesia",
          title: "Revert amnesia",
          description: "After reverting changes, Cursor doesn't retain context about why the revert happened",
          vrekoMitigation: "Vreko's rollback correlation analysis shows which patterns led to reverts, preventing repeat mistakes",
          relevanceScore: 0.85
        }
      ],
      "claude-code": [
        {
          id: "claude-bash-blast-radius",
          title: "Bash execution blast radius",
          description: "Claude Code can execute shell commands with limited awareness of cross-directory impact",
          vrekoMitigation: "Vreko's diffusion analysis tracks cross-directory change patterns, flagging high-risk sessions",
          relevanceScore: 0.95
        },
        {
          id: "claude-session-boundary",
          title: "Session boundary blindness",
          description: "Claude doesn't distinguish between volatile exploration and production-ready work",
          vrekoMitigation: "Vreko's session coherence scoring identifies scattered vs. focused work patterns",
          relevanceScore: 0.8
        }
      ],
      "github-copilot": [
        {
          id: "copilot-suggestion-only",
          title: "Suggestion-only mode limits context",
          description: "Copilot suggests code but doesn't track whether suggestions were accepted, rejected, or modified",
          vrekoMitigation: "Vreko's fatigue detection identifies rapid-accept patterns that may indicate uncritical acceptance",
          relevanceScore: 0.7
        },
        {
          id: "copilot-test-adjacency",
          title: "Test adjacency gaps",
          description: "Copilot generates code without verifying test coverage exists",
          vrekoMitigation: "Vreko's test adjacency analysis flags files with high churn but weak test proximity",
          relevanceScore: 0.85
        }
      ],
      devin: [
        {
          id: "devin-autonomous-risk",
          title: "Autonomous execution without checkpoints",
          description: "Devin creates branches and PRs autonomously, sometimes without human review gates",
          vrekoMitigation: "Vreko's recovery risk scoring provides automated checkpoints based on historical rollback patterns",
          relevanceScore: 1
        },
        {
          id: "devin-branch-proliferation",
          title: "Branch proliferation",
          description: "Devin creates timestamped branches (devin/{timestamp}-*) that can accumulate",
          vrekoMitigation: "Vreko's branch abandonment detection identifies orphaned branches for cleanup",
          relevanceScore: 0.75
        }
      ],
      windsurf: [
        {
          id: "windsurf-cascade-edits",
          title: "Cascade edit tracking",
          description: "Windsurf's Cascade mode makes rapid multi-file edits that can be hard to track",
          vrekoMitigation: "Vreko's burst-write detection identifies rapid edit sessions and increases snapshot density",
          relevanceScore: 0.9
        }
      ],
      augment: [
        {
          id: "augment-context-window",
          title: "Context window limitations",
          description: "Augment Code has limited context about historical patterns in the workspace",
          vrekoMitigation: "Vreko's knowledge store provides historical pattern context across all sessions",
          relevanceScore: 0.8
        }
      ],
      cline: [
        {
          id: "cline-shell-access",
          title: "Full shell access risk",
          description: "Cline has full shell access with LLM-determined approval gates",
          vrekoMitigation: "Vreko's structural risk analysis flags high-impact changes before execution",
          relevanceScore: 0.9
        }
      ],
      roocode: [
        {
          id: "roocode-shell-access",
          title: "Full shell access risk",
          description: "RooCode (Cline fork) has full shell access with LLM-determined approval gates",
          vrekoMitigation: "Vreko's structural risk analysis flags high-impact changes before execution",
          relevanceScore: 0.9
        }
      ],
      aider: [
        {
          id: "aider-cli-context",
          title: "CLI context limitations",
          description: "Aider operates in CLI mode with limited IDE integration",
          vrekoMitigation: "Vreko's daemon provides continuous workspace monitoring regardless of tool interface",
          relevanceScore: 0.7
        }
      ]
    };
    __name8(getTopBlindSpots, "getTopBlindSpots");
    __name8(formatBlindSpotsForBriefing, "formatBlindSpotsForBriefing");
  }
});
function formatVrekoResponse(data) {
  const session = data.session ?? {
    id: void 0,
    taskId: void 0,
    task: data.task ?? "unknown"
  };
  const beginData = data.beginData ?? null;
  const riskAssessment = data.riskAssessment ?? null;
  const contextData = data.contextData ?? null;
  const sessionVitals = data.sessionVitals ?? null;
  const recurrenceCandidates = data.recurrenceCandidates ?? [];
  const sections = [];
  if (data.isResumed) {
    sections.push(`Session resumed for: ${data.task}`);
    sections.push("Continuing from previous work in this workspace.");
    if (sessionVitals) {
      const { filesModified, snapshotsCreated, learningsCaptured } = sessionVitals;
      sections.push(`
\u{1F4CA} **Session state**: ${filesModified} files modified | ${snapshotsCreated} snapshots | ${learningsCaptured} learnings captured`);
    }
  } else {
    sections.push(`Session started for: ${data.task}`);
  }
  const tier1Sections = [];
  let hasTier1 = false;
  if (beginData?.context?.queryAwareLearnings && beginData.context.queryAwareLearnings.length > 0) {
    hasTier1 = true;
    tier1Sections.push("### Relevant Learnings for This Task\n");
    for (const learning of beginData.context.queryAwareLearnings) {
      const severityIcon = learning.severity === "critical" ? "\u{1F6A8}" : learning.severity === "warning" ? "\u26A0\uFE0F" : "\u2139\uFE0F";
      tier1Sections.push(`- ${severityIcon} ${learning.insight}`);
      if (learning.files?.length > 0) {
        tier1Sections.push(`  - Applies to: ${learning.files.slice(0, 3).join(", ")}${learning.files.length > 3 ? "..." : ""}`);
      }
    }
  }
  if (beginData?.context?.fragileFilesInScope && beginData.context.fragileFilesInScope.length > 0) {
    hasTier1 = true;
    tier1Sections.push("\n### Fragile Files in Scope\n");
    for (const file of beginData.context.fragileFilesInScope) {
      const reason = file.reason ?? (file.fragility != null ? `Fragility score: ${file.fragility.toFixed(2)}` : "High fragility detected");
      tier1Sections.push(`- \u26A0\uFE0F **${file.path}**: ${reason}`);
    }
  }
  if (beginData?.learnings && beginData.learnings.length > 0) {
    const queryInsights = new Set((beginData.context?.queryAwareLearnings ?? []).map((l) => l.insight));
    const legacyLearnings = beginData.learnings.filter((l) => !queryInsights.has(`${l.trigger} \u2192 ${l.action}`));
    if (legacyLearnings.length > 0) {
      hasTier1 = true;
      tier1Sections.push("\n### Previous Session Learnings\n");
      for (const learning of legacyLearnings.slice(0, 5)) {
        const prefix = learning.type ? `[${learning.type}]` : "";
        tier1Sections.push(`- ${prefix} ${learning.trigger} \u2192 ${learning.action}`);
      }
    }
  }
  const prevSession = beginData?.previousSession;
  if (prevSession) {
    hasTier1 = true;
    const durationMin = Math.round(prevSession.duration / 6e4);
    tier1Sections.push(`
### Previous Session: ${prevSession.taskName}`);
    tier1Sections.push(`${durationMin}m \xB7 ${prevSession.fileCount} files \xB7 ${prevSession.snapshotCount} snapshots \xB7 ${prevSession.learningCount} learnings`);
    if (prevSession.modifiedFiles?.length > 0) {
      tier1Sections.push(`Files touched: ${prevSession.modifiedFiles.slice(0, 8).join(", ")}${prevSession.modifiedFiles.length > 8 ? "..." : ""}`);
    }
  }
  if (hasTier1) {
    sections.push("\n## \u{1F3AF} Direct Relevance\n");
    sections.push(...tier1Sections);
  }
  const tier2Sections = [];
  let hasTier2 = false;
  if (beginData?.context?.queryAwareKnowledge && beginData.context.queryAwareKnowledge.length > 0) {
    hasTier2 = true;
    tier2Sections.push("### Knowledge Base\n");
    for (const chunk of beginData.context.queryAwareKnowledge.slice(0, 3)) {
      const preview = chunk.content.length > 150 ? `${chunk.content.substring(0, 150)}...` : chunk.content;
      tier2Sections.push(`- ${preview}`);
    }
  }
  if (beginData?.context?.coChangePatternsInScope && beginData.context.coChangePatternsInScope.length > 0) {
    hasTier2 = true;
    tier2Sections.push("\n### Files That Change Together\n");
    for (const pair of beginData.context.coChangePatternsInScope) {
      tier2Sections.push(`- When you modify **${pair.source}**, you usually need to update **${pair.target}** too (${Math.round(pair.confidence * 100)}% confidence)`);
    }
  }
  if (RECURRENCE_SURFACING_ENABLED) {
    const surfaceable = recurrenceCandidates.filter((c) => c.surfaceable);
    if (surfaceable.length > 0) {
      hasTier2 = true;
      tier2Sections.push("\n### Recurrence Signals\n");
      for (const candidate of surfaceable.slice(0, 3)) {
        tier2Sections.push(`- \u26A0\uFE0F **${candidate.file}**: ${candidate.wording}`);
      }
    }
  }
  if (contextData?.learnings && contextData.learnings.length > 0) {
    const beginLearningKeys = new Set((beginData?.learnings ?? []).map((l) => `${l.trigger} \u2192 ${l.action}`));
    const queryInsightKeys = new Set((beginData?.context?.queryAwareLearnings ?? []).map((l) => l.insight));
    const newLearnings = contextData.learnings.filter((l) => !beginLearningKeys.has(`${l.trigger} \u2192 ${l.action}`) && !queryInsightKeys.has(`${l.trigger} \u2192 ${l.action}`));
    if (newLearnings.length > 0) {
      hasTier2 = true;
      tier2Sections.push("\n### Additional Learnings\n");
      for (const learning of newLearnings.slice(0, 3)) {
        const typeTag = learning.type ? `[${learning.type}]` : "";
        tier2Sections.push(`- ${typeTag} ${learning.trigger} \u2192 ${learning.action}`);
      }
    }
  }
  if (hasTier2) {
    sections.push("\n## \u{1F4DA} Ambient Context\n");
    sections.push(...tier2Sections);
  }
  const tier3Sections = [];
  let hasTier3 = false;
  if (beginData?.context?.concurrentSessionWarning) {
    const warn2 = beginData.context.concurrentSessionWarning;
    hasTier3 = true;
    tier3Sections.push("### \u26A0\uFE0F Concurrent Session Detected\n");
    tier3Sections.push(`Another session is active in this workspace, touching: ${warn2.touchedFiles?.join(", ") ?? "unknown files"}`);
  }
  if (riskAssessment) {
    const warnings = extractWarnings(riskAssessment);
    if (warnings.length > 0) {
      hasTier3 = true;
      tier3Sections.push("\n### Warnings\n");
      for (const warning of warnings) {
        tier3Sections.push(`- \u26A0\uFE0F ${warning}`);
      }
    }
    const riskFragile = extractFragileFiles(riskAssessment);
    const beginFragilePaths = new Set((beginData?.context?.fragileFilesInScope ?? []).map((f) => f.path));
    const newFragile = riskFragile.filter((f) => !beginFragilePaths.has(f.path));
    if (newFragile.length > 0) {
      hasTier3 = true;
      tier3Sections.push("\n### Additional Fragile Files\n");
      for (const file of newFragile) {
        tier3Sections.push(`- \u26A0\uFE0F **${file.path}**: ${file.reason}`);
      }
    }
    const riskCoChanges = extractCoChangeAlerts(riskAssessment);
    const beginCoChangePairs = /* @__PURE__ */ new Set();
    for (const pair of beginData?.context?.coChangePatternsInScope ?? []) {
      beginCoChangePairs.add(`${pair.source}:${pair.target}`);
      beginCoChangePairs.add(`${pair.target}:${pair.source}`);
    }
    const newCoChanges = riskCoChanges.filter((c) => !beginCoChangePairs.has(`${c.file}:${c.partner}`));
    if (newCoChanges.length > 0) {
      hasTier3 = true;
      tier3Sections.push("\n### Additional Co-Change Patterns\n");
      for (const pair of newCoChanges) {
        tier3Sections.push(`- When you modify **${pair.file}**, **${pair.partner}** usually needs updating too`);
      }
    }
  }
  if (hasTier3) {
    sections.push("\n## \u{1F4A1} Guidance\n");
    sections.push(...tier3Sections);
  }
  const toolIdentity = beginData?.metadata?.toolIdentity;
  if (toolIdentity && toolIdentity.confidence >= 0.7) {
    const blindSpotsSection = formatBlindSpotsForBriefing(toolIdentity.tool, toolIdentity.confidence);
    if (blindSpotsSection) {
      sections.push(blindSpotsSection);
    }
  }
  const learningCount = contextData?.metadata?.learningCount ?? 0;
  if (!hasTier1 && !hasTier2) {
    if (learningCount === 0) {
      sections.push("\n\u{1F4CA} No intelligence yet  -  Vreko is learning. Data accumulates with each session. Learnings, fragile file warnings, and co-change patterns will surface here once the workspace has history.");
    } else {
      sections.push(`
\u{1F4CA} ${learningCount} learning${learningCount === 1 ? "" : "s"} in knowledge base  -  no patterns matched the current task/files. Specify files or a more specific task to surface relevant intelligence.`);
    }
  }
  sections.push("\nVreko is now monitoring this session. Files are automatically snapshotted when risk pressure exceeds 70%  -  no manual action needed.");
  sections.push('\n\u26A0\uFE0F **REQUIRED**: Call `vreko_end({ outcome: "completed" })` when your task is complete to close the session and capture learnings.');
  const kbNote = learningCount > 0 ? ` [${learningCount} learnings in knowledge base]` : " [session 1  -  building baseline]";
  sections.push(`
[session: ${session.id ?? session.taskId}]${kbNote}`);
  return {
    content: [
      {
        type: "text",
        text: sections.join("\n")
      }
    ]
  };
}
__name(formatVrekoResponse, "formatVrekoResponse");
function extractWarnings(risk) {
  const warnings = [];
  if (risk.gitRisk) {
    if (risk.gitRisk.uncommittedChanges) {
      warnings.push("You have uncommitted changes. Consider committing before making AI-assisted modifications.");
    }
    if (risk.gitRisk.detachedHead) {
      warnings.push("You're on a detached HEAD. Changes may be hard to recover without Vreko.");
    }
    if (risk.gitRisk.onMainBranch) {
      warnings.push("You're working directly on main branch. Consider creating a feature branch.");
    }
  }
  if (risk.fatigue) {
    if (risk.fatigue.level === "fatigued" || risk.fatigue.level === "elevated") {
      warnings.push("High change velocity detected. You've been making rapid changes  -  slow down and review.");
    }
  }
  if (risk.rollbackWarning) {
    warnings.push(risk.rollbackWarning.message);
  }
  if (risk.poisoning?.detected) {
    const files = risk.poisoning.files?.join(", ") || "some files";
    warnings.push(`Repeated pattern of rejected changes detected in ${files}. The AI may have a persistent misunderstanding about this code.`);
  }
  const riskScore = risk.score ?? 0;
  if (riskScore >= 2) {
    warnings.push(`Overall risk is HIGH (${riskScore.toFixed(1)}). Proceed with extra caution  -  Vreko will snapshot aggressively.`);
  } else if (riskScore >= 1.5) {
    warnings.push(`Elevated risk detected (${riskScore.toFixed(1)}). Vreko will create additional safety snapshots.`);
  }
  return warnings;
}
__name(extractWarnings, "extractWarnings");
function extractFragileFiles(risk) {
  const fragile = [];
  if (risk.fileRisks) {
    for (const [path4, fileRisk] of Object.entries(risk.fileRisks)) {
      if (fileRisk.fragility > 0.7) {
        fragile.push({
          path: path4,
          reason: fileRisk.reason || `${fileRisk.rollbackCount || "Multiple"} rollbacks in recent sessions`
        });
      }
    }
  }
  return fragile;
}
__name(extractFragileFiles, "extractFragileFiles");
function extractCoChangeAlerts(risk) {
  const pairs = [];
  if (risk.coChanges) {
    for (const pair of risk.coChanges) {
      pairs.push({
        file: pair.source,
        partner: pair.target
      });
    }
  }
  return pairs;
}
__name(extractCoChangeAlerts, "extractCoChangeAlerts");
var RECURRENCE_SURFACING_ENABLED;
var handleVreko;
var vrekoTool;
var init_vreko = __esm2({
  "src/tools/v2/vreko.ts"() {
    init_input_sanitization();
    init_utils3();
    init_pending_queue();
    init_telemetry();
    init_tool_blind_spots();
    RECURRENCE_SURFACING_ENABLED = false;
    handleVreko = /* @__PURE__ */ __name8(async (args, context) => {
      const validation = validateSnapStartParams(args);
      if (!validation.success) {
        return {
          content: [
            {
              type: "text",
              text: `\u274C Input validation failed:
${validation.errors?.join("\n") ?? "Unknown validation error"}`
            }
          ],
          isError: true
        };
      }
      const validatedData = validation.data;
      const params = {
        task: validatedData?.task ?? args.task ?? "",
        files: validatedData?.files
      };
      if (!params.task?.trim()) {
        return {
          content: [
            {
              type: "text",
              text: "Error: task parameter is required and cannot be empty."
            }
          ],
          isError: true
        };
      }
      const workspace = resolveWorkspace(context);
      const daemon = getDaemonClient2(workspace);
      try {
        await drainPendingQueue(workspace, async (entry) => {
          await daemon.request("learning/add", {
            workspace,
            type: entry.type,
            trigger: entry.trigger,
            action: entry.action,
            ...entry.source ? {
              source: entry.source
            } : {}
          });
        });
      } catch {
      }
      let session = null;
      let isResumed = false;
      let beginData = null;
      let sessionVitals = null;
      let recurrenceCandidates = [];
      try {
        const currentSession = await daemon.request("session/current", buildSessionCurrentParams(workspace));
        if (currentSession && isActiveSession2(currentSession)) {
          session = {
            id: currentSession.id,
            taskId: currentSession.taskId,
            task: currentSession.task,
            active: true
          };
          isResumed = true;
          if (params.task && params.task !== session.task) {
            const previousTask = session.task;
            session.task = params.task;
            try {
              await daemon.request("session/update-task", {
                workspacePath: workspace,
                task: params.task
              });
            } catch (_updateError) {
              session.task = previousTask;
            }
          }
          try {
            const fragileResult = await daemon.request("intelligence/fragile-files", {
              workspace
            });
            const coChangeResult = await daemon.request("intelligence/co-changes", {
              workspace
            });
            const _warningsResult = await daemon.request("intelligence/warnings", {
              workspace
            });
            try {
              const recurrenceResult = await daemon.request("intelligence/recurrence-candidates", {
                workspace
              });
              recurrenceCandidates = recurrenceResult?.candidates ?? [];
            } catch {
            }
            beginData = {
              id: currentSession.id,
              context: {
                fragileFilesInScope: fragileResult?.files ?? [],
                coChangePatternsInScope: mapCoChangePairs(coChangeResult),
                concurrentSessionWarning: null
              },
              learnings: []
            };
            try {
              const vitalsResult = await daemon.request("session/vitals", {
                workspacePath: workspace
              });
              if (vitalsResult) {
                sessionVitals = {
                  filesModified: vitalsResult.filesModified ?? 0,
                  snapshotsCreated: vitalsResult.snapshotsCreated ?? 0,
                  learningsCaptured: vitalsResult.learningsCaptured ?? 0
                };
              }
            } catch (vitalsError) {
              logNonFatalError({
                tool: "vreko",
                operation: "session/vitals",
                workspace,
                error: vitalsError,
                metadata: {
                  sessionId: currentSession.id
                }
              });
            }
          } catch (resumeError) {
            logNonFatalError({
              tool: "vreko",
              operation: "session-resume-intelligence",
              workspace,
              error: resumeError,
              metadata: {
                sessionId: currentSession.id
              }
            });
          }
        }
      } catch (error) {
        if (isDaemonConnectionError(error)) {
          return formatDaemonError(classifyDaemonError(error));
        }
      }
      if (!session) {
        try {
          const beginResult = await daemon.request("session/start", buildSessionStartParams(workspace, {
            task: params.task,
            files: params.files ?? []
          }));
          beginData = beginResult;
          if (beginResult && (beginResult.id || beginResult.taskId)) {
            session = {
              id: beginResult.id || beginResult.taskId,
              taskId: beginResult.taskId || beginResult.id,
              task: beginResult.metadata?.task || params.task,
              active: true
            };
          } else if (isDaemonNullResponse(beginResult)) {
            let degradedContext = null;
            try {
              degradedContext = await daemon.request("context/get", {
                workspace,
                task: params.task,
                files: params.files
              });
            } catch (contextError) {
              logNonFatalError({
                tool: "vreko",
                operation: "context/get-degraded",
                workspace,
                error: contextError,
                metadata: {
                  task: params.task
                }
              });
            }
            const sections = [];
            sections.push(`\u26A0\uFE0F **Degraded Mode**: Vreko session could not be created for task "${params.task}".`);
            sections.push("\n**Likely cause:** The Vreko daemon rejected the session request (memory pressure / admission throttle) or is not running.\n**Fix:** Tell the developer to run `vreko daemon start --detach` in their terminal if the daemon is not running, or wait 30s and retry if it is.");
            sections.push("\n**Why sessions require the daemon**: Vreko session state is daemon-resident  -  not held in the MCP server process. This means session context (learned patterns, risk tracking, snapshot history) does not persist between MCP tool calls unless the daemon is running. This is by design: the daemon is the single source of truth shared across VS Code, CLI, and MCP tools. Without it, `vreko_learn` and `vreko_end` cannot find the session started here.");
            const degradedLearnings = degradedContext?.learnings ?? [];
            if (degradedLearnings.length > 0) {
              sections.push("\n## Relevant Learnings (read-only)\n");
              for (const learning of degradedLearnings.slice(0, 5)) {
                const typeTag = learning.type ? `[${learning.type}]` : "";
                sections.push(`- ${typeTag} ${learning.trigger} \u2192 ${learning.action}`);
              }
            }
            sections.push("\n**Available**: Learnings (read-only via context/get), Manual snapshots via CLI");
            sections.push("**Unavailable**: Session tracking, cross-tool state, vreko_learn, vreko_end, automatic snapshots");
            return {
              content: [
                {
                  type: "text",
                  text: sections.join("\n")
                }
              ]
            };
          }
        } catch (error) {
          if (isDaemonConnectionError(error)) {
            return formatDaemonError(classifyDaemonError(error));
          }
          return formatUnexpectedError("session begin", error);
        }
      }
      if (!session) {
        const health = await checkDaemonHealth(daemon, workspace);
        if (!health.healthy) {
          return formatDaemonError(classifyDaemonError(null));
        }
        return formatDaemonError();
      }
      let riskAssessment = null;
      if (params.files && params.files.length > 0) {
        try {
          riskAssessment = await daemon.request("risk/assess", {
            workspace,
            files: params.files,
            context: {
              task: params.task,
              sessionId: session.id || session.taskId
            }
          });
        } catch (riskError) {
          logNonFatalError({
            tool: "vreko",
            operation: "risk/assess",
            workspace,
            error: riskError,
            metadata: {
              sessionId: session.id || session.taskId,
              fileCount: params.files?.length
            }
          });
        }
      }
      let contextData = null;
      try {
        contextData = await daemon.request("context/get", {
          workspace,
          task: params.task,
          files: params.files
        });
        if (isResumed && beginData && contextData?.learnings) {
          beginData = {
            ...beginData,
            learnings: contextData.learnings
          };
        }
      } catch (contextError) {
        logNonFatalError({
          tool: "vreko",
          operation: "context/get",
          workspace,
          error: contextError,
          metadata: {
            sessionId: session.id || session.taskId,
            task: params.task
          }
        });
      }
      return formatVrekoResponse({
        session,
        isResumed,
        riskAssessment,
        contextData,
        beginData,
        task: params.task,
        sessionVitals,
        recurrenceCandidates
      });
    }, "handleVreko");
    __name8(formatVrekoResponse, "formatVrekoResponse");
    __name8(extractWarnings, "extractWarnings");
    __name8(extractFragileFiles, "extractFragileFiles");
    __name8(extractCoChangeAlerts, "extractCoChangeAlerts");
    vrekoTool = {
      name: "vreko",
      description: `Call this before any other tool when starting a coding task. Opens a session, returns codebase intelligence your context window does not have  -  fragile files, co-change patterns, past learnings  -  and enables automatic snapshot protection. Without this call: no session tracking, no automatic snapshots, vreko_pulse and vreko_end return empty results.

WHEN TO CALL:
- At the start of every task, before any file reads or changes  -  even if you don't know which files yet
- Safe to call multiple times  -  resumes the existing session without creating duplicates

WHEN NOT TO CALL:
- Mid-task health check \u2192 vreko_pulse
- Record a discovery \u2192 vreko_learn
- Close the session \u2192 vreko_end

WORKFLOW: vreko \u2192 [work] \u2192 vreko_learn (anytime) \u2192 vreko_pulse (optional) \u2192 vreko_end

WHAT YOU GET BACK (natural language text, not structured JSON):
- Session ID and status (new or resumed)
- Relevant learnings from previous sessions in this codebase
- Fragile files in scope (files with high rollback or violation history)
- Co-change patterns (files that historically change together)
- Risk assessment for the files you intend to modify
- Session health vitals (pulse, pressure, trajectory)

AUTOMATIC PROTECTION: Snapshots fire when risk pressure exceeds 70%. No manual action needed.

REQUIRED CLOSE: Call vreko_end when task is complete to capture learnings.

COLD START: First session returns sparse output. Intelligence accumulates with each session  -  call it anyway.

EXAMPLE:
vreko({ task: "Refactor auth middleware to support OAuth2 PKCE", files: ["src/auth/middleware.ts"] })`,
      inputSchema: {
        type: "object",
        properties: {
          task: {
            type: "string",
            title: "Task Description",
            description: `What you are about to do.

GOOD: "Refactor authentication middleware to support OAuth2 PKCE flow"
GOOD: "Fix the race condition in session persistence on daemon restart"
BAD:  "work on stuff"
BAD:  "coding"

This text is stored with the session and appears in the closing ceremony.
Be specific  -  it helps Vreko surface relevant learnings.`
          },
          files: {
            type: "array",
            items: {
              type: "string"
            },
            title: "Target Files",
            description: `Files you expect to modify. Improves risk assessment accuracy.
Omit if unsure  -  Vreko tracks all modified files automatically.

Example: ["src/auth/middleware.ts", "src/auth/oauth.ts"]`
          }
        },
        required: [
          "task"
        ]
      },
      annotations: {
        title: "\u{1F98E}\uFE0F Vreko",
        readOnlyHint: false,
        destructiveHint: false,
        idempotentHint: true,
        openWorldHint: true
      },
      tier: "free"
    };
  }
});
function formatDuration22(ms) {
  const seconds = Math.floor(ms / 1e3);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  if (hours > 0) {
    const remainingMinutes = minutes % 60;
    return `${hours}h ${remainingMinutes}m`;
  }
  if (minutes > 0) {
    return `${minutes}m`;
  }
  return `${seconds}s`;
}
__name(formatDuration22, "formatDuration2");
var init_format_duration = __esm2({
  "src/utils/format-duration.ts"() {
    __name8(formatDuration22, "formatDuration");
  }
});
function formatEndResponse(data) {
  const sections = [];
  const task = data.session.task ?? data.session.metadata?.task ?? "unnamed task";
  sections.push("# \u{1F3C1} Session Complete\n");
  sections.push(`**Task**: ${task}`);
  sections.push(`**Outcome**: ${data.outcome}`);
  const metrics = gatherMetrics(data.endResult, data.ceremonyData, data.session);
  sections.push("\n## \u{1F4CA} Session Summary\n");
  sections.push(`- **Duration**: ${formatDuration22(metrics.duration ?? 0)}`);
  sections.push(`- **Files modified**: ${metrics.filesModified}`);
  sections.push(`- **Safety snapshots**: ${metrics.snapshotsCreated}`);
  sections.push(`- **Learnings captured**: ${metrics.learningsCaptured}`);
  if (metrics.pitfallsAvoided > 0) {
    sections.push(`- **Pitfalls avoided**: ${metrics.pitfallsAvoided}`);
  }
  if (metrics.tokensSaved) {
    sections.push(`- **Context preserved**: ~${metrics.tokensSaved} tokens`);
  }
  if (metrics.coherenceScore !== null && metrics.coherenceScore !== void 0) {
    const coherenceDesc = describeCoherence(metrics.coherenceScore);
    sections.push(`- **Session coherence**: ${coherenceDesc}`);
  }
  const tier2Sections = [];
  let hasTier2 = false;
  if (data.ceremonyData?.topLearnings && data.ceremonyData.topLearnings.length > 0) {
    hasTier2 = true;
    tier2Sections.push("### \u{1F9E0} Top Learnings\n");
    for (const learning of data.ceremonyData.topLearnings.slice(0, 5)) {
      const confidence = Math.round(learning.confidence * 100);
      tier2Sections.push(`- ${learning.content} (${confidence}% confidence)`);
    }
  }
  if (data.ceremonyData?.ceremony?.learnings?.patterns) {
    const patterns = data.ceremonyData.ceremony.learnings.patterns;
    if (patterns.length > 0) {
      hasTier2 = true;
      tier2Sections.push("\n### \u{1F4C8} Patterns Discovered\n");
      for (const pattern of patterns.slice(0, 3)) {
        const typeIcon = pattern.type === "fragile-file" ? "\u26A0\uFE0F" : pattern.type === "co-change" ? "\u{1F517}" : "\u{1F4A1}";
        tier2Sections.push(`- ${typeIcon} **${pattern.type}**: ${pattern.description}`);
        if (pattern.filesInvolved.length > 0) {
          tier2Sections.push(`  - Files: ${pattern.filesInvolved.slice(0, 3).join(", ")}`);
        }
      }
    }
  }
  if (data.ceremonyData?.insights && data.ceremonyData.insights.length > 0) {
    hasTier2 = true;
    tier2Sections.push("\n### \u{1F4A1} Insights\n");
    for (const insight of data.ceremonyData.insights) {
      tier2Sections.push(`- ${insight}`);
    }
  }
  if (data.ceremonyData?.ceremony?.pitfalls?.warnings) {
    const warnings = data.ceremonyData.ceremony.pitfalls.warnings;
    if (warnings.length > 0) {
      hasTier2 = true;
      tier2Sections.push("\n### \u26A0\uFE0F Pitfalls Avoided\n");
      for (const warning of warnings.slice(0, 5)) {
        tier2Sections.push(`- ${warning.trigger}`);
        tier2Sections.push(`  - Risk: ${warning.risk}`);
      }
      const timeSaved = data.ceremonyData.ceremony.pitfalls.estimatedTimeSaved;
      if (timeSaved > 0) {
        tier2Sections.push(`
_Estimated time saved: ~${timeSaved} minutes_`);
      }
    }
  }
  if (data.aiSynthesis) {
    hasTier2 = true;
    tier2Sections.push("\n### \u{1F916} AI Analysis\n");
    tier2Sections.push(data.aiSynthesis.summary);
    if (data.aiSynthesis.whyItMatters) {
      tier2Sections.push(`
**Why it matters**: ${data.aiSynthesis.whyItMatters}`);
    }
    if (data.aiSynthesis.topRisks && data.aiSynthesis.topRisks.length > 0) {
      tier2Sections.push("\n**Top risks identified:**");
      for (const risk of data.aiSynthesis.topRisks) {
        tier2Sections.push(`- ${risk}`);
      }
    }
    if (data.aiSynthesis.insights && data.aiSynthesis.insights.length > 0) {
      tier2Sections.push("\n**AI Insights:**");
      for (const insight of data.aiSynthesis.insights) {
        const typeEmoji = insight.type === "warning" ? "\u26A0\uFE0F" : insight.type === "suggestion" ? "\u{1F4A1}" : "\u{1F52E}";
        tier2Sections.push(`- ${typeEmoji} **${insight.title}**: ${insight.body}`);
      }
    }
    if (data.aiSynthesis.model) {
      tier2Sections.push(`
_Analysis powered by ${data.aiSynthesis.model}${data.aiSynthesis.cached ? " (cached)" : ""}_`);
    }
  }
  if (hasTier2) {
    sections.push("\n## \u{1F3AF} Intelligence Highlights\n");
    sections.push(...tier2Sections);
  }
  const tier3Sections = [];
  let hasTier3 = false;
  if (data.ceremonyData?.carryForward && data.ceremonyData.carryForward.length > 0) {
    hasTier3 = true;
    tier3Sections.push("### \u{1F4CB} Next Session\n");
    tier3Sections.push("When you return to this codebase, these items need attention:");
    for (const item of data.ceremonyData.carryForward) {
      tier3Sections.push(`- ${item}`);
    }
  }
  if (data.ceremonyData?.ceremony?.timeline) {
    const timeline = data.ceremonyData.ceremony.timeline;
    const significantEvents = timeline.filter((e) => e.severity === "warning" || e.severity === "critical" || e.type === "learning-added");
    if (significantEvents.length > 0) {
      hasTier3 = true;
      tier3Sections.push("\n### \u{1F4C5} Session Timeline\n");
      for (const event of significantEvents.slice(0, 5)) {
        const icon = event.severity === "critical" ? "\u{1F6A8}" : event.severity === "warning" ? "\u26A0\uFE0F" : "\u{1F4CC}";
        tier3Sections.push(`- ${icon} ${event.summary}`);
      }
    }
  }
  if (data.aiSynthesis?.nextAction) {
    hasTier3 = true;
    tier3Sections.push("\n### \u{1F3AF} Recommended Action\n");
    tier3Sections.push(data.aiSynthesis.nextAction);
  }
  if (hasTier3) {
    sections.push("\n## \u{1F4CC} Next Steps\n");
    sections.push(...tier3Sections);
  }
  if (metrics.snapshotsCreated > 0) {
    sections.push(`
---
${metrics.snapshotsCreated} snapshots were created during this session. If any changes need to be reverted, the developer can restore from these snapshots.`);
  }
  const allZeros = metrics.filesModified === 0 && metrics.snapshotsCreated === 0 && metrics.learningsCaptured === 0;
  if (allZeros && data.outcome && data.outcome.length > 10) {
    sections.push("\n---\n\u{1F6A7} **Ceremony zeros detected**: All metrics show 0. This can happen when:\n- The session was restarted mid-task (resumed session tracks only the new leg)\n- `vreko` was not called before beginning work\n- The daemon was restarted during the session\n\nFiles were still protected by Vreko's automatic monitoring. For full metrics, call `vreko` at the start of every task.");
  }
  return {
    content: [
      {
        type: "text",
        text: sections.join("\n")
      }
    ]
  };
}
__name(formatEndResponse, "formatEndResponse");
function buildSessionDiagnostics(session, workspace) {
  const lines = [
    "**Diagnostic Info:**"
  ];
  if (session === null) {
    lines.push("- Daemon returned no session for this workspace");
    lines.push("- Possible causes:");
    lines.push("  1. `vreko` was never called to start a session");
    lines.push("  2. Session was auto-ended after 15 minutes of inactivity");
    lines.push("  3. Daemon restarted and session wasn't restored");
  } else {
    lines.push("- Session found but not active");
    lines.push(`- Session state: ${session.state ?? "unknown"}`);
    lines.push(`- Session ID: ${session.id ?? session.taskId ?? "none"}`);
    if (session.state === "ended") {
      lines.push("- Session was already ended (possibly by idle timeout or previous vreko_end call)");
    } else if (session.state === "idle") {
      lines.push("- Session is idle (possibly waiting to be started)");
    }
  }
  lines.push(`- Workspace: ${workspace}`);
  lines.push('\n**To start a new session:** Call `vreko({ task: "your task" })` before beginning work.');
  return lines.join("\n");
}
__name(buildSessionDiagnostics, "buildSessionDiagnostics");
function gatherMetrics(endResult, ceremonyData, _session) {
  const filesModified = ceremonyData?.filesModified ?? endResult?.filesModified ?? 0;
  const snapshotsCreated = ceremonyData?.checkpointsCreated ?? ceremonyData?.snapshotsCreated ?? endResult?.snapshotsCreated ?? 0;
  const learningsCaptured = ceremonyData?.learningsCaptured ?? endResult?.learningsCaptured ?? 0;
  const duration = ceremonyData?.duration ?? ceremonyData?.ceremony?.summary?.duration ?? endResult?.duration ?? null;
  return {
    filesModified,
    snapshotsCreated,
    learningsCaptured,
    pitfallsAvoided: ceremonyData?.pitfallsAvoided ?? ceremonyData?.ceremony?.pitfalls?.warnings?.length ?? 0,
    tokensSaved: ceremonyData?.tokensSaved?.toString() ?? ceremonyData?.ceremony?.metrics?.tokenSavingsEstimate?.toString() ?? estimateTokenSavings(endResult),
    coherenceScore: ceremonyData?.coherenceScore ?? ceremonyData?.ceremony?.metrics?.coherenceScore ?? endResult?.coherenceScore ?? null,
    duration
  };
}
__name(gatherMetrics, "gatherMetrics");
function describeCoherence(score) {
  if (typeof score === "string") {
    const descriptions = {
      high: "Highly focused session  -  changes were well-scoped and consistent",
      medium: "Good coherence  -  mostly focused with minor tangents",
      low: "Mixed session  -  several distinct concerns addressed",
      scattered: "Scattered session  -  many unrelated changes"
    };
    return descriptions[score] ?? `Unknown coherence level: ${score}`;
  }
  let normalizedScore = score;
  if (typeof normalizedScore === "number" && normalizedScore > 1) {
    normalizedScore = normalizedScore / 100;
  }
  if (normalizedScore >= 0.8) {
    return "Highly focused session  -  changes were well-scoped and consistent";
  }
  if (normalizedScore >= 0.6) {
    return "Good coherence  -  mostly focused with minor tangents";
  }
  if (normalizedScore >= 0.4) {
    return "Mixed session  -  several distinct concerns addressed";
  }
  if (normalizedScore >= 0.2) {
    return "Scattered session  -  many unrelated changes";
  }
  return "Low coherence  -  this session touched many unrelated areas";
}
__name(describeCoherence, "describeCoherence");
function estimateTokenSavings(endResult) {
  const learnings = endResult?.learningsCaptured ?? 0;
  if (learnings === 0) {
    return null;
  }
  const estimate = learnings * 350;
  return estimate.toLocaleString();
}
__name(estimateTokenSavings, "estimateTokenSavings");
function buildAIMetadataPayload(ceremonyData, session) {
  const domains = inferDomainsFromFiles(session.touchedFiles ?? []);
  const byType = {
    "anti-pattern": 0,
    "best-practice": 0,
    decision: 0,
    dependency: 0,
    sequence: 0
  };
  const byDomain = {
    exports: 0,
    dependencies: 0,
    testing: 0,
    bundling: 0,
    performance: 0,
    security: 0,
    typescript: 0,
    architecture: 0,
    general: 0
  };
  const topLearnings = ceremonyData?.topLearnings ?? [];
  for (const learning of topLearnings) {
    const method = learning.captureMethod?.toLowerCase() ?? "";
    if (method.includes("anti") || method.includes("pitfall")) {
      byType["anti-pattern"]++;
    } else if (method.includes("best") || method.includes("pattern")) {
      byType["best-practice"]++;
    } else if (method.includes("decision")) {
      byType.decision++;
    } else {
      byType["best-practice"]++;
    }
  }
  const totalPatterns = topLearnings.length;
  const avgConfidence = topLearnings.length > 0 ? topLearnings.reduce((sum, l) => sum + (l.confidence ?? 0.5), 0) / topLearnings.length : 0.5;
  for (const domain of domains) {
    if (domain in byDomain) {
      byDomain[domain]++;
    }
  }
  return {
    session: {
      mode: "review",
      domains,
      violationCount: 0,
      scopeType: inferScopeType(session.touchedFiles ?? [])
    },
    patterns: {
      total: totalPatterns,
      byType,
      byDomain,
      avgConfidence,
      regressionRate: 0
    },
    query: {
      type: "synthesis"
    }
  };
}
__name(buildAIMetadataPayload, "buildAIMetadataPayload");
function inferDomainsFromFiles(files) {
  const domains = /* @__PURE__ */ new Set();
  for (const file of files) {
    const lower = file.toLowerCase();
    if (lower.includes("test") || lower.includes("spec") || lower.endsWith(".test.ts") || lower.endsWith(".spec.ts")) {
      domains.add("testing");
    }
    if (lower.endsWith(".ts") || lower.endsWith(".tsx")) {
      domains.add("typescript");
      domains.add("exports");
    }
    if (lower.includes("package.json") || lower.includes("pnpm") || lower.includes("yarn") || lower.includes("npm")) {
      domains.add("dependencies");
    }
    if (lower.includes("webpack") || lower.includes("vite") || lower.includes("rollup") || lower.includes("esbuild")) {
      domains.add("bundling");
    }
    if (lower.includes("perf") || lower.includes("benchmark") || lower.includes("optimize")) {
      domains.add("performance");
    }
    if (lower.includes("auth") || lower.includes("security") || lower.includes("crypto")) {
      domains.add("security");
    }
    if (lower.includes("src/") || lower.includes("lib/") || lower.includes("apps/") || lower.includes("packages/")) {
      domains.add("architecture");
    }
  }
  if (domains.size === 0) {
    domains.add("general");
  }
  return Array.from(domains);
}
__name(inferDomainsFromFiles, "inferDomainsFromFiles");
function inferScopeType(files) {
  if (files.length === 0) {
    return "unknown";
  }
  const packageIndicators = files.filter((f) => f.includes("packages/") || f.includes("apps/") || f.includes("libs/"));
  const uniquePackages = new Set(packageIndicators.map((f) => {
    const match = f.match(/(?:packages|apps|libs)\/([^/]+)/);
    return match?.[1] ?? "";
  }));
  if (uniquePackages.size > 1) {
    return "monorepo";
  }
  if (uniquePackages.size === 1) {
    return "single-package";
  }
  return "unknown";
}
__name(inferScopeType, "inferScopeType");
async function fetchAISynthesis(payload, sessionId, daemonBaseUrl, apiKey) {
  if (!AI_SYNTHESIS_CONFIG.enabled) {
    return null;
  }
  try {
    const controller = new AbortController();
    const timeoutId = setTimeout(() => controller.abort(), AI_SYNTHESIS_CONFIG.timeoutMs);
    const headers = {
      "Content-Type": "application/json"
    };
    if (apiKey) {
      headers.Authorization = `Bearer ${apiKey}`;
    }
    const response = await fetch(`${daemonBaseUrl}${AI_SYNTHESIS_CONFIG.endpoint}`, {
      method: "POST",
      headers,
      body: JSON.stringify({
        ...payload,
        sessionId
      }),
      signal: controller.signal
    });
    clearTimeout(timeoutId);
    if (!response.ok) {
      return null;
    }
    return await response.json();
  } catch (error) {
    logNonFatalError({
      tool: "vreko_end",
      operation: "fetch-ai-synthesis",
      error,
      metadata: {
        sessionId,
        endpoint: AI_SYNTHESIS_CONFIG.endpoint
      }
    });
    return null;
  }
}
__name(fetchAISynthesis, "fetchAISynthesis");
var handleVrekoEnd;
var AI_SYNTHESIS_CONFIG;
var vrekoEndTool;
var init_vreko_end = __esm2({
  "src/tools/v2/vreko-end.ts"() {
    init_input_sanitization();
    init_format_duration();
    init_utils3();
    init_telemetry();
    handleVrekoEnd = /* @__PURE__ */ __name8(async (args, context) => {
      const rawArgs = args;
      const rawOutcome = typeof rawArgs.outcome === "string" ? rawArgs.outcome : "";
      const sanitizedOutcome = normalizeContent(rawOutcome).slice(0, MAX_LENGTHS.description);
      const params = {
        outcome: sanitizedOutcome
      };
      const workspace = resolveWorkspace(context);
      const daemon = getDaemonClient2(workspace);
      let currentSession = null;
      try {
        currentSession = await daemon.request("session/current", buildSessionCurrentParams(workspace));
      } catch (error) {
        if (isDaemonConnectionError(error)) {
          return formatDaemonError();
        }
        return formatUnexpectedError("session check", error);
      }
      const rawSession = currentSession;
      if (!isActiveSession2(currentSession)) {
        if (currentSession === null) {
          const health = await checkDaemonHealth(daemon, workspace);
          if (!health.healthy) {
            const healthReasonToCategory = {
              connected: void 0,
              not_running: "not_running",
              starting: "connection_refused",
              connection_failed: "connection_refused",
              timeout: "timeout",
              null_response: "null_response"
            };
            return formatDaemonError(healthReasonToCategory[health.reason]);
          }
        }
        if (rawSession !== null && (rawSession.state === "ended" || rawSession.state === "idle")) {
          const sessionId = rawSession.id ?? rawSession.taskId;
          if (sessionId) {
            try {
              const lateCeremony = await daemon.request("session/review", buildSessionReviewParams(workspace, {
                sessionId
              }));
              if (lateCeremony && (lateCeremony.filesModified || lateCeremony.duration || lateCeremony.learningsCaptured)) {
                const expiredNote = rawSession.state === "ended" ? "\n\n\u26A0\uFE0F **Note**: This session auto-expired (15min inactivity) before `vreko_end` was called. The ceremony below reflects work done before expiry." : "";
                const lateResult = formatEndResponse({
                  endResult: null,
                  ceremonyData: lateCeremony,
                  outcome: params.outcome + expiredNote,
                  session: rawSession,
                  aiSynthesis: null
                });
                const existingText = lateResult.content[0]?.text ?? "";
                return {
                  ...lateResult,
                  content: [
                    {
                      type: "text",
                      text: `## Late Ceremony (session recovered)

${existingText}`
                    }
                  ]
                };
              }
            } catch {
            }
            try {
              const walRecovery = await daemon.request("ledger/recover", {
                sessionId
              });
              if (walRecovery && walRecovery.ceremonyAvailable && walRecovery.ceremony) {
                const expiredNote = `

\u26A0\uFE0F **Note**: Session was recovered from the write-ahead log (WAL). Expiry reason: ${walRecovery.expiryReason}. ${walRecovery.eventCount} event(s) captured.`;
                return {
                  content: [
                    {
                      type: "text",
                      text: `## WAL Recovery Ceremony

Session \`${sessionId}\` was recovered from disk.
Events captured: ${walRecovery.eventCount}${expiredNote}`
                    }
                  ]
                };
              }
            } catch {
            }
          }
        }
        const diagnosticInfo = buildSessionDiagnostics(rawSession, workspace);
        return {
          content: [
            {
              type: "text",
              text: "No active session to close. If you were working without calling `vreko` first, your files were still protected by Vreko's automatic monitoring, but session intelligence (learnings, patterns, risk tracking) was not captured.\n\n" + diagnosticInfo
            }
          ]
        };
      }
      let endResult = null;
      try {
        endResult = await daemon.request("session/end-daemon", {
          workspacePath: workspace,
          outcome: params.outcome,
          sessionId: currentSession.id
        });
      } catch (error) {
        if (isDaemonConnectionError(error)) {
          return formatDaemonError();
        }
        return formatUnexpectedError("session end", error);
      }
      let ceremonyData = null;
      try {
        ceremonyData = await daemon.request("session/review", buildSessionReviewParams(workspace, {
          sessionId: currentSession.id
        }));
      } catch (error) {
        logNonFatalError({
          tool: "vreko_end",
          operation: "session/review",
          workspace,
          error,
          metadata: {
            sessionId: currentSession.id ?? currentSession.taskId
          }
        });
      }
      let aiSynthesis = null;
      try {
        const payload = buildAIMetadataPayload(ceremonyData, currentSession);
        const daemonBaseUrl = process.env.VREKO_API_URL ?? "http://127.0.0.1:4200";
        const apiKey = process.env.VREKO_API_KEY;
        const urlHost = new URL(daemonBaseUrl.startsWith("http") ? daemonBaseUrl : `http://${daemonBaseUrl}`).hostname;
        const isLoopback = urlHost === "127.0.0.1" || urlHost === "localhost" || urlHost === "::1";
        if (!daemonBaseUrl.startsWith("https://") && !isLoopback) {
          aiSynthesis = null;
        } else {
          aiSynthesis = await fetchAISynthesis(payload, currentSession.id ?? currentSession.taskId ?? "unknown", daemonBaseUrl, apiKey);
        }
      } catch (error) {
        logNonFatalError({
          tool: "vreko_end",
          operation: "ai-synthesis",
          workspace,
          error,
          metadata: {
            sessionId: currentSession.id ?? currentSession.taskId
          }
        });
        aiSynthesis = null;
      }
      return formatEndResponse({
        endResult,
        ceremonyData,
        outcome: params.outcome,
        session: currentSession,
        aiSynthesis
      });
    }, "handleVrekoEnd");
    __name8(formatEndResponse, "formatEndResponse");
    __name8(buildSessionDiagnostics, "buildSessionDiagnostics");
    __name8(gatherMetrics, "gatherMetrics");
    __name8(describeCoherence, "describeCoherence");
    __name8(estimateTokenSavings, "estimateTokenSavings");
    AI_SYNTHESIS_CONFIG = {
      /** API endpoint for ceremony synthesis (relative to daemon base URL). */
      endpoint: "/ai/generate-insights",
      /** Timeout for AI synthesis in milliseconds. */
      timeoutMs: 1e4,
      /** Whether AI synthesis is enabled (can be toggled via env). */
      enabled: process.env.VREKO_AI_SYNTHESIS !== "false"
    };
    __name8(buildAIMetadataPayload, "buildAIMetadataPayload");
    __name8(inferDomainsFromFiles, "inferDomainsFromFiles");
    __name8(inferScopeType, "inferScopeType");
    __name8(fetchAISynthesis, "fetchAISynthesis");
    vrekoEndTool = {
      name: "vreko_end",
      description: `Close the Vreko session opened by vreko and receive a summary
of what was accomplished.

WHAT IT DOES:
- Closes the active session for this workspace
- Generates a closing ceremony with session metrics:
  files modified, snapshots created, learnings captured,
  session coherence score, and carry-forward insights
- AI-powered synthesis (Pro): Cross-domain pattern analysis with
  risk assessment and actionable recommendations
- Persisted session data is cleaned up

WHEN TO CALL:
- At the end of every task, after your work is complete and deliverable is ready
- Right after generating a final artifact/code  -  before your closing message to the user
- When abandoning a task partway through (use outcome to explain why)
- Before starting a completely different task in the same workspace

WHEN NOT TO CALL:
- Mid-task to check progress (use vreko_pulse instead)
- Still iterating or waiting for user input
- If no session was started (the tool handles this gracefully,
  but calling vreko first gives you a meaningful ceremony)

WORKFLOW POSITION: 4 of 4 (final)
  vreko \u2192 [your work] \u2192 vreko_pulse (optional) \u2192 vreko_end

  This closes the session opened by vreko. Every vreko call
  should eventually have a matching vreko_end.

WHAT YOU GET BACK (natural language text):
- Task name and outcome
- Files modified count
- Snapshots created count
- Learnings captured during the session
- Session coherence score (how related were the files you touched)
- Carry-forward insights for the next session
- Session duration
- AI Analysis section (Pro): summary, risks, insights, next action

Always safe to call  -  gracefully handles missing sessions
with a clear message instead of an error.

EXAMPLE:
vreko_end({ outcome: "Refactored auth middleware to support OAuth2 PKCE. Token refresh is now in src/auth/refresh.ts. Left incomplete: revoke-token endpoint still uses the old pattern. Decided to keep session cookies HTTP-only after security review." })`,
      inputSchema: {
        type: "object",
        properties: {
          outcome: {
            type: "string",
            title: "Task Outcome",
            description: `What you accomplished, what remains incomplete, and any decisions made. This becomes the opening context for your next session  -  a vague outcome produces a sparse briefing next time. One paragraph minimum.

GOOD: "Refactored auth middleware to support OAuth2 PKCE. Token refresh is now in src/auth/refresh.ts. Left incomplete: revoke-token endpoint still uses the old pattern. Decided to keep session cookies HTTP-only after security review."
GOOD: "Partially done  -  auth flow works but token refresh not implemented yet"
GOOD: "Abandoned  -  realized this approach won't work, need different strategy"

BAD:  "done"
BAD:  "completed the task"

Be honest  -  future sessions start from this context.`
          }
        },
        required: [
          "outcome"
        ]
      },
      annotations: {
        title: "\u{1F3C1} Vreko End",
        readOnlyHint: false,
        destructiveHint: false,
        idempotentHint: false,
        openWorldHint: true
      },
      tier: "free"
    };
  }
});
function getToolCreditEstimate(toolName) {
  const jobType = TOOL_CREDIT_JOB_MAP[toolName];
  if (!jobType) return void 0;
  const range = getCreditRange(jobType);
  return {
    jobType,
    min: range.min,
    max: range.max,
    typical: range.typical,
    note: TOOL_CREDIT_NOTES[toolName] ?? ""
  };
}
__name(getToolCreditEstimate, "getToolCreditEstimate");
var TOOL_CREDIT_JOB_MAP;
var TOOL_CREDIT_NOTES;
var init_credit_registry = __esm2({
  "src/tools/v2/utils/credit-registry.ts"() {
    TOOL_CREDIT_JOB_MAP = {
      vreko_pulse: "synthesis",
      vreko_learn: "memory_sync"
    };
    TOOL_CREDIT_NOTES = {
      vreko_pulse: "Credits consumed when intelligence synthesis runs on the API tier.",
      vreko_learn: "Credits consumed when intelligence is persisted to the remote knowledge base."
    };
    __name8(getToolCreditEstimate, "getToolCreditEstimate");
  }
});
function formatLearnResponse(data) {
  const sections = [];
  const typeLabel = data.type !== "pattern" ? ` [${data.type}]` : "";
  sections.push(`Rule encoded${typeLabel} (${data.severity}): when ${data.trigger} \u2192 ${data.action}`);
  if (data.implicitSession) {
    sections.push("\nNote: No active session was found, so one was created automatically. For full codebase intelligence, call `vreko` before starting work.");
  }
  if (data.relatedLearnings.length > 0) {
    sections.push(`
## Related Knowledge (${data.relatedLearnings.length} existing)
`);
    sections.push("Previous rules about this area:");
    for (const learning of data.relatedLearnings) {
      sections.push(`- ${learning}`);
    }
  }
  const creditEstimate = getToolCreditEstimate("vreko_learn");
  return {
    content: [
      {
        type: "text",
        text: sections.join("\n")
      }
    ],
    structuredContent: creditEstimate ? {
      creditsEstimate: creditEstimate
    } : {}
  };
}
__name(formatLearnResponse, "formatLearnResponse");
function mapSeverityToType(severity) {
  switch (severity) {
    case "critical":
      return "pitfall";
    case "warning":
      return "pitfall";
    case "info":
      return "pattern";
  }
}
__name(mapSeverityToType, "mapSeverityToType");
function deriveAction(_insight, severity) {
  if (severity === "critical") {
    return "CRITICAL: Review this before modifying related files";
  }
  if (severity === "warning") {
    return "Warning: Be cautious when working in this area";
  }
  return "Consider this pattern when working with related files";
}
__name(deriveAction, "deriveAction");
var handleVrekoLearn;
var vrekoLearnTool;
var init_vreko_learn = __esm2({
  "src/tools/v2/vreko-learn.ts"() {
    init_input_sanitization();
    init_credit_registry();
    init_utils3();
    init_pending_queue();
    init_telemetry();
    handleVrekoLearn = /* @__PURE__ */ __name8(async (args, context) => {
      const rawArgs = args;
      const sanitizedArgs = {
        ...rawArgs,
        ...typeof rawArgs.trigger === "string" && {
          trigger: normalizeContent(rawArgs.trigger).slice(0, MAX_LENGTHS.trigger)
        },
        ...typeof rawArgs.action === "string" && {
          action: normalizeContent(rawArgs.action).slice(0, MAX_LENGTHS.action)
        },
        ...typeof rawArgs.insight === "string" && {
          insight: normalizeContent(rawArgs.insight).slice(0, MAX_LENGTHS.trigger)
        }
      };
      const params = sanitizedArgs;
      const workspace = resolveWorkspace(context);
      const daemon = getDaemonClient2(workspace);
      const severity = params.severity ?? "info";
      const trigger = params.trigger ?? params.insight ?? "";
      const action = params.action ?? deriveAction(params.insight ?? "", severity);
      const type = params.type ?? mapSeverityToType(severity);
      let implicitSession = false;
      let currentSessionId;
      let daemonAvailable = true;
      try {
        const currentSession = await daemon.request("session/current", buildSessionCurrentParams(workspace));
        if (isDaemonNullResponse(currentSession)) {
          daemonAvailable = false;
        } else if (!isActiveSession2(currentSession)) {
          const beginResult = await daemon.request("session/start", buildSessionStartParams(workspace, {
            task: "Implicit session (learning captured before explicit session start)",
            files: params.files ?? []
          }));
          if (!isDaemonNullResponse(beginResult)) {
            currentSessionId = beginResult?.sessionId ?? beginResult?.taskId;
            implicitSession = true;
          } else {
            daemonAvailable = false;
          }
        } else {
          currentSessionId = currentSession.id ?? currentSession.taskId;
        }
      } catch (error) {
        if (isDaemonConnectionError(error)) {
          daemonAvailable = false;
        }
      }
      const learningPayload = {
        type,
        trigger,
        action,
        source: "llm-observation"
      };
      let addResult = null;
      try {
        addResult = await daemon.request("learning/add", {
          workspace,
          ...learningPayload,
          ...currentSessionId ? {
            sessionId: currentSessionId
          } : {}
        });
      } catch (error) {
        if (isDaemonConnectionError(error)) {
          return formatDaemonError();
        }
        return formatUnexpectedError("learning add", error);
      }
      if (isDaemonNullResponse(addResult)) {
        if (!daemonAvailable) {
          try {
            appendToPendingQueue(workspace, {
              type,
              trigger,
              action,
              source: typeof args.source === "string" ? args.source : void 0,
              queuedAt: /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString()
            });
          } catch {
          }
          return formatDegradedModeResponse(`Learning queued (offline): when ${trigger} \u2192 ${action}`, {
            reason: "null_response",
            featuresAvailable: [],
            featuresUnavailable: [
              "Learning storage (daemon offline  -  start with `vreko daemon start`)",
              "Related learning lookup",
              "Session attribution"
            ]
          });
        }
        return formatDaemonError("null_response");
      }
      let relatedLearnings = [];
      try {
        const searchResult = await daemon.request("learning/search", buildLearningSearchParams(workspace, trigger, 5));
        const related = searchResult?.results ? searchResult.results.filter((r) => r.id !== addResult?.id).map((r) => r.solution ?? r.insight ?? r.content).filter((v) => Boolean(v)) : (searchResult?.learnings ?? []).map((r) => r.trigger && r.action ? `${r.trigger} -> ${r.action}` : void 0).filter((v) => Boolean(v));
        relatedLearnings = related.slice(0, 3);
      } catch (error) {
        logNonFatalError({
          tool: "vreko_learn",
          operation: "learning/search",
          workspace,
          error,
          metadata: {
            trigger,
            learningId: addResult?.id
          }
        });
      }
      return formatLearnResponse({
        captured: addResult?.success === true,
        implicitSession,
        relatedLearnings,
        severity,
        type,
        trigger,
        action
      });
    }, "handleVrekoLearn");
    __name8(formatLearnResponse, "formatLearnResponse");
    __name8(mapSeverityToType, "mapSeverityToType");
    __name8(deriveAction, "deriveAction");
    vrekoLearnTool = {
      name: "vreko_learn",
      description: `Encode a reusable IF/THEN rule into the workspace knowledge base. trigger: the condition under which this rule applies ('when editing auth.ts without running tests'). action: what to do when that condition is true ('always snapshot before touching token refresh logic'). Not for observations or file annotations  -  call vreko_pulse with a query param for retrieval.

CALL IMMEDIATELY WHEN:
- User corrects an assumption you made ("Actually, we use X not Y")
- You discover a codebase pattern that contradicts your expectations
- An error reveals an unknown constraint or fragile dependency
- You find undocumented behavior that future sessions must know about
- You identify a team preference or hard rule

WHEN NOT TO CALL:
- To retrieve learnings \u2192 vreko_pulse with query param (read-only)
- For trivial observations that won't matter in future sessions
- To confirm expected/standard behavior

WORKFLOW POSITION: Anytime during a session.
  vreko \u2192 [work] \u2192 vreko_learn \u2192 [work] \u2192 vreko_learn \u2192 vreko_end

  Works without an active session, but for full intelligence context,
  call vreko first. Without a session, learnings are stored but
  not attributed to any task in the closing ceremony.

WHAT YOU GET BACK (natural language text):
- Confirmation the rule was stored
- Related learnings already in the knowledge base (deduplication loop)

EXAMPLE:
vreko_learn({
  trigger: "when modifying token refresh logic in auth.ts",
  action: "snapshot first  -  this code has 3 rollbacks across 2 sessions",
  type: "pitfall",
  severity: "warning",
  files: ["src/auth/auth.ts"]
})`,
      inputSchema: {
        type: "object",
        properties: {
          trigger: {
            type: "string",
            title: "Condition",
            description: `The condition. Starts with 'when'  -  when X is true, when Y happens, when you're in Z state.

GOOD: "when editing auth.ts without running tests first"
GOOD: "when the daemon restarts during an active session"
BAD:  "auth.ts is fragile"
BAD:  "I noticed something"`
          },
          action: {
            type: "string",
            title: "Action",
            description: `What to do when the trigger fires. Starts with a verb.

GOOD: "always snapshot before touching token refresh logic"
GOOD: "check that session.json was cleaned up before starting a new session"
BAD:  "be careful"
BAD:  "token refresh is tricky"`
          },
          files: {
            type: "array",
            items: {
              type: "string"
            },
            title: "Related Files",
            description: `Files this rule is scoped to. Narrows retrieval relevance.

Example: ["src/services/session-registry.ts"]`
          },
          type: {
            type: "string",
            enum: LearningType.options,
            title: "Rule Type",
            description: `Controls how this learning surfaces in future briefings.

"pattern"     -  A repeating structure worth following
"pitfall"     -  A known failure mode to avoid
"constraint"  -  A hard rule that must not be violated
"preference"  -  A soft team preference`
          },
          severity: {
            type: "string",
            enum: [
              "info",
              "warning",
              "critical"
            ],
            title: "Severity Level",
            description: `How urgently this rule surfaces.

"info"      -  Shown when relevant files are in scope (default)
"warning"   -  Shown whenever related files are modified
"critical"  -  Shown in vreko even on cold start, regardless of file scope`
          },
          insight: {
            type: "string",
            title: "Insight (deprecated)",
            description: "Deprecated. Use trigger + action instead."
          }
        },
        required: [
          "trigger",
          "action"
        ]
      },
      annotations: {
        title: "\u{1F4DA} Vreko Learn",
        readOnlyHint: false,
        destructiveHint: false,
        idempotentHint: false,
        openWorldHint: true
      },
      tier: "free"
    };
  }
});
function getDurationGuidance(durationMin, filesTouched, snapshotsCreated, pressure) {
  if (durationMin === void 0) {
    return null;
  }
  const files = filesTouched ?? 0;
  const snapshots = snapshotsCreated ?? 0;
  const riskPressure = pressure ?? 0;
  if (durationMin < DURATION_SHORT) {
    if (files === 0) {
      return "\u{1F4A1} Just getting started. Vreko will surface relevant patterns once you begin editing files.";
    }
    return null;
  }
  if (durationMin < DURATION_MEDIUM) {
    if (files >= 3 && snapshots === 0 && riskPressure > 0.5) {
      return "\u{1F4A1} You've modified several files without snapshots. Consider pausing to review changes before continuing.";
    }
    if (files >= 5 && riskPressure < 0.3) {
      return "\u{1F4A1} Multiple files touched with low risk. Your changes seem well-scoped.";
    }
    return null;
  }
  if (durationMin < DURATION_LONG) {
    if (riskPressure > 0.7) {
      return "\u{1F4A1} Extended session with high risk pressure. Consider committing your changes or taking a short break.";
    }
    if (files >= 10 && snapshots < 2) {
      return "\u{1F4A1} You've touched many files. Vreko recommends creating a checkpoint snapshot before continuing.";
    }
    return "\u{1F4A1} Session running for 30+ minutes. Consider a brief pause to review progress.";
  }
  if (riskPressure > 0.5) {
    return "\u{1F4A1} Extended session detected. Consider wrapping up or creating a summary of changes made.";
  }
  return "\u{1F4A1} Long session in progress. Remember to take breaks and review your work periodically.";
}
__name(getDurationGuidance, "getDurationGuidance");
function normalizeSessionStatus(raw) {
  if (!raw) {
    return null;
  }
  if (!isActiveSession2(raw)) {
    return null;
  }
  const durationMin = raw.startedAt ? Math.round((Date.now() - new Date(raw.startedAt).getTime()) / 6e4) : void 0;
  return {
    active: true,
    durationMin,
    filesTouched: raw.touchedFiles?.length,
    snapshotsCreated: raw.snapshotIds?.length,
    touchedFiles: raw.touchedFiles
  };
}
__name(normalizeSessionStatus, "normalizeSessionStatus");
function formatPulseResponse(data) {
  const { session, vitals, warnings, fragile, coChanges, guardHealth, topologyStatus, topologyQuery, learningSearch, focus, task } = data;
  const sections = [];
  const filteredWarnings = (warnings?.warnings ?? []).filter((w) => w.severity !== "info");
  const filteredFragile = (fragile?.files ?? []).filter((f) => f.fragility >= RISK_THRESHOLD);
  const missingCoChanges = findMissingCoChanges(session.touchedFiles, coChanges);
  const queryLearnings = extractLearningsFromSearch(learningSearch);
  const hasCriticalDeviations = (topologyStatus?.critical_deviations?.length ?? 0) > 0;
  const topologyHealthy = !topologyStatus?.exists || !topologyStatus.stale;
  const isHealthy2 = filteredWarnings.length === 0 && filteredFragile.length === 0 && missingCoChanges.length === 0 && topologyHealthy && !hasCriticalDeviations;
  if (isHealthy2) {
    const healthyText = formatHealthyResponse(session, vitals);
    const _guardHealthFormatted = guardHealth ? formatGuardHealth(guardHealth, session.touchedFiles) : "";
    const guardHealthText = _guardHealthFormatted ? `

${_guardHealthFormatted}` : "";
    const learningsText = queryLearnings.length > 0 ? `

${formatQueryLearnings(queryLearnings, task)}` : "";
    return {
      content: [
        {
          type: "text",
          text: healthyText + guardHealthText + learningsText
        }
      ]
    };
  }
  if (!focus || focus === "risk") {
    if (filteredWarnings.length > 0) {
      sections.push(formatAlerts(filteredWarnings));
    }
    if (filteredFragile.length > 0) {
      sections.push(formatRiskSurface(filteredFragile));
    }
  }
  if (!focus || focus === "cochanges") {
    if (missingCoChanges.length > 0) {
      sections.push(formatMissingCoChanges(missingCoChanges));
    }
  }
  if (topologyStatus?.exists) {
    sections.push(formatTopologySection(topologyStatus, topologyQuery, task));
  }
  if (queryLearnings.length > 0) {
    sections.push(formatQueryLearnings(queryLearnings, task));
  }
  if (!focus || focus === "vitals") {
    sections.push(formatVitals(session, vitals));
    if (guardHealth) {
      const gh = formatGuardHealth(guardHealth, session.touchedFiles);
      if (gh) {
        sections.push(gh);
      }
    }
  }
  const durationGuidance = getDurationGuidance(session.durationMin, session.filesTouched, session.snapshotsCreated, vitals?.pressure);
  if (durationGuidance) {
    sections.push(durationGuidance);
  }
  return {
    content: [
      {
        type: "text",
        text: sections.join("\n\n")
      }
    ]
  };
}
__name(formatPulseResponse, "formatPulseResponse");
function extractLearningsFromSearch(result7) {
  if (!result7) {
    return [];
  }
  const items = result7.results ?? result7.learnings ?? [];
  if (items.length === 0) {
    return [];
  }
  return items.map((item) => {
    const entry = item;
    const text2 = entry.insight ?? entry.solution ?? entry.content ?? (entry.action && entry.trigger ? `${Array.isArray(entry.trigger) ? entry.trigger.join(", ") : entry.trigger} \u2192 ${entry.action}` : entry.action ?? "");
    const score = entry.relevanceScore ?? 0.5;
    return {
      text: text2,
      relevanceScore: score
    };
  }).filter((l) => l.text.length > 0).sort((a, b) => b.relevanceScore - a.relevanceScore).slice(0, 5);
}
__name(extractLearningsFromSearch, "extractLearningsFromSearch");
function formatQueryLearnings(learnings, query) {
  const header = query ? `\u{1F4A1} Relevant Learnings for "${query.length > 60 ? `${query.substring(0, 57)}...` : query}"` : "\u{1F4A1} Relevant Learnings";
  const lines = [
    header
  ];
  for (const learning of learnings) {
    lines.push(`- ${learning.text}`);
  }
  return lines.join("\n");
}
__name(formatQueryLearnings, "formatQueryLearnings");
function formatHealthyResponse(session, vitals) {
  const parts = [
    "\u2705 Session healthy."
  ];
  if (session.durationMin) {
    parts.push(`${session.durationMin} min`);
  }
  if (vitals?.filesModified) {
    parts.push(`${vitals.filesModified} files touched`);
  } else if (session.filesTouched) {
    parts.push(`${session.filesTouched} files touched`);
  }
  const healthySnapshots = vitals?.snapshotsCreated ?? session.snapshotsCreated;
  if (healthySnapshots) {
    parts.push(`${healthySnapshots} snapshots`);
  }
  if (vitals) {
    parts.push(`Pulse: ${vitals.pulse}`);
    parts.push(`Pressure: ${vitals.pressure}%`);
    parts.push(`Trajectory: ${vitals.trajectory}`);
  }
  parts.push("No concerns.");
  const durationGuidance = getDurationGuidance(session.durationMin, session.filesTouched, session.snapshotsCreated, vitals?.pressure);
  if (durationGuidance) {
    parts.push(`
${durationGuidance}`);
  }
  return parts.join(", ");
}
__name(formatHealthyResponse, "formatHealthyResponse");
function formatAlerts(warnings) {
  const lines = [];
  for (const w of warnings) {
    const icon = w.severity === "critical" ? "\u{1F6A8}" : w.severity === "warn" ? "\u26A0\uFE0F" : "\u2139\uFE0F";
    let line = `${icon} ALERT: ${w.message}`;
    if (w.action) {
      line += ` \u2192 ${w.action}`;
    }
    lines.push(line);
  }
  return lines.join("\n");
}
__name(formatAlerts, "formatAlerts");
function formatRiskSurface(files) {
  const lines = [
    "\u{1F534} RISK SURFACE:"
  ];
  for (const f of files) {
    const level = f.fragility >= 0.7 ? "CRITICAL" : f.fragility >= 0.5 ? "HIGH" : "MEDIUM";
    const reason = f.reason ?? "elevated risk";
    lines.push(`  ${f.path}  -  ${level} (${f.fragility.toFixed(2)})  -  ${reason}`);
  }
  return lines.join("\n");
}
__name(formatRiskSurface, "formatRiskSurface");
function formatMissingCoChanges(pairs) {
  const lines = [
    "\u{1F4CE} MISSING CO-CHANGE:"
  ];
  for (const p of pairs) {
    lines.push(`You modified ${p.file} but not ${p.partner}  -  these files change together ${Math.round(p.confidence * 100)}% of the time.`);
  }
  return lines.join("\n");
}
__name(formatMissingCoChanges, "formatMissingCoChanges");
function formatVitals(session, vitals) {
  const parts = [
    "\u{1FAC0} VITALS:"
  ];
  if (session.durationMin) {
    parts.push(`${session.durationMin} min session`);
  }
  const filesTouched = vitals?.filesModified ?? session.filesTouched ?? 0;
  const snapshotsCreated = vitals?.snapshotsCreated ?? session.snapshotsCreated ?? 0;
  if (filesTouched > 0) {
    parts.push(`${filesTouched} files touched`);
  }
  if (snapshotsCreated > 0) {
    parts.push(`${snapshotsCreated} snapshots`);
  }
  if (vitals) {
    parts.push(`Pulse: ${vitals.pulse}`);
    parts.push(`Pressure: ${vitals.pressure}%`);
    parts.push(`Trajectory: ${vitals.trajectory}`);
  }
  const unsnapshoted = filesTouched - snapshotsCreated;
  if (unsnapshoted > 3) {
    parts.push(`\u2192 ${unsnapshoted} unsnapshotted changes`);
  }
  return parts.join(" \xB7 ");
}
__name(formatVitals, "formatVitals");
function formatGuardHealth(health, touchedFiles) {
  const passCount = health.guards.filter((g) => g.status === "pass").length;
  const warnCount = health.guards.filter((g) => g.status === "warn").length;
  const failCount = health.guards.filter((g) => g.status === "fail").length;
  const total = health.guards.length;
  if (total === 0 && !health.refreshing) {
    return "";
  }
  if (total === 0 && health.refreshing) {
    return "\u{1F50D} GUARD HEALTH: checking...";
  }
  let statusIcon2 = "\u2705";
  if (failCount > 0) {
    statusIcon2 = "\u274C";
  } else if (warnCount > 0) {
    statusIcon2 = "\u26A0\uFE0F";
  }
  const staleSeconds = Math.floor(health.staleMs / 1e3);
  const staleText = staleSeconds > 60 ? `${Math.floor(staleSeconds / 60)}m ago` : staleSeconds > 0 ? `${staleSeconds}s ago` : "just now";
  const refreshingText = health.refreshing ? " (refreshing...)" : "";
  const lines = [];
  lines.push(`${statusIcon2} GUARD HEALTH: ${passCount}/${total} passing${refreshingText}`);
  if (failCount > 0 || warnCount > 0) {
    lines.push(`   Issues: ${failCount} failing, ${warnCount} warning${warnCount !== 1 ? "s" : ""}`);
  }
  const relevantIssues = [];
  const touchedSet = touchedFiles ? new Set(touchedFiles.map((f) => f.replace(/\\/g, "/").replace(/\/+$/, ""))) : /* @__PURE__ */ new Set();
  for (const guard of health.guards) {
    if (guard.status === "pass") {
      continue;
    }
    for (const file of guard.files) {
      const normalizedFilePath = file.path.replace(/\\/g, "/").replace(/\/+$/, "");
      if (touchedSet.size === 0 || touchedSet.has(normalizedFilePath) || relevantIssues.length < 3) {
        relevantIssues.push({
          guard: guard.guard,
          status: guard.status,
          file
        });
      }
    }
  }
  if (relevantIssues.length > 0) {
    lines.push("   Relevant issues:");
    for (const issue of relevantIssues.slice(0, 5)) {
      const icon = issue.status === "fail" ? "\u274C" : "\u26A0\uFE0F";
      const lineText = issue.file.line ? `:${issue.file.line}` : "";
      const guardName = issue.guard.split("-").map((w) => w.charAt(0).toUpperCase() + w.slice(1)).join(" ");
      lines.push(`   ${icon} ${issue.file.path}${lineText}  -  ${guardName}`);
      if (issue.file.message) {
        lines.push(`      ${issue.file.message.slice(0, 80)}${issue.file.message.length > 80 ? "..." : ""}`);
      }
    }
  }
  lines.push(`   Last checked: ${staleText}`);
  return lines.join("\n");
}
__name(formatGuardHealth, "formatGuardHealth");
function formatTopologySection(status, query, task) {
  const lines = [];
  const staleIcon = status.stale ? "\u26A0\uFE0F" : "\u{1F5FA}\uFE0F";
  const staleText = status.stale ? " (STALE  -  rescan recommended)" : "";
  lines.push(`${staleIcon} TOPOLOGY: ${status.framework ?? "unknown"}${staleText}`);
  if (status.critical_deviations && status.critical_deviations.length > 0) {
    lines.push(`   \u{1F6A8} Critical deviations (${status.critical_deviations.length}):`);
    for (const d of status.critical_deviations.slice(0, 3)) {
      lines.push(`   - ${d.file_path}: ${d.description}`);
    }
  }
  if (task && query?.briefing) {
    const briefing = query.briefing;
    lines.push("");
    lines.push(`\u{1F4CB} TASK SCOPE: "${task.slice(0, 50)}${task.length > 50 ? "..." : ""}"`);
    lines.push(`   Confidence: ${Math.round(briefing.confidence * 100)}%`);
    if (briefing.primary_files.length > 0) {
      lines.push(`   Primary files (${briefing.primary_files.length}):`);
      for (const f of briefing.primary_files.slice(0, 5)) {
        lines.push(`   - ${f.path} (${f.role})  -  ${f.reason}`);
      }
    }
    if (briefing.context_files.length > 0) {
      lines.push(`   Context files: ${briefing.context_files.map((f) => f.path).join(", ")}`);
    }
    if (briefing.test_files.length > 0) {
      lines.push(`   Test files: ${briefing.test_files.map((f) => f.path).join(", ")}`);
    }
    if (briefing.boundaries.modules.length > 0) {
      lines.push(`   Modules: ${briefing.boundaries.modules.join(", ")}`);
    }
    if (briefing.boundaries.must_not_cross.length > 0) {
      lines.push(`   \u26A0\uFE0F Do not touch: ${briefing.boundaries.must_not_cross.join(", ")}`);
    }
    if (briefing.behavioral.fragile_files.length > 0) {
      lines.push(`   \u{1F534} Fragile files in scope: ${briefing.behavioral.fragile_files.join(", ")}`);
    }
    if (briefing.deviations.length > 0) {
      lines.push(`   Deviations affecting scope (${briefing.deviations.length}):`);
      for (const d of briefing.deviations.slice(0, 2)) {
        lines.push(`   - [${d.severity}] ${d.description}`);
      }
    }
  } else if (task) {
    lines.push("   \u2139\uFE0F Task scope not available  -  topology may be missing or stale");
  }
  lines.push(`   Files indexed: ${status.file_count ?? 0} | Deviations: ${status.deviation_count ?? 0}`);
  return lines.join("\n");
}
__name(formatTopologySection, "formatTopologySection");
function composeHint2(params) {
  if (params.daemonError) {
    return "Pulse unavailable (daemon error). Proceed with caution; Vreko intelligence degraded.";
  }
  if (params.noSession) {
    return "No active session. Call vreko to open one before working.";
  }
  if (params.noFilesTracked) {
    return "Session open, no files tracked yet. First save will register files with Vreko.";
  }
  const lines = [];
  lines.push(`You are in ${params.workspaceName}, session ${params.sessionId} (${params.durationMinutes}m).`);
  const fragile = params.fragileFiles;
  if (fragile.length === 1) {
    const f = fragile[0];
    const evidence = f.evidence ? `; ${f.evidence}` : "";
    lines.push(`${f.path} is flagged fragile (${f.rollbackCount} rollbacks in 90 days${evidence}).`);
  } else if (fragile.length >= 2) {
    const list = fragile.map((f) => f.path).join(", ");
    const topByRollback = [
      ...fragile
    ].sort((a, b) => b.rollbackCount - a.rollbackCount)[0];
    lines.push(`${fragile.length} files in scope are flagged fragile: ${list}. Highest risk: ${topByRollback.path} (${topByRollback.rollbackCount} rollbacks).`);
  }
  const untouched = params.coChanges.filter((c) => !c.partnerTouched);
  if (untouched.length > 0) {
    const c = untouched[0];
    lines.push(`Co-change partner ${c.partner} hasn't been touched this session; if you're changing ${c.primary}, verify ${c.partner} expectations match.`);
  }
  if (params.warnings.length > 0) {
    const severityRank = {
      critical: 3,
      warn: 2,
      info: 1
    };
    const top = [
      ...params.warnings
    ].sort((a, b) => (severityRank[b.severity] ?? 0) - (severityRank[a.severity] ?? 0))[0];
    lines.push(`Active warnings: ${params.warnings.length}. Highest severity: ${top.message}.`);
  }
  const hasFragility = fragile.length > 0;
  const hasWarnings = params.warnings.length > 0;
  if (!hasFragility && !hasWarnings) {
    lines.push("No concerning signals. Proceed.");
  } else if (hasFragility && !hasWarnings) {
    lines.push(`Recommend: before editing ${fragile[0].path}, restate change plan and call vreko_learn.`);
  } else if (!hasFragility && hasWarnings) {
    lines.push("Recommend: address warning before continuing.");
  } else {
    lines.push(`Recommend: before editing ${fragile[0].path}, restate change plan and call vreko_learn. Also address warning before continuing.`);
  }
  return lines.join("\n");
}
__name(composeHint2, "composeHint");
function buildComposeHintParams(input) {
  const fragileFiles = input.fragile.filter((f) => f.fragility >= RISK_THRESHOLD).map((f) => ({
    path: f.path,
    rollbackCount: f.rollbackCount ?? 0,
    evidence: f.reason
  }));
  const coChanges = input.missingCoChanges.map((c) => ({
    primary: c.file,
    partner: c.partner,
    partnerTouched: false
  }));
  const warnings = input.warnings.filter((w) => w.severity !== "info").map((w) => ({
    message: w.message,
    severity: w.severity
  }));
  return {
    workspaceName: basename(input.workspace) || input.workspace,
    sessionId: input.sessionId ?? "unknown",
    durationMinutes: Math.max(0, input.durationMinutes | 0),
    fragileFiles,
    coChanges,
    warnings
  };
}
__name(buildComposeHintParams, "buildComposeHintParams");
function findMissingCoChanges(touchedFiles, pairs) {
  if (!touchedFiles || !pairs) {
    return [];
  }
  const touchedSet = new Set(touchedFiles);
  const missing = [];
  for (const pair of pairs) {
    if (pair.confidence < CO_CHANGE_CONFIDENCE_THRESHOLD) {
      continue;
    }
    if (touchedSet.has(pair.source) && !touchedSet.has(pair.target)) {
      missing.push({
        file: pair.source,
        partner: pair.target,
        confidence: pair.confidence
      });
    }
    if (touchedSet.has(pair.target) && !touchedSet.has(pair.source)) {
      missing.push({
        file: pair.target,
        partner: pair.source,
        confidence: pair.confidence
      });
    }
  }
  return missing.slice(0, 3);
}
__name(findMissingCoChanges, "findMissingCoChanges");
function countMissingCoChanges(touchedFiles, pairs) {
  return findMissingCoChanges(touchedFiles, pairs).length;
}
__name(countMissingCoChanges, "countMissingCoChanges");
var RISK_THRESHOLD;
var CO_CHANGE_CONFIDENCE_THRESHOLD;
var DURATION_SHORT;
var DURATION_MEDIUM;
var DURATION_LONG;
var handleVrekoPulse;
var vrekoPulseTool;
var init_vreko_pulse = __esm2({
  "src/tools/v2/vreko-pulse.ts"() {
    init_input_sanitization();
    init_credit_registry();
    init_utils3();
    RISK_THRESHOLD = 0.3;
    CO_CHANGE_CONFIDENCE_THRESHOLD = 0.5;
    DURATION_SHORT = 5;
    DURATION_MEDIUM = 30;
    DURATION_LONG = 60;
    __name8(getDurationGuidance, "getDurationGuidance");
    __name8(normalizeSessionStatus, "normalizeSessionStatus");
    handleVrekoPulse = /* @__PURE__ */ __name8(async (args, context) => {
      const params = args ?? {};
      const workspace = resolveWorkspace(context);
      const daemon = getDaemonClient2(workspace);
      const rawTask = params.task ? normalizeContent(params.task).slice(0, MAX_LENGTHS.task) : void 0;
      const rawQuery = params.query ? normalizeContent(params.query).slice(0, MAX_LENGTHS.task) : void 0;
      const effectiveQuery = rawTask ?? rawQuery;
      const [sessionResult, vitalsResult, warningsResult, fragileResult, coChangeResult, guardHealthResult, topologyStatusResult, topologyQueryResult, learningSearchResult] = await Promise.allSettled([
        daemon.request("session/current", buildSessionCurrentParams(workspace)),
        daemon.request("session/vitals", {
          workspacePath: workspace
        }),
        daemon.request("intelligence/warnings", {
          workspace
        }),
        daemon.request("intelligence/fragile-files", {
          workspace
        }),
        daemon.request("intelligence/co-changes", {
          workspace
        }),
        daemon.request("health/workspace", {
          workspace,
          profile: "fast"
        }),
        // Phase D CTI: Always get topology status for critical deviations
        daemon.request("topology/status", {
          workspace
        }),
        // Phase D CTI: Query topology with task context when provided
        effectiveQuery ? daemon.request("topology/query", {
          workspace,
          task: effectiveQuery
        }) : Promise.resolve(null),
        // Report card v2 fix: BM25 learning search when query/task is provided
        effectiveQuery ? daemon.request("learning/search", buildLearningSearchParams(workspace, effectiveQuery, 5)) : Promise.resolve(null)
      ]);
      const rawSession = sessionResult.status === "fulfilled" ? sessionResult.value : null;
      const session = normalizeSessionStatus(rawSession);
      const vitals = vitalsResult.status === "fulfilled" ? vitalsResult.value : null;
      const warnings = warningsResult.status === "fulfilled" ? warningsResult.value : null;
      const fragile = fragileResult.status === "fulfilled" ? fragileResult.value : null;
      const coChanges = mapCoChangePairs(coChangeResult.status === "fulfilled" ? coChangeResult.value : null);
      const guardHealth = guardHealthResult.status === "fulfilled" ? guardHealthResult.value : null;
      const topologyStatus = topologyStatusResult.status === "fulfilled" ? topologyStatusResult.value : null;
      const topologyQuery = topologyQueryResult.status === "fulfilled" ? topologyQueryResult.value : null;
      const learningSearch = learningSearchResult.status === "fulfilled" ? learningSearchResult.value : null;
      if (!session && sessionResult.status === "rejected") {
        if (isDaemonConnectionError(sessionResult.reason)) {
          const errorResponse = formatDaemonError();
          const llmHint = composeHint2({
            workspaceName: basename(workspace) || workspace,
            sessionId: "unknown",
            durationMinutes: 0,
            fragileFiles: [],
            coChanges: [],
            warnings: [],
            daemonError: true
          });
          return {
            ...errorResponse,
            llmHint,
            structuredContent: {
              ...errorResponse.structuredContent ?? {},
              llmHint
            }
          };
        }
      }
      if (!session || !session.active) {
        const llmHint = composeHint2({
          workspaceName: basename(workspace) || workspace,
          sessionId: "unknown",
          durationMinutes: 0,
          fragileFiles: [],
          coChanges: [],
          warnings: [],
          noSession: true
        });
        return {
          content: [
            {
              type: "text",
              text: 'No active session. Call vreko({ task: "..." }) to start one.'
            }
          ],
          llmHint,
          structuredContent: {
            llmHint
          }
        };
      }
      const response = formatPulseResponse({
        session,
        vitals,
        warnings,
        fragile,
        coChanges,
        guardHealth,
        topologyStatus,
        topologyQuery,
        learningSearch,
        focus: params.focus,
        task: effectiveQuery
      });
      const filteredWarningsForHint = (warnings?.warnings ?? []).filter((w) => w.severity !== "info");
      const hintParams = session.touchedFiles && session.touchedFiles.length === 0 ? {
        workspaceName: basename(workspace) || workspace,
        sessionId: rawSession?.id ?? "unknown",
        durationMinutes: Math.max(0, (session.durationMin ?? 0) | 0),
        fragileFiles: [],
        coChanges: [],
        warnings: [],
        noFilesTracked: true
      } : buildComposeHintParams({
        workspace,
        sessionId: rawSession?.id,
        durationMinutes: session.durationMin ?? 0,
        fragile: fragile?.files ?? [],
        missingCoChanges: findMissingCoChanges(session.touchedFiles, coChanges),
        warnings: filteredWarningsForHint
      });
      const hasHistory = hintParams.noFilesTracked === true || (hintParams.fragileFiles?.length ?? 0) > 0 || (hintParams.coChanges?.length ?? 0) > 0 || (hintParams.warnings?.length ?? 0) > 0;
      if (hasHistory) {
        const llmHint = composeHint2(hintParams);
        response.llmHint = llmHint;
        response.structuredContent = {
          ...response.structuredContent ?? {},
          llmHint
        };
      }
      const creditEstimate = getToolCreditEstimate("vreko_pulse");
      if (creditEstimate) {
        response.structuredContent = {
          ...response.structuredContent ?? {},
          creditsEstimate: creditEstimate
        };
      }
      void daemon.request("intelligence/capture", {
        workspace,
        type: "pulse_check",
        data: {
          alertCount: warnings?.warnings?.length ?? 0,
          fragileFilesInScope: fragile?.files?.length ?? 0,
          missingCoChanges: countMissingCoChanges(session.touchedFiles, coChanges),
          sessionDurationMin: session.durationMin ?? 0
        }
      })?.catch(() => {
      });
      return response;
    }, "handleVrekoPulse");
    __name8(formatPulseResponse, "formatPulseResponse");
    __name8(extractLearningsFromSearch, "extractLearningsFromSearch");
    __name8(formatQueryLearnings, "formatQueryLearnings");
    __name8(formatHealthyResponse, "formatHealthyResponse");
    __name8(formatAlerts, "formatAlerts");
    __name8(formatRiskSurface, "formatRiskSurface");
    __name8(formatMissingCoChanges, "formatMissingCoChanges");
    __name8(formatVitals, "formatVitals");
    __name8(formatGuardHealth, "formatGuardHealth");
    __name8(formatTopologySection, "formatTopologySection");
    __name8(composeHint2, "composeHint");
    __name8(buildComposeHintParams, "buildComposeHintParams");
    __name8(findMissingCoChanges, "findMissingCoChanges");
    __name8(countMissingCoChanges, "countMissingCoChanges");
    vrekoPulseTool = {
      name: "vreko_pulse",
      description: `Read-only session health check. Does not write, learn, or create snapshots.

WHAT IT DOES:
- Returns current session vitals: pulse (activity level), pressure
  (ratio of unprotected files), trajectory (increasing/stable/decreasing)
- Returns guard health status: pass/warn/fail counts from workspace health checks
- Surfaces active warnings, fragile files in scope, and co-change gaps
- Shows which files you've touched and how many are snapshot-protected
- When task provided: returns topology-enhanced briefing with relevant files,
  architectural boundaries, and critical deviations
- When task or query provided: BM25 searches the knowledge base and returns relevant learnings

WHEN TO CALL:
- After modifying 3+ files, to check if pressure is building
- After 3+ tool calls on the same task without a checkpoint
- Before a destructive change (delete, revert, major refactor)
- When multiple valid implementation paths exist  -  before committing to one
- Before asking the user a clarifying question (capture state first)
- When switching context between different areas of the codebase
- To verify the session is still active after an interruption
- When starting work on a task  -  provide task description for oriented briefing

WHEN NOT TO CALL:
- To start a session (use vreko instead)
- To end a session (use vreko_end instead)
- Immediately after vreko  -  the session just started, there's nothing to check yet

WORKFLOW POSITION: 3 of 4 (optional, repeatable)
  vreko \u2192 [your work] \u2192 vreko_pulse \u2192 [more work] \u2192 vreko_pulse \u2192 vreko_end

WHAT YOU GET BACK (natural language text):
- Session duration and file count
- Vitals: pulse (resting/elevated/racing/critical),
  pressure (0-100%, ratio of files without snapshots),
  trajectory (increasing/stable/decreasing)
- Whether a snapshot is recommended
- Active warnings from the intelligence engine
- Fragile files you've touched
- Co-change gaps (files that usually change together but you've only touched one)
- Topology: framework, critical deviations, and task-scoped briefing (when task provided)

EXAMPLE:
vreko_pulse() or vreko_pulse({ focus: "risk" }) or vreko_pulse({ task: "add rate limiting to API" })
To query learnings: vreko_pulse({ query: "What should I know about authentication?" })`,
      inputSchema: {
        type: "object",
        properties: {
          focus: {
            type: "string",
            title: "Focus Area",
            description: `Narrow the response to a specific aspect.

"risk"       -  Emphasize risk factors and warnings for files you've touched
"cochanges"  -  Show files that typically change together with your modified files.
              Useful when you suspect you're missing a related change.
"vitals"     -  Focus on session health numbers: pulse, pressure, trajectory,
              and whether a snapshot is recommended

Omit for a balanced overview of all aspects.`,
            enum: [
              "risk",
              "cochanges",
              "vitals"
            ]
          },
          task: {
            type: "string",
            title: "Task Description",
            description: `Provide task context for topology-enhanced briefing.
When provided, returns a task-scoped briefing including:
- Primary files you likely need to modify
- Context files for patterns/conventions
- Test files to update
- Architectural boundaries (modules, layers)
- Critical and task-relevant deviations
- Relevant learnings from the knowledge base (BM25 search)

Example: "add rate limiting to API endpoints"`
          },
          query: {
            type: "string",
            title: "Query",
            description: `Retrieve relevant learnings from the knowledge base. Read-only  -  does not store anything. To write a new learning, use vreko_learn instead.

Results are returned from BM25 search over captured learnings.

Example: "What should I know before changing the auth middleware?"
Example: "patterns for database transaction handling"`
          }
        },
        required: []
      },
      annotations: {
        title: "\u{1FAC0} Vreko Pulse",
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true,
        openWorldHint: true
      },
      tier: "free"
    };
  }
});
var v2_exports = {};
__export2(v2_exports, {
  V2_HANDLERS: /* @__PURE__ */ __name(() => V2_HANDLERS, "V2_HANDLERS"),
  V2_MINIMAL_TOOLS: /* @__PURE__ */ __name(() => V2_MINIMAL_TOOLS, "V2_MINIMAL_TOOLS"),
  V2_TOOLS: /* @__PURE__ */ __name(() => V2_TOOLS, "V2_TOOLS"),
  composeHint: /* @__PURE__ */ __name(() => composeHint2, "composeHint"),
  handleVreko: /* @__PURE__ */ __name(() => handleVreko, "handleVreko"),
  handleVrekoEnd: /* @__PURE__ */ __name(() => handleVrekoEnd, "handleVrekoEnd"),
  handleVrekoLearn: /* @__PURE__ */ __name(() => handleVrekoLearn, "handleVrekoLearn"),
  handleVrekoPulse: /* @__PURE__ */ __name(() => handleVrekoPulse, "handleVrekoPulse"),
  vrekoEndTool: /* @__PURE__ */ __name(() => vrekoEndTool, "vrekoEndTool"),
  vrekoLearnTool: /* @__PURE__ */ __name(() => vrekoLearnTool, "vrekoLearnTool"),
  vrekoPulseTool: /* @__PURE__ */ __name(() => vrekoPulseTool, "vrekoPulseTool"),
  vrekoTool: /* @__PURE__ */ __name(() => vrekoTool, "vrekoTool")
});
var V2_TOOLS;
var V2_MINIMAL_TOOLS;
var V2_HANDLERS;
var init_v2 = __esm2({
  "src/tools/v2/index.ts"() {
    init_vreko();
    init_vreko_end();
    init_vreko_learn();
    init_vreko_pulse();
    init_vreko();
    init_vreko_end();
    init_vreko_learn();
    init_vreko_pulse();
    V2_TOOLS = [
      vrekoTool,
      vrekoLearnTool,
      vrekoEndTool,
      vrekoPulseTool
    ];
    V2_MINIMAL_TOOLS = [
      vrekoTool,
      vrekoLearnTool,
      vrekoEndTool
    ];
    V2_HANDLERS = {
      vreko: handleVreko,
      vreko_learn: handleVrekoLearn,
      vreko_end: handleVrekoEnd,
      vreko_pulse: handleVrekoPulse
    };
  }
});
function getIntegrationOrchestratorService(config2, logger5) {
  if (!instance2) {
    instance2 = new IntegrationOrchestratorService(config2, logger5);
  }
  return instance2;
}
__name(getIntegrationOrchestratorService, "getIntegrationOrchestratorService");
var DEFAULT_CONFIG3;
var IntegrationOrchestratorService;
var defaultLogger2;
var instance2;
var init_integration_orchestrator_service = __esm2({
  "src/services/integration-orchestrator-service.ts"() {
    init_local_service_adapter();
    DEFAULT_CONFIG3 = {
      enabled: true,
      github: {
        enabled: false,
        timeoutMs: 5e3
      },
      context7: {
        enabled: false,
        timeoutMs: 5e3
      },
      sentry: {
        enabled: false,
        timeoutMs: 5e3
      },
      cache: {
        enabled: true,
        githubTtlMs: 5 * 60 * 1e3,
        context7TtlMs: 30 * 60 * 1e3,
        sentryTtlMs: 5 * 60 * 1e3,
        maxEntries: 100
      },
      circuitBreaker: {
        failureThreshold: 3,
        recoveryTimeMs: 60 * 1e3
      }
    };
    IntegrationOrchestratorService = class {
      static {
        __name(this, "IntegrationOrchestratorService");
      }
      static {
        __name8(this, "IntegrationOrchestratorService");
      }
      config;
      logger;
      workspaceRoot;
      // Circuit breaker state per integration
      circuits = /* @__PURE__ */ new Map();
      // LRU Cache per integration (key → CacheEntry)
      githubCache = /* @__PURE__ */ new Map();
      context7Cache = /* @__PURE__ */ new Map();
      sentryCache = /* @__PURE__ */ new Map();
      // Cache statistics
      cacheHits = 0;
      cacheMisses = 0;
      // Connection state
      initialized = false;
      constructor(config2, logger5) {
        this.config = this.mergeConfig(config2);
        this.logger = logger5 ?? defaultLogger2;
        this.workspaceRoot = config2?.workspaceRoot;
        this.circuits.set("github", {
          failures: 0,
          lastFailure: 0,
          isOpen: false
        });
        this.circuits.set("context7", {
          failures: 0,
          lastFailure: 0,
          isOpen: false
        });
        this.circuits.set("sentry", {
          failures: 0,
          lastFailure: 0,
          isOpen: false
        });
      }
      // ===========================================================================
      // LIFECYCLE
      // ===========================================================================
      /**
      * Initialize all enabled integrations
      *
      * Day 4: Integration clients are now managed by the daemon.
      * This method validates configuration and sets up circuit breakers.
      */
      async initialize() {
        if (this.initialized) {
          return;
        }
        if (!this.config.enabled) {
          this.logger.info("Integration orchestrator disabled");
          this.initialized = true;
          return;
        }
        const enabledIntegrations = [];
        if (this.config.github.enabled) {
          enabledIntegrations.push("github");
        }
        if (this.config.context7.enabled) {
          enabledIntegrations.push("context7");
        }
        if (this.config.sentry.enabled) {
          enabledIntegrations.push("sentry");
        }
        this.logger.info(`Integration orchestrator initialized: ${enabledIntegrations.length} integrations configured (daemon-managed)`);
        this.initialized = true;
      }
      /**
      * Shutdown all integrations and clear caches
      * Day 4: Daemon manages client lifecycle, we just clear caches.
      */
      async shutdown() {
        this.githubCache.clear();
        this.context7Cache.clear();
        this.sentryCache.clear();
        this.initialized = false;
        this.logger.info("Integration orchestrator shutdown complete");
      }
      // ===========================================================================
      // HEALTH
      // ===========================================================================
      /**
      * Get health status of all integrations
      */
      getHealth() {
        const github = this.getIntegrationStatus("github");
        const context7 = this.getIntegrationStatus("context7");
        const sentry = this.getIntegrationStatus("sentry");
        const connectedCount = [
          github,
          context7,
          sentry
        ].filter((s) => s.connected && !s.circuitOpen).length;
        const enabledCount = [
          github,
          context7,
          sentry
        ].filter((s) => s.enabled).length;
        let status;
        if (enabledCount === 0 || connectedCount === enabledCount) {
          status = "healthy";
        } else if (connectedCount > 0) {
          status = "degraded";
        } else {
          status = "unavailable";
        }
        return {
          status,
          integrations: {
            github,
            context7,
            sentry
          },
          cache: {
            hits: this.cacheHits,
            misses: this.cacheMisses,
            size: this.githubCache.size + this.context7Cache.size + this.sentryCache.size
          }
        };
      }
      getIntegrationStatus(name) {
        const circuit = this.circuits.get(name);
        if (!circuit) {
          throw new Error(`Circuit for ${name} not initialized`);
        }
        const config2 = this.config[name];
        const connected = config2.enabled && !circuit.isOpen;
        return {
          enabled: config2.enabled,
          connected,
          circuitOpen: circuit.isOpen,
          failureCount: circuit.failures,
          lastError: circuit.failures > 0 ? `${circuit.failures} consecutive failures` : void 0
        };
      }
      // ===========================================================================
      // ENRICHMENT
      // ===========================================================================
      /**
      * Enrich context with data from all available integrations
      *
      * Day 4: Uses daemon IPC for integration context.
      * Returns null if no integrations are available. Never throws.
      *
      * @param params - Enrichment parameters
      * @returns Enriched context or null if unavailable
      */
      async enrichContext(params) {
        if (!this.config.enabled || !this.initialized) {
          return null;
        }
        if (!this.workspaceRoot) {
          this.logger.warn("Cannot enrich context: workspaceRoot not set");
          return null;
        }
        const startTime = Date.now();
        try {
          const daemonResult = await getIntegrationContextViaDaemon(this.workspaceRoot, {
            files: params.files,
            intent: params.intent,
            skipCache: params.skipCache
          });
          if (!daemonResult.success || !daemonResult.context) {
            return null;
          }
          const daemonContext = daemonResult.context;
          const contexts = /* @__PURE__ */ new Map();
          const contextsMap = daemonContext.contexts;
          if (contextsMap && typeof contextsMap === "object") {
            for (const [filePath, ctx] of Object.entries(contextsMap)) {
              const aggregated = {
                file: filePath,
                gatheredAt: /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString(),
                durationMs: Date.now() - startTime,
                sources: ctx.sources || [],
                riskFactors: daemonContext.riskFactors || []
              };
              contexts.set(filePath, aggregated);
            }
          }
          const riskFactors = (daemonContext.riskFactors || []).map((f) => ({
            name: f.name,
            score: f.score,
            description: f.description,
            source: f.source,
            suggestion: f.suggestion,
            docsUrl: f.docsUrl
          }));
          return {
            contexts,
            riskFactors,
            riskScore: daemonContext.riskScore || 0,
            sources: daemonContext.sources || [],
            durationMs: Date.now() - startTime
          };
        } catch (error) {
          this.logger.warn("Failed to enrich context via daemon", {
            error: error instanceof Error ? error.message : "Unknown"
          });
          return null;
        }
      }
      // ===========================================================================
      // INDIVIDUAL CONTEXT GETTERS
      // ===========================================================================
      /**
      * Get GitHub context for a file
      *
      * Day 4: Uses daemon IPC for integration context.
      * Returns null if GitHub integration is unavailable or circuit is open.
      */
      async getGitHubContext(filePath, _skipCache = false) {
        if (!this.canUseIntegration("github") || !this.workspaceRoot) {
          return null;
        }
        if (!_skipCache && this.config.cache.enabled) {
          const cached = this.getFromCache(this.githubCache, filePath, this.config.cache.githubTtlMs);
          if (cached) {
            this.cacheHits++;
            return cached;
          }
          this.cacheMisses++;
        }
        try {
          const daemonResult = await getIntegrationContextViaDaemon(this.workspaceRoot, {
            files: [
              {
                path: filePath
              }
            ],
            intent: "github"
          });
          if (daemonResult.success && daemonResult.context) {
            const githubCtx = daemonResult.context.githubContext;
            if (githubCtx) {
              this.recordSuccess("github");
              this.setCache(this.githubCache, filePath, githubCtx);
              return githubCtx;
            }
          }
          return null;
        } catch (error) {
          const msg = error instanceof Error ? error.message : "Unknown error";
          this.recordFailure("github", msg);
          return null;
        }
      }
      /**
      * Get Context7 documentation validation for a file
      *
      * Day 4: Uses daemon IPC for integration context.
      * Validates imports, API usage, and patterns against current documentation.
      * Returns null if Context7 integration is unavailable or circuit is open.
      *
      * @param filePath - Path to the file being validated
      * @param content - File content to analyze
      * @param language - Language identifier (e.g., "typescript", "javascript")
      * @param skipCache - Skip cache and fetch fresh data
      */
      async getDocsValidation(filePath, content, language, skipCache = false) {
        if (!this.canUseIntegration("context7") || !this.workspaceRoot) {
          return null;
        }
        const contentHash = this.hashContent(content);
        const cacheKey = `${filePath}:${language}:${contentHash}`;
        if (!skipCache && this.config.cache.enabled) {
          const cached = this.getFromCache(this.context7Cache, cacheKey, this.config.cache.context7TtlMs);
          if (cached) {
            this.cacheHits++;
            return cached;
          }
          this.cacheMisses++;
        }
        try {
          const daemonResult = await getIntegrationContextViaDaemon(this.workspaceRoot, {
            files: [
              {
                path: filePath,
                content,
                language
              }
            ],
            intent: "context7"
          });
          if (daemonResult.success && daemonResult.context) {
            const docsCtx = daemonResult.context.docsValidation;
            if (docsCtx) {
              this.recordSuccess("context7");
              this.setCache(this.context7Cache, cacheKey, docsCtx);
              return docsCtx;
            }
          }
          return null;
        } catch (error) {
          const msg = error instanceof Error ? error.message : "Unknown error";
          this.recordFailure("context7", msg);
          return null;
        }
      }
      /**
      * Simple content hash for cache key generation
      */
      hashContent(content) {
        const prefix = content.slice(0, 100).replace(/\s/g, "");
        return `${prefix.length}_${content.length}`;
      }
      /**
      * Get Sentry error context for a file
      *
      * Day 4: Uses daemon IPC for integration context.
      * Returns null if Sentry integration is unavailable or circuit is open.
      */
      async getSentryContext(filePath, skipCache = false) {
        if (!this.canUseIntegration("sentry") || !this.workspaceRoot) {
          return null;
        }
        if (!skipCache && this.config.cache.enabled) {
          const cached = this.getFromCache(this.sentryCache, filePath, this.config.cache.sentryTtlMs);
          if (cached) {
            this.cacheHits++;
            return cached;
          }
          this.cacheMisses++;
        }
        try {
          const daemonResult = await getIntegrationContextViaDaemon(this.workspaceRoot, {
            files: [
              {
                path: filePath
              }
            ],
            intent: "sentry"
          });
          if (daemonResult.success && daemonResult.context) {
            const sentryCtx = daemonResult.context.sentryContext;
            if (sentryCtx) {
              this.recordSuccess("sentry");
              this.setCache(this.sentryCache, filePath, sentryCtx);
              return sentryCtx;
            }
          }
          return null;
        } catch (error) {
          const msg = error instanceof Error ? error.message : "Unknown error";
          this.recordFailure("sentry", msg);
          return null;
        }
      }
      // ===========================================================================
      // CIRCUIT BREAKER
      // ===========================================================================
      /**
      * Check if an integration can be used (enabled, connected, circuit closed)
      */
      canUseIntegration(name) {
        const config2 = this.config[name];
        if (!config2.enabled) {
          return false;
        }
        const circuit = this.circuits.get(name);
        if (!circuit) {
          throw new Error(`Circuit for ${name} not initialized`);
        }
        if (circuit.isOpen) {
          const timeSinceFailure = Date.now() - circuit.lastFailure;
          if (timeSinceFailure >= this.config.circuitBreaker.recoveryTimeMs) {
            circuit.isOpen = false;
            circuit.failures = 0;
            this.logger.info(`Circuit breaker reset for ${name} (recovery time passed)`);
          } else {
            return false;
          }
        }
        return true;
      }
      /**
      * Record a successful call (resets failure count)
      */
      recordSuccess(name) {
        const circuit = this.circuits.get(name);
        if (!circuit) {
          throw new Error(`Circuit for ${name} not initialized`);
        }
        circuit.failures = 0;
        circuit.isOpen = false;
      }
      /**
      * Record a failed call (may open circuit)
      */
      recordFailure(name, error) {
        const circuit = this.circuits.get(name);
        if (!circuit) {
          throw new Error(`Circuit for ${name} not initialized`);
        }
        circuit.failures++;
        circuit.lastFailure = Date.now();
        if (circuit.failures >= this.config.circuitBreaker.failureThreshold) {
          circuit.isOpen = true;
          this.logger.warn(`Circuit breaker opened for ${name} after ${circuit.failures} failures`, {
            error
          });
        }
      }
      // ===========================================================================
      // CACHE
      // ===========================================================================
      getFromCache(cache, key, _ttlMs) {
        const entry = cache.get(key);
        if (!entry) {
          return null;
        }
        if (Date.now() > entry.expiresAt) {
          cache.delete(key);
          return null;
        }
        return entry.data;
      }
      setCache(cache, key, data) {
        if (cache.size >= this.config.cache.maxEntries) {
          const firstKey = cache.keys().next().value;
          if (firstKey) {
            cache.delete(firstKey);
          }
        }
        let ttlMs = this.config.cache.githubTtlMs;
        if (cache === this.context7Cache) {
          ttlMs = this.config.cache.context7TtlMs;
        }
        if (cache === this.sentryCache) {
          ttlMs = this.config.cache.sentryTtlMs;
        }
        cache.set(key, {
          data,
          expiresAt: Date.now() + ttlMs
        });
      }
      /**
      * Clear all caches
      */
      clearCache() {
        this.githubCache.clear();
        this.context7Cache.clear();
        this.sentryCache.clear();
        this.cacheHits = 0;
        this.cacheMisses = 0;
      }
      mergeConfig(partial) {
        if (!partial) {
          return {
            ...DEFAULT_CONFIG3
          };
        }
        return {
          enabled: partial.enabled ?? DEFAULT_CONFIG3.enabled,
          github: {
            ...DEFAULT_CONFIG3.github,
            ...partial.github
          },
          context7: {
            ...DEFAULT_CONFIG3.context7,
            ...partial.context7
          },
          sentry: {
            ...DEFAULT_CONFIG3.sentry,
            ...partial.sentry
          },
          cache: {
            ...DEFAULT_CONFIG3.cache,
            ...partial.cache
          },
          circuitBreaker: {
            ...DEFAULT_CONFIG3.circuitBreaker,
            ...partial.circuitBreaker
          }
        };
      }
    };
    defaultLogger2 = {
      debug: /* @__PURE__ */ __name8((_msg, _data) => {
      }, "debug"),
      info: /* @__PURE__ */ __name8((_msg, _data) => {
      }, "info"),
      warn: /* @__PURE__ */ __name8((_msg, _data) => {
      }, "warn"),
      error: /* @__PURE__ */ __name8((_msg, _data) => {
      }, "error")
    };
    instance2 = null;
    __name8(getIntegrationOrchestratorService, "getIntegrationOrchestratorService");
  }
});
function normalizeLearningType(raw) {
  const t = raw?.toLowerCase();
  if (t && VALID_LEARNING_TYPES.has(t)) {
    return t;
  }
  return "pattern";
}
__name(normalizeLearningType, "normalizeLearningType");
function truncate(str, maxLen) {
  if (str.length <= maxLen) {
    return str;
  }
  return `${str.slice(0, maxLen - 1)}\u2026`;
}
__name(truncate, "truncate");
function getLearningTrajectory(workspaceRoot) {
  try {
    const service = createTieredLearningService(workspaceRoot);
    const usageStats = service.getUsageStats();
    const totalTracked = Object.keys(usageStats).length;
    const hotTierPath = `${workspaceRoot}/.vreko/learnings/hot.jsonl`;
    let hotCount = 0;
    try {
      if (existsSync(hotTierPath)) {
        const content = readFileSync(hotTierPath, "utf-8");
        hotCount = content.trim().split("\n").filter(Boolean).length;
      }
    } catch {
      hotCount = Object.values(usageStats).filter((s) => s.accessCount >= 3 || s.appliedCount >= 1).length;
    }
    const maxHotSize = 20;
    return {
      totalTracked,
      hotCount,
      maxHotSize
    };
  } catch {
    return null;
  }
}
__name(getLearningTrajectory, "getLearningTrajectory");
var VALID_LEARNING_TYPES;
var LEARNING_TYPE_META;
var init_learning_meta = __esm2({
  "src/utils/learning-meta.ts"() {
    init_tiered_learning_service();
    VALID_LEARNING_TYPES = /* @__PURE__ */ new Set([
      "pattern",
      "pitfall",
      "efficiency",
      "discovery",
      "workflow"
    ]);
    LEARNING_TYPE_META = {
      pattern: {
        emoji: "\u{1F3AF}",
        verb: "Pattern captured",
        future: "will auto-surface in similar contexts"
      },
      pitfall: {
        emoji: "\u{1F6E1}\uFE0F",
        verb: "Pitfall recorded",
        future: "will warn you before repeating this mistake"
      },
      efficiency: {
        emoji: "\u26A1",
        verb: "Efficiency insight saved",
        future: "will speed up future similar work"
      },
      discovery: {
        emoji: "\u{1F4CD}",
        verb: "Discovery mapped",
        future: "builds your repo's mental model"
      },
      workflow: {
        emoji: "\u{1F504}",
        verb: "Workflow improvement noted",
        future: "will guide future task sequences"
      }
    };
    __name8(normalizeLearningType, "normalizeLearningType");
    __name8(truncate, "truncate");
    __name8(getLearningTrajectory, "getLearningTrajectory");
  }
});
function getWorkspaceRoot(context) {
  if (!context.workspaceRoot) {
    throw new Error("No workspace root available. The MCP server must be initialized with a workspace root. Ensure the client provides the workspace path during initialization.");
  }
  return context.workspaceRoot;
}
__name(getWorkspaceRoot, "getWorkspaceRoot");
function calculateConfidence(warningCount, learningCount, violationCount) {
  let confidence = 85;
  confidence -= warningCount * 10;
  confidence -= violationCount * 15;
  confidence += Math.min(learningCount * 5, 15);
  return Math.max(10, Math.min(100, confidence));
}
__name(calculateConfidence, "calculateConfidence");
function getRecommendation(confidence) {
  if (confidence >= 80) {
    return "proceed";
  }
  if (confidence >= 60) {
    return "review";
  }
  if (confidence >= 40) {
    return "caution";
  }
  return "stop";
}
__name(getRecommendation, "getRecommendation");
async function findSimilarPatterns(targetFiles, workspaceRoot, limit = 5) {
  return findSimilarPatternsViaDaemon(workspaceRoot, targetFiles, limit);
}
__name(findSimilarPatterns, "findSimilarPatterns");
function buildStudyRecommendations(_task, files, intent, workspaceRoot, violations, learnings) {
  const recommendations = [];
  const addedFiles = /* @__PURE__ */ new Set();
  for (const violation of violations.slice(0, 2)) {
    if (!addedFiles.has(violation.file)) {
      addedFiles.add(violation.file);
      recommendations.push({
        file: violation.file,
        reason: `Past violation: ${violation.type}`,
        priority: "high"
      });
    }
  }
  for (const learning of learnings.slice(0, 3)) {
    const fileMatch = learning.action.match(/([\w-]+\.(?:ts|tsx|js|jsx))/gi);
    if (fileMatch) {
      for (const mentioned of fileMatch) {
        const found = findFileInWorkspace(mentioned, workspaceRoot);
        if (found && !addedFiles.has(found)) {
          addedFiles.add(found);
          recommendations.push({
            file: found,
            reason: `From learning: ${learning.trigger.slice(0, 40)}`,
            priority: "medium"
          });
        }
      }
    }
  }
  const intentFiles = getIntentBasedFiles(intent, workspaceRoot);
  for (const { file, reason } of intentFiles) {
    if (!addedFiles.has(file)) {
      addedFiles.add(file);
      recommendations.push({
        file,
        reason,
        priority: "low"
      });
    }
  }
  for (const file of files.slice(0, 2)) {
    const related = findRelatedFiles(file, workspaceRoot);
    for (const relFile of related) {
      if (!addedFiles.has(relFile)) {
        addedFiles.add(relFile);
        recommendations.push({
          file: relFile,
          reason: `Related to ${basename(file)}`,
          priority: "medium"
        });
      }
    }
  }
  return recommendations.slice(0, 5);
}
__name(buildStudyRecommendations, "buildStudyRecommendations");
function findFileInWorkspace(filename, workspaceRoot) {
  const searchDirs = [
    "src",
    "packages",
    "apps",
    "lib"
  ];
  for (const dir of searchDirs) {
    const dirPath = join(workspaceRoot, dir);
    if (!existsSync(dirPath)) {
      continue;
    }
    try {
      const found = findFileRecursive(dirPath, filename);
      if (found) {
        return relative(workspaceRoot, found);
      }
    } catch {
    }
  }
  return null;
}
__name(findFileInWorkspace, "findFileInWorkspace");
function findFileRecursive(dir, filename, depth = 0) {
  if (depth > 5) {
    return null;
  }
  try {
    const entries = readdirSync(dir, {
      withFileTypes: true
    });
    for (const entry of entries) {
      const fullPath = join(dir, entry.name);
      if (entry.isDirectory() && !entry.name.startsWith(".") && entry.name !== "node_modules") {
        const found = findFileRecursive(fullPath, filename, depth + 1);
        if (found) {
          return found;
        }
      } else if (entry.name === filename) {
        return fullPath;
      }
    }
  } catch {
  }
  return null;
}
__name(findFileRecursive, "findFileRecursive");
function getIntentBasedFiles(intent, workspaceRoot) {
  const results = [];
  const intentPatterns = {
    implement: [
      {
        pattern: "types",
        reason: "Review type definitions"
      },
      {
        pattern: "interfaces",
        reason: "Check existing interfaces"
      }
    ],
    refactor: [
      {
        pattern: "test",
        reason: "Ensure tests pass"
      },
      {
        pattern: "spec",
        reason: "Check test coverage"
      }
    ],
    debug: [
      {
        pattern: "utils",
        reason: "Review utility functions"
      },
      {
        pattern: "helpers",
        reason: "Check helper functions"
      }
    ],
    test: [
      {
        pattern: "fixtures",
        reason: "Review test fixtures"
      },
      {
        pattern: "mock",
        reason: "Check existing mocks"
      }
    ],
    deploy: [
      {
        pattern: "config",
        reason: "Review configuration"
      },
      {
        pattern: "env",
        reason: "Check environment setup"
      }
    ]
  };
  const patterns = intentPatterns[intent] || [];
  for (const { pattern, reason } of patterns) {
    const searchDirs = [
      "src",
      "packages",
      "apps"
    ];
    for (const dir of searchDirs) {
      const dirPath = join(workspaceRoot, dir, pattern);
      if (existsSync(dirPath)) {
        results.push({
          file: join(dir, pattern),
          reason
        });
        break;
      }
    }
  }
  return results.slice(0, 2);
}
__name(getIntentBasedFiles, "getIntentBasedFiles");
function findRelatedFiles(file, workspaceRoot) {
  const results = [];
  const fullPath = file.startsWith("/") ? file : join(workspaceRoot, file);
  const dir = dirname(fullPath);
  try {
    const indexPath = join(dir, "index.ts");
    if (existsSync(indexPath) && indexPath !== fullPath) {
      results.push(relative(workspaceRoot, indexPath));
    }
    const typesPath = join(dir, "types.ts");
    if (existsSync(typesPath) && typesPath !== fullPath) {
      results.push(relative(workspaceRoot, typesPath));
    }
    const baseName = basename(file, ".ts");
    const testPath = join(dir, `${baseName}.test.ts`);
    if (existsSync(testPath)) {
      results.push(relative(workspaceRoot, testPath));
    }
  } catch {
  }
  return results;
}
__name(findRelatedFiles, "findRelatedFiles");
function buildConfidenceReason(confidence, warnings, learnings, violations, loopDetected) {
  const parts = [];
  if (loopDetected) {
    parts.push("Agent loop detected - confidence reduced");
  }
  if (violations.length > 0) {
    parts.push(`${violations.length} past violation(s) in this area`);
  }
  if (warnings.length > 0) {
    parts.push(`${warnings.length} warning(s): ${warnings[0]?.message?.slice(0, 50) || "see details"}`);
  }
  if (learnings.length > 0) {
    parts.push(`${learnings.length} relevant learning(s) available`);
  }
  if (parts.length === 0) {
    if (confidence >= 80) {
      return "No issues detected - safe to proceed";
    }
    return "Standard confidence level for this task type";
  }
  return parts.join("; ");
}
__name(buildConfidenceReason, "buildConfidenceReason");
async function getIntegrationConfidenceAdjustment(files, context) {
  const result7 = {
    adjustment: 0,
    reasons: [],
    ext: {},
    enrichment: null
  };
  const integrations = context.services?.integrations ?? getIntegrationOrchestratorService();
  if (!integrations) {
    return result7;
  }
  const health = integrations.getHealth();
  if (health.status === "unavailable") {
    return result7;
  }
  try {
    const enrichment = await integrations.enrichContext({
      files: files.map((f) => ({
        path: f
      })),
      skipCache: false
    });
    if (!enrichment) {
      return result7;
    }
    result7.enrichment = enrichment;
    let hasActivePR = false;
    let prNumber;
    let volatileFiles = 0;
    let totalErrors = 0;
    let deprecationCount = 0;
    let hasGitHub = false;
    let hasSentry = false;
    let hasContext7 = false;
    for (const [, fileContext] of enrichment.contexts) {
      const { sources } = fileContext;
      if (sources.github) {
        hasGitHub = true;
        const github = sources.github;
        if (github.activePR && !hasActivePR) {
          hasActivePR = true;
          prNumber = github.activePR.number;
        }
        if (github.changeFrequency === "volatile") {
          volatileFiles++;
        }
      }
      if (sources.sentry) {
        hasSentry = true;
        totalErrors += sources.sentry.errors?.length ?? 0;
      }
      if (sources.context7) {
        hasContext7 = true;
        const deprecated = sources.context7.imports?.filter((i) => i.isDeprecated) ?? [];
        deprecationCount += deprecated.length;
      }
    }
    if (hasGitHub) {
      if (hasActivePR) {
        result7.ext.github = `pr:${prNumber}`;
        result7.adjustment += 3;
        result7.reasons.push("Code under PR review (+3%)");
      } else if (volatileFiles > 0) {
        result7.ext.github = `volatile:${volatileFiles}`;
        result7.adjustment -= 10;
        result7.reasons.push(`High file volatility (${volatileFiles} volatile file(s)) (-10%)`);
      } else {
        result7.ext.github = "ok";
      }
    }
    if (hasSentry) {
      if (totalErrors >= 5) {
        result7.adjustment -= 15;
        result7.reasons.push(`High Sentry errors (${totalErrors}) - production issues (-15%)`);
        result7.ext.sentry = `${totalErrors}err`;
      } else if (totalErrors >= 2) {
        result7.adjustment -= 10;
        result7.reasons.push(`Sentry errors (${totalErrors}) - some production issues (-10%)`);
        result7.ext.sentry = `${totalErrors}err`;
      } else if (totalErrors > 0) {
        result7.adjustment -= 5;
        result7.reasons.push(`Minor Sentry errors (${totalErrors}) (-5%)`);
        result7.ext.sentry = `${totalErrors}err`;
      } else {
        result7.adjustment += 5;
        result7.reasons.push("Clean production - no Sentry errors (+5%)");
        result7.ext.sentry = "ok";
      }
    }
    if (hasContext7) {
      if (deprecationCount > 0) {
        result7.adjustment -= 5;
        result7.reasons.push(`Deprecated APIs detected (${deprecationCount}) (-5%)`);
        result7.ext.context7 = `${deprecationCount}dep`;
      } else {
        result7.ext.context7 = "ok";
      }
    }
  } catch {
  }
  return result7;
}
__name(getIntegrationConfidenceAdjustment, "getIntegrationConfidenceAdjustment");
function extractDominantApproach(similarPatterns, learnings) {
  if (similarPatterns.length === 0 && learnings.length === 0) {
    return null;
  }
  const patternRegex = /\b(middleware|factory|singleton|observer|strategy|adapter|decorator|proxy|builder|facade|repository|service|controller|handler)\b/gi;
  const patternCounts = /* @__PURE__ */ new Map();
  const maxPatterns = Math.min(similarPatterns.length, 10);
  for (let i = 0; i < maxPatterns; i++) {
    const relevance = similarPatterns[i].relevance.slice(0, 50).toLowerCase();
    const matches = relevance.match(patternRegex);
    if (matches) {
      for (const match of matches) {
        const normalized = match.toLowerCase();
        patternCounts.set(normalized, (patternCounts.get(normalized) || 0) + 1);
      }
    }
  }
  const maxLearnings = Math.min(learnings.length, 10);
  for (let i = 0; i < maxLearnings; i++) {
    const action = learnings[i].action.slice(0, 50).toLowerCase();
    const matches = action.match(patternRegex);
    if (matches) {
      for (const match of matches) {
        const normalized = match.toLowerCase();
        patternCounts.set(normalized, (patternCounts.get(normalized) || 0) + 1);
      }
    }
  }
  if (patternCounts.size === 0) {
    return null;
  }
  const totalSources = maxPatterns + maxLearnings;
  const threshold = totalSources * 0.5;
  let maxCount = 0;
  let dominantPattern = null;
  for (const [pattern, count] of patternCounts.entries()) {
    if (count > maxCount && count >= threshold) {
      maxCount = count;
      dominantPattern = pattern;
    }
  }
  return dominantPattern;
}
__name(extractDominantApproach, "extractDominantApproach");
function buildWhatIKnow(learnings, warnings, violations, learningTypes) {
  const lines = [
    "WHAT I KNOW ABOUT THIS:"
  ];
  if (learnings.length > 0) {
    const typeNames = learningTypes.slice(0, 3).join(", ");
    lines.push(`  \u{1F4DA} ${learnings.length} relevant patterns loaded (${typeNames})`);
  }
  if (warnings.length > 0) {
    const zone = warnings.length >= 3 ? "high fragility zone" : "elevated risk area";
    lines.push(`  \u26A0\uFE0F ${warnings.length} warning${warnings.length > 1 ? "s" : ""} in this area  -  ${zone}`);
  }
  if (violations.length > 0) {
    lines.push(`  \u{1F525} ${violations.length} past violation${violations.length > 1 ? "s" : ""}  -  handle with care`);
  }
  return lines.join("\n");
}
__name(buildWhatIKnow, "buildWhatIKnow");
function buildRecommendedApproach(dominantApproach, similarPatterns, learnings) {
  const lines = [
    "RECOMMENDED APPROACH:"
  ];
  if (dominantApproach && similarPatterns.length > 0) {
    const dominantCount = similarPatterns.filter((p) => p.relevance.toLowerCase().includes(dominantApproach)).length;
    lines.push(`  \u2192 Use ${dominantApproach} pattern (seen in ${dominantCount} similar implementations)`);
    const altPattern = similarPatterns.find((p) => !p.relevance.toLowerCase().includes(dominantApproach) && p.score >= 0.6);
    if (altPattern && dominantCount - 1 >= 2) {
      const altMatch = altPattern.relevance.match(/\b(middleware|factory|singleton|observer|strategy|adapter|decorator|proxy|builder|facade)\b/gi);
      if (altMatch && altMatch[0].toLowerCase() !== dominantApproach) {
        const altName = altMatch[0].toLowerCase();
        lines.push(`  \u26A0\uFE0F Alternative: ${altName} exists but less common (${dominantCount} vs 1 use)`);
      }
    }
  }
  const actionable = learnings.filter((l) => l.type === "pattern" || l.type === "workflow").slice(0, 2);
  for (const learning of actionable) {
    const action = truncate(learning.action, 60);
    const directive = action.replace(/^(always |you should |consider |try to )/i, "");
    const capitalized = directive.charAt(0).toUpperCase() + directive.slice(1);
    lines.push(`  \u2192 ${capitalized}`);
  }
  return lines.join("\n");
}
__name(buildRecommendedApproach, "buildRecommendedApproach");
function buildStartHere(studyFirst) {
  if (studyFirst.length === 0) {
    return "";
  }
  const lines = [
    "START HERE (do these first):"
  ];
  for (let i = 0; i < Math.min(studyFirst.length, 5); i++) {
    const study = studyFirst[i];
    const icon = study.priority === "high" ? "\u{1F534}" : study.priority === "medium" ? "\u{1F7E1}" : "\u{1F7E2}";
    const num = i + 1;
    let directiveReason = study.reason;
    if (study.reason.startsWith("Past violation:")) {
      directiveReason = `Review this  -  ${study.reason.toLowerCase()}`;
    } else if (study.reason.startsWith("From learning:")) {
      directiveReason = `Study this pattern  -  ${study.reason.replace("From learning:", "").trim()}`;
    } else if (study.reason.startsWith("Related to")) {
      directiveReason = `Check dependencies  -  ${study.reason.toLowerCase()}`;
    }
    lines.push(`  ${num}. ${icon} Read \`${study.file}\`  -  ${directiveReason}`);
  }
  return lines.join("\n");
}
__name(buildStartHere, "buildStartHere");
function buildWatchOutFor(warnings, violations, integrationAdjustment) {
  const lines = [
    "WATCH OUT FOR:"
  ];
  let hasContent = false;
  for (const reason of integrationAdjustment.reasons.slice(0, 2)) {
    lines.push(`  \u26A0\uFE0F ${reason}`);
    hasContent = true;
  }
  for (const warning of warnings.slice(0, 2)) {
    lines.push(`  \u26A0\uFE0F ${warning.message}`);
    hasContent = true;
  }
  for (const violation of violations.slice(0, 2)) {
    lines.push(`  \u274C Past violation: ${violation.type}  -  don't repeat this`);
    hasContent = true;
  }
  return hasContent ? lines.join("\n") : "";
}
__name(buildWatchOutFor, "buildWatchOutFor");
function buildAdviseHint(confidence, _recommendation, studyCount, dominantApproach) {
  const hints = [];
  if (dominantApproach) {
    hints.push("Follow RECOMMENDED APPROACH above  -  it's battle-tested in this codebase");
  }
  if (studyCount > 0 && hints.length < 3) {
    hints.push("Complete START HERE steps before implementation");
  }
  if (hints.length < 3) {
    if (confidence < 40) {
      hints.push("STOP and investigate warnings before proceeding");
    } else if (confidence < 60) {
      hints.push("Call check(mode:'architecture') to validate approach");
    } else if (confidence >= 80) {
      hints.push("High confidence  -  proceed with implementation");
    }
  }
  const capped = hints.slice(0, 3);
  return `LLM_HINT:
${capped.map((h) => `- ${h}`).join("\n")}`;
}
__name(buildAdviseHint, "buildAdviseHint");
async function handleAdvise(args, context) {
  const { task = "general task", files = [], intent = "implement" } = args;
  const filesArray = files;
  const workspaceRoot = getWorkspaceRoot(context);
  let loopDetected = false;
  let loopConfidence = 0;
  let loopType;
  let loopEvidence = [];
  if (context.sessionId && context.services?.sessionManager) {
    try {
      const loopResult = context.services.sessionManager.detectLoop(context.sessionId);
      loopDetected = loopResult.detected;
      loopConfidence = loopResult.confidence;
      loopType = loopResult.type;
      loopEvidence = loopResult.evidence;
    } catch {
    }
  }
  let availableHelpers = [];
  if (context.services?.mcp) {
    try {
      const tools = await context.services.mcp.listTools();
      availableHelpers = tools.filter((t) => [
        "sequential-thinking",
        "search_web",
        "context7"
      ].includes(t.name)).map((t) => t.name);
    } catch {
    }
  }
  const warnings = [];
  const learnings = [];
  const violations = [];
  let hint = "";
  let confidence = calculateConfidence(warnings.length, learnings.length, violations.length);
  if (loopDetected) {
    if (loopConfidence >= 0.8) {
      confidence = Math.min(confidence, 25);
    } else if (loopConfidence >= 0.5) {
      confidence = Math.min(confidence, 45);
    }
  }
  const integrationAdjustment = await getIntegrationConfidenceAdjustment(filesArray, context);
  confidence = Math.max(10, Math.min(100, confidence + integrationAdjustment.adjustment));
  const recommendation = getRecommendation(confidence);
  if (loopDetected && availableHelpers.length > 0 && !hint) {
    hint = `Agent loop detected - try ${availableHelpers[0].replace(/-/g, "_")} to break pattern`;
  } else if (loopDetected && !hint) {
    hint = "Agent loop detected - consider alternative approach or bail out";
  }
  let studyFirst = [];
  let similarPatterns = [];
  let dominantApproach = null;
  try {
    if (filesArray.length > 0) {
      similarPatterns = await findSimilarPatterns(filesArray, workspaceRoot, 3);
    }
    studyFirst = buildStudyRecommendations(task, filesArray, intent, workspaceRoot, violations, learnings);
    dominantApproach = extractDominantApproach(similarPatterns, learnings);
  } catch {
  }
  let confidenceReason = buildConfidenceReason(confidence, warnings, learnings, violations, loopDetected);
  if (integrationAdjustment.reasons.length > 0) {
    confidenceReason = `${confidenceReason}; ${integrationAdjustment.reasons.join("; ")}`;
  }
  const wireParts = [
    "\u{1F9E0}",
    `conf:${confidence}`,
    `rec:${recommendation}`,
    `warn:${warnings.length}`,
    `learn:${learnings.length}`,
    `viol:${violations.length}`
  ];
  if (studyFirst.length > 0) {
    wireParts.push(`study:${studyFirst.length}`);
  }
  if (similarPatterns.length > 0) {
    wireParts.push(`sim:${similarPatterns.length}`);
  }
  if (dominantApproach) {
    wireParts.push(`approach:${dominantApproach}`);
  }
  if (loopDetected) {
    wireParts.push("loop:Y");
    wireParts.push(`lc:${Math.round(loopConfidence * 100)}`);
    if (loopType) {
      const typeAbbr = loopType === "structural" ? "s" : loopType === "semantic" ? "m" : "b";
      wireParts.push(`lt:${typeAbbr}`);
    }
    if (loopEvidence.length > 0) {
      const evidence = loopEvidence[0].replace(/\s+/g, "_").slice(0, 25);
      wireParts.push(`ev:${evidence}`);
    }
  }
  if (hint) {
    wireParts.push(`hint:${hint.slice(0, 30).replace(/\s+/g, "_")}`);
  }
  const extParts = [];
  if (integrationAdjustment.ext.github) {
    extParts.push(`gh:${integrationAdjustment.ext.github}`);
  }
  if (integrationAdjustment.ext.sentry) {
    extParts.push(`sn:${integrationAdjustment.ext.sentry}`);
  }
  if (integrationAdjustment.ext.context7) {
    extParts.push(`c7:${integrationAdjustment.ext.context7}`);
  }
  if (extParts.length > 0) {
    wireParts.push(`ext:${extParts.join(",")}`);
  }
  const wire = wireParts.filter(Boolean).join("|");
  const learningTypes = Array.from(new Set(learnings.map((l) => l.type)));
  const whatIKnow = buildWhatIKnow(learnings, warnings, violations, learningTypes);
  const recommendedApproach = dominantApproach || learnings.length > 0 ? buildRecommendedApproach(dominantApproach, similarPatterns, learnings) : "";
  const startHere = studyFirst.length > 0 ? buildStartHere(studyFirst) : "";
  const watchOutFor = warnings.length > 0 || violations.length > 0 || integrationAdjustment.reasons.length > 0 ? buildWatchOutFor(warnings, violations, integrationAdjustment) : "";
  const llmHint = buildAdviseHint(confidence, recommendation, studyFirst.length, dominantApproach);
  const learningTrajectory = getLearningTrajectory(workspaceRoot);
  const trajectoryContext = learningTrajectory ? `Based on ${learningTrajectory.totalTracked} learnings and ${similarPatterns.length} similar patterns` : `Based on ${learnings.length} relevant learnings`;
  const confidenceLabel = confidence >= 80 ? "PROCEED" : confidence >= 60 ? "REVIEW CAREFULLY" : confidence >= 40 ? "CAUTION" : "STOP AND INVESTIGATE";
  const sections = [
    whatIKnow,
    recommendedApproach,
    startHere,
    watchOutFor,
    llmHint
  ].filter(Boolean).join("\n\n");
  const summary = `${sections}
---
\u{1F9E0} Vreko Advisory: ${trajectoryContext}
  Confidence: ${confidence}% (${confidenceLabel})  -  ${confidenceReason}
  Intent: ${intent} | Files: ${filesArray.length > 0 ? filesArray.join(", ") : "none"}`;
  const structuredContent = {
    confidence,
    recommendation,
    warnings: warnings.map((w) => ({
      level: w.level,
      code: w.code,
      message: w.message
    })),
    learnings: learnings.slice(0, 5),
    violations: violations.slice(0, 3),
    studyFirst,
    similarPatterns,
    loopDetection: loopDetected ? {
      detected: true,
      confidence: loopConfidence,
      type: loopType,
      evidence: loopEvidence
    } : {
      detected: false
    }
  };
  if (integrationAdjustment.enrichment) {
    structuredContent.integrations = {
      adjustment: integrationAdjustment.adjustment,
      reasons: integrationAdjustment.reasons,
      status: integrationAdjustment.ext,
      riskScore: integrationAdjustment.enrichment.riskScore,
      riskFactors: integrationAdjustment.enrichment.riskFactors,
      sources: integrationAdjustment.enrichment.sources
    };
  }
  return {
    content: [
      {
        type: "text",
        text: `${wire}
${summary}`
      }
    ],
    structuredContent
  };
}
__name(handleAdvise, "handleAdvise");
var init_advise = __esm2({
  "src/tools/consolidated/advise.ts"() {
    init_local_service_adapter();
    init_integration_orchestrator_service();
    init_learning_meta();
    __name8(getWorkspaceRoot, "getWorkspaceRoot");
    __name8(calculateConfidence, "calculateConfidence");
    __name8(getRecommendation, "getRecommendation");
    __name8(findSimilarPatterns, "findSimilarPatterns");
    __name8(buildStudyRecommendations, "buildStudyRecommendations");
    __name8(findFileInWorkspace, "findFileInWorkspace");
    __name8(findFileRecursive, "findFileRecursive");
    __name8(getIntentBasedFiles, "getIntentBasedFiles");
    __name8(findRelatedFiles, "findRelatedFiles");
    __name8(buildConfidenceReason, "buildConfidenceReason");
    __name8(getIntegrationConfidenceAdjustment, "getIntegrationConfidenceAdjustment");
    __name8(extractDominantApproach, "extractDominantApproach");
    __name8(buildWhatIKnow, "buildWhatIKnow");
    __name8(buildRecommendedApproach, "buildRecommendedApproach");
    __name8(buildStartHere, "buildStartHere");
    __name8(buildWatchOutFor, "buildWatchOutFor");
    __name8(buildAdviseHint, "buildAdviseHint");
    __name8(handleAdvise, "handleAdvise");
  }
});
function getCheckModeDefinition(name) {
  return CHECK_MODE_REGISTRY.find((m) => m.name === name);
}
__name(getCheckModeDefinition, "getCheckModeDefinition");
function isCheckModeImplemented(name) {
  const mode = getCheckModeDefinition(name);
  return mode?.implemented ?? false;
}
__name(isCheckModeImplemented, "isCheckModeImplemented");
function getCheckModesByCategory(category) {
  return CHECK_MODE_REGISTRY.filter((m) => m.category === category && m.implemented);
}
__name(getCheckModesByCategory, "getCheckModesByCategory");
function formatCheckModesList(includeUnimplemented = false) {
  const modes = includeUnimplemented ? CHECK_MODE_REGISTRY : CHECK_MODE_REGISTRY.filter((m) => m.implemented);
  const lines = modes.map((m) => {
    const prefix = m.implemented ? "  " : "  [ROADMAP] ";
    const suffix = m.implemented ? "" : " (not yet implemented)";
    return `${prefix}${m.name.padEnd(12)} - ${m.description} ${m.estimatedTime}${suffix}`;
  });
  return lines.join("\n");
}
__name(formatCheckModesList, "formatCheckModesList");
var CHECK_MODE_REGISTRY;
var IMPLEMENTED_CHECK_MODES;
var IMPLEMENTED_MODE_COUNT;
var init_check_modes_registry = __esm2({
  "src/check-modes-registry.ts"() {
    CHECK_MODE_REGISTRY = [
      // =============================================================================
      // VALIDATION - Fast checks for code quality
      // =============================================================================
      {
        name: "quick",
        description: "Fast parallel validation (TypeScript + lint)",
        category: "validation",
        estimatedTime: "~2-5s",
        implemented: true
      },
      {
        name: "full",
        description: "Comprehensive 7-layer validation",
        category: "validation",
        estimatedTime: "~15s",
        implemented: true
      },
      {
        name: "patterns",
        description: "Pattern-only validation against workspace rules",
        category: "validation",
        estimatedTime: "~3s",
        implemented: true
      },
      {
        name: "build",
        description: "Build verification (runs pnpm build)",
        category: "validation",
        estimatedTime: "~30s",
        implemented: true
      },
      // =============================================================================
      // ANALYSIS - Deep codebase understanding
      // =============================================================================
      {
        name: "impact",
        description: "Bundle size delta and ROI for dead code removal",
        category: "analysis",
        estimatedTime: "~5s",
        implemented: true
      },
      {
        name: "circular",
        description: "Circular dependency detection (via madge)",
        category: "analysis",
        estimatedTime: "~3s",
        implemented: true
      },
      {
        name: "docs",
        description: "Documentation freshness check",
        category: "analysis",
        estimatedTime: "~2s",
        implemented: true
      },
      {
        name: "learnings",
        description: "Learning tier stats and hot tier promotion",
        category: "analysis",
        estimatedTime: "~1s",
        implemented: true
      },
      {
        name: "architecture",
        description: "Layer dependency and import rule validation",
        category: "analysis",
        estimatedTime: "~5s",
        implemented: true
      },
      {
        name: "trace",
        description: "Full dependency graph with confidence scoring",
        category: "analysis",
        estimatedTime: "~5s",
        implemented: true
      },
      {
        name: "coverage",
        description: "Test coverage per file and overall",
        category: "analysis",
        estimatedTime: "~10s",
        implemented: true
      },
      {
        name: "orphans",
        description: "Find orphan files and skipped/disabled tests",
        category: "analysis",
        estimatedTime: "~3s",
        implemented: true
      },
      {
        name: "evolution",
        description: "Deprecated patterns and @deprecated annotations",
        category: "analysis",
        estimatedTime: "~3s",
        implemented: true
      },
      // =============================================================================
      // SECURITY - Vulnerability and threat detection
      // =============================================================================
      {
        name: "security",
        description: "Comprehensive security scan (secrets + threats)",
        category: "security",
        estimatedTime: "~10s",
        implemented: true
      },
      // =============================================================================
      // MAINTENANCE - System health and integrations
      // =============================================================================
      {
        name: "health",
        description: "MCP diagnostics (connection, uptime, errors)",
        category: "maintenance",
        estimatedTime: "~1s",
        implemented: true
      },
      {
        name: "integrations",
        description: "External connection health (GitHub, Context7, Sentry)",
        category: "maintenance",
        estimatedTime: "~3s",
        implemented: true
      },
      {
        name: "wiring",
        description: "DI container validation (interfaces, implementations)",
        category: "maintenance",
        estimatedTime: "~2s",
        implemented: true
      },
      // =============================================================================
      // ROADMAP - Planned but not yet implemented
      // =============================================================================
      {
        name: "precommit",
        description: "Pre-commit validation hook",
        category: "validation",
        estimatedTime: "~5s",
        implemented: false
      }
    ];
    IMPLEMENTED_CHECK_MODES = CHECK_MODE_REGISTRY.filter((m) => m.implemented).map((m) => m.name);
    IMPLEMENTED_MODE_COUNT = IMPLEMENTED_CHECK_MODES.length;
    CHECK_MODE_REGISTRY.map((m) => m.name);
    __name8(getCheckModeDefinition, "getCheckModeDefinition");
    __name8(isCheckModeImplemented, "isCheckModeImplemented");
    __name8(getCheckModesByCategory, "getCheckModesByCategory");
    __name8(formatCheckModesList, "formatCheckModesList");
  }
});
function getEvolutionDetectorService(workspaceRoot, options) {
  const key = workspaceRoot;
  if (!services3.has(key)) {
    services3.set(key, new EvolutionDetectorService(workspaceRoot, options));
  }
  const service = services3.get(key);
  if (!service) {
    throw new Error(`Failed to get or create EvolutionDetectorService for ${workspaceRoot}`);
  }
  return service;
}
__name(getEvolutionDetectorService, "getEvolutionDetectorService");
var DEFAULT_SOURCE_DIRS;
var DEPRECATION_NAMING_PATTERNS;
var DEPRECATION_COMMENT_PATTERNS;
var OLD_FALSE_POSITIVES;
var EvolutionDetectorService;
var services3;
var init_evolution_detector_service = __esm2({
  "src/services/evolution-detector-service.ts"() {
    DEFAULT_SOURCE_DIRS = [
      "src",
      "lib",
      "packages"
    ];
    DEPRECATION_NAMING_PATTERNS = [
      {
        pattern: /Legacy$/i,
        confidence: 0.75
      },
      {
        pattern: /^legacy/i,
        confidence: 0.75
      },
      {
        pattern: /^old[A-Z]/i,
        confidence: 0.7
      },
      {
        pattern: /Old$/i,
        confidence: 0.65
      },
      {
        pattern: /V1$/i,
        confidence: 0.7
      },
      {
        pattern: /^v1[A-Z]/i,
        confidence: 0.7
      },
      {
        pattern: /deprecated/i,
        confidence: 0.85
      }
    ];
    DEPRECATION_COMMENT_PATTERNS = [
      {
        pattern: /TODO:?\s*deprecate/i,
        confidence: 0.6
      },
      {
        pattern: /FIXME:?\s*remove.*legacy/i,
        confidence: 0.65
      },
      {
        pattern: /TODO\s+deprecate:/i,
        confidence: 0.6
      },
      {
        pattern: /@deprecated.*scheduled/i,
        confidence: 0.8
      }
    ];
    OLD_FALSE_POSITIVES = [
      "gold",
      "bold",
      "cold",
      "fold",
      "hold",
      "mold",
      "sold",
      "told",
      "older",
      "oldest"
    ];
    EvolutionDetectorService = class {
      static {
        __name(this, "EvolutionDetectorService");
      }
      static {
        __name8(this, "EvolutionDetectorService");
      }
      workspaceRoot;
      sourceDirectories;
      includeTests;
      constructor(workspaceRoot, options) {
        this.workspaceRoot = workspaceRoot;
        this.sourceDirectories = options?.sourceDirectories ?? DEFAULT_SOURCE_DIRS;
        this.includeTests = options?.includeTests ?? false;
      }
      // =========================================================================
      // Main Scan Method
      // =========================================================================
      /**
      * Scan for deprecations in the workspace
      * @param files - Optional specific files to scan (relative paths)
      */
      async scanForDeprecations(files) {
        const deprecations = [];
        const seenSymbols = /* @__PURE__ */ new Set();
        let totalScanned = 0;
        const filesToScan = files ? files.map((f) => join(this.workspaceRoot, f)) : await this.discoverSourceFiles();
        for (const filePath of filesToScan) {
          if (!existsSync(filePath)) {
            continue;
          }
          if (!/\.(ts|tsx|js|jsx)$/.test(filePath)) {
            continue;
          }
          totalScanned++;
          try {
            const content = readFileSync(filePath, "utf-8");
            const relativePath = filePath.replace(`${this.workspaceRoot}/`, "");
            const jsdocDeprecations = this.scanJSDocDeprecated(content, relativePath);
            for (const dep of jsdocDeprecations) {
              const key = `${dep.symbol}:${dep.filePath}`;
              if (!seenSymbols.has(key)) {
                seenSymbols.add(key);
                deprecations.push(dep);
              }
            }
            const namingDeprecations = this.scanNamingPatterns(content, relativePath);
            for (const dep of namingDeprecations) {
              const key = `${dep.symbol}:${dep.filePath}`;
              if (!seenSymbols.has(key)) {
                seenSymbols.add(key);
                deprecations.push(dep);
              }
            }
            const commentDeprecations = this.scanComments(content, relativePath);
            for (const dep of commentDeprecations) {
              const key = `${dep.symbol}:${dep.filePath}`;
              if (!seenSymbols.has(key)) {
                seenSymbols.add(key);
                deprecations.push(dep);
              }
            }
          } catch {
          }
        }
        const patternCounts = /* @__PURE__ */ new Map();
        for (const dep of deprecations) {
          patternCounts.set(dep.type, (patternCounts.get(dep.type) ?? 0) + 1);
        }
        const patterns = Array.from(patternCounts.entries()).map(([type, count]) => ({
          type,
          count
        }));
        return {
          deprecations,
          totalScanned,
          patterns
        };
      }
      // =========================================================================
      // Discovery
      // =========================================================================
      /**
      * Discover TypeScript/JavaScript source files
      */
      async discoverSourceFiles() {
        const patterns = [];
        for (const dir of this.sourceDirectories) {
          const dirPath = join(this.workspaceRoot, dir);
          if (existsSync(dirPath)) {
            patterns.push(`${dir}/**/*.{ts,tsx}`);
          }
        }
        if (patterns.length === 0) {
          patterns.push("**/*.{ts,tsx}");
        }
        const ignore = [
          "**/node_modules/**",
          "**/dist/**",
          "**/.next/**"
        ];
        if (!this.includeTests) {
          ignore.push("**/*.test.*", "**/*.spec.*", "**/__tests__/**");
        }
        const files = await glob(patterns, {
          cwd: this.workspaceRoot,
          absolute: true,
          ignore
        });
        return files;
      }
      // =========================================================================
      // JSDoc Scanning (TypeScript AST)
      // =========================================================================
      /**
      * Scan file for @deprecated JSDoc tags using TypeScript AST
      */
      scanJSDocDeprecated(content, filePath) {
        const deprecations = [];
        try {
          const sourceFile = ts.createSourceFile(filePath, content, ts.ScriptTarget.Latest, true, ts.ScriptKind.TS);
          const visit = /* @__PURE__ */ __name8((node) => {
            const jsDocs = ts.getJSDocTags(node);
            for (const tag of jsDocs) {
              if (tag.tagName.text === "deprecated") {
                const symbol = this.getNodeName(node);
                if (symbol) {
                  const message = this.extractJSDocMessage(tag);
                  const line = sourceFile.getLineAndCharacterOfPosition(node.getStart()).line + 1;
                  deprecations.push({
                    type: "jsdoc",
                    symbol,
                    filePath,
                    line,
                    message,
                    suggestedReplacement: this.extractReplacement(message),
                    confidence: 0.95
                  });
                }
              }
            }
            ts.forEachChild(node, visit);
          }, "visit");
          visit(sourceFile);
        } catch {
        }
        return deprecations;
      }
      /**
      * Get the name of a node (function, class, interface, etc.)
      */
      getNodeName(node) {
        if (ts.isFunctionDeclaration(node) && node.name) {
          return node.name.text;
        }
        if (ts.isClassDeclaration(node) && node.name) {
          return node.name.text;
        }
        if (ts.isInterfaceDeclaration(node) && node.name) {
          return node.name.text;
        }
        if (ts.isTypeAliasDeclaration(node) && node.name) {
          return node.name.text;
        }
        if (ts.isVariableStatement(node)) {
          const decl = node.declarationList.declarations[0];
          if (decl && ts.isIdentifier(decl.name)) {
            return decl.name.text;
          }
        }
        if (ts.isMethodDeclaration(node) && node.name && ts.isIdentifier(node.name)) {
          return node.name.text;
        }
        return null;
      }
      /**
      * Extract message text from JSDoc tag
      */
      extractJSDocMessage(tag) {
        if (tag.comment) {
          if (typeof tag.comment === "string") {
            return tag.comment.trim();
          }
          const parts = tag.comment;
          return parts.map((p) => p.getText()).join(" ").trim();
        }
        return void 0;
      }
      /**
      * Extract replacement suggestion from deprecation message
      */
      extractReplacement(message) {
        if (!message) {
          return void 0;
        }
        const patterns = [
          /[Uu]se\s+(\w+)\s+instead/,
          /[Mm]igrate\s+to\s+(\w+)/,
          /[Rr]eplace\s+with\s+(\w+)/,
          /→\s*(\w+)/,
          /prefer\s+(\w+)/i
        ];
        for (const pattern of patterns) {
          const match = message.match(pattern);
          if (match) {
            return match[1];
          }
        }
        return void 0;
      }
      // =========================================================================
      // Naming Pattern Scanning
      // =========================================================================
      /**
      * Scan for deprecation-indicating naming patterns
      */
      scanNamingPatterns(content, filePath) {
        const deprecations = [];
        content.split("\n");
        const exportPattern = /export\s+(?:function|class|interface|type|const|let|var)\s+(\w+)/g;
        let match;
        while ((match = exportPattern.exec(content)) !== null) {
          const symbol = match[1];
          const lowerSymbol = symbol.toLowerCase();
          if (OLD_FALSE_POSITIVES.some((fp) => lowerSymbol.includes(fp))) {
            continue;
          }
          for (const { pattern, confidence } of DEPRECATION_NAMING_PATTERNS) {
            if (pattern.test(symbol)) {
              const lineIndex = this.findLineIndex(content, match.index);
              deprecations.push({
                type: "naming",
                symbol,
                filePath,
                line: lineIndex + 1,
                message: `Symbol name suggests deprecation: matches pattern ${pattern.source}`,
                confidence
              });
              break;
            }
          }
        }
        return deprecations;
      }
      // =========================================================================
      // Comment Scanning
      // =========================================================================
      /**
      * Scan for TODO/FIXME deprecation comments // Issue: LIN-0000
      */
      scanComments(content, filePath) {
        const deprecations = [];
        const lines = content.split("\n");
        for (let i = 0; i < lines.length; i++) {
          const line = lines[i];
          for (const { pattern, confidence } of DEPRECATION_COMMENT_PATTERNS) {
            if (pattern.test(line)) {
              const nextSymbol = this.findNextExportedSymbol(lines, i);
              if (nextSymbol) {
                deprecations.push({
                  type: "comment",
                  symbol: nextSymbol.name,
                  filePath,
                  line: i + 1,
                  message: line.trim(),
                  confidence
                });
              }
              break;
            }
          }
        }
        return deprecations;
      }
      /**
      * Find the next exported symbol after a comment
      */
      findNextExportedSymbol(lines, startLine) {
        for (let i = startLine + 1; i < Math.min(startLine + 6, lines.length); i++) {
          const line = lines[i];
          const exportMatch = line.match(/export\s+(?:function|class|interface|type|const|let|var)\s+(\w+)/);
          if (exportMatch) {
            return {
              name: exportMatch[1],
              line: i
            };
          }
        }
        return null;
      }
      /**
      * Find line index for a character position
      */
      findLineIndex(content, charIndex) {
        let lineIndex = 0;
        let charCount = 0;
        for (const line of content.split("\n")) {
          charCount += line.length + 1;
          if (charCount > charIndex) {
            return lineIndex;
          }
          lineIndex++;
        }
        return lineIndex;
      }
    };
    services3 = /* @__PURE__ */ new Map();
    __name8(getEvolutionDetectorService, "getEvolutionDetectorService");
  }
});
async function getGitDiffFiles(workspacePath, scope = "staged") {
  return new Promise((resolve72) => {
    let args;
    switch (scope) {
      case "staged":
        args = [
          "diff",
          "--cached",
          "--name-only",
          "--diff-filter=ACMR"
        ];
        break;
      case "changed":
        args = [
          "diff",
          "--name-only",
          "--diff-filter=ACMR"
        ];
        break;
      case "uncommitted":
        args = [
          "diff",
          "HEAD",
          "--name-only",
          "--diff-filter=ACMR"
        ];
        break;
      case "all":
        args = [
          "ls-files"
        ];
        break;
    }
    const proc = spawn("git", args, {
      cwd: workspacePath,
      stdio: [
        "pipe",
        "pipe",
        "pipe"
      ]
    });
    let stdout = "";
    proc.stdout?.on("data", (data) => {
      stdout += data.toString();
    });
    proc.on("close", (code) => {
      if (code !== 0) {
        resolve72([]);
        return;
      }
      const files = stdout.split("\n").map((f) => f.trim()).filter((f) => f.length > 0).filter((f) => /\.(ts|tsx|js|jsx)$/.test(f));
      resolve72(files);
    });
    proc.on("error", () => {
      resolve72([]);
    });
  });
}
__name(getGitDiffFiles, "getGitDiffFiles");
async function autoDetectFiles(workspacePath) {
  const staged = await getGitDiffFiles(workspacePath, "staged");
  if (staged.length > 0) {
    return {
      files: staged,
      source: "staged"
    };
  }
  const uncommitted = await getGitDiffFiles(workspacePath, "uncommitted");
  if (uncommitted.length > 0) {
    return {
      files: uncommitted,
      source: "uncommitted"
    };
  }
  return {
    files: [],
    source: "none"
  };
}
__name(autoDetectFiles, "autoDetectFiles");
var init_file_selector = __esm2({
  "src/services/validation/file-selector.ts"() {
    __name8(getGitDiffFiles, "getGitDiffFiles");
    __name8(autoDetectFiles, "autoDetectFiles");
  }
});
function runCommand2(cmd, args, cwd, timeoutMs = 3e4) {
  const start = Date.now();
  return new Promise((resolve72) => {
    let stdout = "";
    let stderr = "";
    let killed = false;
    const proc = spawn(cmd, args, {
      cwd,
      shell: false,
      stdio: [
        "pipe",
        "pipe",
        "pipe"
      ]
    });
    const timeout = setTimeout(() => {
      killed = true;
      proc.kill("SIGTERM");
    }, timeoutMs);
    proc.stdout?.on("data", (data) => {
      stdout += data.toString();
    });
    proc.stderr?.on("data", (data) => {
      stderr += data.toString();
    });
    proc.on("close", (code) => {
      clearTimeout(timeout);
      resolve72({
        exitCode: killed ? -1 : code ?? 0,
        stdout,
        stderr,
        duration: Date.now() - start
      });
    });
    proc.on("error", (err22) => {
      clearTimeout(timeout);
      resolve72({
        exitCode: -1,
        stdout,
        stderr: err22.message,
        duration: Date.now() - start
      });
    });
  });
}
__name(runCommand2, "runCommand2");
var ParallelValidator;
var init_parallel_validator = __esm2({
  "src/services/validation/parallel-validator.ts"() {
    __name8(runCommand2, "runCommand");
    ParallelValidator = class {
      static {
        __name(this, "ParallelValidator");
      }
      static {
        __name8(this, "ParallelValidator");
      }
      /**
      * Check TypeScript compilation.
      */
      async checkTypeScript(files, workspacePath) {
        const start = Date.now();
        try {
          const result7 = await runCommand2("npx", [
            "--yes",
            "tsc",
            "--noEmit",
            "--pretty",
            "false"
          ], workspacePath, 15e3);
          const errors = [];
          const warnings = [];
          const lines = (result7.stdout + result7.stderr).split("\n");
          for (const line of lines) {
            if (line.includes("error TS")) {
              const isRelevant = files.length === 0 || files.some((f) => line.includes(f) || line.includes(f.replace(/\\/g, "/")));
              if (isRelevant) {
                errors.push(line.trim());
              }
            } else if (line.includes("warning")) {
              warnings.push(line.trim());
            }
          }
          return {
            passed: result7.exitCode === 0 || errors.length === 0,
            duration: Date.now() - start,
            errors: errors.slice(0, 10),
            warnings: warnings.slice(0, 5)
          };
        } catch (error) {
          const errMsg = errorMessage(error);
          return {
            passed: false,
            duration: Date.now() - start,
            errors: [
              `TypeScript check failed: ${errMsg}. Ensure TypeScript is installed: npm install -D typescript`
            ],
            warnings: []
          };
        }
      }
      /**
      * Check tests.
      */
      async checkTests(files, workspacePath, runTests) {
        const start = Date.now();
        const testFiles = [];
        for (const file of files) {
          const dir = file.substring(0, file.lastIndexOf("/")) || ".";
          const base = file.split("/").pop()?.replace(/\.(ts|tsx)$/, "") || "";
          const patterns = [
            `${dir}/${base}.test.ts`,
            `${dir}/${base}.test.tsx`,
            `${dir}/${base}.spec.ts`,
            `${dir}/${base}.spec.tsx`,
            `${dir}/__tests__/${base}.test.ts`,
            `${dir}/__tests__/${base}.test.tsx`
          ];
          for (const pattern of patterns) {
            if (existsSync(join(workspacePath, pattern))) {
              testFiles.push(pattern);
            }
          }
        }
        if (testFiles.length === 0) {
          return {
            passed: true,
            duration: Date.now() - start,
            discovered: 0,
            warnings: [
              "No related test files found"
            ],
            errors: []
          };
        }
        if (!runTests) {
          return {
            passed: true,
            duration: Date.now() - start,
            discovered: testFiles.length,
            warnings: [
              `${testFiles.length} test file(s) discovered but not run`
            ],
            errors: []
          };
        }
        try {
          const result7 = await runCommand2("npx", [
            "--yes",
            "vitest",
            "run",
            ...testFiles,
            "--reporter=json"
          ], workspacePath, 6e4);
          const errors = [];
          let passed = 0;
          let failed = 0;
          try {
            const jsonMatch = result7.stdout.match(/\{[\s\S]*"testResults"[\s\S]*\}/);
            if (jsonMatch) {
              const testResult = JSON.parse(jsonMatch[0]);
              passed = testResult.numPassedTests || 0;
              failed = testResult.numFailedTests || 0;
              if (testResult.testResults) {
                for (const tr of testResult.testResults) {
                  if (tr.status === "failed" && tr.message) {
                    errors.push(tr.message.slice(0, 200));
                  }
                }
              }
            }
          } catch {
            const passMatch = result7.stdout.match(/(\d+) passed/);
            const failMatch = result7.stdout.match(/(\d+) failed/);
            if (passMatch) {
              passed = Number.parseInt(passMatch[1], 10);
            }
            if (failMatch) {
              failed = Number.parseInt(failMatch[1], 10);
              errors.push(`${failed} test(s) failed`);
            }
          }
          return {
            passed: result7.exitCode === 0,
            duration: Date.now() - start,
            discovered: testFiles.length,
            run: testFiles.length,
            passedCount: passed,
            failedCount: failed,
            errors: errors.slice(0, 5),
            warnings: []
          };
        } catch (error) {
          const errMsg = errorMessage(error);
          return {
            passed: false,
            duration: Date.now() - start,
            discovered: testFiles.length,
            errors: [
              `Test execution failed: ${errMsg}. Ensure Vitest is installed: npm install -D vitest`
            ],
            warnings: []
          };
        }
      }
      /**
      * Check lint.
      */
      async checkLint(files, workspacePath) {
        const start = Date.now();
        try {
          const result7 = await runCommand2("npx", [
            "--yes",
            "biome",
            "check",
            "--reporter=json",
            ...files
          ], workspacePath, 15e3);
          const errors = [];
          const warnings = [];
          try {
            const diagnostics = JSON.parse(result7.stdout);
            if (Array.isArray(diagnostics)) {
              for (const diag of diagnostics) {
                if (diag.severity === "error") {
                  errors.push(`${diag.path}: ${diag.message}`);
                } else if (diag.severity === "warning") {
                  warnings.push(`${diag.path}: ${diag.message}`);
                }
              }
            }
          } catch {
            if (result7.exitCode !== 0) {
              const lines = result7.stderr.split("\n").filter((l) => l.trim());
              errors.push(...lines.slice(0, 5));
            }
          }
          return {
            passed: result7.exitCode === 0 || errors.length === 0,
            duration: Date.now() - start,
            errors: errors.slice(0, 10),
            warnings: warnings.slice(0, 5)
          };
        } catch (error) {
          const errMsg = errorMessage(error);
          return {
            passed: false,
            duration: Date.now() - start,
            errors: [
              `Lint check failed: ${errMsg}. Ensure Biome is installed: npm install -D @biomejs/biome`
            ],
            warnings: []
          };
        }
      }
      /**
      * Run parallel validation (TypeScript, tests, lint).
      */
      async run(input) {
        const start = Date.now();
        const { workspacePath, files = [], skipTypeScript = false, skipTests = false, skipLint = false, runTests = false } = input;
        if (!existsSync(join(workspacePath, "node_modules"))) {
          return {
            ok: false,
            error: "Dependencies missing. Run `pnpm install`.",
            code: "DEPS_MISSING"
          };
        }
        if (!existsSync(join(workspacePath, "tsconfig.json"))) {
          return {
            ok: false,
            error: "TypeScript config missing. Create tsconfig.json.",
            code: "TSCONFIG_MISSING"
          };
        }
        const [tsResult, testResult, lintResult] = await Promise.all([
          skipTypeScript ? Promise.resolve({
            passed: true,
            duration: 0,
            errors: [],
            warnings: [
              "Skipped"
            ]
          }) : this.checkTypeScript(files, workspacePath),
          skipTests ? Promise.resolve({
            passed: true,
            duration: 0,
            errors: [],
            warnings: [
              "Skipped"
            ],
            discovered: 0
          }) : this.checkTests(files, workspacePath, runTests),
          skipLint ? Promise.resolve({
            passed: true,
            duration: 0,
            errors: [],
            warnings: [
              "Skipped"
            ]
          }) : this.checkLint(files, workspacePath)
        ]);
        let overall = "pass";
        if (!tsResult.passed || !testResult.passed || !lintResult.passed) {
          overall = "fail";
        } else if (tsResult.warnings.length > 0 || testResult.warnings.length > 0 || lintResult.warnings.length > 0) {
          overall = "warn";
        }
        return {
          ok: true,
          data: {
            overall,
            typescript: tsResult,
            tests: testResult,
            lint: lintResult,
            duration: Date.now() - start
          }
        };
      }
    };
  }
});
var SecurityValidator;
var init_security_validator = __esm2({
  "src/services/validation/security-validator.ts"() {
    init_local_service_adapter();
    SecurityValidator = class {
      static {
        __name(this, "SecurityValidator");
      }
      static {
        __name8(this, "SecurityValidator");
      }
      /**
      * Run security scan on content.
      * Routes through daemon IPC (validate/quick). Content-based threat detection
      * is delegated to daemon; falls back to empty threat list when unavailable.
      */
      async runSecurityScan(_content, filePath, workspaceRoot) {
        const threats = [];
        if (workspaceRoot && filePath) {
          try {
            const daemon = createServiceClient2(workspaceRoot);
            const validation = await daemon.request("validate/quick", {
              workspace: workspaceRoot,
              file: filePath
            });
            if (validation && !validation.passed) {
              if (validation.typescript && !validation.typescript.passed && validation.typescript.errors > 0) {
                threats.push({
                  description: `TypeScript validation failed (${validation.typescript.errors} error(s)) in ${filePath}`,
                  severity: 7
                });
              }
              if (validation.lint && !validation.lint.passed && validation.lint.errors > 0) {
                threats.push({
                  description: `Lint validation failed (${validation.lint.errors} error(s)) in ${filePath}`,
                  severity: 4
                });
              }
            }
          } catch {
          }
        }
        return {
          secrets: {
            findings: [],
            riskScore: 0,
            hasHighRisk: false
          },
          threats
        };
      }
      /**
      * Scan multiple files for security issues.
      * PERFORMANCE: Uses parallel file reading for 50-80% speedup on multiple files.
      */
      async scanFiles(workspacePath, files) {
        const existingFiles = files.filter((file) => {
          const filePath = join(workspacePath, file);
          return existsSync(filePath);
        });
        const results = await Promise.all(existingFiles.map(async (file) => {
          const filePath = join(workspacePath, file);
          try {
            const content = await readFile(filePath, "utf-8");
            const { secrets, threats } = await this.runSecurityScan(content, file, workspacePath);
            const findings = [];
            for (const finding of secrets.findings) {
              findings.push({
                type: "secret",
                severity: finding.severity === "critical" ? "critical" : "high",
                message: `Potential secret: ${finding.type}`,
                file,
                line: finding.line
              });
            }
            for (const threat of threats) {
              findings.push({
                type: "threat",
                severity: threat.severity >= 8 ? "critical" : threat.severity >= 5 ? "high" : "medium",
                message: threat.description,
                file
              });
            }
            return findings;
          } catch {
            return [];
          }
        }));
        return results.flat();
      }
      /**
      * Run full security scan.
      */
      async scan(input) {
        try {
          let files = input.files ? [
            ...input.files
          ] : [];
          if (files.length === 0) {
            const detected = await input.autoDetectFiles();
            files = detected.files;
          }
          const findings = await this.scanFiles(input.workspacePath, files);
          return {
            ok: true,
            data: {
              findings,
              criticalCount: findings.filter((f) => f.severity === "critical").length,
              highCount: findings.filter((f) => f.severity === "high").length,
              mediumCount: findings.filter((f) => f.severity === "medium").length,
              lowCount: findings.filter((f) => f.severity === "low").length
            }
          };
        } catch (error) {
          return {
            ok: false,
            error: errorMessage(error),
            code: "SECURITY_SCAN_FAILED"
          };
        }
      }
    };
  }
});
function getValidationService(deps) {
  if (!validationServiceInstance) {
    validationServiceInstance = new ValidationService(deps);
  }
  return validationServiceInstance;
}
__name(getValidationService, "getValidationService");
var PATTERN_RULES;
var ValidationService;
var validationServiceInstance;
var init_validation2 = __esm2({
  "src/services/validation/index.ts"() {
    init_evolution_detector_service();
    init_test_coverage_service();
    init_file_selector();
    init_parallel_validator();
    init_security_validator();
    PATTERN_RULES = [
      {
        name: "no-console-log",
        pattern: /\bconsole\.log\s*\(/g,
        message: "process.stdout.write() detected  -  use structured logger instead",
        suggestion: "Replace process.stdout.write with injected logger.info() or logger.debug()"
      },
      {
        name: "no-any-type",
        pattern: /:\s*any\b/g,
        message: "Explicit 'any' type detected",
        suggestion: "Use a specific type, 'unknown', or a generic type parameter"
      },
      {
        name: "no-silent-catch",
        pattern: /catch\s*\([^)]*\)\s*\{\s*\}/g,
        message: "Empty catch block  -  error is silently swallowed",
        suggestion: "Log the error or re-throw. Never silently swallow exceptions."
      },
      {
        name: "no-todo-in-code",
        pattern: /\/\/\s*TODO(?:\s|:)/gi,
        message: "TODO comment detected in production code",
        suggestion: "Resolve TODOs before merging or convert to tracked issue"
      },
      {
        name: "no-magic-numbers",
        pattern: /(?<![.\d])\b(?!(?:0|1|2|-1)\b)\d{2,}\b(?![.\d])/g,
        message: "Magic number detected  -  extract to named constant",
        suggestion: "Define a named constant for readability and maintainability"
      },
      {
        name: "prefer-const",
        pattern: /\blet\s+(\w+)\s*=/g,
        message: "'let' used where 'const' may be appropriate",
        suggestion: "Use 'const' for variables that are never reassigned"
      },
      {
        name: "no-nested-ternary",
        pattern: /\?[^:]*\?/g,
        message: "Nested ternary expression detected",
        suggestion: "Refactor to if/else or extract to a helper function for clarity"
      }
    ];
    ValidationService = class {
      static {
        __name(this, "ValidationService");
      }
      static {
        __name8(this, "ValidationService");
      }
      securityValidator;
      parallelValidator;
      constructor(_deps = {}) {
        this.securityValidator = new SecurityValidator();
        this.parallelValidator = new ParallelValidator();
      }
      // ==================== Quick/Full Check ====================
      async quickCheck(input) {
        const startTime = Date.now();
        try {
          let files = input.files ? [
            ...input.files
          ] : [];
          if (files.length === 0) {
            const detected = await autoDetectFiles(input.workspacePath);
            files = detected.files;
          }
          if (files.length === 0) {
            return {
              ok: true,
              data: {
                passed: true,
                diagnostics: [],
                errorCount: 0,
                warningCount: 0,
                duration: Date.now() - startTime,
                filesChecked: 0
              }
            };
          }
          const [tsResult, lintResult, securityFindings] = await Promise.all([
            this.runTypeScriptCheck(input.workspacePath, files),
            this.runLintCheck(input.workspacePath, files, input.fix),
            this.securityValidator.scanFiles(input.workspacePath, files)
          ]);
          const diagnostics = [
            ...tsResult.diagnostics,
            ...lintResult.diagnostics,
            ...securityFindings.map((f) => ({
              severity: f.severity === "critical" || f.severity === "high" ? "error" : "warning",
              code: `SECURITY_${f.type.toUpperCase()}`,
              message: f.message,
              file: f.file
            }))
          ];
          const errorCount = diagnostics.filter((d) => d.severity === "error").length;
          const warningCount = diagnostics.filter((d) => d.severity === "warning").length;
          return {
            ok: true,
            data: {
              passed: errorCount === 0,
              diagnostics,
              errorCount,
              warningCount,
              duration: Date.now() - startTime,
              filesChecked: files.length
            }
          };
        } catch (error) {
          return {
            ok: false,
            error: errorMessage(error),
            code: "QUICK_CHECK_FAILED"
          };
        }
      }
      async fullCheck(input) {
        const startTime = Date.now();
        try {
          let files = input.files ? [
            ...input.files
          ] : [];
          if (files.length === 0) {
            const detected = await autoDetectFiles(input.workspacePath);
            files = detected.files;
          }
          const [quickResult, securityResult, patternResult] = await Promise.allSettled([
            this.quickCheck({
              ...input,
              files
            }),
            this.scanSecurity({
              ...input,
              files
            }),
            this.checkPatternsForFiles(input.workspacePath, files)
          ]);
          const diagnostics = [];
          if (quickResult.status === "fulfilled" && quickResult.value.ok) {
            diagnostics.push(...quickResult.value.data.diagnostics);
          }
          if (securityResult.status === "fulfilled" && securityResult.value.ok) {
            diagnostics.push(...securityResult.value.data.findings.map((f) => ({
              severity: f.severity === "critical" || f.severity === "high" ? "error" : "warning",
              code: `SECURITY_${f.type.toUpperCase()}`,
              message: f.message,
              file: f.file
            })));
          }
          if (patternResult.status === "fulfilled") {
            diagnostics.push(...patternResult.value);
          }
          const errorCount = diagnostics.filter((d) => d.severity === "error").length;
          const warningCount = diagnostics.filter((d) => d.severity === "warning").length;
          return {
            ok: true,
            data: {
              passed: errorCount === 0,
              diagnostics,
              errorCount,
              warningCount,
              duration: Date.now() - startTime,
              filesChecked: files.length
            }
          };
        } catch (error) {
          return {
            ok: false,
            error: errorMessage(error),
            code: "FULL_CHECK_FAILED"
          };
        }
      }
      // ==================== Delegated Methods ====================
      async scanSecurity(input) {
        return this.securityValidator.scan({
          workspacePath: input.workspacePath,
          files: input.files,
          autoDetectFiles: /* @__PURE__ */ __name8(() => autoDetectFiles(input.workspacePath), "autoDetectFiles")
        });
      }
      async runParallelValidation(input) {
        return this.parallelValidator.run(input);
      }
      // ==================== Simple Validators ====================
      async checkPatterns(input) {
        try {
          const violations = [];
          const suggestions = [];
          const lines = input.code.split("\n");
          for (const rule of PATTERN_RULES) {
            const pattern = new RegExp(rule.pattern.source, rule.pattern.flags);
            for (let lineNum = 0; lineNum < lines.length; lineNum++) {
              const line = lines[lineNum];
              let match;
              pattern.lastIndex = 0;
              while ((match = pattern.exec(line)) !== null) {
                violations.push({
                  severity: "warning",
                  code: rule.name,
                  message: rule.message,
                  file: input.filePath,
                  line: lineNum + 1,
                  column: match.index + 1
                });
                if (!suggestions.includes(rule.suggestion)) {
                  suggestions.push(rule.suggestion);
                }
              }
            }
          }
          return {
            ok: true,
            data: {
              compliant: violations.length === 0,
              violations,
              suggestions
            }
          };
        } catch (error) {
          return {
            ok: false,
            error: errorMessage(error),
            code: "PATTERN_CHECK_FAILED"
          };
        }
      }
      async checkBuild(input) {
        const startTime = Date.now();
        try {
          const result7 = await this.runBuildCommand(input.workspacePath);
          return {
            ok: true,
            data: {
              success: result7.success,
              diagnostics: result7.diagnostics,
              duration: Date.now() - startTime
            }
          };
        } catch (error) {
          return {
            ok: false,
            error: errorMessage(error),
            code: "BUILD_CHECK_FAILED"
          };
        }
      }
      async checkCoverage(input) {
        try {
          const coverageService = getTestCoverageService(input.workspacePath);
          const projectCoverage = coverageService.getProjectCoverage();
          const lowCoverageFiles = coverageService.getLowCoverageFiles(input.threshold ?? 50);
          const threshold = input.threshold ?? 80;
          const totalPercent = projectCoverage?.lines ?? 0;
          return {
            ok: true,
            data: {
              totalPercent,
              filesCovered: lowCoverageFiles.length,
              filesTotal: lowCoverageFiles.length,
              belowThreshold: lowCoverageFiles,
              passed: totalPercent >= threshold
            }
          };
        } catch (error) {
          return {
            ok: false,
            error: errorMessage(error),
            code: "COVERAGE_CHECK_FAILED"
          };
        }
      }
      async computeHealthScore(input) {
        try {
          const [validationResult, coverageResult, orphanResult] = await Promise.allSettled([
            this.quickCheck({
              workspacePath: input.workspacePath
            }),
            this.checkCoverage({
              workspacePath: input.workspacePath
            }),
            this.checkOrphans({
              workspacePath: input.workspacePath
            })
          ]);
          const dimensions = {};
          if (validationResult.status === "fulfilled" && validationResult.value.ok) {
            dimensions.validation = validationResult.value.data.passed ? 100 : Math.max(0, 100 - validationResult.value.data.errorCount * 10);
          }
          if (coverageResult.status === "fulfilled" && coverageResult.value.ok) {
            dimensions.coverage = coverageResult.value.data.totalPercent;
          }
          if (orphanResult.status === "fulfilled" && orphanResult.value.ok) {
            dimensions.orphanRatio = Math.max(0, 100 - orphanResult.value.data.count * 5);
          }
          const overall = Object.values(dimensions).reduce((a, b) => a + b, 0) / Object.keys(dimensions).length || 0;
          return {
            ok: true,
            data: {
              overall: Math.round(overall),
              dimensions,
              trend: "stable"
            }
          };
        } catch (error) {
          return {
            ok: false,
            error: errorMessage(error),
            code: "HEALTH_SCORE_FAILED"
          };
        }
      }
      async analyzeEvolution(input) {
        try {
          const evolutionService = getEvolutionDetectorService(input.workspacePath);
          const result7 = await evolutionService.scanForDeprecations();
          const snapshots = result7.deprecations.map((d) => ({
            timestamp: Date.now(),
            score: Math.round(d.confidence * 100),
            errorCount: result7.deprecations.filter((dep) => dep.confidence > 0.7).length,
            warningCount: result7.deprecations.filter((dep) => dep.confidence <= 0.7).length
          }));
          return {
            ok: true,
            data: {
              snapshots,
              trend: "stable"
            }
          };
        } catch (error) {
          return {
            ok: false,
            error: errorMessage(error),
            code: "EVOLUTION_ANALYSIS_FAILED"
          };
        }
      }
      async checkCircularDeps(input) {
        try {
          const cycles = await this.runMadgeCircular(input.workspacePath);
          return {
            ok: true,
            data: {
              cycles,
              hasCycles: cycles.length > 0
            }
          };
        } catch (error) {
          return {
            ok: false,
            error: errorMessage(error),
            code: "CIRCULAR_DEPS_CHECK_FAILED"
          };
        }
      }
      async runMadgeCircular(workspacePath) {
        return new Promise((resolve72) => {
          const madgePath = join(workspacePath, "node_modules", ".bin", "madge");
          const hasLocalMadge = existsSync(madgePath);
          const args = [
            "--circular",
            "--json",
            "."
          ];
          const command = hasLocalMadge ? madgePath : "npx";
          const commandArgs = hasLocalMadge ? args : [
            "madge",
            ...args
          ];
          const proc = spawn(command, commandArgs, {
            cwd: workspacePath,
            stdio: [
              "pipe",
              "pipe",
              "pipe"
            ]
          });
          let stdout = "";
          proc.stdout?.on("data", (data) => {
            stdout += data.toString();
          });
          proc.on("close", () => {
            try {
              const result7 = JSON.parse(stdout);
              if (Array.isArray(result7)) {
                resolve72(result7);
              } else {
                resolve72([]);
              }
            } catch {
              resolve72([]);
            }
          });
          proc.on("error", () => {
            resolve72([]);
          });
        });
      }
      async checkOrphans(input) {
        try {
          const orphanResult = await detectOrphans(input.workspacePath);
          const orphans = Array.isArray(orphanResult) ? orphanResult : [];
          return {
            ok: true,
            data: {
              orphans,
              count: orphans.length
            }
          };
        } catch (error) {
          return {
            ok: false,
            error: errorMessage(error),
            code: "ORPHAN_CHECK_FAILED"
          };
        }
      }
      async checkIntegrations(_input) {
        try {
          const services4 = {};
          const daemonResult = await this.checkDaemonSocket();
          services4.daemon = daemonResult;
          const tscResult = await this.checkToolAvailability("tsc", [
            "--version"
          ]);
          services4.typescript = tscResult;
          const biomeResult = await this.checkToolAvailability("biome", [
            "--version"
          ]);
          services4.biome = biomeResult;
          const healthy = Object.values(services4).every((s) => s.healthy);
          return {
            ok: true,
            data: {
              healthy,
              services: services4
            }
          };
        } catch (error) {
          return {
            ok: false,
            error: errorMessage(error),
            code: "INTEGRATIONS_CHECK_FAILED"
          };
        }
      }
      async checkDaemonSocket() {
        const socketPath = process.env.VREKO_DAEMON_SOCKET ?? getDefaultSocketPath();
        const startTime = Date.now();
        if (!existsSync(socketPath)) {
          return {
            healthy: false,
            latency: 0
          };
        }
        return {
          healthy: true,
          latency: Date.now() - startTime
        };
      }
      async checkToolAvailability(tool, args) {
        return new Promise((resolve72) => {
          const startTime = Date.now();
          const proc = spawn("npx", [
            tool,
            ...args
          ], {
            stdio: [
              "pipe",
              "pipe",
              "pipe"
            ],
            timeout: 5e3
          });
          proc.on("close", (code) => {
            resolve72({
              healthy: code === 0,
              latency: Date.now() - startTime
            });
          });
          proc.on("error", () => {
            resolve72({
              healthy: false,
              latency: Date.now() - startTime
            });
          });
        });
      }
      async checkWiring(input) {
        try {
          const issues = [];
          const orphans = await this.runMadgeOrphans(input.workspacePath);
          for (const orphan of orphans) {
            issues.push({
              severity: "warning",
              code: "ORPHANED_FILE",
              message: "File is not imported by any other module",
              file: orphan
            });
          }
          const entryPointIssues = await this.checkEntryPoints(input.workspacePath);
          issues.push(...entryPointIssues);
          return {
            ok: true,
            data: {
              valid: issues.filter((i) => i.severity === "error").length === 0,
              issues
            }
          };
        } catch (error) {
          return {
            ok: false,
            error: errorMessage(error),
            code: "WIRING_CHECK_FAILED"
          };
        }
      }
      async runMadgeOrphans(workspacePath) {
        return new Promise((resolve72) => {
          const madgePath = join(workspacePath, "node_modules", ".bin", "madge");
          const hasLocalMadge = existsSync(madgePath);
          const args = [
            "--orphans",
            "--json",
            "."
          ];
          const command = hasLocalMadge ? madgePath : "npx";
          const commandArgs = hasLocalMadge ? args : [
            "madge",
            ...args
          ];
          const proc = spawn(command, commandArgs, {
            cwd: workspacePath,
            stdio: [
              "pipe",
              "pipe",
              "pipe"
            ]
          });
          let stdout = "";
          proc.stdout?.on("data", (data) => {
            stdout += data.toString();
          });
          proc.on("close", () => {
            try {
              const result7 = JSON.parse(stdout);
              if (Array.isArray(result7)) {
                resolve72(result7);
              } else {
                resolve72([]);
              }
            } catch {
              resolve72([]);
            }
          });
          proc.on("error", () => {
            resolve72([]);
          });
        });
      }
      async checkEntryPoints(workspacePath) {
        const issues = [];
        const commonEntryPoints = [
          "src/index.ts",
          "src/main.ts",
          "index.ts",
          "main.ts"
        ];
        let hasEntryPoint = false;
        for (const entry of commonEntryPoints) {
          if (existsSync(join(workspacePath, entry))) {
            hasEntryPoint = true;
            break;
          }
        }
        const pkgPath = join(workspacePath, "package.json");
        if (existsSync(pkgPath)) {
          try {
            const pkg = JSON.parse(readFileSync(pkgPath, "utf-8"));
            if (pkg.main || pkg.exports) {
              hasEntryPoint = true;
            }
          } catch {
          }
        }
        if (!hasEntryPoint) {
          issues.push({
            severity: "warning",
            code: "MISSING_ENTRY_POINT",
            message: "No entry point found (src/index.ts, main, or exports in package.json)"
          });
        }
        return issues;
      }
      // ==================== Private Helpers ====================
      async checkPatternsForFiles(workspacePath, files) {
        const diagnostics = [];
        for (const file of files) {
          try {
            const filePath = file.startsWith("/") ? file : join(workspacePath, file);
            if (!existsSync(filePath)) {
              continue;
            }
            const code = readFileSync(filePath, "utf-8");
            const result7 = await this.checkPatterns({
              workspacePath,
              code,
              filePath: file
            });
            if (result7.ok) {
              diagnostics.push(...result7.data.violations);
            }
          } catch {
          }
        }
        return diagnostics;
      }
      async runTypeScriptCheck(workspacePath, _files) {
        return new Promise((resolve72) => {
          const diagnostics = [];
          const tscPath = join(workspacePath, "node_modules", ".bin", "tsc");
          const hasLocalTsc = existsSync(tscPath);
          const args = [
            "--noEmit",
            "--skipLibCheck",
            "--incremental",
            "--pretty",
            "false"
          ];
          const command = hasLocalTsc ? tscPath : "npx";
          const commandArgs = hasLocalTsc ? args : [
            "tsc",
            ...args
          ];
          const proc = spawn(command, commandArgs, {
            cwd: workspacePath,
            stdio: [
              "pipe",
              "pipe",
              "pipe"
            ]
          });
          let stderr = "";
          proc.stderr?.on("data", (data) => {
            stderr += data.toString();
          });
          proc.on("close", () => {
            const lines = stderr.split("\n");
            for (const line of lines) {
              const match = line.match(/(.+)\((\d+),(\d+)\): error (.+): (.+)/);
              if (match) {
                diagnostics.push({
                  severity: "error",
                  code: match[4].trim(),
                  message: match[5].trim(),
                  file: match[1].trim(),
                  line: Number.parseInt(match[2], 10),
                  column: Number.parseInt(match[3], 10)
                });
              }
            }
            resolve72({
              diagnostics
            });
          });
          proc.on("error", () => {
            resolve72({
              diagnostics: []
            });
          });
        });
      }
      async runLintCheck(workspacePath, files, fix = false) {
        return new Promise((resolve72) => {
          const diagnostics = [];
          const biomePath = join(workspacePath, "node_modules", ".bin", "biome");
          const hasLocalBiome = existsSync(biomePath);
          const args = [
            "check",
            "--reporter=json",
            ...files
          ];
          if (fix) {
            args.push("--write");
          }
          const command = hasLocalBiome ? biomePath : "npx";
          const commandArgs = hasLocalBiome ? args : [
            "biome",
            ...args
          ];
          const proc = spawn(command, commandArgs, {
            cwd: workspacePath,
            stdio: [
              "pipe",
              "pipe",
              "pipe"
            ]
          });
          let stdout = "";
          proc.stdout?.on("data", (data) => {
            stdout += data.toString();
          });
          proc.on("close", () => {
            const lines = stdout.split("\n");
            for (const line of lines) {
              const match = line.match(/(.+):(\d+):(\d+)\s+(error|warning)\s+(.+)/i);
              if (match) {
                diagnostics.push({
                  severity: match[4].toLowerCase(),
                  code: "LINT",
                  message: match[5].trim(),
                  file: match[1].trim(),
                  line: Number.parseInt(match[2], 10),
                  column: Number.parseInt(match[3], 10)
                });
              }
            }
            resolve72({
              diagnostics
            });
          });
          proc.on("error", () => {
            resolve72({
              diagnostics: []
            });
          });
        });
      }
      async runBuildCommand(workspacePath) {
        return new Promise((resolve72) => {
          const proc = spawn("npm", [
            "run",
            "build"
          ], {
            cwd: workspacePath,
            stdio: [
              "pipe",
              "pipe",
              "pipe"
            ]
          });
          let stderr = "";
          proc.stderr?.on("data", (data) => {
            stderr += data.toString();
          });
          proc.on("close", (code) => {
            const diagnostics = [];
            if (code !== 0) {
              const lines = stderr.split("\n");
              for (const line of lines) {
                if (line.includes("error")) {
                  diagnostics.push({
                    severity: "error",
                    code: "BUILD_ERROR",
                    message: line.trim()
                  });
                }
              }
            }
            resolve72({
              success: code === 0,
              diagnostics
            });
          });
          proc.on("error", () => {
            resolve72({
              success: false,
              diagnostics: []
            });
          });
        });
      }
    };
    validationServiceInstance = null;
    __name8(getValidationService, "getValidationService");
  }
});
var init_validation_service = __esm2({
  "src/services/validation-service.ts"() {
    init_validation2();
  }
});
async function updateWorkflowStageAsync(workspaceRoot, stage) {
  await pushObservationAsync(workspaceRoot, {
    type: "progress",
    message: `Workflow stage: ${stage}`
  });
}
__name(updateWorkflowStageAsync, "updateWorkflowStageAsync");
async function shouldSuggestSnapEndAsync(workspaceRoot) {
  const state = await getSessionStateAsync(workspaceRoot);
  if (!state) {
    return {
      shouldSuggest: false
    };
  }
  if (!state.currentTask) {
    return {
      shouldSuggest: false
    };
  }
  if (state.changesSinceTaskStart.length === 0) {
    return {
      shouldSuggest: false
    };
  }
  const recentValidation = state.validationEvents.length > 0 ? state.validationEvents[state.validationEvents.length - 1] : null;
  if (!recentValidation || !recentValidation.success) {
    return {
      shouldSuggest: false
    };
  }
  const lastToolCall = state.toolCallLog.length > 0 ? state.toolCallLog[state.toolCallLog.length - 1] : null;
  const now = Date.now();
  const idleTime = lastToolCall ? now - lastToolCall.timestamp : 0;
  if (idleTime < IDLE_THRESHOLD_MS) {
    return {
      shouldSuggest: false
    };
  }
  return {
    shouldSuggest: true,
    reason: `Validation passed ${Math.floor(idleTime / 6e4)}m ago, ${state.changesSinceTaskStart.length} files modified. Ready to close session?`
  };
}
__name(shouldSuggestSnapEndAsync, "shouldSuggestSnapEndAsync");
async function detectCeremonyRequirementAsync(workspaceRoot) {
  const state = await getSessionStateAsync(workspaceRoot);
  if (!state || !state.currentTask) {
    return null;
  }
  const changedFiles = state.changesSinceTaskStart.map((c) => c.file);
  const priorFailures = state.validationEvents.filter((e) => !e.success).length;
  const highRiskFiles = changedFiles.filter((file) => HIGH_RISK_PATHS.some((pattern) => pattern.test(file)));
  if (highRiskFiles.length > 0) {
    return {
      required: true,
      level: "standard",
      reason: `High-risk paths modified: ${highRiskFiles.slice(0, 3).join(", ")}${highRiskFiles.length > 3 ? "..." : ""}`,
      enforced: true
    };
  }
  if (changedFiles.length > HIGH_VOLUME_FILE_THRESHOLD) {
    return {
      required: true,
      level: "comprehensive",
      reason: `High volume of changes: ${changedFiles.length} files modified`,
      enforced: true
    };
  }
  if (priorFailures > 0) {
    return {
      required: true,
      level: "standard",
      reason: `Prior validation failures: ${priorFailures} checks failed earlier in session`,
      enforced: false
    };
  }
  return null;
}
__name(detectCeremonyRequirementAsync, "detectCeremonyRequirementAsync");
async function suggestNextActionAsync(workspaceRoot, currentTool, currentMode) {
  const state = await getSessionStateAsync(workspaceRoot);
  if (!state) {
    return [];
  }
  const suggestions = [];
  if (currentTool === "vreko" && currentMode === "start") {
    suggestions.push({
      tool: "check",
      mode: "quick",
      priority: "recommended",
      reason: "Validate code before making changes"
    });
    suggestions.push({
      tool: "advise",
      priority: "optional",
      reason: "Get recommendations for this task"
    });
  }
  if (currentTool === "check") {
    const recentValidation = state.validationEvents.length > 0 ? state.validationEvents[state.validationEvents.length - 1] : null;
    if (recentValidation?.success) {
      const { shouldSuggest, reason } = await shouldSuggestSnapEndAsync(workspaceRoot);
      if (shouldSuggest) {
        suggestions.push({
          tool: "snap_end",
          priority: "required",
          reason: reason || "Task complete, close session"
        });
      }
    }
  }
  if (currentTool === "snap_learn") {
    const hasChanges = state.changesSinceTaskStart.length > 0;
    if (hasChanges) {
      suggestions.push({
        tool: "check",
        mode: "quick",
        priority: "recommended",
        reason: "Validate changes before closing session"
      });
    }
    suggestions.push({
      tool: "snap_end",
      priority: "optional",
      reason: "Close session if task is complete"
    });
  }
  const ceremonyReq = await detectCeremonyRequirementAsync(workspaceRoot);
  if (ceremonyReq?.required && ceremonyReq.enforced) {
    const snapEndIndex = suggestions.findIndex((s) => s.tool === "snap_end");
    if (snapEndIndex !== -1) {
      suggestions[snapEndIndex] = {
        ...suggestions[snapEndIndex],
        priority: "required",
        reason: `${ceremonyReq.reason} - ceremony required`
      };
    }
  }
  return suggestions;
}
__name(suggestNextActionAsync, "suggestNextActionAsync");
var IDLE_THRESHOLD_MS;
var HIGH_RISK_PATHS;
var HIGH_VOLUME_FILE_THRESHOLD;
var init_workflow_lifecycle_async = __esm2({
  "src/session/workflow-lifecycle-async.ts"() {
    init_session_adapter();
    IDLE_THRESHOLD_MS = 3 * 60 * 1e3;
    HIGH_RISK_PATHS = [
      /^packages\//,
      /^config\//,
      /\bauth\b/i,
      /\bsecurity\b/i,
      /\.env/,
      /package\.json$/
    ];
    HIGH_VOLUME_FILE_THRESHOLD = 10;
    __name8(updateWorkflowStageAsync, "updateWorkflowStageAsync");
    __name8(shouldSuggestSnapEndAsync, "shouldSuggestSnapEndAsync");
    __name8(detectCeremonyRequirementAsync, "detectCeremonyRequirementAsync");
    __name8(suggestNextActionAsync, "suggestNextActionAsync");
  }
});
function normalizeMode(params, modeMap) {
  const rawMode = params.mode || params.m || modeMap.default;
  return modeMap.aliases[rawMode] || modeMap.default;
}
__name(normalizeMode, "normalizeMode");
function normalizeFiles(params) {
  const rawFiles = params.files || params.f;
  if (!rawFiles) {
    return [];
  }
  return Array.isArray(rawFiles) ? rawFiles : [
    rawFiles
  ];
}
__name(normalizeFiles, "normalizeFiles");
var CHECK_MODE_MAP;
var init_param_normalizer = __esm2({
  "src/utils/param-normalizer.ts"() {
    init_check_modes_registry();
    CHECK_MODE_MAP = {
      default: "quick",
      aliases: {
        // Short aliases for common modes
        q: "quick",
        f: "full",
        p: "patterns",
        // All implemented modes (derived from registry - SSOT)
        ...Object.fromEntries(IMPLEMENTED_CHECK_MODES.map((mode) => [
          mode,
          mode
        ]))
      }
    };
    __name8(normalizeMode, "normalizeMode");
    __name8(normalizeFiles, "normalizeFiles");
  }
});
var check_exports = {};
__export2(check_exports, {
  checkTool: /* @__PURE__ */ __name(() => checkTool, "checkTool"),
  handleCheck: /* @__PURE__ */ __name(() => handleCheck, "handleCheck")
});
async function detectGitConflicts(workspaceRoot) {
  const { spawn: spawn6 } = await import('child_process');
  const { access: access22 } = await import('fs/promises');
  const conflictedFiles = await new Promise((resolve72) => {
    const proc = spawn6("git", [
      "diff",
      "--name-only",
      "--diff-filter=U"
    ], {
      cwd: workspaceRoot,
      stdio: [
        "pipe",
        "pipe",
        "pipe"
      ]
    });
    let stdout = "";
    proc.stdout?.on("data", (data) => {
      stdout += data.toString();
    });
    proc.on("close", () => {
      const files = stdout.split("\n").map((f) => f.trim()).filter((f) => f.length > 0);
      resolve72(files);
    });
    proc.on("error", () => resolve72([]));
  });
  let source = "unknown";
  try {
    await access22(`${workspaceRoot}/.git/MERGE_HEAD`);
    source = "merge";
  } catch {
    try {
      await access22(`${workspaceRoot}/.git/rebase-merge`);
      source = "rebase";
    } catch {
      try {
        await access22(`${workspaceRoot}/.git/CHERRY_PICK_HEAD`);
        source = "cherry-pick";
      } catch {
        if (conflictedFiles.length > 0) {
          source = "stash";
        }
      }
    }
  }
  return {
    hasConflicts: conflictedFiles.length > 0,
    conflictedFiles,
    source
  };
}
__name(detectGitConflicts, "detectGitConflicts");
async function getGitDiffFiles2(workspaceRoot, scope = "staged") {
  const { spawn: spawn6 } = await import('child_process');
  return new Promise((resolve72) => {
    let args;
    switch (scope) {
      case "staged":
        args = [
          "diff",
          "--cached",
          "--name-only",
          "--diff-filter=ACMR"
        ];
        break;
      case "changed":
        args = [
          "diff",
          "--name-only",
          "--diff-filter=ACMR"
        ];
        break;
      case "uncommitted":
        args = [
          "diff",
          "HEAD",
          "--name-only",
          "--diff-filter=ACMR"
        ];
        break;
      case "all":
        args = [
          "ls-files"
        ];
        break;
    }
    const proc = spawn6("git", args, {
      cwd: workspaceRoot,
      stdio: [
        "pipe",
        "pipe",
        "pipe"
      ]
    });
    let stdout = "";
    proc.stdout?.on("data", (data) => {
      stdout += data.toString();
    });
    proc.on("close", (code) => {
      if (code !== 0) {
        resolve72([]);
        return;
      }
      const files = stdout.split("\n").map((f) => f.trim()).filter((f) => f.length > 0).filter((f) => /\.(ts|tsx|js|jsx)$/.test(f));
      resolve72(files);
    });
    proc.on("error", () => resolve72([]));
  });
}
__name(getGitDiffFiles2, "getGitDiffFiles2");
async function autoDetectFiles2(workspaceRoot) {
  const staged = await getGitDiffFiles2(workspaceRoot, "staged");
  if (staged.length > 0) {
    return {
      files: staged,
      source: "staged"
    };
  }
  const uncommitted = await getGitDiffFiles2(workspaceRoot, "uncommitted");
  if (uncommitted.length > 0) {
    return {
      files: uncommitted,
      source: "uncommitted"
    };
  }
  return {
    files: [],
    source: "none"
  };
}
__name(autoDetectFiles2, "autoDetectFiles2");
function normalizeCheckParams(params) {
  const mode = normalizeMode(params, CHECK_MODE_MAP);
  const files = normalizeFiles(params);
  const compact = params.compact || false;
  return {
    mode,
    files,
    compact
  };
}
__name(normalizeCheckParams, "normalizeCheckParams");
async function formatCheckResult(result7, mode, options, fileSource, workspaceRoot) {
  try {
    const text2 = result7.content[0]?.text || "";
    const data = JSON.parse(text2);
    const passed = data.passed;
    const errors = data.errors?.length || 0;
    const warnings = data.warnings || 0;
    if (workspaceRoot) {
      await updateWorkflowStageAsync(workspaceRoot, "validating");
      await recordValidationEventAsync(workspaceRoot, {
        source: "snap_tool",
        category: mode === "quick" ? "typescript" : "build",
        tool: "check",
        mode,
        success: passed,
        details: passed ? "All checks passed" : `${errors} error(s), ${warnings} warning(s)`
      });
    }
    const wireFields = {
      mode,
      status: passed ? "\u2713" : "\u2717",
      err: `${errors}E`,
      warn: `${warnings}W`
    };
    if (fileSource && fileSource !== "explicit" && fileSource !== "none") {
      wireFields.src = fileSource;
    }
    const wire = formatWireLabeled("C", wireFields, options);
    let issueDetails = "";
    if (!passed && data.errors) {
      const issues = data.errors.slice(0, 3).map((e) => {
        const file = e.file ? basename(e.file) : "";
        const location = e.line !== void 0 ? `:${e.line}${e.column !== void 0 ? `:${e.column}` : ""}` : "";
        const code = e.code ? `[${e.code}]` : "";
        const msg = (e.message || "error").slice(0, 40);
        return file ? `${file}${location} ${code} ${msg}`.trim() : msg;
      });
      issueDetails = `|issues:${issues.join(",")}`;
    }
    const humanMessage = passed ? messages.validation.passed() : messages.validation.issues(errors, warnings);
    let nextActionHint = "";
    if (workspaceRoot && passed) {
      const { shouldSuggest, reason } = await shouldSuggestSnapEndAsync(workspaceRoot);
      if (shouldSuggest) {
        nextActionHint = `

\u{1F449} **Next:** ${reason}
   \u2192 Call snap_end() to close session`;
      } else {
        const suggestions = await suggestNextActionAsync(workspaceRoot, "check", mode);
        if (suggestions.length > 0) {
          const primary = suggestions[0];
          nextActionHint = `

\u{1F449} **Next:** ${primary.reason}
   \u2192 ${primary.tool}${primary.mode ? `({ mode: "${primary.mode}" })` : "()"}`;
        }
      }
    }
    const structuredContent = {
      passed,
      mode,
      errors,
      warnings,
      // ENRICHED: Include full diagnostic details with file:line:column for LLM parsing
      issues: data.errors?.slice(0, 5).map((e) => ({
        file: e.file,
        line: e.line,
        column: e.column,
        code: e.code,
        message: e.message,
        severity: e.severity
      })) || [],
      fileSource: fileSource !== "explicit" ? fileSource : void 0
    };
    return {
      content: [
        {
          type: "text",
          text: `${wire}${issueDetails}${INTERNAL_SEPARATOR}${humanMessage}${nextActionHint}`
        }
      ],
      structuredContent
    };
  } catch {
    return result7;
  }
}
__name(formatCheckResult, "formatCheckResult");
async function handleQuick(files, context) {
  const service = getValidationService();
  const input = {
    workspacePath: context.workspaceRoot,
    files
  };
  const result7 = await service.quickCheck(input);
  if (!result7.ok) {
    return {
      content: [
        {
          type: "text",
          text: JSON.stringify({
            passed: false,
            errors: [
              {
                message: result7.error
              }
            ]
          })
        }
      ]
    };
  }
  return {
    content: [
      {
        type: "text",
        text: JSON.stringify(result7.data)
      }
    ]
  };
}
__name(handleQuick, "handleQuick");
async function handleFull(files, context) {
  const service = getValidationService();
  const input = {
    workspacePath: context.workspaceRoot,
    files
  };
  const result7 = await service.fullCheck(input);
  if (!result7.ok) {
    return {
      content: [
        {
          type: "text",
          text: JSON.stringify({
            passed: false,
            errors: [
              {
                message: result7.error
              }
            ]
          })
        }
      ]
    };
  }
  return {
    content: [
      {
        type: "text",
        text: JSON.stringify(result7.data)
      }
    ]
  };
}
__name(handleFull, "handleFull");
async function handleSecurity(files, context) {
  const service = getValidationService();
  const input = {
    workspacePath: context.workspaceRoot,
    files
  };
  const result7 = await service.scanSecurity(input);
  if (!result7.ok) {
    return {
      content: [
        {
          type: "text",
          text: JSON.stringify({
            passed: false,
            errors: [
              {
                message: result7.error
              }
            ]
          })
        }
      ]
    };
  }
  const passed = result7.data.criticalCount === 0 && result7.data.highCount === 0;
  return {
    content: [
      {
        type: "text",
        text: JSON.stringify({
          passed,
          errors: result7.data.findings.filter((f) => f.severity === "critical" || f.severity === "high"),
          warnings: result7.data.findings.filter((f) => f.severity === "medium" || f.severity === "low").length
        })
      }
    ]
  };
}
__name(handleSecurity, "handleSecurity");
async function handleCoverage(_files, context) {
  const service = getValidationService();
  const input = {
    workspacePath: context.workspaceRoot
  };
  const result7 = await service.checkCoverage(input);
  if (!result7.ok) {
    return {
      content: [
        {
          type: "text",
          text: JSON.stringify({
            passed: false,
            errors: [
              {
                message: result7.error
              }
            ]
          })
        }
      ]
    };
  }
  return {
    content: [
      {
        type: "text",
        text: JSON.stringify(result7.data)
      }
    ]
  };
}
__name(handleCoverage, "handleCoverage");
async function handleCircular(files, context) {
  const service = getValidationService();
  const input = {
    workspacePath: context.workspaceRoot,
    files
  };
  const result7 = await service.checkCircularDeps(input);
  if (!result7.ok) {
    return {
      content: [
        {
          type: "text",
          text: JSON.stringify({
            passed: false,
            errors: [
              {
                message: result7.error
              }
            ]
          })
        }
      ]
    };
  }
  return {
    content: [
      {
        type: "text",
        text: JSON.stringify({
          passed: !result7.data.hasCycles,
          errors: result7.data.cycles,
          warnings: 0
        })
      }
    ]
  };
}
__name(handleCircular, "handleCircular");
async function handleOrphans(context) {
  const service = getValidationService();
  const input = {
    workspacePath: context.workspaceRoot
  };
  const result7 = await service.checkOrphans(input);
  if (!result7.ok) {
    return {
      content: [
        {
          type: "text",
          text: JSON.stringify({
            passed: false,
            errors: [
              {
                message: result7.error
              }
            ]
          })
        }
      ]
    };
  }
  return {
    content: [
      {
        type: "text",
        text: JSON.stringify({
          passed: result7.data.count === 0,
          errors: result7.data.orphans,
          warnings: result7.data.count
        })
      }
    ]
  };
}
__name(handleOrphans, "handleOrphans");
async function handleHealth(context) {
  const service = getValidationService();
  const input = {
    workspacePath: context.workspaceRoot
  };
  const result7 = await service.computeHealthScore(input);
  if (!result7.ok) {
    return {
      content: [
        {
          type: "text",
          text: JSON.stringify({
            passed: false,
            errors: [
              {
                message: result7.error
              }
            ]
          })
        }
      ]
    };
  }
  return {
    content: [
      {
        type: "text",
        text: JSON.stringify({
          passed: result7.data.overall >= 70,
          errors: result7.data.overall < 50 ? [
            {
              message: `Health score low: ${result7.data.overall}`
            }
          ] : [],
          warnings: result7.data.overall < 70 ? 1 : 0
        })
      }
    ]
  };
}
__name(handleHealth, "handleHealth");
async function handleEvolution(_files, context) {
  const service = getValidationService();
  const input = {
    workspacePath: context.workspaceRoot
  };
  const result7 = await service.analyzeEvolution(input);
  if (!result7.ok) {
    return {
      content: [
        {
          type: "text",
          text: JSON.stringify({
            passed: false,
            errors: [
              {
                message: result7.error
              }
            ]
          })
        }
      ]
    };
  }
  return {
    content: [
      {
        type: "text",
        text: JSON.stringify({
          passed: result7.data.trend !== "declining",
          errors: result7.data.trend === "declining" ? [
            {
              message: `Health trend: ${result7.data.trend}`
            }
          ] : [],
          warnings: result7.data.trend === "stable" ? 1 : 0
        })
      }
    ]
  };
}
__name(handleEvolution, "handleEvolution");
async function handleIntegrations(context) {
  const service = getValidationService();
  const input = {
    workspacePath: context.workspaceRoot
  };
  const result7 = await service.checkIntegrations(input);
  if (!result7.ok) {
    return {
      content: [
        {
          type: "text",
          text: JSON.stringify({
            passed: false,
            errors: [
              {
                message: result7.error
              }
            ]
          })
        }
      ]
    };
  }
  const unhealthyServices = Object.entries(result7.data.services).filter(([, s]) => !s.healthy).map(([name, _s]) => ({
    message: `${name}: unhealthy`
  }));
  return {
    content: [
      {
        type: "text",
        text: JSON.stringify({
          passed: result7.data.healthy,
          errors: unhealthyServices,
          warnings: 0
        })
      }
    ]
  };
}
__name(handleIntegrations, "handleIntegrations");
async function handleWiring(_files, context) {
  const service = getValidationService();
  const input = {
    workspacePath: context.workspaceRoot
  };
  const result7 = await service.checkWiring(input);
  if (!result7.ok) {
    return {
      content: [
        {
          type: "text",
          text: JSON.stringify({
            passed: false,
            errors: [
              {
                message: result7.error
              }
            ]
          })
        }
      ]
    };
  }
  return {
    content: [
      {
        type: "text",
        text: JSON.stringify({
          passed: result7.data.valid,
          errors: result7.data.issues.map((i) => ({
            message: i
          })),
          warnings: 0
        })
      }
    ]
  };
}
__name(handleWiring, "handleWiring");
async function handleBuild(context) {
  const service = getValidationService();
  const input = {
    workspacePath: context.workspaceRoot
  };
  const result7 = await service.checkBuild(input);
  if (!result7.ok) {
    return {
      content: [
        {
          type: "text",
          text: JSON.stringify({
            passed: false,
            errors: [
              {
                message: result7.error
              }
            ]
          })
        }
      ]
    };
  }
  return {
    content: [
      {
        type: "text",
        text: JSON.stringify({
          passed: result7.data.success,
          errors: result7.data.success ? [] : result7.data.diagnostics.map((d) => ({
            message: d.message
          })),
          warnings: 0
        })
      }
    ]
  };
}
__name(handleBuild, "handleBuild");
async function handlePatterns(params, context) {
  const { handleCheckPatterns: handleCheckPatterns2 } = await Promise.resolve().then(() => (init_handlers(), handlers_exports));
  return handleCheckPatterns2({
    code: params.code || "",
    filePath: ""
  }, context);
}
__name(handlePatterns, "handlePatterns");
function notImplementedHandler(mode) {
  return async () => ({
    content: [
      {
        type: "text",
        text: JSON.stringify({
          passed: false,
          errors: [
            `Mode '${mode}' is not yet implemented. Use mode='quick' or mode='full' instead.`
          ],
          warnings: []
        })
      }
    ]
  });
}
__name(notImplementedHandler, "notImplementedHandler");
var handleImpact;
var handleComplexity;
var handleDocs;
var handleLearnings;
var handleArchitecture;
var handleTrace;
var handleCheck;
var checkTool;
var init_check = __esm2({
  "src/tools/consolidated/check.ts"() {
    init_session_adapter();
    init_branding();
    init_check_modes_registry();
    init_input_sanitization();
    init_session_file_tracker();
    init_validation_service();
    init_workflow_lifecycle_async();
    init_param_normalizer();
    __name8(detectGitConflicts, "detectGitConflicts");
    __name8(getGitDiffFiles2, "getGitDiffFiles");
    __name8(autoDetectFiles2, "autoDetectFiles");
    __name8(normalizeCheckParams, "normalizeCheckParams");
    __name8(formatCheckResult, "formatCheckResult");
    __name8(handleQuick, "handleQuick");
    __name8(handleFull, "handleFull");
    __name8(handleSecurity, "handleSecurity");
    __name8(handleCoverage, "handleCoverage");
    __name8(handleCircular, "handleCircular");
    __name8(handleOrphans, "handleOrphans");
    __name8(handleHealth, "handleHealth");
    __name8(handleEvolution, "handleEvolution");
    __name8(handleIntegrations, "handleIntegrations");
    __name8(handleWiring, "handleWiring");
    __name8(handleBuild, "handleBuild");
    __name8(handlePatterns, "handlePatterns");
    __name8(notImplementedHandler, "notImplementedHandler");
    handleImpact = notImplementedHandler("impact");
    handleComplexity = notImplementedHandler("complexity");
    handleDocs = notImplementedHandler("docs");
    handleLearnings = notImplementedHandler("learnings");
    handleArchitecture = notImplementedHandler("architecture");
    handleTrace = notImplementedHandler("trace");
    handleCheck = /* @__PURE__ */ __name8(async (args, context) => {
      const validation = validateCheckParams(args);
      if (!validation.success) {
        return {
          content: [
            {
              type: "text",
              text: `\u274C Input validation failed:
${validation.errors?.join("\n") ?? "Unknown validation error"}`
            }
          ],
          isError: true
        };
      }
      const params = validation.data;
      let { mode, files, compact } = normalizeCheckParams(params);
      const shouldCheckConflicts = [
        "full",
        "security",
        "build"
      ].includes(mode);
      if (shouldCheckConflicts) {
        const conflictCheck = await detectGitConflicts(context.workspaceRoot);
        if (conflictCheck.hasConflicts) {
          return {
            content: [
              {
                type: "text",
                text: JSON.stringify({
                  passed: false,
                  errors: [
                    {
                      message: `Git ${conflictCheck.source} conflicts detected in ${conflictCheck.conflictedFiles.length} file(s): ${conflictCheck.conflictedFiles.join(", ")}. Resolve conflicts before running Vreko checks.`,
                      code: "GIT_CONFLICTS",
                      conflictedFiles: conflictCheck.conflictedFiles,
                      source: conflictCheck.source,
                      suggestedAction: "Resolve conflicts: git status, edit files, git add <files>, git commit"
                    }
                  ],
                  warnings: []
                })
              }
            ],
            isError: true
          };
        }
      }
      const needsFiles = [
        "quick",
        "full",
        "patterns",
        "impact",
        "trace",
        "security",
        "coverage"
      ].includes(mode);
      let fileSource = "explicit";
      if (files.length === 0 && needsFiles && !params.code) {
        if (params.diff) {
          const diffFiles = await getGitDiffFiles2(context.workspaceRoot, params.diff);
          files = diffFiles;
          fileSource = params.diff;
        } else {
          const detected = await autoDetectFiles2(context.workspaceRoot);
          files = detected.files;
          fileSource = detected.source;
        }
      }
      if (files.length > 0 && mode !== "learnings") {
        const tracker = getSessionFileTracker(context.workspaceRoot);
        tracker.trackFiles(files, "validated");
      }
      const options = {
        compact
      };
      let result7;
      switch (mode) {
        case "quick":
          result7 = await handleQuick(files, context);
          break;
        case "full":
          result7 = await handleFull(files, context);
          break;
        case "patterns":
          result7 = await handlePatterns(params, context);
          break;
        case "build":
          result7 = await handleBuild(context);
          break;
        case "impact":
          result7 = await handleImpact();
          break;
        case "circular":
          result7 = await handleCircular(files, context);
          break;
        case "complexity":
          result7 = await handleComplexity();
          break;
        case "docs":
          result7 = await handleDocs();
          break;
        case "learnings":
          result7 = await handleLearnings();
          break;
        case "architecture":
          result7 = await handleArchitecture();
          break;
        case "trace":
          result7 = await handleTrace();
          break;
        case "security":
          result7 = await handleSecurity(files, context);
          break;
        case "coverage":
          result7 = await handleCoverage(files, context);
          break;
        case "orphans":
          result7 = await handleOrphans(context);
          break;
        case "health":
          result7 = await handleHealth(context);
          break;
        case "evolution":
          result7 = await handleEvolution(files, context);
          break;
        case "integrations":
          result7 = await handleIntegrations(context);
          break;
        case "wiring":
          result7 = await handleWiring(files, context);
          break;
        default:
          result7 = {
            content: [
              {
                type: "text",
                text: JSON.stringify({
                  passed: false,
                  errors: [
                    {
                      message: `Unknown mode: ${mode}`
                    }
                  ]
                })
              }
            ]
          };
      }
      return await formatCheckResult(result7, mode, options, fileSource, context.workspaceRoot);
    }, "handleCheck");
    checkTool = {
      name: "check",
      description: `[DEPRECATED  -  use vreko_begin/vreko_learn/vreko_end/vreko_pulse] Validate code quality: types, lint, security, architecture.

WHEN TO USE:
- Pre-commit or pre-merge validation
- Security audits before deployment
- Architectural compliance checks
- Dead code detection and cleanup

COMMON MODES (${IMPLEMENTED_MODE_COUNT} total):
- quick (q): Fast validation (TS + lint, ~30s)
- full (f): Comprehensive (TS + lint + tests + architecture, ~2min)
- security (s): Security scan (OWASP Top 10, credentials, ~45s)
- circular (c): Circular dependency detection (~15s)
- architecture (a): Layer violations and coupling (~1min)

For full mode list: snap_help({ topic: "modes" })

CLI EQUIVALENT:
  npx vreko check --mode quick
  npx vreko check --mode full --files "src/**/*.ts"

WORKFLOW POSITION: Post-implementation validation
  vreko({ mode: "start" }) \u2192 [modify files] \u2192 check({ mode: "quick" }) \u2192 snap_end()`,
      inputSchema: {
        type: "object",
        properties: {
          mode: {
            type: "string",
            enum: IMPLEMENTED_CHECK_MODES,
            description: `Validation mode (${IMPLEMENTED_MODE_COUNT} available):

COMMON:
  \u2022 quick (q): Fast validation (TS + lint, ~30s)
  \u2022 full (f): Comprehensive validation (TS + lint + tests + architecture, ~2min)
  \u2022 security (s): Security scan (OWASP Top 10, credentials, injection patterns, ~45s)

ANALYSIS:
  \u2022 circular (c): Circular dependency detection (~15s)
  \u2022 architecture (a): Layer violations and coupling analysis (~1min)
  \u2022 orphans (o): Dead code detection (~30s)
  \u2022 coverage (cov): Test coverage gaps (~45s)
  \u2022 trace (t): Dependency tracing (~20s)

MAINTENANCE:
  \u2022 docs (d): Documentation freshness check (~10s)
  \u2022 evolution (e): Deprecated pattern detection (~20s)
  \u2022 learnings (l): Learning tier health (~5s)
  \u2022 health (h): MCP server health check (~5s)

For complete details: snap_help({ topic: "modes" })`
          },
          files: {
            type: "string",
            description: "File(s) to check  -  single path or comma-separated paths"
          },
          diff: {
            type: "string",
            enum: [
              "staged",
              "changed",
              "uncommitted"
            ],
            description: "Diff scope for auto-detecting files. If omitted, uses staged first then uncommitted."
          },
          code: {
            type: "string",
            description: "Code string to validate (for patterns mode)"
          },
          tests: {
            type: "boolean",
            description: "Also run tests (quick mode only)"
          },
          compact: {
            type: "boolean",
            default: false,
            description: "Use compact wire format instead of labeled (default: labeled)"
          }
        }
      },
      outputSchema: {
        type: "object",
        properties: {
          passed: {
            type: "boolean",
            description: "Whether validation passed"
          },
          errors: {
            type: "number",
            description: "Error count"
          },
          warnings: {
            type: "number",
            description: "Warning count"
          },
          issues: {
            type: "array",
            items: {
              type: "string"
            },
            description: "Issue summaries"
          }
        }
      },
      annotations: {
        title: "\u{1F98E} Vreko Check",
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true
      },
      tier: "free"
    };
  }
});
function applyRiskModifications(steps, context) {
  const modifications = [];
  let modifiedSteps = [
    ...steps
  ];
  if (context.risk === "H") {
    const agentActionIndex = modifiedSteps.findIndex((s) => s.isAgentAction);
    if (agentActionIndex > 0) {
      const securityStep = {
        order: 0,
        tool: "check",
        params: {
          mode: "security"
        },
        reason: "High risk detected - security validation required"
      };
      modifiedSteps.splice(agentActionIndex, 0, securityStep);
      modifications.push({
        change: "Added security check before implementation",
        reason: "High risk level detected"
      });
    }
    const lastCheckIndex = modifiedSteps.map((s, idx) => ({
      s,
      idx
    })).reverse().find(({ s }) => s.tool === "check" && !s.isAgentAction)?.idx ?? -1;
    if (lastCheckIndex >= 0 && modifiedSteps[lastCheckIndex].params?.mode !== "full") {
      modifiedSteps[lastCheckIndex] = {
        ...modifiedSteps[lastCheckIndex],
        params: {
          ...modifiedSteps[lastCheckIndex].params,
          mode: "full"
        },
        reason: "Upgraded to full validation due to high risk"
      };
      modifications.push({
        change: "Upgraded final check to full mode",
        reason: "High risk requires comprehensive validation"
      });
    }
  }
  if (context.risk === "M" || context.risk === "H") {
    const hasArchCheck = modifiedSteps.some((s) => s.tool === "check" && s.params?.mode === "architecture");
    if (!hasArchCheck) {
      const insertIndex = modifiedSteps.findIndex((s) => s.isAgentAction);
      if (insertIndex > 0) {
        const archStep = {
          order: 0,
          tool: "check",
          params: {
            mode: "architecture"
          },
          reason: "Verify architecture boundaries"
        };
        modifiedSteps.splice(insertIndex, 0, archStep);
        modifications.push({
          change: "Added architecture check",
          reason: `${context.risk === "H" ? "High" : "Medium"} risk - verify boundaries before changes`
        });
      }
    }
  }
  if (context.files?.some((f) => /auth|security|password|token|secret/i.test(f))) {
    const hasSecurityCheck = modifiedSteps.some((s) => s.tool === "check" && s.params?.mode === "security");
    if (!hasSecurityCheck) {
      const agentActionIndex = modifiedSteps.findIndex((s) => s.isAgentAction);
      if (agentActionIndex > 0) {
        const securityStep = {
          order: 0,
          tool: "check",
          params: {
            mode: "security"
          },
          reason: "Security-sensitive files detected"
        };
        modifiedSteps.splice(agentActionIndex, 0, securityStep);
        modifications.push({
          change: "Added security check",
          reason: "Touching security-sensitive files"
        });
      }
    }
  }
  if (context.files && context.files.length > 5) {
    const hasArchCheck = modifiedSteps.some((s) => s.tool === "check" && s.params?.mode === "architecture");
    if (!hasArchCheck) {
      const insertIndex = modifiedSteps.findIndex((s) => s.isAgentAction);
      if (insertIndex > 0) {
        const archStep = {
          order: 0,
          tool: "check",
          params: {
            mode: "architecture"
          },
          reason: "Large changeset - verify architecture"
        };
        modifiedSteps.splice(insertIndex, 0, archStep);
        modifications.push({
          change: "Added architecture check",
          reason: `Touching ${context.files.length} files - verify boundaries`
        });
      }
    }
  }
  modifiedSteps = modifiedSteps.map((step, index) => ({
    ...step,
    order: index + 1
  }));
  return {
    steps: modifiedSteps,
    modifications
  };
}
__name(applyRiskModifications, "applyRiskModifications");
function formatWireWorkflow(response, compact) {
  const planStr = response.plan.filter((s) => !s.isAgentAction).slice(0, 5).map((s) => {
    const mode = s.params?.mode;
    return mode ? `${s.tool}\u2192${mode}` : s.tool;
  }).join(",");
  return formatWireLabeled("G", {
    intent: response.intent,
    risk: response.risk,
    steps: response.plan.length,
    mods: response.modifications.length,
    plan: planStr
  }, {
    compact
  });
}
__name(formatWireWorkflow, "formatWireWorkflow");
function formatHumanWorkflow(response) {
  const lines = [];
  lines.push(`
\u{1F4CB} WORKFLOW PLAN (${response.intent}):
`);
  for (const step of response.plan) {
    if (step.isAgentAction) {
      lines.push(`${step.order}. [YOU: ${step.reason}]`);
    } else {
      const paramsStr = step.params ? `({ ${Object.entries(step.params).map(([k, v]) => `${k}: "${v}"`).join(", ")} })` : "()";
      lines.push(`${step.order}. ${step.tool}${paramsStr}`);
      lines.push(`   \u2192 ${step.reason}`);
    }
    if (step.conditional) {
      lines.push(`   \u26A0\uFE0F ${step.conditional}`);
    }
    lines.push("");
  }
  if (response.modifications.length > 0) {
    lines.push("MODIFICATIONS (based on context):");
    for (const mod of response.modifications) {
      lines.push(`- ${mod.change}`);
      lines.push(`  Reason: ${mod.reason}`);
    }
  }
  return lines.join("\n");
}
__name(formatHumanWorkflow, "formatHumanWorkflow");
function getExplanation(intent, modCount) {
  const base = {
    debug: "Debug workflow: Start session, validate state, get advice, fix, validate, and capture learnings.",
    implement: "Implementation workflow: Start session, get architecture guidance, implement, validate, and close.",
    refactor: "Refactoring workflow: Start session, check architecture, refactor, full validation with ceremony.",
    review: "Review workflow: Start session, check health, security scan, architecture validation, document findings.",
    explore: "Exploration workflow: Lightweight context load, explore, capture discoveries."
  };
  const modNote = modCount > 0 ? ` (${modCount} modifications applied based on your context)` : "";
  return base[intent] + modNote;
}
__name(getExplanation, "getExplanation");
var BASE_TEMPLATES;
var handleGuide;
var init_guide = __esm2({
  "src/tools/consolidated/guide.ts"() {
    init_session_adapter();
    init_branding();
    init_input_sanitization();
    BASE_TEMPLATES = {
      debug: [
        {
          order: 1,
          tool: "vreko",
          params: {
            mode: "start",
            intent: "debug"
          },
          reason: "Load context and anti-patterns relevant to debugging"
        },
        {
          order: 2,
          tool: "check",
          params: {
            mode: "quick"
          },
          reason: "Validate current state before making changes"
        },
        {
          order: 3,
          tool: "advise",
          params: {
            intent: "debug"
          },
          reason: "Get recommendations based on violation history"
        },
        {
          order: 4,
          tool: "[AGENT]",
          reason: "Investigate and implement the fix",
          isAgentAction: true
        },
        {
          order: 5,
          tool: "check",
          params: {
            mode: "full"
          },
          reason: "Comprehensive validation after fix",
          conditional: "If fails, return to step 4"
        },
        {
          order: 6,
          tool: "snap_end",
          params: {
            outcome: "completed"
          },
          reason: "Capture learnings for future debugging sessions"
        }
      ],
      implement: [
        {
          order: 1,
          tool: "vreko",
          params: {
            mode: "start",
            intent: "implement"
          },
          reason: "Load context and architecture patterns"
        },
        {
          order: 2,
          tool: "advise",
          params: {
            intent: "implement"
          },
          reason: "Get architecture guidance before implementation"
        },
        {
          order: 3,
          tool: "[AGENT]",
          reason: "Implement the feature",
          isAgentAction: true
        },
        {
          order: 4,
          tool: "check",
          params: {
            mode: "quick"
          },
          reason: "Pre-commit validation"
        },
        {
          order: 5,
          tool: "snap_end",
          params: {
            outcome: "completed"
          },
          reason: "Complete session and capture learnings"
        }
      ],
      refactor: [
        {
          order: 1,
          tool: "vreko",
          params: {
            mode: "start",
            intent: "refactor"
          },
          reason: "Load architecture patterns and create restore point"
        },
        {
          order: 2,
          tool: "check",
          params: {
            mode: "architecture"
          },
          reason: "Verify clean boundaries before refactoring"
        },
        {
          order: 3,
          tool: "advise",
          params: {
            intent: "refactor"
          },
          reason: "Get refactoring recommendations"
        },
        {
          order: 4,
          tool: "[AGENT]",
          reason: "Perform the refactoring",
          isAgentAction: true
        },
        {
          order: 5,
          tool: "check",
          params: {
            mode: "full"
          },
          reason: "Full validation including circular dependencies"
        },
        {
          order: 6,
          tool: "snap_end",
          params: {
            outcome: "completed",
            ceremony: "standard"
          },
          reason: "Close with ceremony validation for confidence"
        }
      ],
      review: [
        {
          order: 1,
          tool: "vreko",
          params: {
            mode: "start",
            intent: "review"
          },
          reason: "Load context for review"
        },
        {
          order: 2,
          tool: "pulse",
          reason: "Check workspace health"
        },
        {
          order: 3,
          tool: "check",
          params: {
            mode: "security"
          },
          reason: "Security scan of codebase"
        },
        {
          order: 4,
          tool: "check",
          params: {
            mode: "architecture"
          },
          reason: "Architecture validation"
        },
        {
          order: 5,
          tool: "[AGENT]",
          reason: "Review findings and document issues",
          isAgentAction: true
        },
        {
          order: 6,
          tool: "snap_end",
          params: {
            outcome: "completed"
          },
          reason: "Complete review session"
        }
      ],
      explore: [
        {
          order: 1,
          tool: "vreko",
          params: {
            mode: "context"
          },
          reason: "Get context without creating snapshot (lightweight)"
        },
        {
          order: 2,
          tool: "[AGENT]",
          reason: "Explore the codebase",
          isAgentAction: true
        },
        {
          order: 3,
          tool: "snap_learn",
          params: {
            type: "discovery"
          },
          reason: "Capture any discoveries for future sessions",
          conditional: "If you learned something useful"
        }
      ]
    };
    __name8(applyRiskModifications, "applyRiskModifications");
    __name8(formatWireWorkflow, "formatWireWorkflow");
    __name8(formatHumanWorkflow, "formatHumanWorkflow");
    handleGuide = /* @__PURE__ */ __name8(async (args, context) => {
      const validation = validateGuideParams(args);
      if (!validation.success) {
        return {
          content: [
            {
              type: "text",
              text: `\u274C Input validation failed:
${validation.errors?.join("\n") ?? "Unknown validation error"}`
            }
          ],
          isError: true
        };
      }
      const params = validation.data;
      const { intent, task, files, compact = false } = params;
      if (!intent || !BASE_TEMPLATES[intent]) {
        return {
          content: [
            {
              type: "text",
              text: `Invalid intent: ${intent}. Must be one of: debug, implement, refactor, review, explore`
            }
          ],
          isError: true
        };
      }
      let risk = "L";
      let dirtyFiles = 0;
      let hasActiveSession = false;
      if (context.workspaceRoot) {
        try {
          const sessionState = await getSessionStateAsync(context.workspaceRoot);
          if (sessionState) {
            hasActiveSession = !!sessionState.currentTask;
            dirtyFiles = sessionState.changesSinceTaskStart?.length || 0;
            const hasRiskAreas = sessionState.riskAreasTouched?.some((area) => [
              "auth",
              "payment",
              "security",
              "database"
            ].includes(area));
            const hasViolations = (sessionState.riskAreasTouched?.length ?? 0) > 0;
            if (hasRiskAreas || hasViolations) {
              risk = "H";
            } else if (dirtyFiles > 5 || files && files.length > 5) {
              risk = "M";
            }
          }
        } catch {
        }
      }
      if (files?.some((f) => /auth|security|password|token|secret|payment/i.test(f))) {
        risk = "H";
      }
      const baseSteps = JSON.parse(JSON.stringify(BASE_TEMPLATES[intent]));
      const modificationContext = {
        risk,
        files
      };
      const { steps: modifiedSteps, modifications } = applyRiskModifications(baseSteps, modificationContext);
      const adviseStep = modifiedSteps.find((s) => s.tool === "advise");
      if (adviseStep && task) {
        adviseStep.params = {
          ...adviseStep.params,
          task
        };
      }
      const response = {
        intent,
        plan: modifiedSteps,
        explanation: getExplanation(intent, modifications.length),
        modifications,
        risk
      };
      const wire = formatWireWorkflow(response, compact);
      const human = formatHumanWorkflow(response);
      const brandedMessage = `\u{1F98E} Vreko: Here's your ${intent} workflow plan.`;
      const structuredContent = {
        intent,
        risk,
        stepCount: modifiedSteps.length,
        modifications: modifications.length,
        plan: modifiedSteps,
        hasActiveSession
      };
      return {
        content: [
          {
            type: "text",
            text: `${wire}${INTERNAL_SEPARATOR}${brandedMessage}${human}`
          }
        ],
        structuredContent
      };
    }, "handleGuide");
    __name8(getExplanation, "getExplanation");
  }
});
function getWorkspaceRoot2(context) {
  if (!context.workspaceRoot) {
    throw new Error("No workspace root available. The MCP server must be initialized with a workspace root. Ensure the client provides the workspace path during initialization.");
  }
  return context.workspaceRoot;
}
__name(getWorkspaceRoot2, "getWorkspaceRoot2");
function detectLanguage(filePath) {
  const ext = extname(filePath).toLowerCase().slice(1);
  const langMap = {
    ts: "typescript",
    tsx: "typescript",
    js: "javascript",
    jsx: "javascript",
    mjs: "javascript",
    cjs: "javascript",
    py: "python",
    go: "go",
    rs: "rust",
    java: "java",
    rb: "ruby",
    php: "php",
    cs: "csharp",
    cpp: "cpp",
    c: "c",
    swift: "swift",
    kt: "kotlin"
  };
  return langMap[ext] ?? "unknown";
}
__name(detectLanguage, "detectLanguage");
function readFileContent(filePath, workspaceRoot) {
  const fullPath = filePath.startsWith("/") ? filePath : join(workspaceRoot, filePath);
  try {
    if (existsSync(fullPath)) {
      return readFileSync(fullPath, "utf-8");
    }
    return null;
  } catch {
    return null;
  }
}
__name(readFileContent, "readFileContent");
function enrichedToAggregated(filePath, enriched) {
  if (!enriched || !enriched.contexts.has(filePath)) {
    return {
      file: filePath,
      gatheredAt: /* @__PURE__ */ (/* @__PURE__ */ new Date()).toISOString(),
      durationMs: 0,
      sources: {},
      riskFactors: []
    };
  }
  const fileContext = enriched.contexts.get(filePath);
  return {
    file: filePath,
    gatheredAt: fileContext.gatheredAt,
    durationMs: fileContext.durationMs,
    sources: fileContext.sources,
    riskFactors: fileContext.riskFactors
  };
}
__name(enrichedToAggregated, "enrichedToAggregated");
function getTimeRangeMs(timeRange) {
  const day = 24 * 60 * 60 * 1e3;
  switch (timeRange) {
    case "7d":
      return 7 * day;
    case "30d":
      return 30 * day;
    case "90d":
      return 90 * day;
  }
}
__name(getTimeRangeMs, "getTimeRangeMs");
async function handleValidateChange(args, context) {
  const validation = validateValidateChangeParams(args);
  if (!validation.success) {
    return {
      content: [
        {
          type: "text",
          text: `\u274C Input validation failed:
${validation.errors?.join("\n") ?? "Unknown validation error"}`
        }
      ],
      isError: true
    };
  }
  const input = validation.data;
  const workspaceRoot = getWorkspaceRoot2(context);
  const language = input.language ?? detectLanguage(input.filePath);
  const integrations = context.services?.integrations ?? getIntegrationOrchestratorService();
  const enriched = await integrations.enrichContext({
    files: [
      {
        path: input.filePath,
        content: input.content,
        language
      }
    ],
    intent: "implement",
    skipCache: false
  });
  const aggregatedContext = enrichedToAggregated(input.filePath, enriched);
  const result7 = await assessRiskViaDaemon(workspaceRoot, input.filePath, input.content, input.previousContent, aggregatedContext);
  if (!result7.success || !result7.assessment) {
    return {
      content: [
        {
          type: "text",
          text: `\u{1F50D}|V|error:${result7.error}|path:${input.filePath}`
        }
      ],
      isError: true
    };
  }
  const assessment = result7.assessment;
  await indexPatternsViaDaemon(workspaceRoot, assessment.factors);
  const wire = [
    "\u{1F50D}",
    "V",
    `score:${assessment.score}`,
    `level:${assessment.level}`,
    `rec:${assessment.recommendation}`,
    `conf:${assessment.confidence}`,
    `factors:${assessment.factors.length}`
  ].join("|");
  const summary = formatRiskAssessmentViaDaemon(assessment);
  const structuredContent = {
    score: assessment.score,
    level: assessment.level,
    recommendation: assessment.recommendation,
    confidence: assessment.confidence,
    factors: (assessment.factors || []).map((f) => ({
      name: f.name,
      score: f.score,
      source: f.source,
      description: f.description,
      suggestion: f.suggestion
    })),
    explanation: assessment.explanation,
    sources: enriched?.sources ?? []
  };
  return {
    content: [
      {
        type: "text",
        text: `${wire}
---
${summary}`
      }
    ],
    structuredContent
  };
}
__name(handleValidateChange, "handleValidateChange");
async function handleGetRiskScore(args, context) {
  const validation = validateGetRiskScoreParams(args);
  if (!validation.success) {
    return {
      content: [
        {
          type: "text",
          text: `\u274C Input validation failed:
${validation.errors?.join("\n") ?? "Unknown validation error"}`
        }
      ],
      isError: true
    };
  }
  const input = validation.data;
  const workspaceRoot = getWorkspaceRoot2(context);
  const content = input.content ?? readFileContent(input.filePath, workspaceRoot);
  if (!content) {
    return {
      content: [
        {
          type: "text",
          text: `\u{1F4CA}|R|error:file_not_found|path:${input.filePath}`
        }
      ],
      isError: true
    };
  }
  const language = detectLanguage(input.filePath);
  const integrations = context.services?.integrations ?? getIntegrationOrchestratorService();
  const enriched = await integrations.enrichContext({
    files: [
      {
        path: input.filePath,
        content,
        language
      }
    ],
    intent: "review",
    skipCache: false
  });
  const aggregatedContext = enrichedToAggregated(input.filePath, enriched);
  const quickResult = await quickAssessRiskViaDaemon(workspaceRoot, aggregatedContext.riskFactors || []);
  if (!quickResult.success || !quickResult.assessment) {
    return {
      content: [
        {
          type: "text",
          text: `\u{1F4CA}|R|error:${quickResult.error}|path:${input.filePath}`
        }
      ],
      isError: true
    };
  }
  const quickAssessment = quickResult.assessment;
  const wire = [
    "\u{1F4CA}",
    "R",
    `score:${quickAssessment.score}`,
    `level:${quickAssessment.level}`,
    `rec:${quickAssessment.recommendation}`
  ].join("|");
  let factorSummary = "";
  if ((aggregatedContext.riskFactors || []).length > 0) {
    factorSummary = "\n\nRisk Factors:\n";
    for (const factor of (aggregatedContext.riskFactors || []).slice(0, 5)) {
      const sign = factor.score >= 0 ? "+" : "";
      factorSummary += `  \u2022 [${factor.source}] ${factor.description} (${sign}${factor.score})
`;
    }
  }
  const summary = `Score: ${quickAssessment.score}/100 (${quickAssessment.level})
Recommendation: ${quickAssessment.recommendation}
Sources: ${enriched?.sources.join(", ") ?? "local only"}${factorSummary}`;
  const structuredContent = {
    score: quickAssessment.score,
    level: quickAssessment.level,
    recommendation: quickAssessment.recommendation,
    factors: (aggregatedContext.riskFactors || []).map((f) => ({
      name: f.name,
      score: f.score,
      source: f.source,
      description: f.description
    })),
    sources: enriched?.sources ?? []
  };
  return {
    content: [
      {
        type: "text",
        text: `${wire}
---
${summary}`
      }
    ],
    structuredContent
  };
}
__name(handleGetRiskScore, "handleGetRiskScore");
async function handleQueryPatterns(args, context) {
  const validation = validateQueryPatternsParams(args);
  if (!validation.success) {
    return {
      content: [
        {
          type: "text",
          text: `\u274C Input validation failed:
${validation.errors?.join("\n") ?? "Unknown validation error"}`
        }
      ],
      isError: true
    };
  }
  const input = validation.data;
  const workspaceRoot = getWorkspaceRoot2(context);
  const timeRange = input.timeRange ?? "30d";
  const limit = input.limit ?? 10;
  const query = {
    search: input.query,
    filePath: input.filePath,
    limit,
    sortBy: "relevance",
    sortOrder: "desc"
  };
  const timeRangeMs = getTimeRangeMs(timeRange);
  query.dateFrom = Date.now() - timeRangeMs;
  const queryResult = await queryPatternsViaDaemon(workspaceRoot, query);
  if (!queryResult.success || !queryResult.results) {
    return {
      content: [
        {
          type: "text",
          text: `\u{1F50E}|P|error:${queryResult.error}|query:${input.query}`
        }
      ],
      isError: true
    };
  }
  const results = queryResult.results;
  const recResult = await recommendPatternsViaDaemon(workspaceRoot, {
    filePath: input.filePath,
    keywords: input.query.toLowerCase().split(/\s+/).filter((w) => w.length > 3)
  });
  const recommendations = recResult.success ? recResult.recommendations ?? [] : [];
  const violationStats = queryResult.violationStats ?? {
    total: 0,
    topViolations: []
  };
  const topResult = results[0];
  const wire = [
    "\u{1F50E}",
    "P",
    `results:${results.length}`,
    topResult ? `top:${topResult.pattern.name}` : "top:none",
    topResult ? `score:${topResult.pattern.score}` : "score:0"
  ].join("|");
  let summary = `Found ${results.length} pattern(s) matching "${input.query}"
`;
  summary += `Time range: ${timeRange}

`;
  if (results.length > 0) {
    summary += "**Matching Patterns:**\n";
    for (const result7 of results.slice(0, 5)) {
      summary += `  \u2022 [${result7.pattern.source}] ${result7.pattern.name}: ${result7.pattern.description} (score: ${result7.pattern.score}, seen: ${result7.usageCount}x)
`;
    }
  }
  if (recommendations.length > 0) {
    summary += "\n**Recommendations:**\n";
    for (const rec of recommendations.slice(0, 3)) {
      summary += `  \u2022 ${rec.pattern.name}: ${rec.reason} (confidence: ${Math.round(rec.confidence * 100)}%)
`;
    }
  }
  if (violationStats.total > 0) {
    summary += `
**Violation Stats:** ${violationStats.total} total violations tracked
`;
  }
  const structuredContent = {
    results: results.map((r) => ({
      pattern: {
        name: r.pattern.name,
        score: r.pattern.score,
        source: r.pattern.source,
        description: r.pattern.description
      },
      relevance: r.relevance,
      usageCount: r.usageCount
    })),
    recommendations: recommendations.map((r) => ({
      pattern: r.pattern.name,
      reason: r.reason,
      confidence: r.confidence
    })),
    violationStats: {
      total: violationStats.total,
      topViolations: violationStats.topViolations.slice(0, 5)
    }
  };
  return {
    content: [
      {
        type: "text",
        text: `${wire}
---
${summary}`
      }
    ],
    structuredContent
  };
}
__name(handleQueryPatterns, "handleQueryPatterns");
async function handleGetContext2(args, context) {
  const validation = validateGetContextParams(args);
  if (!validation.success) {
    return {
      content: [
        {
          type: "text",
          text: `\u274C Input validation failed:
${validation.errors?.join("\n") ?? "Unknown validation error"}`
        }
      ],
      isError: true
    };
  }
  const input = validation.data;
  const workspaceRoot = getWorkspaceRoot2(context);
  const content = readFileContent(input.filePath, workspaceRoot);
  const language = detectLanguage(input.filePath);
  const integrations = context.services?.integrations ?? getIntegrationOrchestratorService();
  const health = integrations.getHealth();
  const startTime = Date.now();
  const enriched = await integrations.enrichContext({
    files: content ? [
      {
        path: input.filePath,
        content,
        language
      }
    ] : [
      input.filePath
    ],
    intent: "explore",
    skipCache: false
  });
  const duration = Date.now() - startTime;
  let filteredSources = enriched?.sources ?? [];
  if (input.sources && input.sources.length > 0) {
    filteredSources = filteredSources.filter((s) => input.sources?.includes(s));
  }
  const wire = [
    "\u{1F4CB}",
    "C",
    `sources:${filteredSources.length > 0 ? filteredSources.join(",") : "none"}`,
    `factors:${enriched?.riskFactors.length ?? 0}`,
    `duration:${duration}ms`
  ].join("|");
  let summary = `Context for: ${input.filePath}
`;
  summary += `Duration: ${duration}ms
`;
  summary += `Health: ${health.status}

`;
  summary += "**Integration Status:**\n";
  for (const [name, status] of Object.entries(health.integrations)) {
    const icon = status.connected && !status.circuitOpen ? "\u2713" : status.enabled ? "\u25CB" : "\u2717";
    summary += `  ${icon} ${name}: ${status.connected ? "connected" : status.enabled ? "not connected" : "disabled"}`;
    if (status.circuitOpen) {
      summary += " (circuit open)";
    }
    summary += "\n";
  }
  if (enriched) {
    const fileContext = enriched.contexts.get(input.filePath);
    if (fileContext) {
      summary += "\n**Sources Contributing:**\n";
      if (fileContext.sources.github) {
        const gh = fileContext.sources.github;
        summary += `  \u2022 GitHub: ${gh.recentCommits?.length ?? 0} commits, ${gh.changeFrequency ?? "unknown"} frequency
`;
      }
      if (fileContext.sources.context7) {
        const c7 = fileContext.sources.context7;
        summary += `  \u2022 Context7: ${c7.imports?.length ?? 0} imports validated
`;
      }
      if (fileContext.sources.sentry) {
        const sn = fileContext.sources.sentry;
        summary += `  \u2022 Sentry: ${sn.errors?.length ?? 0} errors, trend: ${sn.errorTrend ?? "unknown"}
`;
      }
    }
    if ((enriched.riskFactors || []).length > 0) {
      summary += "\n**Risk Factors:**\n";
      for (const factor of (enriched.riskFactors || []).slice(0, 5)) {
        summary += `  \u2022 [${factor.source}] ${factor.description}
`;
      }
    }
  }
  const structuredContent = {
    file: input.filePath,
    duration,
    health,
    sources: filteredSources,
    riskScore: enriched?.riskScore ?? 50,
    riskFactors: enriched?.riskFactors ?? [],
    contexts: enriched ? Object.fromEntries(enriched.contexts) : {}
  };
  return {
    content: [
      {
        type: "text",
        text: `${wire}
---
${summary}`
      }
    ],
    structuredContent
  };
}
__name(handleGetContext2, "handleGetContext2");
async function handleSuggestRollback(args, context) {
  const validation = validateSuggestRollbackParams(args);
  if (!validation.success) {
    return {
      content: [
        {
          type: "text",
          text: `\u274C Input validation failed:
${validation.errors?.join("\n") ?? "Unknown validation error"}`
        }
      ],
      isError: true
    };
  }
  const input = validation.data;
  const workspaceRoot = getWorkspaceRoot2(context);
  const result7 = await suggestRollbackViaDaemon(workspaceRoot, input.filePath, input.symptom);
  if (!result7.success || !result7.suggestions || result7.suggestions.length === 0) {
    return {
      content: [
        {
          type: "text",
          text: result7.error ? `\u23EA|RB|error:${result7.error}` : `\u23EA|RB|error:no_suggestions

---
Could not generate rollback suggestions for ${input.filePath}

Tip: Snapshots are created automatically when files are modified during tasks.`
        }
      ],
      isError: !result7.success
    };
  }
  const suggestions = result7.suggestions;
  const best = suggestions[0];
  const ageHours = Math.round((Date.now() - best.createdAt.getTime()) / (1e3 * 60 * 60));
  const ageStr = ageHours < 1 ? "<1h" : `${ageHours}h`;
  const wire = [
    "\u23EA",
    "RB",
    `best:${best.snapshotId.slice(0, 12)}`,
    `score:${best.score}`,
    `age:${ageStr}`,
    `alternatives:${suggestions.length - 1}`
  ].join("|");
  let summary = `Rollback Suggestions for: ${input.filePath}
`;
  if (input.symptom) {
    summary += `Symptom: "${input.symptom}"
`;
  }
  summary += "\n**Recommended:**\n";
  summary += `  \u{1F3AF} ${best.snapshotId}
`;
  summary += `     Score: ${best.score}/100
`;
  summary += `     ${best.reasoning}
`;
  if (suggestions.length > 1) {
    summary += "\n**Alternatives:**\n";
    for (const alt of suggestions.slice(1, 4)) {
      summary += `  \u2022 ${alt.snapshotId} (score: ${alt.score})
`;
      summary += `    ${alt.reasoning}
`;
    }
  }
  summary += `
To restore: Use snap_fix({ id: "${best.snapshotId}" })`;
  const structuredContent = {
    recommended: {
      snapshotId: best.snapshotId,
      score: best.score,
      createdAt: best.createdAt.toISOString(),
      reasoning: best.reasoning
    },
    alternatives: suggestions.slice(1).map((s) => ({
      snapshotId: s.snapshotId,
      score: s.score,
      createdAt: s.createdAt.toISOString(),
      reasoning: s.reasoning
    })),
    symptom: input.symptom
  };
  return {
    content: [
      {
        type: "text",
        text: `${wire}
---
${summary}`
      }
    ],
    structuredContent
  };
}
__name(handleSuggestRollback, "handleSuggestRollback");
var validateChangeTool;
var getRiskScoreTool;
var queryPatternsTool;
var getContextTool;
var suggestRollbackTool;
var INTELLIGENCE_TOOLS;
var init_intelligence2 = __esm2({
  "src/tools/consolidated/intelligence.ts"() {
    init_intelligence_adapter();
    init_input_sanitization();
    init_integration_orchestrator_service();
    __name8(getWorkspaceRoot2, "getWorkspaceRoot");
    __name8(detectLanguage, "detectLanguage");
    __name8(readFileContent, "readFileContent");
    __name8(enrichedToAggregated, "enrichedToAggregated");
    __name8(getTimeRangeMs, "getTimeRangeMs");
    validateChangeTool = {
      name: "vreko_validate_change",
      description: `[DEPRECATED  -  use vreko_begin/vreko_learn/vreko_end/vreko_pulse] \u{1F50D} Validate a proposed code change against historical context, documentation, and production error data.

**When to use:**
- Before applying a significant code change
- When modifying critical files (auth, payments, config)
- To get risk assessment with specific factors

**Wire Response:**
\u{1F50D}|V|score:45|level:medium|rec:warn|conf:75|factors:3

**Parameters:**
- filePath: Path to the file being changed (required)
- content: New file content (required)
- previousContent: Previous file content for diff analysis
- language: Programming language (auto-detected if not provided)

**Returns:** Risk assessment with score, level, factors, and recommendation`,
      inputSchema: {
        type: "object",
        properties: {
          filePath: {
            type: "string",
            description: "Path to the file being changed"
          },
          content: {
            type: "string",
            description: "New file content"
          },
          previousContent: {
            type: "string",
            description: "Previous file content for diff analysis"
          },
          language: {
            type: "string",
            description: "Programming language (auto-detected if not provided)"
          }
        },
        required: [
          "filePath",
          "content"
        ]
      },
      annotations: {
        title: "\u{1F50D} Validate Change",
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true
      }
    };
    __name8(handleValidateChange, "handleValidateChange");
    getRiskScoreTool = {
      name: "vreko_get_risk_score",
      description: `[DEPRECATED] \u{1F4CA} Get the current risk score for a file based on all available context.

**When to use:**
- Quick risk check before modifying a file
- To understand a file's current health status
- Before deciding whether to proceed with changes

**Wire Response:**
\u{1F4CA}|R|score:35|level:medium|rec:warn

**Parameters:**
- filePath: Path to the file (required)
- content: File content (reads from disk if not provided)

**Returns:** Score 0-100 with breakdown of contributing factors`,
      inputSchema: {
        type: "object",
        properties: {
          filePath: {
            type: "string",
            description: "Path to the file"
          },
          content: {
            type: "string",
            description: "File content (reads from disk if not provided)"
          }
        },
        required: [
          "filePath"
        ]
      },
      annotations: {
        title: "\u{1F4CA} Get Risk Score",
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true
      }
    };
    __name8(handleGetRiskScore, "handleGetRiskScore");
    queryPatternsTool = {
      name: "vreko_query_patterns",
      description: `[DEPRECATED] \u{1F50E} Query behavioral patterns from Vreko's learning system.

**When to use:**
- To find common mistakes in a file or area
- To understand what patterns lead to rollbacks
- To get recommendations based on historical data

**Wire Response:**
\u{1F50E}|P|results:5|top:high_churn_file|score:15

**Parameters:**
- query: Natural language query about patterns (required)
- filePath: Scope query to specific file
- timeRange: Time range for analysis (7d, 30d, 90d)
- limit: Maximum results to return (default: 10)

**Returns:** Matching patterns with relevance scores`,
      inputSchema: {
        type: "object",
        properties: {
          query: {
            type: "string",
            description: "Natural language query about code patterns"
          },
          filePath: {
            type: "string",
            description: "Scope query to specific file"
          },
          timeRange: {
            type: "string",
            enum: [
              "7d",
              "30d",
              "90d"
            ],
            description: "Time range for pattern analysis"
          },
          limit: {
            type: "number",
            description: "Maximum results to return (default: 10)"
          }
        },
        required: [
          "query"
        ]
      },
      annotations: {
        title: "\u{1F50E} Query Patterns",
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true
      }
    };
    __name8(handleQueryPatterns, "handleQueryPatterns");
    getContextTool = {
      name: "vreko_get_context",
      description: `[DEPRECATED] \u{1F4CB} Get aggregated context for a file from all available sources.

**When to use:**
- To understand full context before making changes
- To check what external data is available for a file
- To see GitHub history, docs validation, and error data

**Wire Response:**
\u{1F4CB}|C|sources:github,sentry|factors:4|duration:234ms

**Parameters:**
- filePath: Path to the file (required)
- sources: Which sources to include (github, context7, sentry)

**Returns:** Context from GitHub, Context7, and Sentry with risk factors`,
      inputSchema: {
        type: "object",
        properties: {
          filePath: {
            type: "string",
            description: "Path to the file"
          },
          sources: {
            type: "array",
            items: {
              type: "string",
              enum: [
                "github",
                "context7",
                "sentry"
              ]
            },
            description: "Which context sources to include"
          }
        },
        required: [
          "filePath"
        ]
      },
      annotations: {
        title: "\u{1F4CB} Get Context",
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true
      }
    };
    __name8(handleGetContext2, "handleGetContext");
    suggestRollbackTool = {
      name: "vreko_suggest_rollback",
      description: `[DEPRECATED] \u23EA Suggest the best snapshot to rollback to based on current issues.

**When to use:**
- When something broke and you need to restore
- To analyze which snapshot is the best restore point
- When you have symptoms but don't know what changed

**Wire Response:**
\u23EA|RB|best:snap_abc123|score:85|age:2h|alternatives:2

**Parameters:**
- filePath: Path to the file with issues (required)
- symptom: Description of the problem (helps with matching)

**Returns:** Ranked rollback suggestions with reasoning`,
      inputSchema: {
        type: "object",
        properties: {
          filePath: {
            type: "string",
            description: "Path to the file"
          },
          symptom: {
            type: "string",
            description: "Description of the problem"
          }
        },
        required: [
          "filePath"
        ]
      },
      annotations: {
        title: "\u23EA Suggest Rollback",
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true
      }
    };
    __name8(handleSuggestRollback, "handleSuggestRollback");
    INTELLIGENCE_TOOLS = [
      validateChangeTool,
      getRiskScoreTool,
      queryPatternsTool,
      getContextTool,
      suggestRollbackTool
    ];
  }
});
async function handleLearningGC(args, context) {
  const { operation = "archive", dry_run = true } = args;
  const result7 = await executeLearningGCViaService(context.workspaceRoot, operation, dry_run);
  if (!result7) {
    return {
      content: [
        {
          type: "text",
          text: "Daemon unavailable - learning GC requires daemon for safe execution"
        }
      ],
      isError: true
    };
  }
  const parts = [];
  if (result7.archive) {
    const { archived, dryRun } = result7.archive;
    const prefix = dryRun ? "[DRY-RUN]" : "";
    parts.push(`${prefix} Archive: ${archived.learnings} learnings, ${archived.violations} violations`);
  }
  if (result7.delete) {
    const { deletedCount, dryRun } = result7.delete;
    const prefix = dryRun ? "[DRY-RUN]" : "";
    parts.push(`${prefix} Delete: ${deletedCount} archived learnings permanently removed`);
  }
  return {
    content: [
      {
        type: "text",
        text: parts.join(" | ")
      }
    ],
    structuredContent: result7
  };
}
__name(handleLearningGC, "handleLearningGC");
var learningGCTool;
var init_learning_gc = __esm2({
  "src/tools/consolidated/learning-gc.ts"() {
    init_local_service_adapter();
    learningGCTool = {
      name: "learning_gc",
      description: `[DEPRECATED  -  use vreko_begin/vreko_learn/vreko_end/vreko_pulse] \u{1F98E} Vreko Learning GC - Automated learning lifecycle management.

**When to use:** Periodic cleanup of stale learnings, reducing memory footprint and improving context loading speed.

**Two-Phase Decay:**
- **archive**: Mark unused learnings as archived (30d unused, <3 usage)
- **delete**: Permanently remove archived learnings (90d archived)
- **all**: Execute both operations sequentially

**Safety:** Defaults to dry_run=true for preview without execution.

**Decision Guide:**
| Need | Operation | Dry Run |
|------|-----------|----------|
| Preview cleanup | archive | true (default) |
| Execute cleanup | archive | false |
| Remove archived | delete | true/false |
| Full maintenance | all | true/false |

**Examples:**

Preview archive (safe):
  learning_gc({ operation: "archive", dry_run: true })
  \u2192 [DRY-RUN] Archive: 12 learnings, 3 violations

Execute archive:
  learning_gc({ operation: "archive", dry_run: false })
  \u2192 Archive: 12 learnings, 3 violations

Delete archived permanently:
  learning_gc({ operation: "delete", dry_run: false })
  \u2192 Delete: 5 archived learnings permanently removed

Full maintenance cycle:
  learning_gc({ operation: "all", dry_run: false })
  \u2192 Archive: 12 learnings, 3 violations | Delete: 5 archived learnings permanently removed

**Thresholds:**
- Archive: 30 days unused + less than 3 uses
- Delete: 90 days in archived state

**Impact:**
- Faster vreko({ mode: "start" }) context loading
- Reduced memory footprint (.vreko/learnings/)
- Maintains high-quality learning database

**Boundaries:**

Will:
- Archive low-value learnings (preserves for recovery)
- Permanently delete long-archived learnings
- Skip hot tier learnings (3+ uses)
- Require daemon for safe execution

Will Not:
- Delete recently used learnings
- Remove active task learnings
- Execute without daemon availability
- Auto-run without explicit invocation

**Pro Tip:** Run with dry_run=true first to preview impact. Archive is reversible (learnings preserved), delete is permanent.

**Wire Format:** \u{1F98E}|GC|status:OK|archive:12L+3V|delete:5L`,
      inputSchema: {
        type: "object",
        properties: {
          operation: {
            type: "string",
            enum: [
              "archive",
              "delete",
              "all"
            ],
            description: "GC operation: archive (mark stale), delete (permanent removal), or all",
            default: "archive"
          },
          dry_run: {
            type: "boolean",
            description: "Dry run mode - preview changes without executing (default: true)",
            default: true
          }
        }
      },
      annotations: {
        title: "Learning GC",
        readOnlyHint: false,
        destructiveHint: true,
        idempotentHint: true
      },
      tier: "free"
    };
    __name8(handleLearningGC, "handleLearningGC");
  }
});
async function getPatternsFromDaemon(_context, _options) {
  throw new Error("tiered learning not yet implemented  -  see Deferred Backlog D5");
}
__name(getPatternsFromDaemon, "getPatternsFromDaemon");
async function callDaemonIPC(context, method, params) {
  const daemonClient = context.services?.daemon;
  if (!daemonClient) {
    throw new Error("Daemon client not available in context");
  }
  const result7 = await daemonClient.request(method, params);
  return result7;
}
__name(callDaemonIPC, "callDaemonIPC");
async function handleCapturePattern(args, context) {
  const { content, domain, type, severity, relatedTo } = args;
  if (!content || content.length < 10) {
    return {
      content: [
        {
          type: "text",
          text: "\u274C Content too short (minimum 10 characters)"
        }
      ],
      isError: true
    };
  }
  if (content.length > 500) {
    return {
      content: [
        {
          type: "text",
          text: "\u274C Content too long (maximum 500 characters)"
        }
      ],
      isError: true
    };
  }
  try {
    const result7 = await callDaemonIPC(context, "intelligence.capture", {
      workspace: context.workspaceRoot,
      content,
      domain,
      type,
      severity,
      relatedTo
    });
    if (!result7.success) {
      let summary2 = `\u26A0\uFE0F Pattern quality too low: ${result7.quality.toFixed(2)} / 0.40

`;
      summary2 += "**To improve quality, try:**\n";
      if (result7.warnings && result7.warnings.length > 0) {
        for (const warning of result7.warnings) {
          summary2 += `- ${warning}
`;
        }
      }
      summary2 += "\n**Example of good pattern:**\n";
      summary2 += "\u2705 Always validate JWT expiry before attempting refresh\n";
      summary2 += "\u2705 Never use process.env in client code - use import.meta.env instead\n";
      return {
        content: [
          {
            type: "text",
            text: summary2
          }
        ],
        isError: false
      };
    }
    let summary = "\u2705 Pattern captured successfully!\n\n";
    summary += `**Pattern ID:** ${result7.patternId}
`;
    summary += `**Quality Score:** ${result7.quality.toFixed(2)}
`;
    if (result7.warnings && result7.warnings.length > 0) {
      summary += "\n**Notes:**\n";
      for (const warning of result7.warnings) {
        summary += `- ${warning}
`;
      }
    }
    return {
      content: [
        {
          type: "text",
          text: summary
        }
      ]
    };
  } catch (error) {
    return {
      content: [
        {
          type: "text",
          text: `\u274C Failed to capture pattern: ${error instanceof Error ? error.message : String(error)}`
        }
      ],
      isError: true
    };
  }
}
__name(handleCapturePattern, "handleCapturePattern");
async function handleQueryPatterns2(args, context) {
  const { domains, types, minConfidence, limit = 10 } = args;
  try {
    const patterns = await getPatternsFromDaemon(context, {
      domains,
      types,
      minConfidence,
      limit
    });
    if (patterns.length === 0) {
      return {
        content: [
          {
            type: "text",
            text: "\u{1F4ED} No patterns found matching your query.\n\nTry:\n- Broadening filters\n- Lowering minConfidence\n- Checking different domains"
          }
        ]
      };
    }
    let summary = `\u{1F50D} Found ${patterns.length} pattern${patterns.length === 1 ? "" : "s"}

`;
    for (const pattern of patterns) {
      summary += `**${pattern.core.action.toUpperCase()}: ${pattern.core.subject}**
`;
      summary += `- Content: ${pattern.original}
`;
      summary += `- Confidence: ${pattern.insight.confidence.toFixed(2)} (${pattern.evidence.occurrences} occurrences)
`;
      summary += `- Domain: ${pattern.signal.domain} | Action: ${pattern.core.action}
`;
      summary += `- Rule: ${pattern.insight.rule}
`;
      summary += `- ID: ${pattern.id}

`;
    }
    return {
      content: [
        {
          type: "text",
          text: summary
        }
      ]
    };
  } catch (error) {
    return {
      content: [
        {
          type: "text",
          text: `\u274C Failed to query patterns: ${error instanceof Error ? error.message : String(error)}`
        }
      ],
      isError: true
    };
  }
}
__name(handleQueryPatterns2, "handleQueryPatterns2");
async function handleGenerateInsights(args, context) {
  const { type, domains = [], limit = 5 } = args;
  try {
    const patterns = await getPatternsFromDaemon(context, {
      domains,
      limit: 20
    });
    const insights = patterns.map((p, i) => ({
      id: `insight-${i}`,
      title: p.core.subject,
      detail: p.original,
      priority: p.insight.confidence > 0.8 ? 1 : p.insight.confidence > 0.5 ? 2 : 3,
      basedOn: {
        patterns: [
          p.id
        ]
      },
      action: {
        type: "consider",
        target: p.core.action
      }
    })).filter((_, i) => i < (limit || 5));
    if (insights.length === 0) {
      return {
        content: [
          {
            type: "text",
            text: `\u{1F4ED} No ${type} insights found for current context.

This could mean:
- No relevant patterns exist yet
- Current domains don't match captured patterns
- Try capturing more patterns as you learn`
          }
        ]
      };
    }
    let summary = `\u{1F4A1} Generated ${insights.length} ${type} insight${insights.length === 1 ? "" : "s"}

`;
    for (const insight of insights) {
      const priorityLabel = typeof insight.priority === "number" ? `Priority ${insight.priority}` : String(insight.priority).toUpperCase();
      summary += `**${priorityLabel}: ${insight.title}**
`;
      summary += `${insight.detail}

`;
      if (insight.basedOn.patterns && insight.basedOn.patterns.length > 0) {
        summary += "**Based on Patterns:**\n";
        for (const patternId of insight.basedOn.patterns) {
          summary += `- ${patternId}
`;
        }
        summary += "\n";
      }
      if (insight.action) {
        summary += "**Recommended Action:**\n";
        summary += `${insight.action.type}: ${insight.action.target}

`;
      }
    }
    return {
      content: [
        {
          type: "text",
          text: summary
        }
      ]
    };
  } catch (error) {
    return {
      content: [
        {
          type: "text",
          text: `\u274C Failed to generate insights: ${error instanceof Error ? error.message : String(error)}`
        }
      ],
      isError: true
    };
  }
}
__name(handleGenerateInsights, "handleGenerateInsights");
async function handleExplainInsight(args, context) {
  const { insightId } = args;
  try {
    const patterns = await getPatternsFromDaemon(context, {
      limit: 20
    });
    const pattern = patterns.find((p) => p.id === insightId);
    if (!pattern) {
      return {
        content: [
          {
            type: "text",
            text: `\u274C Insight not found: ${insightId}

Make sure you're using the ID from a recently generated insight.`
          }
        ],
        isError: true
      };
    }
    let summary = `\u{1F52C} Insight Explanation: ${pattern.id}

`;
    summary += `**Detail:** ${pattern.original}

`;
    summary += "**Supporting Evidence:**\n";
    summary += `- Pattern type: ${pattern.core.action}
`;
    summary += `  Rule: ${pattern.insight.rule}
`;
    summary += `  Seen: ${pattern.evidence.occurrences} times
`;
    summary += "\n";
    summary += "**Reasoning Factors:**\n";
    summary += `- Domain: ${pattern.signal.domain}
`;
    summary += `- Confidence: ${pattern.insight.confidence.toFixed(2)}
`;
    summary += `- Subject: ${pattern.core.subject}
`;
    return {
      content: [
        {
          type: "text",
          text: summary
        }
      ]
    };
  } catch (error) {
    return {
      content: [
        {
          type: "text",
          text: `\u274C Failed to explain insight: ${error instanceof Error ? error.message : String(error)}`
        }
      ],
      isError: true
    };
  }
}
__name(handleExplainInsight, "handleExplainInsight");
async function handleRecordOutcome(args, context) {
  const { patternId, outcome, notes } = args;
  try {
    const result7 = await callDaemonIPC(context, "intelligence.outcome", {
      workspace: context.workspaceRoot,
      patternId,
      outcome,
      notes
    });
    let summary = `\u2705 Outcome recorded: ${outcome}

`;
    summary += `**Pattern ID:** ${result7.patternId}
`;
    summary += `**Confidence:** ${result7.previousConfidence.toFixed(2)} \u2192 ${result7.confidence.toFixed(2)}
`;
    const change = result7.confidence - result7.previousConfidence;
    if (change > 0) {
      summary += `**Change:** +${change.toFixed(2)} \u{1F4C8} (pattern strengthened)
`;
    } else if (change < 0) {
      summary += `**Change:** ${change.toFixed(2)} \u{1F4C9} (pattern weakened)
`;
    } else {
      summary += "**Change:** No change (ignored)\n";
    }
    return {
      content: [
        {
          type: "text",
          text: summary
        }
      ]
    };
  } catch (error) {
    return {
      content: [
        {
          type: "text",
          text: `\u274C Failed to record outcome: ${error instanceof Error ? error.message : String(error)}`
        }
      ],
      isError: true
    };
  }
}
__name(handleRecordOutcome, "handleRecordOutcome");
var capturePatternTool;
var queryPatternsTool2;
var generateInsightsTool;
var explainInsightTool;
var recordOutcomeTool;
var LEARNING_INTELLIGENCE_TOOLS;
var init_learning_intelligence = __esm2({
  "src/tools/consolidated/learning-intelligence.ts"() {
    __name8(getPatternsFromDaemon, "getPatternsFromDaemon");
    __name8(callDaemonIPC, "callDaemonIPC");
    capturePatternTool = {
      name: "intelligence.capture",
      description: `[DEPRECATED  -  use vreko_begin/vreko_learn/vreko_end/vreko_pulse] \u{1F4DD} Capture a learning as a structured pattern.

**When to use:**
- Discovered something that worked or failed
- Found a better approach
- Learned a constraint or gotcha
- Want to remember a workflow

**Quality requirements:**
- Must include action verb (always, never, avoid, prefer, must)
- Must have clear subject (what the pattern is about)
- 10-500 characters
- If quality < 0.4, you'll get helpful warnings to rephrase

**Architecture:**
- WRITE operation \u2192 calls daemon IPC
- Daemon validates quality gate (0.4 threshold)
- Daemon handles deduplication (same rule ID strengthens existing)
- Returns success + pattern ID or warnings if quality too low

**Example:**
\`\`\`
Always validate JWT expiry before attempting refresh
\`\`\`
\u2192 Extracted as: prefer-jwt-validation with quality 0.85`,
      inputSchema: {
        type: "object",
        properties: {
          content: {
            type: "string",
            description: "The learning content (10-500 chars). Must include action verb and clear subject.",
            minLength: 10,
            maxLength: 500
          },
          domain: {
            type: "string",
            enum: [
              "architecture",
              "testing",
              "documentation",
              "performance",
              "security",
              "workflow",
              "tooling"
            ],
            description: "Domain classification (optional, auto-detected if omitted)"
          },
          type: {
            type: "string",
            enum: [
              "pattern",
              "anti-pattern",
              "best-practice",
              "constraint",
              "heuristic"
            ],
            description: "Pattern type (optional, auto-detected if omitted)"
          },
          severity: {
            type: "string",
            enum: [
              "info",
              "warning",
              "error"
            ],
            description: "Severity level (optional, defaults to 'info')"
          },
          relatedTo: {
            type: "array",
            items: {
              type: "string"
            },
            description: "Related pattern IDs (optional)"
          }
        },
        required: [
          "content"
        ]
      }
    };
    __name8(handleCapturePattern, "handleCapturePattern");
    queryPatternsTool2 = {
      name: "intelligence.patterns",
      description: `[DEPRECATED] \u{1F50D} Query captured patterns by domain, type, or confidence.

**When to use:**
- Find patterns relevant to current task
- Check what learnings exist for a domain
- See high-confidence patterns only
- Review anti-patterns before making changes

**Query options:**
- domains: Filter by domain (architecture, testing, etc.)
- types: Filter by type (pattern, anti-pattern, best-practice)
- actions: Filter by action (avoid, prefer, require, sequence)
- minConfidence: Minimum confidence threshold (0.0-1.0)
- limit: Maximum results (default: 10)
- sortBy: Sort order (confidence, recency, occurrences, relevance)

**Architecture:**
- READ operation \u2192 accesses PatternStore locally
- No daemon IPC needed for reads
- Fast O(k) queries using in-memory indices

**Returns:** List of matching patterns with:
- Pattern ID, content, rule
- Confidence score, occurrences
- Domain, type, action
- Timestamps (created, updated, last seen)`,
      inputSchema: {
        type: "object",
        properties: {
          domains: {
            type: "array",
            items: {
              type: "string",
              enum: [
                "architecture",
                "testing",
                "documentation",
                "performance",
                "security",
                "workflow",
                "tooling"
              ]
            },
            description: "Filter by domains"
          },
          types: {
            type: "array",
            items: {
              type: "string",
              enum: [
                "pattern",
                "anti-pattern",
                "best-practice",
                "constraint",
                "heuristic"
              ]
            },
            description: "Filter by pattern types"
          },
          actions: {
            type: "array",
            items: {
              type: "string",
              enum: [
                "avoid",
                "prefer",
                "require",
                "sequence",
                "investigate"
              ]
            },
            description: "Filter by actions"
          },
          minConfidence: {
            type: "number",
            minimum: 0,
            maximum: 1,
            description: "Minimum confidence threshold (0.0-1.0)"
          },
          limit: {
            type: "number",
            minimum: 1,
            maximum: 50,
            description: "Maximum results (default: 10)"
          },
          sortBy: {
            type: "string",
            enum: [
              "confidence",
              "recency",
              "occurrences",
              "relevance"
            ],
            description: "Sort order (default: relevance)"
          }
        },
        required: []
      }
    };
    __name8(handleQueryPatterns2, "handleQueryPatterns");
    generateInsightsTool = {
      name: "intelligence.insights",
      description: `[DEPRECATED] \u{1F4A1} Generate contextual insights from patterns.

**When to use:**
- Before making changes (get warnings from anti-patterns)
- Planning refactor (get suggestions from best-practices)
- Understanding hotspots (get synthesis of related patterns)
- Assessing regression risk (get predictions based on violations)

**Insight types:**
- warning: From anti-patterns in relevant domains
- suggestion: From best-practices in relevant domains
- synthesis: Hotspot detection (2+ high-confidence patterns in same area)
- prediction: Regression risks based on recent violations

**Architecture:**
- READ operation \u2192 uses RuleBasedInsightEngine locally
- Loads relevant patterns from PatternStore
- Generates insights based on session context
- Returns prioritized insights with reasoning

**Session context:**
- domains: Current work domains
- filePatterns: Files being modified
- recentViolations: Recent violation types
- mode: Task mode (implement, debug, refactor, review)`,
      inputSchema: {
        type: "object",
        properties: {
          type: {
            type: "string",
            enum: [
              "warning",
              "suggestion",
              "synthesis",
              "prediction"
            ],
            description: "Insight type to generate"
          },
          domains: {
            type: "array",
            items: {
              type: "string",
              enum: [
                "architecture",
                "testing",
                "documentation",
                "performance",
                "security",
                "workflow",
                "tooling"
              ]
            },
            description: "Current work domains"
          },
          filePatterns: {
            type: "array",
            items: {
              type: "string"
            },
            description: "Files being modified (glob patterns)"
          },
          recentViolations: {
            type: "array",
            items: {
              type: "string"
            },
            description: "Recent violation types"
          },
          mode: {
            type: "string",
            enum: [
              "implement",
              "debug",
              "refactor",
              "review"
            ],
            description: "Current task mode"
          },
          focus: {
            type: "string",
            description: "Specific focus area (optional)"
          }
        },
        required: [
          "type"
        ]
      }
    };
    __name8(handleGenerateInsights, "handleGenerateInsights");
    explainInsightTool = {
      name: "intelligence.explain",
      description: `[DEPRECATED] \u{1F52C} Explain how an insight was generated.

**When to use:**
- Understanding why a warning was raised
- Seeing the reasoning behind a suggestion
- Learning what patterns contributed to synthesis
- Debugging prediction logic

**Returns:**
- Which patterns contributed
- Confidence levels of each pattern
- Why they're relevant to current context
- Pattern evidence (occurrences, outcomes)

**Architecture:**
- READ operation \u2192 uses cached insight explanations
- Shows pattern provenance
- Helps build trust in recommendations`,
      inputSchema: {
        type: "object",
        properties: {
          insightId: {
            type: "string",
            description: "Insight ID to explain"
          }
        },
        required: [
          "insightId"
        ]
      }
    };
    __name8(handleExplainInsight, "handleExplainInsight");
    recordOutcomeTool = {
      name: "intelligence.outcome",
      description: `[DEPRECATED] \u{1F4CA} Record the outcome of applying a pattern.

**When to use:**
- After following a pattern's advice
- When a pattern helped resolve an issue
- If applying a pattern caused a regression
- To ignore patterns that don't apply

**Outcomes:**
- resolved: Pattern helped (+0.1 confidence, max 0.95)
- regressed: Pattern caused issues (-0.15 confidence, min 0.1)
- ignored: Pattern not applicable (no confidence change)

**Architecture:**
- WRITE operation \u2192 calls daemon IPC
- Daemon updates pattern confidence
- Updates pattern evidence with outcome
- Tracks outcome history for predictions

**Effect on confidence:**
- 3+ resolved outcomes \u2192 pattern promoted to high confidence
- 2+ regressed outcomes \u2192 pattern confidence drops significantly
- Feedback loop improves pattern quality over time`,
      inputSchema: {
        type: "object",
        properties: {
          patternId: {
            type: "string",
            description: "Pattern ID to record outcome for"
          },
          outcome: {
            type: "string",
            enum: [
              "resolved",
              "regressed",
              "ignored"
            ],
            description: "Outcome of applying the pattern"
          },
          notes: {
            type: "string",
            description: "Additional notes about the outcome (optional)"
          }
        },
        required: [
          "patternId",
          "outcome"
        ]
      }
    };
    __name8(handleRecordOutcome, "handleRecordOutcome");
    LEARNING_INTELLIGENCE_TOOLS = [
      capturePatternTool,
      queryPatternsTool2,
      generateInsightsTool,
      explainInsightTool,
      recordOutcomeTool
    ];
  }
});
var pulseTool;
var init_pulse = __esm2({
  "src/tools/consolidated/pulse.ts"() {
    pulseTool = {
      name: "pulse",
      description: `[DEPRECATED] Use vreko_pulse instead.

Quick workspace health check: changes/min, pressure, risk level.
Use for mid-session health monitoring when you need vitals without starting a new task.
Do NOT use at task start  -  vreko(start) already includes pulse data.

This tool now redirects to vreko_pulse which reads from daemon intelligence.`,
      inputSchema: {
        type: "object",
        properties: {
          record_change: {
            type: "string",
            description: "Ignored (deprecated)  -  daemon tracks changes automatically"
          }
        },
        required: []
      },
      annotations: {
        title: "\u{1FAC0} Vreko Pulse (deprecated)",
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true
      },
      deprecated: true
    };
  }
});
async function handleRefactorAssess(args, context) {
  const { workspace, limit = 10, minPriority = "medium" } = args;
  try {
    if (context.services?.daemon?.isAvailable()) {
      const result7 = await context.services.daemon.request("refactoring.getTopRecommendations", {
        workspace,
        limit,
        minPriority
      });
      if (result7) {
        return formatAssessResult(result7.recommendations);
      }
    }
    return formatAssessResult([]);
  } catch (error) {
    return {
      content: [
        {
          type: "text",
          text: `Refactoring assessment failed: ${error instanceof Error ? error.message : String(error)}`
        }
      ],
      isError: true
    };
  }
}
__name(handleRefactorAssess, "handleRefactorAssess");
function formatAssessResult(recommendations) {
  if (recommendations.length === 0) {
    return {
      content: [
        {
          type: "text",
          text: "No refactoring recommendations found. The codebase appears healthy."
        }
      ]
    };
  }
  const lines = [
    "# Refactoring Assessment Results",
    "",
    `Found ${recommendations.length} recommendation(s):`,
    ""
  ];
  for (const rec of recommendations) {
    const priorityEmoji = rec.priority === "critical" ? "\u{1F534}" : rec.priority === "high" ? "\u{1F7E0}" : rec.priority === "medium" ? "\u{1F7E1}" : "\u{1F7E2}";
    const actionEmoji = rec.action === "refactor-now" ? "\u26A1" : rec.action === "schedule" ? "\u{1F4C5}" : rec.action === "defer" ? "\u23F3" : "\u{1F440}";
    lines.push(`## ${priorityEmoji} ${rec.name}`);
    lines.push(`- **Action**: ${actionEmoji} ${rec.action}`);
    lines.push(`- **Priority**: ${rec.priority} (score: ${rec.priorityScore})`);
    lines.push(`- **Confidence**: ${Math.round(rec.confidence * 100)}%`);
    lines.push(`- **Rationale**: ${rec.rationale}`);
    lines.push(`- **Next Steps**: ${rec.nextSteps.join("; ")}`);
    lines.push("");
  }
  return {
    content: [
      {
        type: "text",
        text: lines.join("\n")
      }
    ]
  };
}
__name(formatAssessResult, "formatAssessResult");
async function handleRefactorReport(args, context) {
  const { workspace, includeAllCandidates = false } = args;
  try {
    if (context.services?.daemon?.isAvailable()) {
      const result7 = await context.services.daemon.request("refactoring.report", {
        workspace
      });
      if (result7) {
        return formatReportResult(result7, includeAllCandidates);
      }
    }
    return formatReportResult({
      summary: {
        totalComponents: 0,
        recommendationsCount: 0,
        criticalCount: 0,
        highCount: 0,
        mediumCount: 0,
        lowCount: 0,
        avgConfidence: 0
      },
      topRecommendations: [],
      dataQuality: {
        overall: "low",
        missingCollectors: [
          "daemon-unavailable"
        ],
        confidenceExplanation: "Daemon IPC unavailable for assessment"
      }
    }, includeAllCandidates);
  } catch (error) {
    return {
      content: [
        {
          type: "text",
          text: `Refactoring report failed: ${error instanceof Error ? error.message : String(error)}`
        }
      ],
      isError: true
    };
  }
}
__name(handleRefactorReport, "handleRefactorReport");
function formatReportResult(report, _includeAllCandidates) {
  const lines = [
    "# Refactoring Intelligence Report",
    "",
    "## Summary",
    `- **Total Components**: ${report.summary.totalComponents}`,
    `- **Recommendations**: ${report.summary.recommendationsCount}`,
    `  - \u{1F534} Critical: ${report.summary.criticalCount}`,
    `  - \u{1F7E0} High: ${report.summary.highCount}`,
    `  - \u{1F7E1} Medium: ${report.summary.mediumCount}`,
    `  - \u{1F7E2} Low: ${report.summary.lowCount}`,
    `- **Average Confidence**: ${Math.round(report.summary.avgConfidence * 100)}%`,
    "",
    "## Data Quality",
    `- **Overall**: ${report.dataQuality.overall}`,
    `- **Missing Collectors**: ${report.dataQuality.missingCollectors.join(", ") || "none"}`,
    `- **Explanation**: ${report.dataQuality.confidenceExplanation}`,
    ""
  ];
  if (report.topRecommendations.length > 0) {
    lines.push("## Top Recommendations");
    lines.push("");
    for (const rec of report.topRecommendations) {
      const priorityEmoji = rec.priority === "critical" ? "\u{1F534}" : rec.priority === "high" ? "\u{1F7E0}" : rec.priority === "medium" ? "\u{1F7E1}" : "\u{1F7E2}";
      const actionEmoji = rec.action === "refactor-now" ? "\u26A1" : rec.action === "schedule" ? "\u{1F4C5}" : rec.action === "defer" ? "\u23F3" : "\u{1F440}";
      lines.push(`### ${priorityEmoji} ${rec.name}`);
      lines.push(`- **Action**: ${actionEmoji} ${rec.action}`);
      lines.push(`- **Priority**: ${rec.priority} (score: ${rec.priorityScore})`);
      lines.push(`- **Confidence**: ${Math.round(rec.confidence * 100)}%`);
      lines.push(`- **Rationale**: ${rec.rationale}`);
      lines.push(`- **Next Steps**: ${rec.nextSteps.join("; ")}`);
      lines.push("");
    }
  }
  return {
    content: [
      {
        type: "text",
        text: lines.join("\n")
      }
    ]
  };
}
__name(formatReportResult, "formatReportResult");
async function handleRefactorSetGate(args, context) {
  const { workspace, componentId, gate, status } = args;
  try {
    if (context.services?.daemon?.isAvailable()) {
      const result7 = await context.services.daemon.request("refactoring.setGate", {
        workspace,
        componentId,
        gate,
        status
      });
      if (result7) {
        return {
          content: [
            {
              type: "text",
              text: `\u2705 Gate updated for ${componentId}

${result7.message}

Current gates:
${Object.entries(result7.gates).map(([k, v]) => `  - ${k}: ${v}`).join("\n")}`
            }
          ]
        };
      }
    }
    return {
      content: [
        {
          type: "text",
          text: `Gate '${gate}' for component '${componentId}' would be set to '${status}'.

Note: Daemon not available. Gates will be persisted when daemon is connected.`
        }
      ]
    };
  } catch (error) {
    return {
      content: [
        {
          type: "text",
          text: `Set gate failed: ${error instanceof Error ? error.message : String(error)}`
        }
      ],
      isError: true
    };
  }
}
__name(handleRefactorSetGate, "handleRefactorSetGate");
async function handleRefactorStats(args, context) {
  const { workspace } = args;
  try {
    if (context.services?.daemon?.isAvailable()) {
      const result7 = await context.services.daemon.request("refactoring.getStats", {
        workspace
      });
      if (result7) {
        return formatStatsResult(result7);
      }
    }
    return formatStatsResult({
      totalComponents: 0,
      recommendationsCount: 0,
      readyToRefactor: 0,
      scheduled: 0,
      deferred: 0,
      byPriority: {
        critical: 0,
        high: 0,
        medium: 0,
        low: 0
      },
      avgConfidence: 0,
      dataQuality: "unavailable"
    });
  } catch (error) {
    return {
      content: [
        {
          type: "text",
          text: `Get stats failed: ${error instanceof Error ? error.message : String(error)}`
        }
      ],
      isError: true
    };
  }
}
__name(handleRefactorStats, "handleRefactorStats");
function formatStatsResult(stats) {
  const lines = [
    "# Refactoring Statistics",
    "",
    "## Overview",
    `- **Total Components**: ${stats.totalComponents}`,
    `- **Recommendations**: ${stats.recommendationsCount}`,
    `- **Ready to Refactor**: ${stats.readyToRefactor}`,
    `- **Scheduled**: ${stats.scheduled}`,
    `- **Deferred**: ${stats.deferred}`,
    "",
    "## By Priority",
    `- \u{1F534} Critical: ${stats.byPriority.critical}`,
    `- \u{1F7E0} High: ${stats.byPriority.high}`,
    `- \u{1F7E1} Medium: ${stats.byPriority.medium}`,
    `- \u{1F7E2} Low: ${stats.byPriority.low}`,
    "",
    "## Data Quality",
    `- **Average Confidence**: ${Math.round(stats.avgConfidence * 100)}%`,
    `- **Data Quality**: ${stats.dataQuality}`
  ];
  return {
    content: [
      {
        type: "text",
        text: lines.join("\n")
      }
    ]
  };
}
__name(formatStatsResult, "formatStatsResult");
var refactorAssessTool;
var refactorReportTool;
var refactorSetGateTool;
var refactorStatsTool;
var REFACTORING_TOOLS;
var init_refactoring = __esm2({
  "src/tools/consolidated/refactoring.ts"() {
    refactorAssessTool = {
      name: "vreko_refactor_assess",
      description: `[DEPRECATED  -  use vreko_begin/vreko_learn/vreko_end/vreko_pulse] Assess refactoring candidates in the workspace using the Refactoring Intelligence Engine.

Analyzes code components and provides prioritized recommendations based on:
- Value drivers: churn \xD7 complexity, defect blast radius, risk exposure
- Cost factors: complexity, coverage gap, dependents, code age
- Confidence scoring based on available data collectors

Returns ranked candidates with priority scores (valueScore - costScore) and execution readiness.`,
      inputSchema: {
        type: "object",
        properties: {
          workspace: {
            type: "string",
            description: "Workspace path to analyze"
          },
          targetComponentId: {
            type: "string",
            description: "Optional: specific component ID to assess (if omitted, assesses all)"
          },
          limit: {
            type: "number",
            description: "Maximum number of recommendations to return (default: 10)",
            default: 10
          },
          minPriority: {
            type: "string",
            enum: [
              "critical",
              "high",
              "medium",
              "low"
            ],
            description: "Minimum priority level to include (default: medium)",
            default: "medium"
          }
        },
        required: [
          "workspace"
        ]
      },
      annotations: {
        title: "Assess Refactoring Candidates",
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true
      }
    };
    refactorReportTool = {
      name: "vreko_refactor_report",
      description: `[DEPRECATED] Generate a comprehensive refactoring report for the workspace.

Provides:
- Summary statistics (total components, recommendations by priority)
- Top recommendations with full analysis
- Data quality assessment
- Deferral cost analysis

Use this for planning refactoring sprints or presenting to stakeholders.`,
      inputSchema: {
        type: "object",
        properties: {
          workspace: {
            type: "string",
            description: "Workspace path to analyze"
          },
          includeAllCandidates: {
            type: "boolean",
            description: "Include all candidates in report (not just recommendations)",
            default: false
          }
        },
        required: [
          "workspace"
        ]
      },
      annotations: {
        title: "Generate Refactoring Report",
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true
      }
    };
    refactorSetGateTool = {
      name: "vreko_refactor_set_gate",
      description: `[DEPRECATED] Set manual execution gate status for a refactoring candidate.

Manual gates include:
- migrationPlan: Migration plan documented
- rollbackStrategy: Rollback strategy defined
- ownerAssigned: Owner assigned
- stakeholderSignoff: Stakeholder sign-off obtained

All gates must be 'pass' for a component to be readyToExecute.`,
      inputSchema: {
        type: "object",
        properties: {
          workspace: {
            type: "string",
            description: "Workspace path"
          },
          componentId: {
            type: "string",
            description: "Component ID to set gate for"
          },
          gate: {
            type: "string",
            enum: [
              "migrationPlan",
              "rollbackStrategy",
              "ownerAssigned",
              "stakeholderSignoff"
            ],
            description: "Gate to set"
          },
          status: {
            type: "string",
            enum: [
              "pass",
              "fail",
              "warn",
              "pending"
            ],
            description: "Gate status"
          }
        },
        required: [
          "workspace",
          "componentId",
          "gate",
          "status"
        ]
      },
      annotations: {
        title: "Set Refactoring Gate",
        readOnlyHint: false,
        destructiveHint: false,
        idempotentHint: true
      }
    };
    refactorStatsTool = {
      name: "vreko_refactor_stats",
      description: `[DEPRECATED] Get refactoring statistics for the workspace.

Returns:
- Total components analyzed
- Recommendations count by action and priority
- Average confidence score
- Data quality assessment`,
      inputSchema: {
        type: "object",
        properties: {
          workspace: {
            type: "string",
            description: "Workspace path"
          }
        },
        required: [
          "workspace"
        ]
      },
      annotations: {
        title: "Get Refactoring Statistics",
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true
      }
    };
    __name8(handleRefactorAssess, "handleRefactorAssess");
    __name8(formatAssessResult, "formatAssessResult");
    __name8(handleRefactorReport, "handleRefactorReport");
    __name8(formatReportResult, "formatReportResult");
    __name8(handleRefactorSetGate, "handleRefactorSetGate");
    __name8(handleRefactorStats, "handleRefactorStats");
    __name8(formatStatsResult, "formatStatsResult");
    REFACTORING_TOOLS = [
      refactorAssessTool,
      refactorReportTool,
      refactorSetGateTool,
      refactorStatsTool
    ];
  }
});
function normalizeParams(params) {
  return {
    filePath: params.filePath || params.f || "",
    proposedDiff: params.proposedDiff || params.diff || "",
    tool: params.tool || "mcp",
    intent: params.intent,
    compact: params.compact || false
  };
}
__name(normalizeParams, "normalizeParams");
function formatResult(result7, options) {
  const actionEmoji = {
    PROCEED: "\u2705",
    PROCEED_WITH_SNAPSHOT: "\u{1F4F8}",
    WARN: "\u26A0\uFE0F",
    BLOCK: "\u{1F6D1}"
  };
  const wireFields = {
    action: result7.action,
    risk: result7.riskScore.toFixed(2),
    factors: `${result7.factors.length}`
  };
  const wire = formatWireLabeled("STW", wireFields, options);
  const emoji = actionEmoji[result7.action];
  let humanMessage = `${emoji} **${result7.action}** (risk: ${result7.riskScore.toFixed(2)})

`;
  humanMessage += `${result7.recommendation}`;
  if (result7.factors.length > 0) {
    humanMessage += "\n\n**Risk Factors:**";
    for (const factor of result7.factors) {
      const sign = factor.score > 0 ? "+" : "";
      humanMessage += `
- [${factor.source}] ${sign}${factor.score.toFixed(2)}: ${factor.description}`;
    }
  }
  if (result7.alternativeActions.length > 0 && (result7.action === "BLOCK" || result7.action === "WARN")) {
    humanMessage += "\n\n**Suggested Actions:**";
    for (const alt of result7.alternativeActions) {
      humanMessage += `
- ${alt.tool}: ${alt.description}`;
    }
  }
  const structuredContent = {
    allowed: result7.allowed,
    action: result7.action,
    riskScore: result7.riskScore,
    factors: result7.factors,
    recommendation: result7.recommendation,
    retryable: result7.retryable,
    alternativeActions: result7.alternativeActions
  };
  return {
    content: [
      {
        type: "text",
        text: `${wire}${INTERNAL_SEPARATOR}${humanMessage}`
      }
    ],
    structuredContent,
    isError: !result7.allowed
  };
}
__name(formatResult, "formatResult");
var handleSafeToWrite;
var safeToWriteTool;
var init_safe_to_write = __esm2({
  "src/tools/consolidated/safe-to-write.ts"() {
    init_branding();
    init_input_sanitization();
    __name8(normalizeParams, "normalizeParams");
    __name8(formatResult, "formatResult");
    handleSafeToWrite = /* @__PURE__ */ __name8(async (args, context) => {
      const validation = validateSafeToWriteParams(args);
      if (!validation.success) {
        return {
          content: [
            {
              type: "text",
              text: `\u274C Input validation failed:
${validation.errors?.join("\n") ?? "Unknown validation error"}`
            }
          ],
          isError: true
        };
      }
      const params = validation.data;
      const { filePath, proposedDiff, tool, intent, compact } = normalizeParams(params);
      if (!filePath) {
        return {
          content: [
            {
              type: "text",
              text: "\u274C Missing required parameter: filePath"
            }
          ],
          isError: true
        };
      }
      if (!proposedDiff) {
        return {
          content: [
            {
              type: "text",
              text: "\u274C Missing required parameter: proposedDiff"
            }
          ],
          isError: true
        };
      }
      const daemonClient = context.services?.daemon;
      if (!daemonClient) {
        return {
          content: [
            {
              type: "text",
              text: "\u26A0\uFE0F Daemon not available - proceeding without risk evaluation"
            }
          ],
          structuredContent: {
            allowed: true,
            action: "PROCEED",
            riskScore: 1,
            factors: [],
            recommendation: "Daemon unavailable - skipping risk evaluation",
            retryable: true,
            alternativeActions: []
          }
        };
      }
      try {
        const result7 = await daemonClient.request("protection/safe-to-write", {
          workspace: context.workspaceRoot,
          filePath,
          proposedDiff,
          tool,
          intent
        });
        return formatResult(result7, {
          compact
        });
      } catch (error) {
        const message = error instanceof Error ? error.message : "Unknown error";
        return {
          content: [
            {
              type: "text",
              text: `\u274C Safe-to-write evaluation failed: ${message}`
            }
          ],
          isError: true
        };
      }
    }, "handleSafeToWrite");
    safeToWriteTool = {
      name: "safe_to_write",
      description: `[DEPRECATED  -  use vreko_begin/vreko_learn/vreko_end/vreko_pulse] \u{1F6E1}\uFE0F Pre-write safety check using 6 Intelligence Pillars.

**When to use:**
- Before writing/editing files with significant changes
- When making changes to critical files (config, core logic)
- When working in risky git states (main branch, mid-rebase)

**What it checks:**
1. Git state risk (branch, conflicts, uncommitted changes)
2. Rollback pattern matching (similar changes that failed before)
3. Developer fatigue (rapid accept patterns)
4. Historical annotations (user feedback calibration)
5. Context poisoning (AI repeating rejected patterns)

**Actions returned:**
- PROCEED: Safe, auto-snapshot and write
- PROCEED_WITH_SNAPSHOT: Elevated risk, explicit snapshot first
- WARN: High risk, review before proceeding
- BLOCK: Critical risk, manual review required

**Example:**
\`\`\`
safe_to_write({
  filePath: "src/auth/login.ts",
  proposedDiff: "--- a/src/auth/login.ts\\n+++ b/src/auth/login.ts\\n...",
  tool: "claude"
})
\`\`\``,
      inputSchema: {
        type: "object",
        properties: {
          filePath: {
            type: "string",
            description: "File path being written (required)"
          },
          f: {
            type: "string",
            description: "Alias for filePath"
          },
          proposedDiff: {
            type: "string",
            description: "Proposed diff or content change (required)"
          },
          diff: {
            type: "string",
            description: "Alias for proposedDiff"
          },
          tool: {
            type: "string",
            description: "AI tool making the change (default: 'mcp')"
          },
          intent: {
            type: "string",
            description: "Intent of the change (optional context)"
          },
          compact: {
            type: "boolean",
            default: false,
            description: "Use compact wire format"
          }
        },
        required: [
          "filePath",
          "proposedDiff"
        ]
      },
      outputSchema: {
        type: "object",
        properties: {
          allowed: {
            type: "boolean",
            description: "Whether the write is allowed"
          },
          action: {
            type: "string",
            enum: [
              "PROCEED",
              "PROCEED_WITH_SNAPSHOT",
              "WARN",
              "BLOCK"
            ],
            description: "Action to take"
          },
          riskScore: {
            type: "number",
            description: "Composite risk score (1.0 = neutral)"
          },
          factors: {
            type: "array",
            items: {
              type: "object",
              properties: {
                source: {
                  type: "string"
                },
                score: {
                  type: "number"
                },
                description: {
                  type: "string"
                }
              }
            },
            description: "Contributing risk factors"
          },
          recommendation: {
            type: "string",
            description: "Human-readable recommendation"
          },
          retryable: {
            type: "boolean",
            description: "Whether the operation can be retried"
          }
        }
      },
      annotations: {
        title: "\u{1F6E1}\uFE0F Safe-To-Write Check",
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true
      },
      tier: "free"
    };
  }
});
function getCeremonyLevel(ceremony) {
  if (!ceremony) {
    return null;
  }
  if (ceremony === true) {
    return "standard";
  }
  return ceremony;
}
__name(getCeremonyLevel, "getCeremonyLevel");
function getScopesForLevel(level, customScopes) {
  if (customScopes && customScopes.length > 0) {
    return customScopes;
  }
  switch (level) {
    case "quick":
      return [
        "typescript",
        "lint"
      ];
    case "standard":
      return [
        "typescript",
        "lint",
        "tests",
        "architecture"
      ];
    case "comprehensive":
      return [
        "typescript",
        "lint",
        "tests",
        "architecture",
        "security",
        "coverage"
      ];
  }
}
__name(getScopesForLevel, "getScopesForLevel");
async function runValidationStep(scope, context, diffScope = "uncommitted") {
  const start = Date.now();
  const INTERNAL_SEPARATOR2 = "---";
  try {
    const { handleCheck: handleCheck2 } = await Promise.resolve().then(() => (init_check(), check_exports));
    const modeMap = {
      typescript: "quick",
      lint: "quick",
      tests: "full",
      architecture: "architecture",
      security: "security",
      coverage: "coverage"
    };
    const mode = modeMap[scope] || "quick";
    const diff = diffScope === "all" ? void 0 : diffScope;
    const result7 = await handleCheck2({
      mode,
      diff
    }, context);
    const content = result7.content[0]?.text || "";
    const hasSuccessMarker = content.includes("status:\u2713") || content.includes("status:OK");
    const hasFailureMarker = content.includes("status:\u2717");
    const hasWireFormat = content.includes("err:") && content.includes("warn:");
    const passed = hasWireFormat && hasSuccessMarker && !hasFailureMarker;
    const errorMatch = content.match(/err:(\d+)E/);
    const warnMatch = content.match(/warn:(\d+)W/);
    const errors = errorMatch ? Number.parseInt(errorMatch[1], 10) : passed ? 0 : 1;
    const warnings = warnMatch ? Number.parseInt(warnMatch[1], 10) : 0;
    return {
      step: scope,
      passed,
      errors,
      warnings,
      duration: Date.now() - start,
      details: passed ? void 0 : content.split(INTERNAL_SEPARATOR2)[0]
    };
  } catch (error) {
    return {
      step: scope,
      passed: false,
      errors: 1,
      warnings: 0,
      duration: Date.now() - start,
      details: error instanceof Error ? error.message : "Validation failed"
    };
  }
}
__name(runValidationStep, "runValidationStep");
async function runVerifyScript(workspaceRoot, scriptPath) {
  const start = Date.now();
  const script = scriptPath || "./verify.sh";
  try {
    const fullPath = join(workspaceRoot, script);
    if (!existsSync(fullPath) && !scriptPath) {
      const alternatives = [
        "pnpm verify",
        "npm run verify",
        "pnpm test && pnpm lint",
        "pnpm build"
      ];
      for (const alt of alternatives) {
        try {
          await execAsync3(alt, {
            cwd: workspaceRoot,
            timeout: 12e4
          });
          return {
            step: "verify-script",
            passed: true,
            errors: 0,
            warnings: 0,
            duration: Date.now() - start,
            details: `Ran: ${alt}`
          };
        } catch {
        }
      }
      return {
        step: "verify-script",
        passed: true,
        errors: 0,
        warnings: 0,
        duration: Date.now() - start,
        details: "No verify script found, skipped"
      };
    }
    const { stdout, stderr } = await execAsync3(script, {
      cwd: workspaceRoot,
      timeout: 3e5
    });
    return {
      step: "verify-script",
      passed: true,
      errors: 0,
      warnings: stderr ? 1 : 0,
      duration: Date.now() - start,
      details: stdout ? stdout.slice(0, 500) : void 0
    };
  } catch (error) {
    const errMsg = error instanceof Error ? error.message : String(error);
    return {
      step: "verify-script",
      passed: false,
      errors: 1,
      warnings: 0,
      duration: Date.now() - start,
      details: errMsg.slice(0, 500)
    };
  }
}
__name(runVerifyScript, "runVerifyScript");
async function runCeremony(params, context) {
  const level = getCeremonyLevel(params.ceremony) ?? "standard";
  const scopes = getScopesForLevel(level, params.scopes);
  const diffScope = params.diffScope ?? "uncommitted";
  const steps = [];
  const blockers = [];
  const warnings = [];
  const totalStart = Date.now();
  if (scopes.includes("typescript") || scopes.includes("lint")) {
    const quickResult = await runValidationStep("quick", context, diffScope);
    steps.push({
      ...quickResult,
      step: "quick (types+lint)"
    });
    await trackActualToolCallAsync(context.workspaceRoot);
    await recordValidationEventAsync(context.workspaceRoot, {
      success: quickResult.passed,
      details: quickResult.details
    });
    if (!quickResult.passed) {
      blockers.push(`Quick validation failed: ${quickResult.errors} error(s)`);
      if (level === "quick") {
        return {
          passed: false,
          level,
          steps,
          totalDuration: Date.now() - totalStart,
          blockers,
          warnings
        };
      }
    }
  }
  if (scopes.includes("tests")) {
    const testResult = await runValidationStep("tests", context, diffScope);
    steps.push(testResult);
    await trackActualToolCallAsync(context.workspaceRoot);
    await recordValidationEventAsync(context.workspaceRoot, {
      success: testResult.passed,
      details: testResult.details
    });
    if (!testResult.passed) {
      blockers.push(`Tests failed: ${testResult.errors} error(s)`);
    }
  }
  if (scopes.includes("architecture")) {
    const archResult = await runValidationStep("architecture", context, diffScope);
    steps.push(archResult);
    await trackActualToolCallAsync(context.workspaceRoot);
    await recordValidationEventAsync(context.workspaceRoot, {
      success: archResult.passed,
      details: archResult.details
    });
    if (!archResult.passed) {
      blockers.push(`Architecture violations: ${archResult.errors} issue(s)`);
    }
  }
  if (scopes.includes("security")) {
    const secResult = await runValidationStep("security", context, diffScope);
    steps.push(secResult);
    await trackActualToolCallAsync(context.workspaceRoot);
    await recordValidationEventAsync(context.workspaceRoot, {
      success: secResult.passed,
      details: secResult.details
    });
    if (!secResult.passed) {
      blockers.push(`Security issues: ${secResult.errors} finding(s)`);
    }
  }
  if (scopes.includes("coverage")) {
    const covResult = await runValidationStep("coverage", context, diffScope);
    steps.push(covResult);
    await trackActualToolCallAsync(context.workspaceRoot);
    await recordValidationEventAsync(context.workspaceRoot, {
      success: covResult.passed,
      details: covResult.details
    });
    if (!covResult.passed) {
      warnings.push(`Coverage gaps: ${covResult.errors} issue(s)`);
    }
  }
  if (params.verifyScript || level === "comprehensive") {
    const verifyResult = await runVerifyScript(context.workspaceRoot, params.verifyScript);
    steps.push(verifyResult);
    await recordValidationEventAsync(context.workspaceRoot, {
      success: verifyResult.passed,
      details: verifyResult.details
    });
    if (!verifyResult.passed) {
      blockers.push(`Verify script failed: ${verifyResult.details}`);
    }
  }
  return {
    passed: blockers.length === 0,
    level,
    steps,
    totalDuration: Date.now() - totalStart,
    blockers,
    warnings
  };
}
__name(runCeremony, "runCeremony");
function buildProgressBar(passed, total) {
  const filled = "\u2588";
  const empty = "\u2591";
  const barLength = total;
  const bar = filled.repeat(passed) + empty.repeat(Math.max(0, barLength - passed));
  const pct = total > 0 ? Math.round(passed / total * 100) : 0;
  return `[${bar}] ${pct}%`;
}
__name(buildProgressBar, "buildProgressBar");
function formatCeremonyResult(result7) {
  const statusIcon2 = result7.passed ? "\u2705" : "\u274C";
  const levelIcon = result7.level === "comprehensive" ? "\u{1F3AF}" : result7.level === "standard" ? "\u{1F50D}" : "\u26A1";
  let output = `
${levelIcon} CEREMONY [${result7.level.toUpperCase()}] ${statusIcon2}
`;
  for (let i = 0; i < result7.steps.length; i++) {
    const step = result7.steps[i];
    const icon = step.passed ? "\u2705" : "\u274C";
    const timing = step.duration < 1e3 ? `${step.duration}ms` : `${(step.duration / 1e3).toFixed(1)}s`;
    const detail = step.errors > 0 ? ` (${step.errors}E ${step.warnings}W)` : step.warnings > 0 ? ` (${step.warnings}W)` : "";
    output += `  ${icon} ${step.step}${detail} ${timing}
`;
  }
  const totalSteps = result7.steps.length;
  const passedSteps = result7.steps.filter((s) => s.passed).length;
  output += `
  ${buildProgressBar(passedSteps, totalSteps)} ${passedSteps}/${totalSteps} checks passed`;
  if (result7.blockers.length > 0) {
    output += `

\u{1F6AB} BLOCKERS (${result7.blockers.length}):
`;
    for (const blocker of result7.blockers) {
      output += `  - ${blocker}
`;
    }
  }
  if (result7.warnings.length > 0) {
    output += `
\u26A0\uFE0F WARNINGS (${result7.warnings.length}):
`;
    for (const warning of result7.warnings) {
      output += `  - ${warning}
`;
    }
  }
  const totalTime = result7.totalDuration < 1e3 ? `${result7.totalDuration}ms` : `${(result7.totalDuration / 1e3).toFixed(1)}s`;
  output += `
\u23F1\uFE0F Total: ${totalTime}`;
  return output;
}
__name(formatCeremonyResult, "formatCeremonyResult");
function buildCeremonyWireFields(result7) {
  return {
    ceremony: result7.level,
    ceremony_passed: result7.passed ? 1 : 0,
    ceremony_steps: result7.steps.length,
    ceremony_errors: result7.steps.reduce((sum, s) => sum + s.errors, 0),
    ceremony_warnings: result7.steps.reduce((sum, s) => sum + s.warnings, 0),
    ceremony_duration: `${result7.totalDuration}ms`
  };
}
__name(buildCeremonyWireFields, "buildCeremonyWireFields");
var execAsync3;
var init_ceremony_validator = __esm2({
  "src/tools/consolidated/vreko-end/ceremony-validator.ts"() {
    init_session_adapter();
    execAsync3 = promisify(exec);
    __name8(getCeremonyLevel, "getCeremonyLevel");
    __name8(getScopesForLevel, "getScopesForLevel");
    __name8(runValidationStep, "runValidationStep");
    __name8(runVerifyScript, "runVerifyScript");
    __name8(runCeremony, "runCeremony");
    __name8(buildProgressBar, "buildProgressBar");
    __name8(formatCeremonyResult, "formatCeremonyResult");
    __name8(buildCeremonyWireFields, "buildCeremonyWireFields");
  }
});
function getEfficiencyMetrics(params, learningsCount) {
  const eff = params.efficiency || {};
  return {
    saved: eff.saved || "not reported",
    prevented: eff.prevented || "none reported",
    helped: eff.helped || "general context",
    newLearnings: learningsCount
  };
}
__name(getEfficiencyMetrics, "getEfficiencyMetrics");
function identifyImprovements(data, params) {
  const improvements = [];
  const filesModified = data.filesModified || 0;
  const validationRuns = data.validationRuns || 0;
  if (filesModified > 3 && validationRuns === 0) {
    improvements.push({
      suggestion: "Run vreko m:c periodically during large edits",
      trigger: "Editing 3+ files without validation",
      priority: "medium"
    });
  }
  if (params.ok === 0) {
    improvements.push({
      suggestion: "Capture specific blocker as pitfall learning",
      trigger: "Task did not complete successfully",
      priority: "high"
    });
  }
  if (!params.l || params.l.length === 0) {
    improvements.push({
      suggestion: "Capture at least one learning per session",
      trigger: "Session ended without learnings",
      priority: "low"
    });
  }
  return improvements;
}
__name(identifyImprovements, "identifyImprovements");
function formatImprovements(improvements) {
  if (improvements.length === 0) {
    return "";
  }
  const lines = improvements.map((i) => `[${i.priority.toUpperCase()}] ${i.suggestion} (when: ${i.trigger})`);
  return `
[IMPROVE] ${lines.join(" | ")}`;
}
__name(formatImprovements, "formatImprovements");
var init_efficiency_metrics = __esm2({
  "src/tools/consolidated/vreko-end/efficiency-metrics.ts"() {
    __name8(getEfficiencyMetrics, "getEfficiencyMetrics");
    __name8(identifyImprovements, "identifyImprovements");
    __name8(formatImprovements, "formatImprovements");
  }
});
async function checkTaskGoal(workspaceRoot) {
  const sessionPath = join(workspaceRoot, ".vreko", "session", "state.json");
  if (!existsSync(sessionPath)) {
    return null;
  }
  try {
    const sessionData = JSON.parse(readFileSync(sessionPath, "utf-8"));
    const goal = sessionData.currentTask?.goal;
    if (!goal) {
      return null;
    }
    const { metric, target, unit } = goal;
    let current = 0;
    if (metric === "bundle") {
      const distPaths = [
        "dist/extension.js",
        "dist/main.js",
        "dist/index.js",
        "dist/bundle.js"
      ];
      for (const distPath of distPaths) {
        const fullPath = join(workspaceRoot, distPath);
        if (existsSync(fullPath)) {
          const stats = statSync(fullPath);
          current = Math.round(stats.size / 1024);
          break;
        }
      }
    }
    const met = current <= target;
    return {
      met,
      metric,
      current,
      target,
      unit
    };
  } catch {
    return null;
  }
}
__name(checkTaskGoal, "checkTaskGoal");
var init_goal_checker = __esm2({
  "src/tools/consolidated/vreko-end/goal-checker.ts"() {
    __name8(checkTaskGoal, "checkTaskGoal");
  }
});
function generateCelebrationMessage(metrics, ceremonyResult, params, trajectory) {
  const celebrations = [];
  if (metrics.saved !== "not reported") {
    celebrations.push(`\u{1F98E} Vreko saved you ${metrics.saved} tokens  -  context that would've been lost without protection.`);
  }
  if (metrics.prevented !== "none reported") {
    celebrations.push(`\u{1F6E1}\uFE0F Prevented ${metrics.prevented} - your repository's patterns caught this before you wasted time.`);
  }
  if (metrics.newLearnings > 0) {
    celebrations.push(`\u2728 ${metrics.newLearnings} new learning(s) added  -  these patterns will inform every future session.`);
  }
  if (metrics.helped && metrics.helped !== "general context") {
    celebrations.push(`\u{1F4DA} Vreko surfaced "${metrics.helped}" at exactly the right time - that's clairvoyant context injection.`);
  }
  if (ceremonyResult?.passed) {
    const checksRun = ceremonyResult.steps.length;
    celebrations.push(`\u2705 ${ceremonyResult.level} ceremony passed (${checksRun} checks) - you're shipping this change with confidence.`);
  }
  if (params.survey?.patterns_used && params.survey.patterns_used > 0) {
    celebrations.push(`\u{1F3AF} Applied ${params.survey.patterns_used} learned pattern(s) - your codebase memory is actively protecting this session.`);
  }
  if (params.survey?.pitfalls_avoided && params.survey.pitfalls_avoided > 0) {
    celebrations.push(`\u26A1 Avoided ${params.survey.pitfalls_avoided} pitfall(s) you've hit before - learning compound interest in action.`);
  }
  if (trajectory && trajectory.totalTracked > 0) {
    const hotText = typeof trajectory.hotTier === "number" ? ` (${trajectory.hotTier} hot patterns)` : "";
    celebrations.push(`\u{1F4CA} Repository intelligence: ${trajectory.totalTracked} tracked learnings${hotText}`);
  }
  return celebrations.length > 0 ? `
\u{1F98E} Vreko Session Complete:
${celebrations.map((c) => `   ${c}`).join("\n")}
` : "";
}
__name(generateCelebrationMessage, "generateCelebrationMessage");
function generateSessionNarrative(data, metrics, params) {
  const story = [];
  const filesModified = data.filesModified || 0;
  if (filesModified > 0) {
    story.push(`\u{1F4DD} Modified ${filesModified} file(s)`);
  }
  if (metrics.helped !== "general context") {
    story.push(`\u{1F9E0} Injected relevant context: "${metrics.helped}"`);
  }
  if (params.survey?.patterns_used && params.survey.patterns_used > 0) {
    story.push(`\u{1F3AF} Applied ${params.survey.patterns_used} learned pattern(s) from your repo history`);
  }
  if (metrics.prevented !== "none reported") {
    story.push(`\u{1F6E1}\uFE0F Prevented mistakes: ${metrics.prevented}`);
  }
  if (metrics.newLearnings > 0) {
    story.push(`\u2728 Captured ${metrics.newLearnings} new learning(s) for future sessions`);
  }
  const outcome = params.outcome || (params.ok === 1 ? "completed" : "incomplete");
  const outcomeEmoji = outcome === "completed" ? "\u2705" : outcome === "abandoned" ? "\u23F8\uFE0F" : outcome === "blocked" ? "\u{1F6AB}" : "\u2705";
  return story.length > 0 ? `
${outcomeEmoji} Session ${outcome}:
${story.map((s) => `   \u2022 ${s}`).join("\n")}
` : "";
}
__name(generateSessionNarrative, "generateSessionNarrative");
function suggestNextSteps(params, ceremonyResult, data) {
  const suggestions = [];
  const filesModified = data?.filesModified || 0;
  const linesAdded = data?.linesAdded || 0;
  const linesRemoved = data?.linesRemoved || 0;
  const totalLinesChanged = linesAdded + linesRemoved;
  if (!params.ceremony) {
    if (filesModified > 10 || totalLinesChanged > 500) {
      suggestions.push("Large change detected - strongly recommend: snap_end({ ceremony: 'standard' }) before committing.");
    } else if (filesModified > 5) {
      suggestions.push("Consider running snap_end({ ceremony: 'quick' }) before committing to add a validation gate.");
    }
  }
  if (ceremonyResult && !ceremonyResult.passed) {
    suggestions.push(`Fix ${ceremonyResult.blockers.length} ceremony blocker(s), then re-run ceremony.`);
  }
  if (ceremonyResult?.passed) {
    suggestions.push("All checks passed - ready to commit with confidence.");
  }
  if (params.survey?.patterns_used && params.survey.patterns_used > 3) {
    suggestions.push("High pattern usage! Consider check({ mode: 'architecture' }) to validate consistency.");
  }
  if ((params.outcome === "completed" || params.ok === 1) && !ceremonyResult && filesModified > 0) {
    suggestions.push("Capture any new patterns with snap_learn() before context resets.");
  }
  return suggestions;
}
__name(suggestNextSteps, "suggestNextSteps");
var init_session_reporter = __esm2({
  "src/tools/consolidated/vreko-end/session-reporter.ts"() {
    __name8(generateCelebrationMessage, "generateCelebrationMessage");
    __name8(generateSessionNarrative, "generateSessionNarrative");
    __name8(suggestNextSteps, "suggestNextSteps");
  }
});
function normalizeOutcome(outcome, ok22) {
  if (!outcome) {
    return ok22 === 0 || ok22 === false ? "abandoned" : "completed";
  }
  const lower = outcome.toLowerCase().trim();
  if (lower === "completed" || lower === "success") {
    return "completed";
  }
  if (lower === "abandoned" || lower === "cancelled" || lower === "canceled") {
    return "abandoned";
  }
  if (lower === "blocked" || lower === "failed" || lower === "failure" || lower === "error") {
    return "blocked";
  }
  if (/\bcomplet|\bfinish|\bdone|\bsuccess/.test(lower)) {
    return "completed";
  }
  if (/\babandon|\bcancel|\bgave up|\bgive up|\bscrapped/.test(lower)) {
    return "abandoned";
  }
  if (/\bblock|\bfail|\bstuck|\bbroken|\berror/.test(lower)) {
    return "blocked";
  }
  return ok22 === 0 || ok22 === false ? "abandoned" : "completed";
}
__name(normalizeOutcome, "normalizeOutcome");
function formatUserStats(metrics) {
  const parts = [];
  if (metrics.saved !== "not reported") {
    parts.push(`\u{1F4B0} ${metrics.saved} tokens saved`);
  }
  if (metrics.prevented !== "none reported") {
    parts.push(`\u{1F6E1}\uFE0F Prevented: ${metrics.prevented}`);
  }
  if (metrics.helped !== "general context") {
    parts.push(`\u{1F4DA} Context: ${metrics.helped}`);
  }
  if (metrics.newLearnings > 0) {
    parts.push(`\u2728 ${metrics.newLearnings} new learning(s)`);
  }
  return parts.length > 0 ? `
${parts.join(" | ")}` : "";
}
__name(formatUserStats, "formatUserStats");
var compressStr;
var handleSnapEnd;
var init_vreko_end2 = __esm2({
  "src/tools/consolidated/vreko-end/index.ts"() {
    init_session_adapter();
    init_branding();
    init_local_service_adapter();
    init_complete_task();
    init_input_sanitization();
    init_session_file_tracker();
    init_tiered_learning_service();
    init_workflow_lifecycle_async();
    init_ceremony_validator();
    init_efficiency_metrics();
    init_goal_checker();
    init_session_reporter();
    compressStr = /* @__PURE__ */ __name8((s, max) => {
      const clean = s.replace(/\s+/g, "\u2192");
      return clean.length > max ? `${clean.slice(0, max - 1)}\u2026` : clean;
    }, "compressStr");
    __name8(normalizeOutcome, "normalizeOutcome");
    handleSnapEnd = /* @__PURE__ */ __name8(async (args, context) => {
      const validation = validateSnapEndParams(args);
      if (!validation.success) {
        return {
          content: [
            {
              type: "text",
              text: `\u274C Input validation failed:
${validation.errors?.join("\n") ?? "Unknown validation error"}`
            }
          ],
          isError: true
        };
      }
      const params = validation.data;
      const options = {
        compact: params.compact ?? false
      };
      if (context.workspaceRoot) {
        await updateWorkflowStageAsync(context.workspaceRoot, "closing");
      }
      const learningStrings = params.learnings ?? params.l ?? [];
      let suggestedCeremony = null;
      let ceremonySuggestionReason = "";
      let ceremonySuggestionConfidence = 0;
      const isCleanCompletion = normalizeOutcome(params.outcome, params.ok) === "completed" || params.ok === 1;
      if (!params.ceremony && context.workspaceRoot && !isCleanCompletion) {
        const sessionState = await getSessionStateAsync(context.workspaceRoot);
        if (sessionState) {
          const filesModified = sessionState.changesSinceTaskStart?.length || 0;
          const hasHighRiskAreas = sessionState.riskAreasTouched?.some((area) => [
            "auth",
            "payment",
            "security",
            "database"
          ].includes(area));
          const taskStartTime = sessionState.currentTask?.startedAt;
          const taskDurationMs = taskStartTime ? Date.now() - taskStartTime : 0;
          const taskDurationMins = taskDurationMs / 6e4;
          if (hasHighRiskAreas) {
            suggestedCeremony = "comprehensive";
            ceremonySuggestionReason = `High-risk areas touched (${sessionState.riskAreasTouched?.join(", ")}) - comprehensive validation recommended`;
            ceremonySuggestionConfidence = 95;
          } else if (filesModified > 10 || taskDurationMins > 30) {
            suggestedCeremony = "standard";
            ceremonySuggestionReason = `Significant session (${filesModified} files, ${Math.round(taskDurationMins)}min) - standard validation recommended`;
            ceremonySuggestionConfidence = 90;
          } else if (filesModified > 3) {
            suggestedCeremony = "quick";
            ceremonySuggestionReason = `${filesModified} files modified - quick validation recommended`;
            ceremonySuggestionConfidence = 80;
          }
        }
      }
      let followThroughInfo = "";
      let followThroughData = null;
      if (context.workspaceRoot) {
        const ftResult = await computeFollowThroughRateAsync(context.workspaceRoot);
        if (ftResult.suggested > 0 || ftResult.altEvents > 0) {
          followThroughData = ftResult;
          const directPct = Math.round(ftResult.rate * 100);
          const altPct = Math.round(ftResult.altRate * 100);
          const totalPct = Math.round(ftResult.combinedRate * 100);
          if (altPct > 0) {
            followThroughInfo = `
\u{1F4CA} Follow-through: ${totalPct}% total
   \u2022 ${directPct}% direct (${ftResult.followed}/${ftResult.suggested} suggested actions)
   \u2022 ${altPct}% alternative (${ftResult.altSuccess} external validations)`;
          } else if (ftResult.suggested > 0) {
            followThroughInfo = `
\u{1F4CA} Follow-through: ${directPct}% (${ftResult.followed}/${ftResult.suggested} suggested actions taken)`;
          }
        }
      }
      let ceremonyResult;
      if (params.ceremony) {
        ceremonyResult = await runCeremony(params, context);
        if (!ceremonyResult.passed && !params.force) {
          const ceremonyOutput = formatCeremonyResult(ceremonyResult);
          const ceremonyWire = buildCeremonyWireFields(ceremonyResult);
          const wire = formatWireLabeled("E", {
            status: "CEREMONY_BLOCKED",
            ...ceremonyWire
          }, options);
          return {
            content: [
              {
                type: "text",
                text: `${wire}${INTERNAL_SEPARATOR}\u{1F6AB} Session close blocked by ceremony validation
${ceremonyOutput}

\u{1F4A1} Fix the issues above, then re-run snap_end({ ceremony: "${getCeremonyLevel(params.ceremony)}" })
   Or use force: true to bypass (not recommended)`
              }
            ],
            structuredContent: {
              status: "CEREMONY_BLOCKED",
              ceremonyPassed: false,
              blockers: ceremonyResult.blockers,
              warnings: ceremonyResult.warnings
            },
            isError: true
          };
        }
      }
      const goalCheck = await checkTaskGoal(context.workspaceRoot);
      if (goalCheck && !goalCheck.met) {
        const wire = formatWireLabeled("E", {
          status: "GOAL_NOT_MET",
          metric: goalCheck.metric,
          current: `${goalCheck.current}${goalCheck.unit}`,
          target: `${goalCheck.target}${goalCheck.unit}`
        }, options);
        return {
          content: [
            {
              type: "text",
              text: `${wire}

\u26A0\uFE0F Goal not achieved: ${goalCheck.metric} is ${goalCheck.current}${goalCheck.unit}, target was ${goalCheck.target}${goalCheck.unit}. Task marked incomplete.`
            }
          ],
          isError: false
        };
      }
      const customLearnings = learningStrings.map((learning) => ({
        type: "pattern",
        trigger: "Task completion",
        action: learning
      }));
      const learningService = createTieredLearningService(context.workspaceRoot);
      let promotionResult;
      let surveyAttributionCount = 0;
      let totalTrackedLearnings = 0;
      const usageStats = learningService.getUsageStats();
      totalTrackedLearnings = Object.keys(usageStats).length;
      if (params.survey) {
        const sessionState = await getSessionStateAsync(context.workspaceRoot);
        const shownLearnings = sessionState?.currentTask?.shownLearnings;
        const helpfulness = params.survey.helpfulness ?? 3;
        if (shownLearnings && helpfulness >= 3) {
          const patternsUsed = params.survey.patterns_used ?? 0;
          for (let i = 0; i < Math.min(patternsUsed, shownLearnings.patterns.length); i++) {
            learningService.trackApplied(shownLearnings.patterns[i]);
            surveyAttributionCount++;
          }
          const pitfallsAvoided = params.survey.pitfalls_avoided ?? 0;
          for (let i = 0; i < Math.min(pitfallsAvoided, shownLearnings.pitfalls.length); i++) {
            learningService.trackApplied(shownLearnings.pitfalls[i]);
            surveyAttributionCount++;
          }
        }
      }
      if (customLearnings.length > 0) {
        for (const learning of customLearnings) {
          const learningId = `vreko-end-${learning.action.slice(0, 30).replace(/\s+/g, "-").toLowerCase()}`;
          learningService.trackApplied(learningId);
        }
        const stats = usageStats;
        const hasReachedThreshold = Object.values(stats).some((s) => s.appliedCount >= HOT_TIER_PROMOTION_THRESHOLD.minAccessCount);
        if (hasReachedThreshold) {
          try {
            promotionResult = await learningService.regenerateHotTier();
          } catch {
          }
        }
      }
      let directLearningsPersisted = 0;
      if (customLearnings.length > 0) {
        for (const learning of customLearnings) {
          const result8 = await addLearningViaDaemon(context.workspaceRoot, learning);
          if (result8.success) {
            directLearningsPersisted++;
          }
        }
      }
      const completeArgs = {
        outcome: normalizeOutcome(params.outcome, params.ok),
        createSnapshot: true,
        notes: params.notes ?? params.outcome
      };
      completeArgs.learningsCaptured = directLearningsPersisted;
      const result7 = await handleCompleteTask(completeArgs, context);
      try {
        const tracker = getSessionFileTracker(context.workspaceRoot);
        if (tracker.hasActiveTask()) {
          tracker.endTask();
        }
      } catch {
      }
      try {
        const content = result7.content[0]?.text || "";
        const data = JSON.parse(content);
        const metrics = getEfficiencyMetrics(params, customLearnings.length);
        const improvements = identifyImprovements(data, params);
        const success = data.success !== false;
        const wireFields = {
          status: success ? "OK" : "FAIL",
          learn: `${data.learningsGenerated || customLearnings.length}L`,
          files: `${data.filesModified || 0}F`,
          lines: `+${data.linesAdded || 0}-${data.linesRemoved || 0}`
        };
        if (learningStrings.length > 0) {
          wireFields.summary = learningStrings.slice(0, 2).map((l) => compressStr(l, 80)).join(",");
        }
        if (params.survey) {
          const surveyJson = JSON.stringify({
            patterns_used: params.survey.patterns_used ?? 0,
            pitfalls_avoided: params.survey.pitfalls_avoided ?? 0,
            helpfulness: params.survey.helpfulness ?? 0,
            unhelpful_count: params.survey.unhelpful_count ?? 0
          });
          wireFields.survey = surveyJson;
        }
        const wire = formatWireLabeled("E", wireFields, options);
        const userStats = formatUserStats(metrics);
        const improvementNotes = formatImprovements(improvements);
        const promotionInfo = promotionResult && promotionResult.promoted > 0 ? ` | \u{1F525} ${promotionResult.promoted} learning(s) promoted to hot tier` : "";
        const feedbackInfo = surveyAttributionCount > 0 ? ` | \u{1F504} ${surveyAttributionCount} learning(s) marked as applied` : "";
        const ceremonyInfo = ceremonyResult ? `
${formatCeremonyResult(ceremonyResult)}${ceremonyResult.passed ? "" : " (forced)"}` : "";
        const ceremonySuggestionInfo = !ceremonyResult && suggestedCeremony ? `
\u{1F4A1} SUGGESTION: Vreko recommends ceremony: "${suggestedCeremony}"` + (ceremonySuggestionConfidence ? ` (${ceremonySuggestionConfidence}% confidence)` : "") + `
   Why: ${ceremonySuggestionReason}
   Run: snap_end({ ceremony: "${suggestedCeremony}" })` : "";
        if (ceremonyResult) {
          const ceremonyFields = buildCeremonyWireFields(ceremonyResult);
          Object.assign(wireFields, ceremonyFields);
        }
        if (suggestedCeremony && !ceremonyResult) {
          wireFields.suggest = suggestedCeremony;
        }
        const celebrationMessage = generateCelebrationMessage(metrics, ceremonyResult, params, {
          totalTracked: totalTrackedLearnings,
          hotTier: promotionResult?.totalHot
        });
        const sessionNarrative = generateSessionNarrative(data, metrics, params);
        const nextSteps = suggestNextSteps(params, ceremonyResult, data);
        const nextStepsBlock = nextSteps.length > 0 ? `

\u{1F4A1} Next steps:
${nextSteps.map((s) => `   \u2022 ${s}`).join("\n")}` : "";
        const baseMessage = celebrationMessage || "\u{1F98E} Vreko Session Complete:\n   \u2728 Session ended successfully";
        const fullResponse = [
          `${wire}${improvementNotes}`,
          INTERNAL_SEPARATOR,
          baseMessage,
          "",
          userStats,
          promotionInfo,
          feedbackInfo,
          followThroughInfo,
          ceremonySuggestionInfo,
          ceremonyInfo,
          sessionNarrative,
          nextStepsBlock
        ].join("");
        const structuredContent = {
          status: success ? "OK" : "FAIL",
          learningsCount: data.learningsGenerated || customLearnings.length,
          filesModified: data.filesModified || 0,
          linesAdded: data.linesAdded || 0,
          linesRemoved: data.linesRemoved || 0,
          promoted: promotionResult?.promoted || 0
        };
        if (suggestedCeremony && !ceremonyResult) {
          structuredContent.suggestedCeremony = {
            level: suggestedCeremony,
            reason: ceremonySuggestionReason
          };
        }
        if (followThroughData) {
          structuredContent.followThrough = {
            rate: followThroughData.rate,
            suggested: followThroughData.suggested,
            followed: followThroughData.followed,
            altRate: followThroughData.altRate,
            altEvents: followThroughData.altEvents,
            altSuccess: followThroughData.altSuccess,
            combinedRate: followThroughData.combinedRate
          };
        }
        return {
          content: [
            {
              type: "text",
              text: fullResponse
            }
          ],
          structuredContent
        };
      } catch {
        return result7;
      }
    }, "handleSnapEnd");
    __name8(formatUserStats, "formatUserStats");
  }
});
var handleSnapFix;
var snapFixTool;
var init_vreko_fix = __esm2({
  "src/tools/consolidated/vreko-fix.ts"() {
    init_branding();
    init_local_service_adapter();
    handleSnapFix = /* @__PURE__ */ __name8(async (args, context) => {
      const params = args;
      const options = {
        compact: params.compact ?? false
      };
      const daemon = createServiceClient2(context.workspaceRoot);
      const diffWith = params.diffWith ?? params.diff;
      const explicitDryRun = params.dryRun ?? params.dry;
      const dryRun = !(explicitDryRun === false && params.confirm === true);
      let action = params.action ?? "list";
      if (!params.action) {
        if (diffWith && params.id) {
          action = "diff";
        } else if (params.id) {
          action = "restore";
        } else if (params.symptom || params.file) {
          action = "suggest";
        }
      }
      if (action === "suggest") {
        const snapshots = await listSnapshotsViaDaemon(context.workspaceRoot);
        const limit = params.limit ?? 5;
        if (snapshots.length === 0) {
          const wire2 = formatWireLabeled("SG", {
            count: 0,
            status: "empty"
          }, options);
          return {
            content: [
              {
                type: "text",
                text: `${wire2}${INTERNAL_SEPARATOR}No snapshots available for suggestions`
              }
            ],
            structuredContent: {
              suggestions: [],
              count: 0
            }
          };
        }
        const restoreSnapshots = snapshots.map((vreko) => ({
          id: vreko.id,
          createdAt: vreko.createdAt,
          fileCount: vreko.fileCount,
          reason: vreko.reason,
          protected: vreko.protected
        }));
        const scoredSnapshots = restoreSnapshots.map((vreko, idx) => ({
          snapshot: vreko,
          score: 100 - idx * 10,
          confidence: 0.7,
          reasoning: "Recent snapshot",
          factors: [
            "recency"
          ]
        }));
        const topSuggestions = scoredSnapshots.slice(0, limit);
        const suggestionSummaries = topSuggestions.map((s) => {
          const createdAt = s.snapshot.createdAt;
          const timestamp = typeof createdAt === "string" ? new Date(createdAt).getTime() : createdAt;
          const age = getRelativeTime(timestamp);
          return `${s.snapshot.id.slice(0, 8)}:${s.score}pts:${age}`;
        }).join(",");
        const wire = formatWireLabeled("SG", {
          count: topSuggestions.length,
          suggestions: suggestionSummaries,
          top: topSuggestions[0]?.snapshot.id.slice(0, 8) ?? "none",
          confidence: topSuggestions[0]?.confidence ?? 0
        }, options);
        const structuredSuggestions = topSuggestions.map((s) => {
          const createdAt = s.snapshot.createdAt;
          const timestamp = typeof createdAt === "string" ? new Date(createdAt).getTime() : createdAt;
          return {
            id: s.snapshot.id,
            score: s.score,
            confidence: s.confidence,
            reasoning: s.reasoning,
            factors: s.factors,
            files: [],
            age: getRelativeTime(timestamp)
          };
        });
        return {
          content: [
            {
              type: "text",
              text: wire
            }
          ],
          structuredContent: {
            action: "suggest",
            suggestions: structuredSuggestions,
            count: structuredSuggestions.length,
            errorContext: {
              symptom: params.symptom,
              file: params.file
            }
          }
        };
      }
      if (action === "list") {
        const snapshots = await listSnapshotsViaDaemon(context.workspaceRoot, 5);
        if (snapshots.length === 0) {
          const wire2 = formatWireLabeled("R", {
            count: 0,
            status: "empty"
          }, options);
          return {
            content: [
              {
                type: "text",
                text: `${wire2}${INTERNAL_SEPARATOR}${messages.restore.notFound()}`
              }
            ]
          };
        }
        const snapshotSummaries = snapshots.map((vreko) => {
          const age = getRelativeTime(new Date(vreko.createdAt).getTime());
          return `${vreko.id.slice(0, 8)}:${age}:${vreko.fileCount}f`;
        }).join(",");
        const wire = formatWireLabeled("R", {
          count: snapshots.length,
          snapshots: snapshotSummaries
        }, options);
        return {
          content: [
            {
              type: "text",
              text: wire
            }
          ]
        };
      }
      if (action === "diff") {
        if (!params.id || !diffWith) {
          const wire2 = formatWireLabeled("!", {
            code: "MISSING_PARAMS",
            reason: "diff requires id and diffWith"
          }, options);
          return {
            content: [
              {
                type: "text",
                text: `${wire2}${INTERNAL_SEPARATOR}Diff requires both snapshot IDs`
              }
            ],
            isError: true
          };
        }
        const snap1Data = await daemon.request("snapshot/get", {
          workspace: context.workspaceRoot,
          snapshotId: params.id
        });
        const snap2Data = await daemon.request("snapshot/get", {
          workspace: context.workspaceRoot,
          snapshotId: diffWith
        });
        if (!snap1Data?.snapshot || !snap2Data?.snapshot) {
          const notFoundId = !snap1Data?.snapshot ? params.id : diffWith;
          const wire2 = formatWireLabeled("!", {
            code: "NOT_FOUND",
            id: notFoundId
          }, options);
          return {
            content: [
              {
                type: "text",
                text: `${wire2}${INTERNAL_SEPARATOR}${messages.error.notFound(`Snapshot ${notFoundId}`)}`
              }
            ],
            isError: true
          };
        }
        const files1 = new Set(snap1Data.snapshot.files.map((f) => f.path));
        const files2 = new Set(snap2Data.snapshot.files.map((f) => f.path));
        const added = [
          ...files2
        ].filter((f) => !files1.has(f)).length;
        const removed = [
          ...files1
        ].filter((f) => !files2.has(f)).length;
        const changed = [
          ...files1
        ].filter((f) => files2.has(f)).length;
        const wire = formatWireLabeled("D", {
          from: params.id.slice(0, 8),
          to: diffWith.slice(0, 8),
          added: `+${added}`,
          removed: `-${removed}`,
          changed: `~${changed}`
        }, options);
        return {
          content: [
            {
              type: "text",
              text: wire
            }
          ]
        };
      }
      if (!params.id) {
        const wire = formatWireLabeled("!", {
          code: "MISSING_ID",
          reason: "restore requires snapshot id"
        }, options);
        return {
          content: [
            {
              type: "text",
              text: `${wire}${INTERNAL_SEPARATOR}Restore requires snapshot ID`
            }
          ],
          isError: true
        };
      }
      const snapData = await daemon.request("snapshot/get", {
        workspace: context.workspaceRoot,
        snapshotId: params.id
      });
      if (!snapData?.snapshot) {
        const wire = formatWireLabeled("!", {
          code: "NOT_FOUND",
          id: params.id
        }, options);
        return {
          content: [
            {
              type: "text",
              text: `${wire}${INTERNAL_SEPARATOR}${messages.restore.notFound()}`
            }
          ],
          isError: true
        };
      }
      const snapshot = snapData.snapshot;
      if (dryRun) {
        const filesToRestore = params.files ? snapshot.files.filter((f) => params.files?.some((p) => f.path.includes(p))) : snapshot.files;
        const fileNames = filesToRestore.slice(0, 5).map((f) => basename(f.path));
        const wire = formatWireLabeled("R", {
          status: "DRY",
          files: `${filesToRestore.length}f`,
          preview: fileNames.join(",")
        }, options);
        return {
          content: [
            {
              type: "text",
              text: `${wire}${INTERNAL_SEPARATOR}${messages.restore.preview(filesToRestore.length)}

\u26A0\uFE0F  To actually restore: snap_fix({ id: "${params.id}", dryRun: false, confirm: true })`
            }
          ]
        };
      }
      try {
        const result7 = await restoreSnapshotViaDaemon(context.workspaceRoot, params.id, {
          files: params.files,
          dryRun: false
        });
        if (!result7.success || !result7.restoredFiles || result7.restoredFiles.length === 0) {
          const wire2 = formatWireLabeled("!", {
            code: "RESTORE_FAILED",
            reason: result7.errors?.[0] ?? "no files restored"
          }, options);
          return {
            content: [
              {
                type: "text",
                text: `${wire2}${INTERNAL_SEPARATOR}${messages.error.generic(result7.errors?.[0] ?? "no files restored")}`
              }
            ],
            isError: true
          };
        }
        const restoredFilePaths = result7.restoredFiles;
        const fileNames = restoredFilePaths.slice(0, 5).map((p) => basename(p));
        let learningId;
        const decisionRecord = await context.services?.decisionStore?.getDecisionForSnapshot(params.id, context.workspaceRoot);
        if (decisionRecord) {
          await context.services?.decisionStore?.recordOutcome(decisionRecord.id, "rolled_back");
        }
        if (context.services?.stateStore) {
          const learning = context.services.stateStore.addLearning({
            type: "pitfall",
            trigger: `When ${fileNames.join(", ")} changed`,
            action: "Rollback required  -  review changes to these files carefully",
            source: "manual",
            tier: "warm",
            domain: "rollback"
          });
          learningId = learning.id;
        }
        if (context.services?.analytics) {
          context.services.analytics.track("rollback_triggered", {
            workspace_hash: generateWorkspaceId(context.workspaceRoot),
            file_count: restoredFilePaths.length,
            ai_tool: decisionRecord?.ai_tool ?? "unknown",
            session_id: context.sessionId,
            had_decision_record: !!decisionRecord
          });
        }
        const wire = formatWireLabeled("R", {
          status: "OK",
          files: `${restoredFilePaths.length}f`,
          restored: fileNames.join(",")
        }, options);
        const timeAgo = getRelativeTimeHuman(snapshot.createdAt);
        const humanMessage = restoredFilePaths.length === 1 ? messages.restore.single(fileNames[0], timeAgo) : messages.restore.multiple(restoredFilePaths.length, timeAgo);
        const learningMessage = learningId ? `

\u{1F4DD} Pitfall pattern recorded (${learningId}).` : "";
        return {
          content: [
            {
              type: "text",
              text: `${wire}${INTERNAL_SEPARATOR}${humanMessage}${learningMessage}`
            }
          ],
          structuredContent: {
            restored: true,
            snapshotId: params.id,
            learningCaptured: learningId,
            fileCount: restoredFilePaths.length,
            files: restoredFilePaths,
            hadDecisionRecord: !!decisionRecord
          }
        };
      } catch (error) {
        const errorMsg = error instanceof Error ? error.message : "Restore failed";
        const wire = formatWireLabeled("!", {
          code: "ERROR",
          reason: errorMsg
        }, options);
        return {
          content: [
            {
              type: "text",
              text: `${wire}${INTERNAL_SEPARATOR}${messages.error.generic(errorMsg)}`
            }
          ],
          isError: true
        };
      }
    }, "handleSnapFix");
    snapFixTool = {
      name: "vreko_fix",
      description: `[DEPRECATED] \u{1F98E} Vreko Fix - Your safety net for file restoration.

**When to use:** Made a mistake? Need to compare changes? Restore specific files? Get intelligent suggestions?

**Actions:**
- **list** (default): Show recent snapshots with age and file counts
- **restore**: Restore files from a specific snapshot
- **diff**: Compare two snapshots to see what changed
- **suggest**: Get AI-scored suggestions for which snapshot to restore

**Behavioral Flow (restore):**
1. **Validate** \u2192 Check snapshot exists and files are restorable
2. **Preview** \u2192 Show what will change (if dryRun: true)
3. **Backup** \u2192 Auto-create new snapshot before restore
4. **Apply** \u2192 Write original file contents
5. **Confirm** \u2192 Report restored files and timing

**Wire Format:**
  List:    \u{1F98E}|R|count:5|snapshots:snap_a:2m:3f,snap_b:1h:5f
  Restore: \u{1F98E}|R|status:OK|files:3f|restored:auth.ts,api.ts
  Diff:    \u{1F98E}|D|from:snap_a|to:snap_b|added:+2|removed:-1
  Suggest: \u{1F98E}|SG|count:3|suggestions:snap_a:85pts:2m,snap_b:72pts:1h

**Examples:**

List recent snapshots:
  snap_fix({ action: "list" })

Get suggestions based on error:
  snap_fix({ action: "suggest", symptom: "JWT validation failed", file: "src/auth.ts" })

Preview restore (ALWAYS do this first - safety-first default):
  snap_fix({ action: "restore", id: "snap_abc123" })
  // OR explicitly: snap_fix({ action: "restore", id: "snap_abc123", dryRun: true })

Actually restore (requires BOTH dryRun: false AND confirm: true):
  snap_fix({ action: "restore", id: "snap_abc123", dryRun: false, confirm: true })

Restore specific files only:
  snap_fix({ action: "restore", id: "snap_abc123", files: ["auth.ts"] })

Compare snapshots:
  snap_fix({ action: "diff", id: "snap_before", diffWith: "snap_after" })

**Boundaries:**

Will:
- List snapshots from .vreko/snapshots/
- Restore file contents to original state
- Create backup snapshot before restore
- Show diff summary between snapshots
- Provide intelligent restoration suggestions

Will Not:
- Restore files outside workspace
- Delete snapshots (manual cleanup only)
- Modify files without explicit restore action
- Restore without valid snapshot ID

**\u26A0\uFE0F Pro Feature:** Requires Vreko Pro tier.

**Safety-First:** Restore defaults to dryRun mode. To actually restore, set dryRun: false AND confirm: true.

**Legacy Support:** Single-letter params (diff, dry) still work but are not shown in schema.`,
      inputSchema: {
        type: "object",
        properties: {
          action: {
            type: "string",
            enum: [
              "list",
              "restore",
              "diff",
              "suggest"
            ],
            description: "Operation: list (default), restore, diff, or suggest"
          },
          id: {
            type: "string",
            description: "Snapshot ID (for restore/diff operations)"
          },
          diffWith: {
            type: "string",
            description: "Second snapshot ID (for diff operation)"
          },
          dryRun: {
            type: "boolean",
            default: true,
            description: "Preview mode (DEFAULT: true). Set false + confirm: true to execute."
          },
          confirm: {
            type: "boolean",
            description: "Confirm actual restore execution. Required when dryRun: false."
          },
          files: {
            type: "array",
            items: {
              type: "string"
            },
            description: "Specific files to restore (optional filter)"
          },
          symptom: {
            type: "string",
            description: "Error symptom/message for suggest action (e.g., 'JWT validation failed')"
          },
          file: {
            type: "string",
            description: "File path with error for suggest action (e.g., 'src/auth.ts')"
          },
          limit: {
            type: "number",
            default: 5,
            description: "Maximum suggestions to return (default: 5)"
          },
          compact: {
            type: "boolean",
            default: false,
            description: "Use compact wire format instead of labeled (default: labeled)"
          }
        }
      },
      outputSchema: {
        type: "object",
        properties: {
          action: {
            type: "string",
            enum: [
              "list",
              "restore",
              "diff",
              "suggest"
            ],
            description: "Action performed"
          },
          count: {
            type: "number",
            description: "Snapshot count (list/suggest) or files restored (restore)"
          },
          snapshots: {
            type: "array",
            items: {
              type: "string"
            },
            description: "Snapshot summaries (list)"
          },
          restoredFiles: {
            type: "array",
            items: {
              type: "string"
            },
            description: "Files restored (restore)"
          },
          restored: {
            type: "boolean",
            description: "Whether restore was successful"
          },
          snapshotId: {
            type: "string",
            description: "Snapshot ID that was restored"
          },
          learningCaptured: {
            type: "string",
            description: "ID of captured pitfall learning (if any)"
          },
          hadDecisionRecord: {
            type: "boolean",
            description: "Whether a decision record was found for this snapshot"
          },
          diff: {
            type: "object",
            properties: {
              added: {
                type: "number"
              },
              removed: {
                type: "number"
              },
              changed: {
                type: "number"
              }
            },
            description: "Diff results (diff)"
          },
          suggestions: {
            type: "array",
            items: {
              type: "object",
              properties: {
                id: {
                  type: "string"
                },
                score: {
                  type: "number"
                },
                confidence: {
                  type: "number"
                },
                reasoning: {
                  type: "string"
                },
                factors: {
                  type: "object",
                  properties: {
                    recency: {
                      type: "number"
                    },
                    fileOverlap: {
                      type: "number"
                    },
                    riskScore: {
                      type: "number"
                    },
                    errorContext: {
                      type: "number"
                    }
                  }
                }
              }
            },
            description: "Scored suggestions (suggest)"
          }
        }
      },
      annotations: {
        title: "\u{1F98E} Vreko Fix",
        readOnlyHint: false,
        destructiveHint: true,
        idempotentHint: false
      },
      tier: "pro"
    };
  }
});
function getToolsHelp() {
  const intelligenceSurface = process.env.VREKO_MCP_INTELLIGENCE_SURFACE || "minimal";
  const surfaceNote = intelligenceSurface === "full" ? "\n\n\u26A1 FULL SURFACE MODE: Intelligence tools enabled (vreko_*, intelligence.*)" : "\n\n\u{1F4A1} MINIMAL SURFACE (default): To enable intelligence tools, set VREKO_MCP_INTELLIGENCE_SURFACE=full";
  return `${BRAND_PREFIX} Available Tools

CORE COMMANDS (7):
TOOL            MODES                           DESCRIPTION
\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
advise          files                           Get recommendations (REASON)
guide           files + intent                  Plan multi-step workflows (REASON)
vreko            mode: start|context             Universal entry point (ACT)
check           mode: (${IMPLEMENTED_MODE_COUNT} modes)                Deep validation modes (ACT)
snap_learn      type: pattern|pitfall|...       Capture mid-session learning (REFLECT)
snap_end        outcome: completed|abandoned|blocked  Complete task with learnings (REFLECT)

SUPPORTING TOOLS (always available):
snap_fix        action: list|restore|diff|suggest   Snapshot operations (UTILITY)
snap_help       topic: tools|wire|thresholds    This help (plain text)

ADVANCED TOOLS (full surface only):
snap_violation  type + file + description       Report code violation
learning_gc     operation: archive|delete|all   Learning lifecycle management
vreko_*      (5 tools)                       External context integration (GitHub, Sentry, Context7)
intelligence.*  (5 tools)                       Pattern-based learning system${surfaceNote}

WORKFLOW STAGES:
  REASON   \u2192 advise()|guide()         Get recommendations and a plan
  ACT      \u2192 vreko()|check()           Execute with validation
  REFLECT  \u2192 snap_learn()|snap_end()  Capture learnings
  (pulse is available but vreko(start) already includes vitals)

Use full-word parameters (e.g., mode:"start") for clarity.
Legacy single-letter params (m:"s") still work for backward compatibility.`;
}
__name(getToolsHelp, "getToolsHelp");
function getStatusHelp() {
  return `${BRAND_PREFIX} Session Status

STATUS: Active
TASK: None currently active
LEARNINGS: Ready to load
SNAPSHOTS: Available for restore

To start a task:
  vreko({ mode: "start", task: "your task description" })

To check code:
  vreko({ mode: "check", files: ["path/to/file.ts"] })`;
}
__name(getStatusHelp, "getStatusHelp");
function getWireHelp() {
  return `${BRAND_PREFIX} Wire Format Reference

RESPONSE STRUCTURE:
  [wire format line]
  ---
  [human-readable summary]

FALLBACK: If wire format is confusing, use the human summary after ---.
          The summary contains the same information in natural language.

WIRE FORMAT (labeled by default):
  \u{1F98E}|TYPE|field:value|field:value|...

TYPE CODES:
  S = Start task response
  C = Check/validation result
  X = Context snapshot
  E = End task summary
  R = Snapshot list/restore
  L = Learning captured
  V = Violation recorded
  \u{1F9E0} = Advise (recommendations)
  \u{1FAA6} = Pulse (vitals)
  ! = Error

FIELD DECODE (for TYPE=S start):
  id:      Task identifier for subsequent calls
  vreko:    Snapshot ID (use with snap_fix to restore)
  risk:    L=Low, M=Medium, H=High
  prot:    Protection coverage 0-100%
  dirty:   Count of uncommitted files
  status:  created|reused|skipped
  learn:   Relevant learnings (spaces compressed to \u2192)

FIELD DECODE (for TYPE=\u{1F9E0} advise):
  conf:    Confidence score 0-100%
  rec:     Recommendation (proceed|caution|stop)
  warn:    Warning count
  learn:   Applicable learnings count
  viol:    Recent violations count
  loop:    Loop detected (Y/N)
  lc:      Loop confidence 0-100%
  lt:      Loop type (s=structural, m=semantic, b=both)
  ev:      Loop evidence (first 25 chars)
  hint:    Advisory hint (30 chars max)

FIELD DECODE (for TYPE=C check):
  status:  \u2713=passed, \u2717=failed
  err:     Error count (e.g., 3E)
  warn:    Warning count (e.g., 2W)
  ts:      TypeScript check (\u2713/\u2717/\u23ED\uFE0F)
  lint:    Lint check (\u2713/\u2717/\u23ED\uFE0F)
  tests:   Tests (\u2713/\u2717/\u23ED\uFE0F)
  issues:  Issue summaries (compressed)

STATUS SYMBOLS:
  \u2713 = passed
  \u2717 = failed
  \u23ED\uFE0F = skipped`;
}
__name(getWireHelp, "getWireHelp");
function getModesHelp() {
  const byCategory = CHECK_MODE_REGISTRY.filter((m) => m.implemented).reduce((acc, mode) => {
    if (!acc[mode.category]) {
      acc[mode.category] = [];
    }
    acc[mode.category].push(mode);
    return acc;
  }, {});
  const formatMode = /* @__PURE__ */ __name8((m) => `  ${m.name.padEnd(12)} - ${m.description} ${m.estimatedTime}`, "formatMode");
  const sections = [];
  if (byCategory.validation?.length) {
    sections.push("VALIDATION:", ...byCategory.validation.map(formatMode));
  }
  if (byCategory.analysis?.length) {
    sections.push("", "ANALYSIS:", ...byCategory.analysis.map(formatMode));
  }
  if (byCategory.security?.length) {
    sections.push("", "SECURITY:", ...byCategory.security.map(formatMode));
  }
  if (byCategory.maintenance?.length) {
    sections.push("", "MAINTENANCE:", ...byCategory.maintenance.map(formatMode));
  }
  return `${BRAND_PREFIX} Mode Reference

SNAP MODES:
  start   - Begin a new task, load context, create snapshot
  context - Get current workspace status without starting task
  check   - (DEPRECATED) Use the check tool instead

CHECK MODES (${IMPLEMENTED_MODE_COUNT} implemented):
${sections.join("\n")}

INTENT TYPES (for vreko/guide/advise):
  implement - Adding new features or functionality
  debug     - Finding and fixing bugs
  refactor  - Restructuring code without changing behavior
  review    - Code review and quality assessment
  explore   - Learning codebase structure and patterns

LEARNING TYPES (for snap_learn):
  pattern    - Reusable approach that worked
  pitfall    - Mistake to avoid
  efficiency - Token/time optimization
  discovery  - New codebase knowledge
  workflow   - Process improvement

SNAP_FIX ACTIONS:
  list    - Show recent snapshots with age and file counts
  restore - Restore files from a specific snapshot
  diff    - Compare two snapshots to see what changed
  suggest - Get AI-scored suggestions for which snapshot to restore

SNAP_END OUTCOMES:
  completed - Task finished successfully
  abandoned - Gave up or wrong approach
  blocked   - External dependency preventing progress`;
}
__name(getModesHelp, "getModesHelp");
function getThresholdsHelp() {
  return `${BRAND_PREFIX} Auto-Promotion Thresholds

VIOLATIONS:
  1-2 occurrences  \u2192 Stored for reference
  3 occurrences    \u2192 Auto-promoted to PATTERN (affects future task starts)
  5 occurrences    \u2192 Marked for AUTOMATION (\u{1F916} flag in response)

LEARNINGS:
  Learnings accessed 3+ times are promoted to "hot tier" for faster loading.
  Hot tier learnings appear automatically in relevant task contexts.

RISK LEVELS:
  L (Low)    - Simple changes, well-tested areas
  M (Medium) - Moderate complexity or less coverage
  H (High)   - Complex changes, critical paths, low protection`;
}
__name(getThresholdsHelp, "getThresholdsHelp");
function getDecisionTreeHelp() {
  const checkUseCases = [
    {
      useCase: "Security scan",
      mode: "security"
    },
    {
      useCase: "Find dead code",
      mode: "orphans"
    },
    {
      useCase: "Architecture review",
      mode: "architecture"
    },
    {
      useCase: "Circular deps",
      mode: "circular"
    },
    {
      useCase: "Test coverage",
      mode: "coverage"
    },
    {
      useCase: "Dependency trace",
      mode: "trace"
    },
    {
      useCase: "Deprecated patterns",
      mode: "evolution"
    },
    {
      useCase: "DI wiring",
      mode: "wiring"
    },
    {
      useCase: "External integrations",
      mode: "integrations"
    },
    {
      useCase: "MCP health",
      mode: "health"
    },
    {
      useCase: "Bundle impact",
      mode: "impact"
    },
    {
      useCase: "Doc freshness",
      mode: "docs"
    },
    {
      useCase: "Learning tiers",
      mode: "learnings"
    },
    {
      useCase: "Pattern validation",
      mode: "patterns"
    },
    {
      useCase: "Build verification",
      mode: "build"
    },
    {
      useCase: "Quick validation",
      mode: "quick"
    },
    {
      useCase: "Full validation",
      mode: "full"
    }
  ];
  const validUseCases = checkUseCases.filter((uc) => IMPLEMENTED_CHECK_MODES.includes(uc.mode));
  const deepAnalysisLines = validUseCases.map((uc, i) => `${i === validUseCases.length - 1 ? "\u2514\u2500\u2500" : "\u251C\u2500\u2500"} ${uc.useCase.padEnd(18)} \u2192 check({ mode: "${uc.mode}" })`);
  return `${BRAND_PREFIX} Tool Selection Decision Tree

STARTING WORK?
\u2514\u2500\u2500 vreko({ task: "description" })
    CLI: npx vreko start

MID-TASK NEEDS:
\u251C\u2500\u2500 Quick status?        \u2192 vreko() (resumes existing session)
\u2502                           CLI: npx vreko status
\u251C\u2500\u2500 Validate code?       \u2192 check({ mode: "quick" }) or check({ mode: "full" })
\u2502                           CLI: npx vreko check --mode quick
\u251C\u2500\u2500 Something broke?     \u2192 snap_violation({ type, file, description, prevention })
\u2502                           CLI: npx vreko report-violation
\u251C\u2500\u2500 Learned something?   \u2192 vreko_learn({ trigger, action, type })
\u2502                           CLI: npx vreko learn
\u2514\u2500\u2500 Need old version?    \u2192 snap_fix({ action: "list" })
                            CLI: npx vreko snapshots

ENDING WORK?
\u2514\u2500\u2500 vreko_end({ outcome: "completed", learnings: [...] })
    CLI: npx vreko end

MAINTENANCE (periodic):
\u251C\u2500\u2500 Preview learning cleanup  \u2192 learning_gc({ operation: "archive", dry_run: true })
\u251C\u2500\u2500 Execute learning cleanup  \u2192 learning_gc({ operation: "archive", dry_run: false })
\u2514\u2500\u2500 Delete archived learnings \u2192 learning_gc({ operation: "delete", dry_run: false })

DEEP ANALYSIS (use check tool - ${IMPLEMENTED_MODE_COUNT} modes):
${deepAnalysisLines.join("\n")}

QUICK REFERENCE:
\u250C\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510
\u2502 I want to...    \u2502 Use this...                             \u2502 CLI Equivalent              \u2502
\u251C\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253C\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253C\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524
\u2502 Start work      \u2502 vreko({ task: "..." })               \u2502 npx vreko start          \u2502
\u2502 Get advice      \u2502 advise({ files: ["path"] })             \u2502 npx vreko advise         \u2502
\u2502 Quick check     \u2502 check({ mode: "quick" })                \u2502 npx vreko check          \u2502
\u2502 Deep analysis   \u2502 check({ mode: "full" })                 \u2502 npx vreko check --full   \u2502
\u2502 Record mistake  \u2502 snap_violation()                        \u2502 npx vreko report-violation\u2502
\u2502 Save insight    \u2502 vreko_learn()                        \u2502 npx vreko learn          \u2502
\u2502 Restore files   \u2502 snap_fix({ action: "restore", id })     \u2502 npx vreko restore <id>   \u2502
\u2502 Finish task     \u2502 vreko_end({ outcome: "completed" })  \u2502 npx vreko end            \u2502
\u2502 Clean learnings \u2502 learning_gc({ operation: "archive" })   \u2502 npx vreko gc --archive   \u2502
\u2502 Get help        \u2502 snap_help({ topic: "..." })             \u2502 npx vreko help           \u2502
\u2502 Check daemon    \u2502 (N/A - internal)                        \u2502 npx vreko daemon status  \u2502
\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518

TROUBLESHOOTING:
\u250C\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510
\u2502 Issue                   \u2502 Solution                             \u2502
\u251C\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253C\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524
\u2502 Daemon not responding   \u2502 npx vreko daemon status           \u2502
\u2502 Session stuck           \u2502 snap_fix({ action: "list" })         \u2502
\u2502 Need to restore files   \u2502 snap_fix({ action: "suggest" })      \u2502
\u2502 Circular deps error     \u2502 check({ mode: "circular" })          \u2502
\u2502 High risk score         \u2502 advise({ files: [...] })             \u2502
\u2502 Tests failing           \u2502 check({ mode: "quick", tests: true }) \u2502
\u2502 Forgot session details  \u2502 vreko_pulse() or vreko()       \u2502
\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518`;
}
__name(getDecisionTreeHelp, "getDecisionTreeHelp");
var handleSnapHelp;
var snapHelpTool;
var init_vreko_help = __esm2({
  "src/tools/consolidated/vreko-help.ts"() {
    init_branding();
    init_check_modes_registry();
    __name8(getToolsHelp, "getToolsHelp");
    __name8(getStatusHelp, "getStatusHelp");
    __name8(getWireHelp, "getWireHelp");
    __name8(getModesHelp, "getModesHelp");
    __name8(getThresholdsHelp, "getThresholdsHelp");
    __name8(getDecisionTreeHelp, "getDecisionTreeHelp");
    handleSnapHelp = /* @__PURE__ */ __name8(async (args, _context) => {
      const params = args;
      const topic = params.topic || params.q || "all";
      switch (topic) {
        case "tools":
          return {
            content: [
              {
                type: "text",
                text: getToolsHelp()
              }
            ]
          };
        case "status":
          return {
            content: [
              {
                type: "text",
                text: getStatusHelp()
              }
            ]
          };
        case "wire":
          return {
            content: [
              {
                type: "text",
                text: getWireHelp()
              }
            ]
          };
        case "modes":
          return {
            content: [
              {
                type: "text",
                text: getModesHelp()
              }
            ]
          };
        case "thresholds":
          return {
            content: [
              {
                type: "text",
                text: getThresholdsHelp()
              }
            ]
          };
        case "decision":
          return {
            content: [
              {
                type: "text",
                text: getDecisionTreeHelp()
              }
            ]
          };
        default: {
          const allHelp = `${BRAND_PREFIX} Quick Reference

CORE COMMANDS: advise | guide | vreko | check | snap_learn | snap_end
SUPPORTING: snap_violation | snap_fix | learning_gc | snap_help | pulse

WORKFLOW:
  vreko({ mode: "start", task: "description" })  \u2192 Begin task
  advise({ files: ["path"] })                   \u2192 Get recommendations
  check({ mode: "quick" })                      \u2192 Validate code
  snap_learn({ trigger, action })               \u2192 Capture insight
  snap_end({ outcome: "completed" })            \u2192 Complete task

Response format: [wire line] --- [human summary]
TYPE CODES: \u{1F9E0}=Advise, S=Start, C=Check, X=Context, E=End, R=Restore, L=Learn, V=Violation, GC=Learning GC

TOPICS: snap_help({ topic: "tools|wire|modes|thresholds|decision" })`;
          return {
            content: [
              {
                type: "text",
                text: allHelp
              }
            ]
          };
        }
      }
    }, "handleSnapHelp");
    snapHelpTool = {
      name: "vreko_help",
      description: `[DEPRECATED] Tool documentation and wire format reference. Returns plain text (not wire format).
Use when you need mode details, wire format grammar, or a tool selection decision tree.
Do NOT use to check code or session state (that's check or vreko).
Topics: tools, status, wire, modes, thresholds, decision, all (default).`,
      inputSchema: {
        type: "object",
        properties: {
          topic: {
            type: "string",
            enum: [
              "tools",
              "status",
              "wire",
              "modes",
              "thresholds",
              "decision",
              "all"
            ],
            description: "Help topic: tools, status, wire, modes, thresholds, decision, or all (default)"
          }
        }
      },
      annotations: {
        title: "\u{1F98E} Vreko Help",
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true
      },
      tier: "free"
    };
  }
});
function buildRewardSignal(learningType, trigger, action, trajectory) {
  const meta = LEARNING_TYPE_META[learningType] || LEARNING_TYPE_META.pattern;
  const lines = [];
  lines.push(`${meta.emoji} ${meta.verb}!`);
  lines.push(`  When: "${truncate(trigger, 60)}"`);
  lines.push(`  Do: "${truncate(action, 60)}"`);
  lines.push(`  \u2192 This ${meta.future}`);
  if (trajectory) {
    const { totalTracked, hotCount, maxHotSize } = trajectory;
    lines.push("");
    const trackedLabel = totalTracked === 1 ? "\u{1F9E0} First tracked learning  -  your repo is getting smarter." : `\u{1F9E0} Repository intelligence: ${totalTracked} tracked learnings`;
    lines.push(trackedLabel);
    if (hotCount > 0) {
      const bar = buildMiniBar(hotCount, maxHotSize);
      lines.push(`\u{1F525} Hot tier: ${bar} ${hotCount}/${maxHotSize} slots`);
    }
    if (hotCount === 0) {
      lines.push("\u{1F4A1} Tip: Learnings used 3+ times auto-promote to hot tier (always loaded)");
    } else if (hotCount < maxHotSize) {
      const remaining = maxHotSize - hotCount;
      lines.push(`\u{1F4C8} ${remaining} more hot slot${remaining !== 1 ? "s" : ""} until your top insights are always loaded`);
    }
  }
  lines.push("");
  lines.push("LLM_HINT:");
  lines.push(`- When context matches "${truncate(trigger, 50)}", suggest "${truncate(action, 50)}" early.`);
  lines.push("- Highlight this learning when planning steps for similar tasks.");
  return lines.join("\n");
}
__name(buildRewardSignal, "buildRewardSignal");
function buildMiniBar(filled, total) {
  if (total <= 0) {
    return "[-]";
  }
  const barLen = Math.min(total, 10);
  const filledLen = Math.round(filled / total * barLen);
  return `[${"\u2588".repeat(filledLen)}${"\u2591".repeat(Math.max(0, barLen - filledLen))}]`;
}
__name(buildMiniBar, "buildMiniBar");
var handleSnapLearn;
var init_vreko_learn2 = __esm2({
  "src/tools/consolidated/vreko-learn.ts"() {
    init_branding();
    init_handlers();
    init_input_sanitization();
    init_workflow_lifecycle_async();
    init_learning_meta();
    __name8(buildRewardSignal, "buildRewardSignal");
    __name8(buildMiniBar, "buildMiniBar");
    handleSnapLearn = /* @__PURE__ */ __name8(async (args, context) => {
      const validation = validateSnapLearnParams(args);
      if (!validation.success) {
        return {
          content: [
            {
              type: "text",
              text: `\u274C Input validation failed:
${validation.errors?.join("\n") ?? "Unknown validation error"}`
            }
          ],
          isError: true
        };
      }
      const params = validation.data;
      const options = {
        compact: args.compact === true
      };
      const trigger = params.trigger ?? params.t ?? "";
      const action = params.action ?? params.a ?? "";
      const source = params.source ?? params.s ?? "snap_learn";
      const learningType = normalizeLearningType(params.type);
      const learnArgs = {
        type: learningType,
        trigger,
        action,
        source
      };
      const result7 = await handleLearn(learnArgs, context);
      let learningId = "unknown";
      try {
        const content = result7.content[0]?.text || "";
        const data = JSON.parse(content);
        learningId = data.id || "unknown";
      } catch {
      }
      const trajectory = getLearningTrajectory(context.workspaceRoot);
      const wireFields = {
        status: "OK",
        id: learningId,
        type: learningType,
        trigger: truncate(trigger, 40),
        action: truncate(action, 40)
      };
      if (trajectory) {
        wireFields.total = trajectory.totalTracked;
        wireFields.hot = trajectory.hotCount;
      }
      const wire = formatWireLabeled("L", wireFields, options);
      const rewardSignal = buildRewardSignal(learningType, trigger, action, trajectory);
      let nextActionHint = "";
      if (context.workspaceRoot) {
        const suggestions = await suggestNextActionAsync(context.workspaceRoot, "snap_learn");
        if (suggestions.length > 0) {
          const primary = suggestions[0];
          nextActionHint = `

\u{1F449} **Next:** ${primary.reason}
   \u2192 ${primary.tool}${primary.mode ? `({ mode: "${primary.mode}" })` : "()"}`;
        }
      }
      return {
        content: [
          {
            type: "text",
            text: `${wire}${INTERNAL_SEPARATOR}${rewardSignal}${nextActionHint}`
          }
        ]
      };
    }, "handleSnapLearn");
  }
});
var handler;
var snapSessionTool;
var handleSnapSession;
var init_vreko_session = __esm2({
  "src/tools/consolidated/vreko-session.ts"() {
    init_local_service_adapter();
    handler = /* @__PURE__ */ __name8(async (args, context) => {
      const params = args;
      const { workspaceRoot } = context;
      const sessionState = await getSessionStatusViaService(workspaceRoot);
      if (!sessionState) {
        return {
          content: [
            {
              type: "text",
              text: JSON.stringify({
                active: false,
                message: "No active session or daemon unavailable"
              })
            }
          ]
        };
      }
      const durationMin = sessionState.startedAt ? Math.floor((Date.now() - new Date(sessionState.startedAt).getTime()) / 6e4) : 0;
      const result7 = {
        active: sessionState.active,
        taskId: sessionState.taskId,
        task: sessionState.task,
        startedAt: sessionState.startedAt,
        filesModified: sessionState.filesModified,
        snapshotCount: sessionState.snapshotCount,
        durationMin
      };
      if (params.includeAIAttribution) {
        const sessionChanges = await getSessionChangesViaService(workspaceRoot, {
          includeAIAttribution: true
        });
        result7.aiAttributedChanges = sessionChanges ? sessionChanges.files.filter((f) => f.aiAttributed === true).length : 0;
      }
      const statusEmoji = sessionState.active ? "\u{1F7E2}" : "\u26AA";
      const taskText = sessionState.task ? `
\u{1F4CB} **Task:** ${sessionState.task}` : "";
      const durationText = durationMin > 0 ? `
\u23F1\uFE0F **Duration:** ${durationMin} min` : "";
      const filesText = `
\u{1F4C1} **Files Modified:** ${sessionState.filesModified}`;
      const snapshotsText = `
\u{1F4F8} **Snapshots:** ${sessionState.snapshotCount}`;
      const humanText = `## Session Status ${statusEmoji}
**Active:** ${sessionState.active ? "Yes" : "No"}` + taskText + durationText + filesText + snapshotsText;
      return {
        content: [
          {
            type: "text",
            text: `${JSON.stringify(result7)}

${humanText}`
          }
        ]
      };
    }, "handler");
    snapSessionTool = {
      name: "vreko_session",
      description: "[DEPRECATED  -  use vreko_begin/vreko_learn/vreko_end/vreko_pulse] \u{1F4CA} Get current development session state from daemon: files touched, duration, snapshots. Query the daemon's session tracking for real-time state.",
      inputSchema: {
        type: "object",
        properties: {
          includeAIAttribution: {
            type: "boolean",
            description: "Include AI attribution details (default: false)"
          }
        }
      },
      annotations: {
        title: "\u{1F4CA} Session State",
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true
      }
    };
    handleSnapSession = handler;
  }
});
var handleSnapViolation;
var snapViolationTool;
var init_vreko_violation = __esm2({
  "src/tools/consolidated/vreko-violation.ts"() {
    init_branding();
    init_handlers();
    init_input_sanitization();
    handleSnapViolation = /* @__PURE__ */ __name8(async (args, context) => {
      const validation = validateSnapViolationParams(args);
      if (!validation.success) {
        return {
          content: [
            {
              type: "text",
              text: `\u274C Input validation failed:
${validation.errors?.join("\n") ?? "Unknown validation error"}`
            }
          ],
          isError: true
        };
      }
      const params = validation.data;
      const options = {
        compact: args.compact === true
      };
      const description = params.description ?? params.what ?? "";
      const reason = params.reason ?? params.why ?? "";
      const prevention = params.prevention ?? params.prevent ?? "";
      const violationArgs = {
        type: params.type,
        file: params.file,
        whatHappened: description,
        whyItHappened: reason,
        prevention
      };
      const result7 = await handleReportViolation(violationArgs, context);
      try {
        const content = result7.content[0]?.text || "";
        const data = JSON.parse(content);
        const count = data.count || 1;
        const wireFields = {
          status: "OK",
          type: params.type,
          count
        };
        if (data.shouldPromote) {
          wireFields.promote = "PROMOTED";
        }
        if (data.shouldAutomate) {
          wireFields.auto = "YES";
        }
        const wire = formatWireLabeled("V", wireFields, options);
        const humanMessage = data.shouldAutomate ? messages.violation.automate(params.type) : data.shouldPromote ? messages.violation.promoted(params.type) : messages.violation.recorded(params.type, count);
        return {
          content: [
            {
              type: "text",
              text: `${wire}${INTERNAL_SEPARATOR}${humanMessage}`
            }
          ]
        };
      } catch {
        return result7;
      }
    }, "handleSnapViolation");
    snapViolationTool = {
      name: "vreko_violation",
      description: `[DEPRECATED] \u{1F98E} Vreko Violation - Your mistake tracker that learns.

**When to use:** Something broke or went wrong? Report it so you never repeat it.

**vs snap_learn pitfall:**
- snap_violation: For BUGS/ERRORS that actually happened (track occurrences)
- snap_learn({ type: "pitfall" }): For POTENTIAL mistakes to avoid (no tracking)

**Auto-Escalation:**
- **1st time**: Recorded, tracked
- **3rd time**: \u{1F4E5} Promoted to pattern (prevents future occurrences)
- **5th time**: \u{1F916} Flagged for automation (seriously, automate this check)

**What to report:**
- Bugs that slipped through
- Security issues caught late
- Performance problems discovered
- Architecture violations
- Test failures from bad patterns

**Wire Format:** \u{1F98E}|V|status:OK|type:silent_catch|count:3|promote:PROMOTED

**Examples:**

Report a caught error pattern:
  snap_violation({
    type: "silent_catch",
    file: "auth.ts",
    description: "Catch block swallowed error without logging",
    reason: "Rushed implementation, forgot logging",
    prevention: "Always log in catch blocks with context"
  })

Architecture violation:
  snap_violation({
    type: "wrong_layer_import",
    file: "packages/core/utils.ts",
    description: "Core imported from platform layer",
    reason: "Didn't check dependency direction",
    prevention: "Core should never import platform - use dependency injection"
  })

Security issue:
  snap_violation({
    type: "exposed_secret",
    file: "config.ts",
    description: "API key hardcoded in source",
    reason: "Testing shortcut left in code",
    prevention: "Use environment variables, never commit secrets"
  })

**Boundaries:**

Will:
- Track occurrence count locally
- Auto-promote at thresholds
- Store in .vreko/patterns/violations.jsonl
- Surface in future vreko starts

Will Not:
- Share violations externally
- Auto-fix code
- Require manual category management
- Expire violations (cleanup is manual)

**Pro Tip:** Be specific with type names ("silent_catch" not "error"). Specific types promote faster and help more.

**Legacy Support:** Single-letter params (what, why, prevent) still work but are not shown in schema.`,
      inputSchema: {
        type: "object",
        properties: {
          type: {
            type: "string",
            description: "Violation type key (e.g., 'silent_catch', 'missing_auth_check')"
          },
          file: {
            type: "string",
            description: "File where violation occurred"
          },
          description: {
            type: "string",
            description: "What went wrong - description of the violation"
          },
          reason: {
            type: "string",
            description: "Why it happened - root cause analysis (optional)"
          },
          prevention: {
            type: "string",
            description: "How to prevent this in future"
          },
          compact: {
            type: "boolean",
            default: false,
            description: "Use compact wire format instead of labeled (default: labeled)"
          }
        },
        required: [
          "type",
          "file",
          "description",
          "prevention"
        ]
      },
      outputSchema: {
        type: "object",
        properties: {
          type: {
            type: "string",
            description: "Violation type recorded"
          },
          count: {
            type: "number",
            description: "Occurrence count for this type"
          },
          promoted: {
            type: "boolean",
            description: "Whether promoted to pattern"
          },
          flaggedForAutomation: {
            type: "boolean",
            description: "Whether flagged for automation"
          }
        }
      },
      annotations: {
        title: "\u{1F98E} Vreko Violation",
        readOnlyHint: false,
        destructiveHint: false,
        idempotentHint: false
      },
      tier: "free"
    };
  }
});
function getMigrationGuidance(legacyTool) {
  const mapping = LEGACY_TO_CONSOLIDATED[legacyTool];
  return mapping?.message;
}
__name(getMigrationGuidance, "getMigrationGuidance");
var LEGACY_TO_CONSOLIDATED;
var init_legacy_migrations = __esm2({
  "src/tools/consolidated/legacy-migrations.ts"() {
    LEGACY_TO_CONSOLIDATED = {
      // Legacy tools (pre-consolidation)
      begin_task: {
        tool: "vreko",
        mode: "s",
        message: 'Use vreko({m:"s", t:"task", f:["files"]}) instead'
      },
      get_context: {
        tool: "vreko",
        mode: "x",
        message: 'Use vreko({m:"x"}) instead'
      },
      prepare_workspace: {
        tool: "vreko",
        mode: "s",
        message: 'Use vreko({m:"s"}) which includes prepare behavior'
      },
      get_learnings: {
        tool: "vreko",
        mode: "s",
        message: 'Learnings included in vreko({m:"s"}) response'
      },
      quick_check: {
        tool: "check",
        mode: "q",
        message: 'Use check({m:"q", f:["files"]}) instead'
      },
      check_patterns: {
        tool: "check",
        mode: "p",
        message: 'Use check({m:"p", code:"..."}) instead'
      },
      validate: {
        tool: "check",
        mode: "f",
        message: 'Use check({m:"f", code:"..."}) instead'
      },
      complete_task: {
        tool: "snap_end",
        message: "Use snap_end({ok:1, l:[...]}) instead"
      },
      review_work: {
        tool: "snap_end",
        message: "Use snap_end({ok:1}) instead - includes review"
      },
      what_changed: {
        tool: "snap_end",
        message: "Use snap_end({ok:1}) instead - includes changes"
      },
      learn: {
        tool: "snap_learn",
        message: 'Use snap_learn({t:"trigger", a:"action"}) instead'
      },
      snapshot_list: {
        tool: "snap_fix",
        message: "Use snap_fix() with no params for list"
      },
      snapshot_restore: {
        tool: "snap_fix",
        message: 'Use snap_fix({id:"snapshot_id"}) instead'
      },
      compare_snapshots: {
        tool: "snap_fix",
        message: 'Use snap_fix({id:"id1", diff:"id2"}) instead'
      },
      report_violation: {
        tool: "snap_violation",
        message: 'Use snap_violation({type:"...", file:"..."}) instead'
      },
      meta: {
        tool: "snap_help",
        message: "Use snap_help() instead"
      },
      get_pairing_protocol: {
        tool: "snap_help",
        message: "Use snap_help() instead"
      },
      // pulse → vreko_pulse (V2 migration)
      pulse: {
        tool: "vreko_pulse",
        message: "Use vreko_pulse() for mid-session intelligence check (read-only, no local state)"
      },
      // =============================================================================
      // Naming unification (Phase 4) - vreko_* → snap_*
      // =============================================================================
      // Intelligence tools: vreko_* → snap_*
      vreko_validate_change: {
        tool: "snap_validate",
        message: "Use snap_validate({filePath, content}) instead"
      },
      vreko_get_risk_score: {
        tool: "snap_risk",
        message: "Use snap_risk({filePath}) instead"
      },
      vreko_query_patterns: {
        tool: "snap_patterns",
        message: "Use snap_patterns({query}) instead"
      },
      vreko_get_context: {
        tool: "snap_context",
        message: "Use snap_context({filePath}) instead"
      },
      vreko_suggest_rollback: {
        tool: "snap_rollback",
        message: "Use snap_rollback({filePath, symptom}) instead"
      },
      // Learning intelligence: intelligence.* → snap_learn_*
      "intelligence.capture": {
        tool: "snap_learn_capture",
        message: "Use snap_learn_capture({content, domain, type}) instead"
      },
      "intelligence.patterns": {
        tool: "snap_learn_patterns",
        message: "Use snap_learn_patterns({domains, types}) instead"
      },
      "intelligence.insights": {
        tool: "snap_learn_insights",
        message: "Use snap_learn_insights({type, context}) instead"
      },
      "intelligence.explain": {
        tool: "snap_learn_explain",
        message: "Use snap_learn_explain({insightId}) instead"
      },
      "intelligence.outcome": {
        tool: "snap_learn_outcome",
        message: "Use snap_learn_outcome({patternId, outcome}) instead"
      },
      // Refactoring: vreko_refactor_* → snap_refactor_*
      vreko_refactor_assess: {
        tool: "snap_refactor",
        message: "Use snap_refactor({workspace}) instead"
      },
      vreko_refactor_report: {
        tool: "snap_refactor_report",
        message: "Use snap_refactor_report({workspace}) instead"
      },
      vreko_refactor_set_gate: {
        tool: "snap_refactor_gate",
        message: "Use snap_refactor_gate({workspace, componentId, gate, status}) instead"
      },
      vreko_refactor_stats: {
        tool: "snap_refactor_stats",
        message: "Use snap_refactor_stats({workspace}) instead"
      },
      // Lifecycle: learning_gc → snap_gc
      learning_gc: {
        tool: "snap_gc",
        message: "Use snap_gc() instead"
      }
    };
    __name8(getMigrationGuidance, "getMigrationGuidance");
  }
});
var registry_exports = {};
__export2(registry_exports, {
  CONSOLIDATED_HANDLERS: /* @__PURE__ */ __name(() => CONSOLIDATED_HANDLERS, "CONSOLIDATED_HANDLERS"),
  CONSOLIDATED_TOOLS: /* @__PURE__ */ __name(() => CONSOLIDATED_TOOLS, "CONSOLIDATED_TOOLS"),
  EXPOSED_MCP_TOOLS: /* @__PURE__ */ __name(() => EXPOSED_MCP_TOOLS, "EXPOSED_MCP_TOOLS"),
  LEGACY_TO_CONSOLIDATED: /* @__PURE__ */ __name(() => LEGACY_TO_CONSOLIDATED, "LEGACY_TO_CONSOLIDATED"),
  getConsolidatedHandler: /* @__PURE__ */ __name(() => getConsolidatedHandler, "getConsolidatedHandler"),
  getMigrationGuidance: /* @__PURE__ */ __name(() => getMigrationGuidance, "getMigrationGuidance"),
  isConsolidatedTool: /* @__PURE__ */ __name(() => isConsolidatedTool, "isConsolidatedTool")
});
function isConsolidatedTool(name) {
  return name in CONSOLIDATED_HANDLERS;
}
__name(isConsolidatedTool, "isConsolidatedTool");
function getConsolidatedHandler(name) {
  return CONSOLIDATED_HANDLERS[name];
}
__name(getConsolidatedHandler, "getConsolidatedHandler");
var EXPOSED_MCP_TOOLS;
var CONSOLIDATED_TOOLS;
var CONSOLIDATED_HANDLERS;
var init_registry2 = __esm2({
  "src/tools/consolidated/registry.ts"() {
    init_tool();
    init_v2();
    init_vreko_pulse();
    init_advise();
    init_check();
    init_guide();
    init_intelligence2();
    init_learning_gc();
    init_learning_intelligence();
    init_pulse();
    init_refactoring();
    init_safe_to_write();
    init_vreko_end2();
    init_vreko_fix();
    init_vreko_help();
    init_vreko_learn2();
    init_vreko_session();
    init_vreko_violation();
    init_legacy_migrations();
    EXPOSED_MCP_TOOLS = [
      // V2 BLACK-BOX INTELLIGENCE SURFACE (3-tool minimal surface)
      // vreko_pulse is excluded from minimal  -  available in full surface
      ...V2_MINIMAL_TOOLS
    ];
    CONSOLIDATED_TOOLS = [
      // V2 BLACK-BOX INTELLIGENCE SURFACE (exposed by default)
      ...V2_TOOLS,
      // PERCEIVE (not in minimal surface  -  vreko(start) already includes pulse data)
      pulseTool,
      // V1 CORE SURFACE (full set  -  available in full intelligence surface)
      // NOTE: snapTool, snapEndTool, snapLearnTool removed - V2 tools replace them
      checkTool,
      snapFixTool,
      snapHelpTool,
      // SUPPORTING (not in minimal surface  -  raw data access, debugging)
      snapViolationTool,
      snapSessionTool,
      learningGCTool,
      // INTELLIGENCE LAYER (External MCP Integration)
      ...INTELLIGENCE_TOOLS,
      // LEARNING INTELLIGENCE (Pattern-based Learning)
      ...LEARNING_INTELLIGENCE_TOOLS,
      // REFACTORING INTELLIGENCE (v2.1)
      ...REFACTORING_TOOLS,
      // INTELLIGENCE PILLARS (Pillar 06: Pre-Write Interception)
      safeToWriteTool
    ];
    CONSOLIDATED_HANDLERS = {
      // PERCEIVE stage
      pulse: handleVrekoPulse,
      // REASON stage
      advise: handleAdvise,
      guide: handleGuide,
      // ACT stage
      vreko: V2_HANDLERS.vreko,
      check: handleCheck,
      // REFLECT stage
      vreko_learn: V2_HANDLERS.vreko_learn,
      vreko_violation: handleSnapViolation,
      vreko_end: V2_HANDLERS.vreko_end,
      vreko_session: handleSnapSession,
      // UTILITY
      vreko_fix: handleSnapFix,
      vreko_help: handleSnapHelp,
      // Backward-compat aliases (snap_* deprecated names → same handlers)
      snap_learn: handleSnapLearn,
      snap_violation: handleSnapViolation,
      snap_end: handleSnapEnd,
      snap_session: handleSnapSession,
      snap_fix: handleSnapFix,
      snap_help: handleSnapHelp,
      // LIFECYCLE MANAGEMENT
      learning_gc: handleLearningGC,
      // HEALTH
      workspace_health: handleWorkspaceHealth,
      "health/check": handleWorkspaceHealth,
      // =============================================================================
      // INTELLIGENCE LAYER (External MCP Integration)
      // =============================================================================
      // Canonical names (snap_*)
      snap_validate: handleValidateChange,
      snap_risk: handleGetRiskScore,
      snap_patterns: handleQueryPatterns,
      snap_context: handleGetContext2,
      snap_rollback: handleSuggestRollback,
      // =============================================================================
      // LEARNING INTELLIGENCE (Pattern-based Learning)
      // =============================================================================
      // Canonical names (snap_learn_*)
      snap_learn_capture: handleCapturePattern,
      snap_learn_patterns: handleQueryPatterns2,
      snap_learn_insights: handleGenerateInsights,
      snap_learn_explain: handleExplainInsight,
      snap_learn_outcome: handleRecordOutcome,
      // =============================================================================
      // REFACTORING INTELLIGENCE (v2.1)
      // =============================================================================
      // Canonical names (snap_refactor_*)
      snap_refactor: handleRefactorAssess,
      snap_refactor_report: handleRefactorReport,
      snap_refactor_gate: handleRefactorSetGate,
      snap_refactor_stats: handleRefactorStats,
      // =============================================================================
      // LIFECYCLE (unified naming)
      // =============================================================================
      snap_gc: handleLearningGC,
      // =============================================================================
      // INTELLIGENCE PILLARS (Pillar 06: Pre-Write Interception)
      // =============================================================================
      safe_to_write: handleSafeToWrite,
      snap_safe: handleSafeToWrite,
      // =============================================================================
      // V2 BLACK-BOX INTELLIGENCE SURFACE (3-tool surface)
      // =============================================================================
      ...V2_HANDLERS
    };
    __name8(isConsolidatedTool, "isConsolidatedTool");
    __name8(getConsolidatedHandler, "getConsolidatedHandler");
  }
});
init_session_adapter();
init_session_adapter();
var THREAT_PATTERNS = {
  critical: [
    {
      pattern: /rm\s+-rf/gi,
      description: "destructive rm -rf command",
      severity: 10
    },
    {
      pattern: /DROP\s+TABLE/gi,
      description: "DROP TABLE statement",
      severity: 10
    },
    {
      pattern: /TRUNCATE\s+TABLE/gi,
      description: "TRUNCATE TABLE statement",
      severity: 10
    }
  ],
  high: [
    {
      pattern: /password\s*[:=]\s*['"][^'"]+['"]/,
      description: "hardcoded password",
      severity: 8
    },
    {
      pattern: /AKIA[A-Z0-9]{16}/,
      description: "AWS access key",
      severity: 9
    },
    {
      pattern: /ghp_[a-zA-Z0-9]{36}/,
      description: "GitHub token",
      severity: 9
    },
    {
      pattern: /eval\(/,
      description: "eval() usage",
      severity: 8
    }
  ],
  medium: [
    {
      pattern: /jest\.mock\(/,
      description: "jest.mock in production",
      severity: 5
    },
    {
      pattern: /vi\.mock\(/,
      description: "vitest mock in production",
      severity: 5
    },
    {
      pattern: /exec\(/,
      description: "exec() command execution",
      severity: 5
    }
  ]
};
var AnalyzeBeforeApplyInputSchema = z.object({
  changes: z.array(z.object({
    added: z.boolean().optional().default(false),
    removed: z.boolean().optional().default(false),
    value: z.string(),
    count: z.number().optional()
  }))
});
async function analyzeBeforeApply(changes) {
  const parsed = AnalyzeBeforeApplyInputSchema.parse({
    changes
  });
  const content = parsed.changes.map((change) => change.value).join("\n");
  try {
    const threats = [];
    for (const level of [
      "critical",
      "high",
      "medium"
    ]) {
      for (const threat of THREAT_PATTERNS[level]) {
        threat.pattern.lastIndex = 0;
        if (threat.pattern.test(content)) {
          threats.push({
            description: threat.description,
            severity: threat.severity
          });
        }
      }
    }
    const riskScore = threats.length > 0 ? Math.max(...threats.map((t) => t.severity)) : 0;
    const decision = riskScore >= 5 ? "Review" : "Apply";
    return {
      decision,
      riskScore,
      reasons: threats.map((t) => t.description),
      recommendations: threats.length > 0 ? [
        "Review detected issues before applying",
        "Consider security implications"
      ] : []
    };
  } catch (error) {
    return {
      decision: "Review",
      riskScore: 10,
      reasons: [
        `Analysis error: ${error instanceof Error ? error.message : "Unknown error"}`
      ],
      recommendations: [
        "Review changes manually due to analysis failure"
      ]
    };
  }
}
__name(analyzeBeforeApply, "analyzeBeforeApply");
__name8(analyzeBeforeApply, "analyzeBeforeApply");
function formatAnalysisResult(result7) {
  const decisionText = result7.decision === "Apply" ? "\u2705 Safe to Apply" : "\u26A0\uFE0F  Review Required";
  let output = `${decisionText} (Risk Score: ${result7.riskScore}/10)

`;
  if (result7.reasons.length > 0) {
    output += "Reasons:\n";
    for (const reason of result7.reasons) {
      output += `  \u2022 ${reason}
`;
    }
    output += "\n";
  }
  if (result7.recommendations.length > 0) {
    output += "Recommendations:\n";
    for (const recommendation of result7.recommendations) {
      output += `  \u2022 ${recommendation}
`;
    }
    output += "\n";
  }
  return output;
}
__name(formatAnalysisResult, "formatAnalysisResult");
__name8(formatAnalysisResult, "formatAnalysisResult");
var MCP_ROUTES = {
  // Session lifecycle
  "mcp.startSession": {
    method: "POST",
    path: "/v1/mcp/session/start"
  },
  "mcp.endSession": {
    method: "POST",
    path: "/v1/mcp/session/end"
  },
  "mcp.getSessionStats": {
    method: "GET",
    path: "/v1/mcp/session/stats"
  },
  // Recommendations & Learning
  "mcp.getRecommendations": {
    method: "POST",
    path: "/v1/mcp/recommendations"
  },
  "mcp.recordLearning": {
    method: "POST",
    path: "/v1/mcp/learning/record"
  },
  "mcp.recordActivity": {
    method: "POST",
    path: "/v1/mcp/activity/record"
  },
  // Analysis (API-backed)
  "mcp.analyzeRisk": {
    method: "POST",
    path: "/v1/analysis/risk"
  },
  "mcp.validatePackage": {
    method: "POST",
    path: "/v1/analysis/package"
  },
  // Snapshots (Pro tier) - NOW USING oRPC ENDPOINTS
  // These route to the proper oRPC snapshotsRouter with database persistence
  "mcp.createSnapshot": {
    method: "POST",
    path: "/api/snapshots/create"
  },
  "mcp.listSnapshots": {
    method: "GET",
    path: "/api/snapshots/list"
  },
  "mcp.restoreSnapshot": {
    method: "POST",
    path: "/api/snapshots/restore"
  },
  // Tool execution (generic endpoint)
  "mcp.execute": {
    method: "POST",
    path: "/v1/mcp/execute"
  }
};
(class {
  static {
    __name(this, "VrekoAPIClient");
  }
  static {
    __name8(this, "VrekoAPIClient");
  }
  client;
  timeoutMs;
  constructor(config2) {
    this.timeoutMs = config2.timeout ?? 3e4;
    this.client = ky.create({
      prefixUrl: config2.baseUrl,
      timeout: this.timeoutMs,
      retry: {
        limit: 0
      },
      hooks: {
        beforeRequest: [
          (request) => {
            request.headers.set("Authorization", `Bearer ${config2.apiKey}`);
            request.headers.set("Accept", "application/json");
          }
        ]
      },
      ...config2.fetch && {
        fetch: config2.fetch
      }
    });
  }
  /**
  * Type-safe request using route registry
  */
  async call(route, params) {
    const { method, path: path4 } = MCP_ROUTES[route];
    const options = {
      method
    };
    if (params && method !== "GET") {
      options.body = JSON.stringify(params);
      options.headers = {
        "Content-Type": "application/json"
      };
    }
    return this.fetchWithRetry(path4, options);
  }
  /**
  * Legacy request method - maps old names to routes
  * @deprecated Use call() with route keys instead
  */
  async request(method, params) {
    if (method in MCP_ROUTES) {
      return this.call(method, params);
    }
    return this.call("mcp.execute", {
      tool: method,
      args: params
    });
  }
  async fetchWithRetry(endpoint, options, maxAttempts = 3) {
    const wrappedFetch = withBreaker("vreko-api-client", (input) => this.doFetch(input.endpoint, input.options), {
      timeoutMs: this.timeoutMs,
      errorThresholdPercentage: 50,
      volumeThreshold: 5,
      resetMs: 3e4,
      rollingCountMs: 6e4,
      rollingCountBuckets: 6
    });
    let lastError;
    for (let attempt = 1; attempt <= maxAttempts; attempt++) {
      try {
        return await wrappedFetch({
          endpoint,
          options
        });
      } catch (error) {
        lastError = error instanceof Error ? error : new Error(String(error));
        if (attempt < maxAttempts) {
          await this.delay(100 * 2 ** (attempt - 1));
        }
      }
    }
    throw lastError ?? new Error("Request failed");
  }
  async doFetch(endpoint, options) {
    const url = endpoint.startsWith("/") ? endpoint.slice(1) : endpoint;
    try {
      const response = await this.client(url, options);
      return response.json();
    } catch (error) {
      if (error instanceof HTTPError) {
        const status = error.response?.status || 500;
        const statusText = error.response?.statusText || "Unknown Error";
        throw new Error(`API error: ${status} ${statusText}`);
      }
      throw error;
    }
  }
  delay(ms) {
    return new Promise((resolve72) => setTimeout(resolve72, ms));
  }
  /**
  * Get circuit breaker state for monitoring
  */
  getCircuitState() {
    const state = getCircuitBreakerState("vreko-api-client");
    if (!state) {
      return "closed";
    }
    if (state.isOpen) {
      return "open";
    }
    if (state.isHalfOpen) {
      return "half-open";
    }
    return "closed";
  }
});
init_branding();
init_session_adapter();
var BridgeReceiver = class {
  static {
    __name(this, "BridgeReceiver");
  }
  static {
    __name8(this, "BridgeReceiver");
  }
  server = null;
  port;
  host;
  defaultWorkspaceRoot;
  // Stats
  observationsReceived = 0;
  changesReceived = 0;
  lastActivity = null;
  constructor(config2 = {}) {
    this.port = config2.port ?? 3100;
    this.host = config2.host ?? "127.0.0.1";
    this.defaultWorkspaceRoot = config2.defaultWorkspaceRoot ?? process.cwd();
  }
  /**
  * Start the bridge receiver
  */
  async start() {
    if (this.server) {
      return;
    }
    return new Promise((resolve72, reject) => {
      this.server = createServer((req, res) => this.handleRequest(req, res));
      this.server.on("error", (err22) => {
        reject(err22);
      });
      this.server.listen(this.port, this.host, () => {
        resolve72();
      });
    });
  }
  /**
  * Stop the bridge receiver
  */
  async stop() {
    if (!this.server) {
      return;
    }
    return new Promise((resolve72) => {
      this.server?.close(() => {
        this.server = null;
        resolve72();
      });
    });
  }
  /**
  * Get receiver status
  */
  getStatus() {
    return {
      running: this.server !== null,
      port: this.port,
      host: this.host,
      observationsReceived: this.observationsReceived,
      changesReceived: this.changesReceived,
      lastActivity: this.lastActivity
    };
  }
  /**
  * Handle incoming HTTP request
  */
  handleRequest(req, res) {
    res.setHeader("Access-Control-Allow-Origin", "*");
    res.setHeader("Access-Control-Allow-Methods", "POST, GET, OPTIONS");
    res.setHeader("Access-Control-Allow-Headers", "Content-Type");
    if (req.method === "OPTIONS") {
      res.writeHead(204);
      res.end();
      return;
    }
    const url = req.url ?? "/";
    if (url === "/bridge/push" && req.method === "POST") {
      this.handlePush(req, res);
    } else if (url === "/bridge/status" && req.method === "GET") {
      this.handleStatus(res);
    } else if (url === "/bridge/health" && req.method === "GET") {
      this.handleHealth(res);
    } else {
      this.sendJson(res, 404, {
        error: "Not found"
      });
    }
  }
  /**
  * Handle /bridge/push - receive observations and changes
  */
  handlePush(req, res) {
    let body = "";
    req.on("data", (chunk) => {
      body += chunk;
      if (body.length > 1024 * 1024) {
        this.sendJson(res, 413, {
          error: "Payload too large"
        });
        req.destroy();
      }
    });
    req.on("end", async () => {
      try {
        const payload = JSON.parse(body);
        await this.processPush(payload);
        this.lastActivity = Date.now();
        this.sendJson(res, 200, {
          received: true,
          observationsCount: payload.observations?.length ?? 0,
          changesCount: payload.changes?.length ?? 0
        });
      } catch (error) {
        const message = error instanceof Error ? error.message : "Unknown error";
        this.sendJson(res, 400, {
          error: `Invalid payload: ${message}`
        });
      }
    });
    req.on("error", (_err) => {
      this.sendJson(res, 500, {
        error: "Request error"
      });
    });
  }
  /**
  * Process push payload
  */
  async processPush(payload) {
    const workspaceRoot = payload.workspaceRoot ?? this.defaultWorkspaceRoot;
    if (payload.observations && payload.observations.length > 0) {
      for (const observation of payload.observations) {
        await pushObservationAsync(workspaceRoot, observation);
        this.observationsReceived++;
      }
    }
    if (payload.changes && payload.changes.length > 0) {
      for (const change of payload.changes) {
        await recordFileChangeAsync(workspaceRoot, change);
        this.changesReceived++;
      }
    }
  }
  /**
  * Handle /bridge/status - return bridge status
  */
  handleStatus(res) {
    const status = this.getStatus();
    this.sendJson(res, 200, status);
  }
  /**
  * Handle /bridge/health - health check endpoint
  */
  handleHealth(res) {
    this.sendJson(res, 200, {
      healthy: true,
      timestamp: Date.now()
    });
  }
  /**
  * Send JSON response
  */
  sendJson(res, status, data) {
    res.writeHead(status, {
      "Content-Type": "application/json"
    });
    res.end(JSON.stringify(data));
  }
};
var bridgeReceiver = null;
function getBridgeReceiver(config2) {
  if (!bridgeReceiver) {
    bridgeReceiver = new BridgeReceiver(config2);
  }
  return bridgeReceiver;
}
__name(getBridgeReceiver, "getBridgeReceiver");
__name8(getBridgeReceiver, "getBridgeReceiver");
async function startBridgeReceiver(config2) {
  const receiver = getBridgeReceiver(config2);
  await receiver.start();
  return receiver;
}
__name(startBridgeReceiver, "startBridgeReceiver");
__name8(startBridgeReceiver, "startBridgeReceiver");
async function stopBridgeReceiver() {
  if (bridgeReceiver) {
    await bridgeReceiver.stop();
    bridgeReceiver = null;
  }
}
__name(stopBridgeReceiver, "stopBridgeReceiver");
__name8(stopBridgeReceiver, "stopBridgeReceiver");
function createRiskObservation(file, reason) {
  return {
    type: "risk",
    message: `High-risk file modified: ${file} (${reason})`,
    timestamp: Date.now(),
    context: {
      file,
      riskLevel: "high"
    }
  };
}
__name(createRiskObservation, "createRiskObservation");
__name8(createRiskObservation, "createRiskObservation");
function createPatternObservation(patternName, message, file) {
  return {
    type: "pattern",
    message,
    timestamp: Date.now(),
    context: {
      patternName,
      file
    }
  };
}
__name(createPatternObservation, "createPatternObservation");
__name8(createPatternObservation, "createPatternObservation");
function createWarningObservation(message, context) {
  return {
    type: "warning",
    message,
    timestamp: Date.now(),
    context
  };
}
__name(createWarningObservation, "createWarningObservation");
__name8(createWarningObservation, "createWarningObservation");
function createSuggestionObservation(message, context) {
  return {
    type: "suggestion",
    message,
    timestamp: Date.now(),
    context
  };
}
__name(createSuggestionObservation, "createSuggestionObservation");
__name8(createSuggestionObservation, "createSuggestionObservation");
function createProgressObservation(message) {
  return {
    type: "progress",
    message,
    timestamp: Date.now()
  };
}
__name(createProgressObservation, "createProgressObservation");
__name8(createProgressObservation, "createProgressObservation");
var DEFAULT_REMOTE_URL = "https://mcp.vreko.dev/mcp";
var DEFAULT_TIMEOUT_MS = 3e4;
var MAX_RETRIES = 2;
var RETRY_STATUS_CODES = [
  502,
  503,
  504
];
var REMOTE_ONLY_TOOLS = [
  {
    name: "vreko_get_risk_score",
    description: "Intelligence tool (remote): Get risk score for code change",
    inputSchema: {
      type: "object",
      properties: {
        filePath: {
          type: "string",
          description: "Path to the file being changed"
        },
        changeType: {
          type: "string",
          description: "Type of change (add, modify, delete)"
        }
      }
    }
  }
];
function resolveRemoteConfig(config2) {
  if (!config2.apiKey && !config2.sessionToken) {
    return null;
  }
  if (!config2.workspaceRoot) {
    return null;
  }
  return {
    url: config2.url ?? DEFAULT_REMOTE_URL,
    apiKey: config2.apiKey,
    sessionToken: config2.sessionToken,
    sessionType: config2.sessionType ?? "bearer",
    workspaceId: config2.workspaceId,
    workspaceRoot: config2.workspaceRoot,
    timeoutMs: config2.timeoutMs ?? DEFAULT_TIMEOUT_MS
  };
}
__name(resolveRemoteConfig, "resolveRemoteConfig");
__name8(resolveRemoteConfig, "resolveRemoteConfig");
function createRemoteClient(config2) {
  const headers = {
    "Content-Type": "application/json",
    "X-Workspace-Path": config2.workspaceRoot
  };
  if (config2.sessionToken) {
    const authPrefix = config2.sessionType === "api_key" ? "ApiKey" : "Bearer";
    headers.Authorization = `${authPrefix} ${config2.sessionToken}`;
  } else if (config2.apiKey) {
    headers["X-API-Key"] = config2.apiKey;
  }
  if (config2.workspaceId) {
    headers["X-Workspace-ID"] = config2.workspaceId;
  }
  const client = ky.create({
    prefixUrl: config2.url,
    timeout: config2.timeoutMs,
    retry: {
      limit: MAX_RETRIES,
      methods: [
        "post"
      ],
      statusCodes: RETRY_STATUS_CODES
    },
    headers
  });
  return {
    /**
    * Invoke a remote tool via JSON-RPC.
    *
    * @param toolName - Name of the tool to invoke
    * @param args - Tool arguments
    * @returns Tool result in MCP format
    */
    async invoke(toolName, args) {
      try {
        const response = await client.post("mcp", {
          json: {
            jsonrpc: "2.0",
            method: "tools/call",
            params: {
              name: toolName,
              arguments: args
            },
            id: Date.now()
          }
        }).json();
        if (response.error) {
          return {
            content: [
              {
                type: "text",
                text: `Remote error: ${response.error.message}`
              }
            ],
            isError: true
          };
        }
        return {
          content: response.result?.content ?? [
            {
              type: "text",
              text: "No content returned from remote"
            }
          ]
        };
      } catch (error) {
        return handleRemoteError(error);
      }
    },
    /**
    * Check if remote API is healthy.
    *
    * @returns true if API responds to health check
    */
    async healthCheck() {
      try {
        const response = await client.get("health");
        return response.ok;
      } catch {
        return false;
      }
    },
    /**
    * Get list of remote-only tool definitions.
    */
    getToolDefinitions() {
      return REMOTE_ONLY_TOOLS;
    },
    /**
    * Get the resolved configuration (for debugging).
    */
    getConfig() {
      return config2;
    }
  };
}
__name(createRemoteClient, "createRemoteClient");
__name8(createRemoteClient, "createRemoteClient");
function handleRemoteError(error) {
  if (isHTTPError(error)) {
    const message2 = error.message || "HTTP error";
    return {
      content: [
        {
          type: "text",
          text: `Intelligence unavailable: ${message2}

The remote intelligence server is currently unavailable. Local tools are still available.`
        }
      ],
      isError: true
    };
  }
  const message = error instanceof Error ? error.message : "Unknown error";
  return {
    content: [
      {
        type: "text",
        text: `Remote bridge failed: ${message}`
      }
    ],
    isError: true
  };
}
__name(handleRemoteError, "handleRemoteError");
__name8(handleRemoteError, "handleRemoteError");
function isHTTPError(error) {
  return typeof error === "object" && error !== null && "response" in error && "request" in error;
}
__name(isHTTPError, "isHTTPError");
__name8(isHTTPError, "isHTTPError");
var REMOTE_ONLY_TOOL_NAMES = new Set(REMOTE_ONLY_TOOLS.map((t) => t.name));
var REMOTE_ENHANCED_TOOLS = /* @__PURE__ */ new Set([]);
var DAEMON_PREFERRED_TOOLS = /* @__PURE__ */ new Set([
  "vreko",
  "vreko_learn",
  "vreko_end",
  "vreko_pulse",
  "vreko",
  "check",
  "advise",
  "guide",
  "pulse"
]);
function createBackendRouter(remoteConfig) {
  return {
    /**
    * Determine backend for a tool call.
    *
    * Routing rules:
    * 1. If no remote config → always daemon
    * 2. If tool is in REMOTE_ONLY_TOOL_NAMES → remote
    * 3. If tool is in REMOTE_ENHANCED_TOOLS → remote (with fallback)
    * 4. If tool is in DAEMON_PREFERRED_TOOLS → daemon
    * 5. Default → daemon
    *
    * @param toolName - Name of the tool being called
    * @returns Backend target
    */
    route(toolName) {
      if (!remoteConfig) {
        return "daemon";
      }
      if (REMOTE_ONLY_TOOL_NAMES.has(toolName)) {
        return "remote";
      }
      if (REMOTE_ENHANCED_TOOLS.has(toolName)) {
        return "remote";
      }
      if (DAEMON_PREFERRED_TOOLS.has(toolName)) {
        return "daemon";
      }
      return "daemon";
    },
    /**
    * Check if remote backend is configured.
    */
    hasRemote() {
      return remoteConfig !== null;
    },
    /**
    * Get list of remote-only tool names.
    */
    getRemoteTools() {
      return Array.from(REMOTE_ONLY_TOOL_NAMES);
    }
  };
}
__name(createBackendRouter, "createBackendRouter");
__name8(createBackendRouter, "createBackendRouter");
function isRemoteOnlyTool(toolName) {
  return REMOTE_ONLY_TOOL_NAMES.has(toolName);
}
__name(isRemoteOnlyTool, "isRemoteOnlyTool");
__name8(isRemoteOnlyTool, "isRemoteOnlyTool");
function isDaemonPreferredTool(toolName) {
  return DAEMON_PREFERRED_TOOLS.has(toolName);
}
__name(isDaemonPreferredTool, "isDaemonPreferredTool");
__name8(isDaemonPreferredTool, "isDaemonPreferredTool");
function getRemoteToolNames() {
  return Array.from(REMOTE_ONLY_TOOL_NAMES);
}
__name(getRemoteToolNames, "getRemoteToolNames");
__name8(getRemoteToolNames, "getRemoteToolNames");
var defaultLogger = {
  debug: /* @__PURE__ */ __name8((msg, meta) => process.stderr.write(`[SSE] DEBUG ${msg} ${meta ? JSON.stringify(meta) : ""}
`), "debug"),
  info: /* @__PURE__ */ __name8((msg, meta) => process.stderr.write(`[SSE] ${msg} ${meta ? JSON.stringify(meta) : ""}
`), "info"),
  warn: /* @__PURE__ */ __name8((msg, meta) => process.stderr.write(`[SSE] WARN ${msg} ${meta ? JSON.stringify(meta) : ""}
`), "warn"),
  error: /* @__PURE__ */ __name8((msg, meta) => process.stderr.write(`[SSE] ERROR ${msg} ${meta ? JSON.stringify(meta) : ""}
`), "error")
};
var PATH_TRAVERSAL_PATTERNS = [
  /\.\./,
  /\.\.\//,
  /\.\.\\/,
  /^\/etc/,
  /^\/proc/,
  /^\/sys/,
  /^\/dev/,
  /^\/root/,
  /^\//
];
var ALLOWED_WORKSPACE_PREFIXES = [
  process.env.HOME ?? "",
  "/Users/",
  "/home/",
  "/Volumes/"
];
function isValidWorkspacePath(workspace) {
  if (!workspace || !workspace.trim()) {
    return false;
  }
  for (const pattern of PATH_TRAVERSAL_PATTERNS) {
    if (pattern.test(workspace)) {
      return false;
    }
  }
  const isAllowedPrefix = ALLOWED_WORKSPACE_PREFIXES.some((prefix) => prefix && workspace.startsWith(prefix));
  const isRelativePath = !workspace.startsWith("/");
  return isAllowedPrefix || isRelativePath;
}
__name(isValidWorkspacePath, "isValidWorkspacePath");
__name8(isValidWorkspacePath, "isValidWorkspacePath");
function isValidLocalhostOrigin(origin) {
  if (!origin) {
    return true;
  }
  try {
    const url = new URL(origin);
    return url.hostname === "localhost" || url.hostname === "127.0.0.1" || url.hostname === "[::1]";
  } catch {
    return false;
  }
}
__name(isValidLocalhostOrigin, "isValidLocalhostOrigin");
__name8(isValidLocalhostOrigin, "isValidLocalhostOrigin");
var SSELocalTransport = class extends EventEmitter {
  static {
    __name(this, "SSELocalTransport");
  }
  static {
    __name8(this, "SSELocalTransport");
  }
  server = null;
  clients = /* @__PURE__ */ new Map();
  token = null;
  pingTimer = null;
  config;
  logger;
  constructor(config2) {
    super();
    this.logger = config2?.logger ?? defaultLogger;
    this.config = {
      port: 8765,
      maxClients: 10,
      idleTimeoutMs: 15 * 60 * 1e3,
      pingIntervalMs: 30 * 1e3,
      maxMissedPongs: 3,
      tokenPath: join(homedir(), ".vreko", "mcp-token"),
      ...config2
    };
  }
  /**
  * Start the SSE transport server
  */
  async start() {
    if (this.server) {
      throw new Error("SSE transport already started");
    }
    await this.ensureToken();
    this.server = createServer((req, res) => {
      res.setHeader("Access-Control-Allow-Origin", "*");
      res.setHeader("Access-Control-Allow-Headers", "Authorization, Content-Type");
      res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
      if (req.method === "OPTIONS") {
        res.writeHead(200);
        res.end();
        return;
      }
      if (req.url === "/events" && req.method === "GET") {
        this.handleConnect(req, res);
      } else if (req.url === "/pong" && req.method === "POST") {
        this.handlePongRequest(req, res);
      } else if (req.url === "/health" && req.method === "GET") {
        res.writeHead(200, {
          "Content-Type": "application/json"
        });
        res.end(JSON.stringify({
          status: "ok",
          clients: this.clients.size
        }));
      } else if (req.url === "/digest" && req.method === "POST") {
        this.handleDigestRequest(req, res);
      } else if (req.url === "/session/list" && req.method === "POST") {
        this.handleSessionListRequest(req, res);
      } else {
        res.writeHead(404);
        res.end("Not Found");
      }
    });
    return new Promise((resolve72, reject) => {
      this.server?.listen(this.config.port, "127.0.0.1", () => {
        this.logger.info(`Listening on localhost:${this.config.port}`);
        this.startPingLoop();
        resolve72();
      });
      this.server?.on("error", reject);
    });
  }
  /**
  * Stop the SSE transport server
  */
  async stop() {
    if (this.pingTimer) {
      clearInterval(this.pingTimer);
      this.pingTimer = null;
    }
    for (const [clientId, client] of this.clients.entries()) {
      this.sendEvent(client, "disconnect", {
        reason: "server_shutdown"
      });
      client.res.end();
      this.clients.delete(clientId);
    }
    if (this.server) {
      return new Promise((resolve72, reject) => {
        this.server?.close((err22) => {
          if (err22) {
            reject(err22);
          } else {
            this.server = null;
            this.logger.info("Transport stopped");
            resolve72();
          }
        });
      });
    }
  }
  /**
  * Broadcast an event to all connected clients
  */
  broadcast(event, data) {
    const deadClients = [];
    for (const [clientId, client] of this.clients.entries()) {
      try {
        this.sendEvent(client, event, data);
      } catch (err22) {
        this.logger.error(`Failed to send to client ${clientId}`, {
          error: String(err22)
        });
        deadClients.push(clientId);
      }
    }
    for (const clientId of deadClients) {
      this.removeClient(clientId);
    }
  }
  /**
  * Get current client count
  */
  get clientCount() {
    return this.clients.size;
  }
  /**
  * Check if daemon should suppress idle shutdown
  * Returns true when clients are connected
  */
  shouldSuppressIdleShutdown() {
    return this.clients.size > 0;
  }
  // =========================================================================
  // Private Methods
  // =========================================================================
  /**
  * Ensure token exists, generate if not
  */
  async ensureToken() {
    try {
      await access(this.config.tokenPath);
      this.token = (await readFile(this.config.tokenPath, "utf8")).trim();
    } catch {
      await mkdir(dirname(this.config.tokenPath), {
        recursive: true
      });
      this.token = randomBytes(32).toString("hex");
      await writeFile(this.config.tokenPath, this.token, {
        mode: 384
      });
      await chmod(this.config.tokenPath, 384);
    }
    return this.token;
  }
  /**
  * Validate authentication using unified auth pattern
  *
  * Supports both:
  * 1. API keys (sk_live_xxx, sk_test_xxx) - validates against Better Auth
  * 2. Local tokens - validates against ~/.vreko/mcp-token
  *
  * @param token - The token to validate
  * @returns Auth result with user context if valid
  */
  async validateAuth(token) {
    if (token.startsWith("sk_live_") || token.startsWith("sk_test_")) {
      if (this.config.apiKeyValidator) {
        try {
          const verified = await this.config.apiKeyValidator.verifyApiKey(token);
          if (verified?.isValid && verified?.userId) {
            let tier = "free";
            if (verified.metadata?.tier === "admin") {
              tier = "admin";
            } else if (verified.metadata?.tier === "pro" || verified.metadata?.plan === "pro") {
              tier = "pro";
            }
            return {
              valid: true,
              authType: "apiKey",
              userId: verified.userId,
              tier,
              permissions: Object.keys(verified.permissions || {})
            };
          }
        } catch (err22) {
          this.logger.warn("API key validation failed", {
            error: String(err22)
          });
        }
      }
      return {
        valid: false,
        authType: "apiKey"
      };
    }
    if (this.token && token === this.token) {
      return {
        valid: true,
        authType: "localToken"
      };
    }
    return {
      valid: false,
      authType: "localToken"
    };
  }
  /**
  * Handle new SSE connection with unified auth
  */
  handleConnect(req, res) {
    if (this.clients.size >= this.config.maxClients) {
      res.writeHead(503, {
        "Content-Type": "text/plain"
      });
      res.end("Max clients reached");
      return;
    }
    const authHeader = req.headers.authorization;
    if (!authHeader || !authHeader.startsWith("Bearer ")) {
      res.writeHead(401, {
        "Content-Type": "text/plain"
      });
      res.end("Unauthorized: Missing bearer token");
      return;
    }
    const providedToken = authHeader.substring(7);
    this.validateAuth(providedToken).then((authResult) => {
      if (!authResult.valid) {
        res.writeHead(403, {
          "Content-Type": "text/plain"
        });
        res.end("Forbidden: Invalid token");
        return;
      }
      res.writeHead(200, {
        "Content-Type": "text/event-stream",
        "Cache-Control": "no-cache",
        Connection: "keep-alive"
      });
      const clientId = randomBytes(16).toString("hex");
      const now = Date.now();
      const client = {
        id: clientId,
        res,
        lastPong: now,
        missedPongs: 0,
        createdAt: now,
        authType: authResult.authType,
        userId: authResult.userId,
        tier: authResult.tier
      };
      this.clients.set(clientId, client);
      this.sendEvent(client, "connected", {
        clientId,
        maxIdleMs: this.config.idleTimeoutMs,
        authType: authResult.authType,
        userId: authResult.userId
      });
      req.on("close", () => {
        this.removeClient(clientId);
      });
      this.emit("client:connected", clientId, authResult);
      this.logger.info("Client connected", {
        clientId,
        count: this.clients.size,
        max: this.config.maxClients,
        authType: authResult.authType,
        userId: authResult.userId
      });
    }).catch((err22) => {
      this.logger.error("Auth validation failed", {
        error: String(err22)
      });
      res.writeHead(500, {
        "Content-Type": "text/plain"
      });
      res.end("Internal server error during auth validation");
    });
  }
  /**
  * Handle pong request from client
  */
  handlePongRequest(req, res) {
    let body = "";
    req.on("data", (chunk) => {
      body += chunk.toString();
    });
    req.on("end", () => {
      try {
        const { clientId } = JSON.parse(body);
        this.handlePong(clientId);
        res.writeHead(200);
        res.end("OK");
      } catch {
        res.writeHead(400);
        res.end("Bad Request");
      }
    });
  }
  /**
  * Handle digest request for Pioneer Dashboard
  * Direct local-first access from web console on localhost
  *
  * Security measures:
  * - Origin header validation (CSRF protection)
  * - Path traversal prevention
  * - Request timeout (5 seconds)
  */
  handleDigestRequest(req, res) {
    const origin = req.headers.origin;
    if (!isValidLocalhostOrigin(origin)) {
      this.logger.warn("Digest request rejected - invalid origin", {
        origin
      });
      res.writeHead(403, {
        "Content-Type": "application/json"
      });
      res.end(JSON.stringify({
        status: "error",
        code: "UNAUTHORIZED",
        message: "Access denied - localhost only"
      }));
      return;
    }
    const digestService = this.config.digestService;
    if (!digestService) {
      res.writeHead(503, {
        "Content-Type": "application/json"
      });
      res.end(JSON.stringify({
        status: "error",
        code: "INTERNAL_ERROR",
        message: "Digest service not available"
      }));
      return;
    }
    const TIMEOUT_MS = 5e3;
    let timeoutId = null;
    let responded = false;
    const sendResponse = /* @__PURE__ */ __name8((statusCode, data) => {
      if (responded) {
        return;
      }
      responded = true;
      if (timeoutId) {
        clearTimeout(timeoutId);
      }
      res.writeHead(statusCode, {
        "Content-Type": "application/json"
      });
      res.end(JSON.stringify(data));
    }, "sendResponse");
    timeoutId = setTimeout(() => {
      sendResponse(504, {
        status: "error",
        code: "INTERNAL_ERROR",
        message: "Request timeout"
      });
    }, TIMEOUT_MS);
    let body = "";
    req.on("data", (chunk) => {
      body += chunk.toString();
    });
    req.on("end", async () => {
      try {
        const params = JSON.parse(body);
        if (!params.workspace || typeof params.workspace !== "string") {
          sendResponse(400, {
            status: "error",
            code: "NOT_FOUND",
            message: "Missing or invalid 'workspace' parameter"
          });
          return;
        }
        if (!isValidWorkspacePath(params.workspace)) {
          this.logger.warn("Digest request rejected - invalid workspace path", {
            workspace: params.workspace
          });
          sendResponse(400, {
            status: "error",
            code: "NOT_FOUND",
            message: "Invalid workspace path"
          });
          return;
        }
        const result7 = await digestService.compute({
          workspace: params.workspace,
          force: params.force ?? false,
          user: params.user,
          sessionMetrics: params.sessionMetrics
        });
        sendResponse(200, result7);
      } catch (error) {
        this.logger.error("Digest request failed", {
          error: String(error)
        });
        sendResponse(500, {
          status: "error",
          code: "INTERNAL_ERROR",
          message: error instanceof Error ? error.message : "Unknown error"
        });
      }
    });
    req.on("error", (error) => {
      this.logger.error("Digest request stream error", {
        error: String(error)
      });
      sendResponse(500, {
        status: "error",
        code: "INTERNAL_ERROR",
        message: "Request stream error"
      });
    });
  }
  /**
  * Handle POST /session/list  -  exposes session history to web dashboard.
  * Localhost-only: validates Origin header against allowed origins.
  */
  handleSessionListRequest(req, res) {
    const origin = req.headers.origin;
    if (!isValidLocalhostOrigin(origin)) {
      this.logger.warn("Session list request rejected - invalid origin", {
        origin
      });
      res.writeHead(403, {
        "Content-Type": "application/json"
      });
      res.end(JSON.stringify({
        status: "error",
        code: "UNAUTHORIZED",
        message: "Access denied - localhost only"
      }));
      return;
    }
    const sessionService = this.config.sessionService;
    if (!sessionService) {
      res.writeHead(503, {
        "Content-Type": "application/json"
      });
      res.end(JSON.stringify({
        status: "error",
        code: "INTERNAL_ERROR",
        message: "Session service not available"
      }));
      return;
    }
    const TIMEOUT_MS = 5e3;
    let timeoutId = null;
    let responded = false;
    const sendResponse = /* @__PURE__ */ __name8((statusCode, data) => {
      if (responded) {
        return;
      }
      responded = true;
      if (timeoutId) {
        clearTimeout(timeoutId);
      }
      res.writeHead(statusCode, {
        "Content-Type": "application/json"
      });
      res.end(JSON.stringify(data));
    }, "sendResponse");
    timeoutId = setTimeout(() => {
      sendResponse(504, {
        status: "error",
        code: "INTERNAL_ERROR",
        message: "Request timeout"
      });
    }, TIMEOUT_MS);
    let body = "";
    req.on("data", (chunk) => {
      body += chunk.toString();
    });
    req.on("end", async () => {
      try {
        const params = body ? JSON.parse(body) : {};
        const result7 = await sessionService.list({
          limit: params.limit ?? 50,
          includeEnded: params.includeEnded ?? true,
          workspacePath: params.workspacePath
        });
        sendResponse(200, {
          sessions: result7.sessions,
          nextCursor: result7.hasMore ? String((params.cursor ?? 0) + 1) : null,
          total: result7.sessions.length
        });
      } catch (error) {
        this.logger.error("Session list request failed", {
          error: String(error)
        });
        sendResponse(500, {
          status: "error",
          code: "INTERNAL_ERROR",
          message: error instanceof Error ? error.message : "Unknown error"
        });
      }
    });
    req.on("error", (error) => {
      this.logger.error("Session list request stream error", {
        error: String(error)
      });
      sendResponse(500, {
        status: "error",
        code: "INTERNAL_ERROR",
        message: "Request stream error"
      });
    });
  }
  /**
  * Process pong from client
  */
  handlePong(clientId) {
    const client = this.clients.get(clientId);
    if (!client) {
      return;
    }
    client.lastPong = Date.now();
    client.missedPongs = 0;
  }
  /**
  * Start the ping loop for keepalive
  */
  startPingLoop() {
    if (this.pingTimer) {
      return;
    }
    this.pingTimer = setInterval(() => {
      const now = Date.now();
      const deadClients = [];
      for (const [clientId, client] of this.clients.entries()) {
        const idleMs = now - client.lastPong;
        if (idleMs > this.config.idleTimeoutMs) {
          this.logger.info("Client idle, disconnecting", {
            clientId,
            idleSec: Math.round(idleMs / 1e3)
          });
          deadClients.push(clientId);
          continue;
        }
        if (client.missedPongs >= this.config.maxMissedPongs) {
          this.logger.info("Client missed pongs, disconnecting", {
            clientId,
            missedPongs: client.missedPongs
          });
          deadClients.push(clientId);
          continue;
        }
        try {
          this.sendEvent(client, "ping", {
            timestamp: now
          });
          client.missedPongs++;
        } catch (err22) {
          this.logger.error("Failed to ping client", {
            clientId,
            error: String(err22)
          });
          deadClients.push(clientId);
        }
      }
      for (const clientId of deadClients) {
        this.removeClient(clientId);
      }
    }, this.config.pingIntervalMs);
  }
  /**
  * Send an SSE event to a client
  */
  sendEvent(client, event, data) {
    const payload = JSON.stringify(data);
    client.res.write(`event: ${event}
`);
    client.res.write(`data: ${payload}

`);
  }
  /**
  * Remove and clean up a client
  */
  removeClient(clientId) {
    const client = this.clients.get(clientId);
    if (!client) {
      return;
    }
    try {
      this.sendEvent(client, "disconnect", {
        reason: "cleanup"
      });
      client.res.end();
    } catch {
    }
    this.clients.delete(clientId);
    this.emit("client:disconnected", clientId);
    this.logger.info("Client disconnected", {
      clientId,
      count: this.clients.size,
      max: this.config.maxClients
    });
  }
};
(class extends EventEmitter {
  static {
    __name(this, "TransportManager");
  }
  static {
    __name8(this, "TransportManager");
  }
  sseTransport = null;
  config;
  constructor(config2) {
    super();
    this.config = {
      ssePort: 8765,
      maxClients: 10,
      idleTimeoutMs: 15 * 60 * 1e3,
      ...config2
    };
  }
  /**
  * Set the digest service for Pioneer Dashboard
  * Can be called after construction but before start()
  */
  setDigestService(digestService) {
    this.config.digestService = digestService;
  }
  /**
  * Set the session service for web dashboard /session/list endpoint
  * Can be called after construction but before start()
  */
  setSessionService(sessionService) {
    this.config.sessionService = sessionService;
  }
  /**
  * Start all transports
  */
  async start() {
    this.sseTransport = new SSELocalTransport({
      port: this.config.ssePort,
      maxClients: this.config.maxClients,
      idleTimeoutMs: this.config.idleTimeoutMs,
      logger: this.config.logger,
      apiKeyValidator: this.config.apiKeyValidator,
      digestService: this.config.digestService,
      sessionService: this.config.sessionService
    });
    this.sseTransport.on("client:connected", (clientId) => {
      const event = {
        transport: "sse",
        clientId
      };
      this.emit("client:connected", event);
    });
    this.sseTransport.on("client:disconnected", (clientId) => {
      const event = {
        transport: "sse",
        clientId
      };
      this.emit("client:disconnected", event);
    });
    await this.sseTransport.start();
  }
  /**
  * Stop all transports
  */
  async stop() {
    if (this.sseTransport) {
      await this.sseTransport.stop();
      this.sseTransport = null;
    }
  }
  /**
  * Broadcast an event to all connected clients
  */
  broadcast(event, data) {
    if (this.sseTransport) {
      this.sseTransport.broadcast(event, data);
    }
  }
  /**
  * Check if any transport has active clients
  * Used by daemon to suppress idle shutdown
  */
  shouldSuppressIdleShutdown() {
    return this.sseTransport?.shouldSuppressIdleShutdown() ?? false;
  }
  /**
  * Get transport statistics
  */
  getStats() {
    return {
      sse: {
        clientCount: this.sseTransport?.clientCount ?? 0
      }
    };
  }
});
init_errors();
init_handlers();
init_intelligence();
init_local_service_adapter();
var PRESSURE_THRESHOLDS = {
  medium: 40,
  high: 60,
  critical: 80
};
async function getSessionHealth(context) {
  const vitalsResult = await getVitalsViaDaemon(context.workspaceRoot);
  const vitals = vitalsResult.success && vitalsResult.vitals ? vitalsResult.vitals : null;
  const snapshots = await listSnapshotsViaDaemon(context.workspaceRoot, 1);
  const lastSnapshot = snapshots[0];
  const pressure = vitals?.pressure ?? 0;
  const healthScore = Math.max(0, Math.min(100, 100 - pressure));
  const activeWarnings = [];
  if (pressure >= PRESSURE_THRESHOLDS.high) {
    activeWarnings.push("High pressure - consider creating a snapshot");
  }
  if (vitals?.snapshotCount === 0) {
    activeWarnings.push("No snapshots in this session");
  }
  const suggestions = [];
  if (pressure >= PRESSURE_THRESHOLDS.medium) {
    suggestions.push("Consider creating a snapshot before making more changes");
  }
  return {
    workspaceId: context.workspaceRoot,
    healthScore,
    trajectory: pressure >= PRESSURE_THRESHOLDS.critical ? "critical" : pressure >= PRESSURE_THRESHOLDS.high ? "degrading" : "stable",
    activeWarnings,
    recentViolations: 0,
    lastSnapshot: lastSnapshot ? {
      id: lastSnapshot.id,
      createdAt: new Date(lastSnapshot.createdAt).toISOString(),
      minutesAgo: Math.floor((Date.now() - new Date(lastSnapshot.createdAt).getTime()) / 6e4)
    } : null,
    suggestions
  };
}
__name(getSessionHealth, "getSessionHealth");
__name8(getSessionHealth, "getSessionHealth");
async function wrapWithSessionHealth(data, context) {
  const session = await getSessionHealth(context);
  return {
    data,
    session
  };
}
__name(wrapWithSessionHealth, "wrapWithSessionHealth");
__name8(wrapWithSessionHealth, "wrapWithSessionHealth");
async function sessionAwareResult(data, context) {
  const enhanced = await wrapWithSessionHealth(data, context);
  return {
    content: [
      {
        type: "text",
        text: JSON.stringify(enhanced, null, 2)
      }
    ],
    isError: false
  };
}
__name(sessionAwareResult, "sessionAwareResult");
__name8(sessionAwareResult, "sessionAwareResult");
function isRiskyState(health) {
  return health.healthScore < 50 || health.trajectory === "critical" || health.trajectory === "degrading" || health.activeWarnings.length > 2;
}
__name(isRiskyState, "isRiskyState");
__name8(isRiskyState, "isRiskyState");
function getRecommendedActions(health, taskContext) {
  const recommendations = [];
  if (health.healthScore < 30 || health.trajectory === "critical") {
    recommendations.push({
      tool: "snapshot_create",
      reason: "Critical health score - create safety snapshot before proceeding",
      priority: "critical"
    });
  }
  if (taskContext?.isNewTask) {
    recommendations.push({
      tool: "get_context",
      reason: "Starting new task - gather patterns, constraints, and relevant learnings",
      priority: "high"
    });
  }
  if (health.recentViolations > 3) {
    recommendations.push({
      tool: "get_learnings",
      reason: `${health.recentViolations} recent violations - review past patterns to avoid repeating mistakes`,
      priority: "high"
    });
  }
  if (taskContext?.hasCodeChanges) {
    recommendations.push({
      tool: "check_patterns",
      reason: "Code changes detected - validate against architectural patterns before commit",
      priority: "medium"
    });
  }
  if ((taskContext?.fileCount ?? 0) > 3 && !health.lastSnapshot) {
    recommendations.push({
      tool: "prepare_workspace",
      reason: "Multi-file changes planned with no snapshot - run pre-flight check",
      priority: "medium"
    });
  }
  if (health.lastSnapshot && health.lastSnapshot.minutesAgo > 120 && health.trajectory === "degrading") {
    recommendations.push({
      tool: "snapshot_create",
      reason: "Last snapshot over 2 hours ago and trajectory is degrading",
      priority: "low"
    });
  }
  if (health.suggestions.length > 0) {
    recommendations.push({
      tool: "report_violation",
      reason: "Active suggestions available - consider recording learnings for pattern promotion",
      priority: "low"
    });
  }
  const priorityOrder = {
    critical: 0,
    high: 1,
    medium: 2,
    low: 3
  };
  return recommendations.sort((a, b) => priorityOrder[a.priority] - priorityOrder[b.priority]).slice(0, 3);
}
__name(getRecommendedActions, "getRecommendedActions");
__name8(getRecommendedActions, "getRecommendedActions");
function getRecommendedAction(health) {
  const actions = getRecommendedActions(health);
  return actions.length > 0 ? {
    tool: actions[0].tool,
    reason: actions[0].reason
  } : null;
}
__name(getRecommendedAction, "getRecommendedAction");
__name8(getRecommendedAction, "getRecommendedAction");
init_session_adapter();
init_local_service_adapter();
init_errors();
init_integration();
var ToolCapability = {
  // Snapshot operations
  SNAPSHOT_READ: "snapshot:read",
  SNAPSHOT_WRITE: "snapshot:write",
  SNAPSHOT_DELETE: "snapshot:delete",
  // Session operations
  SESSION_READ: "session:read",
  SESSION_WRITE: "session:write",
  // Learning operations
  LEARNING_READ: "learning:read",
  LEARNING_WRITE: "learning:write",
  // Validation operations
  VALIDATE_READ: "validate:read",
  // Telemetry operations
  TELEMETRY_WRITE: "telemetry:write",
  // Enterprise capabilities
  TEAM_READ: "team:read",
  TEAM_WRITE: "team:write",
  COMPLIANCE_READ: "compliance:read",
  COMPLIANCE_EXPORT: "compliance:export",
  AUDIT_READ: "audit:read"
};
var ALL_CAPABILITIES = Object.values(ToolCapability);
var CapabilityGroups = {
  /** Enterprise-only capabilities */
  ENTERPRISE: [
    ToolCapability.TEAM_READ,
    ToolCapability.TEAM_WRITE,
    ToolCapability.COMPLIANCE_READ,
    ToolCapability.COMPLIANCE_EXPORT,
    ToolCapability.AUDIT_READ
  ]
};
var TIER_HIERARCHY = [
  "free",
  "pro",
  "enterprise"
];
function isZodSchema(schema) {
  return typeof schema.safeParse === "function";
}
__name(isZodSchema, "isZodSchema");
__name8(isZodSchema, "isZodSchema");
function resolveInputSchemaForMcp(schema) {
  if (!isZodSchema(schema)) {
    return schema;
  }
  try {
    const def = schema._def;
    if (def?.typeName === "ZodObject" && typeof def.shape === "function") {
      const shape = def.shape();
      const properties = {};
      const required = [];
      for (const [key, field] of Object.entries(shape)) {
        properties[key] = {};
        if (!field.isOptional?.()) {
          required.push(key);
        }
      }
      return {
        type: "object",
        properties,
        ...required.length ? {
          required
        } : {}
      };
    }
  } catch {
  }
  return {
    type: "object"
  };
}
__name(resolveInputSchemaForMcp, "resolveInputSchemaForMcp");
__name8(resolveInputSchemaForMcp, "resolveInputSchemaForMcp");
var ToolNotFoundError = class extends Error {
  static {
    __name(this, "ToolNotFoundError");
  }
  static {
    __name8(this, "ToolNotFoundError");
  }
  toolName;
  constructor(toolName) {
    super(`Tool '${toolName}' not found`);
    this.name = "ToolNotFoundError";
    this.toolName = toolName;
  }
};
var TierNotAuthorizedError = class extends Error {
  static {
    __name(this, "TierNotAuthorizedError");
  }
  static {
    __name8(this, "TierNotAuthorizedError");
  }
  toolName;
  requiredTier;
  constructor(toolName, requiredTier) {
    super(`Tool '${toolName}' requires '${requiredTier}' tier or higher`);
    this.name = "TierNotAuthorizedError";
    this.toolName = toolName;
    this.requiredTier = requiredTier;
  }
};
var CapabilityNotGrantedError = class extends Error {
  static {
    __name(this, "CapabilityNotGrantedError");
  }
  static {
    __name8(this, "CapabilityNotGrantedError");
  }
  toolName;
  missingCapabilities;
  constructor(toolName, missingCapabilities) {
    super(`Tool '${toolName}' requires capabilities: ${missingCapabilities.join(", ")}`);
    this.name = "CapabilityNotGrantedError";
    this.toolName = toolName;
    this.missingCapabilities = missingCapabilities;
  }
};
var InputValidationError2 = class extends Error {
  static {
    __name(this, "InputValidationError");
  }
  static {
    __name8(this, "InputValidationError");
  }
  toolName;
  validationErrors;
  constructor(toolName, errors) {
    super(`Tool '${toolName}' input validation failed: ${errors.join("; ")}`);
    this.name = "InputValidationError";
    this.toolName = toolName;
    this.validationErrors = errors;
  }
};
var TOOL_CAPABILITY_MAP = {
  // V2 tools (primary)
  vreko: [
    ToolCapability.SNAPSHOT_WRITE,
    ToolCapability.SESSION_WRITE,
    ToolCapability.LEARNING_READ
  ],
  vreko_learn: [
    ToolCapability.LEARNING_WRITE
  ],
  vreko_end: [
    ToolCapability.SESSION_WRITE,
    ToolCapability.TELEMETRY_WRITE
  ],
  vreko_pulse: [
    ToolCapability.SESSION_READ
  ],
  // Consolidated tools (legacy, for backward compatibility)
  snap_end: [
    ToolCapability.SESSION_WRITE,
    ToolCapability.TELEMETRY_WRITE
  ],
  snap_learn: [
    ToolCapability.LEARNING_WRITE
  ],
  snap_violation: [
    ToolCapability.LEARNING_WRITE
  ],
  snap_fix: [
    ToolCapability.SNAPSHOT_WRITE
  ],
  snap_help: [],
  check: [
    ToolCapability.VALIDATE_READ
  ]
};
function inferCapabilities(toolName) {
  return TOOL_CAPABILITY_MAP[toolName] ?? [];
}
__name(inferCapabilities, "inferCapabilities");
__name8(inferCapabilities, "inferCapabilities");
function createLegacyContextFromNew(newContext) {
  return {
    workspaceRoot: newContext.workspace.path,
    tier: newContext.workspace.tier,
    storageMode: "local",
    userId: newContext.user?.id,
    sessionId: void 0
  };
}
__name(createLegacyContextFromNew, "createLegacyContextFromNew");
__name8(createLegacyContextFromNew, "createLegacyContextFromNew");
function adaptLegacyHandler(legacyHandler) {
  return async (input, context) => {
    const legacyContext = createLegacyContextFromNew(context);
    const result7 = await legacyHandler(input, legacyContext);
    return result7;
  };
}
__name(adaptLegacyHandler, "adaptLegacyHandler");
__name8(adaptLegacyHandler, "adaptLegacyHandler");
function adaptLegacyTool(legacyTool, handler2) {
  const mapTier = /* @__PURE__ */ __name8((legacyTier) => {
    if (legacyTier === "pro") {
      return "pro";
    }
    return "free";
  }, "mapTier");
  return {
    name: legacyTool.name,
    description: legacyTool.description ?? `Tool: ${legacyTool.name}`,
    inputSchema: legacyTool.inputSchema,
    capabilities: inferCapabilities(legacyTool.name),
    tier: mapTier(legacyTool.tier),
    handler: adaptLegacyHandler(handler2),
    annotations: legacyTool.annotations,
    deprecated: legacyTool.deprecated
  };
}
__name(adaptLegacyTool, "adaptLegacyTool");
__name8(adaptLegacyTool, "adaptLegacyTool");
function adaptLegacyTools(tools) {
  return tools.map(([tool, handler2]) => adaptLegacyTool(tool, handler2));
}
__name(adaptLegacyTools, "adaptLegacyTools");
__name8(adaptLegacyTools, "adaptLegacyTools");
async function getAdaptedConsolidatedTools() {
  const { CONSOLIDATED_TOOLS: CONSOLIDATED_TOOLS2, EXPOSED_MCP_TOOLS: EXPOSED_MCP_TOOLS2, CONSOLIDATED_HANDLERS: CONSOLIDATED_HANDLERS2 } = await Promise.resolve().then(() => (init_registry2(), registry_exports));
  const intelligenceSurface = process.env.VREKO_MCP_INTELLIGENCE_SURFACE || "minimal";
  const toolsToExpose = intelligenceSurface === "full" ? CONSOLIDATED_TOOLS2 : EXPOSED_MCP_TOOLS2;
  const toolPairs = toolsToExpose.map((tool) => [
    tool,
    CONSOLIDATED_HANDLERS2[tool.name]
  ]);
  return adaptLegacyTools(toolPairs);
}
__name(getAdaptedConsolidatedTools, "getAdaptedConsolidatedTools");
__name8(getAdaptedConsolidatedTools, "getAdaptedConsolidatedTools");
init_errors();
async function applyV2Surface(registry) {
  const startupCtx = createRequestContext("vreko-mcp-startup");
  let v2RegisteredCount = 0;
  try {
    const { V2_TOOLS: V2_TOOLS2, V2_HANDLERS: V2_HANDLERS2 } = await Promise.resolve().then(() => (init_v2(), v2_exports));
    const v2ToolPairs = V2_TOOLS2.map((tool) => [
      tool,
      V2_HANDLERS2[tool.name]
    ]);
    const adaptedV2Tools = adaptLegacyTools(v2ToolPairs);
    for (const tool of adaptedV2Tools) {
      try {
        if (registry.has(tool.name)) {
          registry.unregister?.(tool.name);
        }
        registry.register(tool);
        v2RegisteredCount++;
      } catch (perToolError) {
        const msg = perToolError instanceof Error ? perToolError.message : String(perToolError);
        logError(startupCtx, "E503_INTERNAL_ERROR", `V2 tool registration failed for '${tool.name}': ${msg}`, {
          toolName: tool.name
        });
      }
    }
  } catch (err22) {
    err22 instanceof Error ? err22.message : String(err22);
    logError(startupCtx);
  }
  if (v2RegisteredCount === 0) {
    throw new Error("V2 tool registration failed: zero of 4 tools registered. The MCP server cannot start without its primary tool surface. Check server logs for per-tool errors.");
  }
  return v2RegisteredCount;
}
__name(applyV2Surface, "applyV2Surface");
__name8(applyV2Surface, "applyV2Surface");
var DEFAULT_RULES = [
  // Free tier gets basic capabilities
  {
    capabilities: [
      ToolCapability.SNAPSHOT_READ,
      ToolCapability.SNAPSHOT_WRITE,
      ToolCapability.SESSION_READ,
      ToolCapability.SESSION_WRITE,
      ToolCapability.LEARNING_READ,
      ToolCapability.LEARNING_WRITE,
      ToolCapability.VALIDATE_READ,
      ToolCapability.TELEMETRY_WRITE
    ],
    tier: "free"
  },
  // Pro tier gets snapshot delete
  {
    capabilities: [
      ToolCapability.SNAPSHOT_DELETE
    ],
    tier: "pro"
  },
  // Enterprise gets team and compliance
  {
    capabilities: [
      ...CapabilityGroups.ENTERPRISE
    ],
    tier: "enterprise"
  }
];
var CapabilityManager = class {
  static {
    __name(this, "CapabilityManager");
  }
  static {
    __name8(this, "CapabilityManager");
  }
  rules;
  featureFlags = /* @__PURE__ */ new Map();
  constructor(customRules) {
    this.rules = customRules ?? DEFAULT_RULES;
  }
  /**
  * Set a feature flag
  */
  setFeatureFlag(flag, enabled) {
    this.featureFlags.set(flag, enabled);
  }
  /**
  * Get active capabilities for a workspace/user
  */
  getActiveCapabilities(workspace, user) {
    const active = /* @__PURE__ */ new Set();
    const tierHierarchy = [
      "free",
      "pro",
      "enterprise"
    ];
    const userTierIndex = tierHierarchy.indexOf(workspace.tier);
    for (const rule of this.rules) {
      const ruleTierIndex = tierHierarchy.indexOf(rule.tier ?? "free");
      if (userTierIndex < ruleTierIndex) {
        continue;
      }
      if (rule.featureFlag && !this.featureFlags.get(rule.featureFlag)) {
        continue;
      }
      if (rule.condition && !rule.condition(workspace, user)) {
        continue;
      }
      for (const cap of rule.capabilities) {
        active.add(cap);
      }
    }
    return active;
  }
  /**
  * Check if specific capabilities are granted
  */
  hasCapabilities(required, workspace, user) {
    const active = this.getActiveCapabilities(workspace, user);
    const missing = required.filter((cap) => !active.has(cap));
    return {
      granted: missing.length === 0,
      missing
    };
  }
  /**
  * Check a single capability
  */
  hasCapability(capability, workspace, user) {
    return this.hasCapabilities([
      capability
    ], workspace, user).granted;
  }
  /**
  * Add a custom rule
  */
  addRule(rule) {
    this.rules.push(rule);
  }
  /**
  * Get all available capabilities
  */
  getAllCapabilities() {
    return ALL_CAPABILITIES;
  }
  /**
  * Get capabilities for a specific tier
  */
  getCapabilitiesForTier(tier) {
    const mockWorkspace = {
      path: "/mock",
      tier
    };
    return Array.from(this.getActiveCapabilities(mockWorkspace));
  }
};
init_session_adapter();
function validateInputSchema(schema, input) {
  if (isZodSchema(schema)) {
    const result7 = schema.safeParse(input);
    if (result7.success) {
      return {
        valid: true
      };
    }
    const errors2 = result7.error?.issues.map((i) => `${i.path.join(".") || "input"}: ${i.message}`) ?? [
      "Validation failed"
    ];
    return {
      valid: false,
      errors: errors2
    };
  }
  const jsonSchema = schema;
  const errors = [];
  if (jsonSchema.type === "object") {
    if (typeof input !== "object" || input === null) {
      return {
        valid: false,
        errors: [
          "Input must be an object"
        ]
      };
    }
    const inputObj = input;
    if (jsonSchema.required) {
      for (const field of jsonSchema.required) {
        if (!(field in inputObj)) {
          errors.push(`Missing required field: ${field}`);
        }
      }
    }
  }
  return {
    valid: errors.length === 0,
    errors: errors.length > 0 ? errors : void 0
  };
}
__name(validateInputSchema, "validateInputSchema");
__name8(validateInputSchema, "validateInputSchema");
var ToolRegistry = class {
  static {
    __name(this, "ToolRegistry");
  }
  static {
    __name8(this, "ToolRegistry");
  }
  tools = /* @__PURE__ */ new Map();
  capabilityManager;
  constructor(capabilityManager) {
    this.capabilityManager = capabilityManager ?? new CapabilityManager();
  }
  /**
  * Register a tool
  */
  register(tool) {
    this.validateToolDefinition(tool);
    if (this.tools.has(tool.name)) {
      throw new Error(`Tool '${tool.name}' is already registered`);
    }
    this.tools.set(tool.name, tool);
  }
  /**
  * Get a tool by name
  */
  get(name) {
    return this.tools.get(name);
  }
  /**
  * List all tools (optionally filtered by tier)
  */
  list(options) {
    const allTools = Array.from(this.tools.values());
    if (!options?.tier) {
      return allTools;
    }
    const maxTierIndex = TIER_HIERARCHY.indexOf(options.tier);
    return allTools.filter((tool) => {
      const toolTierIndex = TIER_HIERARCHY.indexOf(tool.tier ?? "free");
      return toolTierIndex <= maxTierIndex;
    });
  }
  /**
  * Execute a tool with capability checking
  */
  async execute(name, input, context) {
    const startTime = Date.now();
    const tool = this.tools.get(name);
    if (!tool) {
      throw new ToolNotFoundError(name);
    }
    if (!this.isTierAuthorized(tool, context)) {
      throw new TierNotAuthorizedError(name, tool.tier ?? "free");
    }
    const missingCapabilities = this.getMissingCapabilities(tool, context);
    if (missingCapabilities.length > 0) {
      throw new CapabilityNotGrantedError(name, missingCapabilities);
    }
    const validationResult = this.validateInput(name, input);
    if (!validationResult.valid) {
      throw new InputValidationError2(name, validationResult.errors ?? []);
    }
    try {
      if (tool.hooks?.beforeExecute) {
        await tool.hooks.beforeExecute(input, context);
      }
      const output = await tool.handler(input, context);
      if (tool.hooks?.afterExecute) {
        await tool.hooks.afterExecute(output, context);
      }
      if (context.workspace?.path) {
        const mode = typeof input === "object" && input !== null && "mode" in input ? String(input.mode) : void 0;
        await trackActualToolCallAsync(context.workspace.path, name, mode);
      }
      return {
        success: true,
        result: output,
        durationMs: Date.now() - startTime
      };
    } catch (error) {
      if (tool.hooks?.onError) {
        await tool.hooks.onError(error, context);
      }
      const errorMessage7 = error instanceof Error ? error.message : String(error);
      const errorStack = error instanceof Error ? error.stack : void 0;
      return {
        success: false,
        error: errorMessage7,
        stack: errorStack,
        durationMs: Date.now() - startTime
      };
    }
  }
  /**
  * Check if a tool is available for given context
  */
  isAvailable(name, context) {
    const tool = this.tools.get(name);
    if (!tool) {
      return false;
    }
    return this.isTierAuthorized(tool, context) && this.getMissingCapabilities(tool, context).length === 0;
  }
  /**
  * Validate input against tool schema
  */
  validateInput(name, input) {
    const tool = this.tools.get(name);
    if (!tool) {
      return {
        valid: false,
        errors: [
          `Tool '${name}' not found`
        ]
      };
    }
    return validateInputSchema(tool.inputSchema, input);
  }
  /**
  * Get the capability manager
  */
  getCapabilityManager() {
    return this.capabilityManager;
  }
  /**
  * Get all registered tool names
  */
  getToolNames() {
    return Array.from(this.tools.keys());
  }
  /**
  * Check if tool is registered
  */
  has(name) {
    return this.tools.has(name);
  }
  /**
  * Unregister a tool (mainly for testing)
  */
  unregister(name) {
    return this.tools.delete(name);
  }
  /**
  * Clear all registered tools (mainly for testing)
  */
  clear() {
    this.tools.clear();
  }
  // ==========================================================================
  // Private Methods
  // ==========================================================================
  isTierAuthorized(tool, context) {
    const requiredTierIndex = TIER_HIERARCHY.indexOf(tool.tier ?? "free");
    const userTierIndex = TIER_HIERARCHY.indexOf(context.workspace.tier);
    return userTierIndex >= requiredTierIndex;
  }
  getMissingCapabilities(tool, context) {
    return tool.capabilities.filter((cap) => !context.activeCapabilities.has(cap));
  }
  validateToolDefinition(tool) {
    if (!tool.name || typeof tool.name !== "string") {
      throw new Error("Tool must have a valid name");
    }
    if (!tool.handler || typeof tool.handler !== "function") {
      throw new Error(`Tool '${tool.name}' must have a handler function`);
    }
    if (!Array.isArray(tool.capabilities)) {
      throw new Error(`Tool '${tool.name}' must declare capabilities array`);
    }
    if (!tool.inputSchema || typeof tool.inputSchema !== "object") {
      throw new Error(`Tool '${tool.name}' must have an inputSchema`);
    }
  }
};
function createToolRegistry(capabilityManager) {
  return new ToolRegistry(capabilityManager);
}
__name(createToolRegistry, "createToolRegistry");
__name8(createToolRegistry, "createToolRegistry");
init_hybrid_retrieval_service();
var FileSystemAlertQueueReader = class {
  static {
    __name(this, "FileSystemAlertQueueReader");
  }
  static {
    __name8(this, "FileSystemAlertQueueReader");
  }
  readPendingAlerts(workspaceRoot, maxAlerts = 3) {
    try {
      const path4 = __require3("path");
      const fs22 = __require3("fs");
      const alertsPath = path4.join(workspaceRoot, ".vreko", "alerts.jsonl");
      if (!fs22.existsSync(alertsPath)) {
        return [];
      }
      const content = fs22.readFileSync(alertsPath, "utf-8");
      const lines = content.trim().split("\n").filter(Boolean);
      const pendingAlerts = [];
      for (const line of lines.slice(-maxAlerts)) {
        try {
          const entry = JSON.parse(line);
          if (!entry.delivered && entry.confidence >= 80) {
            pendingAlerts.push({
              id: entry.id,
              timestamp: entry.timestamp,
              severity: entry.severity,
              category: entry.category,
              summary: entry.summary,
              details: entry.details,
              suggested_action: entry.suggested_action,
              confidence: entry.confidence,
              dismissible: entry.dismissible
            });
          }
        } catch {
        }
      }
      return pendingAlerts;
    } catch {
      return [];
    }
  }
};
var VitalsEnricherService = class {
  static {
    __name(this, "VitalsEnricherService");
  }
  static {
    __name8(this, "VitalsEnricherService");
  }
  vitalsProvider;
  alertReader;
  constructor(vitalsProvider, alertReader = new FileSystemAlertQueueReader()) {
    this.vitalsProvider = vitalsProvider;
    this.alertReader = alertReader;
  }
  /**
  * Enhance a tool result with SessionHealth data and proactive alerts
  *
  * @param result - The tool result to enhance
  * @param workspaceRoot - Workspace root path
  * @param eventTracker - Optional event tracker for telemetry
  * @returns Enhanced tool result
  */
  async enhance(result7, workspaceRoot, eventTracker) {
    if (!result7 || !Array.isArray(result7.content)) {
      return {
        content: [
          {
            type: "text",
            text: JSON.stringify({
              error: "Invalid result structure"
            })
          }
        ],
        isError: true
      };
    }
    if (result7.isError) {
      return result7;
    }
    try {
      const vitals = await Promise.resolve(this.vitalsProvider.getVitalsSnapshot(workspaceRoot));
      if (!vitals) {
        return result7;
      }
      const sessionHealth = this.buildSessionHealth(vitals, workspaceRoot, eventTracker);
      const originalText = result7.content[0]?.text;
      if (originalText) {
        try {
          const parsed = JSON.parse(originalText);
          const enhanced = {
            ...parsed,
            _sessionHealth: sessionHealth
          };
          return {
            content: [
              {
                type: "text",
                text: JSON.stringify(enhanced, null, 2)
              }
            ],
            isError: false
          };
        } catch {
          return result7;
        }
      }
      return result7;
    } catch {
      return result7;
    }
  }
  /**
  * Build session health summary from vitals
  */
  buildSessionHealth(vitals, workspaceRoot, eventTracker) {
    const sessionHealth = {
      pulse: vitals.pulse.level,
      pressure: vitals.pressure.value,
      trajectory: vitals.trajectory,
      snapshotRecommended: vitals.pressure.value > 60 || vitals.trajectory === "escalating"
    };
    const pendingAlerts = this.alertReader.readPendingAlerts(workspaceRoot);
    if (pendingAlerts.length > 0) {
      sessionHealth.proactive_alerts = pendingAlerts;
      if (eventTracker) {
        for (const alert of pendingAlerts) {
          eventTracker.trackAlertShown({
            alert_id: alert.id,
            category: alert.category,
            severity: alert.severity,
            confidence: alert.confidence,
            delivery_method: "enriched_response"
          });
        }
      }
      const critical = pendingAlerts.find((a) => a.severity === "critical");
      if (critical) {
        sessionHealth.next_action = critical.suggested_action || "Review critical alert";
      }
    }
    if (vitals.trajectory === "critical" || vitals.pressure.value > 80) {
      sessionHealth.hint = "High risk state detected - proceed with caution, major changes should be incremental";
    } else if (vitals.pulse.level === "racing") {
      sessionHealth.hint = "High change velocity detected - take care with modifications";
    }
    return sessionHealth;
  }
};
var MCP_EVENTS = {
  TOOL_CALLED: "mcp_tool_called",
  CONTEXT_PROVIDED: "mcp_context_provided",
  AGENT_SELF_CHECK: "mcp_agent_self_check",
  // Proactive alert events (Phase 0 telemetry)
  ALERT_SHOWN: "mcp_alert_shown",
  ALERT_ACTED: "mcp_alert_acted",
  ALERT_DISMISSED: "mcp_alert_dismissed"
};
var SENSITIVE_KEYS = /* @__PURE__ */ new Set([
  "path",
  "filePath",
  "file_path",
  "code",
  "content",
  "secret",
  "token",
  "password",
  "key",
  "apiKey",
  "api_key"
]);
function sanitizeParams(params) {
  const sanitized = {};
  for (const [key, value] of Object.entries(params)) {
    if (SENSITIVE_KEYS.has(key.toLowerCase())) {
      sanitized[key] = "[redacted]";
    } else if (typeof value === "string" && value.length > 100) {
      sanitized[key] = `${value.slice(0, 50)}...[truncated]`;
    } else if (Array.isArray(value)) {
      sanitized[key] = `[array:${value.length}]`;
    } else if (typeof value === "object" && value !== null) {
      sanitized[key] = `[object:${Object.keys(value).length} keys]`;
    } else {
      sanitized[key] = value;
    }
  }
  return sanitized;
}
__name(sanitizeParams, "sanitizeParams");
__name8(sanitizeParams, "sanitizeParams");
var McpEventTracker = class {
  static {
    __name(this, "McpEventTracker");
  }
  static {
    __name8(this, "McpEventTracker");
  }
  telemetry;
  constructor(telemetry) {
    this.telemetry = telemetry;
  }
  /**
  * Track tool call event
  *
  * Called after each tool execution in server.ts
  *
  * @param props - Tool call properties
  */
  trackToolCalled(props) {
    if (!this.telemetry) {
      return;
    }
    this.telemetry.log(MCP_EVENTS.TOOL_CALLED, {
      ...props,
      parameters: sanitizeParams(props.parameters),
      timestamp: Date.now()
    });
  }
  /**
  * Track context provided event
  *
  * Called when context is gathered for AI agents
  *
  * @param props - Context provided properties
  */
  trackContextProvided(props) {
    if (!this.telemetry) {
      return;
    }
    this.telemetry.log(MCP_EVENTS.CONTEXT_PROVIDED, {
      ...props,
      timestamp: Date.now()
    });
  }
  /**
  * Track agent self-check event
  *
  * Called when agent runs validation checks
  *
  * @param props - Self-check properties
  */
  trackAgentSelfCheck(props) {
    if (!this.telemetry) {
      return;
    }
    this.telemetry.log(MCP_EVENTS.AGENT_SELF_CHECK, {
      ...props,
      timestamp: Date.now()
    });
  }
  // =========================================================================
  // Proactive Alert Telemetry (Phase 0)
  // =========================================================================
  /**
  * Track alert shown event
  *
  * Called when a proactive alert is delivered to the AI agent.
  * Used to measure alert delivery rate and categorization.
  *
  * @param props - Alert shown properties
  */
  trackAlertShown(props) {
    if (!this.telemetry) {
      return;
    }
    this.telemetry.log(MCP_EVENTS.ALERT_SHOWN, {
      ...props,
      timestamp: Date.now()
    });
  }
  /**
  * Track alert acted event
  *
  * Called when user/AI takes action based on an alert.
  * Key metric for measuring alert effectiveness.
  *
  * @param props - Alert acted properties
  */
  trackAlertActed(props) {
    if (!this.telemetry) {
      return;
    }
    this.telemetry.log(MCP_EVENTS.ALERT_ACTED, {
      ...props,
      timestamp: Date.now()
    });
  }
  /**
  * Track alert dismissed event
  *
  * Called when user dismisses an alert.
  * Used to tune alert frequency and relevance.
  *
  * @param props - Alert dismissed properties
  */
  trackAlertDismissed(props) {
    if (!this.telemetry) {
      return;
    }
    this.telemetry.log(MCP_EVENTS.ALERT_DISMISSED, {
      ...props,
      timestamp: Date.now()
    });
  }
  /**
  * Track alert correlation event
  *
  * Called to correlate alert → action → outcome for effectiveness analysis.
  * Example: Alert about risky file → snapshot created → restore needed = alert was valuable
  *
  * @param alertId - ID of the alert
  * @param outcome - What happened after the alert
  */
  trackAlertCorrelation(alertId, outcome) {
    if (!this.telemetry) {
      return;
    }
    this.telemetry.log("mcp_alert_correlation", {
      alert_id: alertId,
      outcome,
      timestamp: Date.now()
    });
  }
};
init_registry2();
init_validation();
function createVitalsProvider(workspaceRoot, daemonClient) {
  return {
    getVitalsSnapshot: /* @__PURE__ */ __name8(async (_root) => {
      try {
        const result7 = await daemonClient.request("session/vitals", {
          workspacePath: workspaceRoot
        });
        if (result7) {
          return {
            pulse: {
              level: result7.pulse,
              changesPerMinute: result7.changesPerMinute ?? 0
            },
            pressure: {
              value: result7.pressure
            },
            trajectory: result7.trajectory
          };
        }
        return {
          pulse: {
            level: "resting",
            changesPerMinute: 0
          },
          pressure: {
            value: 0
          },
          trajectory: "stable"
        };
      } catch {
        return {
          pulse: {
            level: "resting",
            changesPerMinute: 0
          },
          pressure: {
            value: -1
          },
          trajectory: "stable"
        };
      }
    }, "getVitalsSnapshot")
  };
}
__name(createVitalsProvider, "createVitalsProvider");
__name8(createVitalsProvider, "createVitalsProvider");
async function createMcpServer(options) {
  return createUnifiedServer(options);
}
__name(createMcpServer, "createMcpServer");
__name8(createMcpServer, "createMcpServer");
async function createMcpServerWithRegistry(options) {
  return createUnifiedServer(options, options.registry);
}
__name(createMcpServerWithRegistry, "createMcpServerWithRegistry");
__name8(createMcpServerWithRegistry, "createMcpServerWithRegistry");
async function createUnifiedServer(options, injectedRegistry) {
  const { workspaceRoot, tier } = options;
  if (options.storageMode !== "remote") {
    if (!fs.existsSync(workspaceRoot)) {
      throw new Error(`Workspace does not exist: ${workspaceRoot}`);
    }
    if (!fs.statSync(workspaceRoot).isDirectory()) {
      throw new Error(`Workspace is not a directory: ${workspaceRoot}`);
    }
  }
  if (options.storageMode !== "remote") {
    Promise.resolve().then(async () => {
      try {
        const health = await validateWorkspaceHealth(workspaceRoot);
        if (!health.healthy) {
          if (health.autoFixable.length > 0) {
            const _repairResult = await repairWorkspaceIssues(workspaceRoot, health.autoFixable);
          }
          if (health.requiresUserAction.length > 0) {
            for (const _issue of health.requiresUserAction) {
            }
          }
        }
      } catch (_healthError) {
      }
    });
  }
  generateWorkspaceId(workspaceRoot);
  const registry = injectedRegistry ?? createToolRegistry();
  let toolsReady = null;
  if (!injectedRegistry) {
    toolsReady = (async () => {
      try {
        const adaptedTools = await getAdaptedConsolidatedTools();
        for (const tool of adaptedTools) {
          if (!registry.has(tool.name)) {
            registry.register(tool);
          }
        }
      } catch (_err) {
      }
      await applyV2Surface(registry);
    })();
  }
  const server = new Server({
    name: "vreko-mcp",
    version: "1.0.0"
  }, {
    capabilities: {
      tools: {},
      resources: {},
      experimental: {
        serverInfo: {
          storageMode: options.storageMode || "readonly",
          writeCapable: (options.storageMode || "readonly") === "local",
          cliAvailable: true,
          cliSetupCommand: "npm install -g @vreko/cli && vreko tools configure"
        }
      }
    },
    instructions: [
      "Vreko MCP  -  V2 4-Tool Surface (authoritative, overrides individual descriptions):",
      "",
      "Workflow: vreko \u2192 [work] \u2192 vreko_learn (optional) \u2192 vreko_end.",
      "",
      "Tool Usage:",
      "  1. Call vreko FIRST before any code changes  -  returns intelligence briefing",
      "  2. Call vreko_learn when you discover something worth remembering",
      "  3. Call vreko_end when task is complete  -  returns session summary",
      "",
      "Key Points:",
      "  - vreko is idempotent  -  calling twice resumes the same session",
      "  - vreko_learn creates implicit session if needed",
      "  - All files are automatically protected  -  no manual snapshot needed",
      "  - Workspace isolation: learnings and context are scoped to this workspace"
    ].join("\n")
  });
  const daemonClient = createServiceClient2(workspaceRoot);
  options.apiClient ? {} : void 0;
  const baseNewContext = {
    workspace: {
      path: workspaceRoot,
      tier
    },
    user: options.auth?.userId ? {
      id: options.auth.userId,
      email: ""
    } : void 0,
    request: {
      id: "unknown",
      timestamp: /* @__PURE__ */ new Date(),
      source: "unknown"
    }
  };
  const eventTracker = new McpEventTracker(options.telemetry ?? null);
  const vitalsProvider = createVitalsProvider(workspaceRoot, daemonClient);
  const vitalsEnricher = new VitalsEnricherService(vitalsProvider);
  const capManager = new CapabilityManager();
  injectedRegistry ? registry.getToolNames().length : "(loading)";
  initializeHybridRetrieval(workspaceRoot).catch((_error) => {
  });
  server.setRequestHandler(PingRequestSchema, async () => ({}));
  server.setRequestHandler(ListToolsRequestSchema, async () => {
    if (toolsReady) {
      await toolsReady;
    }
    const tools = registry.list({
      tier
    });
    return {
      tools: tools.map((tool) => ({
        name: tool.name,
        description: tool.description,
        inputSchema: resolveInputSchemaForMcp(tool.inputSchema),
        annotations: tool.annotations
      }))
    };
  });
  server.setRequestHandler(CallToolRequestSchema, async (request) => {
    if (toolsReady) {
      await toolsReady;
    }
    const { name, arguments: args } = request.params;
    const startTime = Date.now();
    const clientInfo = server.getClientVersion?.() ?? null;
    const clientName = clientInfo?.name ?? "unknown";
    const clientVersion = clientInfo?.version ?? "unknown";
    const reqCtx = createRequestContext(name, {
      userId: options.auth?.userId,
      tier,
      clientName,
      clientVersion
    });
    log2("info", reqCtx);
    if (!isConsolidatedTool(name)) {
      const guidance = getMigrationGuidance(name);
      if (guidance) {
        log2("warn", reqCtx);
        return {
          content: [
            {
              type: "text",
              text: JSON.stringify({
                error: "E601_DEPRECATED_TOOL",
                message: `Tool '${name}' is deprecated. ${guidance}`,
                migration: guidance,
                availableTools: CONSOLIDATED_TOOLS.map((t) => t.name)
              })
            }
          ],
          isError: true
        };
      }
    }
    const { args: sanitizedArgs, warnings: inputWarnings } = sanitizeInput(name, args || {});
    if (inputWarnings.length > 0) {
      log2("info", reqCtx, `INPUT_SANITIZED: Stripped ${inputWarnings.length} invalid field(s) with warnings`);
    }
    const schema = getToolSchema(name);
    if (schema) {
      const validation = validateInput(schema, sanitizedArgs);
      if (!validation.valid) {
        logError(reqCtx, "E104_VALIDATION_FAILED", validation.error);
        const guidance = getValidationGuidance(name, validation.error);
        return {
          content: [
            {
              type: "text",
              text: JSON.stringify({
                error: "E104_VALIDATION_FAILED",
                message: validation.error + guidance,
                tool: name,
                issues: validation.issues,
                hint: "Check the tool's inputSchema for required parameters and accepted values"
              })
            }
          ],
          isError: true
        };
      }
    }
    try {
      const activeCapabilities = capManager.getActiveCapabilities({
        path: workspaceRoot,
        tier
      });
      const intelligenceServices = {
        // Required services (stubs - tools should use storage directly)
        runtime: {
          createSnapshot: /* @__PURE__ */ __name8(async () => ({
            snapshotId: "",
            snapshot: {}
          }), "createSnapshot"),
          getSnapshot: /* @__PURE__ */ __name8(async () => null, "getSnapshot"),
          listSnapshots: /* @__PURE__ */ __name8(async () => [], "listSnapshots"),
          restoreSnapshot: /* @__PURE__ */ __name8(async () => ({
            success: false,
            restoredFiles: [],
            conflicts: [],
            errors: []
          }), "restoreSnapshot"),
          deleteSnapshot: /* @__PURE__ */ __name8(async () => false, "deleteSnapshot")
        },
        telemetry: {
          track: /* @__PURE__ */ __name8(async () => void 0, "track"),
          flush: /* @__PURE__ */ __name8(async () => void 0, "flush")
        },
        learning: {
          getRelevant: /* @__PURE__ */ __name8(async () => [], "getRelevant"),
          create: /* @__PURE__ */ __name8(async (_learning) => ({
            id: `learn_${Date.now()}`
          }), "create"),
          list: /* @__PURE__ */ __name8(async () => [], "list")
        },
        // Intelligence v2 stores - delegate to daemon/local-service
        decisionStore: {
          getDecisionForSnapshot: /* @__PURE__ */ __name8(async (snapshotId, _workspace) => {
            if (daemonClient.isAvailable()) {
              return daemonClient.request("intelligence.getDecision", {
                snapshotId,
                workspace: workspaceRoot
              });
            }
            return null;
          }, "getDecisionForSnapshot"),
          recordOutcome: /* @__PURE__ */ __name8(async (id, outcome) => {
            if (daemonClient.isAvailable()) {
              await daemonClient.request("intelligence.decisionOutcome", {
                decisionId: id,
                outcome: outcome === "rolled_back" ? "recovered" : outcome,
                workspace: workspaceRoot
              });
            }
          }, "recordOutcome")
        },
        stateStore: {
          addLearning: /* @__PURE__ */ __name8((learning) => {
            if (daemonClient.isAvailable()) {
              daemonClient.request("intelligence.capture", {
                workspace: workspaceRoot,
                content: `${learning.trigger}
${learning.action}`,
                domain: learning.domain,
                type: learning.type,
                filePath: learning.trigger,
                aiTool: "claude",
                risk: learning.tier === "hot" ? "high" : learning.tier === "warm" ? "medium" : "low"
              }).catch((_err) => {
              });
            }
            return {
              id: `learn_${Date.now()}`
            };
          }, "addLearning")
        },
        analytics: {
          track: /* @__PURE__ */ __name8((eventName, properties) => {
            if (daemonClient.isAvailable()) {
              daemonClient.request("analytics.track", {
                event: eventName,
                properties: {
                  ...properties,
                  workspace: workspaceRoot
                }
              }).catch(() => {
              });
            }
          }, "track")
        }
      };
      const context = {
        ...baseNewContext,
        activeCapabilities,
        services: intelligenceServices,
        request: {
          id: `req-${Date.now()}`,
          timestamp: /* @__PURE__ */ new Date(),
          source: "unknown"
        }
      };
      const result7 = await registry.execute(name, sanitizedArgs, context);
      const durationMs = Date.now() - startTime;
      const mode = args && typeof args === "object" && "mode" in args ? String(args.mode) : void 0;
      await recordToolCallEventAsync(workspaceRoot, {
        tool: name,
        mode,
        durationMs,
        success: result7.success,
        errorCode: result7.error,
        clientName,
        clientVersion
      });
      eventTracker.trackToolCalled({
        tool_name: name,
        client_type: clientName,
        client_version: clientVersion,
        parameters: args || {},
        execution_time_ms: durationMs,
        was_successful: result7.success,
        error_code: result7.error
      });
      if (!result7.success) {
        return {
          content: [
            {
              type: "text",
              text: JSON.stringify({
                error: result7.error,
                tool: name,
                stack: result7.stack
              })
            }
          ],
          isError: true
        };
      }
      const toolResult = result7.result;
      if (toolResult?.content && Array.isArray(toolResult.content)) {
        logSuccess(reqCtx, "Tool call completed", {
          isError: toolResult.isError
        });
        const shouldEnrich = name !== "meta" && name !== "vreko_end";
        const enhanced = shouldEnrich ? await vitalsEnricher.enhance(toolResult, workspaceRoot, eventTracker) : toolResult;
        const enrichedContent = (enhanced.content || []).map((c) => ({
          type: "text",
          text: c.text
        }));
        if (inputWarnings.length > 0) {
          const warningsText = [
            "\u26A0\uFE0F INPUT_SANITIZED:",
            ...inputWarnings.map((w) => {
              const opts = w.validOptions ? ` Valid: ${w.validOptions.join("|")}` : "";
              const suggestion = w.suggestion ? ` Example: ${JSON.stringify(w.suggestion)}` : "";
              return `  \u2022 ${w.message}${opts}${suggestion}`;
            }),
            ""
          ].join("\n");
          const firstContent = enrichedContent[0];
          if (firstContent && typeof firstContent.text === "string") {
            const separatorIndex = firstContent.text.indexOf("---");
            if (separatorIndex > 0) {
              const wirePart = firstContent.text.slice(0, separatorIndex + 3);
              const humanPart = firstContent.text.slice(separatorIndex + 3);
              firstContent.text = `${wirePart}
${warningsText}${humanPart}`;
            } else {
              firstContent.text = `${warningsText}${firstContent.text}`;
            }
          }
        }
        if (enhanced.isError && name !== "vreko_learn" && name !== "vreko_end") {
          const firstContent = enrichedContent[0];
          if (firstContent && typeof firstContent.text === "string") {
            const learningHint = [
              "",
              "\u{1F4A1} SUGGESTED_LEARNING: This tool call failed. Consider capturing the failure pattern:",
              `  vreko_learn({ type: "pitfall", trigger: "${name} error", action: "..." })`,
              "  (Replace '...' with the specific insight from this failure)",
              ""
            ].join("\n");
            firstContent.text = `${firstContent.text}${learningHint}`;
          }
        }
        return {
          content: enrichedContent,
          isError: enhanced.isError
        };
      }
      return {
        content: [
          {
            type: "text",
            text: JSON.stringify(result7.result)
          }
        ],
        isError: false
      };
    } catch (outerError) {
      const errorMessage7 = outerError instanceof Error ? outerError.message : String(outerError);
      const errorStack = outerError instanceof Error ? outerError.stack : void 0;
      logError(reqCtx);
      const durationMs = Date.now() - startTime;
      args && typeof args === "object" && "mode" in args ? String(args.mode) : void 0;
      await recordToolCallEventAsync(workspaceRoot, {
        tool: name,
        success: false,
        errorCode: outerError instanceof Error ? outerError.name : "UNKNOWN_ERROR"
      });
      eventTracker.trackToolCalled({
        tool_name: name,
        client_type: clientName,
        client_version: clientVersion,
        parameters: args || {},
        execution_time_ms: durationMs,
        was_successful: false,
        error_code: outerError instanceof Error ? outerError.name : "UNKNOWN_ERROR"
      });
      try {
        const { formatDiagnosticError: formatDiagnosticError2 } = await Promise.resolve().then(() => (init_errors(), errors_exports));
        const diagnostic = formatDiagnosticError2(outerError, {
          tool: name,
          workspace: workspaceRoot
        });
        const diagnosticList = Array.isArray(diagnostic.diagnostics) ? diagnostic.diagnostics : [];
        const diagnosticText = [
          `\u{1F9E2}|H|status:ERROR|code:${diagnostic.code}`,
          "DIAGNOSTICS:",
          ...diagnosticList.map((d) => `- ${d}`),
          diagnostic.fix ? `
FIX: ${diagnostic.fix}` : ""
        ].filter(Boolean).join("\n");
        return {
          content: [
            {
              type: "text",
              text: diagnosticText
            }
          ],
          isError: true
        };
      } catch {
        return {
          content: [
            {
              type: "text",
              text: JSON.stringify({
                error: errorMessage7,
                tool: name,
                stack: errorStack,
                _source: "unhandled_calltool_error"
              })
            }
          ],
          isError: true
        };
      }
    }
  });
  return server;
}
__name(createUnifiedServer, "createUnifiedServer");
__name8(createUnifiedServer, "createUnifiedServer");
function getValidationGuidance(name, error) {
  if (name === "vreko" && error.includes("mode")) {
    return `

\u{1F4A1} TIP: The 'vreko' tool accepts EITHER:
  - mode: "start" | "context" (full-word, recommended)
  - m: "s" | "x" (legacy single-letter)

Example: vreko({ mode: "start", task: "fix bug" })`;
  }
  if (name === "vreko_learn" && error.includes("trigger")) {
    return `

\u{1F4A1} TIP: The 'vreko_learn' tool requires BOTH:
  - trigger: "when X happens" (or legacy: t)
  - action: "do Y" (or legacy: a)

Example: vreko_learn({ trigger: "auth token refresh", action: "check expiry before API call" })`;
  }
  if (name === "vreko_violation" && error.includes("description")) {
    return `

\u{1F4A1} TIP: The 'vreko_violation' tool requires:
  - type: "violation_key" (e.g., 'silent_catch')
  - file: "path/to/file"
  - description: "what went wrong" (or legacy: what)
  - prevention: "how to avoid" (or legacy: prevent)

Example: vreko_violation({ type: "silent_catch", file: "auth.ts", description: "Caught error without logging", prevention: "Always log in catch blocks" })`;
  }
  if (name === "check" && error.includes("mode")) {
    return `

\u{1F4A1} TIP: The 'check' tool accepts EITHER:
  - mode: "quick" | "full" | "patterns" | "build" | "impact" | "circular" | "docs" | "learnings" | "architecture" (recommended)
  - m: "q" | "f" | "p" | "b" | "i" | "c" | "d" | "l" | "a" (legacy)

Example: check({ mode: "quick", files: ["src/auth.ts"] })`;
  }
  return "";
}
__name(getValidationGuidance, "getValidationGuidance");
__name8(getValidationGuidance, "getValidationGuidance");
var VALID_GOAL_METRICS = [
  "bundle",
  "performance",
  "coverage"
];
var KNOWN_INVALID_METRICS = {
  momentum: "Momentum is a scoring system concept, not a task goal metric",
  velocity: "Velocity is not a valid goal metric",
  quality: "Quality is not a valid goal metric - use 'coverage' for test coverage",
  speed: "Speed is not a valid goal metric - use 'performance' for timing",
  size: "Size is not a valid goal metric - use 'bundle' for bundle size"
};
function sanitizeSnapInput(args) {
  const warnings = [];
  const sanitized = {
    ...args
  };
  if (sanitized.goal && typeof sanitized.goal === "object") {
    const goal = sanitized.goal;
    const metric = goal.metric;
    if (metric !== void 0 && !VALID_GOAL_METRICS.includes(metric)) {
      const knownReason = typeof metric === "string" ? KNOWN_INVALID_METRICS[metric.toLowerCase()] : void 0;
      const message = knownReason ? `Invalid goal.metric "${metric}" ignored. ${knownReason}.` : `Invalid goal.metric "${metric}" ignored.`;
      warnings.push({
        field: "goal.metric",
        received: metric,
        message,
        validOptions: VALID_GOAL_METRICS,
        suggestion: {
          metric: "coverage",
          target: 80,
          unit: "%"
        }
      });
      delete sanitized.goal;
    }
  }
  return {
    args: sanitized,
    warnings
  };
}
__name(sanitizeSnapInput, "sanitizeSnapInput");
__name8(sanitizeSnapInput, "sanitizeSnapInput");
function sanitizeInput(name, args) {
  switch (name) {
    case "vreko":
      return sanitizeSnapInput(args);
    default:
      return {
        args,
        warnings: []
      };
  }
}
__name(sanitizeInput, "sanitizeInput");
__name8(sanitizeInput, "sanitizeInput");
init_snapshot_service();
init_utils2();
init_registry2();
var HttpTransportManager = class {
  static {
    __name(this, "HttpTransportManager");
  }
  static {
    __name8(this, "HttpTransportManager");
  }
  sessions = /* @__PURE__ */ new Map();
  webSessions = /* @__PURE__ */ new Map();
  options;
  cleanupInterval = null;
  registry = null;
  registryInitialized = false;
  constructor(options) {
    this.options = {
      sessionTimeoutMs: 30 * 60 * 1e3,
      maxSessions: 1e3,
      enableJsonResponse: true,
      ...options
    };
    this.cleanupInterval = setInterval(() => this.cleanupStaleSessions(), 6e4);
  }
  /**
  * Initialize the shared ToolRegistry (ADR-003)
  * Called lazily on first request to avoid blocking constructor
  */
  async ensureRegistryInitialized() {
    if (this.registryInitialized && this.registry) {
      return this.registry;
    }
    this.registry = createToolRegistry();
    const adaptedTools = await getAdaptedConsolidatedTools();
    for (const tool of adaptedTools) {
      this.registry.register(tool);
    }
    await applyV2Surface(this.registry);
    this.registryInitialized = true;
    return this.registry;
  }
  /**
  * Handle incoming HTTP request
  * Routes to appropriate transport based on session ID
  */
  async handleRequest(req, res, body) {
    const sessionId = req.headers["mcp-session-id"];
    switch (req.method) {
      case "POST":
        await this.handlePost(req, res, body, sessionId);
        break;
      case "GET":
        await this.handleGet(req, res, sessionId);
        break;
      case "DELETE":
        await this.handleDelete(req, res, sessionId);
        break;
      default:
        res.writeHead(405, {
          "Content-Type": "application/json"
        });
        res.end(JSON.stringify({
          error: "Method not allowed"
        }));
    }
  }
  /**
  * Handle POST requests (client-to-server messages)
  */
  async handlePost(req, res, body, sessionId) {
    let transport;
    if (sessionId && this.sessions.has(sessionId)) {
      const entry = this.sessions.get(sessionId);
      entry.lastAccessedAt = Date.now();
      transport = entry.transport;
    } else if (!sessionId && isInitializeRequest(body)) {
      if (this.sessions.size >= (this.options.maxSessions || 1e3)) {
        res.writeHead(503, {
          "Content-Type": "application/json"
        });
        res.end(JSON.stringify({
          jsonrpc: "2.0",
          error: {
            code: -32e3,
            message: "Server at capacity"
          },
          id: null
        }));
        return;
      }
      transport = new StreamableHTTPServerTransport({
        sessionIdGenerator: /* @__PURE__ */ __name8(() => randomUUID(), "sessionIdGenerator"),
        enableJsonResponse: this.options.enableJsonResponse,
        onsessioninitialized: /* @__PURE__ */ __name8((id) => {
          this.sessions.set(id, {
            transport,
            createdAt: Date.now(),
            lastAccessedAt: Date.now()
          });
        }, "onsessioninitialized")
      });
      transport.onclose = () => {
        if (transport.sessionId) {
          this.sessions.delete(transport.sessionId);
        }
      };
      const registry = await this.ensureRegistryInitialized();
      const server = await createMcpServerWithRegistry({
        ...this.options,
        registry
      });
      await server.connect(transport);
    } else {
      res.writeHead(400, {
        "Content-Type": "application/json"
      });
      res.end(JSON.stringify({
        jsonrpc: "2.0",
        error: {
          code: -32e3,
          message: sessionId ? "Session not found or expired" : "Bad Request: Not an initialize request"
        },
        id: null
      }));
      return;
    }
    await transport.handleRequest(req, res, body);
  }
  /**
  * Handle GET requests (server-to-client SSE stream)
  */
  async handleGet(req, res, sessionId) {
    if (!sessionId || !this.sessions.has(sessionId)) {
      const status = sessionId ? 404 : 400;
      res.writeHead(status, {
        "Content-Type": "application/json"
      });
      res.end(JSON.stringify({
        error: sessionId ? "Session not found or expired" : "Invalid or missing session"
      }));
      return;
    }
    const entry = this.sessions.get(sessionId);
    entry.lastAccessedAt = Date.now();
    await entry.transport.handleRequest(req, res);
  }
  /**
  * Handle incoming Web Standard Request (Hono / Cloudflare Workers / Deno / Bun).
  * Returns a Web Standard Response  -  no Node.js adapters required.
  * Use this method when running inside a Hono/web-standard HTTP framework.
  */
  async handleWebRequest(request, parsedBody) {
    const sessionId = request.headers.get("mcp-session-id") ?? void 0;
    switch (request.method) {
      case "POST":
        return this.handleWebPost(request, parsedBody, sessionId);
      case "GET":
        return this.handleWebGet(request, sessionId);
      case "DELETE":
        return this.handleWebDelete(request, sessionId);
      default:
        return new Response(JSON.stringify({
          error: "Method not allowed"
        }), {
          status: 405,
          headers: {
            "Content-Type": "application/json"
          }
        });
    }
  }
  async handleWebPost(request, parsedBody, sessionId) {
    let transport;
    if (sessionId && this.webSessions.has(sessionId)) {
      const entry = this.webSessions.get(sessionId);
      entry.lastAccessedAt = Date.now();
      transport = entry.transport;
    } else if (!sessionId && isInitializeRequest(parsedBody)) {
      if (this.webSessions.size >= (this.options.maxSessions || 1e3)) {
        return new Response(JSON.stringify({
          jsonrpc: "2.0",
          error: {
            code: -32e3,
            message: "Server at capacity"
          },
          id: null
        }), {
          status: 503,
          headers: {
            "Content-Type": "application/json"
          }
        });
      }
      transport = new WebStandardStreamableHTTPServerTransport({
        sessionIdGenerator: /* @__PURE__ */ __name8(() => randomUUID(), "sessionIdGenerator"),
        enableJsonResponse: this.options.enableJsonResponse,
        onsessioninitialized: /* @__PURE__ */ __name8((id) => {
          this.webSessions.set(id, {
            transport,
            createdAt: Date.now(),
            lastAccessedAt: Date.now()
          });
        }, "onsessioninitialized"),
        onsessionclosed: /* @__PURE__ */ __name8((id) => {
          this.webSessions.delete(id);
        }, "onsessionclosed")
      });
      const registry = await this.ensureRegistryInitialized();
      const server = await createMcpServerWithRegistry({
        ...this.options,
        registry
      });
      await server.connect(transport);
    } else {
      const status = sessionId ? 404 : 400;
      return new Response(JSON.stringify({
        jsonrpc: "2.0",
        error: {
          code: -32e3,
          message: sessionId ? "Session not found or expired" : "Bad Request: Not an initialize request"
        },
        id: null
      }), {
        status,
        headers: {
          "Content-Type": "application/json"
        }
      });
    }
    return transport.handleRequest(request, {
      parsedBody
    });
  }
  async handleWebGet(request, sessionId) {
    if (!sessionId || !this.webSessions.has(sessionId)) {
      const status = sessionId ? 404 : 400;
      return new Response(JSON.stringify({
        error: sessionId ? "Session not found or expired" : "Invalid or missing session"
      }), {
        status,
        headers: {
          "Content-Type": "application/json"
        }
      });
    }
    const entry = this.webSessions.get(sessionId);
    entry.lastAccessedAt = Date.now();
    return entry.transport.handleRequest(request);
  }
  async handleWebDelete(request, sessionId) {
    if (!sessionId || !this.webSessions.has(sessionId)) {
      const status = sessionId ? 404 : 400;
      return new Response(JSON.stringify({
        error: sessionId ? "Session not found or expired" : "Invalid or missing session"
      }), {
        status,
        headers: {
          "Content-Type": "application/json"
        }
      });
    }
    const entry = this.webSessions.get(sessionId);
    const response = await entry.transport.handleRequest(request);
    this.webSessions.delete(sessionId);
    return response;
  }
  /**
  * Handle DELETE requests (session termination)
  */
  async handleDelete(req, res, sessionId) {
    if (!sessionId || !this.sessions.has(sessionId)) {
      const status = sessionId ? 404 : 400;
      res.writeHead(status, {
        "Content-Type": "application/json"
      });
      res.end(JSON.stringify({
        error: sessionId ? "Session not found or expired" : "Invalid or missing session"
      }));
      return;
    }
    const entry = this.sessions.get(sessionId);
    await entry.transport.handleRequest(req, res);
    this.sessions.delete(sessionId);
  }
  /**
  * Clean up stale sessions
  */
  cleanupStaleSessions() {
    const now = Date.now();
    const timeout = this.options.sessionTimeoutMs || 30 * 60 * 1e3;
    for (const [sessionId, entry] of this.sessions) {
      if (now - entry.lastAccessedAt > timeout) {
        entry.transport.close();
        this.sessions.delete(sessionId);
      }
    }
    for (const [sessionId, entry] of this.webSessions) {
      if (now - entry.lastAccessedAt > timeout) {
        entry.transport.close();
        this.webSessions.delete(sessionId);
      }
    }
  }
  /**
  * Get active session count
  */
  getSessionCount() {
    return this.sessions.size + this.webSessions.size;
  }
  /**
  * Close all sessions and cleanup
  */
  async close() {
    if (this.cleanupInterval) {
      clearInterval(this.cleanupInterval);
      this.cleanupInterval = null;
    }
    for (const [_sessionId, entry] of this.sessions) {
      await entry.transport.close();
    }
    this.sessions.clear();
    for (const [_sessionId, entry] of this.webSessions) {
      await entry.transport.close();
    }
    this.webSessions.clear();
  }
};
function createHttpTransport(options) {
  return new HttpTransportManager(options);
}
__name(createHttpTransport, "createHttpTransport");
__name8(createHttpTransport, "createHttpTransport");
var connectionState = null;
function getConnectionHealth() {
  return connectionState ? {
    ...connectionState
  } : null;
}
__name(getConnectionHealth, "getConnectionHealth");
__name8(getConnectionHealth, "getConnectionHealth");
function setConnectionState(state) {
  connectionState = state;
}
__name(setConnectionState, "setConnectionState");
__name8(setConnectionState, "setConnectionState");
function updateConnectionState(updates) {
  if (connectionState) {
    Object.assign(connectionState, updates);
  }
}
__name(updateConnectionState, "updateConnectionState");
__name8(updateConnectionState, "updateConnectionState");
async function runStdioMcpServer(options) {
  setConnectionState({
    startTime: /* @__PURE__ */ new Date(),
    lastActivity: /* @__PURE__ */ new Date(),
    toolsLoaded: false,
    toolCount: 0,
    errorCount: 0,
    isShuttingDown: false
  });
  process.on("uncaughtException", (_error) => {
    const health = getConnectionHealth();
    if (health) {
      updateConnectionState({
        errorCount: health.errorCount + 1
      });
    }
    const updatedHealth = getConnectionHealth();
    if (updatedHealth && updatedHealth.errorCount > 5) {
      process.exit(1);
    }
  });
  process.on("unhandledRejection", (reason, _promise) => {
    const health = getConnectionHealth();
    if (health) {
      updateConnectionState({
        errorCount: health.errorCount + 1
      });
    }
    reason instanceof Error ? {
      message: reason.message,
      stack: reason.stack
    } : String(reason);
  });
  process.stdin.on("error", (error) => {
    if (error.message.includes("EPIPE") || error.message.includes("EOF")) {
      updateConnectionState({
        isShuttingDown: true
      });
      process.exit(0);
    }
  });
  process.stdout.on("error", (error) => {
    if (error.message.includes("EPIPE")) {
      updateConnectionState({
        isShuttingDown: true
      });
      process.exit(0);
    }
  });
  process.stdin.on("close", () => {
    updateConnectionState({
      isShuttingDown: true
    });
    process.exit(0);
  });
  process.stdin.on("end", () => {
    updateConnectionState({
      isShuttingDown: true
    });
    process.exit(0);
  });
  const server = await createMcpServerWithRegistry(options);
  const transport = new StdioServerTransport();
  server.oninitialized = () => {
    void (async () => {
      try {
        const rootsResult = await server.listRoots();
        if (rootsResult.roots.length > 0) {
          const firstRoot = rootsResult.roots[0];
          const rootPath = firstRoot.uri.startsWith("file://") ? decodeURIComponent(firstRoot.uri.slice(7)) : firstRoot.uri;
          if (rootPath && options.workspaceRoot && rootPath !== options.workspaceRoot) {
          }
        }
      } catch {
      }
    })();
  };
  try {
    await server.connect(transport);
    updateConnectionState({
      lastActivity: /* @__PURE__ */ new Date()
    });
  } catch (_connectError) {
    process.exit(1);
  }
  registerWithDaemonForSupervision(options.workspaceRoot).catch(() => {
  });
  const resolvedRemote = options.remote ? resolveRemoteConfig(options.remote) : null;
  if (resolvedRemote) {
    createRemoteClient(resolvedRemote);
  }
  updateConnectionState({
    toolsLoaded: true,
    toolCount: 8
  });
  const gracefulShutdown = /* @__PURE__ */ __name8(async (_signal) => {
    const health = getConnectionHealth();
    if (health?.isShuttingDown) {
      return;
    }
    updateConnectionState({
      isShuttingDown: true
    });
    try {
      await server.close();
    } catch (_closeError) {
    }
    process.exit(0);
  }, "gracefulShutdown");
  process.on("SIGINT", () => gracefulShutdown("SIGINT"));
  process.on("SIGTERM", () => gracefulShutdown("SIGTERM"));
  process.on("SIGHUP", () => gracefulShutdown("SIGHUP"));
  const healthInterval = setInterval(() => {
    const health = getConnectionHealth();
    if (health && !health.isShuttingDown) {
      Math.floor((Date.now() - health.startTime.getTime()) / 1e3);
    }
  }, 5 * 60 * 1e3);
  process.on("exit", () => {
    clearInterval(healthInterval);
  });
}
__name(runStdioMcpServer, "runStdioMcpServer");
__name8(runStdioMcpServer, "runStdioMcpServer");
async function registerWithDaemonForSupervision(workspaceRoot) {
  try {
    const { createDaemonClient } = await Promise.resolve().then(() => (init_local_service_adapter(), local_service_adapter_exports));
    const daemonClient = createDaemonClient(workspaceRoot);
    await daemonClient.request("supervisor/register", {
      componentType: "mcp-stdio",
      pid: process.pid,
      workspace: workspaceRoot,
      heartbeatIntervalMs: 3e4,
      restartStrategy: "one-for-one"
    });
    startHeartbeatLoop(daemonClient, workspaceRoot);
  } catch (_error) {
  }
}
__name(registerWithDaemonForSupervision, "registerWithDaemonForSupervision");
__name8(registerWithDaemonForSupervision, "registerWithDaemonForSupervision");
function startHeartbeatLoop(daemonClient, _workspaceRoot) {
  let consecutiveFailures = 0;
  const MAX_FAILURES = 3;
  const heartbeatInterval = setInterval(async () => {
    try {
      const health = getConnectionHealth();
      await daemonClient.request("supervisor/heartbeat", {
        pid: process.pid,
        status: health ? {
          uptimeSeconds: Math.floor((Date.now() - health.startTime.getTime()) / 1e3),
          toolsLoaded: health.toolsLoaded,
          errorCount: health.errorCount
        } : {
          error: "Health check unavailable"
        }
      });
      consecutiveFailures = 0;
    } catch (_error) {
      consecutiveFailures++;
      if (consecutiveFailures >= MAX_FAILURES) {
        clearInterval(heartbeatInterval);
        updateConnectionState({
          isShuttingDown: true
        });
        process.exit(0);
      }
    }
  }, 3e4);
  process.on("exit", () => {
    clearInterval(heartbeatInterval);
  });
}
__name(startHeartbeatLoop, "startHeartbeatLoop");
__name8(startHeartbeatLoop, "startHeartbeatLoop");
function calculatePressure(metrics) {
  const weights = {
    changes: 0.4,
    time: 0.2,
    aiActivity: 0.2,
    fileRisk: 0.2
  };
  const changeScore = Math.min(100, metrics.linesChanged / 500 * 100);
  const timeScore = Math.min(100, metrics.minutesSinceSnapshot / 10 * 10);
  const aiScore = metrics.aiGeneratedPercentage;
  const riskScore = metrics.averageFileRiskScore;
  const value = Math.round(changeScore * weights.changes + timeScore * weights.time + aiScore * weights.aiActivity + riskScore * weights.fileRisk);
  let level;
  if (value <= 40) {
    level = "low";
  } else if (value <= 70) {
    level = "moderate";
  } else if (value <= 85) {
    level = "elevated";
  } else {
    level = "critical";
  }
  return {
    value,
    level,
    snapshotRecommended: value > 70,
    breakdown: {
      changes: Math.round(changeScore * weights.changes),
      time: Math.round(timeScore * weights.time),
      aiActivity: Math.round(aiScore * weights.aiActivity),
      fileRisk: Math.round(riskScore * weights.fileRisk)
    }
  };
}
__name(calculatePressure, "calculatePressure");
__name8(calculatePressure, "calculatePressure");
var LOW_CONFIDENCE_THRESHOLD = 0.25;
(class {
  static {
    __name(this, "InteractionProtocol");
  }
  static {
    __name8(this, "InteractionProtocol");
  }
  /**
  * Evaluate a task through protocol gates
  *
  * @param _task - The task description (unused but kept for API consistency)
  * @param context - Task context including signals, staleness, etc.
  * @returns Protocol decision with action and options
  */
  async evaluate(_task, context) {
    if (!context.signals.fileExists) {
      return {
        action: "warn",
        reason: "File not found - unable to perform analysis",
        options: [
          {
            id: "create",
            label: "Create the file"
          },
          {
            id: "skip",
            label: "Skip this task"
          }
        ]
      };
    }
    if (context.staleness.stale) {
      return {
        action: "warn",
        reason: context.staleness.suggestion || "Analysis is stale",
        options: [
          {
            id: "refresh",
            label: "Re-run analysis"
          },
          {
            id: "proceed",
            label: "Use cached (may be incomplete)"
          }
        ]
      };
    }
    if (context.ambiguityLevel === "high") {
      return {
        action: "clarify",
        reason: context.ambiguityReason || "Multiple interpretations possible",
        options: (context.ambiguitySuggestions || []).map((s, i) => ({
          id: `opt_${i}`,
          label: s
        }))
      };
    }
    if (context.signals.scopeSize === "massive" && !context.explicitConfirmation) {
      const scope = context.scope;
      return {
        action: "confirm",
        reason: scope ? `This affects ${scope.fileCount} files across ${scope.packageCount} packages.` : "This is a large-scale change.",
        options: [
          {
            id: "proceed",
            label: scope ? `Yes, modify all ${scope.fileCount} files` : "Yes, proceed"
          },
          {
            id: "narrow",
            label: "Let me narrow the scope"
          }
        ]
      };
    }
    const confidenceScore = this.calculateConfidence(context.signals);
    if (confidenceScore < LOW_CONFIDENCE_THRESHOLD) {
      return {
        action: "warn",
        reason: `Low confidence (${Math.round(confidenceScore * 100)}%): Insufficient evidence for reliable analysis`,
        options: [
          {
            id: "proceed",
            label: "Proceed anyway"
          },
          {
            id: "investigate",
            label: "Gather more context first"
          }
        ]
      };
    }
    return {
      action: "proceed",
      reason: "All gates passed"
    };
  }
  /**
  * Calculate simplified confidence score
  */
  calculateConfidence(signals) {
    const base = 0.35;
    const evidenceScore = (signals.graphCoverage + signals.learningRelevance) / 2;
    const historicalScore = signals.similarTaskSuccessRate * 0.7 + signals.userTrustScore * 0.3;
    let score = base + evidenceScore * 0.4 + historicalScore * 0.25;
    if (signals.hasConflictingLearnings) {
      score *= 0.8;
    }
    if (signals.taskAmbiguity === "vague") {
      score *= 0.7;
    }
    return Math.max(0.1, Math.min(0.95, score));
  }
});
(class {
  static {
    __name(this, "GraphEnhancedConfig");
  }
  static {
    __name8(this, "GraphEnhancedConfig");
  }
  semanticWeight;
  keywordWeight;
  graphProximityWeight;
  k;
  constructor(options = {}) {
    let semantic = options.semanticWeight ?? 0.4;
    let keyword = options.keywordWeight ?? 0.3;
    let graph = options.graphProximityWeight ?? 0.3;
    const total = semantic + keyword + graph;
    if (Math.abs(total - 1) > 0.01) {
      semantic /= total;
      keyword /= total;
      graph /= total;
    }
    this.semanticWeight = semantic;
    this.keywordWeight = keyword;
    this.graphProximityWeight = graph;
    this.k = options.k ?? 60;
  }
});
var INTENT_BUDGETS = {
  explore: {
    maxTokens: 500,
    maxLearnings: 10,
    formatCompliance: false
  },
  debug: {
    maxTokens: 300,
    maxLearnings: 5,
    formatCompliance: true
  },
  implement: {
    maxTokens: 400,
    maxLearnings: 7,
    formatCompliance: true
  },
  refactor: {
    maxTokens: 600,
    maxLearnings: 10,
    formatCompliance: true
  },
  review: {
    maxTokens: 800,
    maxLearnings: 15,
    formatCompliance: true
  }
};
var SCOPE_MULTIPLIERS = {
  trivial: 0.3,
  small: 0.5,
  medium: 1,
  large: 1.5,
  massive: 2
};
function calculateResponseBudget(intent, scope) {
  const base = INTENT_BUDGETS[intent];
  const multiplier = SCOPE_MULTIPLIERS[scope] ?? 1;
  return {
    maxTokens: Math.round(base.maxTokens * multiplier),
    maxLearnings: Math.round(base.maxLearnings * multiplier),
    formatCompliance: base.formatCompliance
  };
}
__name(calculateResponseBudget, "calculateResponseBudget");
__name8(calculateResponseBudget, "calculateResponseBudget");
function getIntentBudget(intent) {
  return {
    ...INTENT_BUDGETS[intent]
  };
}
__name(getIntentBudget, "getIntentBudget");
__name8(getIntentBudget, "getIntentBudget");
function getScopeMultiplier(scope) {
  return SCOPE_MULTIPLIERS[scope] ?? 1;
}
__name(getScopeMultiplier, "getScopeMultiplier");
__name8(getScopeMultiplier, "getScopeMultiplier");
function validateBudget(budget) {
  return budget.maxTokens > 0 && budget.maxTokens <= 2e3 && budget.maxLearnings > 0 && budget.maxLearnings <= 50;
}
__name(validateBudget, "validateBudget");
__name8(validateBudget, "validateBudget");
init_check_modes_registry();

// src/utils/tier.ts
function resolveTier(cliTier) {
  if (cliTier && [
    "free",
    "pro",
    "enterprise"
  ].includes(cliTier)) {
    return cliTier;
  }
  const envTier = process.env.VREKO_TIER;
  if (envTier && [
    "free",
    "pro",
    "enterprise"
  ].includes(envTier)) {
    return envTier;
  }
  if (process.env.VREKO_API_KEY) {
    return "pro";
  }
  return "free";
}
__name(resolveTier, "resolveTier");

// src/commands/mcp.ts
async function runStdioMcpServer2(options) {
  const server = await createMcpServer({
    workspaceRoot: options.workspaceRoot,
    tier: options.tier,
    storageMode: options.storageMode ?? "local"
  });
  const transport = new StdioServerTransport();
  await server.connect(transport);
}
__name(runStdioMcpServer2, "runStdioMcpServer");
async function runHttpMcpServer(_options, _port) {
  console.error(chalk2.yellow("[Vreko MCP] HTTP mode is deprecated. Use --stdio instead."));
  console.error(chalk2.yellow("[Vreko MCP] HTTP support may be removed in a future version."));
  console.error(chalk2.red("[Vreko MCP] HTTP mode not yet implemented for local server."));
  console.error(chalk2.blue("[Vreko MCP] Use: vreko mcp --stdio"));
  process.exit(1);
}
__name(runHttpMcpServer, "runHttpMcpServer");
function createMcpCommand() {
  const cmd = createCommand("mcp");
  cmd.description("MCP server management for Cursor/Claude/VS Code integration");
  cmd.option("--stdio", "Use stdio transport (default)").option("--http", "Use HTTP transport (SSE for remote clients like Qoder, Cursor)").option("--port <port>", "HTTP port (default: 3002)", "3002").option("--ws <path>", "Workspace root path (auto-resolved if not provided)").option("--workspace <path>", "Alias for --ws (workspace root path)").option("--tier <tier>", "Override user tier (free|pro|enterprise). Auto-detected from VREKO_API_KEY or VREKO_TIER env var.").option("--fast-mode", "Enable fast startup mode (skip embeddings preloading). Use VREKO_MCP_FAST_MODE=true env var or this flag.").action(async (options) => {
    const useHttp = options.http || process.env.VREKO_MCP_HTTP === "true";
    const useStdio = options.stdio || !useHttp && !options.http;
    if (useHttp) {
      Number.parseInt(options.port, 10);
      const workspacePath = options.ws || options.workspace;
      const workspaceValidation = resolveWorkspaceRoot(workspacePath);
      if (!workspaceValidation.valid) {
        process.exit(1);
      }
      resolveTier(options.tier);
      ({
        workspaceRoot: workspaceValidation.root});
      await runHttpMcpServer();
    } else if (useStdio) {
      try {
        if (options.fastMode) {
          process.env.VREKO_MCP_FAST_MODE = "true";
        }
        const workspacePath = options.ws || options.workspace;
        const workspaceValidation = resolveWorkspaceRoot(workspacePath);
        if (!workspaceValidation.valid) {
          process.exit(1);
        }
        const tier = resolveTier(options.tier);
        const manager = getWorkspaceSessionManager();
        const session = await manager.getSession(workspaceValidation.root);
        const envApiKey = WorkspaceSessionManager.getEnvVarApiKey();
        const authContext = session ? {
          apiKey: session.apiKey ?? session.tokens.accessToken
        } : envApiKey ? {
          apiKey: envApiKey
        } : void 0;
        if (!session && WorkspaceSessionManager.isUsingLegacyEnvVar()) {
          console.error(chalk2.yellow("[Vreko MCP] Using legacy env var auth. Consider running: vr auth login"));
        }
        const serverOptions = {
          workspaceRoot: workspaceValidation.root,
          tier,
          storageMode: "local",
          // Auth context - apiKey is tied to userId via database foreign key
          auth: authContext
        };
        await runStdioMcpServer2(serverOptions);
      } catch (error) {
        console.error("[Vreko MCP] Error:", error instanceof Error ? error.message : String(error));
        process.exit(1);
      }
    } else {
      cmd.outputHelp();
    }
  });
  cmd.command("scan").description("Discover MCP configs across supported AI clients").action(async () => {
    try {
      const result7 = await scanMcpClients();
      console.log(formatScanResult(result7));
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`MCP scan failed: ${message}`);
      process.exit(1);
    }
  });
  cmd.command("link").description("Configure Vreko MCP server in an AI client").requiredOption("--client <client>", "Target client (claude, cursor, vscode, qoder, windsurf, etc.)").option("--workspace <path>", "Workspace root path").option("--api-key <key>", "API key for Pro features").option("--workspace-id <id>", "Workspace ID").option("--doppler", "Use Doppler for environment injection (recommended for local dev)").option("--doppler-project <project>", "Doppler project name", "vreko-shared").option("--doppler-config <config>", "Doppler config name", "dev").option("--sse", "Use HTTP/SSE transport to mcp.vreko.dev (recommended for remote access)").option("--remote", "Alias for --sse (use remote MCP server)").action(async (options) => {
    try {
      const result7 = await linkMcpClient({
        client: options.client.toLowerCase(),
        workspace: options.workspace,
        apiKey: options.apiKey,
        workspaceId: options.workspaceId,
        localCliPath: process.argv[1],
        useDoppler: options.doppler,
        dopplerProject: options.dopplerProject,
        dopplerConfig: options.dopplerConfig,
        useSse: options.sse || options.remote,
        useStreamableHttp: options.sse || options.remote
      });
      console.log(formatLinkResult(result7));
      if (!result7.success) {
        process.exit(1);
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`MCP link failed: ${message}`);
      process.exit(1);
    }
  });
  cmd.command("unlink").description("Remove Vreko MCP server from an AI client").requiredOption("--client <client>", "Target client (claude, cursor, vscode, qoder, windsurf, etc.)").action(async (options) => {
    try {
      const result7 = await unlinkMcpClient({
        client: options.client.toLowerCase()
      });
      console.log(formatUnlinkResult(result7));
      if (!result7.success) {
        process.exit(1);
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`MCP unlink failed: ${message}`);
      process.exit(1);
    }
  });
  cmd.command("fix-conflicts").description("Resolve MCP configuration conflicts (workspace vs global)").option("--auto", "Automatically resolve conflicts (prefer workspace configs)", false).action(async (options) => {
    try {
      const result7 = detectAIClients();
      const conflicts = [];
      for (const client of result7.detected) {
        if (client.hasVreko) {
          const workspaceConfig = detectWorkspaceConfig();
          if (workspaceConfig && client.configPath.startsWith(homedir())) {
            conflicts.push({
              client,
              globalPath: client.configPath,
              workspacePath: workspaceConfig.path,
              workspaceType: workspaceConfig.type
            });
          }
        }
      }
      if (conflicts.length === 0) {
        console.log(chalk2.green("\n\u2713 No MCP configuration conflicts detected"));
        console.log(chalk2.gray("  All configurations are properly scoped\n"));
        return;
      }
      console.log(chalk2.bold("\n\u{1F50D} MCP Configuration Conflicts\n"));
      for (const conflict of conflicts) {
        console.log(chalk2.cyan(`${conflict.client.displayName}:`));
        console.log(chalk2.gray(`  Global:    ${conflict.globalPath}`));
        console.log(chalk2.gray(`  Workspace: ${conflict.workspacePath} (${conflict.workspaceType})`));
        console.log();
        if (options.auto) {
          const result8 = removeVrekoConfig(conflict.client);
          if (result8.success) {
            console.log(chalk2.green("  \u2713 Removed global config (workspace takes precedence)"));
          } else {
            console.log(chalk2.red("  \u2717 Failed to remove global config"));
          }
        } else {
          console.log(chalk2.yellow("  Run with --auto to resolve automatically"));
        }
      }
      if (options.auto) {
        console.log(chalk2.green("\n\u2713 Conflicts resolved"));
      } else {
        console.log(chalk2.gray("\nRun: vr mcp fix-conflicts --auto"));
      }
    } catch (error) {
      console.error("[Vreko MCP] Error:", error instanceof Error ? error.message : String(error));
      process.exit(1);
    }
  });
  cmd.command("repair").description("Repair stale or broken MCP configurations (escape hatch for multi-client issues)").option("--client <client>", "Target client to repair (claude, cursor, qoder, etc.)").option("--all", "Repair all detected clients", false).option("--force", "Force complete reconfiguration", false).option("--workspace <path>", "Workspace root path").action(async (options) => {
    try {
      const workspaceValidation = resolveWorkspaceRoot(options.workspace);
      const workspaceRoot = workspaceValidation.valid ? workspaceValidation.root : process.cwd();
      let clientsToRepair = [];
      if (options.all) {
        const detection = detectAIClients({
          cwd: workspaceRoot
        });
        clientsToRepair = detection.detected.filter((c) => c.hasVreko);
        if (clientsToRepair.length === 0) {
          console.log(chalk2.yellow("No Vreko-configured clients found"));
          return;
        }
      } else if (options.client) {
        const clientName = options.client.toLowerCase();
        const client = getClient(clientName);
        if (!client) {
          console.log(chalk2.red(`Unknown client: ${options.client}`));
          process.exit(1);
        }
        clientsToRepair = [
          client
        ];
      } else {
        const detection = detectAIClients({
          cwd: workspaceRoot
        });
        const configuredClients = detection.detected.filter((c) => c.hasVreko);
        console.log(chalk2.bold("\n\u{1F527} MCP Configuration Validation\n"));
        let hasIssues = false;
        for (const client of configuredClients) {
          const validation = validateClientConfig(client);
          const errors = validation.issues.filter((i) => i.severity === "error");
          const warnings = validation.issues.filter((i) => i.severity === "warning");
          if (errors.length > 0) {
            console.log(chalk2.red(`\u2717 ${client.displayName}: ${errors.length} error(s)`));
            for (const err6 of errors) {
              console.log(chalk2.gray(`    - ${err6.message}`));
              if (err6.fix) {
                console.log(chalk2.gray(`      Fix: ${err6.fix}`));
              }
            }
            hasIssues = true;
          } else if (warnings.length > 0) {
            console.log(chalk2.yellow(`\u26A0 ${client.displayName}: ${warnings.length} warning(s)`));
            for (const warn2 of warnings) {
              console.log(chalk2.gray(`    - ${warn2.message}`));
            }
          } else {
            console.log(chalk2.green(`\u2713 ${client.displayName}: Valid`));
          }
        }
        if (hasIssues) {
          console.log(chalk2.cyan("\nTo repair all clients, run:"));
          console.log(chalk2.gray("  vr mcp repair --all\n"));
        } else {
          console.log(chalk2.green("\n\u2713 All configurations are valid\n"));
        }
        return;
      }
      console.log(chalk2.bold("\n\u{1F527} Repairing MCP Configurations\n"));
      let repairCount = 0;
      for (const client of clientsToRepair) {
        console.log(chalk2.cyan(`Repairing ${client.displayName}...`));
        const result7 = repairClientConfig(client, {
          workspaceRoot,
          force: options.force
        });
        if (result7.success) {
          console.log(chalk2.green(`  \u2713 ${client.displayName} repaired`));
          if (result7.fixed.length > 0) {
            for (const fix of result7.fixed) {
              console.log(chalk2.gray(`    - ${fix}`));
            }
          }
          repairCount++;
        } else {
          console.log(chalk2.red(`  \u2717 ${client.displayName} repair failed`));
          if (result7.error) {
            console.log(chalk2.gray(`    Error: ${result7.error}`));
          }
          for (const remaining of result7.remaining) {
            console.log(chalk2.gray(`    - ${remaining}`));
          }
        }
      }
      if (repairCount > 0) {
        console.log(chalk2.green(`
\u2713 Repaired ${repairCount}/${clientsToRepair.length} client(s)`));
        console.log(chalk2.gray("  Restart your IDE/editor to apply changes\n"));
      } else {
        console.log(chalk2.yellow("\n\u26A0 No clients were repaired. Try --force to reconfigure completely.\n"));
      }
    } catch (error) {
      console.error(chalk2.red("Repair failed:"), error instanceof Error ? error.message : String(error));
      process.exit(1);
    }
  });
  return cmd;
}
__name(createMcpCommand, "createMcpCommand");
var mcpCommand = createMcpCommand();
function createToolsCommand() {
  const tools = new Command("tools").description("Configure AI tools");
  tools.command("configure").description("Auto-setup MCP for Cursor, Claude, or other AI tools").option("--cursor", "Configure for Cursor only").option("--claude", "Configure for Claude Desktop only").option("--windsurf", "Configure for Windsurf only").option("--continue", "Configure for Continue only").option("--vscode", "Configure for VS Code only").option("--zed", "Configure for Zed only").option("--cline", "Configure for Cline only").option("--gemini", "Configure for Gemini/Antigravity only").option("--aider", "Configure for Aider only").option("--roo-code", "Configure for Roo Code only").option("--qoder", "Configure for Qoder only").option("--list", "List available tools").option("--dry-run", "Show what would be configured without writing").option("--force", "Reconfigure even if already set up").option("-y, --yes", "Skip confirmation prompts (for CI/scripts)").option("--non-interactive", "Run without prompts (alias for --yes, for programmatic use)").option("--json", "Output structured JSON result (for extension integration)").option("--api-key <key>", "API key for Pro features").option("--npm", "Use npm-installed CLI via npx (recommended for npm users)").option("--dev", "Use local development mode (direct node execution with inferred workspace)").option("--remote", "Use remote MCP server instead of local service (requires API key)").option("--workspace <path>", "Override workspace root path").action(async (options) => {
    try {
      const skipPrompts = options.yes || options.nonInteractive || cliState.yes;
      const jsonOutput = options.json;
      if (options.list) {
        await listTools(jsonOutput);
        return;
      }
      const toolsToConfig = [];
      if (options.cursor) {
        toolsToConfig.push("cursor");
      }
      if (options.claude) {
        toolsToConfig.push("claude");
      }
      if (options.windsurf) {
        toolsToConfig.push("windsurf");
      }
      if (options.continue) {
        toolsToConfig.push("continue");
      }
      if (options.vscode) {
        toolsToConfig.push("vscode");
      }
      if (options.zed) {
        toolsToConfig.push("zed");
      }
      if (options.cline) {
        toolsToConfig.push("cline");
      }
      if (options.gemini) {
        toolsToConfig.push("gemini");
      }
      if (options.aider) {
        toolsToConfig.push("aider");
      }
      if (options["roo-code"] || options.rooCode) {
        toolsToConfig.push("roo-code");
      }
      if (options.qoder) {
        toolsToConfig.push("qoder");
      }
      if (toolsToConfig.length === 0) {
        const result7 = await autoConfigureTools({
          dryRun: options.dryRun,
          force: options.force,
          skipPrompts,
          jsonOutput,
          apiKey: options.apiKey,
          devMode: options.dev,
          npmMode: options.npm,
          remoteMode: options.remote,
          workspaceOverride: options.workspace
        });
        if (jsonOutput) {
          print(JSON.stringify(result7, null, 2));
        }
      } else {
        for (const tool of toolsToConfig) {
          await configureTool(tool, options.dryRun, skipPrompts, options.apiKey, options.dev, options.npm, options.workspace, options.remote);
        }
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      if (options.json) {
        const errorResult = {
          success: false,
          clients: {},
          configured: [],
          skipped: [],
          notInstalled: [],
          failed: [],
          version: getVersion(),
          error: message
        };
        print(JSON.stringify(errorResult, null, 2));
        process.exit(1);
      }
      print(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  tools.command("status").description("Check MCP configuration status").option("--verbose", "Show detailed validation information").action(async (options) => {
    await checkToolsStatus(options.verbose);
  });
  tools.command("validate").description("Validate MCP configurations for all detected AI tools").option("--verbose", "Show detailed validation issues").action(async (options) => {
    await validateTools(options.verbose);
  });
  tools.command("repair").description("Repair broken MCP configurations").option("-y, --yes", "Skip confirmation prompts").option("--workspace <path>", "Override workspace root path").option("--api-key <key>", "API key for Pro features").action(async (options) => {
    await repairTools(options.yes || cliState.yes, options.workspace, options.apiKey);
  });
  return tools;
}
__name(createToolsCommand, "createToolsCommand");
function getVersion() {
  try {
    const pkg = require_package();
    return pkg.version || "0.0.0";
  } catch {
    return "0.0.0";
  }
}
__name(getVersion, "getVersion");
async function listTools(jsonOutput = false) {
  const detection = detectAIClients();
  if (jsonOutput) {
    const configured = detection.detected.filter((c) => c.hasVreko);
    const result7 = {
      success: true,
      clients: configured.reduce((acc, c) => {
        acc[c.name] = "already_configured";
        return acc;
      }, {}),
      configured: configured.map((c) => c.name),
      skipped: [],
      notInstalled: detection.clients.filter((c) => !c.exists).map((c) => c.name),
      failed: [],
      version: getVersion()
    };
    print(JSON.stringify(result7, null, 2));
    return;
  }
  if (detection.clients.length === 0) {
    print("No AI clients detected. Install Cursor, Claude, or another supported client.");
    return;
  }
  for (const client of detection.clients) {
    const status = client.exists ? client.hasVreko ? chalk2.green("\u2713 Configured") : chalk2.yellow("\u25CB Needs setup") : chalk2.gray("Not installed");
    print(`  ${client.displayName.padEnd(20)} ${status}`);
  }
}
__name(listTools, "listTools");
async function autoConfigureTools(options) {
  const { dryRun, force, skipPrompts, jsonOutput, apiKey: providedApiKey, devMode, npmMode, remoteMode, workspaceOverride } = options;
  const result7 = {
    success: true,
    clients: {},
    configured: [],
    skipped: [],
    notInstalled: [],
    failed: [],
    version: getVersion()
  };
  const detection = detectAIClients();
  for (const client of detection.clients) {
    if (!client.exists) {
      result7.notInstalled.push(client.name);
      result7.clients[client.name] = "not_installed";
    }
  }
  if (detection.detected.length === 0) {
    if (!jsonOutput) {
      print("No AI tools detected. Supported tools: Cursor, Claude Code, GitHub Copilot, Windsurf, Cline, and more.");
    }
    return result7;
  }
  const needsSetup = force ? detection.detected : detection.needsSetup;
  for (const client of detection.detected) {
    if (client.hasVreko && !needsSetup.includes(client)) {
      result7.skipped.push(client.name);
      result7.clients[client.name] = "already_configured";
    }
  }
  if (needsSetup.length === 0) {
    if (!jsonOutput) {
      showNextSteps();
    }
    return result7;
  }
  if (!jsonOutput) {
    print("Detected AI tools:");
    for (const client of detection.detected) {
      const status = client.hasVreko ? chalk2.green("(configured)") : chalk2.yellow("(needs setup)");
      print(`  ${client.displayName.padEnd(20)} ${status}`);
    }
  }
  if (!skipPrompts && !jsonOutput) {
    const clientNames = needsSetup.map((c) => c.displayName).join(", ");
    const proceedResult = await clack3.confirm({
      message: `Configure \u{1F98E} Vreko for ${clientNames}?`,
      initialValue: true
    });
    if (clack3.isCancel(proceedResult)) {
      clack3.cancel("Cancelled.");
      result7.success = false;
      result7.error = "User cancelled";
      return result7;
    }
    if (!proceedResult) {
      result7.success = false;
      result7.error = "User cancelled";
      return result7;
    }
  }
  const apiKey = await resolveApiKey(providedApiKey, skipPrompts || jsonOutput);
  for (const client of needsSetup) {
    const configResult = await configureClientWithResult(client, dryRun, apiKey, devMode, npmMode, workspaceOverride, remoteMode);
    if (configResult.success) {
      result7.configured.push(client.name);
      result7.clients[client.name] = "configured";
    } else {
      result7.failed.push(client.name);
      result7.clients[client.name] = "failed";
      result7.success = false;
    }
  }
  if (!jsonOutput) {
    showNextSteps();
  }
  return result7;
}
__name(autoConfigureTools, "autoConfigureTools");
async function configureTool(toolName, dryRun, skipPrompts = false, providedApiKey, devMode = false, npmMode = false, workspaceOverride, remoteMode = false) {
  const detection = detectAIClients();
  const client = detection.clients.find((c) => c.name === toolName);
  if (!client) {
    return;
  }
  if (!client.exists) {
    return;
  }
  const apiKey = await resolveApiKey(providedApiKey, skipPrompts);
  await configureClient(client, dryRun, apiKey, devMode, npmMode, workspaceOverride, remoteMode);
  showNextSteps();
}
__name(configureTool, "configureTool");
function isGlobalConfig(configPath) {
  const home = homedir();
  return configPath.includes(home) && !configPath.includes(process.cwd());
}
__name(isGlobalConfig, "isGlobalConfig");
async function configureClient(client, dryRun, apiKey, devMode = false, npmMode = false, workspaceOverride, remoteMode = false) {
  const spinner2 = ora(`Configuring ${client.displayName}...`).start();
  try {
    const workspaceRoot = workspaceOverride || findWorkspaceRoot2(process.cwd());
    const workspaceConfig = detectWorkspaceConfig(workspaceRoot);
    let localCliPath;
    let effectiveDevMode = devMode;
    let effectiveNpmMode = npmMode;
    if (workspaceConfig && isGlobalConfig(client.configPath)) {
      if (client.format === "qoder" && client.hasVreko) {
        const existingValidation = validateClientConfig(client);
        if (existingValidation.valid) {
          spinner2.info(`${client.displayName} workspace config detected (global config valid, skipping)`);
          return;
        }
        spinner2.text = `Repairing broken global ${client.displayName} config...`;
        effectiveDevMode = false;
        effectiveNpmMode = false;
        localCliPath = void 0;
      } else {
        spinner2.info(`${client.displayName} workspace config detected`);
        return;
      }
    }
    if (effectiveDevMode) {
      localCliPath = findCliDistPath(workspaceRoot);
      if (!localCliPath) {
        spinner2.fail("Could not find CLI dist. Run 'pnpm build' first.");
        return;
      }
      spinner2.text = `Configuring ${client.displayName} (dev mode)...`;
    } else if (effectiveNpmMode) {
      spinner2.text = `Configuring ${client.displayName} (npm/npx mode)...`;
    }
    if (client.hasVreko) {
      spinner2.text = `Validating existing config for ${client.displayName}...`;
      const validation = validateClientConfig(client);
      if (!validation.valid) {
        const errors = validation.issues.filter((i) => i.severity === "error");
        if (errors.length > 0) {
          spinner2.warn("Existing config has issues, will be replaced");
        }
      }
    }
    const mcpConfig = getVrekoMCPConfig({
      apiKey,
      useNpx: effectiveNpmMode,
      useLocalDev: effectiveDevMode,
      localCliPath,
      workspaceRoot,
      client: client.format,
      useStreamableHttp: remoteMode
    });
    if (dryRun) {
      spinner2.info(`Would configure ${client.displayName}`);
      if (effectiveDevMode) {
        spinner2.info("  Mode: local dev (dist/index.js)");
      } else if (effectiveNpmMode) {
        spinner2.info("  Mode: npm/npx");
      } else {
        spinner2.info("  Mode: system install");
      }
      return;
    }
    const result7 = writeClientConfig(client, mcpConfig);
    if (result7.success) {
      const postValidation = validateClientConfig({
        ...client,
        hasVreko: true
      });
      const modeLabel = effectiveDevMode ? " (dev mode)" : effectiveNpmMode ? " (npm mode)" : "";
      if (postValidation.valid) {
        spinner2.succeed(`Configured ${client.displayName}${modeLabel}`);
      } else {
        const warnings = postValidation.issues.filter((i) => i.severity === "warning");
        if (warnings.length > 0) {
          spinner2.succeed(`Configured ${client.displayName} (with warnings)`);
          for (const warning of warnings) {
            print(`    ${chalk2.yellow("\u26A0")} ${warning.message}`);
          }
        } else {
          spinner2.succeed(`Configured ${client.displayName}${modeLabel}`);
        }
      }
      if (effectiveDevMode || effectiveNpmMode) {
        print(`    Configured in ${effectiveDevMode ? "dev" : "npm"} mode  -  restart your AI tool to load changes`);
      }
      if (result7.backup) {
        print(`    Previous config backed up to: ${result7.backup}`);
      }
    } else {
      spinner2.fail(`Failed to configure ${client.displayName}`);
    }
  } catch (error) {
    spinner2.fail(`Failed to configure ${client.displayName}`);
    throw error;
  }
}
__name(configureClient, "configureClient");
async function configureClientWithResult(client, dryRun, apiKey, devMode = false, npmMode = false, workspaceOverride, remoteMode = false) {
  try {
    const workspaceRoot = workspaceOverride || findWorkspaceRoot2(process.cwd());
    const workspaceConfig = detectWorkspaceConfig(workspaceRoot);
    let localCliPath;
    let effectiveDevMode = devMode;
    let effectiveNpmMode = npmMode;
    if (workspaceConfig && isGlobalConfig(client.configPath)) {
      if (client.format === "qoder" && client.hasVreko) {
        const existingValidation = validateClientConfig(client);
        if (existingValidation.valid) {
          return {
            success: true
          };
        }
        effectiveDevMode = false;
        effectiveNpmMode = false;
        localCliPath = void 0;
      } else {
        return {
          success: true
        };
      }
    }
    if (effectiveDevMode) {
      localCliPath = findCliDistPath(workspaceRoot);
      if (!localCliPath) {
        return {
          success: false,
          error: "Could not find CLI dist. Run 'pnpm build' first."
        };
      }
    }
    const mcpConfig = getVrekoMCPConfig({
      apiKey,
      useNpx: effectiveNpmMode,
      useLocalDev: effectiveDevMode,
      localCliPath,
      workspaceRoot,
      client: client.format,
      useStreamableHttp: remoteMode
    });
    if (dryRun) {
      return {
        success: true
      };
    }
    const result7 = writeClientConfig(client, mcpConfig);
    if (result7.success) {
      const postValidation = validateClientConfig({
        ...client,
        hasVreko: true
      });
      if (!postValidation.valid) {
        const errors = postValidation.issues.filter((i) => i.severity === "error");
        if (errors.length > 0) {
          return {
            success: false,
            error: errors.map((e) => e.message).join("; ")
          };
        }
      }
      return {
        success: true
      };
    }
    return {
      success: false,
      error: result7.error
    };
  } catch (error) {
    return {
      success: false,
      error: error instanceof Error ? error.message : "Unknown error"
    };
  }
}
__name(configureClientWithResult, "configureClientWithResult");
function findWorkspaceRoot2(startDir) {
  let dir = startDir;
  const root = "/";
  while (dir !== root) {
    if (existsSync(join(dir, ".git")) || existsSync(join(dir, "package.json")) || existsSync(join(dir, ".vreko"))) {
      return dir;
    }
    dir = join(dir, "..");
  }
  return startDir;
}
__name(findWorkspaceRoot2, "findWorkspaceRoot");
function findCliDistPath(workspaceRoot) {
  const possiblePaths = [
    join(workspaceRoot, "apps", "cli", "dist", "index.js"),
    join(workspaceRoot, "dist", "index.js")
  ];
  for (const path4 of possiblePaths) {
    if (existsSync(path4)) {
      return path4;
    }
  }
  return void 0;
}
__name(findCliDistPath, "findCliDistPath");
async function resolveApiKey(providedApiKey, skipPrompts = false) {
  if (providedApiKey) {
    return providedApiKey;
  }
  const envKey = process.env.VREKO_API_KEY;
  if (envKey) {
    return envKey;
  }
  if (await isLoggedIn()) {
    const credentials = await getCredentials();
    if (credentials?.accessToken) {
      return credentials.accessToken;
    }
  }
  if (!skipPrompts) {
    const wantApiKeyResult = await clack3.confirm({
      message: "Do you have a \u{1F98E} Vreko API key for Pro features?",
      initialValue: false
    });
    if (clack3.isCancel(wantApiKeyResult)) {
      clack3.cancel("Cancelled.");
      return void 0;
    }
    if (wantApiKeyResult) {
      const keyResult = await clack3.password({
        message: "Enter your API key:",
        mask: "*"
      });
      if (clack3.isCancel(keyResult)) {
        clack3.cancel("Cancelled.");
        return void 0;
      }
      return keyResult || void 0;
    }
  }
  return void 0;
}
__name(resolveApiKey, "resolveApiKey");
async function checkToolsStatus(verbose = false) {
  const detection = detectAIClients();
  let hasIssues = false;
  for (const client of detection.clients) {
    let icon;
    let status;
    if (!client.exists) {
      icon = chalk2.gray("\u25CB");
      status = chalk2.gray("Not installed");
    } else if (client.hasVreko) {
      const validation = validateClientConfig(client);
      if (validation.valid) {
        icon = chalk2.green("\u2713");
        status = chalk2.green("Configured");
      } else {
        const errors = validation.issues.filter((i) => i.severity === "error");
        const warnings = validation.issues.filter((i) => i.severity === "warning");
        if (errors.length > 0) {
          icon = chalk2.red("\u2717");
          status = chalk2.red(`Invalid (${errors.length} error(s))`);
          hasIssues = true;
        } else if (warnings.length > 0) {
          icon = chalk2.yellow("\u26A0");
          status = chalk2.yellow(`Configured (${warnings.length} warning(s))`);
        } else {
          icon = chalk2.green("\u2713");
          status = chalk2.green("Configured");
        }
      }
      print(`  ${icon} ${client.displayName.padEnd(20)} ${status}`);
      if (verbose && validation.issues.length > 0) {
        for (const issue of validation.issues) {
          const issueIcon = issue.severity === "error" ? chalk2.red("  \u2717") : issue.severity === "warning" ? chalk2.yellow("  \u26A0") : chalk2.blue("  \u2139");
          print(`    ${issueIcon} ${issue.message}${issue.fix ? `  \u2192  ${issue.fix}` : ""}`);
        }
      }
    } else {
      icon = chalk2.yellow("\u25CB");
      status = chalk2.yellow("Detected but not configured");
      print(`  ${icon} ${client.displayName.padEnd(20)} ${status}`);
    }
  }
  if (hasIssues) {
    print();
    print("  Run `vreko tools repair` to fix configuration issues");
  } else if (detection.needsSetup.length > 0) {
    print();
    print("  Run `vreko tools configure` to set up detected tools");
  } else if (detection.detected.length > 0) {
    print();
    print("  All detected tools are configured");
  } else {
    print("  No AI tools detected");
  }
}
__name(checkToolsStatus, "checkToolsStatus");
async function validateTools(verbose = false) {
  const detection = detectAIClients();
  const configured = detection.detected.filter((c) => c.hasVreko);
  if (configured.length === 0) {
    print("No tools configured. Run `vreko tools configure` to get started.");
    return;
  }
  let totalErrors = 0;
  let totalWarnings = 0;
  for (const client of configured) {
    const validation = validateClientConfig(client);
    const errors = validation.issues.filter((i) => i.severity === "error");
    const warnings = validation.issues.filter((i) => i.severity === "warning");
    const infos = validation.issues.filter((i) => i.severity === "info");
    totalErrors += errors.length;
    totalWarnings += warnings.length;
    if (validation.valid && errors.length === 0 && warnings.length === 0) {
      print(`  ${chalk2.green("\u2713")} ${client.displayName}`);
    } else if (errors.length > 0) {
      print(`  ${chalk2.red("\u2717")} ${client.displayName}  -  ${errors.length} error(s)`);
    } else {
      print(`  ${chalk2.yellow("\u26A0")} ${client.displayName}  -  ${warnings.length} warning(s)`);
    }
    if (verbose || errors.length > 0) {
      for (const issue of [
        ...errors,
        ...warnings,
        ...verbose ? infos : []
      ]) {
        const icon = issue.severity === "error" ? chalk2.red("  \u2717") : issue.severity === "warning" ? chalk2.yellow("  \u26A0") : chalk2.blue("  \u2139");
        print(`    ${icon} ${issue.message}${issue.fix ? `  \u2192  ${issue.fix}` : ""}`);
      }
    }
  }
  print();
  if (totalErrors > 0) {
    print(`  ${chalk2.red(`${totalErrors} error(s)`)} found. Run \`vreko tools repair\` to fix.`);
    process.exit(1);
  } else if (totalWarnings > 0) {
    print(`  ${chalk2.yellow(`${totalWarnings} warning(s)`)} found. Run \`vreko tools repair\` to fix.`);
  } else {
    print(`  ${chalk2.green("All configurations valid")}`);
  }
}
__name(validateTools, "validateTools");
async function repairTools(skipPrompts = false, workspaceOverride, providedApiKey) {
  const detection = detectAIClients();
  const configured = detection.detected.filter((c) => c.hasVreko);
  if (configured.length === 0) {
    return;
  }
  const clientsWithIssues = [];
  for (const client of configured) {
    const validation = validateClientConfig(client);
    if (!validation.valid || validation.issues.some((i) => i.severity === "error" || i.severity === "warning")) {
      clientsWithIssues.push({
        client,
        validation
      });
    }
  }
  if (clientsWithIssues.length === 0) {
    print("All configurations are valid  -  nothing to repair.");
    return;
  }
  print(`Found issues in ${clientsWithIssues.length} configuration(s):`);
  for (const { client, validation } of clientsWithIssues) {
    const errors = validation.issues.filter((i) => i.severity === "error");
    const warnings = validation.issues.filter((i) => i.severity === "warning");
    print(`  ${client.displayName}:`);
    for (const issue of [
      ...errors,
      ...warnings
    ]) {
      const icon = issue.severity === "error" ? chalk2.red("  \u2717") : chalk2.yellow("  \u26A0");
      print(`    ${icon} ${issue.message}`);
    }
  }
  if (!skipPrompts) {
    const proceedResult = await clack3.confirm({
      message: `Repair ${clientsWithIssues.length} configuration(s)?`,
      initialValue: true
    });
    if (clack3.isCancel(proceedResult)) {
      clack3.cancel("Cancelled.");
      return;
    }
    if (!proceedResult) {
      return;
    }
  }
  const apiKey = await resolveApiKey(providedApiKey, skipPrompts);
  const spinner2 = ora("Repairing configurations...").start();
  let repaired = 0;
  let failed = 0;
  for (const { client } of clientsWithIssues) {
    spinner2.text = `Repairing ${client.displayName}...`;
    const result7 = repairClientConfig(client, {
      apiKey,
      workspaceRoot: workspaceOverride || findWorkspaceRoot2(process.cwd()),
      force: true
    });
    if (result7.success) {
      repaired++;
    } else {
      failed++;
      spinner2.warn(`Failed to repair ${client.displayName}: ${result7.error}`);
    }
  }
  spinner2.stop();
  if (repaired > 0) {
    print(`  ${chalk2.green("\u2713")} Repaired ${repaired} configuration(s)`);
  }
  if (failed > 0) {
    print(`  ${chalk2.red("\u2717")} Failed to repair ${failed} configuration(s)`);
  }
  if (repaired > 0) {
    print();
    print("  Restart your AI tool to load the repaired configuration.");
  }
}
__name(repairTools, "repairTools");
function showNextSteps() {
  print();
  print("Next steps:");
  print("  Restart your AI tool to load Vreko MCP.");
  print("  Run `vreko service start` to begin protecting your workspace.");
  print();
}
__name(showNextSteps, "showNextSteps");
var POINTER_SENTINEL = "<!-- vreko-pointer -->";
var POINTER_LINES = {
  claude: "@.vreko/docs/INTELLIGENCE.md",
  cursor: "See: .vreko/docs/INTELLIGENCE.md",
  copilot: "Read: .vreko/docs/INTELLIGENCE.md",
  universal: "See: .vreko/docs/INTELLIGENCE.md",
  windsurf: "See: .vreko/docs/INTELLIGENCE.md"
};
var TOOL_CANDIDATES = [
  {
    tool: "claude",
    candidates: [
      "CLAUDE.md",
      ".claude/CLAUDE.md"
    ]
  },
  {
    tool: "cursor",
    candidates: [
      ".cursor/rules/vreko.mdc",
      ".cursorrules"
    ]
  },
  {
    tool: "copilot",
    candidates: [
      ".github/copilot-instructions.md"
    ]
  },
  {
    tool: "universal",
    candidates: [
      "AGENTS.md"
    ]
  },
  {
    tool: "windsurf",
    candidates: [
      ".windsurfrules",
      ".windsurf/rules/vreko.md"
    ]
  }
];
function findWorkspaceRoot3(startDir) {
  let dir = startDir;
  const root = "/";
  while (dir !== root) {
    if (existsSync(join(dir, ".vreko")) || existsSync(join(dir, ".git"))) {
      return dir;
    }
    dir = join(dir, "..");
  }
  return startDir;
}
__name(findWorkspaceRoot3, "findWorkspaceRoot");
async function fileExists(abs) {
  try {
    await access(abs);
    return true;
  } catch {
    return false;
  }
}
__name(fileExists, "fileExists");
async function readConfig(workspaceRoot) {
  const configPath = join(workspaceRoot, ".vreko", "config.json");
  try {
    const raw = await readFile(configPath, "utf8");
    return JSON.parse(raw);
  } catch {
    return {};
  }
}
__name(readConfig, "readConfig");
async function writeConfig(workspaceRoot, config2) {
  const configPath = join(workspaceRoot, ".vreko", "config.json");
  const tmpPath = `${configPath}.vreko-tmp`;
  await writeFile(tmpPath, JSON.stringify(config2, null, 2), "utf8");
  await rename(tmpPath, configPath);
}
__name(writeConfig, "writeConfig");
async function detectTargets(workspaceRoot) {
  const config2 = await readConfig(workspaceRoot);
  const optedOutFiles = config2.projections?.docs?.optedOutFiles ?? [];
  const targets = [];
  for (const { tool, candidates } of TOOL_CANDIDATES) {
    for (const candidate of candidates) {
      const abs = join(workspaceRoot, candidate);
      if (await fileExists(abs)) {
        const content = await readFile(abs, "utf8").catch(() => "");
        const alreadyPresent = content.includes(POINTER_SENTINEL);
        const optedOut = optedOutFiles.some((f) => f === candidate || abs.endsWith(f));
        targets.push({
          file: abs,
          relFile: candidate,
          tool,
          pointerLine: POINTER_LINES[tool],
          alreadyPresent,
          optedOut
        });
        break;
      }
    }
  }
  return targets;
}
__name(detectTargets, "detectTargets");
async function injectPointer(filePath, pointerLine) {
  let content;
  try {
    const existing = await readFile(filePath, "utf8");
    if (existing.includes(POINTER_SENTINEL)) {
      return;
    }
    const trimmed = existing.trimEnd();
    content = `${trimmed}

${POINTER_SENTINEL}
${pointerLine}
`;
  } catch {
    content = `${POINTER_SENTINEL}
${pointerLine}
`;
  }
  const tmpPath = `${filePath}.vreko-tmp`;
  await writeFile(tmpPath, content, "utf8");
  await rename(tmpPath, filePath);
}
__name(injectPointer, "injectPointer");
async function removePointer(filePath) {
  const raw = await readFile(filePath, "utf8");
  const lines = raw.split("\n");
  const newLines = [];
  let i = 0;
  while (i < lines.length) {
    if (lines[i] === POINTER_SENTINEL) {
      i += 1;
      if (i < lines.length) {
        i += 1;
      }
      continue;
    }
    newLines.push(lines[i]);
    i += 1;
  }
  while (newLines.length > 0 && newLines[newLines.length - 1] === "") {
    newLines.pop();
  }
  const cleaned = newLines.join("\n") + (newLines.length > 0 ? "\n" : "");
  const tmpPath = `${filePath}.vreko-tmp`;
  await writeFile(tmpPath, cleaned, "utf8");
  await rename(tmpPath, filePath);
}
__name(removePointer, "removePointer");
async function previewDocs() {
  const workspaceRoot = findWorkspaceRoot3(process.cwd());
  const targets = await detectTargets(workspaceRoot);
  if (targets.length === 0) {
    print("No AI tool config files detected in this workspace.");
    print("Supported files: CLAUDE.md, .cursorrules, .cursor/rules/vreko.mdc,");
    print("  .github/copilot-instructions.md, AGENTS.md, .windsurfrules");
    return;
  }
  print(chalk2.bold("Docs projection preview (dry-run  -  no files written):"));
  print();
  for (const target of targets) {
    if (target.optedOut) {
      print(`  ${chalk2.gray("\u25CB")} ${target.relFile}  -  ${chalk2.gray("user-opted-out (skipped)")}`);
    } else if (target.alreadyPresent) {
      print(`  ${chalk2.green("\u2713")} ${target.relFile}  -  pointer already present`);
    } else {
      print(`  ${chalk2.cyan("+")} Would add to ${target.relFile}: ${chalk2.dim(target.pointerLine)}`);
    }
  }
  print();
  print(`Run ${chalk2.bold("vreko projections docs enable")} to inject pointer lines.`);
}
__name(previewDocs, "previewDocs");
async function enableDocs(options) {
  const workspaceRoot = findWorkspaceRoot3(process.cwd());
  const targets = await detectTargets(workspaceRoot);
  if (targets.length === 0) {
    print("No AI tool config files detected in this workspace.");
    return;
  }
  const eligible = targets.filter((t) => {
    if (t.alreadyPresent) {
      return false;
    }
    if (t.optedOut && !options.force) {
      return false;
    }
    return true;
  });
  const alreadyDone = targets.filter((t) => t.alreadyPresent);
  if (alreadyDone.length > 0) {
    print(chalk2.gray(`Already configured: ${alreadyDone.map((t) => t.relFile).join(", ")}`));
  }
  if (eligible.length === 0) {
    print("All detected config files already have pointer injection or are opted out.");
    print(`Use ${chalk2.bold("--force")} to re-enable opted-out files.`);
    return;
  }
  print(chalk2.bold("Vreko will add a pointer line to the following files:"));
  print();
  for (const t of eligible) {
    print(`  ${t.relFile}: ${chalk2.dim(t.pointerLine)}`);
  }
  print();
  clack3.intro("Vreko Docs Projection");
  const choices = eligible.map((t) => ({
    value: t.file,
    label: t.relFile,
    hint: t.pointerLine
  }));
  const selected = await clack3.multiselect({
    message: "Select files to inject pointer lines into:",
    options: choices,
    required: false
  });
  if (clack3.isCancel(selected)) {
    clack3.cancel("Cancelled. No changes made.");
    return;
  }
  const selectedPaths = selected;
  if (selectedPaths.length === 0) {
    clack3.cancel("No files selected. No changes made.");
    return;
  }
  const config2 = await readConfig(workspaceRoot);
  config2.projections = config2.projections ?? {};
  config2.projections.docs = config2.projections.docs ?? {};
  config2.projections.docs.enabled = true;
  config2.projections.docs.enabledAt = (/* @__PURE__ */ new Date()).toISOString();
  config2.projections.docs.approvedFiles = config2.projections.docs.approvedFiles ?? [];
  config2.projections.docs.optedOutFiles = config2.projections.docs.optedOutFiles ?? [];
  let injected = 0;
  for (const filePath of selectedPaths) {
    const target = eligible.find((t) => t.file === filePath);
    if (!target) {
      continue;
    }
    await injectPointer(filePath, target.pointerLine);
    if (!config2.projections.docs.approvedFiles.includes(filePath)) {
      config2.projections.docs.approvedFiles.push(filePath);
    }
    config2.projections.docs.optedOutFiles = config2.projections.docs.optedOutFiles.filter((f) => f !== filePath && !filePath.endsWith(f));
    injected++;
  }
  await writeConfig(workspaceRoot, config2);
  clack3.outro(injected === 1 ? "Added pointer to 1 file." : `Added pointer to ${injected} files.`);
}
__name(enableDocs, "enableDocs");
async function disableDocs() {
  const workspaceRoot = findWorkspaceRoot3(process.cwd());
  const targets = await detectTargets(workspaceRoot);
  const active = targets.filter((t) => t.alreadyPresent);
  if (active.length === 0) {
    print("No active pointer injections found. Nothing to disable.");
    return;
  }
  for (const target of active) {
    await removePointer(target.file);
  }
  const config2 = await readConfig(workspaceRoot);
  config2.projections = config2.projections ?? {};
  config2.projections.docs = config2.projections.docs ?? {};
  config2.projections.docs.enabled = false;
  config2.projections.docs.optedOutFiles = config2.projections.docs.optedOutFiles ?? [];
  config2.projections.docs.approvedFiles = config2.projections.docs.approvedFiles ?? [];
  for (const target of active) {
    if (!config2.projections.docs.optedOutFiles.includes(target.file)) {
      config2.projections.docs.optedOutFiles.push(target.file);
    }
    config2.projections.docs.approvedFiles = config2.projections.docs.approvedFiles.filter((f) => f !== target.file);
  }
  await writeConfig(workspaceRoot, config2);
  const count = active.length;
  print(count === 1 ? chalk2.green("Removed pointer from 1 file. Docs projection disabled.") : chalk2.green(`Removed pointer from ${count} files. Docs projection disabled.`));
}
__name(disableDocs, "disableDocs");
async function statusDocs() {
  const workspaceRoot = findWorkspaceRoot3(process.cwd());
  const targets = await detectTargets(workspaceRoot);
  print(chalk2.bold("Docs projection status:"));
  print();
  if (targets.length === 0) {
    print(`  ${chalk2.gray("\u25CB")} (no AI tool config files detected in this workspace)`);
  }
  for (const target of targets) {
    let icon;
    let label;
    if (target.alreadyPresent) {
      icon = chalk2.green("\u25CF");
      label = chalk2.green("active");
    } else if (target.optedOut) {
      icon = chalk2.gray("\u25CB");
      label = chalk2.gray("user-opted-out");
    } else {
      icon = chalk2.yellow("\u25CB");
      label = chalk2.yellow("available");
    }
    print(`  ${icon} ${target.relFile.padEnd(40)} ${label}`);
  }
  print();
  const activeCount = targets.filter((t) => t.alreadyPresent).length;
  const config2 = await readConfig(workspaceRoot);
  const enabled = config2.projections?.docs?.enabled === true;
  if (activeCount > 0) {
    print(`  Projection enabled: ${chalk2.green("yes")}`);
  } else {
    print(`  Projection enabled: ${enabled ? chalk2.green("yes") : chalk2.gray("no")} (no active pointers)`);
  }
  if (activeCount === 0 && targets.length > 0) {
    print();
    print(`  Run ${chalk2.bold("vreko projections docs enable")} to add pointer lines.`);
  }
}
__name(statusDocs, "statusDocs");
function createProjectionsCommand() {
  const cmd = new Command("projections");
  cmd.description("Manage Vreko intelligence projections");
  const docs = new Command("docs");
  docs.description("Manage .vreko/docs/ pointer injection into AI tool config files");
  docs.command("enable").description("Enable docs projection for this workspace (inject pointer lines)").option("--force", "Re-enable even if previously opted out").action(async (options) => {
    try {
      await enableDocs(options);
    } catch (err6) {
      print(chalk2.red(`Error: ${String(err6)}`));
      process.exit(1);
    }
  });
  docs.command("disable").description("Disable docs projection and remove pointer lines").action(async () => {
    try {
      await disableDocs();
    } catch (err6) {
      print(chalk2.red(`Error: ${String(err6)}`));
      process.exit(1);
    }
  });
  docs.command("preview").description("Preview what would be written without making changes (dry-run)").action(async () => {
    try {
      await previewDocs();
    } catch (err6) {
      print(chalk2.red(`Error: ${String(err6)}`));
      process.exit(1);
    }
  });
  docs.command("status").description("Show current projection state per detected config file").action(async () => {
    try {
      await statusDocs();
    } catch (err6) {
      print(chalk2.red(`Error: ${String(err6)}`));
      process.exit(1);
    }
  });
  cmd.addCommand(docs);
  return cmd;
}
__name(createProjectionsCommand, "createProjectionsCommand");
var AuditLogger = class {
  static {
    __name(this, "AuditLogger");
  }
  stream = null;
  logPath;
  initError = null;
  constructor(logPath) {
    this.logPath = logPath || path10.join(os.homedir(), ".vreko", "audit", "acp.jsonl");
    try {
      const dir = path10.dirname(this.logPath);
      if (!fs.existsSync(dir)) {
        fs.mkdirSync(dir, {
          recursive: true
        });
      }
      this.stream = fs.createWriteStream(this.logPath, {
        flags: "a"
      });
      this.stream.on("error", (error) => {
        this.initError = error;
        this.stream = null;
      });
    } catch (error) {
      this.initError = error instanceof Error ? error : new Error(String(error));
    }
  }
  /**
  * Log an audit entry.
  */
  log(method, params, sessionId) {
    if (!this.stream) {
      return;
    }
    const entry = {
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      method,
      params: this.sanitizeParams(params),
      sessionId
    };
    try {
      this.stream.write(`${JSON.stringify(entry)}
`);
    } catch {
    }
  }
  /**
  * Sanitize params to remove sensitive data before logging.
  */
  sanitizeParams(params) {
    if (params === void 0 || params === null) {
      return void 0;
    }
    if (typeof params !== "object") {
      return params;
    }
    if (Array.isArray(params)) {
      return params.map((item) => this.sanitizeParams(item));
    }
    const sanitized = {};
    const sensitiveKeys = [
      "password",
      "token",
      "secret",
      "apiKey",
      "credentials"
    ];
    for (const [key, value] of Object.entries(params)) {
      if (sensitiveKeys.some((k) => key.toLowerCase().includes(k))) {
        sanitized[key] = "[REDACTED]";
      } else if (typeof value === "object" && value !== null) {
        sanitized[key] = this.sanitizeParams(value);
      } else {
        sanitized[key] = value;
      }
    }
    return sanitized;
  }
  /**
  * Close the audit logger.
  */
  async close() {
    return new Promise((resolve11) => {
      if (this.stream) {
        this.stream.end(() => {
          this.stream = null;
          resolve11();
        });
      } else {
        resolve11();
      }
    });
  }
  /**
  * Get the log file path.
  */
  get path() {
    return this.logPath;
  }
  /**
  * Check if the logger is active.
  */
  get isActive() {
    return this.stream !== null;
  }
  /**
  * Get any initialization error.
  */
  get error() {
    return this.initError;
  }
};
var NoOpAuditLogger = class {
  static {
    __name(this, "NoOpAuditLogger");
  }
  log(_method, _params, _sessionId) {
  }
  async close() {
  }
};
var InitializeParamsSchema = z.object({
  clientInfo: z.object({
    name: z.string(),
    version: z.string()
  }),
  workspacePath: z.string().optional(),
  capabilities: z.object({
    streaming: z.boolean().optional(),
    progress: z.boolean().optional()
  }).optional()
});
var SessionCreateParamsSchema = z.object({
  name: z.string().optional(),
  workspacePath: z.string().optional(),
  metadata: z.record(z.unknown()).optional()
});
var SessionCloseParamsSchema = z.object({
  sessionId: z.string()
});
var ToolCallParamsSchema = z.object({
  sessionId: z.string().optional(),
  name: z.string(),
  arguments: z.record(z.unknown())
});
var SessionManager2 = class {
  static {
    __name(this, "SessionManager");
  }
  sessions = /* @__PURE__ */ new Map();
  logFn;
  client = null;
  constructor(logFn) {
    this.logFn = logFn ?? (() => {
    });
  }
  /**
  * Initialize connection to daemon
  */
  async initialize() {
    this.client = createServiceClient();
    await connectServiceClient(this.client);
  }
  /**
  * Create a new session.
  * Day 5: Routes through daemon IPC.
  */
  async create(params) {
    if (this.client) {
      try {
        const daemonResult = await createSessionViaDaemon(this.client, params);
        if (daemonResult.success && daemonResult.result) {
          const session2 = {
            id: daemonResult.result.id,
            name: params.name,
            workspacePath: daemonResult.result.workspacePath ?? params.workspacePath,
            metadata: daemonResult.result.metadata,
            createdAt: new Date(daemonResult.result.createdAt),
            lastActivityAt: new Date(daemonResult.result.lastActivityAt)
          };
          this.sessions.set(session2.id, session2);
          this.logFn("Session created via daemon", {
            sessionId: session2.id,
            name: session2.name
          });
          return session2;
        }
      } catch (error) {
        this.logFn("Daemon session create failed, using local fallback", {
          error: String(error)
        });
      }
    }
    const session = {
      id: randomUUID(),
      name: params.name,
      workspacePath: params.workspacePath,
      metadata: params.metadata,
      createdAt: /* @__PURE__ */ new Date(),
      lastActivityAt: /* @__PURE__ */ new Date()
    };
    this.sessions.set(session.id, session);
    this.logFn("Session created locally", {
      sessionId: session.id,
      name: session.name
    });
    return session;
  }
  /**
  * Get a session by ID.
  * Updates the lastActivityAt timestamp.
  */
  get(id) {
    const session = this.sessions.get(id);
    if (session) {
      session.lastActivityAt = /* @__PURE__ */ new Date();
    }
    return session;
  }
  /**
  * Check if a session exists.
  */
  has(id) {
    return this.sessions.has(id);
  }
  /**
  * Close a session by ID.
  * Day 5: Routes through daemon IPC.
  */
  async close(id) {
    const existed = this.sessions.has(id);
    if (this.client && existed) {
      try {
        const workspacePath = this.sessions.get(id)?.workspacePath;
        if (workspacePath) {
          await endSessionViaDaemon(this.client, id, workspacePath);
        }
      } catch {
      }
    }
    this.sessions.delete(id);
    if (existed) {
      this.logFn("Session closed", {
        sessionId: id
      });
    }
    return existed;
  }
  /**
  * Close all sessions.
  */
  async closeAll() {
    const count = this.sessions.size;
    if (this.client) {
      const closePromises = Array.from(this.sessions.values()).map((session) => {
        if (!session.workspacePath) {
          return Promise.resolve();
        }
        return endSessionViaDaemon(this.client, session.id, session.workspacePath).catch(() => {
        });
      });
      await Promise.all(closePromises);
    }
    this.sessions.clear();
    this.logFn("All sessions closed", {
      count
    });
  }
  /**
  * List all active sessions.
  */
  list() {
    return Array.from(this.sessions.values());
  }
  /**
  * Get the count of active sessions.
  */
  get count() {
    return this.sessions.size;
  }
  /**
  * Clean up inactive sessions (idle for longer than timeout).
  */
  cleanup(maxIdleMs = 30 * 60 * 1e3) {
    const now = Date.now();
    let cleaned = 0;
    for (const [id, session] of this.sessions) {
      if (now - session.lastActivityAt.getTime() > maxIdleMs) {
        this.sessions.delete(id);
        cleaned++;
      }
    }
    if (cleaned > 0) {
      this.logFn("Sessions cleaned up", {
        cleaned,
        remaining: this.sessions.size
      });
    }
    return cleaned;
  }
};
function createSnapshotService2(_workspaceRoot) {
  const withClient = /* @__PURE__ */ __name(async (fn) => {
    const client = new VrekoLocalClient();
    try {
      await client.connect();
      await client.initialize({
        protocolVersion: "1.0.0",
        clientInfo: {
          name: "cli-acp",
          version: "1.0.0"
        }
      });
      return await fn(client);
    } finally {
      client.close();
    }
  }, "withClient");
  return {
    async createFromFiles(files, _options) {
      try {
        const result7 = await withClient((client) => client.snapshot.create({
          // Daemon-style create: read first file content for single-file snapshots
          // Multi-file support will be wired via snapshot/create-daemon in Phase 3B
          filePath: files[0],
          content: (() => {
            try {
              return readFileSync(files[0], "utf8");
            } catch {
              return "";
            }
          })(),
          trigger: "manual"
        }));
        return {
          success: true,
          snapshot: {
            id: result7.id,
            fileCount: files.length,
            createdAt: typeof result7.createdAt === "number" ? result7.createdAt : Date.now()
          }
        };
      } catch (err6) {
        return {
          success: false,
          error: err6 instanceof Error ? err6.message : "Snapshot creation failed"
        };
      }
    },
    listSnapshots(limit) {
      return [];
    },
    async restore(snapshotId, options) {
      try {
        const result7 = await withClient((client) => client.snapshot.restore({
          snapshotId,
          createBackup: true,
          dryRun: options.preview ?? false
        }));
        if (options.preview && result7.preview) {
          return {
            success: true,
            preview: true,
            files: [
              result7.preview
            ]
          };
        }
        return {
          success: true,
          restoredFiles: result7.snapshot ? [
            result7.snapshot.id
          ] : void 0
        };
      } catch (err6) {
        return {
          success: false,
          error: err6 instanceof Error ? err6.message : "Restore failed"
        };
      }
    },
    async diff(snapshotId, options) {
      try {
        const result7 = await withClient((client) => client.snapshot.diff({
          baseSnapshotId: snapshotId,
          compareSnapshotId: "current",
          format: "unified",
          contextLines: 3
        }));
        const filePath = options.file ?? "";
        const diffFiles = [
          {
            file: filePath,
            snapshotSize: 0,
            currentSize: null,
            changed: result7.stats.additions > 0 || result7.stats.deletions > 0,
            exists: true
          }
        ];
        return {
          success: true,
          snapshotId,
          files: diffFiles,
          summary: {
            total: result7.stats.filesChanged,
            changed: result7.stats.additions + result7.stats.deletions > 0 ? 1 : 0,
            unchanged: result7.stats.additions + result7.stats.deletions > 0 ? 0 : 1
          }
        };
      } catch (err6) {
        return {
          success: false,
          error: err6 instanceof Error ? err6.message : "Diff failed"
        };
      }
    }
  };
}
__name(createSnapshotService2, "createSnapshotService");
var serviceCache2 = /* @__PURE__ */ new Map();
function getService(workspacePath) {
  if (!serviceCache2.has(workspacePath)) {
    serviceCache2.set(workspacePath, createSnapshotService2());
  }
  const service = serviceCache2.get(workspacePath);
  if (!service) {
    throw new Error(`Failed to get snapshot service for ${workspacePath}`);
  }
  return service;
}
__name(getService, "getService");
var snapshotTools = [
  {
    name: "snapshot.create",
    description: "Create a snapshot of one or more files",
    inputSchema: {
      type: "object",
      properties: {
        files: {
          type: "array",
          items: {
            type: "string"
          },
          description: "File paths to snapshot (relative to workspace)"
        },
        message: {
          type: "string",
          description: "Optional snapshot message/description"
        },
        metadata: {
          type: "object",
          description: "Optional metadata to attach"
        }
      },
      required: [
        "files"
      ]
    }
  },
  {
    name: "snapshot.list",
    description: "List available snapshots",
    inputSchema: {
      type: "object",
      properties: {
        file: {
          type: "string",
          description: "Filter by file path (optional)"
        },
        limit: {
          type: "number",
          description: "Maximum number of snapshots to return (default: 20)"
        },
        before: {
          type: "string",
          description: "Return snapshots before this timestamp (ISO 8601)"
        }
      }
    }
  },
  {
    name: "snapshot.restore",
    description: "Restore files from a snapshot",
    inputSchema: {
      type: "object",
      properties: {
        snapshotId: {
          type: "string",
          description: "Snapshot ID to restore from"
        },
        files: {
          type: "array",
          items: {
            type: "string"
          },
          description: "Specific files to restore (optional, defaults to all)"
        },
        preview: {
          type: "boolean",
          description: "If true, return diff without applying (default: false)"
        }
      },
      required: [
        "snapshotId"
      ]
    }
  },
  {
    name: "snapshot.diff",
    description: "Show diff between current state and a snapshot",
    inputSchema: {
      type: "object",
      properties: {
        snapshotId: {
          type: "string",
          description: "Snapshot ID to compare against"
        },
        file: {
          type: "string",
          description: "Specific file to diff (optional)"
        },
        format: {
          type: "string",
          enum: [
            "unified",
            "side-by-side",
            "json"
          ],
          description: "Diff output format (default: unified)"
        }
      },
      required: [
        "snapshotId"
      ]
    }
  }
];
var SnapshotCreateInputSchema = z.object({
  files: z.array(z.string()).min(1),
  message: z.string().optional(),
  metadata: z.record(z.unknown()).optional()
});
var SnapshotListInputSchema = z.object({
  file: z.string().optional(),
  limit: z.number().int().positive().max(100).default(20),
  before: z.string().optional()
});
var SnapshotRestoreInputSchema = z.object({
  snapshotId: z.string(),
  files: z.array(z.string()).optional(),
  preview: z.boolean().default(false)
});
var SnapshotDiffInputSchema = z.object({
  snapshotId: z.string(),
  file: z.string().optional(),
  format: z.enum([
    "unified",
    "side-by-side",
    "json"
  ]).default("unified")
});
var snapshotHandlers = {
  async create(params, context) {
    try {
      const input = SnapshotCreateInputSchema.parse(params);
      context.auditLogger.log("snapshot.create", {
        files: input.files,
        message: input.message
      });
      if (!context.workspacePath) {
        return {
          content: [
            {
              type: "text",
              text: "Workspace not initialized"
            }
          ],
          isError: true
        };
      }
      const service = getService(context.workspacePath);
      const result7 = await service.createFromFiles(input.files, {
        description: input.message ?? "ACP snapshot",
        trigger: "manual"
      });
      if (!result7.success) {
        return {
          content: [
            {
              type: "text",
              text: result7.error ?? "Failed to create snapshot"
            }
          ],
          isError: true
        };
      }
      return {
        content: [
          {
            type: "json",
            json: {
              success: true,
              snapshot: result7.reused ? {
                id: result7.reusedSnapshotId,
                reused: true,
                reason: result7.reusedReason
              } : {
                id: result7.snapshot?.id,
                fileCount: result7.snapshot?.fileCount,
                createdAt: result7.snapshot?.createdAt !== void 0 ? new Date(result7.snapshot.createdAt).toISOString() : void 0
              },
              message: result7.reused ? `Reused existing snapshot: ${result7.reusedSnapshotId}` : `Created snapshot: ${result7.snapshot?.id}`
            }
          }
        ],
        isError: false
      };
    } catch (error) {
      return {
        content: [
          {
            type: "text",
            text: `Failed to create snapshot: ${error instanceof Error ? error.message : "Unknown error"}`
          }
        ],
        isError: true
      };
    }
  },
  async list(params, context) {
    try {
      const input = SnapshotListInputSchema.parse(params);
      if (!context.workspacePath) {
        return {
          content: [
            {
              type: "text",
              text: "Workspace not initialized"
            }
          ],
          isError: true
        };
      }
      const service = getService(context.workspacePath);
      let snapshots = service.listSnapshots(input.limit);
      if (input.file) {
        snapshots = snapshots.filter((s) => s.files.some((f) => f.path === input.file || f.path.endsWith(`/${input.file}`)));
      }
      if (input.before) {
        const beforeDate = new Date(input.before).getTime();
        snapshots = snapshots.filter((s) => s.createdAt < beforeDate);
      }
      return {
        content: [
          {
            type: "json",
            json: {
              snapshots: snapshots.map((s) => ({
                id: s.id,
                files: s.files.map((f) => f.path),
                description: s.description,
                createdAt: new Date(s.createdAt).toISOString(),
                trigger: s.trigger ?? "unknown"
              })),
              total: snapshots.length
            }
          }
        ],
        isError: false
      };
    } catch (error) {
      return {
        content: [
          {
            type: "text",
            text: `Failed to list snapshots: ${error instanceof Error ? error.message : "Unknown error"}`
          }
        ],
        isError: true
      };
    }
  },
  async restore(params, context) {
    try {
      const input = SnapshotRestoreInputSchema.parse(params);
      context.auditLogger.log("snapshot.restore", {
        snapshotId: input.snapshotId,
        files: input.files,
        preview: input.preview
      });
      if (!context.workspacePath) {
        return {
          content: [
            {
              type: "text",
              text: "Workspace not initialized"
            }
          ],
          isError: true
        };
      }
      const service = getService(context.workspacePath);
      const result7 = await service.restore(input.snapshotId, {
        files: input.files,
        preview: input.preview
      });
      if (!result7.success) {
        return {
          content: [
            {
              type: "text",
              text: result7.error ?? "Restore failed"
            }
          ],
          isError: true
        };
      }
      if (result7.preview) {
        return {
          content: [
            {
              type: "json",
              json: {
                preview: true,
                snapshotId: input.snapshotId,
                files: result7.files,
                message: `Would restore ${result7.files?.length ?? 0} file(s)`
              }
            }
          ],
          isError: false
        };
      }
      return {
        content: [
          {
            type: "json",
            json: {
              success: true,
              restored: result7.restoredFiles,
              errors: result7.errors,
              message: `Restored ${result7.restoredFiles?.length ?? 0} file(s) from snapshot`
            }
          }
        ],
        isError: false
      };
    } catch (error) {
      return {
        content: [
          {
            type: "text",
            text: `Failed to restore: ${error instanceof Error ? error.message : "Unknown error"}`
          }
        ],
        isError: true
      };
    }
  },
  async diff(params, context) {
    try {
      const input = SnapshotDiffInputSchema.parse(params);
      if (!context.workspacePath) {
        return {
          content: [
            {
              type: "text",
              text: "Workspace not initialized"
            }
          ],
          isError: true
        };
      }
      const service = getService(context.workspacePath);
      const result7 = await service.diff(input.snapshotId, {
        file: input.file
      });
      if (!result7.success) {
        return {
          content: [
            {
              type: "text",
              text: result7.error ?? "Diff failed"
            }
          ],
          isError: true
        };
      }
      const diffInfo = {
        snapshotId: result7.snapshotId,
        createdAt: result7.createdAt ? new Date(result7.createdAt).toISOString() : void 0,
        files: result7.files,
        summary: result7.summary
      };
      if (input.format === "json") {
        return {
          content: [
            {
              type: "json",
              json: diffInfo
            }
          ],
          isError: false
        };
      }
      const lines = [
        `Snapshot: ${result7.snapshotId}`,
        `Created: ${diffInfo.createdAt}`,
        `Files (${result7.summary?.changed ?? 0} changed, ${result7.summary?.unchanged ?? 0} unchanged):`,
        ...result7.files?.map((d) => `  ${d.changed ? "M" : " "} ${d.file}`) ?? []
      ];
      return {
        content: [
          {
            type: "text",
            text: lines.join("\n")
          }
        ],
        isError: false
      };
    } catch (error) {
      return {
        content: [
          {
            type: "text",
            text: `Failed to generate diff: ${error instanceof Error ? error.message : "Unknown error"}`
          }
        ],
        isError: true
      };
    }
  }
};
var statusTools = [
  {
    name: "status.protection",
    description: "Get current protection status for the workspace",
    inputSchema: {
      type: "object",
      properties: {
        file: {
          type: "string",
          description: "Check protection status for a specific file"
        }
      }
    }
  },
  {
    name: "status.health",
    description: "Get Vreko system health status",
    inputSchema: {
      type: "object",
      properties: {}
    }
  }
];
var ProtectionStatusInputSchema = z.object({
  file: z.string().optional()
});
var statusHandlers = {
  async protection(params, context) {
    try {
      const input = ProtectionStatusInputSchema.parse(params);
      if (!context.workspacePath) {
        return {
          content: [
            {
              type: "text",
              text: "Workspace not initialized"
            }
          ],
          isError: true
        };
      }
      const { existsSync: existsSync26, readFileSync: readFileSync13 } = await import('fs');
      const { join: join28 } = await import('path');
      const vrekoDir2 = join28(context.workspacePath, ".vreko");
      const isInitialized = existsSync26(vrekoDir2);
      if (!isInitialized) {
        return {
          content: [
            {
              type: "json",
              json: {
                initialized: false,
                protected: false,
                message: "Workspace not initialized with Vreko"
              }
            }
          ],
          isError: false
        };
      }
      const protectedFilesPath = join28(vrekoDir2, "protected-files.json");
      let protectedFiles = [];
      if (existsSync26(protectedFilesPath)) {
        try {
          protectedFiles = JSON.parse(readFileSync13(protectedFilesPath, "utf8"));
        } catch {
        }
      }
      const snapshotsDir = join28(vrekoDir2, "snapshots");
      let snapshotCount = 0;
      if (existsSync26(snapshotsDir)) {
        const { readdirSync: readdirSync5 } = await import('fs');
        snapshotCount = readdirSync5(snapshotsDir).filter((f) => f.endsWith(".json")).length;
      }
      const status = {
        initialized: true,
        protected: protectedFiles.length > 0,
        protectedFileCount: protectedFiles.length,
        snapshotCount,
        workspacePath: context.workspacePath
      };
      if (input.file) {
        const isProtected = protectedFiles.some((f) => f === input.file || f.endsWith(`/${input.file}`));
        return {
          content: [
            {
              type: "json",
              json: {
                ...status,
                file: input.file,
                fileProtected: isProtected
              }
            }
          ],
          isError: false
        };
      }
      return {
        content: [
          {
            type: "json",
            json: status
          }
        ],
        isError: false
      };
    } catch (error) {
      return {
        content: [
          {
            type: "text",
            text: `Failed to get protection status: ${error instanceof Error ? error.message : "Unknown error"}`
          }
        ],
        isError: true
      };
    }
  },
  async health(_params, context) {
    try {
      const { join: join28 } = await import('path');
      const { existsSync: existsSync26, statSync: statSync8 } = await import('fs');
      const { platform: platform6, freemem, totalmem, uptime } = await import('os');
      const vrekoDir2 = context.workspacePath ? join28(context.workspacePath, ".vreko") : null;
      const storageInfo = {
        initialized: false,
        snapshotCount: 0,
        totalSize: 0
      };
      if (vrekoDir2 && existsSync26(vrekoDir2)) {
        storageInfo.initialized = true;
        const snapshotsDir = join28(vrekoDir2, "snapshots");
        if (existsSync26(snapshotsDir)) {
          const { readdirSync: readdirSync5 } = await import('fs');
          const files = readdirSync5(snapshotsDir);
          storageInfo.snapshotCount = files.filter((f) => f.endsWith(".json")).length;
          for (const file of files) {
            try {
              const stats = statSync8(join28(snapshotsDir, file));
              storageInfo.totalSize += stats.size;
            } catch {
            }
          }
        }
      }
      let version = "unknown";
      try {
        const { fileURLToPath: fileURLToPath5 } = await import('url');
        const { readFileSync: readFileSync13 } = await import('fs');
        const __dirname2 = fileURLToPath5(new URL(".", import.meta.url));
        const pkgPath = join28(__dirname2, "../../../../package.json");
        if (existsSync26(pkgPath)) {
          const pkg = JSON.parse(readFileSync13(pkgPath, "utf8"));
          version = pkg.version;
        }
      } catch {
      }
      return {
        content: [
          {
            type: "json",
            json: {
              status: "healthy",
              version,
              platform: platform6(),
              uptime: Math.floor(uptime()),
              memory: {
                free: freemem(),
                total: totalmem(),
                usedPercent: Math.round((1 - freemem() / totalmem()) * 100)
              },
              storage: storageInfo
            }
          }
        ],
        isError: false
      };
    } catch (error) {
      return {
        content: [
          {
            type: "text",
            text: `Failed to get health status: ${error instanceof Error ? error.message : "Unknown error"}`
          }
        ],
        isError: true
      };
    }
  }
};

// src/acp/tools/index.ts
var toolRegistry = [
  ...snapshotTools,
  ...statusTools
];
var toolHandlers = {
  // Snapshot operations
  "snapshot.create": snapshotHandlers.create,
  "snapshot.list": snapshotHandlers.list,
  "snapshot.restore": snapshotHandlers.restore,
  "snapshot.diff": snapshotHandlers.diff,
  // Status operations
  "status.protection": statusHandlers.protection,
  "status.health": statusHandlers.health
};

// src/acp/transport/framing.ts
var ContentLengthFramer = class {
  static {
    __name(this, "ContentLengthFramer");
  }
  buffer = "";
  /**
  * Feed raw data into the framer.
  * Returns complete messages as they become available.
  */
  feed(data) {
    this.buffer += data;
    const messages2 = [];
    while (true) {
      const message = this.tryExtractMessage();
      if (!message) {
        break;
      }
      messages2.push(message);
    }
    return messages2;
  }
  /**
  * Try to extract a complete message from the buffer.
  * Returns null if no complete message is available.
  */
  tryExtractMessage() {
    const headerEnd = this.buffer.indexOf("\r\n\r\n");
    if (headerEnd === -1) {
      return null;
    }
    const header = this.buffer.substring(0, headerEnd);
    const match = header.match(/Content-Length:\s*(\d+)/i);
    if (!match) {
      this.buffer = this.buffer.substring(headerEnd + 4);
      return null;
    }
    const contentLength = Number.parseInt(match[1], 10);
    const payloadStart = headerEnd + 4;
    const bufferBytes = Buffer.byteLength(this.buffer.substring(payloadStart), "utf8");
    if (bufferBytes < contentLength) {
      return null;
    }
    const payloadBuffer = Buffer.from(this.buffer.substring(payloadStart), "utf8");
    const payload = payloadBuffer.subarray(0, contentLength).toString("utf8");
    const actualPayloadChars = Buffer.from(payload, "utf8").toString("utf8").length;
    this.buffer = this.buffer.substring(payloadStart + actualPayloadChars);
    return {
      contentLength,
      payload
    };
  }
  /**
  * Reset the buffer (useful for error recovery).
  */
  reset() {
    this.buffer = "";
  }
  /**
  * Get current buffer size (for debugging).
  */
  get bufferSize() {
    return this.buffer.length;
  }
  /**
  * Frame a message for sending.
  *
  * @param payload - The JSON payload to frame
  * @returns The framed message ready for transport
  */
  static frame(payload) {
    const bytes = Buffer.byteLength(payload, "utf8");
    return `Content-Length: ${bytes}\r
\r
${payload}`;
  }
};

// src/acp/transport/types.ts
var ErrorCodes2 = {
  // Standard JSON-RPC errors
  ParseError: -32700,
  InvalidRequest: -32600,
  MethodNotFound: -32601,
  InternalError: -32603,
  // ACP-specific error codes (reserved range: -32000 to -32099)
  SessionNotFound: -32001,
  ToolNotFound: -32002};

// src/acp/server.ts
var ACPError = class extends Error {
  static {
    __name(this, "ACPError");
  }
  code;
  data;
  constructor(code, message, data) {
    super(message), this.code = code, this.data = data;
    this.name = "ACPError";
  }
};
var ACPServer = class extends EventEmitter {
  static {
    __name(this, "ACPServer");
  }
  framer = new ContentLengthFramer();
  sessionManager;
  auditLogger;
  verbose;
  input = null;
  output = null;
  initialized = false;
  shuttingDown = false;
  workspacePath;
  version = "1.0.0";
  constructor(options) {
    super();
    this.workspacePath = options.workspacePath;
    this.verbose = options.verbose ?? false;
    this.sessionManager = new SessionManager2(this.verbose ? (msg, data) => this.log("info", msg, data) : void 0);
    this.auditLogger = options.auditPath ? new AuditLogger(options.auditPath) : new NoOpAuditLogger();
  }
  // ─────────────────────────────────────────────────────────────────────────
  // Lifecycle
  // ─────────────────────────────────────────────────────────────────────────
  /**
  * Start the ACP server on the given input/output streams.
  */
  start(input, output) {
    this.input = input;
    this.output = output;
    this.input.setEncoding("utf8");
    this.input.on("data", (chunk) => {
      this.handleInput(chunk);
    });
    this.input.on("end", () => {
      this.shutdown();
    });
    this.input.on("error", (error) => {
      this.log("error", "Input stream error", {
        error: error.message
      });
      this.shutdown();
    });
    this.log("info", "ACP server started");
  }
  /**
  * Shutdown the ACP server.
  */
  async shutdown() {
    if (this.shuttingDown) {
      return;
    }
    this.shuttingDown = true;
    this.log("info", "Shutting down ACP server");
    await this.sessionManager.closeAll();
    await this.auditLogger.close();
    this.emit("shutdown");
  }
  // ─────────────────────────────────────────────────────────────────────────
  // Message Handling
  // ─────────────────────────────────────────────────────────────────────────
  handleInput(data) {
    const messages2 = this.framer.feed(data);
    for (const { payload } of messages2) {
      try {
        const message = JSON.parse(payload);
        this.handleMessage(message).catch((error) => {
          this.log("error", "Unhandled error in message handler", {
            error: String(error)
          });
        });
      } catch {
        this.sendError(null, ErrorCodes2.ParseError, "Parse error");
      }
    }
  }
  async handleMessage(message) {
    const isRequest = "id" in message;
    const id = isRequest ? message.id : null;
    try {
      const result7 = await this.dispatch(message.method, message.params);
      if (isRequest && id !== null) {
        this.sendResult(id, result7);
      }
    } catch (error) {
      if (isRequest && id !== null) {
        if (error instanceof ACPError) {
          this.sendError(id, error.code, error.message, error.data);
        } else {
          this.sendError(id, ErrorCodes2.InternalError, error instanceof Error ? error.message : "Internal error");
        }
      }
    }
  }
  async dispatch(method, params) {
    if (!this.initialized && method !== "initialize") {
      throw new ACPError(ErrorCodes2.InvalidRequest, "Server not initialized");
    }
    this.log("debug", `Dispatching: ${method}`);
    this.auditLogger.log(method, params);
    switch (method) {
      case "initialize":
        return this.handleInitialize(params);
      case "shutdown":
        return this.handleShutdown();
      case "session/create":
        return this.handleSessionCreate(params);
      case "session/close":
        return this.handleSessionClose(params);
      case "tools/list":
        return this.handleToolsList();
      case "tools/call":
        return this.handleToolCall(params);
      default:
        throw new ACPError(ErrorCodes2.MethodNotFound, `Unknown method: ${method}`);
    }
  }
  // ─────────────────────────────────────────────────────────────────────────
  // Protocol Handlers
  // ─────────────────────────────────────────────────────────────────────────
  async handleInitialize(params) {
    if (this.initialized) {
      throw new ACPError(ErrorCodes2.InvalidRequest, "Already initialized");
    }
    const validated = InitializeParamsSchema.parse(params);
    this.log("info", `Initializing for ${validated.clientInfo.name} v${validated.clientInfo.version}`);
    if (validated.workspacePath) {
      this.workspacePath = validated.workspacePath;
    }
    this.initialized = true;
    return {
      serverInfo: {
        name: "vreko",
        version: this.version
      },
      capabilities: {
        tools: toolRegistry,
        sessions: true,
        streaming: false
      }
    };
  }
  async handleShutdown() {
    this.log("info", "Shutdown requested");
    await this.shutdown();
    return {
      success: true
    };
  }
  async handleSessionCreate(params) {
    const validated = SessionCreateParamsSchema.parse(params);
    const session = await this.sessionManager.create({
      name: validated.name,
      workspacePath: validated.workspacePath || this.workspacePath,
      metadata: validated.metadata
    });
    return {
      sessionId: session.id,
      createdAt: session.createdAt.toISOString()
    };
  }
  async handleSessionClose(params) {
    const validated = SessionCloseParamsSchema.parse(params);
    const success = await this.sessionManager.close(validated.sessionId);
    if (!success) {
      throw new ACPError(ErrorCodes2.SessionNotFound, "Session not found");
    }
    return {
      success: true
    };
  }
  handleToolsList() {
    return {
      tools: toolRegistry
    };
  }
  async handleToolCall(params) {
    const validated = ToolCallParamsSchema.parse(params);
    const handler2 = toolHandlers[validated.name];
    if (!handler2) {
      throw new ACPError(ErrorCodes2.ToolNotFound, `Unknown tool: ${validated.name}`);
    }
    const session = validated.sessionId ? this.sessionManager.get(validated.sessionId) : null;
    const context = {
      sessionId: validated.sessionId,
      workspacePath: session?.workspacePath || this.workspacePath,
      auditLogger: this.auditLogger
    };
    return handler2(validated.arguments, context);
  }
  // ─────────────────────────────────────────────────────────────────────────
  // Response Helpers
  // ─────────────────────────────────────────────────────────────────────────
  send(message) {
    if (!this.output) {
      return;
    }
    const payload = JSON.stringify(message);
    const framed = ContentLengthFramer.frame(payload);
    this.output.write(framed);
  }
  sendResult(id, result7) {
    this.send({
      jsonrpc: "2.0",
      id,
      result: result7
    });
  }
  sendError(id, code, message, data) {
    if (id === null) {
      this.log("error", `Error in notification: ${message}`, {
        code,
        data
      });
      return;
    }
    this.send({
      jsonrpc: "2.0",
      id,
      error: {
        code,
        message,
        data
      }
    });
  }
  // ─────────────────────────────────────────────────────────────────────────
  // Logging
  // ─────────────────────────────────────────────────────────────────────────
  log(level, message, data) {
    if (level === "debug" && !this.verbose) {
      return;
    }
    const timestamp = (/* @__PURE__ */ new Date()).toISOString();
    const prefix = `[${timestamp}] [ACP] [${level.toUpperCase()}]`;
    const dataStr = data ? ` ${JSON.stringify(data)}` : "";
    process.stderr.write(`${prefix} ${message}${dataStr}
`);
  }
};

// src/commands/acp.ts
async function getPackageVersion() {
  try {
    const { fileURLToPath: fileURLToPath5 } = await import('url');
    const __dirname2 = fileURLToPath5(new URL(".", import.meta.url));
    const pkgPath = join(__dirname2, "../../package.json");
    if (existsSync(pkgPath)) {
      const pkg = JSON.parse(readFileSync(pkgPath, "utf8"));
      return pkg.version;
    }
  } catch {
  }
  return "1.0.0";
}
__name(getPackageVersion, "getPackageVersion");
function createAcpCommand() {
  const cmd = createCommand("acp");
  cmd.description("Agent Communication Protocol (ACP) integration for editor agents (Zed, JetBrains, Neovim)");
  cmd.command("serve").description("Start ACP server for editor integration (stdio transport)").option("-w, --workspace <path>", "Workspace path", process.cwd()).option("--audit <path>", "Audit log path").option("-v, --verbose", "Enable verbose logging").action(async (options) => {
    const server = new ACPServer({
      workspacePath: options.workspace,
      auditPath: options.audit,
      verbose: options.verbose
    });
    process.on("SIGINT", () => server.shutdown());
    process.on("SIGTERM", () => server.shutdown());
    server.on("shutdown", () => {
      process.exit(0);
    });
    server.start(process.stdin, process.stdout);
  });
  cmd.command("info").description("Print ACP agent metadata and configuration examples").option("--json", "Output as JSON").action(async (options) => {
    const version = await getPackageVersion();
    const info = {
      name: "vreko",
      version,
      description: "AI-assisted code protection agent",
      capabilities: {
        tools: toolRegistry.map((t) => ({
          name: t.name,
          description: t.description
        })),
        sessions: true,
        streaming: false
      },
      config: {
        zed: {
          agent_servers: {
            vreko: {
              command: "vreko",
              args: [
                "acp",
                "serve"
              ]
            }
          }
        },
        jetbrains: {
          agent_servers: {
            vreko: {
              command: "vreko",
              args: [
                "acp",
                "serve"
              ],
              use_idea_mcp: true
            }
          }
        }
      }
    };
    if (options.json) {
      console.log(JSON.stringify(info, null, 2));
      return;
    }
    console.log();
    console.log(chalk2.bold.cyan("Vreko ACP Agent") + chalk2.gray(` v${version}`));
    console.log(chalk2.white("AI-assisted code protection for any ACP editor"));
    console.log();
    console.log(chalk2.bold("Available Tools:"));
    for (const tool of info.capabilities.tools) {
      console.log(`  ${chalk2.green("\u2022")} ${chalk2.cyan(tool.name)}: ${tool.description}`);
    }
    console.log();
    console.log(chalk2.bold("Configuration Examples:"));
    console.log();
    console.log(chalk2.yellow("Zed") + chalk2.gray(" (~/.config/zed/settings.json):"));
    console.log(chalk2.gray(JSON.stringify(info.config.zed, null, 2)));
    console.log();
    console.log(chalk2.yellow("JetBrains") + chalk2.gray(" (~/.config/jetbrains/ai-assistant.json):"));
    console.log(chalk2.gray(JSON.stringify(info.config.jetbrains, null, 2)));
    console.log();
    console.log(chalk2.gray("Run: vreko acp configure --zed  # Auto-configure for Zed"));
    console.log();
  });
  cmd.command("configure").description("Auto-configure ACP for supported editors").option("--zed", "Configure for Zed").option("--jetbrains", "Configure for JetBrains").option("--all", "Configure for all detected editors").option("--dry-run", "Show what would be configured without making changes").action(async (options) => {
    const editors = [];
    if (options.zed || options.all) {
      const zedConfigPath = join(homedir(), ".config", "zed", "settings.json");
      editors.push({
        name: "Zed",
        detected: existsSync(join(homedir(), ".config", "zed")),
        configPath: zedConfigPath,
        configure: /* @__PURE__ */ __name(async () => configureZed(zedConfigPath), "configure")
      });
    }
    if (options.jetbrains || options.all) {
      const jetbrainsConfigPath = join(homedir(), ".config", "jetbrains", "ai-assistant.json");
      editors.push({
        name: "JetBrains",
        detected: existsSync(join(homedir(), ".config", "jetbrains")),
        configPath: jetbrainsConfigPath,
        configure: /* @__PURE__ */ __name(async () => configureJetBrains(jetbrainsConfigPath), "configure")
      });
    }
    if (editors.length === 0) {
      console.log(chalk2.yellow("No editors specified. Use --zed, --jetbrains, or --all"));
      return;
    }
    for (const editor of editors) {
      if (!editor.detected && !options.all) {
        console.log(chalk2.yellow(`\u26A0 ${editor.name} not detected, skipping`));
        continue;
      }
      if (options.dryRun) {
        console.log(chalk2.cyan(`[dry-run] Would configure ${editor.name}`));
        console.log(chalk2.gray(`  Config: ${editor.configPath}`));
        continue;
      }
      try {
        await editor.configure();
        console.log(chalk2.green(`\u2713 ${editor.name} configured`));
        console.log(chalk2.gray(`  Config: ${editor.configPath}`));
      } catch (error) {
        console.log(chalk2.red(`\u2717 Failed to configure ${editor.name}: ${error instanceof Error ? error.message : "Unknown error"}`));
      }
    }
  });
  return cmd;
}
__name(createAcpCommand, "createAcpCommand");
async function configureZed(configPath) {
  await addAgentToConfig(configPath, "vreko", {
    command: "vreko",
    args: [
      "acp",
      "serve"
    ]
  });
}
__name(configureZed, "configureZed");
async function configureJetBrains(configPath) {
  await addAgentToConfig(configPath, "vreko", {
    command: "vreko",
    args: [
      "acp",
      "serve"
    ],
    use_idea_mcp: true
  });
}
__name(configureJetBrains, "configureJetBrains");
async function addAgentToConfig(configPath, agentName, agentConfig) {
  const dir = join(configPath, "..");
  if (!existsSync(dir)) {
    mkdirSync(dir, {
      recursive: true
    });
  }
  let config2 = {};
  if (existsSync(configPath)) {
    const content = readFileSync(configPath, "utf8");
    try {
      config2 = JSON.parse(content);
    } catch {
    }
  }
  if (!config2.agent_servers) {
    config2.agent_servers = {};
  }
  config2.agent_servers[agentName] = agentConfig;
  writeFileSync(configPath, JSON.stringify(config2, null, 2));
}
__name(addAgentToConfig, "addAgentToConfig");
var acpCommand = createAcpCommand();
function resolveVrekodBin() {
  const bundleFile = fileURLToPath(import.meta.url);
  const workspaceRoot = resolve(dirname(bundleFile), "../../..");
  const bin = join(workspaceRoot, "apps", "local-service", "dist", "main.js");
  if (!existsSync(bin)) {
    throw new Error(`Cannot find service binary at ${bin}.
Run 'pnpm --filter @vreko/local-service build' first.`);
  }
  return bin;
}
__name(resolveVrekodBin, "resolveVrekodBin");
function shouldUseDoppler() {
  try {
    execSync("which doppler", {
      stdio: "ignore"
    });
    const project = execSync("doppler configure get project --plain", {
      stdio: "pipe",
      encoding: "utf-8",
      timeout: 5e3
    }).trim();
    const config2 = execSync("doppler configure get config --plain", {
      stdio: "pipe",
      encoding: "utf-8",
      timeout: 5e3
    }).trim();
    return Boolean(project && config2);
  } catch {
    return false;
  }
}
__name(shouldUseDoppler, "shouldUseDoppler");
async function waitForDaemonHealthy(maxWaitMs) {
  const start = Date.now();
  await new Promise((resolve11, reject) => {
    const check = /* @__PURE__ */ __name(async () => {
      if (await isServiceHealthy()) {
        resolve11();
        return;
      }
      if (Date.now() - start > maxWaitMs) {
        reject(new Error("Service failed to start within timeout"));
        return;
      }
      setTimeout(() => {
        void check();
      }, 100);
    }, "check");
    setTimeout(() => {
      void check();
    }, 100);
  });
}
__name(waitForDaemonHealthy, "waitForDaemonHealthy");
async function startDaemonDetached(options) {
  const vrekodBin = resolveVrekodBin();
  const vrekodArgs = [
    "--idle-timeout",
    options.idleTimeout
  ];
  const useDoppler = shouldUseDoppler();
  const logDir = join(homedir(), ".vreko", "daemon");
  const logPath = join(logDir, "daemon.log");
  mkdirSync(logDir, {
    recursive: true
  });
  const logFd = openSync(logPath, "a");
  const spawnArgs = useDoppler ? [
    "run",
    "--",
    process.execPath,
    vrekodBin,
    ...vrekodArgs
  ] : [
    vrekodBin,
    ...vrekodArgs
  ];
  const spawnCmd = useDoppler ? "doppler" : process.execPath;
  const child = spawn(spawnCmd, spawnArgs, {
    detached: true,
    stdio: [
      "ignore",
      logFd,
      logFd
    ]
  });
  child.unref();
  await waitForDaemonHealthy(options.maxWaitMs ?? 5e3);
  return readServicePid();
}
__name(startDaemonDetached, "startDaemonDetached");
function buildStartTasks(profile) {
  const isJson = cliState.renderMode === "json";
  const renderer = isJson ? "silent" : process.stdout.isTTY ? "default" : "verbose";
  const taskDefs = [
    {
      title: "Initialize workspace",
      enabled: /* @__PURE__ */ __name(() => profile === "VIRGIN", "enabled"),
      task: /* @__PURE__ */ __name(async () => {
        await Promise.resolve();
      }, "task")
    },
    {
      title: "Create workspace",
      enabled: /* @__PURE__ */ __name(() => profile === "NEW_WORKSPACE", "enabled"),
      task: /* @__PURE__ */ __name(async () => {
        await Promise.resolve();
      }, "task")
    },
    {
      title: "Verify snapshot integrity",
      enabled: /* @__PURE__ */ __name(() => profile === "COLD_RETURN", "enabled"),
      task: /* @__PURE__ */ __name(async () => {
        await Promise.resolve();
      }, "task")
    },
    {
      title: "Resume session",
      enabled: /* @__PURE__ */ __name(() => profile === "WARM_RETURN", "enabled"),
      // HOT_RECONNECT skips resume  -  process is already running, session active.
      task: /* @__PURE__ */ __name(async () => {
        await Promise.resolve();
      }, "task")
    },
    {
      title: "Connect to daemon",
      enabled: /* @__PURE__ */ __name(() => true, "enabled"),
      // All profiles reach this step  -  final IPC health confirmation.
      task: /* @__PURE__ */ __name(async () => {
        await Promise.resolve();
      }, "task")
    }
  ];
  const listr = new Listr(taskDefs, {
    renderer,
    rendererOptions: {
      collapseSubtasks: false
    }
  });
  return {
    tasks: taskDefs,
    run: /* @__PURE__ */ __name(() => listr.run(), "run")
  };
}
__name(buildStartTasks, "buildStartTasks");

// src/commands/start.ts
function createVrStartCommand() {
  return new Command("start").description("Start the Vreko service and open the dashboard").option("--service-only", "Start service without opening TUI").action(async (options) => {
    const alreadyRunning = await isServiceHealthy();
    const isJson = cliState.renderMode === "json";
    if (!alreadyRunning) {
      if (!isJson && process.stdout.isTTY) {
        process.stdout.write(`${chalk2.gray("Starting Vreko service...")}
`);
      }
      try {
        await startDaemonDetached({
          idleTimeout: "240",
          maxWaitMs: 5e3
        });
      } catch (err6) {
        const msg = err6 instanceof Error ? err6.message : String(err6);
        if (!isJson && process.stdout.isTTY) {
          console.error(chalk2.red("Failed to start service:"), msg);
        } else {
          process.stdout.write(`${JSON.stringify(StartStatusOutput.parse({
            ok: false,
            error: msg
          }))}
`);
        }
        process.exit(1);
      }
    }
    let bootProfile = "WARM_RETURN";
    try {
      const client = await connectToDaemon();
      const initResp = await client.initialize({
        protocolVersion: "1.0.0",
        clientInfo: {
          name: "@vreko/cli",
          version: "1.0.0"
        }
      });
      if (initResp.bootProfile) {
        bootProfile = initResp.bootProfile;
      }
    } catch {
    }
    if (options.serviceOnly) {
      if (isJson) {
        process.stdout.write(`${JSON.stringify(StartStatusOutput.parse({
          ok: true,
          serviceRunning: true,
          bootProfile,
          tui: false
        }))}
`);
      } else {
        process.stdout.write(`${chalk2.green(alreadyRunning ? "Service already running." : "Service started.")}
`);
      }
      return;
    }
    if (isJson) {
      process.stdout.write(`${JSON.stringify(StartStatusOutput.parse({
        ok: true,
        serviceRunning: true,
        bootProfile,
        tui: false
      }))}
`);
      return;
    }
    if (cliState.renderMode === "ink") {
      await buildStartTasks(bootProfile).run();
    }
    const { launchTui } = await import('./tui-PFFYLIOW.js');
    await launchTui("dashboard");
  });
}
__name(createVrStartCommand, "createVrStartCommand");
function createVrStopCommand() {
  return new Command("stop").description("Gracefully drain the session and stop the Vreko service").option("--no-ceremony", "Skip the closing ceremony output").option("--no-tui", "Use plain text ceremony output (for scripts/CI)").action(async (options) => {
    if (!await isServiceHealthy()) {
      if (isInteractive()) {
        console.log(chalk2.gray("Vreko service is not running."));
      } else {
        process.stdout.write(`${JSON.stringify({
          ok: true,
          serviceWasRunning: false
        })}
`);
      }
      return;
    }
    const client = createServiceClient();
    let ceremonyData = null;
    try {
      await connectServiceClient(client);
      try {
        const cwd = process.cwd();
        const response = await client.session.current({
          workspacePath: cwd
        });
        const activeSession = response;
        if (activeSession?.id) {
          if (isInteractive()) {
            process.stdout.write(`${chalk2.gray("Ending session...")}
`);
          }
          const endResult = await endSessionViaDaemon(client, activeSession.id, cwd);
          if (endResult.success) {
            ceremonyData = endResult;
          }
        }
      } catch {
      }
    } catch (err6) {
      if (isInteractive()) {
        console.error(chalk2.yellow("Warning: could not connect to service for drain:"), err6 instanceof Error ? err6.message : String(err6));
      }
    } finally {
      try {
        client.close();
      } catch {
      }
    }
    if (isInteractive()) {
      process.stdout.write(`${chalk2.gray("Stopping service...")}
`);
    }
    try {
      spawnSync("vreko", [
        "service",
        "stop"
      ], {
        stdio: isInteractive() ? "inherit" : "pipe"
      });
    } catch {
    }
    if (!options.noCeremony) {
      if (!isInteractive()) {
        process.stdout.write(`${JSON.stringify({
          ok: true,
          ceremony: ceremonyData
        })}
`);
      } else if (ceremonyData) {
        const useTui = isInteractive() && cliState.renderMode === "ink" && !options.noTui;
        if (useTui) {
          try {
            const { render } = await import('ink');
            const React = await import('react');
            const { CeremonyView } = await import('./CeremonyView-LQS7FTMK.js');
            const instance3 = render(React.createElement(CeremonyView, {
              record: ceremonyData
            }));
            await instance3.waitUntilExit();
          } catch {
            console.log(chalk2.green("\nSession complete."));
          }
        } else {
          try {
            const { renderCeremony } = await import('./ceremony-M7CXVBVA.js');
            console.log(renderCeremony(ceremonyData));
          } catch {
            console.log(chalk2.green("\nSession complete."));
          }
        }
      } else if (isInteractive()) {
        console.log(chalk2.green("Vreko stopped."));
      }
    }
  });
}
__name(createVrStopCommand, "createVrStopCommand");
function formatNumber(n) {
  return n.toLocaleString();
}
__name(formatNumber, "formatNumber");
async function ensureDaemonRunning() {
  if (isServiceRunning()) {
    return true;
  }
  const { spawn: spawn6 } = await import('child_process');
  const child = spawn6(process.execPath, [
    process.argv[1],
    "service",
    "start",
    "--detach"
  ], {
    detached: true,
    stdio: "ignore"
  });
  child.unref();
  const maxWait = 5e3;
  const start = Date.now();
  return new Promise((resolve11) => {
    const check = /* @__PURE__ */ __name(() => {
      if (isServiceRunning()) {
        resolve11(true);
      } else if (Date.now() - start > maxWait) {
        resolve11(false);
      } else {
        setTimeout(check, 100);
      }
    }, "check");
    setTimeout(check, 100);
  });
}
__name(ensureDaemonRunning, "ensureDaemonRunning");
function registerBaselineCommands(program) {
  const baseline = program.command("baseline", {
    hidden: true
  }).description("Manage workspace baselines");
  baseline.command("scan").description("Scan workspace and compute baseline").option("-w, --workspace <path>", "Workspace path", process.cwd()).option("-v, --verbose", "Show detailed progress").action(async (options) => {
    const workspacePath = resolve(options.workspace);
    if (!await ensureDaemonRunning()) {
      process.exit(1);
    }
    const client = createServiceClient();
    try {
      await connectServiceClient(client);
      const startTime = Date.now();
      await client.call("baseline/compute", {
        workspace: workspacePath
      });
      let record = null;
      let lastProgress = 0;
      while (!record) {
        await new Promise((r) => setTimeout(r, 500));
        const status = await client.call("baseline/status", {
          workspace: workspacePath
        });
        if (options.verbose && status.progress > lastProgress) {
          lastProgress = status.progress;
        }
        if (status.status === "ready" || status.progress >= 100) {
          record = await client.call("baseline/get", {
            workspace: workspacePath
          });
        } else if (status.status === "stale") {
        }
      }
      const elapsed = Date.now() - startTime;
      console.log(`\u2713 Baseline computed in ${elapsed}ms`);
      console.log(`  Files: ${record.totalFiles}, Health: ${record.overallHealthScore.toFixed(1)}`);
      if (record.fragileFiles.length > 0) {
        console.log("  Fragile files:");
        for (const file of record.fragileFiles.slice(0, 5)) {
          console.log(`    \u2022 ${file.path} (score: ${file.compositeScore.toFixed(2)})`);
        }
      }
      if (record.domainHealthScores.length > 0) {
        console.log("  Domain health:");
        for (const domain of record.domainHealthScores.slice(0, 5)) {
          console.log(`    \u2022 ${domain.domain}: ${domain.score.toFixed(1)}`);
        }
      }
    } catch (error) {
      console.error("\u2717 Baseline scan failed:", error instanceof Error ? error.message : String(error));
      process.exitCode = 1;
    } finally {
      client.close();
    }
  });
  baseline.argument("[action]", "Action to perform (default: scan)").option("-w, --workspace <path>", "Workspace path", process.cwd()).option("-v, --verbose", "Show detailed progress").action(async (action, options) => {
    if (!action || action === "scan") {
      const workspacePath = resolve(options.workspace);
      if (!await ensureDaemonRunning()) {
        process.exit(1);
      }
      const client = createServiceClient();
      try {
        await connectServiceClient(client);
        const startTime = Date.now();
        await client.call("baseline/compute", {
          workspace: workspacePath
        });
        let record = null;
        while (!record) {
          await new Promise((r) => setTimeout(r, 500));
          const status = await client.call("baseline/status", {
            workspace: workspacePath
          });
          if (status.status === "ready" || status.progress >= 100) {
            record = await client.call("baseline/get", {
              workspace: workspacePath
            });
          }
        }
        const elapsed = Date.now() - startTime;
        console.log(`\u2713 Baseline computed in ${elapsed}ms`);
        console.log(`  Files: ${record.totalFiles}, Health: ${record.overallHealthScore.toFixed(1)}`);
      } catch (error) {
        console.error("\u2717 Baseline scan failed:", error instanceof Error ? error.message : String(error));
        process.exitCode = 1;
      } finally {
        client.close();
      }
    }
  });
  baseline.command("status").description("Show baseline status for workspace").option("-w, --workspace <path>", "Workspace path", process.cwd()).option("-j, --json", "Output as JSON").action(async (options) => {
    const workspacePath = resolve(options.workspace);
    if (!isServiceRunning()) {
      if (options.json) {
        console.log(JSON.stringify({
          status: "daemon_not_running"
        }));
      } else {
        console.log("Service is not running. Start with: vreko service start");
      }
      return;
    }
    const client = createServiceClient();
    try {
      await connectServiceClient(client);
      const status = await client.call("baseline/status", {
        workspace: workspacePath
      });
      if (options.json) {
        console.log(JSON.stringify(status));
      } else {
        console.log(`Status: ${status.status} (${status.progress}%)`);
        if (status.stage) {
          console.log(`  Stage: ${status.stage}`);
        }
      }
    } catch (err6) {
      console.error("\u2717", err6 instanceof Error ? err6.message : String(err6));
      process.exitCode = 1;
    } finally {
      client.close();
    }
  });
  baseline.command("invalidate").description("Invalidate baseline cache for workspace").option("-w, --workspace <path>", "Workspace path", process.cwd()).action(async (options) => {
    const workspacePath = resolve(options.workspace);
    if (!isServiceRunning()) {
      console.log("Service is not running. Start with: vreko service start");
      process.exit(1);
    }
    const client = createServiceClient();
    try {
      await connectServiceClient(client);
      await client.call("baseline/invalidate", {
        workspace: workspacePath
      });
      console.log("\u2713 Baseline invalidated");
    } catch (err6) {
      console.error("\u2717", err6 instanceof Error ? err6.message : String(err6));
      process.exitCode = 1;
    } finally {
      client.close();
    }
  });
  baseline.command("show").description("Show baseline details for workspace").option("-w, --workspace <path>", "Workspace path", process.cwd()).option("-j, --json", "Output as JSON").action(async (options) => {
    const workspacePath = resolve(options.workspace);
    if (!isServiceRunning()) {
      if (options.json) {
        console.log(JSON.stringify({
          error: "daemon_not_running"
        }));
      } else {
        console.log("Service is not running. Start with: vreko service start");
      }
      process.exit(1);
    }
    const client = createServiceClient();
    try {
      await connectServiceClient(client);
      const record = await client.call("baseline/get", {
        workspace: workspacePath
      });
      if (!record) {
        if (options.json) {
          console.log("null");
        } else {
          console.log("No baseline computed. Run: vreko baseline scan");
        }
        return;
      }
      if (options.json) {
        console.log(JSON.stringify(record, null, 2));
      } else {
        console.log("Baseline Details:");
        console.log(`  Workspace:    ${record.workspacePath}`);
        console.log(`  Version:      ${record.version}`);
        console.log(`  Computed:     ${new Date(record.computedAt).toISOString()}`);
        console.log(`  Files:        ${formatNumber(record.totalFiles)}`);
        console.log(`  Lines:        ${formatNumber(record.totalLines)}`);
        console.log(`  Health:       ${record.overallHealthScore}/100`);
        console.log();
        if (record.fragileFiles?.length > 0) {
          console.log("Fragile Files:");
          for (const file of record.fragileFiles.slice(0, 10)) {
            console.log(`  ${file.rank}. ${file.path} (score: ${file.compositeScore.toFixed(1)})`);
          }
          console.log();
        }
        if (record.domainHealthScores?.length > 0) {
          console.log("Domain Health:");
          for (const domain of record.domainHealthScores) {
            console.log(`  ${domain.domain}: ${domain.score}/100 (${domain.fileCount} files)`);
          }
        }
      }
    } catch (err6) {
      console.error("\u2717 Failed to get baseline:", err6 instanceof Error ? err6.message : err6);
      process.exitCode = 1;
    } finally {
      client.close();
    }
  });
}
__name(registerBaselineCommands, "registerBaselineCommands");
function createInstallCommand() {
  return new Command("install").description("Install auto-start for Vreko local service").action(async () => {
    const platformName = platform();
    try {
      switch (platformName) {
        case "darwin":
          await installMacOS();
          break;
        case "linux":
          await installLinux();
          break;
        case "win32":
          await installWindows();
          break;
        default:
          throw new Error(`Unsupported platform: ${platformName}`);
      }
    } catch (_err) {
      process.exit(1);
    }
  });
}
__name(createInstallCommand, "createInstallCommand");
async function installMacOS() {
  const launchAgentsDir = join(homedir(), "Library", "LaunchAgents");
  const plistPath = join(launchAgentsDir, "dev.vreko.daemon.plist");
  if (!existsSync(launchAgentsDir)) {
    mkdirSync(launchAgentsDir, {
      recursive: true
    });
  }
  const vrekoPath = process.argv[1];
  const plistContent = `<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>Label</key>
	<string>dev.vreko.daemon</string>
	<key>ProgramArguments</key>
	<array>
		<string>${process.execPath}</string>
		<string>${vrekoPath}</string>
		<string>service</string>
		<string>start</string>
		<string>--service</string>
	</array>
	<key>RunAtLoad</key>
	<true/>
	<key>KeepAlive</key>
	<dict>
		<key>Crashed</key>
		<true/>
		<key>SuccessfulExit</key>
		<false/>
	</dict>
	<key>ThrottleInterval</key>
	<integer>10</integer>
	<key>StandardOutPath</key>
	<string>${join(homedir(), ".vreko", "service", "stdout.log")}</string>
	<key>StandardErrorPath</key>
	<string>${join(homedir(), ".vreko", "service", "stderr.log")}</string>
</dict>
</plist>
`;
  writeFileSync(plistPath, plistContent, "utf-8");
  try {
    await execa("launchctl", [
      "load",
      plistPath
    ]);
  } catch {
  }
}
__name(installMacOS, "installMacOS");
async function installLinux() {
  const systemdDir = join(homedir(), ".config", "systemd", "user");
  const servicePath = join(systemdDir, "vreko.service");
  if (!existsSync(systemdDir)) {
    mkdirSync(systemdDir, {
      recursive: true
    });
  }
  const vrekoPath = process.argv[1];
  const serviceContent = `[Unit]
Description=Vreko Local Service
After=network.target

[Service]
Type=simple
ExecStart=${process.execPath} ${vrekoPath} service start
Restart=on-failure
RestartSec=10
StandardOutput=append:${join(homedir(), ".vreko", "service", "stdout.log")}
StandardError=append:${join(homedir(), ".vreko", "service", "stderr.log")}

[Install]
WantedBy=default.target
`;
  writeFileSync(servicePath, serviceContent, "utf-8");
  try {
    await execa("systemctl", [
      "--user",
      "service-reload"
    ]);
    await execa("systemctl", [
      "--user",
      "enable",
      "vreko.service"
    ]);
  } catch {
  }
}
__name(installLinux, "installLinux");
async function installWindows() {
  const startupDir = join(process.env.APPDATA || homedir(), "Microsoft", "Windows", "Start Menu", "Programs", "Startup");
  if (!existsSync(startupDir)) {
    mkdirSync(startupDir, {
      recursive: true
    });
  }
  const vbsPath = join(startupDir, "VrekoService.vbs");
  const vrekoPath = process.argv[1];
  const vbsContent = `Set WshShell = CreateObject("WScript.Shell")
WshShell.Run """${process.execPath}"" ""${vrekoPath}"" service start --service", 0, False
Set WshShell = Nothing
`;
  writeFileSync(vbsPath, vbsContent, "utf-8");
  print(`  Created: ${vbsPath}`);
}
__name(installWindows, "installWindows");
function createLogsCommand() {
  return new Command("logs").description("View Vreko local service logs").option("-f, --follow", "Follow log output (like tail -f)").option("-n, --lines <number>", "Number of lines to show", "50").action(async (options) => {
    const logPath = getLogPath();
    if (!existsSync(logPath)) {
      return;
    }
    const lines = Number.parseInt(options.lines, 10);
    if (options.follow) {
      displayLastLines(logPath, lines);
      let lastSize = statSync(logPath).size;
      watchFile(logPath, {
        interval: 500
      }, (curr) => {
        const currentSize = curr.size;
        if (currentSize > lastSize) {
          const content = readFileSync(logPath, "utf-8");
          content.slice(lastSize);
          lastSize = currentSize;
        } else if (currentSize < lastSize) {
          displayLastLines(logPath, lines);
          lastSize = currentSize;
        }
      });
      await new Promise(() => {
      });
    } else {
      displayLastLines(logPath, lines);
    }
  });
}
__name(createLogsCommand, "createLogsCommand");
function displayLastLines(filePath, lineCount) {
  try {
    const content = readFileSync(filePath, "utf-8");
    const lines = content.split("\n");
    const _lastLines = lines.slice(-lineCount).join("\n");
  } catch {
  }
}
__name(displayLastLines, "displayLastLines");
function resolveVrekodBin2() {
  const bundleFile = fileURLToPath(import.meta.url);
  const workspaceRoot = resolve(dirname(bundleFile), "../../..");
  const bin = join(workspaceRoot, "apps", "local-service", "dist", "main.js");
  if (!existsSync(bin)) {
    throw new Error(`Cannot find service binary at ${bin}.
Run 'pnpm --filter @vreko/local-service build' first.`);
  }
  return bin;
}
__name(resolveVrekodBin2, "resolveVrekodBin");
function createStartCommand() {
  return new Command("start").description("Start the Vreko local service").option("-d, --service", "Run service in background (service mode)").option("-t, --idle-timeout <minutes>", "Shutdown after idle (default: 240)", "240").action(async (options) => {
    if (await isServiceHealthy()) {
      return;
    }
    const vrekodArgs = [
      "--idle-timeout",
      options.idleTimeout
    ];
    const vrekodBin = resolveVrekodBin2();
    if (options.service) {
      const child2 = spawn(process.execPath, [
        vrekodBin,
        ...vrekodArgs
      ], {
        detached: true,
        stdio: "ignore"
      });
      child2.unref();
      const maxWait = 5e3;
      const start = Date.now();
      try {
        await new Promise((resolve11, reject) => {
          const check = /* @__PURE__ */ __name(async () => {
            if (await isServiceHealthy()) {
              resolve11();
              return;
            }
            if (Date.now() - start > maxWait) {
              reject(new Error("Service failed to start within timeout"));
              return;
            }
            setTimeout(() => {
              void check();
            }, 100);
          }, "check");
          setTimeout(() => {
            void check();
          }, 100);
        });
      } catch (_err) {
        process.exit(1);
      }
      return;
    }
    const child = spawn(process.execPath, [
      vrekodBin,
      ...vrekodArgs
    ], {
      stdio: "inherit"
    });
    try {
      await new Promise((resolve11, reject) => {
        child.on("exit", (code) => {
          if (code === 0) {
            resolve11();
          } else {
            reject(new Error(`Service exited with code ${code}`));
          }
        });
        child.on("error", reject);
      });
    } catch (_err) {
      process.exit(1);
    }
  });
}
__name(createStartCommand, "createStartCommand");
function createStatusCommand2() {
  return new Command("status").description("Show Vreko local service status").option("-j, --json", "Output as JSON").action(async (options) => {
    if (!await isServiceHealthy()) {
      if (options.json) {
        console.log(JSON.stringify({
          running: false
        }));
      } else {
        console.log("Service is not running");
      }
      return;
    }
    const client = createServiceClient();
    try {
      await connectServiceClient(client);
      const status = await client.call("daemon/status", {});
      if (options.json) {
        console.log(JSON.stringify({
          running: true,
          ...status
        }));
      } else {
        console.log("Service Status:");
        console.log("  Status:       \u2713 Running");
        console.log(`  PID:          ${readServicePid()}`);
        console.log(`  Version:      ${status.version}`);
        console.log(`  Uptime:       ${formatDuration(status.uptime)}`);
        console.log(`  Started:      ${status.startedAt}`);
        console.log(`  Workspaces:   ${status.workspaces}`);
        console.log(`  Connections:  ${status.connections}`);
        console.log(`  Memory:       ${formatBytes(status.memoryUsage.heapUsed)}`);
        console.log(`  Socket:       ${getServiceSocketPath()}`);
      }
    } catch (err6) {
      if (options.json) {
        console.log(JSON.stringify({
          running: true,
          error: err6 instanceof Error ? err6.message : String(err6)
        }));
      } else {
        console.error("\u2717 Failed to get status:", err6 instanceof Error ? err6.message : String(err6));
      }
      process.exitCode = 1;
    } finally {
      client.close();
    }
  });
}
__name(createStatusCommand2, "createStatusCommand");
function createStopCommand() {
  return new Command("stop").description("Stop the Vreko local service").action(async () => {
    if (!await isServiceHealthy()) {
      console.log("Service is not running");
      return;
    }
    console.log("Stopping service...");
    const client = createServiceClient();
    try {
      await connectServiceClient(client);
      await client.daemon.shutdown();
      console.log("\u2713 Service stopped");
    } catch (_err) {
      try {
        const pid = readServicePid();
        if (pid) {
          process.kill(pid, "SIGTERM");
          console.log("\u2713 Service stopped (SIGTERM)");
        }
      } catch (killErr) {
        console.error("\u2717 Failed to stop service:", killErr instanceof Error ? killErr.message : String(killErr));
        process.exitCode = 1;
      }
    } finally {
      client.close();
    }
  });
}
__name(createStopCommand, "createStopCommand");
function createUninstallCommand() {
  return new Command("uninstall").description("Remove auto-start for Vreko local service").action(async () => {
    const platformName = platform();
    try {
      switch (platformName) {
        case "darwin":
          await uninstallMacOS();
          break;
        case "linux":
          await uninstallLinux();
          break;
        case "win32":
          await uninstallWindows();
          break;
        default:
          throw new Error(`Unsupported platform: ${platformName}`);
      }
    } catch (_err) {
      process.exit(1);
    }
  });
}
__name(createUninstallCommand, "createUninstallCommand");
async function uninstallMacOS() {
  const plistPath = join(homedir(), "Library", "LaunchAgents", "dev.vreko.daemon.plist");
  if (!existsSync(plistPath)) {
    return;
  }
  try {
    await execa("launchctl", [
      "unload",
      plistPath
    ]);
  } catch {
  }
  unlinkSync(plistPath);
}
__name(uninstallMacOS, "uninstallMacOS");
async function uninstallLinux() {
  const servicePath = join(homedir(), ".config", "systemd", "user", "vreko.service");
  if (!existsSync(servicePath)) {
    return;
  }
  try {
    await execa("systemctl", [
      "--user",
      "disable",
      "vreko.service"
    ]);
    await execa("systemctl", [
      "--user",
      "stop",
      "vreko.service"
    ]);
    await execa("systemctl", [
      "--user",
      "service-reload"
    ]);
  } catch {
  }
  unlinkSync(servicePath);
}
__name(uninstallLinux, "uninstallLinux");
async function uninstallWindows() {
  const vbsPath = join(process.env.APPDATA || homedir(), "Microsoft", "Windows", "Start Menu", "Programs", "Startup", "VrekoService.vbs");
  if (!existsSync(vbsPath)) {
    return;
  }
  unlinkSync(vbsPath);
  print(`  Removed: ${vbsPath}`);
}
__name(uninstallWindows, "uninstallWindows");

// src/commands/service/index.ts
function registerServiceCommands(program) {
  const service = program.command("service").description("Manage Vreko Local Service (service)");
  service.addCommand(createStartCommand());
  service.addCommand(createStopCommand());
  service.addCommand(createStatusCommand2());
  service.addCommand(createLogsCommand());
  service.addCommand(createInstallCommand());
  service.addCommand(createUninstallCommand());
}
__name(registerServiceCommands, "registerServiceCommands");
function getAliasPath() {
  const homeDir = process.env.HOME || process.env.USERPROFILE || "";
  return path10.join(homeDir, ".vreko", "aliases.json");
}
__name(getAliasPath, "getAliasPath");
function loadAliases() {
  try {
    const aliasPath = getAliasPath();
    if (fs.existsSync(aliasPath)) {
      return JSON.parse(fs.readFileSync(aliasPath, "utf-8"));
    }
  } catch {
  }
  return {
    aliases: {}
  };
}
__name(loadAliases, "loadAliases");
function saveAliases(config2) {
  const aliasPath = getAliasPath();
  const dir = path10.dirname(aliasPath);
  fs.mkdirSync(dir, {
    recursive: true
  });
  fs.writeFileSync(aliasPath, JSON.stringify(config2, null, 2));
}
__name(saveAliases, "saveAliases");
function setAlias(name, command, description) {
  const config2 = loadAliases();
  if (!/^[a-zA-Z][a-zA-Z0-9-_]*$/.test(name)) {
    console.log(chalk2.red("Invalid alias name. Must start with a letter and contain only letters, numbers, hyphens, and underscores."));
    return;
  }
  const reserved = [
    "login",
    "logout",
    "init",
    "status",
    "doctor",
    "help",
    "alias"
  ];
  if (reserved.includes(name)) {
    console.log(chalk2.red(`Cannot use "${name}" as an alias - it's a reserved command name.`));
    return;
  }
  const isUpdate = name in config2.aliases;
  config2.aliases[name] = {
    command,
    description,
    createdAt: (/* @__PURE__ */ new Date()).toISOString()
  };
  saveAliases(config2);
  if (isUpdate) {
    console.log(chalk2.yellow(`Updated alias "${name}"`));
  } else {
    console.log(chalk2.green(`Created alias "${name}"`));
  }
  console.log(chalk2.gray(`  vr ${name} \u2192 vr ${command}`));
}
__name(setAlias, "setAlias");
function deleteAlias(name) {
  const config2 = loadAliases();
  if (!(name in config2.aliases)) {
    console.log(chalk2.yellow(`Alias "${name}" not found`));
    return;
  }
  delete config2.aliases[name];
  saveAliases(config2);
  console.log(chalk2.green(`Deleted alias "${name}"`));
}
__name(deleteAlias, "deleteAlias");
function listAliases() {
  const config2 = loadAliases();
  const aliases = Object.entries(config2.aliases);
  if (aliases.length === 0) {
    console.log(chalk2.yellow("No aliases configured"));
    console.log(chalk2.gray("\nCreate one with: vr alias set <name> <command>"));
    return;
  }
  console.log(chalk2.cyan.bold("\nConfigured Aliases:\n"));
  for (const [name, def] of aliases) {
    console.log(`${chalk2.cyan(name)} \u2192 ${chalk2.white(def.command)}`);
    if (def.description) {
      console.log(chalk2.gray(`  ${def.description}`));
    }
  }
  console.log();
}
__name(listAliases, "listAliases");
var SUGGESTED_ALIASES = [
  {
    name: "st",
    command: "status",
    description: "Quick status check"
  },
  {
    name: "ss",
    command: "snapshot",
    description: "Create snapshot"
  },
  {
    name: "sl",
    command: "list",
    description: "List snapshots"
  },
  {
    name: "ctx",
    command: "context",
    description: "Get context for task"
  },
  {
    name: "val",
    command: "validate",
    description: "Validate code"
  },
  {
    name: "dr",
    command: "doctor",
    description: "Run diagnostics"
  }
];
function showSuggestions() {
  console.log(chalk2.cyan.bold("\n\u{1F4A1} Suggested Aliases:\n"));
  const lines = [];
  for (const { name, command, description } of SUGGESTED_ALIASES) {
    lines.push(`vr alias set ${name} "${command}"`);
    lines.push(chalk2.gray(`  # ${description}`));
  }
  console.log(lines.join("\n"));
}
__name(showSuggestions, "showSuggestions");
function createAliasCommand() {
  const cmd = new Command("alias").description("Create shortcuts for common commands");
  cmd.command("list").description("List all configured aliases").action(() => {
    listAliases();
  });
  cmd.command("set <name> <command>").description("Create or update an alias").option("-d, --description <desc>", "Add a description").action((name, command, options) => {
    setAlias(name, command, options.description);
  });
  cmd.command("delete <name>").description("Delete an alias").action((name) => {
    deleteAlias(name);
  });
  cmd.command("suggest").description("Show suggested aliases").action(() => {
    showSuggestions();
  });
  cmd.action(() => {
    listAliases();
  });
  return cmd;
}
__name(createAliasCommand, "createAliasCommand");
var CONFIG_SCHEMA = [
  // Global config
  {
    key: "apiUrl",
    scope: [
      "global"
    ],
    type: "string",
    description: "Vreko API URL",
    default: "https://api.vreko.dev"
  },
  {
    key: "defaultWorkspace",
    scope: [
      "global"
    ],
    type: "string",
    description: "Default workspace path"
  },
  {
    key: "analytics",
    scope: [
      "global"
    ],
    type: "boolean",
    description: "Enable anonymous usage analytics",
    default: true
  },
  // Workspace config
  {
    key: "protectionLevel",
    scope: [
      "local"
    ],
    type: "string",
    description: "Protection level (standard | strict)",
    default: "standard"
  },
  {
    key: "syncEnabled",
    scope: [
      "local"
    ],
    type: "boolean",
    description: "Enable cloud sync",
    default: true
  },
  {
    key: "tier",
    scope: [
      "local"
    ],
    type: "string",
    description: "User tier (free | pro)",
    default: "free"
  }
];
function createConfigCommand() {
  const config2 = new Command("config").description("Manage Vreko configuration");
  config2.command("list").description("List all configuration values").option("--global", "Show only global config").option("--local", "Show only local/workspace config").option("--json", "Output as JSON").action(async (options) => {
    try {
      await listConfig(options);
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  config2.command("get <key>").description("Get a configuration value").option("--global", "Get from global config").option("--local", "Get from local/workspace config").action(async (key, options) => {
    try {
      await getConfigValue(key, options);
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  config2.command("set <key> <value>").description("Set a configuration value").option("--global", "Set in global config").option("--local", "Set in local/workspace config").action(async (key, value, options) => {
    try {
      await setConfigValue(key, value, options);
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  config2.command("unset <key>").description("Remove a configuration value").option("--global", "Unset from global config").option("--local", "Unset from local/workspace config").action(async (key, options) => {
    try {
      await unsetConfigValue(key, options);
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Error: ${message}`);
      process.exit(1);
    }
  });
  config2.command("path").description("Show configuration file paths").action(async () => {
    await showConfigPaths();
  });
  config2.command("keys").description("List all available configuration keys").action(() => {
    showConfigKeys();
  });
  return config2;
}
__name(createConfigCommand, "createConfigCommand");
async function listConfig(options) {
  const cwd = process.cwd();
  const result7 = {};
  if (!options.local) {
    const globalConfig2 = await getGlobalConfig();
    if (options.json) {
      result7.global = globalConfig2 || {};
    } else {
      if (globalConfig2) {
        console.log(chalk2.cyan.bold("Global Configuration"));
        console.log();
        for (const [key, value] of Object.entries(globalConfig2)) {
          console.log(`  ${chalk2.cyan(key)}: ${formatValue(value)}`);
        }
      } else {
        console.log(chalk2.gray("  (no global config set)"));
      }
    }
  }
  if (!options.global) {
    const hasWorkspace = await isVrekoInitialized(cwd);
    if (hasWorkspace) {
      const workspaceConfig = await getWorkspaceConfig(cwd);
      if (options.json) {
        result7.local = workspaceConfig || {};
      } else {
        if (workspaceConfig) {
          console.log(chalk2.cyan.bold("Workspace Configuration"));
          console.log();
          for (const [key, value] of Object.entries(workspaceConfig)) {
            console.log(`  ${chalk2.cyan(key)}: ${formatValue(value)}`);
          }
        } else {
          console.log(chalk2.gray("  (no workspace config set)"));
        }
      }
    } else if (!options.json) {
      console.log(chalk2.yellow("Not in a Vreko workspace"));
      console.log(chalk2.gray("Run: vr init"));
    }
  }
  if (options.json) {
    console.log(JSON.stringify(result7, null, 2));
  }
}
__name(listConfig, "listConfig");
async function getConfigValue(key, options) {
  const cwd = process.cwd();
  const schema = CONFIG_SCHEMA.find((s) => s.key === key);
  if (!schema) {
    console.log(chalk2.yellow(`Unknown config key: ${key}`));
    console.log(chalk2.gray("Run 'vr config keys' to see available keys"));
    return;
  }
  const scope = determineScope(options, schema);
  let value;
  if (scope === "global") {
    const config2 = await getGlobalConfig();
    value = config2?.[key];
  } else {
    if (!await isVrekoInitialized(cwd)) {
      console.error("Vreko not initialized in this workspace. Run 'vreko init' first.");
      process.exit(1);
    }
    const config2 = await getWorkspaceConfig(cwd);
    value = config2?.[key];
  }
  if (value === void 0) {
    if (schema.default !== void 0) {
      console.log(formatValue(schema.default), chalk2.gray("(default)"));
    } else {
      console.log(chalk2.gray("(not set)"));
    }
  } else {
    console.log(formatValue(value));
  }
}
__name(getConfigValue, "getConfigValue");
async function setConfigValue(key, value, options) {
  const cwd = process.cwd();
  const schema = CONFIG_SCHEMA.find((s) => s.key === key);
  if (!schema) {
    console.log(chalk2.yellow(`Unknown config key: ${key}`));
    console.log(chalk2.gray("Run 'vr config keys' to see available keys"));
    return;
  }
  const scope = determineScope(options, schema);
  const parsedValue = parseValue(value, schema.type);
  if (scope === "global") {
    const config2 = await getGlobalConfig() || {};
    config2[key] = parsedValue;
    await saveGlobalConfig(config2);
  } else {
    if (!await isVrekoInitialized(cwd)) {
      console.error("Vreko not initialized in this workspace. Run 'vreko init' first.");
      process.exit(1);
    }
    const config2 = await getWorkspaceConfig(cwd) || {
      createdAt: (/* @__PURE__ */ new Date()).toISOString(),
      updatedAt: (/* @__PURE__ */ new Date()).toISOString()
    };
    config2[key] = parsedValue;
    config2.updatedAt = (/* @__PURE__ */ new Date()).toISOString();
    await saveWorkspaceConfig(config2, cwd);
  }
  console.log(chalk2.green("\u2713"), `${key} = ${formatValue(parsedValue)}`);
}
__name(setConfigValue, "setConfigValue");
async function unsetConfigValue(key, options) {
  const cwd = process.cwd();
  const schema = CONFIG_SCHEMA.find((s) => s.key === key);
  if (!schema) {
    console.log(chalk2.yellow(`Unknown config key: ${key}`));
    console.log(chalk2.gray("Run 'vr config keys' to see available keys"));
    return;
  }
  const scope = determineScope(options, schema);
  if (scope === "global") {
    const config2 = await getGlobalConfig();
    if (config2 && key in config2) {
      delete config2[key];
      await saveGlobalConfig(config2);
      console.log(chalk2.green("\u2713"), `Unset ${key} from global config`);
    } else {
      console.log(chalk2.gray(`${key} is not set in global config`));
    }
  } else {
    if (!await isVrekoInitialized(cwd)) {
      console.error("Vreko not initialized in this workspace. Run 'vreko init' first.");
      process.exit(1);
    }
    const config2 = await getWorkspaceConfig(cwd);
    if (config2 && key in config2) {
      delete config2[key];
      config2.updatedAt = (/* @__PURE__ */ new Date()).toISOString();
      await saveWorkspaceConfig(config2, cwd);
      console.log(chalk2.green("\u2713"), `Unset ${key} from workspace config`);
    } else {
      console.log(chalk2.gray(`${key} is not set in workspace config`));
    }
  }
}
__name(unsetConfigValue, "unsetConfigValue");
async function showConfigPaths() {
  const cwd = process.cwd();
  console.log(chalk2.cyan.bold("Configuration Paths"));
  console.log();
  console.log(`  ${chalk2.cyan("Global")}: ~/.vreko/config.json`);
  if (await isVrekoInitialized(cwd)) {
    console.log(`  ${chalk2.cyan("Workspace")}: ${cwd}/.vreko/config.json`);
  } else {
    console.log(chalk2.gray("  Workspace: (not initialized)"));
  }
}
__name(showConfigPaths, "showConfigPaths");
function showConfigKeys() {
  console.log(chalk2.cyan.bold("Global Keys"));
  console.log();
  for (const schema of CONFIG_SCHEMA.filter((s) => s.scope.includes("global"))) {
    console.log(`  ${chalk2.cyan(schema.key)} (${schema.type}): ${schema.description}`);
    if (schema.default !== void 0) {
      console.log(`    ${chalk2.gray(`default: ${schema.default}`)}`);
    }
  }
  console.log();
  console.log(chalk2.cyan.bold("Workspace Keys"));
  console.log();
  for (const schema of CONFIG_SCHEMA.filter((s) => s.scope.includes("local"))) {
    console.log(`  ${chalk2.cyan(schema.key)} (${schema.type}): ${schema.description}`);
    if (schema.default !== void 0) {
      console.log(`    ${chalk2.gray(`default: ${schema.default}`)}`);
    }
  }
}
__name(showConfigKeys, "showConfigKeys");
function determineScope(options, schema) {
  if (options.global) {
    return "global";
  }
  if (options.local) {
    return "local";
  }
  return schema.scope[0];
}
__name(determineScope, "determineScope");
function parseValue(value, type) {
  switch (type) {
    case "boolean":
      return value === "true" || value === "1" || value === "yes";
    case "number":
      return Number(value);
    default:
      return value;
  }
}
__name(parseValue, "parseValue");
function formatValue(value) {
  if (typeof value === "boolean") {
    return value ? chalk2.green("true") : chalk2.red("false");
  }
  if (typeof value === "string") {
    return chalk2.yellow(`"${value}"`);
  }
  return String(value);
}
__name(formatValue, "formatValue");
var HOME_PATH_RE = /\/(Users|home)\/[^/]+\//g;
var GH_TOKEN_RE = /(ghp|gho|ghu|ghs)_[A-Za-z0-9]{36}/g;
var API_KEY_RE = /sk-[A-Za-z0-9]{32,}/g;
var SECRET_KEY_RE = /"(token|secret|key|dsn|password|apiKey|api_key)":\s*"[^"]+"/gi;
function scrub(text2) {
  return text2.replace(HOME_PATH_RE, "/~/").replace(GH_TOKEN_RE, "[REDACTED_GH_TOKEN]").replace(API_KEY_RE, "[REDACTED_API_KEY]").replace(SECRET_KEY_RE, (_, k) => `"${k}": "[REDACTED]"`);
}
__name(scrub, "scrub");
function vrekoDir() {
  return join(homedir(), ".vreko");
}
__name(vrekoDir, "vrekoDir");
function readDaemonLogs() {
  const logDir = join(vrekoDir(), "logs");
  if (!existsSync(logDir)) {
    return "(no service logs found)";
  }
  const cutoff = Date.now() - 7 * 24 * 60 * 60 * 1e3;
  const lines = [];
  try {
    const files = readdirSync(logDir).filter((f) => f.endsWith(".log")).map((f) => ({
      name: f,
      mtime: statSync(join(logDir, f)).mtimeMs
    })).filter((f) => f.mtime >= cutoff).sort((a, b) => a.mtime - b.mtime);
    for (const { name } of files) {
      const content = readFileSync(join(logDir, name), "utf8");
      lines.push(`=== ${name} ===
${content}`);
    }
  } catch {
    return "(error reading service logs)";
  }
  return lines.join("\n") || "(no logs in last 7 days)";
}
__name(readDaemonLogs, "readDaemonLogs");
function readRedactedConfig() {
  const configPath = join(vrekoDir(), "config.json");
  if (!existsSync(configPath)) {
    return {};
  }
  try {
    const raw = readFileSync(configPath, "utf8");
    const parsed = JSON.parse(raw);
    return JSON.parse(scrub(JSON.stringify(parsed)));
  } catch {
    return {
      error: "could not parse config.json"
    };
  }
}
__name(readRedactedConfig, "readRedactedConfig");
function readVersions() {
  const versions = {
    platform: platform(),
    os: release(),
    node: process.version
  };
  try {
    const pkgPath = join(dirname(new URL(import.meta.url).pathname), "../../package.json");
    const pkg = JSON.parse(readFileSync(pkgPath, "utf8"));
    versions.cli = pkg.version ?? "unknown";
  } catch {
    versions.cli = "unknown";
  }
  try {
    const statePath = join(vrekoDir(), "service-version.json");
    if (existsSync(statePath)) {
      const state = JSON.parse(readFileSync(statePath, "utf8"));
      versions.service = state.version ?? "unknown";
    }
  } catch {
    versions.service = "unknown";
  }
  return versions;
}
__name(readVersions, "readVersions");
function readWorkspacesSummary() {
  const sessionsDir = join(vrekoDir(), "sessions");
  if (!existsSync(sessionsDir)) {
    return [];
  }
  try {
    return readdirSync(sessionsDir, {
      withFileTypes: true
    }).filter((d) => d.isDirectory()).map((d) => {
      const hash = d.name;
      const snapshotDir = join(vrekoDir(), "snapshots", hash);
      const sessionFiles = existsSync(sessionsDir) ? readdirSync(join(sessionsDir, hash)).filter((f) => f.endsWith(".json")).length : 0;
      const snapshotFiles = existsSync(snapshotDir) ? readdirSync(snapshotDir).filter((f) => f.endsWith(".snap")).length : 0;
      return {
        workspaceHash: hash,
        sessionCount: sessionFiles,
        snapshotCount: snapshotFiles
      };
    });
  } catch {
    return [];
  }
}
__name(readWorkspacesSummary, "readWorkspacesSummary");
function readRecentErrors() {
  const errorLogPath = join(vrekoDir(), "logs", "errors.log");
  if (!existsSync(errorLogPath)) {
    return [];
  }
  try {
    const lines = readFileSync(errorLogPath, "utf8").split("\n").filter(Boolean).slice(-50).map((line) => {
      try {
        return JSON.parse(scrub(line));
      } catch {
        return {
          raw: scrub(line)
        };
      }
    });
    return lines;
  } catch {
    return [];
  }
}
__name(readRecentErrors, "readRecentErrors");
async function writeBundleZip(outputPath) {
  let archiver = null;
  try {
    archiver = (await import('archiver')).default;
  } catch {
  }
  const outputDir = dirname(outputPath);
  if (!existsSync(outputDir)) {
    mkdirSync(outputDir, {
      recursive: true
    });
  }
  const daemonLog = scrub(readDaemonLogs());
  const config2 = readRedactedConfig();
  const versions = readVersions();
  const workspaces = readWorkspacesSummary();
  const recentErrors = readRecentErrors();
  if (archiver) {
    await new Promise((resolve11, reject) => {
      const output = createWriteStream(outputPath);
      const archive = archiver("zip", {
        zlib: {
          level: 6
        }
      });
      output.on("close", resolve11);
      archive.on("error", reject);
      archive.pipe(output);
      const archiveAny = archive;
      archiveAny.append(daemonLog, {
        name: "service.log"
      });
      archiveAny.append(JSON.stringify(config2, null, 2), {
        name: "config.json"
      });
      archiveAny.append(JSON.stringify(versions, null, 2), {
        name: "versions.json"
      });
      archiveAny.append(JSON.stringify(workspaces, null, 2), {
        name: "workspaces.json"
      });
      archiveAny.append(JSON.stringify(recentErrors, null, 2), {
        name: "recent-errors.json"
      });
      archiveAny.finalize();
    });
  } else {
    const bundle = {
      "service.log": daemonLog,
      "config.json": config2,
      "versions.json": versions,
      "workspaces.json": workspaces,
      "recent-errors.json": recentErrors
    };
    await new Promise((resolve11, reject) => {
      const gz = createGzip();
      const out = createWriteStream(outputPath);
      gz.pipe(out);
      gz.on("error", reject);
      out.on("close", resolve11);
      gz.write(JSON.stringify(bundle, null, 2));
      gz.end();
    });
  }
}
__name(writeBundleZip, "writeBundleZip");
function createDiagnosticsCommand() {
  const diagnostics = new Command("diagnostics").description("Diagnostics tools for troubleshooting Vreko");
  diagnostics.command("bundle").description("Create a support bundle (logs, config, versions  -  no file contents)").option("--output <path>", "Output path for the bundle zip", join(homedir(), `vreko-diagnostics-${Date.now()}.zip`)).action(async (opts) => {
    console.log(`Creating diagnostics bundle at: ${opts.output}`);
    try {
      await writeBundleZip(opts.output);
      console.log(`Bundle created: ${opts.output}`);
      console.log("Send this file to support. It contains no file contents or auth tokens.");
    } catch (err6) {
      console.error("Failed to create bundle:", err6 instanceof Error ? err6.message : String(err6));
      process.exit(1);
    }
  });
  return diagnostics;
}
__name(createDiagnosticsCommand, "createDiagnosticsCommand");
var cliVersion = "3.1.6" ;
function createDoctorCommand() {
  return new Command("doctor").description("Diagnose Vreko installation and ecosystem health").option("--json", "Output structured JSON result").option("--local", "Skip network checks").option("--check <system>", "Check specific subsystem (cli|service|workspace|knowledge|mcp|network|auth|extension)").option("--fix", "Attempt automatic repair for known issues").option("--sync-config", "Trigger immediate fingerprint config sync from cloud").option("-q, --quiet", "Only show failures").option("-v, --verbose", "Show all check details").action(async (options) => {
    if (options.syncConfig) {
      await handleSyncConfig();
      return;
    }
    const result7 = await runDoctor(options);
    if (options.json) {
      console.log(JSON.stringify(result7, null, 2));
    }
    process.exit(result7.success ? 0 : 1);
  });
}
__name(createDoctorCommand, "createDoctorCommand");
async function runDoctor(options) {
  const jsonMode = !!options.json;
  const quiet = !!options.quiet;
  const onlyGroup = options.check;
  const checks = [];
  const workspacePath = findWorkspaceRoot(process.cwd());
  if (!jsonMode) {
    console.log(chalk2.cyan(`  \u{1F98E} Vreko Doctor v${cliVersion}`));
    console.log(chalk2.gray(`  Platform: ${platformLabel()}, Node ${process.version}`));
    console.log();
  }
  const groups = [
    [
      "cli",
      () => checkCLI()
    ],
    [
      "service",
      () => checkDaemon()
    ],
    [
      "workspace",
      () => checkWorkspace(workspacePath)
    ],
    [
      "knowledge",
      () => checkKnowledgeStore(workspacePath)
    ],
    [
      "mcp",
      () => checkMCP()
    ],
    [
      "network",
      () => options.local ? skipGroup("network", "Skipped (--local)") : checkNetwork()
    ],
    [
      "auth",
      () => checkAuth()
    ],
    [
      "extension",
      () => checkExtension()
    ]
  ];
  for (const [group2, runner] of groups) {
    if (onlyGroup && onlyGroup !== group2) {
      continue;
    }
    const groupChecks = await runner();
    checks.push(...groupChecks);
    if (!jsonMode) {
      printGroup(group2, groupChecks, quiet);
    }
  }
  if (options.fix) {
    const fixable = checks.filter((c) => c.status === "fail" && c.fixCommand);
    if (fixable.length > 0 && !jsonMode) {
      console.log(chalk2.cyan("  Attempting fixes..."));
      for (const check of fixable) {
        try {
          execSync(check.fixCommand, {
            stdio: "pipe",
            timeout: 15e3
          });
          check.status = "pass";
          check.detail = `Fixed: ${check.fixCommand}`;
          console.log(chalk2.green(`  \u2714 Fixed: ${check.label}`));
        } catch {
          console.log(chalk2.red(`  \u2716 Could not fix: ${check.label}`));
        }
      }
      console.log();
    }
  }
  const summary = {
    total: checks.length,
    pass: checks.filter((c) => c.status === "pass").length,
    warn: checks.filter((c) => c.status === "warn").length,
    fail: checks.filter((c) => c.status === "fail").length,
    skip: checks.filter((c) => c.status === "skip").length
  };
  const success = summary.fail === 0;
  if (!jsonMode) {
    console.log(chalk2.gray(`  Summary: ${summary.pass} passed, ${summary.warn} warning${summary.warn !== 1 ? "s" : ""}, ${summary.fail} failure${summary.fail !== 1 ? "s" : ""}`));
    if (success) {
      console.log(chalk2.green("  \u2714 \u{1F98E} Vreko is healthy"));
    } else {
      console.log(chalk2.red("  \u2716 \u{1F98E} Vreko has issues  -  run the suggested fixes above"));
    }
    console.log();
  }
  return {
    success,
    version: cliVersion,
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    workspace: workspacePath,
    platform: {
      os: platform(),
      arch: arch(),
      nodeVersion: process.version,
      shell: process.env.SHELL || process.env.ComSpec || "unknown"
    },
    summary,
    checks
  };
}
__name(runDoctor, "runDoctor");
async function checkCLI() {
  const checks = [];
  try {
    const which = execSync("which vreko || where vreko", {
      encoding: "utf-8",
      stdio: [
        "pipe",
        "pipe",
        "pipe"
      ]
    }).trim();
    checks.push({
      id: "cli.binary",
      group: "cli",
      label: "CLI binary found",
      status: "pass",
      detail: `${which} (v${cliVersion})`
    });
  } catch {
    checks.push({
      id: "cli.binary",
      group: "cli",
      label: "CLI binary found",
      status: "pass",
      detail: `v${cliVersion}`
    });
  }
  const nodeVersion = process.versions.node;
  const major = Number.parseInt(nodeVersion.split(".")[0], 10);
  if (major >= 18) {
    checks.push({
      id: "cli.node",
      group: "cli",
      label: "Node.js version compatible",
      status: "pass",
      detail: `v${nodeVersion} (>=18 required)`
    });
  } else {
    checks.push({
      id: "cli.node",
      group: "cli",
      label: "Node.js version compatible",
      status: "fail",
      detail: `v${nodeVersion} (>=18 required)`,
      fix: "Upgrade Node.js to v18 or later: https://nodejs.org"
    });
  }
  const globalConfigPath = join(homedir(), ".vreko", "config.json");
  if (existsSync(globalConfigPath)) {
    try {
      JSON.parse(readFileSync(globalConfigPath, "utf-8"));
      checks.push({
        id: "cli.globalConfig",
        group: "cli",
        label: "Global config valid",
        status: "pass",
        detail: globalConfigPath
      });
    } catch {
      checks.push({
        id: "cli.globalConfig",
        group: "cli",
        label: "Global config valid",
        status: "fail",
        detail: `${globalConfigPath}  -  invalid JSON`,
        fix: "Delete and reinitialize: rm ~/.vreko/config.json && vreko init",
        fixCommand: `rm "${globalConfigPath}"`
      });
    }
  } else {
    checks.push({
      id: "cli.globalConfig",
      group: "cli",
      label: "Global config",
      status: "warn",
      detail: "No global config (using defaults)"
    });
  }
  return checks;
}
__name(checkCLI, "checkCLI");
async function checkDaemon() {
  const checks = [];
  const pidPath = getServicePidPath();
  const sockPath = process.env.VREKO_DAEMON_SOCKET ?? getDefaultSocketPath();
  let pid = null;
  if (existsSync(pidPath)) {
    pid = readFileSync(pidPath, "utf-8").trim();
    const alive = isProcessAlive(Number.parseInt(pid, 10));
    if (alive) {
      checks.push({
        id: "service.running",
        group: "service",
        label: "Service process running",
        status: "pass",
        detail: `pid ${pid}`
      });
    } else {
      checks.push({
        id: "service.running",
        group: "service",
        label: "Service process running",
        status: "fail",
        detail: `PID ${pid} in pidfile but process not found (stale PID)`,
        fix: "Start the service: vreko service start --detach",
        fixCommand: "vreko service start --detach"
      });
      return checks;
    }
  } else {
    checks.push({
      id: "service.running",
      group: "service",
      label: "Service process running",
      status: "fail",
      detail: "No PID file found",
      fix: "Start the service: vreko service start --detach",
      fixCommand: "vreko service start --detach"
    });
    return checks;
  }
  if (existsSync(sockPath)) {
    checks.push({
      id: "service.socket",
      group: "service",
      label: "Socket connectable",
      status: "pass",
      detail: sockPath
    });
  } else {
    checks.push({
      id: "service.socket",
      group: "service",
      label: "Socket connectable",
      status: "fail",
      detail: `${sockPath} does not exist`,
      fix: "Restart the service: vreko service restart",
      fixCommand: "vreko service restart"
    });
    return checks;
  }
  try {
    const client = await connectToDaemon();
    const start = Date.now();
    const ping = await client.daemon.ping();
    const latency = Date.now() - start;
    checks.push({
      id: "service.ipc",
      group: "service",
      label: "IPC handshake",
      status: latency < 100 ? "pass" : "warn",
      detail: `v${ping?.version ?? "?"}, ${latency}ms${latency >= 100 ? " (>100ms budget)" : ""}`
    });
    if (ping?.version && ping.version !== cliVersion) {
      const daemonMajor = ping.version.split(".")[0];
      const cliMajor = cliVersion.split(".")[0];
      if (daemonMajor !== cliMajor) {
        checks.push({
          id: "service.version",
          group: "service",
          label: "Service version compatible",
          status: "fail",
          detail: `Service v${ping.version}, CLI v${cliVersion} (major mismatch)`,
          fix: "Restart service to pick up new version: vreko service restart",
          fixCommand: "vreko service restart"
        });
      } else {
        checks.push({
          id: "service.version",
          group: "service",
          label: "Service version compatible",
          status: "warn",
          detail: `Service v${ping.version}, CLI v${cliVersion} (minor mismatch)`,
          fix: "Consider restarting: vreko service restart"
        });
      }
    }
    try {
      const status = await client.daemon.status();
      const uptime = status?.uptime ? formatUptime(status.uptime) : "unknown";
      const workspaces = status?.workspaces ?? "?";
      const clients2 = status?.connections ?? "?";
      checks.push({
        id: "service.status",
        group: "service",
        label: "Service status",
        status: "pass",
        detail: `${workspaces} workspace(s), ${clients2} client(s), uptime ${uptime}`
      });
    } catch {
      checks.push({
        id: "service.status",
        group: "service",
        label: "Service status",
        status: "warn",
        detail: "service/status endpoint unavailable"
      });
    }
  } catch (error) {
    checks.push({
      id: "service.ipc",
      group: "service",
      label: "IPC handshake",
      status: "fail",
      detail: error instanceof Error ? error.message : "Connection refused",
      fix: "Restart the service: vreko service restart",
      fixCommand: "vreko service restart"
    });
  }
  checks.push(...checkDaemonSupervisor());
  return checks;
}
__name(checkDaemon, "checkDaemon");
function checkDaemonSupervisor() {
  const result7 = [];
  const os2 = platform();
  if (os2 === "darwin") {
    result7.push(...checkLaunchd());
  } else if (os2 === "linux") {
    result7.push(...checkSystemd());
  }
  return result7;
}
__name(checkDaemonSupervisor, "checkDaemonSupervisor");
function checkLaunchd() {
  const plistPath = join(homedir(), "Library", "LaunchAgents", "dev.vreko.daemon.plist");
  if (!existsSync(plistPath)) {
    return [
      {
        id: "service.supervisor",
        group: "service",
        label: "Supervisor (launchd) installed",
        status: "warn",
        detail: "Supervisor plist not installed  -  daemon will not auto-restart on crash",
        fix: "Install supervisor: vreko service install",
        fixCommand: "vreko service install"
      }
    ];
  }
  try {
    const launchctlOut = execSync("launchctl list dev.vreko.daemon 2>/dev/null", {
      encoding: "utf-8",
      stdio: [
        "pipe",
        "pipe",
        "pipe"
      ]
    }).trim();
    const isLoaded = launchctlOut.length > 0;
    return [
      {
        id: "service.supervisor",
        group: "service",
        label: "Supervisor (launchd) installed",
        status: isLoaded ? "pass" : "warn",
        detail: isLoaded ? "Plist loaded by launchd" : "Plist present but not loaded by launchd",
        ...isLoaded ? {} : {
          fix: "Load it: vreko service start",
          fixCommand: "vreko service start"
        }
      }
    ];
  } catch {
    return [
      {
        id: "service.supervisor",
        group: "service",
        label: "Supervisor (launchd) installed",
        status: "warn",
        detail: "Plist present but launchctl query failed"
      }
    ];
  }
}
__name(checkLaunchd, "checkLaunchd");
function checkSystemd() {
  const unitPath = join(homedir(), ".config", "systemd", "user", "vrekod.service");
  if (!existsSync(unitPath)) {
    return [
      {
        id: "service.supervisor",
        group: "service",
        label: "Supervisor (systemd) installed",
        status: "warn",
        detail: "Supervisor unit not installed  -  daemon will not auto-restart on crash",
        fix: "Install supervisor: vreko service install",
        fixCommand: "vreko service install"
      }
    ];
  }
  try {
    const systemctlOut = execSync("systemctl --user is-active vrekod 2>/dev/null", {
      encoding: "utf-8",
      stdio: [
        "pipe",
        "pipe",
        "pipe"
      ]
    }).trim();
    const isActive = systemctlOut === "active";
    return [
      {
        id: "service.supervisor",
        group: "service",
        label: "Supervisor (systemd) installed",
        status: isActive ? "pass" : "warn",
        detail: isActive ? "Unit active" : `Unit present but status: ${systemctlOut}`,
        ...isActive ? {} : {
          fix: "Start it: vreko service start",
          fixCommand: "vreko service start"
        }
      }
    ];
  } catch {
    return [
      {
        id: "service.supervisor",
        group: "service",
        label: "Supervisor (systemd) installed",
        status: "warn",
        detail: "Unit present but systemctl query failed"
      }
    ];
  }
}
__name(checkSystemd, "checkSystemd");
async function checkWorkspace(workspacePath) {
  const checks = [];
  if (!workspacePath) {
    checks.push({
      id: "workspace.detected",
      group: "workspace",
      label: "Workspace detected",
      status: "warn",
      detail: "Not in a Vreko workspace (no .vreko/ found in parents)",
      fix: "Run: vreko init"
    });
    return checks;
  }
  const vrekoDir2 = join(workspacePath, ".vreko");
  if (existsSync(vrekoDir2)) {
    checks.push({
      id: "workspace.directory",
      group: "workspace",
      label: ".vreko/ directory exists",
      status: "pass"
    });
  } else {
    checks.push({
      id: "workspace.directory",
      group: "workspace",
      label: ".vreko/ directory exists",
      status: "fail",
      fix: "Run: vreko init",
      fixCommand: `vreko init "${workspacePath}" --yes`
    });
    return checks;
  }
  const configPath = join(vrekoDir2, "config.json");
  if (existsSync(configPath)) {
    try {
      const config2 = JSON.parse(readFileSync(configPath, "utf-8"));
      const created = config2.createdAt ? `created ${formatAge2(Date.now() - new Date(config2.createdAt).getTime())} ago` : "";
      checks.push({
        id: "workspace.config",
        group: "workspace",
        label: "config.json valid",
        status: "pass",
        detail: created
      });
    } catch {
      checks.push({
        id: "workspace.config",
        group: "workspace",
        label: "config.json valid",
        status: "fail",
        detail: "Invalid JSON",
        fix: "Reinitialize: vreko init --force",
        fixCommand: `vreko init "${workspacePath}" --force --yes`
      });
    }
  } else {
    checks.push({
      id: "workspace.config",
      group: "workspace",
      label: "config.json exists",
      status: "fail",
      fix: "Run: vreko init",
      fixCommand: `vreko init "${workspacePath}" --yes`
    });
  }
  try {
    if (isDaemonConnected()) {
      const client = getDaemonClient();
      await client.session.current({
        workspacePath
      });
      checks.push({
        id: "workspace.registered",
        group: "workspace",
        label: "Workspace registered with service",
        status: "pass"
      });
    } else {
      checks.push({
        id: "workspace.registered",
        group: "workspace",
        label: "Workspace registered with service",
        status: "warn",
        detail: "Could not verify (service may not be running)"
      });
    }
  } catch {
    checks.push({
      id: "workspace.registered",
      group: "workspace",
      label: "Workspace registered with service",
      status: "warn",
      detail: "Could not verify (service may not be running)"
    });
  }
  try {
    if (isDaemonConnected()) {
      const client = getDaemonClient();
      await client.watch.subscribe({
        workspace: workspacePath
      });
      checks.push({
        id: "workspace.watcher",
        group: "workspace",
        label: "File watcher active",
        status: "pass"
      });
    } else {
      checks.push({
        id: "workspace.watcher",
        group: "workspace",
        label: "File watcher",
        status: "warn",
        detail: "Could not verify file watcher status (service not running)"
      });
    }
  } catch {
    checks.push({
      id: "workspace.watcher",
      group: "workspace",
      label: "File watcher",
      status: "warn",
      detail: "Could not verify file watcher status"
    });
  }
  checks.push(...checkAgentsJsonFreshness(workspacePath));
  return checks;
}
__name(checkWorkspace, "checkWorkspace");
function resolveWorkspaceJsonPath(workspacePath) {
  const newPath = join(workspacePath, ".agents", "workspace.json");
  const legacyPath = join(workspacePath, ".agents", "agents.workspace.json");
  return existsSync(newPath) ? newPath : legacyPath;
}
__name(resolveWorkspaceJsonPath, "resolveWorkspaceJsonPath");
function checkAgentsJsonFreshness(workspacePath) {
  const agentsJsonPath = resolveWorkspaceJsonPath(workspacePath);
  if (!existsSync(agentsJsonPath)) {
    return [
      {
        id: "workspace.agents-json-fresh",
        group: "workspace",
        label: "workspace.json",
        status: "warn",
        detail: ".agents/workspace.json not found  -  daemon may not have emitted yet",
        fix: "Restart daemon: vreko service restart",
        fixCommand: "vreko service restart"
      }
    ];
  }
  try {
    const agentsStat = statSync(agentsJsonPath);
    const ageMs = Date.now() - agentsStat.mtimeMs;
    const ONE_HOUR_MS = 60 * 60 * 1e3;
    if (ageMs > ONE_HOUR_MS) {
      return [
        {
          id: "workspace.agents-json-fresh",
          group: "workspace",
          label: "workspace.json fresh",
          status: "warn",
          detail: `Last updated ${formatAge2(ageMs)} ago  -  daemon may have stopped emitting`,
          fix: "Restart daemon: vreko service restart",
          fixCommand: "vreko service restart"
        }
      ];
    }
    return [
      {
        id: "workspace.agents-json-fresh",
        group: "workspace",
        label: "workspace.json fresh",
        status: "pass",
        detail: `Updated ${formatAge2(ageMs)} ago`
      }
    ];
  } catch {
    return [];
  }
}
__name(checkAgentsJsonFreshness, "checkAgentsJsonFreshness");
async function checkKnowledgeStore(workspacePath) {
  const checks = [];
  if (!workspacePath) {
    checks.push({
      id: "knowledge.exists",
      group: "knowledge",
      label: "Knowledge store",
      status: "skip",
      detail: "No workspace detected"
    });
    return checks;
  }
  const dbPath = join(workspacePath, ".vreko", "knowledge.db");
  if (!existsSync(dbPath)) {
    checks.push({
      id: "knowledge.exists",
      group: "knowledge",
      label: "knowledge.db exists",
      status: "warn",
      detail: "No knowledge store yet (will be created on first session)"
    });
    return checks;
  }
  const stat2 = statSync(dbPath);
  const sizeMB = (stat2.size / 1024 / 1024).toFixed(1);
  checks.push({
    id: "knowledge.exists",
    group: "knowledge",
    label: "knowledge.db exists",
    status: "pass",
    detail: `${dbPath} (${sizeMB}MB)`
  });
  const counts = await queryKnowledgeCountsDirect(dbPath);
  if (counts) {
    checks.push({
      id: "knowledge.chunks",
      group: "knowledge",
      label: "Chunks indexed (FTS5)",
      status: counts.chunks > 0 ? "pass" : "warn",
      detail: `${counts.chunks} chunks`
    });
    checks.push({
      id: "knowledge.embeddings",
      group: "knowledge",
      label: "Embeddings stored",
      status: counts.embeddings > 0 ? "pass" : "warn",
      detail: `${counts.embeddings} embeddings`
    });
    checks.push({
      id: "knowledge.edges",
      group: "knowledge",
      label: "Knowledge edges",
      status: counts.edges > 0 ? "pass" : "warn",
      detail: counts.edges > 0 ? `${counts.edges} edges` : "0 edges (graph retrieval not yet wired  -  expected)"
    });
    checks.push({
      id: "knowledge.outcomes",
      group: "knowledge",
      label: "Outcomes recorded",
      status: counts.outcomes > 0 ? "pass" : "warn",
      detail: counts.outcomes > 0 ? `${counts.outcomes} outcomes` : "0 outcomes (populates over time  -  expected for new workspaces)"
    });
  }
  return checks;
}
__name(checkKnowledgeStore, "checkKnowledgeStore");
async function queryKnowledgeCountsDirect(dbPath) {
  try {
    const query = /* @__PURE__ */ __name((table) => {
      try {
        const result7 = execSync(`sqlite3 "${dbPath}" "SELECT COUNT(*) FROM ${table};"`, {
          encoding: "utf-8",
          stdio: [
            "pipe",
            "pipe",
            "pipe"
          ],
          timeout: 3e3
        }).trim();
        return Number.parseInt(result7, 10) || 0;
      } catch {
        return 0;
      }
    }, "query");
    return {
      chunks: query("chunks"),
      embeddings: query("embeddings"),
      edges: query("knowledge_edges"),
      outcomes: query("outcomes")
    };
  } catch {
    return null;
  }
}
__name(queryKnowledgeCountsDirect, "queryKnowledgeCountsDirect");
async function checkMCP() {
  const checks = [];
  const workspaceRoot = findWorkspaceRoot(process.cwd()) ?? process.cwd();
  const mcpJsonPath = join(workspaceRoot, ".mcp.json");
  if (existsSync(mcpJsonPath)) {
    try {
      const raw = readFileSync(mcpJsonPath, "utf-8");
      const parsed = JSON.parse(raw);
      const servers = parsed.mcpServers;
      const vrekoEntry = servers?.vreko ?? servers?.["vreko-claude-code"];
      if (vrekoEntry && typeof vrekoEntry === "object") {
        const entry = vrekoEntry;
        if (entry.command && !isCommandExecutable2(entry.command)) {
          checks.push({
            id: "mcp.mcp-json.command",
            group: "mcp",
            label: ".mcp.json command",
            status: "fail",
            detail: `Command not executable: ${entry.command}`,
            fix: "Run: vr claude-sync --force",
            fixCommand: "vreko claude-sync --force"
          });
        } else {
          checks.push({
            id: "mcp.mcp-json.valid",
            group: "mcp",
            label: ".mcp.json",
            status: "pass",
            detail: "Valid Vreko entry found"
          });
        }
      } else {
        checks.push({
          id: "mcp.mcp-json.missing-vreko",
          group: "mcp",
          label: ".mcp.json",
          status: "warn",
          detail: "File exists but no Vreko entry",
          fix: "Run: vr claude-sync",
          fixCommand: "vreko claude-sync"
        });
      }
    } catch {
      checks.push({
        id: "mcp.mcp-json.parse",
        group: "mcp",
        label: ".mcp.json",
        status: "fail",
        detail: "File exists but is not valid JSON",
        fix: "Delete and regenerate: vr claude-sync --force",
        fixCommand: "vreko claude-sync --force"
      });
    }
  }
  const detection = detectAIClients();
  if (detection.clients.length === 0) {
    checks.push({
      id: "mcp.detection",
      group: "mcp",
      label: "AI client detection",
      status: "warn",
      detail: "No AI clients found in standard locations"
    });
    return checks;
  }
  const byName = /* @__PURE__ */ new Map();
  for (const client of detection.clients) {
    const existing = byName.get(client.name) ?? [];
    existing.push(client);
    byName.set(client.name, existing);
  }
  for (const [, clientGroup] of byName) {
    const client = clientGroup.reduce((a, b) => {
      if (a.hasVreko) {
        return a;
      }
      if (b.hasVreko) {
        return b;
      }
      if (a.exists) {
        return a;
      }
      return b;
    });
    if (!client.exists) {
      checks.push({
        id: `mcp.${client.name}.installed`,
        group: "mcp",
        label: `${client.displayName}`,
        status: "skip",
        detail: "Not installed"
      });
      continue;
    }
    if (!client.hasVreko) {
      checks.push({
        id: `mcp.${client.name}.configured`,
        group: "mcp",
        label: `${client.displayName}`,
        status: "warn",
        detail: "Installed but Vreko not configured",
        fix: `Configure: vreko tools configure --${client.name}`,
        fixCommand: `vreko tools configure --${client.name} --yes`
      });
      continue;
    }
    const validation = validateClientConfig(client);
    if (validation.valid) {
      checks.push({
        id: `mcp.${client.name}.valid`,
        group: "mcp",
        label: `${client.displayName}`,
        status: "pass",
        detail: "Configured, config valid"
      });
      const probeResult = await probeMCPProcess(client);
      if (probeResult) {
        checks.push(probeResult);
      }
      const serverConfig = (() => {
        const rawCfg = readClientConfig(client);
        if (!rawCfg?.mcpServers) {
          return null;
        }
        const key = getServerKey(client.format);
        return rawCfg.mcpServers[key] ?? rawCfg.mcpServers.vreko ?? null;
      })();
      if (serverConfig?.command) {
        try {
          const manager = getWorkspaceSessionManager();
          const workspacePath = findWorkspaceRoot(process.cwd()) ?? process.cwd();
          const session = await manager.getSession(workspacePath);
          if (session?.isValid) {
            checks.push({
              id: `mcp.${client.name}.auth`,
              group: "mcp",
              label: `${client.displayName} session auth`,
              status: "pass",
              detail: `Authenticated as ${session.user.email} (${session.user.tier})`
            });
          } else if (session && !session.isValid) {
            checks.push({
              id: `mcp.${client.name}.auth`,
              group: "mcp",
              label: `${client.displayName} session auth`,
              status: "warn",
              detail: `Session for ${session.user.email} is expired  -  Pro tools unavailable until re-auth`,
              fix: "Re-authenticate: vr login",
              fixCommand: "vr login"
            });
          } else {
            checks.push({
              id: `mcp.${client.name}.auth`,
              group: "mcp",
              label: `${client.displayName} session auth`,
              status: "warn",
              detail: "No session  -  running in free tier (local tools only). Run 'vr login' to access Pro features.",
              fix: "Authenticate: vr login",
              fixCommand: "vr login"
            });
          }
        } catch {
        }
      }
    } else {
      const errors = validation.issues.filter((i) => i.severity === "error");
      const warnings = validation.issues.filter((i) => i.severity === "warning");
      checks.push({
        id: `mcp.${client.name}.valid`,
        group: "mcp",
        label: `${client.displayName}`,
        status: errors.length > 0 ? "fail" : "warn",
        detail: [
          ...errors,
          ...warnings
        ].map((i) => i.message).join("; "),
        fix: `Repair: vreko tools configure --${client.name} --force`,
        fixCommand: `vreko tools configure --${client.name} --force --yes`
      });
    }
  }
  return checks;
}
__name(checkMCP, "checkMCP");
async function probeMCPProcess(client) {
  const rawConfig = readClientConfig(client);
  if (!rawConfig?.mcpServers) {
    return null;
  }
  const serverKey = getServerKey(client.format);
  const serverConfig = rawConfig.mcpServers[serverKey] ?? rawConfig.mcpServers.vreko;
  if (!serverConfig) {
    return null;
  }
  if (!serverConfig.command) {
    return {
      id: `mcp.${client.name}.connectivity`,
      group: "mcp",
      label: `${client.displayName} connectivity`,
      status: "skip",
      detail: "HTTP transport (remote server  -  not probed locally)"
    };
  }
  const HANDSHAKE_TIMEOUT_MS = 5e3;
  return new Promise((resolve11) => {
    let settled = false;
    let stderrLog = "";
    const settle = /* @__PURE__ */ __name((result7) => {
      if (settled) {
        return;
      }
      settled = true;
      clearTimeout(timer);
      try {
        proc.kill("SIGTERM");
      } catch {
      }
      resolve11(result7);
    }, "settle");
    let proc;
    try {
      proc = spawn(serverConfig.command, serverConfig.args ?? [], {
        stdio: [
          "pipe",
          "pipe",
          "pipe"
        ],
        env: {
          ...process.env,
          ...serverConfig.env ?? {}
        }
      });
    } catch (spawnErr) {
      return resolve11({
        id: `mcp.${client.name}.connectivity`,
        group: "mcp",
        label: `${client.displayName} server starts`,
        status: "fail",
        detail: `spawn error: ${spawnErr instanceof Error ? spawnErr.message : String(spawnErr)}`,
        fix: `Repair config: vr mcp repair --client ${client.name}`,
        fixCommand: `vreko mcp repair --client ${client.name} --force`
      });
    }
    proc.stderr?.on("data", (chunk) => {
      stderrLog += chunk.toString().slice(0, 500);
    });
    const initRequest = JSON.stringify({
      jsonrpc: "2.0",
      id: 1,
      method: "initialize",
      params: {
        protocolVersion: "2024-11-05",
        capabilities: {},
        clientInfo: {
          name: "vreko-doctor",
          version: "1.0.0"
        }
      }
    });
    try {
      proc.stdin?.write(`${initRequest}
`);
      proc.stdin?.end();
    } catch {
    }
    let stdoutBuf = "";
    proc.stdout?.on("data", (chunk) => {
      stdoutBuf += chunk.toString();
      const lines = stdoutBuf.split("\n");
      stdoutBuf = lines.pop() ?? "";
      for (const line of lines) {
        const trimmed = line.trim();
        if (!trimmed) {
          continue;
        }
        try {
          const msg = JSON.parse(trimmed);
          if (msg.jsonrpc === "2.0" && msg.id === 1 && msg.result) {
            const result7 = msg.result;
            const serverInfo = result7.serverInfo;
            const serverName = serverInfo?.name ?? "unknown";
            const serverVersion = serverInfo?.version ?? "?";
            settle({
              id: `mcp.${client.name}.connectivity`,
              group: "mcp",
              label: `${client.displayName} server starts`,
              status: "pass",
              detail: `JSON-RPC handshake OK  -  ${serverName} v${serverVersion}`
            });
          } else if (msg.jsonrpc === "2.0" && msg.error) {
            const err6 = msg.error;
            settle({
              id: `mcp.${client.name}.connectivity`,
              group: "mcp",
              label: `${client.displayName} server starts`,
              status: "warn",
              detail: `Server running but initialize returned error: ${err6.message ?? String(err6.code)}`
            });
          }
        } catch {
        }
      }
    });
    const timer = setTimeout(() => {
      settle({
        id: `mcp.${client.name}.connectivity`,
        group: "mcp",
        label: `${client.displayName} server starts`,
        status: "warn",
        detail: `Server started but did not respond to initialize within ${HANDSHAKE_TIMEOUT_MS}ms`
      });
    }, HANDSHAKE_TIMEOUT_MS);
    proc.on("error", (err6) => {
      const isNotFound = err6.code === "ENOENT";
      settle({
        id: `mcp.${client.name}.connectivity`,
        group: "mcp",
        label: `${client.displayName} server starts`,
        status: "fail",
        detail: isNotFound ? `Binary not found: '${serverConfig.command}'  -  not in IDE process PATH. Use absolute path.` : err6.message,
        fix: isNotFound ? `Re-link with absolute path: vr mcp repair --client ${client.name} --force` : `Repair config: vr mcp repair --client ${client.name}`,
        fixCommand: `vreko mcp repair --client ${client.name} --force`
      });
    });
    proc.on("close", (code) => {
      if (settled) {
        return;
      }
      const hint = stderrLog ? ` stderr: ${stderrLog.split("\n")[0]}` : "";
      if (code !== 0 && code !== null) {
        settle({
          id: `mcp.${client.name}.connectivity`,
          group: "mcp",
          label: `${client.displayName} server starts`,
          status: "fail",
          detail: `Process crashed on startup (exit code ${code})  -  check service is running.${hint}`,
          fix: `Start service then repair: vr service start && vr mcp repair --client ${client.name}`,
          fixCommand: "vreko service start"
        });
      } else {
        settle({
          id: `mcp.${client.name}.connectivity`,
          group: "mcp",
          label: `${client.displayName} server starts`,
          status: "warn",
          detail: `Process exited cleanly (code 0) without responding to initialize.${hint}`
        });
      }
    });
  });
}
__name(probeMCPProcess, "probeMCPProcess");
async function checkNetwork() {
  const checks = [];
  try {
    const start = Date.now();
    const response = await fetch("https://api.vreko.dev/health", {
      signal: AbortSignal.timeout(5e3)
    });
    const latency = Date.now() - start;
    checks.push({
      id: "network.api",
      group: "network",
      label: "api.vreko.dev reachable",
      status: response.ok ? "pass" : "warn",
      detail: `${response.status} (${latency}ms)`
    });
  } catch {
    checks.push({
      id: "network.api",
      group: "network",
      label: "api.vreko.dev reachable",
      status: "warn",
      detail: "Unreachable (Pro features require network)",
      fix: "Check internet connection or firewall settings"
    });
  }
  const apiKey = process.env.VREKO_API_KEY;
  if (apiKey) {
    if (apiKey.startsWith("sk-") && apiKey.length > 10) {
      checks.push({
        id: "network.apiKey",
        group: "network",
        label: "API key configured",
        status: "pass",
        detail: `sk-...${apiKey.slice(-4)}`
      });
    } else {
      checks.push({
        id: "network.apiKey",
        group: "network",
        label: "API key configured",
        status: "warn",
        detail: "Key format looks invalid",
        fix: "Check your API key at https://console.vreko.dev/app/settings/api-keys"
      });
    }
  } else {
    checks.push({
      id: "network.apiKey",
      group: "network",
      label: "API key configured",
      status: "warn",
      detail: "No API key (Pro features unavailable)",
      fix: "Set via: export VREKO_API_KEY=sk-..."
    });
  }
  return checks;
}
__name(checkNetwork, "checkNetwork");
function isTokenExpired(expiresAt) {
  if (!expiresAt) {
    return false;
  }
  const expMs = typeof expiresAt === "number" ? expiresAt < 1e12 ? expiresAt * 1e3 : expiresAt : new Date(expiresAt).getTime();
  return Number.isFinite(expMs) && expMs < Date.now();
}
__name(isTokenExpired, "isTokenExpired");
function readAuthCheck(authPath) {
  try {
    const authData = JSON.parse(readFileSync(authPath, "utf-8"));
    if (!(authData.token ?? authData.accessToken)) {
      return {
        id: "auth.token",
        group: "auth",
        label: "Auth token present",
        status: "fail",
        detail: "auth.json exists but contains no token",
        fix: "Re-authenticate: vr login",
        fixCommand: "vr login"
      };
    }
    const expired = isTokenExpired(authData.expiresAt);
    const userEmail = authData.user?.email ?? "unknown";
    const userTier = authData.user?.tier ?? "unknown";
    return expired ? {
      id: "auth.token",
      group: "auth",
      label: "Auth token valid",
      status: "fail",
      detail: `Token expired (${userEmail})  -  Pro features unavailable`,
      fix: "Re-authenticate: vr login",
      fixCommand: "vr login"
    } : {
      id: "auth.token",
      group: "auth",
      label: "Auth token valid",
      status: "pass",
      detail: `Authenticated as ${userEmail} (${userTier})`
    };
  } catch {
    return {
      id: "auth.token",
      group: "auth",
      label: "Auth token valid",
      status: "warn",
      detail: "auth.json unreadable or corrupt",
      fix: "Re-authenticate: vr login",
      fixCommand: "vr login"
    };
  }
}
__name(readAuthCheck, "readAuthCheck");
async function checkAuth() {
  const authPath = join(homedir(), ".vreko", "auth.json");
  if (!existsSync(authPath)) {
    return [
      {
        id: "auth.token",
        group: "auth",
        label: "Auth token present",
        status: "warn",
        detail: "No auth token  -  running in free tier (local tools only)",
        fix: "Authenticate: vr login",
        fixCommand: "vr login"
      }
    ];
  }
  return [
    readAuthCheck(authPath)
  ];
}
__name(checkAuth, "checkAuth");
async function checkExtension() {
  const checks = [];
  const home = homedir();
  const extensionPaths = [
    join(home, ".vscode", "extensions"),
    join(home, ".vscode-insiders", "extensions"),
    join(home, ".qoder", "extensions"),
    // macOS alternate
    join(home, ".cursor", "extensions")
  ];
  let found = false;
  let extensionVersion = null;
  for (const extDir of extensionPaths) {
    if (!existsSync(extDir)) {
      continue;
    }
    try {
      const entries = execSync(`ls "${extDir}" 2>/dev/null`, {
        encoding: "utf-8"
      }).split("\n").filter((e) => e.includes("vreko"));
      if (entries.length > 0) {
        found = true;
        const match = entries[0].match(/(\d+\.\d+\.\d+)/);
        if (match) {
          extensionVersion = match[1];
        }
        break;
      }
    } catch {
    }
  }
  if (found) {
    checks.push({
      id: "extension.installed",
      group: "extension",
      label: "VS Code extension detected",
      status: "pass",
      detail: extensionVersion ? `v${extensionVersion}` : "Version unknown"
    });
  } else {
    checks.push({
      id: "extension.installed",
      group: "extension",
      label: "VS Code extension",
      status: "warn",
      detail: "Not detected (checked VS Code, Cursor, Qoder extension dirs)",
      fix: "Install from marketplace: ext install vreko.vreko"
    });
  }
  try {
    if (isDaemonConnected()) {
      const client = getDaemonClient();
      const status = await client.daemon.status();
      const clients2 = status?.connections ?? 0;
      if (clients2 > 0) {
        checks.push({
          id: "extension.connected",
          group: "extension",
          label: "Extension connected to service",
          status: "pass",
          detail: `${clients2} client(s) connected`
        });
      } else {
        checks.push({
          id: "extension.connected",
          group: "extension",
          label: "Extension connected to service",
          status: "warn",
          detail: "No clients connected (extension may not be active)"
        });
      }
    }
  } catch {
  }
  return checks;
}
__name(checkExtension, "checkExtension");
function printGroup(_group, checks, quiet) {
  if (quiet && checks.every((c) => c.status === "pass" || c.status === "skip")) {
    return;
  }
  for (const check of checks) {
    if (quiet && (check.status === "pass" || check.status === "skip")) {
      continue;
    }
    const icon = statusIcon(check.status);
    const color = statusColor(check.status);
    const detail = check.detail ? chalk2.gray(` (${check.detail})`) : "";
    console.log(`  ${color(icon)} ${check.label}${detail}`);
    if (check.fix && check.status !== "pass") {
      console.log(`    Fix: ${check.fix}`);
    }
  }
}
__name(printGroup, "printGroup");
function statusIcon(status) {
  switch (status) {
    case "pass":
      return "\u2714";
    case "warn":
      return "\u26A0";
    case "fail":
      return "\u2716";
    case "skip":
      return "\u25CB";
  }
}
__name(statusIcon, "statusIcon");
function statusColor(status) {
  switch (status) {
    case "pass":
      return chalk2.green;
    case "warn":
      return chalk2.yellow;
    case "fail":
      return chalk2.red;
    case "skip":
      return chalk2.gray;
  }
}
__name(statusColor, "statusColor");
function isProcessAlive(pid) {
  try {
    process.kill(pid, 0);
    return true;
  } catch {
    return false;
  }
}
__name(isProcessAlive, "isProcessAlive");
function formatUptime(seconds) {
  if (seconds < 60) {
    return `${Math.floor(seconds)}s`;
  }
  if (seconds < 3600) {
    return `${Math.floor(seconds / 60)}m`;
  }
  const h = Math.floor(seconds / 3600);
  const m = Math.floor(seconds % 3600 / 60);
  return `${h}h ${m}m`;
}
__name(formatUptime, "formatUptime");
function formatAge2(ms) {
  const minutes = Math.floor(ms / 6e4);
  if (minutes < 60) {
    return `${minutes}m`;
  }
  const hours = Math.floor(minutes / 60);
  if (hours < 24) {
    return `${hours}h`;
  }
  return `${Math.floor(hours / 24)}d`;
}
__name(formatAge2, "formatAge");
function platformLabel() {
  const os2 = platform();
  const a = arch();
  const labels = {
    darwin: "macOS",
    linux: "Linux",
    win32: "Windows"
  };
  return `${labels[os2] || os2} ${a}`;
}
__name(platformLabel, "platformLabel");
function skipGroup(group2, reason) {
  return Promise.resolve([
    {
      id: `${group2}.skipped`,
      group: group2,
      label: group2,
      status: "skip",
      detail: reason
    }
  ]);
}
__name(skipGroup, "skipGroup");
async function handleSyncConfig() {
  console.log(chalk2.blue("Syncing fingerprint config from cloud..."));
  console.log(chalk2.yellow("\u25CB Fingerprint sync unavailable  -  handler not registered in daemon"));
}
__name(handleSyncConfig, "handleSyncConfig");

// src/output/JsonRenderer.ts
var SCHEMA_VERSION = "1.0.0";
function renderJson(kind, data) {
  const output = {
    schemaVersion: SCHEMA_VERSION,
    kind,
    data
  };
  process.stdout.write(`${JSON.stringify(output)}
`);
}
__name(renderJson, "renderJson");
function renderJsonError(code, message, quiet = false) {
  const data = {
    code,
    message
  };
  renderJson("error", data);
  if (!quiet) {
    process.stderr.write(`[vreko] error: ${message}
`);
  }
}
__name(renderJsonError, "renderJsonError");

// src/output/OutputMode.ts
function detectOutputMode(opts = {}) {
  if (opts.json) return "json";
  if (process.env.VREKO_JSON === "1") return "json";
  if (!process.stdout.isTTY) return "json";
  return "tty";
}
__name(detectOutputMode, "detectOutputMode");

// src/commands/intel.ts
function classifyError(err6) {
  const msg = String(err6);
  if (msg.includes("ENOENT") || msg.includes("connect") || msg.includes("daemon")) return 3;
  if (msg.includes("auth") || msg.includes("workspace")) return 4;
  return 1;
}
__name(classifyError, "classifyError");
function registerIntelCommand(program) {
  const intel = program.command("intel", {
    hidden: true
  }).description("Access workspace intelligence data");
  intel.command("snapshot").description("Get the full IntelligenceSnapshot for the current workspace").option("--workspace <path>", "Target workspace path (default: cwd)", process.cwd()).option("--json", "Emit machine-readable JSON output").option("--quiet", "Suppress stderr in JSON mode").action(async (opts) => {
    const mode = detectOutputMode(opts);
    const quiet = opts.quiet ?? false;
    try {
      await connectToDaemon();
      const client = getDaemonClient();
      if (!client) {
        if (mode === "json") {
          renderJsonError(3, "daemon unavailable", quiet);
        } else {
          console.error("Error: daemon unavailable  -  run `vr daemon start`");
        }
        process.exit(3);
      }
      const snap = await client.intelligence.snapshot({
        workspace: opts.workspace
      });
      if (mode === "json") {
        renderJson("intel", snap);
        process.exit(0);
      } else {
        console.log(JSON.stringify(snap, null, 2));
        process.exit(0);
      }
    } catch (err6) {
      const code = classifyError(err6);
      if (mode === "json") {
        renderJsonError(code, String(err6), quiet);
      } else {
        console.error("Error:", String(err6));
      }
      process.exit(code);
    }
  });
}
__name(registerIntelCommand, "registerIntelCommand");
function createUndoCommand() {
  const cmd = new Command("undo").description("Undo the last destructive operation").option("--list", "List recent undoable operations").action(async (options) => {
    if (options.list) {
      const operations = getRecentOperations(10);
      if (operations.length === 0) {
        console.log("No recent operations to undo");
        return;
      }
      for (let i = 0; i < operations.length; i++) {
        const op = operations[i];
        const status = op.canUndo ? chalk2.green("\u25CF") : chalk2.gray("\u25CB");
        const time = new Date(op.timestamp).toLocaleString();
        console.log(`  ${status} [${i + 1}] ${op.description}  -  ${time}`);
      }
      return;
    }
    await undoLastOperation();
  });
  return cmd;
}
__name(createUndoCommand, "createUndoCommand");
var execAsync4 = promisify(exec);
var PACKAGE_NAME = "@vreko/cli";
var NPM_REGISTRY = "https://registry.npmjs.org";
function createUpgradeCommand() {
  return new Command("upgrade").description("Check for and install CLI updates").option("--check", "Only check for updates, don't install").option("--force", "Force reinstall even if up to date").option("--canary", "Install latest canary/pre-release version").action(async (options) => {
    try {
      const spinner2 = ora("Checking for updates...").start();
      const versionInfo = await getVersionInfo(options.canary);
      spinner2.stop();
      if (!versionInfo.updateAvailable && !options.force) {
        console.log(`\u2713 Already up to date (${versionInfo.current})`);
        return;
      }
      if (options.check) {
        if (versionInfo.updateAvailable) {
          console.log(`Update available: ${versionInfo.current} \u2192 ${versionInfo.latest}`);
          console.log("Run: vr upgrade");
        }
        return;
      }
      if (versionInfo.updateAvailable) {
        console.log(`Upgrading ${versionInfo.current} \u2192 ${versionInfo.latest}...`);
      } else {
        console.log(`Reinstalling ${versionInfo.current}...`);
      }
      await performUpgrade(options.canary);
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error(`\u2717 Upgrade failed: ${message}`);
      process.exit(1);
    }
  });
}
__name(createUpgradeCommand, "createUpgradeCommand");
async function getVersionInfo(canary = false) {
  const current = "3.1.6";
  let latest = current;
  try {
    const tag = canary ? "canary" : "latest";
    const response = await fetch(`${NPM_REGISTRY}/${PACKAGE_NAME}/${tag}`);
    if (response.ok) {
      const data = await response.json();
      latest = data.version;
    } else {
      const { stdout } = await execAsync4(`npm view ${PACKAGE_NAME}${canary ? "@canary" : ""} version`);
      latest = stdout.trim();
    }
  } catch {
  }
  return {
    current,
    latest,
    updateAvailable: compareVersions(latest, current) > 0
  };
}
__name(getVersionInfo, "getVersionInfo");
function compareVersions(a, b) {
  const partsA = a.replace(/^v/, "").split(".").map(Number);
  const partsB = b.replace(/^v/, "").split(".").map(Number);
  for (let i = 0; i < 3; i++) {
    const partA = partsA[i] || 0;
    const partB = partsB[i] || 0;
    if (partA > partB) {
      return 1;
    }
    if (partA < partB) {
      return -1;
    }
  }
  return 0;
}
__name(compareVersions, "compareVersions");
async function performUpgrade(canary = false) {
  const spinner2 = ora("Installing update...").start();
  try {
    const packageManager = await detectPackageManager();
    const tag = canary ? "canary" : "latest";
    let command;
    switch (packageManager) {
      case "pnpm":
        command = `pnpm add -g ${PACKAGE_NAME}@${tag}`;
        break;
      case "yarn":
        command = `yarn global add ${PACKAGE_NAME}@${tag}`;
        break;
      case "bun":
        command = `bun add -g ${PACKAGE_NAME}@${tag}`;
        break;
      default:
        command = `npm install -g ${PACKAGE_NAME}@${tag}`;
    }
    spinner2.text = `Running: ${command}`;
    const { stderr } = await execAsync4(command);
    if (stderr?.includes("ERR!")) {
      throw new Error(stderr);
    }
    spinner2.succeed("Update installed");
  } catch (error) {
    spinner2.fail("Installation failed");
    throw error;
  }
}
__name(performUpgrade, "performUpgrade");
async function detectPackageManager() {
  try {
    await execAsync4("pnpm --version");
    const { stdout } = await execAsync4("pnpm list -g @vreko/cli 2>/dev/null || true");
    if (stdout.includes("@vreko/cli")) {
      return "pnpm";
    }
  } catch {
  }
  try {
    await execAsync4("yarn --version");
    const { stdout } = await execAsync4("yarn global list 2>/dev/null || true");
    if (stdout.includes("@vreko/cli")) {
      return "yarn";
    }
  } catch {
  }
  try {
    await execAsync4("bun --version");
    return "bun";
  } catch {
  }
  return "npm";
}
__name(detectPackageManager, "detectPackageManager");
function registerDaemonCommands(program) {
  const daemon = program.command("daemon", {
    hidden: true
  }).description("Manage \u{1F98E} Vreko service");
  daemon.command("start").description("Start the \u{1F98E} Vreko service").option("--no-detach", "Run service in foreground (attached to terminal)").option("-f, --foreground", "Same as --no-detach").option("-t, --idle-timeout <minutes>", "Shutdown after idle (default: 240)", "240").action(async (options) => {
    if (await isServiceHealthy()) {
      const pid = readServicePid();
      console.log(`Service is already running (PID: ${pid})`);
      return;
    }
    const vrekodArgs = [
      "--idle-timeout",
      options.idleTimeout
    ];
    let vrekodBin;
    try {
      const resolved = await import.meta.resolve("@vreko/local-service/dist/main.js");
      vrekodBin = resolved.replace("file://", "");
    } catch {
      if (__require?.resolve) {
        vrekodBin = __require.resolve("@vreko/local-service/dist/main.js");
      } else {
        const { fileURLToPath: fileURLToPath5 } = await import('url');
        const { dirname: dirname11, join: join28 } = await import('path');
        const __dirname2 = dirname11(fileURLToPath5(import.meta.url));
        vrekodBin = join28(__dirname2, "../../local-service/dist/main.js");
      }
    }
    if (options.detach && !options.foreground) {
      const pid = await startDaemonDetached({
        idleTimeout: options.idleTimeout,
        maxWaitMs: 5e3
      });
      console.log(`\u2713 Service started (PID: ${pid ?? readServicePid()})`);
      return;
    }
    const child = spawn(process.execPath, [
      vrekodBin,
      ...vrekodArgs
    ], {
      stdio: "inherit"
    });
    await new Promise((resolve11, reject) => {
      child.on("exit", (code) => {
        if (code === 0) {
          resolve11();
        } else {
          reject(new Error(`Service exited with code ${code}`));
        }
      });
      child.on("error", reject);
    });
  });
  daemon.command("stop").description("Stop the \u{1F98E} Vreko service").action(async () => {
    if (!await isServiceHealthy()) {
      console.log("Service is not running");
      return;
    }
    console.log("Stopping service...");
    const client = createServiceClient();
    try {
      await connectServiceClient(client);
      await client.daemon.shutdown();
      console.log("\u2713 Service stopped");
    } catch (_err) {
      try {
        const pid = readServicePid();
        if (pid) {
          process.kill(pid, "SIGTERM");
          console.log("\u2713 Service stopped (SIGTERM)");
        }
      } catch (killErr) {
        console.error("\u2717 Failed to stop service:", killErr instanceof Error ? killErr.message : String(killErr));
        process.exitCode = 1;
      }
    } finally {
      client.close();
    }
  });
  daemon.command("status").description("Show service status").option("-j, --json", "Output as JSON").action(async (options) => {
    if (!await isServiceHealthy()) {
      if (options.json) {
        console.log(JSON.stringify({
          running: false
        }));
      } else {
        console.log("Service is not running");
      }
      return;
    }
    const client = createServiceClient();
    try {
      await connectServiceClient(client);
      const status = await client.call("daemon/status", {});
      if (options.json) {
        console.log(JSON.stringify({
          running: true,
          ...status
        }));
      } else {
        console.log("Service Status:");
        console.log("  Status:       \u2713 Running");
        console.log(`  PID:          ${status.pid}`);
        console.log(`  Version:      ${status.version}`);
        console.log(`  Uptime:       ${formatDuration(status.uptime)}`);
        console.log(`  Started:      ${status.startedAt}`);
        console.log(`  Workspaces:   ${status.workspaces}`);
        console.log(`  Connections:  ${status.connections}`);
        console.log(`  Memory:       ${formatBytes(status.memoryUsage.heapUsed)}`);
        console.log(`  Socket:       ${getServiceSocketPath()}`);
      }
    } catch (_err) {
      if (options.json) {
        console.log(JSON.stringify({
          running: true,
          error: _err instanceof Error ? _err.message : String(_err)
        }));
      } else {
        console.error("\u2717 Failed to get status:", _err instanceof Error ? _err.message : String(_err));
      }
    } finally {
      client.close();
    }
  });
  daemon.command("restart").description("Restart the Vreko service").option("-t, --idle-timeout <minutes>", "Shutdown after idle (default: 15)", "15").action(async (options) => {
    if (await isServiceHealthy()) {
      const stopClient = createServiceClient();
      try {
        await connectServiceClient(stopClient);
        await stopClient.daemon.shutdown();
      } catch {
      } finally {
        stopClient.close();
      }
      const stopStart = Date.now();
      await new Promise((resolve11) => {
        const wait = /* @__PURE__ */ __name(async () => {
          if (!await isServiceHealthy() || Date.now() - stopStart > 3e3) {
            resolve11();
          } else {
            setTimeout(() => {
              void wait();
            }, 100);
          }
        }, "wait");
        setTimeout(() => {
          void wait();
        }, 100);
      });
    }
    let useDoppler = false;
    try {
      execSync("which doppler", {
        stdio: "ignore"
      });
      if (process.env.DOPPLER_TOKEN) {
        useDoppler = true;
      } else {
        try {
          execSync("doppler me", {
            stdio: "ignore"
          });
          useDoppler = true;
        } catch {
        }
      }
    } catch {
    }
    const logDir = join(homedir(), ".vreko", "daemon");
    const logPath = join(logDir, "daemon.log");
    let logFd;
    try {
      mkdirSync(logDir, {
        recursive: true
      });
      logFd = openSync(logPath, "a");
    } catch {
      logFd = openSync("/dev/null", "w");
    }
    const spawnArgs = useDoppler ? [
      "run",
      "--",
      process.execPath,
      process.argv[1],
      "daemon",
      "start",
      "--idle-timeout",
      options.idleTimeout
    ] : [
      process.argv[1],
      "daemon",
      "start",
      "--idle-timeout",
      options.idleTimeout
    ];
    const spawnCmd = useDoppler ? "doppler" : process.execPath;
    const child = spawn(spawnCmd, spawnArgs, {
      detached: true,
      stdio: [
        "ignore",
        logFd,
        logFd
      ]
    });
    child.unref();
    const maxWait = 5e3;
    const start = Date.now();
    await new Promise((resolve11, reject) => {
      const check = /* @__PURE__ */ __name(async () => {
        if (await isServiceHealthy()) {
          resolve11();
        } else if (Date.now() - start > maxWait) {
          reject(new Error("Service failed to restart within timeout"));
        } else {
          setTimeout(() => {
            void check();
          }, 100);
        }
      }, "check");
      setTimeout(() => {
        void check();
      }, 100);
    });
    console.log(`\u2713 Service restarted (PID: ${readServicePid()})`);
  });
  daemon.command("ping").description("Ping the service").action(async () => {
    if (!await isServiceHealthy()) {
      console.log("Service is not running");
      return;
    }
    const client = createServiceClient();
    try {
      const start = Date.now();
      await connectServiceClient(client);
      const result7 = await client.daemon.ping();
      const elapsed = Date.now() - start;
      console.log(`pong (${elapsed}ms, uptime: ${formatDuration(result7.uptime)})`);
    } catch (_err) {
      console.error("\u2717 Ping failed:", _err instanceof Error ? _err.message : String(_err));
    } finally {
      client.close();
    }
  });
  daemon.command("health").description("Show service and MCP health (proactive health check)").option("-w, --watch", "Continuously poll health (Ctrl+C to stop)").option("-i, --interval <seconds>", "Poll interval in seconds (default: 30)", "30").option("-j, --json", "Output as JSON").action(async (options) => {
    const intervalMs = Number.parseInt(options.interval, 10) * 1e3;
    const runCheck = /* @__PURE__ */ __name(async () => {
      const timestamp = (/* @__PURE__ */ new Date()).toISOString();
      const result7 = {
        timestamp,
        daemon: {
          status: "unknown"
        },
        mcp: []
      };
      if (!await isServiceHealthy()) {
        result7.daemon = {
          status: "down",
          error: "Not running (no PID or process dead)"
        };
      } else {
        const healthClient = createServiceClient();
        try {
          const t0 = Date.now();
          await connectServiceClient(healthClient);
          const ping = await healthClient.daemon.ping();
          const latencyMs = Date.now() - t0;
          const { readServicePid: readServicePid2 } = await import('./local-service-adapter-G7CJAIMC.js');
          result7.daemon = {
            status: latencyMs < 100 ? "healthy" : "degraded",
            pid: readServicePid2() ?? void 0,
            version: ping.version,
            uptime: formatDuration(ping.uptime),
            latencyMs
          };
        } catch (err6) {
          result7.daemon = {
            status: "unreachable",
            error: err6 instanceof Error ? err6.message : String(err6)
          };
        } finally {
          healthClient.close();
        }
      }
      try {
        const { detectAIClients: detectAIClients2, readClientConfig: readClientConfig2, getServerKey: getServerKey2 } = await import('./dist-5ZYKNNU3.js');
        const detection = detectAIClients2();
        const byName = /* @__PURE__ */ new Map();
        for (const client of detection.clients) {
          const existing = byName.get(client.name) ?? [];
          existing.push(client);
          byName.set(client.name, existing);
        }
        const dedupedClients = Array.from(byName.values()).map((group2) => group2.reduce((a, b) => {
          if (a.hasVreko) {
            return a;
          }
          if (b.hasVreko) {
            return b;
          }
          if (a.exists) {
            return a;
          }
          return b;
        }));
        for (const client of dedupedClients) {
          if (!client.hasVreko) {
            continue;
          }
          const rawConfig = readClientConfig2(client);
          if (!rawConfig?.mcpServers) {
            continue;
          }
          const serverKey = getServerKey2(client.format);
          const serverConfig = rawConfig.mcpServers[serverKey] ?? rawConfig.mcpServers.vreko;
          if (!serverConfig?.command) {
            result7.mcp.push({
              status: "http-transport",
              details: `${client.displayName}: remote HTTP (not locally probed)`
            });
            continue;
          }
          const cmd = serverConfig.command;
          try {
            execSync(`which ${JSON.stringify(cmd)} 2>/dev/null || command -v ${JSON.stringify(cmd)}`, {
              stdio: "ignore"
            });
            result7.mcp.push({
              status: "configured",
              details: `${client.displayName}: command '${cmd}' found`
            });
          } catch {
            result7.mcp.push({
              status: "broken",
              details: `${client.displayName}: command '${cmd}' not found in PATH`
            });
          }
        }
      } catch (err6) {
        result7.mcp.push({
          status: "error",
          details: `Could not inspect MCP config: ${err6 instanceof Error ? err6.message : String(err6)}`
        });
      }
      if (options.json) {
        console.log(JSON.stringify(result7, null, 2));
        return;
      }
      const daemonIcon = result7.daemon.status === "healthy" ? "\u2705" : result7.daemon.status === "degraded" ? "\u26A0\uFE0F" : "\u274C";
      if (options.watch) {
        process.stdout.write(`\r\x1B[K[${timestamp.slice(11, 19)}] `);
      }
      console.log(`${daemonIcon} Service: ${result7.daemon.status}` + (result7.daemon.pid ? ` (PID: ${result7.daemon.pid})` : "") + (result7.daemon.latencyMs !== void 0 ? ` ${result7.daemon.latencyMs}ms` : "") + (result7.daemon.error ? `  -  ${result7.daemon.error}` : ""));
      if (result7.daemon.version) {
        console.log(`   Version: ${result7.daemon.version}  Uptime: ${result7.daemon.uptime}`);
      }
      for (const mcp of result7.mcp) {
        const mcpIcon = mcp.status === "configured" ? "\u2705" : mcp.status === "http-transport" ? "\u2139\uFE0F" : "\u274C";
        console.log(`${mcpIcon} MCP: ${mcp.details}`);
      }
      if (result7.mcp.length === 0) {
        console.log("\u2139\uFE0F  No MCP clients configured");
      }
    }, "runCheck");
    if (options.watch) {
      await runCheck();
      const interval = setInterval(runCheck, intervalMs);
      process.on("SIGINT", () => {
        clearInterval(interval);
        process.exit(0);
      });
      await new Promise(() => {
      });
    } else {
      await runCheck();
    }
  });
}
__name(registerDaemonCommands, "registerDaemonCommands");

// src/services/analytics.ts
var ANALYTICS_ENABLED = process.env.VREKO_ANALYTICS !== "false";
var API_URL2 = process.env.VREKO_API_URL || "https://api.vreko.dev";
process.env.VREKO_DEBUG === "true";
async function captureEvent(eventName, properties) {
  if (!ANALYTICS_ENABLED) {
    return;
  }
  try {
    fetch(`${API_URL2}/v1/analytics/events`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        event: eventName,
        properties: {
          ...properties,
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        }
      })
    }).catch(() => {
    });
  } catch {
  }
}
__name(captureEvent, "captureEvent");
var DEFAULT_STATS = {
  snapshotsCreated: 0,
  restoresPerformed: 0,
  risksDetected: 0,
  filesAnalyzed: 0,
  highRiskAverted: 0,
  currentStreak: 0,
  longestStreak: 0,
  totalDaysActive: 0,
  sharesGenerated: 0,
  shareClicks: 0
};
var DEFAULT_PREFERENCES = {
  verbosity: "normal",
  colorScheme: "auto",
  showAchievements: true,
  showProgress: true,
  defaultSnapshot: {
    includeNodeModules: false,
    includeGitIgnored: false
  }
};
function createDefaultPioneerState() {
  return {
    pioneerNumber: generatePioneerNumber(),
    level: "bronze",
    totalPoints: 0,
    unlockedAchievements: [],
    stats: {
      ...DEFAULT_STATS
    },
    currentStreak: 0,
    lastActiveDate: null,
    createdAt: (/* @__PURE__ */ new Date()).toISOString(),
    updatedAt: (/* @__PURE__ */ new Date()).toISOString()
  };
}
__name(createDefaultPioneerState, "createDefaultPioneerState");
function generatePioneerNumber() {
  const base = Date.now() % 1e4;
  const rand = Math.floor(Math.random() * 1e3);
  return base + rand;
}
__name(generatePioneerNumber, "generatePioneerNumber");
var SCHEMA = {};
var StateManager = class StateManager2 {
  static {
    __name(this, "StateManager");
  }
  conf;
  cache = null;
  constructor() {
    this.conf = new Conf({
      projectName: "vreko",
      projectVersion: "1.0.0",
      // biome-ignore lint/suspicious/noExplicitAny: Conf<T> schema type is too narrow for Zod-inferred schema
      schema: SCHEMA,
      defaults: {
        version: 1,
        pioneer: createDefaultPioneerState(),
        preferences: DEFAULT_PREFERENCES
      },
      migrations: {}
    });
  }
  /**
  * Get Pioneer state
  */
  getPioneerState() {
    if (!this.cache) {
      this.cache = this.conf.store;
    }
    return {
      ...this.cache.pioneer
    };
  }
  /**
  * Save Pioneer state
  */
  savePioneerState(state) {
    state.updatedAt = (/* @__PURE__ */ new Date()).toISOString();
    this.conf.set("pioneer", state);
    if (this.cache) {
      this.cache.pioneer = state;
    }
  }
  /**
  * Get user preferences
  */
  getPreferences() {
    return {
      ...this.conf.get("preferences")
    };
  }
  /**
  * Update user preferences
  */
  setPreferences(prefs) {
    const current = this.getPreferences();
    this.conf.set("preferences", {
      ...current,
      ...prefs
    });
  }
  /**
  * Reset all state (for testing/debugging)
  */
  reset() {
    this.conf.clear();
    this.cache = null;
  }
  /**
  * Get storage file path (for debugging)
  */
  getPath() {
    return this.conf.path;
  }
  /**
  * Check if this is a fresh installation
  */
  isFirstRun() {
    const pioneer = this.getPioneerState();
    return pioneer.stats.snapshotsCreated === 0 && pioneer.stats.filesAnalyzed === 0 && pioneer.unlockedAchievements.length === 0;
  }
  /**
  * Export state for backup
  */
  export() {
    return JSON.parse(JSON.stringify(this.conf.store));
  }
  /**
  * Import state from backup
  */
  import(state) {
    if (state.version !== this.conf.get("version")) {
      throw new Error("State version mismatch");
    }
    this.conf.store = state;
    this.cache = null;
  }
};
var userState = new StateManager();
var LOGO_LARGE = `\u2588\u2588\u2557   \u2588\u2588\u2557\u2588\u2588\u2588\u2588\u2588\u2588\u2557 \u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2557\u2588\u2588\u2557  \u2588\u2588\u2557 \u2588\u2588\u2588\u2588\u2588\u2588\u2557
\u2588\u2588\u2551   \u2588\u2588\u2551\u2588\u2588\u2554\u2550\u2550\u2588\u2588\u2557\u2588\u2588\u2554\u2550\u2550\u2550\u2550\u255D\u2588\u2588\u2551 \u2588\u2588\u2554\u255D\u2588\u2588\u2554\u2550\u2550\u2550\u2588\u2588\u2557
\u2588\u2588\u2551   \u2588\u2588\u2551\u2588\u2588\u2588\u2588\u2588\u2588\u2554\u255D\u2588\u2588\u2588\u2588\u2588\u2557  \u2588\u2588\u2588\u2588\u2588\u2554\u255D \u2588\u2588\u2551   \u2588\u2588\u2551
\u255A\u2588\u2588\u2557 \u2588\u2588\u2554\u255D\u2588\u2588\u2554\u2550\u2550\u2588\u2588\u2557\u2588\u2588\u2554\u2550\u2550\u255D  \u2588\u2588\u2554\u2550\u2588\u2588\u2557 \u2588\u2588\u2551   \u2588\u2588\u2551
 \u255A\u2588\u2588\u2588\u2588\u2554\u255D \u2588\u2588\u2551  \u2588\u2588\u2551\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2557\u2588\u2588\u2551  \u2588\u2588\u2557\u255A\u2588\u2588\u2588\u2588\u2588\u2588\u2554\u255D
  \u255A\u2550\u2550\u2550\u255D  \u255A\u2550\u255D  \u255A\u2550\u255D\u255A\u2550\u2550\u2550\u2550\u2550\u2550\u255D\u255A\u2550\u255D  \u255A\u2550\u255D \u255A\u2550\u2550\u2550\u2550\u2550\u255D`;
var LOGO_COMPACT = `\u2566  \u2566 \u2566\u2550\u2557 \u2554\u2550\u2557 \u2566\u2554\u2550 \u2554\u2550\u2557
\u2566  \u2566 \u2560\u2566\u255D \u2551\u2563  \u2560\u2569\u2557 \u2551 \u2551
\u255A\u2550\u2550\u255D \u2569\u255A\u2550 \u255A\u2550\u255D \u2569 \u2569 \u255A\u2550\u255D`;
var LOGO_MINIMAL = `\u25B8 vreko`;
function getLogo(terminalWidth) {
  const width = terminalWidth ?? process.stdout.columns ?? 80;
  if (width >= 72) {
    return LOGO_LARGE;
  }
  if (width >= 50) {
    return LOGO_COMPACT;
  }
  return LOGO_MINIMAL;
}
__name(getLogo, "getLogo");
function displayBrandedHeader(options = {}) {
  const { version, showTagline = true, color = true } = options;
  const logo = getLogo();
  const coloredLogo = color ? chalk2.hex("#4ADE80")(logo) : logo;
  const lines = [
    coloredLogo
  ];
  if (showTagline) {
    lines.push("");
    lines.push(color ? `    ${chalk2.hex("#4ADE80")("\u25B0\u25CF\u25B8")}  ${chalk2.bold("developer intelligence")}` : "    \u25B0\u25CF\u25B8  developer intelligence");
  }
  if (version) {
    lines.push(color ? chalk2.gray(`    v${version}`) : `    v${version}`);
  }
  return lines.join("\n");
}
__name(displayBrandedHeader, "displayBrandedHeader");
function displayWelcomeMessage() {
  const logo = displayBrandedHeader({
    showTagline: true
  });
  const message = `
${logo}

${chalk2.bold("Welcome to Vreko")}

Vreko makes your codebase smarter with every AI session.
Learn patterns, prevent problems, restore in seconds.

${chalk2.hex("#4ADE80")("Quick Start:")}
  ${chalk2.gray("1.")} ${chalk2.hex("#4ADE80")("vr login")}      ${chalk2.gray("Connect your account")}
  ${chalk2.gray("2.")} ${chalk2.hex("#4ADE80")("vr init")}       ${chalk2.gray("Initialize workspace")}
  ${chalk2.gray("3.")} ${chalk2.hex("#4ADE80")("vr tools configure")} ${chalk2.gray("Set up AI tools")}

${chalk2.dim("Learn more: https://docs.vreko.dev")}
`;
  return message;
}
__name(displayWelcomeMessage, "displayWelcomeMessage");

// src/index.ts
Sentry.init(createSentryConfig({
  dsn: process.env.SENTRY_DSN_CLI || process.env.SENTRY_DSN || "",
  surface: "cli"
}));
async function createCLI() {
  const program = new Command();
  program.name("vreko").description("Intelligence-driven development  -  observe, learn, and surface warnings before the next mistake").version("3.1.6", "-v, --version", "Display version number").helpOption("-h, --help", "Display help for command").addOption(new Option("--verbose", "Enable verbose output").env("VREKO_VERBOSE").default(process.env.VREKO_VERBOSE ?? false)).addOption(new Option("-q, --quiet", "Suppress non-essential output").default(false)).addOption(new Option("--no-color", "Disable colored output").env("NO_COLOR").default(false)).addOption(new Option("--debug", "Enable debug mode with detailed logging").env("VREKO_DEBUG").default(process.env.VREKO_DEBUG ?? false)).addOption(new Option("--json", "Output in JSON format for LLM consumption").env("VREKO_JSON").default(process.env.VREKO_JSON ?? false)).addOption(new Option("--plain", "Disable TUI; output JSON for scripts and CI (same as VREKO_PLAIN=1)").env("VREKO_PLAIN").default(process.env.VREKO_PLAIN ?? false)).addOption(new Option("-y, --yes", "Skip confirmation prompts (for CI/automation)").default(false)).hook("preAction", (thisCommand) => {
    const opts = thisCommand.opts();
    cliState.verbose = opts.verbose || false;
    cliState.quiet = opts.quiet || false;
    cliState.debug = opts.debug || false;
    cliState.noColor = opts.color === false;
    cliState.json = opts.json || false;
    cliState.yes = opts.yes || false;
    if (opts.plain) {
      process.env.VREKO_PLAIN = "1";
    }
    cliState.renderMode = getRenderMode();
    if (process.env.FORCE_COLOR) {
      chalk2.level = Number(process.env.FORCE_COLOR) || 1;
    } else if (cliState.noColor || cliState.json) {
      chalk2.level = 0;
    }
    if (cliState.debug) {
      cliState.verbose = true;
    }
  }).addHelpText("after", `
Workflows:
  First time setup:  vreko init \u2192 vreko analyze \u2192 vreko sync \u2192 vreko metrics
  Daily protection:  vreko status \u2192 vreko check \u2192 vreko snapshot -m "before work"
  After AI session:  vreko validate --all \u2192 vreko learn \u2192 vreko patterns
  Debug/restore:     vreko doctor \u2192 vreko undo \u2192 vreko snapshot --list

Agent Workflows (--json flag enables structured output on all commands):
  Assess workspace:  vreko status --json && vreko metrics --json
  Before editing:    vreko context "<task>" --json
  Validate changes:  vreko validate --all --json
  Score a file:      vreko metrics <path> --json
  Run diagnostics:   vreko doctor --json

Common Examples:
  $ vreko init                Initialize Vreko in current directory (or: vr init)
  $ vreko status              Show workspace health and status (or: vr status)
  $ vreko check               Check staged files for risky changes (or: vr check)
  $ vreko snapshot -m "backup"  Create a named snapshot (or: vr snapshot)
  $ vreko sync && vreko metrics  Collect signals then show critical (or: vr sync)
  $ vreko doctor              Run diagnostic checks (or: vr doctor)

Environment Variables:
  VREKO_VERBOSE    Enable verbose output (same as --verbose)
  VREKO_DEBUG      Enable debug mode (same as --debug)
  VREKO_JSON       Output in JSON format for LLM consumption (same as --json)
  VREKO_PLAIN      Disable TUI, output JSON for scripts/CI (same as --plain)
  VREKO_API_URL    Override API endpoint (default: https://api.vreko.dev)
  NO_COLOR            Disable colored output (https://no-color.org)
  FORCE_COLOR         Force colored output even when piped (https://force-color.org)

Configuration:
  Global config:    ~/.vreko/config.json
  Workspace config: .vreko/config.json

Documentation:
  https://docs.vreko.dev
  https://github.com/vreko-dev/vreko-cli
  https://vreko.dev/llms.txt  (machine-readable docs for LLM agents)
`).enablePositionalOptions().passThroughOptions().configureHelp({
    helpWidth: 80,
    sortSubcommands: true,
    sortOptions: true
  }).showSuggestionAfterError().showHelpAfterError("(run vreko --help for available commands)");
  program.addCommand(createLoginCommand(), {
    hidden: true
  });
  program.addCommand(createLogoutCommand(), {
    hidden: true
  });
  program.addCommand(createSetKeyCommand(), {
    hidden: true
  });
  program.addCommand(createWhoamiCommand(), {
    hidden: true
  });
  program.addCommand(createWorkspacesCommand(), {
    hidden: true
  });
  program.addCommand(createInitCommand());
  program.addCommand(createPurgeCommand(), {
    hidden: true
  });
  program.addCommand(createOnboardCommand(), {
    hidden: true
  });
  program.addCommand(createClaudeSyncCommand(), {
    hidden: true
  });
  program.addCommand(createAnalyzeCommand(), {
    hidden: true
  });
  program.addCommand(createStatusCommand());
  program.addCommand(createFixCommand(), {
    hidden: true
  });
  program.addCommand(createToolsCommand(), {
    hidden: true
  });
  program.addCommand(mcpCommand, {
    hidden: true
  });
  program.addCommand(acpCommand, {
    hidden: true
  });
  program.addCommand(createProtectCommand(), {
    hidden: true
  });
  program.addCommand(createSessionCommand());
  program.addCommand(createSnapshotCommand());
  program.addCommand(createContextCommand(), {
    hidden: true
  });
  program.addCommand(createValidateCommand(), {
    hidden: true
  });
  program.addCommand(createStatsCommand(), {
    hidden: true
  });
  program.addCommand(createPulseCommand(), {
    hidden: true
  });
  program.addCommand(createProjectionsCommand(), {
    hidden: true
  });
  program.addCommand(createHooksCommand(), {
    hidden: true
  });
  registerIntelCommand(program);
  program.addCommand(createSyncCommand(), {
    hidden: true
  });
  program.addCommand(createMetricsCommand(), {
    hidden: true
  });
  program.addCommand(createRefreshCommand(), {
    hidden: true
  });
  program.addCommand(createLearnCommand(), {
    hidden: true
  });
  program.addCommand(createPatternsCommand(), {
    hidden: true
  });
  program.addCommand(createConsolidateCommand(), {
    hidden: true
  });
  program.addCommand(createCheckCommand(), {
    hidden: true
  });
  program.addCommand(createRiskAnalyzeCommand(), {
    hidden: true
  });
  program.addCommand(createWatchCommand(), {
    hidden: true
  });
  program.addCommand(createVrStartCommand());
  program.addCommand(createVrStopCommand());
  program.addCommand(createInteractiveCommand(), {
    hidden: true
  });
  program.addCommand(createConfigCommand(), {
    hidden: true
  });
  program.addCommand(createDoctorCommand(), {
    hidden: true
  });
  program.addCommand(createDiagnosticsCommand(), {
    hidden: true
  });
  program.addCommand(createUpgradeCommand(), {
    hidden: true
  });
  program.addCommand(createUndoCommand(), {
    hidden: true
  });
  program.addCommand(createAliasCommand(), {
    hidden: true
  });
  program.addCommand(createCompletionCommand(), {
    hidden: true
  });
  registerDaemonCommands(program);
  registerServiceCommands(program);
  registerBaselineCommands(program);
  program.on("command:*", (unknownCommand) => {
    const cmd = unknownCommand[0];
    displayUnknownCommandError(cmd);
    Sentry.captureException(new Error(`Unknown command: ${String(cmd).slice(0, 200)}`));
    void captureEvent("cli.unknown_command", {
      command: String(cmd).slice(0, 200)
    });
    process.exit(1);
  });
  return program;
}
__name(createCLI, "createCLI");
async function smartRouter() {
  if (process.argv.length > 2) {
    return false;
  }
  const cwd = process.cwd();
  try {
    const isFirstRun = userState.isFirstRun();
    const initialized = await isVrekoInitialized(cwd);
    const runSelf = /* @__PURE__ */ __name((args) => execFileSync(process.execPath, [
      process.argv[1],
      ...args
    ], {
      stdio: "inherit"
    }), "runSelf");
    if (!await isServiceHealthy()) {
      process.stdout.write(`${chalk2.gray("Starting service...")}
`);
      runSelf([
        "service",
        "start"
      ]);
    }
    if (isFirstRun) {
      runSelf([
        "init"
      ]);
    }
    if (!initialized) {
      runSelf([
        "init"
      ]);
    }
    const { launchTui } = await import('./tui-PFFYLIOW.js');
    await launchTui("dashboard");
    return true;
  } catch {
    return false;
  }
}
__name(smartRouter, "smartRouter");
function checkDeprecatedBinary() {
}
__name(checkDeprecatedBinary, "checkDeprecatedBinary");
function checkVersionMismatch() {
}
__name(checkVersionMismatch, "checkVersionMismatch");
var _argv1Real = (() => {
  try {
    return realpathSync(process.argv[1]);
  } catch {
    return process.argv[1];
  }
})();
if (_argv1Real === fileURLToPath(import.meta.url)) {
  (async () => {
    try {
      checkDeprecatedBinary();
      checkVersionMismatch();
      if (await smartRouter()) {
        process.exit(0);
      }
      const program = await createCLI();
      await program.parseAsync(process.argv);
    } catch (error) {
      Sentry.captureException(error instanceof Error ? error : new Error(String(error)));
      void captureEvent("cli.unhandled_error", {
        message: error instanceof Error ? error.message.slice(0, 500) : String(error).slice(0, 500)
      });
      displaySmartError(error instanceof Error ? error : String(error));
      await Sentry.close(2e3);
      process.exit(1);
    } finally {
      await Sentry.close(2e3);
    }
  })();
}
/**
 * @license Apache-2.0
 * @copyright SnapBack Contributors
 *
 * This file is part of SnapBack OSS.
 * Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
 *
 * Source: packages/contracts/src/features.ts
 * Generated: 2026-03-13T21:01:56.773Z
 */
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/types/branded.ts
* Generated: 2026-03-13T21:01:56.795Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/id-generator.ts
* Generated: 2026-03-13T21:01:56.773Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/logger.ts
* Generated: 2026-03-13T21:01:56.785Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/observability/InstrumentationProvider.ts
* Generated: 2026-03-13T21:01:56.786Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/observability/SemanticConventions.ts
* Generated: 2026-03-13T21:01:56.786Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/observability/types.ts
* Generated: 2026-03-13T21:01:56.787Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/observability/utils.ts
* Generated: 2026-03-13T21:01:56.787Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/observability/index.ts
* Generated: 2026-03-13T21:01:56.786Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/types/billing.ts
* Generated: 2026-03-13T21:01:56.794Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/types/result.ts
* Generated: 2026-03-13T21:01:56.796Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/workspace/workspace-resolution.ts
* Generated: 2026-03-13T21:01:56.798Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/session.ts
* Generated: 2026-03-13T21:01:56.793Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/session/file-modification.ts
* Generated: 2026-03-13T21:01:56.792Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/session/index.ts
* Generated: 2026-03-13T21:01:56.793Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/signals/index.ts
* Generated: 2026-03-13T21:01:56.793Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/sync-payload.ts
* Generated: 2026-03-13T21:01:56.794Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/types/analysis.ts
* Generated: 2026-03-13T21:01:56.794Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/types/config.ts
* Generated: 2026-03-13T21:01:56.795Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/types/learning.ts
* Generated: 2026-03-13T21:01:56.795Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/types/protection.ts
* Generated: 2026-03-13T21:01:56.796Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/types/protection-utils.ts
* Generated: 2026-03-13T21:01:56.796Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/schemas.ts
* Generated: 2026-03-13T21:01:56.791Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/types/snapshot.ts
* Generated: 2026-03-13T21:01:56.797Z
*/
/**
* @license Apache-2.0
* @copyright SnapBack Contributors
*
* This file is part of SnapBack OSS.
* Auto-generated from packages/contracts - DO NOT EDIT DIRECTLY.
*
* Source: packages/contracts/src/index.ts
* Generated: 2026-03-13T21:01:56.774Z
*/

export { createCLI, displayWelcomeMessage };
//# sourceMappingURL=index.js.map
//# sourceMappingURL=index.js.map